# 协作规范

> 本规范按控制论闭环组织：宪章设目标（设定值）→ 规则指导执行（控制律）
> → 测试/核对测量产出（传感）→ 偏离/返工纠正误差（纠偏）→ 交接/错误预防回流（反馈）。
> 与控制论无关的执行规范（Git/命名/构建）统一归控制律层，不硬贴传感/反馈标签。
> 跨文件引用本规范请用语义锚点（如「控制律层·Git」），勿用数字编号。

回答内容优先使用中文

## 〇、设定值层 Setpoint — 要对齐的目标

### 0.1 架构宪章
- 根目录 `NORTH_STAR.md` 是本项目 UI 系统的中心思想宪章，是所有架构决策、模块设计、性能优化和 API 取舍的最高准绳
- 做任何架构性改动前，必须先对照 `NORTH_STAR.md` 的《核心信条》《关键不变量》和《决策检查清单》，确认方案没有踩《反模式》
- 代码与宪章冲突时，遵循「先改文件再改代码」：要么调整方案对齐宪章，要么按宪章《修订纪律》在《偏离登记》显式登记一次偏离并说明理由、影响范围和回填计划；隐性偏离（不登记就绕过）不可接受
- 代码评审时，用《关键不变量》（I1-I13）逐条核对，任意一条被破坏都应阻断合并
- 信条（NORTH_STAR 第 3 节）和不变量（NORTH_STAR 第 5 节）本身的改动属于重大架构变更，必须经用户确认
- scene 新栈有一批「违反即阻断合并」硬约束（控件契约 R1-R13、宪章不变量 I1-I13、
  布局跨类同步契约、paint/node 铁律等），散落于源码 package-info、类头 Javadoc 与宪章。
  写/评审 scene 代码前必须先读硬约束总目录 `docs/设定值层/硬约束总目录.md` 并按其指针核对，不得在目录外私自绕过任何一条。

## 一、控制律层 Control Law — 如何达成目标

### 1.1 Git 提交规范
- 每次修改必须提交git，保持工作区整洁
- 提交信息应清晰描述更改内容
- 提交标题格式统一为：`[English]: 中文标题`
- 提交标题常用动作建议：`Add`、`Fix`、`Refactor`、`Docs`、`Style`、`Test`、`Chore`
- 提交正文必须与标题换行分隔，并使用 Markdown 记录详细修改内容
- 提交正文必须使用中文，优先说明修改范围、核心变更点、必要的兼容性或影响说明

### 1.2 分支管理规范
- 每次改动前先确认当前分支，改动不要在 `4.0` / `master` 等主分支上直接进行
- 应在合适的命名分支中开发，命名格式建议：`<动作名称>/<任务简述或功能名>`（动作名称例如 `fix`、`add`、`refactor` 等）
- 改动结束后，确认测试通过、工作区干净，再将分支合并回主分支
- 合并时必须使用 `git merge --no-ff`，保留显式合并提交，禁止 Fast-forward 直接移动主分支指针

### 1.3 代码规范
#### 命名规范
- 命名：类名大驼峰，方法名/变量名小驼峰，常量全大写下划线分隔

#### 注释规范
- 类与方法必须添加注释
- 使用中文注释，提高可读性
- 重要方法必须添加Javadoc注释，说明功能、参数和返回值
- 字段、成员变量、临时变量等位置按实际可读性需要选择性添加注释，不强制要求逐一注释
- 复杂逻辑添加行内注释，解释实现思路
- 注释应简洁明了，避免冗余

#### 其他规范
- 文件规模：单个代码文件接近或超过 1000 行时必须评估职责拆分；优先按真实职责、变更频率和复用边界拆分，不按行数机械拆
- 新增大型功能时避免继续向已有千行文件堆叠逻辑，应优先拆出 registry、builder、checker、state 等协作者

### 1.4 工具链与构建执行
- agent 的终端命令编排固定使用 Python 3；OpenCode 的 `cmd.exe` 只负责启动 Python，不承载业务命令、管道、重定向、变量插值或链式逻辑
- 通用外部命令使用 `python scripts/run-agent-command.py -- <executable> <args...>`；复杂或一次性逻辑先用专用文件编辑工具写成可审计 `.py` 文件，再以 `python <script.py>` 执行，禁止复杂 `python -c`
- Python 调用外部程序必须使用 `subprocess` 参数列表、`shell=False`、显式仓库 `cwd`、超时与返回码检查；禁止 `os.system`、命令字符串拼接和任何 `shell=True`
- 文件读写搜索继续优先使用专用工具，不用 Python 或终端绕过；agent 不编写或执行 PowerShell/CMD/Bash 命令串
- agent 不在本机执行 Gradle、编译、构建、测试、运行态或 verify 命令；必需实证交 GitHub Actions CI 或用户执行
- 缺少任务要求的 CI/用户结果时必须返回 `INCOMPLETE`，不得用静态检查冒充构建或测试通过
- 用户与 CI 的稳定入口见 `docs/控制律层/稳定命令.md`

### 1.5 环境所有权
- **环境所有权**：本机环境归用户，CI 环境归 runner；agent 只有只读验证权，无任何环境修复或赋值权。仅按任务逐项读取、验证所需的非敏感变量；敏感变量只检查存在性，禁止回显；禁止全量枚举环境变量。
- agent 禁止在当前会话、子进程及用户/系统级设置、覆盖、清空或删除环境变量，包括 PowerShell `$env:<变量>` 写入、CMD `set[x] <变量>`、POSIX `export <变量>`/`env <变量>=<值>`、注册表环境项、环境写入 API，以及通过 profile、`.env`、`gradle.properties` 持久修复。
- 禁止用 Gradle/JDK home 参数覆盖环境。环境缺失或异常时停止依赖命令，返回 `INCOMPLETE` 并询问用户。
- CI workflow 的声明式环境属 runner 所有权，不构成本地 agent 授权；本地 Gradle、`runClient*` 与 `runServer*` 均交用户。

### 1.6 默认 build 与持久任务
- OpenCode 使用内置默认 `build` 直接工作，不维护仓内自定义 agent；完整工作流见 `docs/控制律层/编排模式/PERSISTENT-WORKFLOW.md`
- 简单问答走 CHAT，不创建任务；需要跨轮设计时走 DESIGN，在 `.opencode/tasks/` 创建 `DRAFT`；目标、范围、写集和验收确认后转 `READY`，开始写盘时才转 `ACTIVE`
- 持久任务格式见 `docs/控制律层/编排模式/TASK-BRIEF.md`，索引为 `.opencode/tasks/INDEX.md`；影响后续动作的目标、决定、假设、状态、证据、阻断和唯一下一步必须在阶段边界写回，聊天上下文不得充当权威状态
- 同一 `ACTIVE` 任务只有一个所有者；默认 `build` 按写集实施、静态核对、对照验收与 Git diff 自审、提交并写回结果。产品方向、公共 API、依赖/版本、不可逆操作、发布、merge、push、密钥或授权仍由用户决定

## 二、传感层 Sensor — 如何测量产出是否达标

### 2.1 硬约束核对
- 宪章不变量 I1-I13、控件契约 R1-R13、布局跨类同步契约、paint/node 铁律等硬约束的指针与核对要求，统一见 `docs/设定值层/硬约束总目录.md`
- 写/评审 scene 代码前必读该总目录并按其指针逐条核对，不得在目录外私自绕过任何一条

### 2.2 测试体系
- 新建/评审 scene 测试前必读 `docs/传感层/测试体系约定.md`（分层总纲、L2 防错清单、断言库速查、输入两入口边界）；L2 纯数学边界靠评审纪律守卫，按其 §6 逐条核对

### 2.3 结构门禁
- 仓内结构检查边界见 `docs/传感层/门禁脚本说明.md`；现行 CI 只机械守卫零 PowerShell，其他 scene/docs 约束由权威目录、测试与对照验收的 diff 自审守卫
- 涉及 docs 改动后或合并前，按权威导航人工核对链接、时态、状态与写回归属

## 三、纠偏层 Actuator — 检测到误差如何纠正

### 3.1 返工纪律
- 自审发现验收偏差时回到任务规划，只修复已确认的误差点；完整流程见 `docs/控制律层/编排模式/PERSISTENT-WORKFLOW.md`
- 纠偏只改误差点，不擅自扩大改动面；发现需连带修改他处时报告回来，不越界

### 3.2 偏离登记
- 代码与宪章冲突时的「先改文件再改代码」与《修订纪律》《偏离登记》流程，权威源为 `NORTH_STAR.md`
- 隐性偏离（不登记就绕过）不可接受；登记须说明理由、影响范围和回填计划

### 3.3 控制器自身修改
- AGENTS.md 的修改必须经过用户确认
- 不可随意更改本文件内容
- 保持工作框架与宪章自洽所必需的修订必须由用户确认范围，由内置 `build` 按持久任务实施并自审，且不得改业务不变量。用户保留产品方向、不可逆 Git/发布/生产操作、密钥/认证/授权，以及 agent 的 model/variant/permission/mode/MCP/provider 等事项。

## 四、反馈层 Feedback — 误差如何回流修正系统

### 4.1 文档协作与写回
- 开始开发前必读 `docs/README.md`（唯一总导航，不承担事实汇总）
- 按变更内容写回对应层，优先覆盖/归并而非按日期追加：
  - 规则层 `AGENTS.md`：协作/编码/Git 规范与写回规则
  - 设定值层 `docs/设定值层/`：宪章指针、硬约束总目录、边界现状锚
  - 控制律层 `docs/控制律层/`：编排模式、稳定命令、项目约定、发布流程
  - 传感层 `docs/传感层/`：测试体系约定、门禁脚本说明
  - 反馈层 `docs/反馈层/`：错误预防、交接（业务状态权威源）、决策
  - 诊断层 `docs/诊断层/`：技术债、项目结构、Config 模块
- 只沉淀对后续协作有持续价值的事实；临时调试、一次性试验结论不单独留档
- 信息能稳定归属到某层时写回原文档，不复制进总导航
- 完成独立任务后至少检查一次上述各层是否需更新；会话结束前必须更新活动持久任务，业务状态变化再更新 `docs/反馈层/交接.md`
- 文档力求简约：指针文档只留分组名+链接不堆正文，架构层不混入「第 N 次会话」等时态编号，能压缩就压缩

### 4.2 交接业务状态权威源
- 业务状态、阻断项、待决策事项以 `docs/反馈层/交接.md` 为权威；实时 Git 分支、HEAD、工作区状态以 `git branch --show-current`、`git rev-parse HEAD`、`git status` 为权威
- 其他文档与交接中的业务状态冲突时，以交接.md 为准；反馈层各路由发现业务状态漂移时回写交接.md 而非另立状态
- 单任务工作记忆归 `.opencode/tasks/<id>.md`；聊天上下文只是易失缓存。任务形成的长期业务状态、决策或错误预防事实分别回流到交接.md/决策/错误预防，任务完成后作为 L3 情景档案保留

### 4.3 错误记录与上溯
- 开发中发现的错误、反复问题、重要教训必须及时记录，避免重复踩坑
- 索引文件 `docs/反馈层/错误预防.md` 是唯一入口：二级标题作指针，详情落 `ERROR-<日期>-<简述>.md`
- 每条至少含：现象、触发场景、根因、修复、预防
- **上溯规则**：同类误差在错误预防中第 2 次出现时，应上溯评估是否需补一条不变量（设定值层）或补一道门禁（传感层），而非仅再记一条通则；只在确认无更高层补丁可打时，才停留在通则层

### 4.4 决策回写
- 关键取舍、边界约束、重要设计结论沉淀到 `docs/反馈层/决策/`，不散落在进展或交接中
- 决策回写与 4.1 写回纪律同源：决策类结论稳定后归决策层，临时讨论不单独留档
