# 协作规范

回答内容优先使用中文

## 0. 架构宪章（North Star）
- 根目录 `NORTH_STAR.md` 是本项目 UI 系统的中心思想宪章，是所有架构决策、模块设计、性能优化和 API 取舍的最高准绳
- 做任何架构性改动前，必须先对照 `NORTH_STAR.md` 的《核心信条》《关键不变量》和《决策检查清单》，确认方案没有踩《反模式》
- 代码与宪章冲突时，遵循「先改文件再改代码」：要么调整方案对齐宪章，要么按宪章《修订纪律》在《偏离登记》显式登记一次偏离并说明理由、影响范围和回填计划；隐性偏离（不登记就绕过）不可接受
- 代码评审时，用《关键不变量》（I1-I12）逐条核对，任意一条被破坏都应阻断合并
- 信条（第 3 节）和不变量（第 5 节）本身的改动属于重大架构变更，必须经用户确认
- scene 新栈有一批「违反即阻断合并」硬约束（控件契约 R1-R12、宪章不变量 I1-I12、
  布局跨类同步契约、paint/node 铁律等），散落于源码 package-info、类头 Javadoc 与宪章。
  写/评审 scene 代码前必须先读硬约束总目录 `docs/记忆/长期事实/硬约束总目录.md` 并按其指针核对，不得在目录外私自绕过任何一条。

## 1. Git提交规范
- 每次修改必须提交git，保持工作区整洁
- 提交信息应清晰描述更改内容
- 提交标题格式统一为：`[English]: 中文标题`
- 提交标题常用动作建议：`Add`、`Fix`、`Refactor`、`Docs`、`Style`、`Test`、`Chore`
- 提交正文必须与标题换行分隔，并使用 Markdown 记录详细修改内容
- 提交正文必须使用中文，优先说明修改范围、核心变更点、必要的兼容性或影响说明

## 2. 分支管理规范
- 每次改动前先确认当前分支，改动不要在 `4.0` / `master` 等主分支上直接进行
- 应在合适的命名分支中开发，命名格式建议：`<动作名称>/<任务简述或功能名>`（动作名称例如 `fix`、`add`、`refactor` 等）
- 改动结束后，确认测试通过、工作区干净，再将分支合并回主分支
- 合并时必须使用 `git merge --no-ff`，保留显式合并提交，禁止 Fast-forward 直接移动主分支指针

## 3. AGENTS.md修改规范
- AGENTS.md的修改必须经过用户确认
- 不可随意更改本文件内容

## 4. AI 记忆文档协作规范
- 开始开发前必读 `docs/AI记忆文档.md`（仅作总导航，不承担流水账与项目事实汇总）
- 按变更内容写回对应层，优先覆盖/归并而非按日期追加：
  - 规则层 `AGENTS.md`：协作/编码/Git 规范与写回规则
  - 当前态 `docs/记忆/当前态/`：阶段目标、上下文、下一步、交接，可频繁覆盖
  - 长期事实 `docs/记忆/长期事实/`：目录结构、架构边界、稳定命令、项目约定
  - 决策 `docs/记忆/决策/`：关键取舍、边界约束、重要设计
  - 专项 `docs/开发者文档/errors|reviews/`：错误教训、审查结论
- 只沉淀对后续协作有持续价值的事实；临时调试、一次性试验结论不单独留档
- 信息能稳定归属到某层时写回原文档，不复制进总导航
- 完成独立任务后至少检查一次上述各层是否需更新；会话结束/交接必更新 `当前态/交接记录.md`
- 文档力求简约：指针文档只留分组名+链接不堆正文，长期事实不混入「第 N 次会话」等时态编号，能压缩就压缩

## 5. 代码规范
### 命名规范
- 命名：类名大驼峰，方法名/变量名小驼峰，常量全大写下划线分隔

### 注释规范
- 类与方法必须添加注释
- 使用中文注释，提高可读性
- 重要方法必须添加Javadoc注释，说明功能、参数和返回值
- 字段、成员变量、临时变量等位置按实际可读性需要选择性添加注释，不强制要求逐一注释
- 复杂逻辑添加行内注释，解释实现思路
- 注释应简洁明了，避免冗余

### 其他规范
- 文件规模：单个代码文件接近或超过 1000 行时必须评估职责拆分；优先按真实职责、变更频率和复用边界拆分，不按行数机械拆
- 新增大型功能时避免继续向已有千行文件堆叠逻辑，应优先拆出 registry、builder、checker、state 等协作者

### 测试规范
- 新建/评审 scene 测试前必读 `docs/记忆/长期事实/测试体系约定.md`（分层总纲、L2 防错清单、断言库速查、输入两入口边界）；L2 纯数学边界靠评审纪律守卫，按其 §6 逐条核对

## 6. 错误记录规范
- 开发中发现的错误、反复问题、重要教训必须及时记录，避免重复踩坑
- 索引文件 `docs/开发者文档/errors/README.md` 是唯一入口：二级标题作指针，详情落 `ERROR-<日期>-<简述>.md`
- 每条至少含：现象、触发场景、根因、修复、预防

## 7. Subagent 协作与 token 成本规范
- 编排细节见 `docs/开发者文档/编排模式/SUBAGENT-ORCHESTRATION.md`：诊断型走多 Oracle+Librarian 复核，实施型走 ABC 循环单元（A 规划→B 实施→C 复审，单元间首尾衔接直到完成）
- 分工：代码侦察 @explorer；外部调研 @librarian；架构裁决/深评/简化审查 @oracle；明确范围的实现与测试 @fixer；UI/UX @designer
- 优先外包可并行的只读工作，主 Agent 聚焦规划/裁决/整合/验证；派发只传路径行号不贴整文件，已派发的不自行重做或全量重读
- 只读 agent（侦察/调研/审查）可并行；写盘 agent（fixer）必须串行，读写不同批
- 子代理中断（空结果/超时/未完成）必须用原 task_id 恢复原 session，不新开让其空跑重做
- 实现完成后必经一次独立审核（代码用 @reviewer，架构复核用全新 @oracle session）
- 决策点用中文 question 向用户拍板，帧率/真机实测必交用户跑

## 8. 工具链与构建验证规范
- 编译/构建/测试/文件操作优先用 JetBrains MCP：构建走 `jetbrainsBuildProject`，读写搜索走对应 MCP 工具
- 默认 shell 仅用于 JetBrains MCP 无对应能力，或 git/包管理等终端原生任务
- shell 编译命令与 GRADLE_USER_HOME 等细节见 `docs/记忆/长期事实/稳定命令.md`（PowerShell 不支持 `&&`，链式用 `;`）
