# 协作规范

## 1. Git提交规范
- 每次修改必须提交git，保持工作区整洁
- 提交信息应清晰描述更改内容
- 提交标题格式统一为：`[动作的英文抬头]: [提交内容概要]`
- 提交标题常用动作建议：`Add`、`Fix`、`Refactor`、`Docs`、`Style`、`Test`、`Chore`
- 提交正文必须与标题换行分隔，并使用 Markdown 记录详细修改内容
- 提交正文应优先说明修改范围、核心变更点、必要的兼容性或影响说明

## 2. AGENTS.md修改规范
- AGENTS.md的修改必须经过用户确认
- 不可随意更改本文件内容

## 3. 代码规范
### 命名规范
- 类名：使用英文大驼峰命名法（如：`PlayerManager`、`BaseOperator`）
- 方法名：使用英文小驼峰命名法（如：`isSamePlayer_checkOnServer`、`registry`）
- 变量名：使用英文小驼峰命名法（如：`playerUUID`、`inPressChainKey`）
- 常量名：使用英文全大写，下划线分隔（如：`MODID`、`CLIENT_PROXY`）

### 注释规范
- 类与方法必须添加注释
- 使用中文注释，提高可读性
- 重要方法必须添加Javadoc注释，说明功能、参数和返回值
- 字段、成员变量、临时变量等位置按实际可读性需要选择性添加注释，不强制要求逐一注释
- 复杂逻辑添加行内注释，解释实现思路
- 注释应简洁明了，避免冗余

### 代码格式
- 保持现有缩进风格（4个空格）
- 大括号风格：左大括号不换行，右大括号独占一行
- 运算符两侧添加空格
- 逗号、分号后添加空格

### 其他规范
- 异常处理：捕获具体异常，避免捕获通用异常
- 日志记录：使用`MyMod.LOG`记录日志，区分日志级别
- 代码复用：提取重复代码为独立方法
- 注释维护：修改代码时同步更新相关注释