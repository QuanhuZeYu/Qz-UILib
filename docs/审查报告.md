# 审查报告

本文件作为审查报告索引，统一记录功能审查、开放化审查、易用性审查、代码评审结论等需要长期留档的结果。

详细报告建议存放在 `docs/reviews/` 目录下；本文件只保留指针、主题和必要摘要，避免单文件持续膨胀。

## 2026-05-08-html-like-developer-usability
- 类型：HTML-like 框架开发者易用性审查
- 详情文档：[`docs/reviews/REVIEW-20260508-html-like-developer-usability.md`](reviews/REVIEW-20260508-html-like-developer-usability.md)
- 后续整改：已完成根节点默认全视口、完整业务页面示例、结构节点与交互控件边界说明，以及诊断入口链路收敛；整改状态只在索引页保留摘要，不回写原始审查正文。

## 2026-05-12-first-version-entry-truthfulness-and-boundary-credibility
- 类型：第一版开发者入口真实性与边界可信度审查
- 详情文档：[`docs/reviews/REVIEW-20260512-first-version-entry-truthfulness-and-boundary-credibility.md`](reviews/REVIEW-20260512-first-version-entry-truthfulness-and-boundary-credibility.md)
- 结论摘要：业务开屏入口与显式诊断命令已经真实落地，但 HUD 交互边界、诊断入口封闭性、配置模板扩展性、宿主图片能力前提以及业务 API 与内部页面体系的隔离程度，仍存在”文档比实现更乐观”的问题。

## 2026-05-18-browser-capability-gap-audit
- 类型：浏览器常用能力差距审查
- 详情文档：[`docs/reviews/REVIEW-20260518-browser-capability-gap-audit.md`](reviews/REVIEW-20260518-browser-capability-gap-audit.md)
- 结论摘要：共核查 65 项浏览器常用能力（CSS 布局/样式/选择器、事件、DOM、表单），其中 27 项完整实现、8 项部分实现（声明与实现不一致）、30 项完全没有实现。发现 `cursor` 属性声明链路完整但系统光标从未映射、`overflow-wrap`/`word-break` 样式已声明但布局引擎未消费、`font-weight`/`font-style` 底层有能力但 CSS 属性层未暴露，三处属”文档比实现更乐观“。后续补齐状态：`cursor`、`overflow-wrap` / `word-break`、`focus()` / `blur()` / `scrollTo()` / `scrollIntoView()`、兄弟节点遍历、`font-weight` / `font-style`、`dblclick` / `contextmenu` / `transitionend` / `animationend`、`textarea`、最小 `select`、flex `order`、`calc()` 最小混合长度、`position:sticky` 首阶段闭环、`text-shadow`、`text-transform`、`text-indent`、`white-space:pre/pre-wrap/pre-line` 和单图 `background-image` 已落地；`font-family`、`display:grid`、`transform`、gradient、多背景、多重阴影、`float`、完整 `textarea` 软换行和完整浏览器原生下拉能力仍按详情文档边界处理。

## 2026-05-20-ui-framework-structure-audit
- 类型：UI 部分代码框架结构审查（明确排除字体服务）
- 详情文档：[`docs/reviews/REVIEW-20260520-ui-framework-structure-audit.md`](reviews/REVIEW-20260520-ui-framework-structure-audit.md)
- 结论摘要：UI 部分整体分层方向合理，单向依赖（dom + style → layout → paint command → renderer → render）、命令-回放分离、`DocumentEffectChain` 共享效果链、注入式时钟与运行时适配器、对外门面（`UiDocumentScreens` + `Internal*` 隔离诊断/示例）等都是优良设计。但存在以下结构性技术债：1) 5 个超千行的 god class（`DocumentLayoutEngine` 2964、`HtmlLikeDocumentWidget` 2107、`UiMainLayerSnapshotService` 1418、`DocumentAnimationTimeline` 1380、`UiRenderContext` 1376、`ElementNode` 1155，合计 ≈ 1.4 万行）；2) 8 个示例 controller 共约 250 KB / 5900 行 package-private 演示代码混入 `src/main` 主产物；3) `screen` 包 29 文件混 6 类抽象层级、`style` 包 51 文件铺一层；4) `dom/control` 实质是应用级控件却归属于 dom 包；5) `layout/UiAlignSelf` 死代码全项目零引用；6) 30+ 个 `DocumentElement*Event` 模板代码重复约 7×N 行；7) input 包反向依赖 `ui.hud` 与 `ui.screen` 破坏方向约束；8) 命名前缀 `Document*` / `Ui*` / `Internal*` / 无前缀 四套并存且无书面规范。已按 P0~P3 排序给出优先级与改动范围、风险评估，待用户决定是否启动具体重构任务。
- 后续整改：P0 全部完成。P0-1 已删除死代码 `layout.UiAlignSelf` 与 `UiLayoutSpec.alignSelf` 字段；P0-2 已新增 `AbstractDocumentElementEvent` 基类，13 个事件类（含 `DocumentCustomEvent`）继承基类消除约 7×N 行模板重复；P0-3 已统一事件取消语义：`DocumentElementFocusEvent` 补齐 `DocumentEventControl`，`DocumentLinkActivationEvent.markHandled()`/`isHandled()` 标记 `@Deprecated` 并映射到 `preventDefault()`/`isDefaultPrevented()`，`DocumentCustomEvent.preventDefault()` 仍按 `cancelable` 控制；P0-4 已将 8 个示例 controller + 6 个示例 model + 8 个示例测试迁到 `ui.screen.example` 子包并提升 `DocumentPageController`/`DocumentPageAuthoringSurface`/`DocumentPageRuntimeView`/`DirectDocumentPageAuthoringSurface` 为 public 作为共享 SPI 边界，业务门面 `UiDocumentScreens` 与 `/qzuilib test` 命令路径不变。P1 全部完成。P1-1 已把 `screen` 包按职责拆为 `screen.page`（6 个 page SPI）+ `screen.internal`（5 个内部托管/诊断类，新增 `InternalInlineDocumentPageController` 断开 `UiDocumentScreens ↔ InternalHostedScreenFactory` 循环依赖）+ `screen.example`（保持 P0-4 状态），主包仅保留 `BaseScreen`/`UiScreenManager`/`UiDocumentScreens`/`UiScreenHostSession`/`UiHostBackgroundBlurRenderer` 五个对外门面与协作类；`Internal*` 类与必要嵌套类型/字段/方法提升 public，`UiDiagnosticsScreens` 借子包名传达内部语义不再依赖 package-private 隐藏，`DevToolsScreenLauncher` 反射路径同步更新；P1-2 已把 51 个 style 文件按职责拆到 `style.values`（11 个值对象）+ `style.props`（27 个枚举）+ `style.cascade`（7 个声明/规则/表/变量/Resolver/ComputedStyle/UiBorderRadiusResolver）+ `style.selector`（3 个选择器），主包仅保留 `UiStyleProperty`/`UiStyleChangeImpact`/`UiStyleChangeListener` 共享元数据；P1-3 已把 `dom/control` 30 个文件迁到 `ui.control`，让 dom 包回归"协议+模型"边界；P1-4 已把 `DocumentCursorHost`/`SystemDocumentCursorHost` 迁到 `ui.host` 并提升为 public，与其他宿主能力抽象同包承载；P1-5 已把单文件包 `ui.theme` 下的 `UiSurfaceStyle` 并入 `ui.style.values`，删除空 `ui.theme` 目录。P2 已完成 7 项拆分：P2-3 `UiRenderContext` 1376 → 三协作类（`ClipStack` / `PaintContextCompositor` / `DeferredPostMainPass`）；P2-5 `ElementNode` 1155 → 三协作类（事件 handler 容器 / 焦点滚动桥接 / ARIA 角色）；P2-4 `DocumentAnimationTimeline` 1544 → 提取 `PropertyRuntimeSemantics` 枚举至独立文件（1354 行）；P2-1a `DocumentLayoutEngine` 3219 → 提取 `TableLayoutHelper`（2886 行，列宽分配/行高测量/单元格定位 16 方法 + `TableRowPlan` 内部类）；P2-1b 提取 `TextLayoutHelper`（2295 行，文本规范化/换行/固有宽度测量/UI 像素 ⇄ 字体原始像素缩放，新增约 650 行）；P2-1c 提取 `FlexLayoutHelper`（1648 行，row/column 主轴分配/auto margin 吸收/order 重排/flex 容器固有宽度测量，新增约 723 行）；P2-2 拆分 `HtmlLikeDocumentWidget` 2287 → 1991：`DocumentCursorResolver`（cursor 级联解析 + 伪类集合构造）+ `DocumentDragController`（程序化 drag handler + HTML5 draggable 两种模式 + 位移阈值激活 + 通过 Host 接口反向回调 widget 能力）。P2-6（`UiStyleDeclaration` ≈ 2349 行、`ComputedStyle` ≈ 750 行 EnumMap + 类型化访问器改造）涉及面过广（UiStyleResolver / 所有控件 / cursor / animation / 测试），按高风险项目顺延，未来按"分多步小步提交"方式独立推进。整改状态只在索引页保留摘要，不回写原始审查正文。
