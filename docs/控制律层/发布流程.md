# 发布流程

当前仓库只保留 `4.6.2` recovery 的历史执行记录；一次性 workflow 已在任务完成后删除。仓库不提供通用 tag 自动发布、Release 创建/上传或其他制品渠道门禁。本文件不构成本地 agent 的 Gradle、网络、workflow dispatch、push、tag 或 Release 授权。

## 固定身份

- tag：`4.6.2`
- annotated tag object：`6155c157b823c928accc25b037f7a95e7e83d669`
- peeled commit：`e86a731cf10c5fa9e0f3dd87fe52126646bf8ed1`
- Release ID：`357902877`

tag 禁止移动、删除重建或强推；历史 workflow 已删除。

## 历史两 job 闭环

已删除的 `.github/workflows/recover-4.6.2-release.yml` 当时只允许从默认分支 dispatch：

1. `verify`（`contents: read`）按固定 tag 完整 checkout，核对 tag object、peeled commit、HEAD 与唯一 `modVersion=4.6.2`；用 Java 25 执行 `test build`；精确收集 main/dev/sources/dev-preshadow 四个 JAR 与 `.changelogs/4.6.2.md`，验证 ZIP、UTF-8、大小和 SHA-256，生成 manifest 并上传本 run bundle artifact。该 job 不读取 Release 或 draft API。
2. `publish` 仅在 `publish-existing-draft` mode 运行，是全仓 workflow 唯一 `contents: write` job。它下载同 run bundle、重新计算 manifest，在 PATCH 前按固定 ID `357902877` 完整核对 draft 状态、notes 和四资产；唯一写操作是设置 `draft=false`。随后同时按 ID 和 tag GET，要求同一 ID、非 draft、非 prerelease、`published_at` 非空且 notes/四资产仍匹配。

## 本次执行结果

- 默认分支 `a86838b7` 上的 [verify run `29989023055`](https://github.com/QuanhuZeYu/Qz-UILib/actions/runs/29989023055) 与 [publish run `29989322298`](https://github.com/QuanhuZeYu/Qz-UILib/actions/runs/29989322298) 均为 attempt 1、`completed/success`。
- Release [`4.6.2`](https://github.com/QuanhuZeYu/Qz-UILib/releases/tag/4.6.2) 已于 `2026-07-23T07:53:45Z` 发布；ID/tag 两端点、notes 与四资产的名称、大小和 digest 一致。
- 一次性 `publish-existing-draft` 已消费并完成，不得 rerun 或作为后续发布入口。

固定 recovery 已无可运行实现；不得移动 tag、删除 Release、重试写入，或把本地静态检查写成远端成功。未来通用 tag 发布、JitPack、Maven 或本地 agent Gradle 能力均须独立任务重新设计与验证。
