# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-07-03 Document旧栈选项A迁移蓝图登记，分支 `4.0` HEAD 合并提交）

### 一句话现状

用户决策选项A（迁依赖方到scene新栈再删旧栈）。explorer 能力 gap 分析结论：scene 控件层成熟但**宿主粘合层完全缺失**（HUD/屏幕/远程/config框架5大类没有），8类依赖方仅1类scene完全具备承接，**可立即迁移子任务=0**。选项A是月级多阶段工程，不适合自主推进。蓝图已登记 scene技术债.md，无代码改动，已合回4.0。

### 主线：选项A迁移蓝图（docs/legacy-removal-blueprint 分支，已合回 4.0）

**explorer 能力 gap 分析**（scene 新栈现状）：
- **控件层（Phase 4）成熟**：Checkbox/Radio/Slider/TextInput/Select/Tab/Table/TextArea/SimpleList/KeyValueMap/ObjectField 等全落地
- **宿主粘合层（Phase 5）完全缺失**：scene 是"控件库 + demo hub"，非完整 UI 框架
- **物理隔离干净**：scene 包零 import 旧栈

**8 类依赖方 scene 承接能力**：
| 依赖方 | scene 能力 |
|---|---|
| 文本域 | ✅ SceneTextArea |
| config 控件层 | ◐ 控件有，模板框架无 |
| 代码编辑器/HUD宿主/屏幕入口/远程HTML | ✗ 完全没有 |

**可立即迁移子任务 = 0**：devtools 是 scene 唯一已迁消费群体，残留 `UiHudDemoController` 依赖 scene 缺失的 HUD 宿主。其余依赖方全是业务核心入口，都依赖 scene 缺失的宿主粘合层。

**推进前置（5大 P2 架构工程，每项需 Oracle 裁决 + 用户拍板）**：
1. scene HUD 宿主（层级/输入抢占/聊天框共存）
2. scene 屏幕入口（GuiScreen 桥接/屏幕管理）
3. scene 远程 UI 协议（HTML 解析/session/客户端桥）
4. scene config 模板框架（FieldSpec/PropertyBinding/草稿/保存）
5. scene 高级控件（CodeEditor/ColorPicker/TreeView/SlotGrid/Autocomplete）

**结论**：选项A是月级工程。需按"补scene宿主能力 → 迁业务入口 → 删旧栈"多会话推进。当前维持现状。

**脏工作区处理**：本次发现2个非本会话的残留改动（SceneControlChrome Javadoc格式化 + SceneTextGeometry误删"禁止逐码点累加"防错警告），已 `git restore` 回退，未混入提交。

### 待做（用户醒来决策）

- **🔴 选项A推进策略（核心）**：5大 P2 架构工程从哪个开始？建议优先级需用户拍板（HUD/屏幕/远程/config/高级控件）
- **文档矛盾仍待裁决**：技术债"已废弃" vs 项目结构"仍是对外主路径"
- **Phase 5 启动时间表**：当前"阻塞中"
- **SceneTab 真机目视**：4 条清单（fill 填满/切页稳定/常规页无回归/tabBar高度）
- **本地 4.0 ahead origin/4.0 共162 commit**：按需 push

### 分支与工作区

- 当前分支：`4.0`
- docs/legacy-removal-blueprint：已合并，保留备用
- 工作区：干净（脏残留已回退）
