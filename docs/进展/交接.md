# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-07-03 U2组合式完结ConfigScreen复用FormPageShell统一骨架口径，分支 `4.0` HEAD 合并提交）

### 一句话现状

config接入工程U2组合式完结：单元1（FormPageShell产品化）+ 单元2（ConfigScreen复用统一骨架口径）均落地合并。ConfigScreen净删31行骨架构建，per-section scroll/scrollbar自建保留，行为零回归（ConfigScreenTest 38绿+FormPageShellTest 7绿+全量绿）。Oracle C复审通过。

### 主线：config高级包作为uilib内置能力（用户新要求）

**用户决策（推翻旧架构）**：
1. 7.3之前旧决策全删（DECISION-2026* 49文件已删，80处悬空引用已清，grep全仓0残留）
2. config高级包作为uilib内置能力，不做基础样板直接做高级样板
3. 继续保持原生层解耦，保证mod核心逻辑完全独立
4. U2走组合式不抽基类，SceneDemoPageShell产品化去Demo语义到uilib.form
5. 单元2ConfigScreen复用FormPageShell统一骨架口径（核心价值在统一口径非减码）

**架构方向（Oracle规划）**：uilib内置泛型表单框架 `uilib.ui.scene.form`，config.schema/runtime保持mod核心独立（零uilib/MC依赖），config.ui变薄适配层。U2不抽AbstractFormHostWidget<D>基类（YAGNI+破U1边界），改组合式shell builder复用。

**已落地（5单元合并回4.0）**：

1. **单元① AbstractSceneHostWidget提正式包**（refactor/scene-host-promote）— 解除config反向依赖违规，136用例绿
2. **旧决策清理**（docs/purge-old-decisions）— 49文件+80引用，grep 0残留
3. **U1 FormFieldShell+FormTheme下沉**（refactor/config-form-framework）— 字段外壳零config依赖，config.ui变薄
4. **U2单元1 FormPageShell产品化**（refactor/form-page-shell）
   - FormTheme扩3骨架token（rootBg/viewportBg/titleColor），defaultDark补同源值（与ConfigTheme+SceneDemoTokens一致）
   - 新建FormPageShell（uilib.ui.scene.form）：产品化页骨架builder，5参/10参build，删statusBar回调（纯demo语义），加attachScroll开关+FormTheme参数，零MC/Forge/GL/config依赖守I10
   - SceneDemoPageShell降级薄包装：委托FormPageShell+DEMO_THEME注入，保留原签名，8个HostWidget调用点零改动
   - ConfigThemeAlignmentTest补3断言（共12断言守护骨架token对齐）
   - 清FormPageShell死import（Oracle C复审项）
5. **U2单元2 ConfigScreen复用FormPageShell**（refactor/config-screen-shell）
   - 构造器改调FormPageShell.build(attachScroll=false)拿Parts
   - 删createRoot/createViewport/createScrollContainer三方法（净删31行，781→750）
   - per-section scroll接线完全保留（双参attach+Signal[]+Computed+setScroll回调）
   - scrollbar自建保留（SceneScrollbar.create(Props)自定义read/write）
   - titleBar保留自建（字号22/12需求，shell titleBar被摘下替换）
   - 主题走ConfigTheme.asFormTheme()桥接
   - 补FormPageShellTest 7用例守护attachScroll分支契约（Oracle C建议项落地）

### API契约收窄登记（需留意）

- **SceneDemoPageShell.showScrollbar语义收窄为attachScroll合并开关**：原`showScrollbar=false`时可独立控制"有受控滚动源不显示滚动条"，新实现合并为单开关`attachScroll`，false时scrollSignal=null且不attach。当前8调用点全传true零回归。FormPageShell Javadoc已注明此为有意设计。

### Oracle C建议项（非阻断，可选follow-up）

1. **shell titleBar无用构造后丢弃**：ConfigScreen传schema.title()+modId给shell建titleBar后被摘下丢弃，有一次无用构造。可传空串省构造，或未来给shell加"不建titleBar"开关。
2. **root.__getChildren().get(0)隐式顺序耦合**：ConfigScreen依赖"shell把titleBar作为root首子"隐式契约。建议让shell在Parts暴露titleBar引用，按引用摘除而非按下标。
3. **双Javadoc块残留**：ConfigScreen的createActionBar/badge有两块叠加Javadoc（历史遗留非单元2引入），前一块是死注释，可顺手清理。

### 待做（用户醒来拍板）

- **U3 config侧改造**：DraftSignalAdapter/Renderer/ConfigScreen 基于U2方案进一步改造（U2组合式已完结骨架口径统一，U3为config侧更深改造，待用户拍板范围）
- **Oracle C建议项3条**（非阻断follow-up）：shell titleBar无用构造/get(0)隐式顺序耦合/双Javadoc残留
- **旧栈Forge配置**：本轮不动（MC guiFactory入口+网络同步能力，迁移独立大工程）
- **SceneTab真机目视**：4条清单（fill填满/切页稳定/常规页无回归/tabBar高度）
- **本地4.0 ahead origin/4.0**：按需push（含本轮合并）

### 分支与工作区

- 当前分支：`4.0`
- refactor/scene-host-promote / docs/purge-old-decisions / refactor/config-form-framework / refactor/form-page-shell / refactor/config-screen-shell：均已合并，保留备用
- 工作区：干净
