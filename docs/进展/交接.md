# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-07-03 config接入启动+旧决策清理+U1落地，分支 `4.0` HEAD 合并提交）

### 一句话现状

config接入工程启动：单元①宿主提正式包解除config反向依赖违规 + 旧决策49文件全删清理80处引用 + U1 FormFieldShell/FormTheme下沉uilib.form。U2a抽象边界草案完成，Oracle建议**不抽基类改组合式**（需用户拍板）。全量测试绿，已合回4.0。

### 主线：config高级包作为uilib内置能力（用户新要求）

**用户决策（推翻旧架构）**：
1. 7.3之前旧决策全删（DECISION-2026* 49文件已删，80处悬空引用已清，grep全仓0残留）
2. config高级包作为uilib内置能力，不做基础样板直接做高级样板
3. 继续保持原生层解耦，保证mod核心逻辑完全独立

**架构方向（Oracle规划）**：uilib内置泛型表单框架 `uilib.ui.scene.form`（泛型D=字段描述符），config.schema/runtime保持mod核心独立（零uilib/MC依赖），config.ui变薄适配层。

**已落地（3单元合并回4.0）**：

1. **单元① AbstractSceneHostWidget提正式包**（refactor/scene-host-promote）
   - 新建 `uilib.ui.scene.host` + `uilib.ui.scene.host.lwjgl` 正式包
   - AbstractSceneHostWidget + 6个Lwjgl族从devtools内部包提到正式包
   - 新建 KeyboardTextInputSource/CursorBackendProvider 接口解耦基类 instanceof（4处等价重构）
   - 解除 config→devtools 反向依赖违规，强化I10，136用例全绿

2. **旧决策清理**（docs/purge-old-decisions）
   - 删除49个旧决策文件 + 清理80处悬空引用（13文档+决策README重写+NORTH_STAR 4处+src注释3处）
   - grep DECISION-2026 全仓0残留

3. **U1 FormFieldShell+FormTheme下沉**（refactor/config-form-framework）
   - 新建 `uilib.ui.scene.form` 包（package-info守I10零config/MC依赖）
   - 新建 FormTheme（15字段record+defaultDark工厂）+ FormFieldShell（字段卡片外壳）
   - config 4 Renderer 改调 FormFieldShell，ConfigTheme.asFormTheme()桥接，删除config FieldShell
   - P1/P2修复：删ConfigTheme 6个dead CARD_*常量 + 加ConfigThemeAlignmentTest守护9共享字段对齐
   - 泛型框架第一步：字段外壳零config依赖，config.ui变薄适配层，行为零回归

**U2a抽象边界草案（Oracle只读设计，未实施）**：
Oracle评估U2（AbstractFormHostWidget<D>骨架下沉）后建议**不抽基类改组合式**：
- 只有一个真实子类ConfigScreen，过早抽象踩YAGNI
- 项目已有组合范式 SceneDemoPageShell（7个demo页复用）
- D泛型会破坏U1刚建立的零config依赖边界（renderField需adapter，config类型泄漏进uilib）
- 纯骨架净收益薄（SceneDemoPageShell已覆盖root/titleBar/viewport/scrollContainer/scrollbar）
- 建议U2降级为"ConfigScreen复用shell builder"组合式重构，泛型基类推迟到真出现第二个scene表单宿主时再做

### 待做（用户醒来拍板）

- **🔴 U2方向裁决（核心）**：Oracle建议组合式不抽基类，用户确认？
  - 若同意组合：U2降级为ConfigScreen复用FormPageShell（需决定SceneDemoPageShell是否产品化到uilib.form）
  - 若坚持抽基类：接受D泛型破坏U1边界或退化成FieldSpec别名（无收益）
- **U3 config侧改造**：DraftSignalAdapter/Renderer/ConfigScreen 基于U2方案改造（待U2定方向）
- **旧栈Forge配置**：本轮不动（MC guiFactory入口+网络同步能力，迁移独立大工程）
- **SceneTab真机目视**：4条清单（fill填满/切页稳定/常规页无回归/tabBar高度）
- **本地4.0 ahead origin/4.0 共172 commit**：按需push

### 分支与工作区

- 当前分支：`4.0`
- refactor/scene-host-promote / docs/purge-old-decisions / refactor/config-form-framework：均已合并，保留备用
- 工作区：干净
