# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-07-02 UI 组件抽象收口完成并合回 4.0，分支 `4.0` HEAD `3a272c52`）

### 一句话现状

UI 组件抽象收口以 5 个 ABC 循环单元 + config 收尾完成，4 个候选全部落地，全量测试绿，已 `git merge --no-ff` 合回 4.0 主线。编排维持现状（两类编排已收敛）。

### 主线：UI 组件抽象收口（refactor/scroll-api-slim 分支，已合回 4.0）

**背景**：explorer 全量侦察 control 包 27 文件，按 ROI 排出 top5 抽象候选，用户拍板推进候选 1-4，编排维持现状。

**5 个 ABC 单元 + config 收尾**（顺序 4a→1→2→4b→3→config）：

1. **候选4a bindComputed API**：SceneRuntime 新增 `<T> bindComputed(Supplier, Consumer)` 便捷方法，语义等价 `bind(Computed.create(...))` + 2 条单测。为候选2 helper 与候选4b 替换铺路。
2. **候选1 SceneTextGeometry**：从 TextInputPrimitive(535→357) + TextAreaPrimitive(856→696) 抽取 9 个码点几何静态方法 + PrefixWidthCache 缓存类，净删 338 行。补 38 条 L2 纯数学测试覆盖 emoji 代理对边界。substringByCodePoints 统一取 TextArea 版（入参 clamp 保证等价）。
3. **候选2 SceneControlChrome**：chrome bind 三件套（bindStandardBorder 9处 / bindCursor 14处 / bindSelectableBackground 6处），跨 13 控件 30 处塌缩。放 control 包守 I6（依赖 runtime+input，不能放 paint）。Button 不进 selectable（primary 是编译期常量）。补 KeyValueMap 第15处 cursor 收口。
4. **候选4b bindComputed 全量替换**：scene.control 54 处 `rt.bind(Computed.create(...))` → `rt.bindComputed(...)`，13 处孤立 import 清理。
5. **候选3 Controls 收 shell**：SceneControlsHostWidget 收进 SceneDemoPageShell（第 8 滚动页归一），删重复 attach + 私有 createTitleBar。
6. **config 收尾**：config/devtools 9 处 bindComputed 收口（FieldShell3/NumberFieldRenderer1/ConfigScreen2/SceneFormHostWidget3），全仓代码层 `bind(Computed.create` 归零（仅 Javadoc 文档示例保留）。

**新建工具类**（均 control 包）：
- `SceneTextGeometry.java`（242 行）：码点几何 + 编辑 + PrefixWidthCache 三区
- `SceneControlChrome.java`（130 行）：chrome bind 三件套

### 决策摘要

- **候选4 拆 4a/4b**：4a 先加 API，候选2 helper 内部用 bindComputed，4b 在候选2 后替换散点，避免「先替换再删除」白工。
- **Button 不进 selectable helper**：primary 是编译期 boolean 非 signal，且 Button 是试金石参考实现，高风险低收益。
- **文本色不抽**：selected 来源差异大（checked/on/handle.selected），抽了参数比重复还乱。
- **Slider/TextInput 背景不进 selectable**：非 selected 四态模式。
- **SceneTextGeometry/SceneControlChrome 放 control 包**：依赖 runtime，放 text 包会循环依赖，放 paint 包破 I6。
- **编排维持现状**：两类编排（诊断型多 Oracle+Librarian / 实施型 ABC 循环单元）已收敛，92 行无明显冗余。

### 验证

- 编译：通过（每单元 + 合并后）
- 全量测试：绿（合并后 37s，含 SceneTextGeometryTest 38 用例 + SceneRuntimeTest bindComputed 等价性 + 各控件端到端）
- 守 NORTH_STAR I1-I12 / R1-R12：工具类纯函数不碰节点/signal/失效级别；helper 走 bind 派生守 R4/I4；分层守 I6
- git merge --no-ff 合回 4.0，保留显式合并提交 `3a272c52`

### 遗留 / 待做

- **千行文件拆分**：SceneDataTable(1150) / SceneKeyValueMap(1113) 破千，属职责拆分范畴（非抽象收口），explorer 已标记，另开任务评估
- **SceneTab contentPanel 高度链**：scene 技术债，延续
- **死 import 清理**：explorer 报告少量预先存在的未使用 import（非本轮引入），另开任务
- **真机验证**：帧率/目视交用户跑，主 Agent 不阻塞

### 分支与工作区

- 当前分支：`4.0`
- 最新 commit：`3a272c52`（merge --no-ff 合并 refactor/scroll-api-slim）
- refactor/scroll-api-slim：已合并，保留备用
- 工作区：干净
- 本地 4.0 ahead origin/4.0（未 push，按需 push）
