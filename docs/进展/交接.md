# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-07-03 底层基础设施优化完成并合回 4.0，分支 `4.0` HEAD 合并提交）

### 一句话现状

底层基础设施经研究型编排（A1+A2 Oracle 双切面诊断 + Librarian 行业调研）系统审视，确认 reactive 全族/布局四协作者/渲染 token/文本度量接缝均已达饱和，仅 3 个实质候选落地（删僵尸缓存方法 + subtreeCount 摘除 + route() 切分），净删 416 行，全量测试绿，已合回 4.0。

### 主线：底层基础设施优化（refactor/scene-infra-slim 分支，已合回 4.0）

**研究型编排（编排一：多 Oracle + Librarian 复核）**：
- A1 Oracle 诊断节点模型 + reactive 核心 + 输入/滚动侧：reactive 全族教科书级零改动；SceneNode 1913 行是"Javadoc 60%+ 行数虚高"非职责过载；SceneRuntime Facade 合理。找到 subtreeCount 每帧维护零消费、route() 225 行巨型方法、僵尸缓存方法三候选
- A2 Oracle 诊断布局引擎 + 渲染引擎 + 文本度量侧：布局四协作者（4.1-4.3 已拆）跨类契约齐全；渲染 token 已收口；文本度量接缝守 I10 纯净。结论：抽象空间已饱和，仅 1 个低 ROI 不推荐候选（ConstraintResolver grow 拆分）
- Librarian 行业调研 4 问：①节点失效模型主流但密度偏高 ②route 巨型单方法属被劝退形态 ③零消费每帧缓存是 YAGNI 经典反例四方一致指删除 ④reactive 核心主流且我们糖位置正确（runtime 包非 reactive 核心）

**2 个 ABC 单元落地**（基于 07b9ff6b）：
1. **删僵尸 + subtreeCount 摘除**（单元A，2 commit）：
   - 删 SceneNode.invalidateLayoutCache/invalidatePaintCache（全库零调用，只清缓存不打脏不冒泡的陷阱）
   - subtreeCount 摘除：删字段/方法/探针/SceneLayoutEngine 每帧后序重算调用 + 整删 SubtreeNodeCountTest。消除每帧 O(深度) 冒泡 + 后序重算。结构变更方法本体保留，只删 markSubtreeCountDirty 调用
   - NORTH_STAR §10 经核查无 subtreeCount 独立登记（"并行基建保留"是宏观表述），保持宪章抽象不动
2. **route() 切分**（单元B，1 commit）：
   - SceneInputRouter.route 229→57 行薄编排，提取 dispatchPointerCancel/dispatchPointerMain/dispatchKeyboardAndText 三子方法
   - 纯提取零行为变更，continue→return 等价性已验证（dispatchPointerMain 是循环体末尾调用），守零标脏硬不变量
   - Javadoc 沉淀 Bug1/N1/I4d/CLICK 知识

### 决策摘要

- **SceneNode 不抽 NodeProps/NodeBuilder**：属性槽与失效级别本质耦合（setter 自动打脏是 I4 封装核心），抽了破 I4 负 ROI。Librarian 建议评估但 A1 实测否定
- **subtreeCount 删除**：用户拍板（触及 §10 但 §10 无独立登记）。行业调研强背书（YAGNI 经典反例，重建成本极低）
- **ConstraintResolver grow 拆分不做**：两函数对称但语义有差异，抽象引入轴向枚举反降可读性（★☆☆）
- **reactive 核心不动**：Signal/Computed/Effect/Owner/Scheduler 教科书级，零改动

### 验证

- 编译：每单元 + 合并后均通过
- 全量测试：2706 用例全绿（合并后 37s），输入侧 SceneInputRouterCaptureCancelTest/SceneRouterImplicitFocusTest/FocusManagerKeyRouterTest 等 key 路径覆盖
- 守 NORTH_STAR I1-I12：subtreeCount 不被任何不变量依赖；route 切分守零标脏硬不变量；结构变更方法本体保留
- git merge --no-ff 合回 4.0

### 遗留 / 待做

- **ConstraintResolver 879 行**：内含两对称 grow 分配器（267 行），拆分 ROI 低不推荐，若未来布局复杂度增长可重评估
- **SceneParallelExecutor + NoBubble 系列**：§10 登记保留的零成本并行骨架，不动（与 subtreeCount 不同，这俩无每帧成本）
- **真机验证**：帧率/目视交用户跑，主 Agent 不阻塞
- **未 push**：本地 4.0 ahead origin/4.0，按需 push

### 分支与工作区

- 当前分支：`4.0`
- refactor/scene-infra-slim：已合并，保留备用
- 工作区：干净
