# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-07-03 剩余遗留自主排查完成并合回 4.0，分支 `4.0` HEAD 合并提交）

### 一句话现状

剩余遗留项目经编排一诊断型自主排查（用户睡觉期间）：K3 裁决不抽（Oracle 有证据）、旧栈20文件判定不自主拆（文档矛盾+无硬约束守护）、render包2文件剔除旧栈清单。结论登记到 scene技术债.md，无代码改动，全量测试绿，已合回 4.0。**用户醒来需决策文档矛盾**（详见待做）。

### 主线：剩余遗留自主排查（refactor/remaining-cleanup 分支，已合回 4.0）

**用户授权"按编排自动完成剩余遗留"后自主推进**，编排一诊断型：

1. **K3 SceneKeyValueMapMutations**：Oracle 裁决**不抽**
   - 增删改方法（updateRow/addRow/removeRow/publishRows）读写 `rows` Signal，无法 L2 纯数学测；L3 端到端已由 SceneKeyValueMapTest 完整覆盖
   - 与 K1 本质不同（K1 是纯 POJO 函数，K3 是有副作用 Signal 编排）
   - 抽出反增耦合（双向牵连 + safeRows 副本 + 类头开销，代码总量反增）
   - 守 AGENTS §5「按职责拆非按行数机械拆」：mutation 与 create/buildRow 是同一编排链
   - 纠正 A1 推断：canAdd/canRemove 已是纯值参数不读 Props

2. **旧栈 20 个 >900 行文件**：explorer 侦察判定**不自主拆**
   - **文档矛盾（致命）**：`scene技术债.md`「旧栈已废弃」vs `项目结构.md:18`「旧栈仍是对外主路径」直接冲突
   - 源码活跃：18 文件零 `@Deprecated`，6 月 60+ 提交（脏子树缓存/ModernConfig 46→75fps/B6 FBO），业务入口深度依赖
   - 旧栈**无硬约束文档**（新栈有 R1-R12/I1-I12+总目录守护，旧栈没有），盲拆风险高
   - **render 包 2 文件剔除旧栈清单**：`UiRenderContext`(1045)/`UiMainLayerSnapshotService`(1195) 是新旧两栈共用支撑（scene devtools 直接 import），不能当旧栈拆
   - 登记到 `scene技术债.md §四`，不实施

3. **SceneTab 真机验证**：交用户目视（fill 填满/切页稳定/常规页无回归/tabBar 高度），不阻塞
4. **ConstraintResolver grow 拆分**：A2 已判低 ROI 不推荐，不做
5. **push**：AGENS 明确不擅自 push，跳过

### 验证

- 全量测试：绿（38s，无代码改动）
- 登记文档：`scene技术债.md` 新增 §四侦察登记 + §五 K3裁决不做
- git merge --no-ff 合回 4.0

### 待做（用户醒来决策）

- **🔴 文档矛盾裁决（最关键）**：`scene技术债.md §四`「旧栈已废弃」vs `项目结构.md:18`「旧栈仍是对外主路径」——哪个是当前真相？这决定旧栈20个>900行文件是否值得拆
  - 若旧栈真将退役 → 不拆，等自然淘汰
  - 若旧栈长期共存 → 按 ABC 拆（但需先建旧栈硬约束文档）
- **Phase 5 退役时间表**：文档只说"阻塞中"无日期，需用户给节奏
- **SceneTab 真机目视**：4 条清单（见上一轮交接）
- **本地 4.0 ahead origin/4.0 共156 commit**：按需 push

### 分支与工作区

- 当前分支：`4.0`
- refactor/remaining-cleanup：已合并，保留备用
- 工作区：干净
