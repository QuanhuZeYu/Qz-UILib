# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-07-03 Document旧栈狭义移除盘点完成，分支 `4.0` HEAD 合并提交）

### 一句话现状

用户决策"Document旧栈狭义移除（仅ui/document+ui/dom）+ 只删0依赖部分"。explorer 精确盘点结论：**74生产文件+19测试，可删=0**——全部被业务深度依赖（反向依赖闭包分析）。"只删0依赖"口径下"全线移除"达成度0%，不能强删致编译断裂。结论已登记 scene技术债.md，无代码改动，已合回4.0。**要真正移除需用户重新决策策略**。

### 主线：Document旧栈狭义移除盘点（docs/legacy-removal-audit 分支，已合回 4.0）

**用户拍板**：狭义范围（仅 ui/document + ui/dom）+ 只删0外部依赖部分（有业务依赖的保留）

**explorer 精确盘点结论（0 可删）**：
- 74 生产文件（document 8 + dom 66）+ 19 测试，**可删 = 0**
- 37 个表面"零外部引用"A 候选，经**反向依赖闭包分析**全部被 B 类种子传递依赖：
  - B 类种子（`ElementNode`/`UiDocument`/`DocumentNode`/`TextNode`/`HtmlLikeDocumentWidget` + 29 event/handler 类）被 config/control/layout/paint/hud/screen/remote/style.cascade/style.selector/animation/net/host/devtools + 30+ test 深度依赖
  - A 候选不可删根因链：`ElementNode extends ElementInteractionNode` → 持有 `ElementInteractionHandlers`(13 handler) → 每个 handler 对应 event 被 dispatcher 持有 → dispatcher 被 `HtmlLikeDocumentWidget` 持有 → widget 被 config/hud 业务入口直接依赖
- **"只删0依赖"口径下"全线移除"达成度 = 0%**

**诚实处置**：不执行任何删除。为凑"移除"而强删有依赖的文件会让编译断裂、业务崩溃，不负责。结论登记 `scene技术债.md §四`。

### 待做（用户醒来重新决策）

- **🔴 移除策略重新决策（核心）**："只删0依赖"口径已证实无法推进（0可删）。要真正移除 ui/document+ui/dom，必须改策略：
  - **选项A**：先迁移依赖方到 scene 新栈（HUD/config/control/layout/paint 等全重写），迁完再删旧栈——多阶段大工程
  - **选项B**：连同业务功能一起删（HUD/config 等直接弃用）——破坏性最大
  - **选项C**：维持现状不移除，等 Phase 5 退役时统一处理
- **文档矛盾仍待裁决**：技术债"已废弃" vs 项目结构"仍是对外主路径"
- **SceneTab 真机目视**：4 条清单（见更早轮次交接）
- **本地 4.0 ahead origin/4.0 共159 commit**：按需 push

### 分支与工作区

- 当前分支：`4.0`
- docs/legacy-removal-audit：已合并，保留备用
- 工作区：干净
