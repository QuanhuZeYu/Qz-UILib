# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-07-03 遗留项目收口完成并合回 4.0，分支 `4.0` HEAD 合并提交）

### 一句话现状

遗留项目经研究型编排（A1+A2 Oracle + Librarian 三方）系统诊断 + 4 ABC 单元实施，完成千行文件拆分（DataTable/KeyValueMap 各抽静态工具类）+ SceneTab 传导父高特性（修本体打通4断裂点）+ 死import 清理38处，全量测试绿，已合回 4.0。

### 主线：遗留项目收口（refactor/legacy-cleanup 分支，已合回 4.0）

**研究型编排（编排一：A1+A2 Oracle + Librarian 三方）**：
- A1 Oracle 千行文件诊断：DataTable 重在样式（消费Primitive自叠chrome）、KeyValueMap 重在校验（消费成品控件）。推荐 D1（抽EditorChrome样式层 ~240行）+ K1（抽Validation校验层 ~96行），公共数据模型留原文件守API兼容
- A2 Oracle SceneTab技术债诊断：contentPanel 高度链根因是 shrink-to-content 设计不传导父高，影响 StressTest 等。另盘点死import 32处。用户拍板"修本体加传导选项"（非仅文档化）
- Librarian 行业调研：D1/K1 方向完全主流（对标 Compose TextFieldDefaults / AntD Table分层 / Flutter validator外置 / Web Zod校验独立化），无需调整

**4 ABC 单元 + P2修复落地**（基于 ce553ad9）：
1. **D1 SceneDataTableEditorChrome**：抽静态样式工具类（21色板常量+decorate*/resolve*/DataTableListboxChrome），DataTable 1148→914 退出千行警戒。TEXT_COLOR两份private副本（主类+EditorChrome）守封装。optionValue数据工具留主类守R12
2. **K1 SceneKeyValueMapValidation**：抽静态校验工具类（validateRows三规则纯函数+firstError+ValidationState），KeyValueMap 1112→1032。校验算法逐字搬迁，主类薄委托
3. **SceneTab fillContentPanel 传导选项**（特性开发）：Props加boolean字段+5参委托构造器（默认false向后兼容，jabel候选b通过）。create()内读常量做静态if（守R3）打通4断裂点：①root fill ②contentPanel fill ③tabBar补preferredHeight（隐藏杀手，照SceneSegmented口径）④调用方panel fill。SceneStressTest启用true。补3条fill测试（传导/兼容/切页零重排）
4. **P2-1**：SceneTab Javadoc 缩进对齐
5. **死import清理**：38处31文件（A2盘点30+漏盘8旧栈杂项），纯删行零行为

### 决策摘要

- **公共数据模型不外提**：Row/Column/KeyValueRow/ValidationError 等被 devtools/test 大量 import，外提是破坏性API变更，属"为拆而拆"
- **SceneTab 修本体而非仅文档化**：用户决策。Oracle A 深度规划发现4处断裂点（含隐藏的 tabBar preferredHeight 杀手），照 SceneSegmented 已踩坑口径解决
- **jabel 候选b record委托构造器**：编译验证通过，保取向后兼容（5参重载默认false）
- **TEXT_COLOR 两份private副本**：避免EditorChrome反向引用主类private破坏壳样式层独立收口范式

### 验证

- 编译：每单元 + 合并后均通过
- 全量测试：绿（合并后 38s），SceneTabTest 11项（原8+fill3）、SceneDataTableTest、SceneKeyValueMapTest 16项全过
- 守 NORTH_STAR I1-I12 / R1-R12：R3（读常量静态if）、R12（壳样式层）、API兼容全守
- git merge --no-ff 合回 4.0

### 遗留 / 待做

- **KeyValueMap 1032 行**：仍接近千行警戒（K1目标校验收口达成非行数达标），若需继续瘦身可做 K3（抽 Mutations 增删改层 ~90行，A1 标 ROI★★二期）
- **其它千行文件**：全仓 >900 行还有 14 个（UiStyleDeclaration 2215、HtmlLikeDocumentWidget 1616 等），属旧栈积债非本任务范围
- **SceneTab 真机验证**：交用户目视（fill=true 内容区填满/切页高度稳定/常规页无回归/tabBar高度正常）
- **ConstraintResolver 879 行 grow 拆分**：低 ROI 不推荐，未来重评估
- **未 push**：本地 4.0 ahead origin/4.0，按需 push

### 分支与工作区

- 当前分支：`4.0`
- refactor/legacy-cleanup：已合并，保留备用
- 工作区：干净
