# 审计包 v2.4：Qz-UILib 全库默认液态玻璃样式（G00→G19 交付外审）

> **自包含审计材料**，供外部评审使用。代码节选与色值取自 `4.0` 本地分支（代码头 `7aeedc21`，文档头 v2.1 提交）；**附录 A 全部表格行由脚本 `auditA_v21_full.py` 直接输出，禁手抄**（v2 的 P-10 教训）。
> **v2.1 修订说明**（因审查者 B 外审第二轮 F-01/F-02 整改）：附录 A 的 success 候选表与虚拟化 ΔL 行系 v2 编写时手抄残留、未经脚本复算，现整表替换为脚本输出；**F-01 坐实：`#A5D6A7` 在 M1 双档通过（6.95/4.57），v2「列举域内无解」结论作废**，successText 委托论证重建；ΔL 定义统一并披露。P-10 状态：数值整改至 v2.1 闭环（脚本生成纪律已固化）。另按审查者 A 意见修订 §3 措辞（见该节「producer 身份」段）。
> **对审查者 A 的事实校正**：其第一轮审查基于 **GitHub 远端 4.0 快照——该远端落后整个施工期（本仓按规范未 push）**。其「命题 B」所引 `SceneButton` 直写 `root::setBackgroundColor`、`SceneControlChrome` 生产写入路径，在本地 HEAD 上不成立：`SceneButton:121` 现走 `SceneSurfaceBinder.bind`；`SceneControlChrome.bindSelectableBackground/bindStandardBorder` 与 `SceneStateColors.*` 的**生产调用点为 0**（保留为公共兼容入口，契约 §6/§10.1 明文登记）。其概念层建议（write-sink 术语、producer ownership test、Role/ActionVariant 拆分、Material 模式显式化、privileged renderer 命名治理）有效，登记于 §6.5 待裁决。
> **v2.2 修订说明**（外审第三轮：A 撤回事实层攻击并升级零点证明口径，本轮按其 L1/L2/L3 三层标准以脚本重验并吸收「node 级单一写入者」精确表述；契约 §7.3 同步补录。现行源码包 `qz-uilib-src-main-4.0-c389ac42.zip`（`src/main/java` 全量，1.9MB）可应审方要求提供，供 Level-2/3 独立零点核验）。
> **v2.3 修订说明**（外审第四轮：审查者 A 基于 c389ac42 全量源码完成独立零点核验——L1/L2/L3 全 PASS；逐节点核验 5 个虚拟化/动态行样本（VirtualGrid/SearchResultList/VariantChooser/StructuredListRenderer/DataTable），**未发现任何同节点 binder+轻量双写**，「node 级单写者」补录前提获外部独立证据；正式签署 L-C，限定语「旧 selectable-background producer 的**生产使用**为零，旧实现能力仍作兼容本体保留」（不许偷换为零能力/全库 setter 唯一来源——v2.2 现文已符合）；§3.1 表口径术语按其建议更名 production/executable reference；§6.5-A 获其交付的具体测试形态（四行为矩阵 + RULE-1~4），状态改为「形态已交付，待立项裁决」）。
> **v2.4 修订说明**（外审第五轮·收口）：审查者 A **正式收口架构边界线**：事实基线原则、L-C 限定签署、node 级单写者五样本证据、§6.5-A 立项形态锁定（四行为矩阵/P1–P4/RULE-1~4 注册制，特别保留 P4）、Role×Variant 等演进项「不得错误提升为当前阻塞缺陷」全部确认；并认可「唯一通用实现（实现归属）vs node 级单写者（运行时不变量）」的分离表述为最终口径。**审查者 B 通道终止（无法再访问）**：其 F-01/F-02 整改已交付至 v2.1+（脚本直出+逐位一致复算），但未获回签——数值链闭环状态如实记为「整改完成、评审者缺席回签、由脚本化纪律与主代理独立重算兜底」。A 定义下一轮评审焦点：**「§6.5-A 测试实现是否真的证明了契约，而不是把契约重新写了一遍」**。总体状态维持：代码完成、真机/GPU/宿主验收待验——不因任何签署提前放行。
> **v2.5 修订说明**（主代理独立重审，非外审轮）：A 线「代码零改动」前提经源码级复核成立（`src/main` 与代码头逐字节一致），L1 生产调用点仍为 0、L3 语义引用归属与披露一致；B 线数值链在脚本级复现**仅 1 处未闭环**（§7 `onAccent-over-selBg` 缺脚本溯源，已修）；并修正两项表述：① §8 的「6 跳过项身份」由待补转为**已实证**（2 项 LaTeX 参考渲染需 `qz.latex.reference.jar`/`QZ_LATEX_REFERENCE_JAR` 外部 jar，4 项 `C9DualFontProbeTest` 字体探针，均 `AssumptionViolatedException`）；② P-11 的「远端落后本地全部施工」按实测订正为**本地领先 3 个文档提交**（详见台账 P-11/P-12）。

---

## 1. 仓库与背景

**Qz-UILib**：Minecraft 1.7.10（GTNH 生态、Java 8 语法基线、Gradle 9 + Jabel）的 **Live2D 式 UI 框架库**——场景树（`SceneNode`/`ContainerNode`/`Transform`）、`SceneRuntime`（固定步进 + `PaintPlan`/`PaintCommand`）、**响应式信号层**（`ReadableSignal`/`WritableSignal`/`Computed`/`Effect`/`Owner` 作用域树）、`UiBackdrop` 滤镜（`UI_BLUR`/`UI_NOISE`/`UI_RIM_LIGHT`/`UI_TINT`），**非内容 Mod**。GLO/GL2 双后端 + headless；颜色 int ARGB；逻辑像素 dp。

施工前痛点：玻璃观感只在测试样板页（14 处手搭配方）；通用控件与配置框架默认是旧深色纯色平面；宿主不用玻璃就没有玻璃观感。**工程目标**（用户裁决：全库默认）：液态玻璃成默认材质，「一构建就默认是」；显式配置与兼容入口继续有效。

## 2. 交付定义与总体结果

交付定义 = 代码与测试完成 + build 全绿 ＋ GPU/Minecraft 宿主视觉与性能验收（**未完成**）。

| 指标 | 施工前 | 施工后 |
| --- | --- | --- |
| 测试规模 | 4073 例全绿 | **5155 例 / 0 失败 / 6 既有跳过**；全量 build exit 0（项目方报告，构建报告未随包；**v2.5 主代理实测复核一致**，见下） |
| 库代码类数 | 462 | 479 |
| FilterSlot 静态声明 | 仅 playground | 62 文件 66 处（**结构预算，非性能测量**，§6.3） |
| 默认外观变化 | — | 17 实例 + 12 renderer + 11 控件语义色，逐项冻结登记 |

**状态判定**：**代码阶段已完成（项目方报告），外部验收未完成**。补证缺口清单见 §8。

**v2.5 主代理独立实测（2026-09-10，HEAD `0856d8b1`）**：`gradlew.bat --offline --console=plain test` 得到 BUILD SUCCESSFUL，由 `build/test-results/test/*.xml` 逐类统计 **5155 tests / 0 failures / 0 errors / 6 skipped**，与项目方报告逐位一致；`src/main/java` 内容 SHA256 `b1a3e530db044f86f290e86eb968000bd7e9fbfe863bcfbec10ea29c7737730c`（687 文件），与代码头 `7aeedc21` 后 `git diff 7aeedc21..HEAD -- src` 为空的结论互证；本条为 `test` 任务实测，`build` 全链仍以台账 L13/验收记录为准（非本轮复核）。

## 3. 架构与冻结契约

### 3.1 权威链与优先级
`ThemeTokens`（原始色板唯一来源）→ `SceneTheme`（不可变快照，逐字段校验回退）→ `SceneSurfaceStyle`（配方值对象）→ `SceneSurfaceBinder`。解析优先级：**显式控件配方 > 局部/页面主题 > runtime 默认 > 库默认 liquidGlassDark**；主题沿 `Owner` 树继承，默认档静态常量零订阅。

**surface producer 身份（v2.2 三层零点证明，审查者 A L1/L2/L3 口径；契约 §7.3 第三轮补录同步）**：
架构不变式为 **node 级单一写入者**：每个 `SceneNode` 的每个 surface 属性恰有一个合法 producer；「全库只有一个 `backgroundColor` 写入形态」是 v2.1 前文的过度表述，已订正（契约 §4 标题同批补录精确化）。producer 分三层：
- **L-A 配方级表面**（窗口/面板/字段/按钮整面六件套）→ 唯一通用实现 `SceneSurfaceBinder`（`SceneLiquidGlassStyle.bindButton/bindForeground` 仅适配委托）；
- **L-B 元素级轻量通道**（虚拟行 hover/selected、caret、选区 highlight、scrollbar thumb、dot、scrim、icon placeholder）→ 控件将主题/局部信号直接绑定到**其独占子节点**（binder 不管理这些子节点，同节点无竞争；§4.1 轻量档与契约「不迁移」族的既定形态）；
- **L-C 兼容入口**（Chrome/StateColors/ChromeTokens 静态系）→ 生产真实引用 0（下表），能力完整保留（边界见 §3.6）。
`cursor` 归 `SceneControlChrome.bindCursor`（不变项）；尺寸/间距/动效常量不属主题接管域（契约 §6）。

**零点证明表**（`audit_zero_proof.py`@`c389ac42` 脚本输出直出；REF=排除定义体声明与注释后的真实引用，**v2.3 术语校准：本列等价于 production/executable reference 口径**，Javadoc 提及不计入——审查者 A 第四轮建议采纳）：
```
bindSelectableBackground: 总=5  定义=1 注释=4 REF=0
bindStandardBorder:       总=11 定义=1 注释=10 REF=0
dangerBackground:         总=1  定义=1 REF=0        listItemBackground: 总=1 定义=1 REF=0
selectedBackground:       总=6  定义=1 注释=4 REF=1 → SceneControlChrome:129（兼容入口定义体内部链）
standardBackground:       总=6  定义=1 注释=4 REF=1 → SceneControlChrome:130（同上）
inputBackground:          总=4  定义=1 注释=3 REF=0
applyPanelChrome:         总=13 定义=1 注释=12 REF=0
applyOuterShell:          总=4  定义=1 注释=2 REF=1 → SceneChromeTokens:22（applyPanelChrome 体内自调）
asFormTheme:              总=28 定义=2 注释=25 REF=1 → ConfigScreen:304（契约 §4.2 宿主桥接豁免域）
```
（注：`selectedBackground/standardBackground` 的 REF=1 均位于 L-C 兼容入口定义体内部（Chrome→StateColors 链），证明「旧生产链在入口内完整可达」但「库生产路径无人接入」，两者共同构成 §3.6 能力边界的实证基础。）

### 3.2 双路径迁移承诺
```java
// SceneSurfaceStyle.withTheme(rt, style, role)：
//   style==null → 主题配方信号（订阅主题）；style!=null → 原 style 常量信号（选择层不订阅）
// 既有 setter：null → 主题通道；非 null → 显式值（旧契约）
```
「不订阅主题」指**配方选择信号层**；`bindForeground` 在配方 `foreground=null` 时回落主题前景（不同层的刻意行为）。旧宿主入口（`ConfigTheme/FormThemes/DialogTheme/ToastTheme`）为兼容 bridge：显式传值行为不变，默认路径与主题同源。`asFormTheme()` 等出口库内调用方清零（守护方式与其效力边界见 §4.2）。

### 3.3 统一 binder 写入面（节选自 `SceneSurfaceBinder.bindCore`）
```java
ReadableSignal<SceneSurfaceStyle.StateStyle> surface = Computed.create(
        () -> surface(recipe.get(), state.get()));
rt.bindComputed(() -> recipe.get().getBorderWidth(), node::setBorderWidth);
rt.bindComputed(() -> recipe.get().getCornerRadius(), node::setCornerRadius);
rt.__bindAnimatedColor(() -> surface.get().getTint(), node::setBackgroundColor, duration);
rt.__bindAnimatedColor(() -> state.get() != State.DISABLED
                && Boolean.TRUE.equals(interaction.focused().get())
        ? recipe.get().getFocusEdge() : surface.get().getEdge(), node::setBorderColor, duration);
rt.__bindAnimatedFloat(() -> surface.get().getElevation(), node::__setSurfaceElevation, duration);
if (motionRoot != null) {
    rt.__bindAnimatedFloat(() -> -recipe.get().getContentLift() * surface.get().getElevation(),
            value -> motionRoot.setTransform(Transform.translate(0.0F, value)), duration);
    rt.__bindAnimatedFloat(() -> state.get() == State.DISABLED
                    ? recipe.get().getDisabledOpacity() : 1.0F, motionRoot::setOpacity, duration);
}
BackdropBinding backdrop = new BackdropBinding(node);
rt.bindComputed(() -> recipe.get().getBackdrop(), backdrop::setBase);
rt.__bindAnimatedFloat(() -> surface.get().getLensFactor(), backdrop::setFactor, duration);
```
**motionRoot 所有权契约（源码 javadoc 原文）**：「motionRoot 非 null 时其 transform/opacity 也归本绑定」；「motionRoot 归本绑定管理，业务内容应挂在其后代，保留自身属性写入权」。不传/null 的让位路径均有显式注释（Dialog/Toast 出入场动画独占、Slider thumb 布局定位独占）。审查者 A 建议将该 ownership 概念升格为全节点统一术语（SurfaceOwnership/MotionOwnership/…），列 §6.5。
生命周期：绑定注册于当前 `Owner`，随作用域释放；无 Owner 时 `__runRoot` 兜底（该兜底路径曾致 P-01 观察，已修）。**订阅释放/换主题动画连续/重复绑定去重的测试证据在套件内，未随包附源码，外审按「项目方报告」处理（§8）。**

### 3.4 材质角色（契约 §2.3 与源码逐字一致）
```java
public enum Role { PANEL, TOOLBAR, INPUT, GROUP, OVERLAY, INDICATOR,
                   BUTTON_STANDARD, BUTTON_PRIMARY, BUTTON_DANGER }
```
PANEL 页面主面板｜TOOLBAR 工具栏/导航底座｜INPUT 输入表面｜GROUP 分组底座｜OVERLAY 浮层｜INDICATOR 小尺度指示｜BUTTON_STANDARD/PRIMARY/DANGER 按钮三档。状态样式由 `darkSurface/lightSurface` 工厂从 idleTint/edge alpha 基推导（dark hover=+6/edge+0x40，pressed=−2/lens 0，disabled=0x06/opacity .65）。`solidDark()`/`withoutBackdrop()` 承担「无滤镜替代档」职责——A 建议将其显式化为材质模式枚举（LIQUID_GLASS/OPAQUE/REDUCED）并防止 `SceneSurfaceStyle` 继续膨胀为万能配方对象，列 §6.5；v2.1 同意「Role 有 material-semantic 与 action-semantic 混装趋势（BUTTON_*）」的观察，`SurfaceRole×ActionVariant` 拆分为待裁决提案。

### 3.5 关键裁决
- 虚拟化复用行轻量档（契约 §4.1）：`0x592A1E68`（选中）/`0x1FFFFFFF`（hover）/edge=0/零 BACKDROP；非虚拟化页面级行不受约束。
- 语义色公共入口四枚（P02）：`danger()` 底图专用禁文本（M1 实算 1.14/1.15:1）；`errorText()` 文本级；`successText()` 委托 `accent()`——论证重建见 §4.4。
- 契约 §7 明令「禁止用 `color == SceneChromeTokens.TEXT_PRIMARY` 推断调用意图」等反嗅探条款。

### 3.6 兼容入口的能力边界（v2.2，回应审查者 A「零使用 ≠ 零能力」追问）
对 L-C 兼容入口（`SceneControlChrome` 绑定 helpers、`SceneStateColors`、`SceneChromeTokens.apply*`），三个安全性问题的实证回答：
| 问题 | 答案 | 依据 |
| --- | --- | --- |
| 能否创建 surface producer？ | **能**（定义体即含 `rt.__bindAnimatedColor(…, node::setBackgroundColor)`，§3.1 零点表 REF 行所示内部链完整） | 源码实证，非声称 |
| 能否绕过 `SceneSurfaceBinder`？ | **能**——对宿主显式调用的节点绕过配方通道，直写静态 token 源 | 同上 |
| 能否产生长期订阅？ | **能**——helpers 在调用方当前 `Owner` 下建立 effect（生命周期随作用域，语义与 binder 同规则） | Chrome javadoc I4/PAINT 级说明 |
**处置**：该能力是兼容承诺的**本体而非疏漏**（宿主未迁移代码调用时行为不变=契约 §6「保留为公共兼容入口」）；风险面因此收敛为「宿主主动调用静态通道，其表面不随主题切换」——这是**已声明语义**（`SceneStateColors` 类 javadoc「纯静态查表，非主题引擎」）。是否降级（`@Deprecated`/改委托 binder/收窄签名）属**退役路径治理**，登记 §6.5-B 供裁决，本轮不动（动=破坏兼容承诺）。
**第四轮签署记录（审查者 A，基于 c389ac42 独立核验）**：签「L-C：旧 selectable-background producer 在 c389ac42 中零生产使用/零生产引用；旧实现能力仍作兼容本体保留」；**不签**「全库不存在旧能力」或「所有 surface setter 只能来自 binder」两个偷换版本（v2.2/v2.3 现文措辞已与其限定语一致）。同轮逐节点核验结论：5 个虚拟化/动态行样本均为「外壳/viewport=binder、行/cell=轻量槽（binder 不管理）、icon/label=图像/文本协议」的分层，**无同节点双写证据**；`SceneVirtualGrid` 的 `icon` cornerRadius/placeholder 写入经其甄别为物品图像占位协议非 surface 通道竞争。

## 4. 证据与验收方法

### 4.1 测试与构建
每实例：定向测试 → 变异 ≥2 红 → 全量 build → 白名单提交 → merge → 主树重跑（两次实证拦截分支盲区）。**边界（自认）**：变异是过程门槛非覆盖度量；全绿支持「高强度迁移与回归已实施」，不自动构成「所有路径充分证明」。补证：覆盖矩阵/未杀变异清单/构建报告/跳过项身份（§8）。

### 4.2 主题跟随证明口径
① 差异主题下验依赖跟随 + 默认档分别断言预期值（合法同值允许）两类拆分；② 撞色假阴性→几何配对计数法；③ 负向对照四项（显式不变/覆盖隔离/清除恢复/销毁断流）存在于套件；④ **写序吞没（P-03）**：binder Effect 吸收绑定后直接写 → 该模式用源码守护替代行为断言。**守护效力边界（自认）**：正则守护防「已知文件已知形式」再现，不证「无别名/间接/新增路径」；`defaultDark()` 钉数冻结数量不冻结用途。正面结构保证：契约 §4 属性表 + §10.1 唯一通用实现 + 兼容入口生产调用点 0（v2.1 新增实证行）。

### 4.3 数值纪律（v2.1 强化）
历轮代理数字出现 5 类错误 + **v2 自身 2 类（F-01/F-02：手抄未经脚本）→ 固化纪律：审计文档数值表只能由可执行脚本输出直接嵌入，模型与输入随数字同档**。

### 4.4 关键 WCAG 结论（全部指向附录 A 脚本输出）
- 正文/次要/错误/警告（dark 档 M1）：8.84 / 6.70 / 6.72 / 6.84:1 过 AA；`danger` 1.14/1.15:1 → 禁文本（比 v1 的 1.43 判据更强）；`disabled` 2.51/2.73 豁免口径。
- **successText 委托 accent 的重建档（v2.1，F-01 后）**：M1 域内 `#A5D6A7` **确实双档通过（6.95/4.57）**——v2「候选域无解」错。但 M2 显示其对纯白世界背景仅 **1.64:1**：玻璃正文色的合格域是「名义底 ∩ 世界包络」的交集，浅绿在 light 档亮世界场景塌方，**对完整声明域仍无稳定解**；且引入分档绿 token = 扩契约 §5 起步色板（治理决策，用户保留权）。故委托 accent 的**妥协继续成立，但论证由「无解」降格为「包络内无稳定解 + 治理未批准」**。真实使用面（色点/短反馈）可读性列入真机项。
- **M2 对称包络**（升为默认启用门槛，§6.1 第 0 条）：dark 档 over 纯白正文 1.28:1 / light 档 over 纯黑 1.36:1——玻璃是 `default rendering material policy` 而非 `default color palette`（A 语），正确性条件部分转嫁给宿主背景，必须以门槛约束。
- **行态数值链复核（v2.5）**：主代理重跑脚本，8.84/6.70/6.72/6.84/1.14/1.15/2.51/2.73/1.22/1.16 与 success 五色、CR 三值、ΔL 三值、fg 6.08/9.69、M2 四值**全部逐位一致**；另坐实**一处脚本溯源缺口**：本节 `onAccent-over-selBg 7.23|6.44` 此前**不在脚本输出内**，属数值纪律「脚本直出」的漏洞（数值本身经独立重算正确：dark 7.2322 / light 6.4412；light 档用 `selFg 0xFFFFFFFF`，非 `onAccent 0xFFEADDFF` 的 4.9950）。修法已执行：补全脚本 `auditA_v25_full.py` 现直出 `dark selBg #4F378B CR=7.2322 / light selBg #6750A4 CR=6.4412`，与文档一致。
- 虚拟化行态（dark，M1 底）：CR selected/idle **1.10**、selected/hover **1.59**、hover/idle **1.45**；ΔL%（统一口径 `(L_A−L_B)/L_B`）：selected−idle **−19.1%**、selected−hover **−59.4%**、hover−idle **+99.4%**（v2 所报「+129%」为手抄错，作废）；fg 于行底 9.69/6.08 ✓。**行态可辨性主要靠色相+选中变暗方向，真机判据用 CR 三值，不再使用无单位「ΔL<2」表述（F-02 采纳）。**

## 5. 平台发现与处置

（P-01~P-07、P-09 同 v2 不重复；此处仅列修订行）

| # | 状态修订（v2.1） |
| --- | --- |
| P-05 | 同 v2：提案采纳、实施排后；真机判暗边不可接受则先修复再发布；「提案≠完成」 |
| **P-10** | **数值整改闭环于 v2.1**：F-01（绿候选表，M1 有解 #A5D6A7，结论重建）、F-02（ΔL 定义与 +129% 手抄错）坐实并订正；根因确认：**v2 附录 A 主表为脚本输出，但候选绿表与 ΔL 行编写时手抄自旧报告未重跑**（B 拒绝归因「旧模型」是对的）；纪律固化见 §4.3 |
| **P-11** | **（v2.1 新增）GitHub 远端 4.0 快照落后本地全部施工（未 push 为规范行为）**：外部审阅者经远端链接看到的是施工前代码（SceneButton 直写、Chrome 生产路径等），产生系统性误判风险。处置：外审一律以自包含审计包或主代理代查现行代码为准；如需远端审，由用户决定是否 push |

## 6. 剩余风险与放行门槛

### 6.1 真机验收
0. **默认启用门槛**（M2）：宿主典型背景下正文/辅助/错误文本、状态可辨识、控件边界、禁滤镜退化的判据**先冻结后测量**；不可读则默认启用阻塞（回退=宿主显式 `solidDark()`/opaque 基/inter-layer 设计路线）。
1. 行态可辨性（CR 1.10/1.59 实测，§4.4）。2. 世界背景包络复核。3. Container 边缘暗边（P-05 判定输入）。4. 聊天材质档/按钮 alpha 微差/`0x26FFFFFF` 撞色/HUD 缩放。5. GROUP 行圆角带、NavRail 把手、Toast 借道 CARD、thumb alpha、**successText 借道面**。6. Motion 180ms 体感。7. 降级环境可读性。8. **性能方案先冻结**：活跃滤镜数（运行时）、处理面积、帧耗时、双后端、降级路径；判据阈值验收启动会定档，禁止事后口径。

### 6.2 静态覆盖账目（347 闭合，口径=正则静态扫描生产 src/main）
主题/信号驱动 141（128+12+1）＋非 surface 域 108＋既有护栏 76＋`defaultDark()` 直读 20＋契约补录 2 ＝ **347**。互斥性：判定顺序首命中。**域限定**：只覆盖已知写入形式；「347 闭合 ≠ surface 架构闭合」——A 的升级意见采纳为 P 级事实陈述：真正应收敛的是「property → 合法 producer → 契约 → 允许上下文 → 测试」五元组（§6.5 提案），当前由契约 §4 属性表 + 调用点 0 实证部分满足。

### 6.3 Filter 预算（结构约束声明）
契约各页上限（面板≤2/导航≤3/列表≤6/聊天≤8/HUD≤2/全页≤16）为**结构预算**；静态声明数≠运行时滤镜数，性能结论全部指向 §6.1 第 8 条。

### 6.4 已知架构级张力（自认清单）
双通道兼容面大（67 调用方、8 旧入口）靠护栏钉数+守护（脆弱性自认）；观感基线源自未经独立评审的 playground；`defaultDark()` int 常量稳定性是契约外依赖；HUD 主题桥未提供；successText 借道未关闭；**surface producer 权限目前契约文档级而非类型级（§3.1）**。

### 6.5 登记待裁决提案（外审 A 三轮迭代后的优先级，待用户裁决）
**A. 建议优先落地：Producer Ownership Contract**（A 第三轮收缩版：非新框架，「现有 binder 契约的测试化表达」）——`属性 → 合法 owner(L-A/L-B 分区) → 正向测试 + 非法竞争静态守护 + 合法多所有者 precedence 测试` 五元组表；运行时测试仅四项（主题切换表面更新/状态切换/动画中途换主题连续性/Owner dispose 断流），成本评估为低。**【第五轮：形态经 A 收口锁定，待用户立项裁决】**A 交付 `SceneSurfaceOwnershipContractTest` JUnit4 骨架（Java 8、零新机制、不改 Owner/Effect/SceneNode）+ precedence 正式四元组 **P1 显式>局部 / P2 局部>runtime / P3 runtime>库默认 / P4 显式配方在主题切换下保持恒定（把优先级与跟随性一并钉死）** + 静态守护 RULE-1~4（RULE-4 明确**不用正则强证「所有 setter 归 binder」**，而以「新 producer 必须登记为 binder/轻量槽例外/兼容豁免」的注册制替代，防误杀占位图/scrim/HUD 等合法例外族；双 binder 竞争定义非法程序状态、归静态审查而非 runtime 契约）。骨架中部分构造/API 名（`new SceneRuntime()`、`interactionState`、`SceneThemes.DEFAULT`、probe getter）需按实际 test harness 适配——立项时由施工实例对齐。**A 定义的下一轮评审焦点：测试实现是否真的证明了这些契约，而不是把契约重新写了一遍。**
**B. 已完成（v2.2）：Legacy Producer = 0 双层证明**——API 级零点表（§3.1，含语义 REF 归属披露）+ 语义级「selectable-background 全部生产解析到 L-A/L-B」；「零能力」追问的答案=§3.6「零使用、能力保留属兼容本体」，退役路径（@Deprecated/委托化）单独待裁决。
**C. 保留采纳：Ownership 统一术语**（Surface/Motion/Content/Interaction Ownership 文档化，契约/审计包/javadoc 三处对齐；纯文档成本）。
**D. 暂缓：`SurfaceRole × ActionVariant` 拆分**（A 自行从「应重构」降级为「观察项」：现九 Role 兼容成本近零、BUTTON_* 膨胀信号未出现；膨胀时以 `surfaceRole=BUTTON × variant` 分解，届时再议）。
**E. 轻量吸收：材质模式概念分层**（先在 factory/概念层区分 glass recipe / opaque recipe / no-backdrop derivative，不扩 public API；防 `SceneSurfaceStyle` 万能化）。
另保留：Reference Renderer 命名治理（SceneButton javadoc 已有「试金石/参考实现」表述，A 建议升级为规范术语防模仿扩散）。**以上均不阻塞真机验收。**

## 7. 附录 A：WCAG 复算（v2.1 全脚本输出；v2.6 工具链随仓 `tools/audit/`，可执行复现）

**合成规则声明**（B 要求项）：src-over 在**编码 sRGB 通道**线性插值后四舍五入取整（近似渲染器逐通道混合，非线性光空间）；亮度用取整后整数 RGB；**判定用未舍入值**。
**M1**＝tint over `FALLBACK_BG 0xFF2B2930`（宿主垫 opaque 基场景）；**M2**＝tint over 纯白/纯黑（无垫底世界包络）。M1 dark 底 `#3A3940`（L=0.041960）/ light 底 `#555459`（L=0.089932）。
输入（`SceneTheme.java` 行号）：dark fg `0xFFE6E1E5` muted `0xFFCAC4D0` disabled `0xFF79747E` onAccent `0xFFEADDFF` accent/selBg `0xFF4F378B` selFg `0xFFEADDFF` danger `0xFF7F1D1D` errorText `0xFFFFB4AB` warningText `0xFFFBBF24` PANEL tint `0x14EAF7FF`（L343-361）；light fg `0xFF1C1B1F` muted `0xFF49454F` disabled `0xFF9E9AA7` onAccent/selFg `0xFFFFFFFF` accent/selBg `0xFF6750A4` danger `0xFFB3261E` errorText `0xFF8C1D18` warningText `0xFF8B5000` PANEL tint `0x33FFFFFF`（L218-235）。

**证据矩阵（v2.6：从「承诺清单」转为「声称 → 可执行复现」）**

| 声称 | 复现方式（随仓可执行） | 结果 | 门禁 |
| --- | --- | --- | --- |
| 附录 A 数值链 | `python tools/audit/auditA_v25_full.py` | 89 个数值 token 现输出 | 反向覆盖：§7 每个数字必须能在现输出中定位 |
| 关键数值基准 | `python tools/audit/check_numeric_traceability.py` | 11 浮点 + 9 色值与脚本现输出一致 | 基准不一致即红灯 |
| Legacy 零点（L1/L2/L3） | `python tools/audit/audit_zero_proof.py` | 兼容入口 REF=0，3 处语义 REF 已披露归属 | 人工复核（外审 A 已独立核验） |
| surface sink 归属 | `python tools/audit/surface_sink_ownership.py --summary` | `src/main` 60 个 sink 条目 / 65 处写入，全部登记类别 | **注册制守护**：SurfaceSinkOwnershipRegistryTest 未登记即红灯 |
| 全量测试基线 | `gradlew test` + XML 统计 | 5155 / 0F / 0E / 6 skip（v2.5 实测） | 构建链 |

**随仓工具链**：`tools/audit/repo_paths.py`（路径不写死）、`auditA_v25_full.py`（数值直出）、`audit_zero_proof.py`（零点三层）、`check_numeric_traceability.py`（数值门禁）、`surface_sink_ownership.py`（sink 扫描）、`numeric_baseline.json`（数值基准）。
脚本输出（原样嵌入）：
```
主表(dark|light)：fg 8.84|2.28  muted 6.70|1.25  errorText 6.72|1.21  warningText 6.84|1.16
danger 1.14|1.15  disabled 2.51|2.73  accent 1.22|1.16  onAccent-over-selBg 7.23|6.44
（light 档对 M1 深色垫底数值为模型 artifact：light 档设计用于亮背景，见 M2）
success 候选域（M1 双档，判定未舍入≥4.5）：
#00E676  6.840137|4.495154  both_pass=False (vs纯白1.67)
#A5D6A7  6.946799|4.565249  both_pass=True  (vs纯白1.64)   ← M1 域内有解（v2 错误订正）
#66BB6A  4.828863|3.173399  both_pass=False (vs纯白2.36)
#4CAF50  4.107850|2.699568  both_pass=False (vs纯白2.78)
#006C34  1.736964|1.141486  both_pass=False (vs纯白6.57)
虚拟化轻量档（M1-dark 上叠）：idle #3A3940 | hover #525157 | selected #34304E
CR sel/idle=1.095536  CR sel/hover=1.592420  CR hover/idle=1.453553
ΔL%(A−B)/B：selected−idle −19.1  selected−hover −59.4  hover−idle +99.4
fg(0xFFE6E1E5) on hover 6.08 | on selected 9.69
M2 包络：纯白→darkPANEL #FDFEFF fg 1.28（✗）/ lightPANEL #FFFFFF fg 17.13（✓）
        纯黑→darkPANEL #121314 fg 14.41（✓）/ lightPANEL #333333 fg 1.36（✗）
```

## 8. 补证清单（应外审要求，承诺提供非声称完成）
构建报告与提交/环境关联（台账 L13 已存本地 artifact 指针；随包与否由用户决定）；**6 跳过项身份（v2.5 已实证闭环）**：`LatexReferenceComparisonTest.generateReferenceComparison` / `.referenceRendersAllShowcaseFormulas`（需外部参考 jar，`AssumptionViolatedException`）+ `C9DualFontProbeTest` 四项字体探针（`probeFontActuallyActive`/`codeBackdropUnderProbeFont`/`quoteBarUnderProbeFont`/`listContinuationUnderProbeFont`）；「控件×renderer×兼容入口×验证性质」矩阵；「控件×renderer×兼容入口×验证性质」矩阵；未杀变异清单（如存在）；生命周期测试源码节选；`SceneTheme/ThemeTokens` 全量色值表；（v2.1 新增）附录 A 脚本原件（v2.6 已随仓：`tools/audit/`，含数值门禁与 sink 注册表工具）。

## 9. 术语速查
`runtime`/`Owner`/`配方`=SceneSurfaceStyle/`relief`=圆角带缘高光+投影/`tint/edge/lensFactor`/`portal`/「收编」「销账」「同源同值」「双通道」；**M1/M2=附录 A 声明模型**；**producer=上游材质生成器；write-sink=SceneNode setter**；「兼容入口」=保留 API 且生产调用点 0。

---

**给评审者的一句话（v2.1）**：数值链自本版起脚本化闭环；架构叙述与现行代码的偏差（含审查者 A 因远端快照造成的误判）已在 §3.1/P-11 显式对账。请攻击方法与仍未闭环处（§6.1 门槛判据、§6.5 未裁决提案、§8 补证），勿再引用 GitHub 远端 4.0 作为现状证据。
