# 全库默认液态玻璃样式：逐实例台账

> 仅主代理更新。字段口径见[任务单](任务单-全库默认液态玻璃样式.md) §3。状态取值：未开始 / 进行中 / 代码完成 / 已验收 / 待验。
> 契约与白名单见[G00 冻结契约](契约-全库默认液态玻璃样式.md)。

## 基线

| 项 | 值 |
| --- | --- |
| 分支 | `4.0` |
| 定案基线提交 | `4549c8dc`（规划文档提交；规划自述的 `8963f48a` 为源码基线，差异仅文档） |
| 历史测试证据 | 4685 tests / 0 failures / 0 errors / 6 skipped（规划基线时点构建；`build/test-results/test` 目录 XML 已被后续复跑覆盖，原始 artifact 不再可直接复核） |
| 最近 merge 后验证 | `7aeedc21`（G19/P02 语义色收编并入后）主树完整 `gradlew.bat build` exit 0：479 测试类 / 5155 tests / 0 failures / 0 errors / 6 skipped（主代理实跑，`qz_test_tally.py` 统计）。审查复核（2026-09-11）：审查主代理于 HEAD `86a1bbe5` 再次实跑完整 build，同口径 479/5155/0/0/6 逐值复现；现存 XML 为该次复跑产物（mtime 集中于复跑时刻，属 Gradle 正常批量写回） |

## 任务族状态

| 任务 | 状态 | 负责人 | 依赖提交 | 备注 |
| --- | --- | --- | --- | --- |
| G00 契约与清单冻结 | 已验收 | 主代理 | `8e669f79` | 交付 `docs/开发者文档/契约-全库默认液态玻璃样式.md` |
| G01 主题基础与作用域 | 已验收 | 主代理（基础负责人） | `0a4484e5` | 完整 build 通过；GPU/宿主未执行 |
| G02 通用玻璃表面与状态配方 | 已验收 | 主代理（基础负责人） | `0a4484e5` | 完整 build 通过；GPU/宿主未执行 |
| G03 普通按钮默认接入 | 已验收 | 主代理（参考实现） | `0a4484e5` | 完整 build 通过；GPU/宿主未执行 |
| G04 默认前景与输入框族 | 已验收（Label/TextInput/TextArea/Autocomplete） | 每实例小模型 | `fcf6bf2c` | 见各实例剩余问题 |
| G05 开关与选项族 | 已验收（共享配方 + Toggle + Checkbox + RadioGroup） | 每实例小模型 | `a826d584` | 含主题统一调参 |
| G06 滑块 | 已验收 | 小模型 | `3dbf643b` | 含一次既有测试顺序耦合观察项 |
| G07 滚动条与滚动外壳 | 已验收（Scrollbar + ScrollContainer） | 每实例小模型 | `a5438bee` | |
| G08 选择框 | 已验收 | 小模型 | `a5438bee` | |
| G09 导航族 | 已验收（Segmented/Tab/NavList/Breadcrumb） | 每实例小模型 | `dc0696bc` | |
| G10 浮层族 | 已验收（Dialog/ContextMenu/Tooltip/Toast） | 每实例小模型 | `a1162e93` | |
| G11 表单模板族 | 已验收（ThemeBridge/PageShell/FieldShell/LabeledControl/ActionBar） | 每实例小模型 | `55edefeb` | `StructuredListFieldRenderer:229` 与 `ConfigScreen.createActionBar` 留给 G15 |
| G12 数据编辑族 | 已验收（SimpleList/DataTable/拖拽把手/ObjectField/KeyValueMap） | 每实例小模型 | `7f66493b` | KeyValueRow 不派发；DataTable 无增删按钮（在其余实例销账） |
| G13 搜索配件族 | 已验收（CategoryNavPane/MemberGrid/SearchResultList/PickerInfoBar/VariantChooser 5/5） | 每实例小模型 | `881e3f30` | 整合证据由 G14 补充 |
| G14 选择器与虚拟网格 | 已验收（PickerPanel/VirtualGrid 2/2） | 每实例小模型 | `47b74d80` | 五配件集成证据齐；Nav 双文件零接缝判定；G13 备忘的宿主层次叠加已消化 |
| G15 配置页迁移 | **已验收（12/12 收官：Theme/Shell/Support + 8 Renderer + 收口）** | 每实例小模型 | `b71af0fd` | binder 签名稳定；main 侧 asFormTheme 消费清零仅剩方法本体（§4.2）；剩余校准全归 G19 |
| G16 Playground 页面迁移 | 已验收（外壳/公共构件 + 9 页全部，11/11） | 每实例小模型 | `663bf691` | 保留样本清单见各实例行与契约覆盖表 |
| G17 聊天/HUD 局部配方衔接 | 已验收（Input/Container/Bubble/Toolbar 4/4） | 每实例小模型 | `0efa493f` | 聊天族材质档（DARK_THIN/DARK_REGULAR/alpha 递进）与两族按钮 hover 档微差归 G19 校准；effect 水位见 P-06；**G19 预扫补注**：`ChatToolbarIcons`/`ChatMarkdownContent` 等未名文件按 §7.3 位图图标协议+Bubble 家族口径**同族零改动销账**，`SceneHudHost`/`UiHudRenderListener` 两处已补契约 §7.3 条目 |
| G18 GlassLab 外壳迁移 | 已验收（代码完成；真机三档对照复测归 G19） | 小模型 | `dca4a89d` | 不扩口 PlaygroundKit 判定成立；ROOT_BG 承托底在册；LIGHT 撞色发现与几何配对法入 G19 校准 |
| G19 集成与最终验收 | **代码收编与验收记录完成；整体状态=待验（真机清单未执行）** | 主代理＋用户/CI 宿主验收 | `86a1bbe5` | 见《验收-全库默认液态玻璃样式》；GPU/Minecraft 真机验收全线未执行待用户环境 |

## 逐实例记录

（按派发顺序追加；未执行项写“未执行”，不得空白默认为通过。）

| 实例 | 白名单 | 依赖提交 | 负责人 | 代码状态 | build 证据 | GPU/宿主 | 交付提交 | 剩余问题 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| G00 契约与清单冻结 | `docs/开发者文档/契约-全库默认液态玻璃样式.md`（纯文档，不改生产代码） | `4549c8dc` | 主代理 | 已验收 | 纯文档免 build（AGENTS.md 授权口径）；链接/编号经 `uilib_glass_plan_check.py` 校验 | 不适用 | `8e669f79` | 施工期两处补录（§4.1 G13、§7.3 HUD）已并入，冻结口径未变 |
| G01 主题基础与作用域 | `ui/reactive/Owner.java`；新增 `ui/scene/theme/{SceneSurfaceStyle,SceneTheme,SceneThemes}.java`；测试 `OwnerScopeTest`、`SceneThemeTest`、`SceneThemeScopeTest` | `8e669f79` | 主代理 | 已验收 | `gradlew.bat build` SUCCESS（含 checkstyleTest） | 未执行 | `0a4484e5` | 视觉参数待 G19 集中校准 |
| G02 通用表面与状态配方 | 新增 `ui/scene/theme/SceneSurfaceBinder.java`；测试 `SceneSurfaceBinderTest` | `0a4484e5`（同批） | 主代理 | 已验收 | 同上 | 未执行 | `0a4484e5` | 同上 |
| G03 普通按钮默认接入 | `ui/scene/control/SceneButton.java`、`SceneLiquidGlassStyle.java`；测试 `SceneButtonTest`、`SceneButtonKeyboardPressTest`、`LwjglButtonKeyboardFeedbackTest`、`SceneDialogTest` | `0a4484e5`（同批） | 主代理 | 已验收 | 同上 | 未执行 | `0a4484e5` | 同上 |
| G04/Label | `SceneLabel.java`、`SceneLabelTest.java` | `a3a0267b` | 子代理（主代理复核） | 已验收 | 定向 35 项全绿 + 完整 build exit 0（4727 tests / 0 fail） | 未执行 | `ef1bc3d0` | 用 `rt.bind(SceneThemes.foreground)` 与契约字面「bindComputed」措辞不同但语义等价，已确认接受 |
| G05/Toggle | `SceneToggle.java`、`SceneToggleTest.java`、`BooleanFieldRendererTest.java`（主代理授权的单行下游断言更新） | `a3a0267b` | 子代理（主代理复核） | 已验收 | 定向 11 项全绿 + 完整 build exit 0（4727 tests / 0 fail） | 未执行 | `86370785`（merge `1207334e`） | ① INDICATOR 圆角待改胶囊 ② 选中 tint alpha 待提高可读性 ③ thumb 待恢复 hover/pressed 反馈（改 `accentSurface` 后由主代理统一处理） |
| G04/TextInput | `SceneTextInput.java`、`SceneTextInputTest.java` | `a3a0267b` | 子代理（主代理复核） | 已验收 | 定向 70 项全绿 + 完整 build exit 0（4736 tests / 0 fail，含 init-script 绕行记录） | 未执行 | `68d7fb66`（merge `97bc067d`） | 聚焦两通道区分：表面缘色=配方 focusEdge、caret=主题 borderFocus，后续输入实例不得混用 |
| G05/Checkbox | `SceneCheckbox.java`、`SceneCheckboxTest.java` | `efc6263d` | 子代理（主代理复核） | 已验收 | 定向 12 项全绿 + 完整 build exit 0（主代理实跑） | 未执行 | `e3a06a2f` | 选中 tint alpha 待提高可读性（同 Toggle） |
| G05/RadioGroup | `SceneRadioGroup.java`、`SceneRadioGroupTest.java` | `1207334e` | 子代理（主代理复核） | 已验收 | 定向 11 项全绿 + 完整 build exit 0（主代理实跑） | 未执行 | `4c1387fd`（merge `7afa1062`） | 禁用时选中项不显示 dot（裁决：接受，禁用整体降级）；**G19 预扫补注**：L166-168 option 行容器静态缘色三件套为代码注释+`SceneRadioGroupTest:420` 反向钉双固化决定（BORDER_DEFAULT 与主题同值，P-04 口径），**裁决在册维持不主题化** |
| G05/共享选中配方 + 主题调参 | `SceneTheme.java`、`SceneThemes.java`；三个选中族测试断言 | `7afa1062` | 主代理 | 已验收 | 定向 34 项全绿 + 完整 build exit 0 | 未执行 | `a826d584` | INDICATOR 圆角改胶囊、选中强度 0x59、新增 accentSurface |
| G04/Autocomplete | `SceneAutocomplete.java`、`SceneAutocompleteTest.java` | `a826d584` | 子代理（主代理复核） | 已验收 | 定向 9 项全绿 + 完整 build exit 0 | 未执行 | `7a243e91` | 候选行覆盖 alpha：高亮 0x59 > hover 0x1F |
| G04/TextArea | `SceneTextArea.java`、`SceneTextAreaPrimitive.java`（授权的可空信号颜色）、`SceneTextAreaTest.java` | `a826d584` | 子代理（主代理复核） | 已验收 | 定向 71 项全绿 + 完整 build exit 0（4750 tests） | 未执行 | `162b3b62`（merge `fcf6bf2c`） | ①选区高亮仍走旧 `SELECTION_*` token（primitive 仅授权 4 个信号，需补授权）②hover/pressed 对 root 表面不可达（interaction 挂 content，既有接线）③viewport 改透明（避免第二层表面挡死玻璃） |
| G07/ScrollContainer | `SceneScrollContainer.java`、`SceneScrollContainerTest.java` | `fcf6bf2c` | 子代理（主代理复核） | 已验收 | 定向 16 项全绿 + 完整 build exit 0 | 未执行 | `0ddf105b`（merge `3dbf643b`） | 显式透明底座无表达通道（既有 int 缺省限制，裁决接受） |
| G06/Slider | `SceneSlider.java`、`SceneSliderTest.java` | `a826d584` | 子代理（主代理复核） | 已验收 | 定向 18 项全绿 + 完整 build exit 0（3 次复跑） | 未执行 | `ca463fc6` | 观察项：一次 `SceneAutocompleteTest` 失败疑为既有测试顺序耦合（同源码复跑 3 次通过、单跑通过、与 G06 无因果） |
| G07/Scrollbar | `SceneScrollbar.java`、`SceneScrollbarTest.java` | `fcf6bf2c` | 子代理（主代理复核） | 已验收 | 定向 48 项全绿 + 完整 build exit 0（4764 tests） | 未执行 | `b3764565`（merge `5dfd96e0`） | `DEFAULT_*_COLOR` 改为主题哨兵 `THEME_COLOR`（语义=跟随主题）；旧 `SCROLLBAR_THUMB_*` 保留兼容 |
| G08/Select | `SceneSelect.java`、`SceneSelectTest.java` | `3dbf643b` | 子代理（主代理复核） | 已验收 | 定向 30 项全绿 + 完整 build exit 0 | 未执行 | `86381424`（merge `a5438bee`） | 无显式配方入口（既有 API 限制）；打开中禁用不自动收起属 primitive 既有语义 |
| G09/Segmented | `SceneSegmented.java`、`SceneSegmentedTest.java`、`ChoiceFieldRendererTest.java`（授权单行断言） | `3dbf643b` | 子代理（主代理复核） | 已验收 | 定向 19 项全绿 + 完整 build exit 0（4794 tests） | 未执行 | `ea096015` + `4fa1f68f` | 文字取 `foreground`（WCAG 验算：白字在 0x59 强调底上仅 1.71:1） |
| G09/Tab | `SceneTab.java`、`SceneTabTest.java` | `5dfd96e0` | 子代理（主代理复核） | 已验收 | 定向 18 项全绿 + 完整 build exit 0（4782 tests） | 未执行 | `5dd61281` + `0568c3f8`（merge `efc7c26c`） | 选中文字按「0x59 叠 TOOLBAR 底」WCAG 择色；既有注释算术瑕疵未动 |
| G09/NavList | `SceneNavList.java`、`SceneNavListTest.java` | `a5438bee` | 子代理（主代理复核） | 已验收 | 定向 13 项全绿 + 完整 build exit 0 | 未执行 | `6ab321e5` + `f9281c8d`（merge `e6bc6f6b`） | 选中文字取 foreground（浅色档 onAccentForeground 仅 1.89:1） |
| G09/Breadcrumb | `SceneBreadcrumb.java`、`SceneBreadcrumbTest.java` | `ea096015` | 子代理（主代理复核） | 已验收 | 定向 11 项全绿 + 完整 build exit 0（4809 tests） | 未执行 | `dc0696bc` | 例外：不装表面/滤镜（无选中态语义），契约 §4.1 已单列 |
| G10/Dialog | `SceneDialog.java`、`SceneDialogTest.java` | `efc7c26c` | 子代理（主代理复核） | 已验收 | 定向 26 项全绿 + 完整 build exit 0（4799 tests，含变异检查） | 未执行 | `4bb0dfec`（merge） | 换行宽与配方 borderWidth 耦合（既有静态布局限制）；正文 mutedForeground 绕行待 Label 前景信号接缝 |
| G10/ContextMenu | `SceneContextMenu.java`、`SceneContextMenuTest.java` | `e6bc6f6b` | 子代理（主代理复核） | 已验收 | 定向 18 项全绿 + 完整 build exit 0（4808 tests） | 未执行 | `82b59063`（merge） | 面板缘色取 focusEdge（打开即聚焦，契约行为）；顺带修复既有 portal 泄漏 |
| G10/Tooltip | `SceneTooltip.java`、`SceneTooltipTest.java` | `1deac1ba` | 子代理（主代理复核） | 已验收 | 定向 18 项全绿 + 完整 build exit 0（4822 tests，含变异检查） | 未执行 | `2b6ec465` | 无 |
| G10/Toast | `SceneToast.java`、`SceneToastTest.java` | `519e05b5` | 子代理（主代理复核） | 已验收 | 定向 16 项全绿 + 完整 build exit 0（4822 tests，含变异检查） | 未执行 | `65b878cb` + `75619c23`（merge） | `Entry` 增 `sourceTheme` 分量（仓内无外部构造点）；SUCCESS 色点对比度待 G19 校准 |
| G11/ThemeBridge | 新增 `FormThemes.java`、`FormThemesTest.java` | `7135b534` | 子代理（主代理复核） | 已验收 | 定向 24 项全绿 + 完整 build exit 0（4833 tests，含变异检查） | 未执行 | `f978bc60`（merge） | 页底色采用「tint 不透明则用、否则回落 FALLBACK_BG」（裁决接受）；dirtyColor 对比度待 G19 |
| G11/PageShell | `FormPageShell.java`、`FormPageShellTest.java` | `3905cab3` | 子代理（主代理复核） | 已验收 | 定向 14 项全绿 + 完整 build exit 0（4844 tests，含 3 处变异检查） | 未执行 | `e868d485`（merge `96d249a4`） | ① 显式 FormTheme 路径保留静态写入者、不挂表面绑定（与 G07 同构，**裁决接受**：ConfigScreen 构建后静态覆盖遮罩，绑定会被 flush 反覆盖）② 默认视口圆角 10→16 属主题接管外观变化，非布局 ③ `FormThemes` 类头「页底色由 FormPageShell 直接写节点背景」表述待校准（G11/FieldShell 或 G19 顺手） |
| G16/Playground 公共外壳 | `TestPlaygroundHost.java`、`PlaygroundKit.java`、`TestPlaygroundHostTest.java` | `2b6ec465` | 子代理（主代理复核） | 已验收 | 定向 18 项全绿 + 完整 build exit 0（4849 tests，含 2 处变异检查） | 未执行 | `be9d4b8f` | ① `PlaygroundKit.installRuntime` **裁决保持包级**，G18 如需统一自建等价接缝或另行申请放宽 ② navBar 只做布局容器、导航底座由 `SceneSegmented` 根承担（**裁决维持**：避免第二层玻璃，契约 §4.1 一致）③ 平台级 effect 泄漏见下「平台观察」 |
| G12/SimpleList | `SceneSimpleList.java`、`SceneSimpleListTest.java` | `a1162e93` | 子代理（主代理复核） | 已验收 | 定向 35 项全绿 + 完整 build exit 0（4851 tests） | 未执行 | `f1557eaf`（merge `8f4a7339`） | ① 空态无独立节点可主题化（基线钉死空列表 viewport 子数=0，**裁决维持**不改结构）② 拖拽把手 `SceneDragReorder` 自身底色/图标仍静态色（本实例只读，另派）③ 行覆盖 alpha（hover `0x1F` / 按下拖拽 `0x33`）留 G19 校准 |
| G11/FieldShell | `FormFieldShell.java`、`FormFieldShellTest.java`（新建 19 条） | `96d249a4` | 子代理（主代理复核） | 已验收 | 定向 50 项全绿 + 完整 build exit 0（4875 tests，含 3 处变异检查） | 未执行 | `2039a6e0`（merge `26bea71b`） | ① dirty 取 accent 深色档约 1.5:1 → G19 校准 ② GROUP 嵌 PANEL 双层采样 → G19 观感确认 ③ 非交互卡片 hover 档：**裁决接受现状**（`SceneInputRouter` 只对最深命中目标写 hovered，卡片被子控件覆盖时不触发；配方 hover 仅轻微提亮），不新增「静态表面」入口 ④ `FormThemes` 类头表述 → G19 ⑤ 字段外壳粒度暂不扩控件级配方形参 |
| G16/公共文本构件 | `PlaygroundKit.java`、新增 `PlaygroundKitTest.java`（6 条） | `96d249a4` | 子代理（主代理复核） | 已验收 | 定向 55 项全绿 + 完整 build exit 0（4862 tests，含 3 处变异检查） | 未执行 | `2f15ee11`（merge `6a116979`） | ① `GlassLabHost` 未调 `installRuntime` 仍走静态回落 → **G18 补一行即可** ② effect 体内构建走静态回落（与 `card()` 语义一致，裁决接受）③ 前景切换为立即写入非 Motion 过渡（与外壳文字一致，G19 决定是否统一加动画） |
| G12/DataTable | `SceneDataTable.java`、`SceneDataTableEditorChrome.java`、`SceneDataTableTest.java` | `f1557eaf` | 子代理（主代理复核） | 已验收 | 定向 26 项全绿 + 完整 build exit 0（4859 tests，含 3 处变异检查） | 未执行 | `d26ba707`（merge `ab0aef58`） | ① 表头取 TOOLBAR：**裁决接受**（条带式薄玻璃，有 playground header 先例；嵌套观感留 G19）② EditorChrome 兼容入口保留、默认路径已不调用（§4.2 口径；`DataTableListboxChrome` 与 `SceneSelect` 私有 chrome 平行 → G19 观察是否收敛）③ 斑马纹 accent `0x14` 比旧版更淡 → G19 校准 ④ 兼容纯函数（无 rt 形参）只能取 `SceneThemes.DEFAULT`，局部主题下不代表实际外观 → G19 观察 |
| G16/OverlayPage | `OverlayPage.java`、新增 `OverlayPageTest.java`（7 条） | `96d249a4` | 子代理（主代理复核） | 已验收 | 定向 62 项全绿 + 完整 build exit 0（4894 tests，含变异检查） | 未执行 | `a3b14a22` + `29db0d7f` + 收尾 `a385847c`（merge `74783fac`） | ① 演示区 Role `GROUP`：裁决接受 ② 本页无刻意色差样本：登记 ③ 测试内 `ProbeHost` 子类桥接宿主探针：裁决接受（不改生产 API）④ 收尾已改用 `PlaygroundKit.title/hint` 默认主题路径，页内自建助手已删除 |
| G11/LabeledControl | `FormLabeledControl.java`、`FormLabeledControlTest.java`（2→9 条） | `6a116979` | 子代理（主代理复核） | 已验收 | 定向 57 项全绿 + 完整 build exit 0（4894 tests，含 4 处变异检查） | 未执行 | `86051648`（merge `59de859d`） | ① `StructuredListFieldRenderer:229` 旧三参调用点未迁移（config 包）→ **G15/StructuredList 实例切带 rt 重载**（一行）② 本类无显式颜色参数：现状接受 ③ mutedForeground 对比度 G19 ④ 契约 §4.1/§7.1 描述由主代理更新（本轮） |
| G16/HomePage | `HomePage.java`、新增 `HomePageTest.java`（4 条） | `6a116979` | 子代理（主代理复核，多实例组） | 已验收 | 定向 4 项 + devtools 全类无回归 + 完整 build exit 0（4897 tests 覆盖三页组，含变异检查） | 未执行 | `3eeae0ad`（merge `3132c2cb`） | 保留显式样本：「`· 页名`」强调标记用 `PlaygroundKit.ACCENT`——**裁决接受**（`PlaygroundKitTest` 既有锚点 + 契约 §7.3 诊断样本 + 深色档同值），已加注释与反向钉住用例 |
| G16/TextInputPage | `TextInputPage.java`、`PlaygroundTextInputPageTest.java`（2→5 条） | `6a116979` | 子代理（主代理复核，多实例组） | 已验收 | 同上 | 未执行 | `3f6d0953`（merge `3132c2cb`） | 无保留样本；`readOnlyInput.setOpacity` 为演示行为非竞争写入者（绑定器无 motionRoot 不写 opacity），登记 |
| G16/TextAreaPage | `TextAreaPage.java`、新增 `TextAreaPageTest.java`（3 条） | `6a116979` | 子代理（主代理复核，多实例组） | 已验收 | 同上 | 未执行 | `0ef8eade`（merge `3132c2cb`） | 无保留样本；pages 子包测试自建 runtime 装配（宿主探针包级不可见）→ 裁决接受，`installRuntime` 维持包级 |
| G11/ActionBar | `FormActionBar.java`、新增 `FormActionBarTest.java`（13 条） | `74783fac` | 子代理（主代理复核） | 已验收 | 定向 70 项全绿 + 完整 build exit 0（4922 tests，含 2 处变异检查） | 未执行 | `64173080`（merge `55edefeb`） | ① 扫描 3 处命中核实为非缺陷（GAP_MD/BUTTON_HEIGHT 布局常量 + 注释引用），G19 静态审查按此口径豁免 ② `ConfigScreen.createActionBar` 仍走显式 12 参路径：留 **G15/Shell** 裁决切默认重载并删后缀静态设色 ③ 条体 hover 属配方冗余能力：登记不处理 |
| G12/拖拽把手 | `SceneDragReorder.java`、新增 `SceneDragReorderTest.java`（5 条） | `74783fac` | 子代理（主代理复核） | 已验收 | 定向+下游 6 类 122 项全绿 + 完整 build exit 0（含 2 处变异检查） | 未执行 | `2296aff4`（merge `b898cf68`） | 三处取舍**裁决接受**留 G19 复核：① idle 底色=INDICATOR 配方极淡档（回退透明仅 1 行）② 圆角保留 `RADIUS_MD`（§4.2 几何常量，不用 PILL 胶囊）③ 切换保持无动画即时语义 |
| G12/ObjectField | `SceneObjectField.java`、`SceneObjectFieldTest.java`（11→17 条） | `ab0aef58` | 子代理（主代理复核） | 已验收 | 定向 17 项全绿 + 完整 build exit 0（4901 tests，含 3 处变异检查） | 未执行 | `1e77b88d`（merge `8efa5dfb`） | 10 处销账（视口 GROUP、toggle 按钮 BUTTON_STANDARD+bindForeground+bindCursor、warning 提示私有派生）；`warningText(rt)` 缺公共便捷派生 → 见平台观察 P-02；静态 set 吸收观察 → P-03 |
| G12/KeyValueMap | `SceneKeyValueMap.java`、`SceneKeyValueMapTest.java`（16→21 条） | `b898cf68` | 子代理（主代理复核） | 已验收 | 定向 21 项全绿 + 完整 build exit 0（4942 tests，含 3 处变异检查） | 未执行 | `cef6537d`（merge `7f66493b`） | ① 错误行底色=`errorText` 同强度档 0x22（旧 `SceneStateColors.errorRowBackground` 销账）：**裁决接受**，G19 统一「错误态表面」口径（DataTable/ObjectField 可复用）② `errorText/danger` 便捷派生缺失 → 并入 P-02 ③ 行不补 hover：**裁决维持**（历史无、最小改动）④ 错误行直角条带观感留 G19。既有断言仅错误行色值等强度更新（§10.1 口径） |
| G13/CategoryNavPane | `CategoryNavPane.java`、`CategoryNavPaneTest.java`（5→13 条）、`ScenePickerPanelTest.java`（**授权扩围** 2 行缘色断言改引 TOOLBAR 配方值，§10.1） | `55edefeb` | 子代理（主代理复核） | 已验收 | 定向三类 54 项全绿 + 完整 build exit 0（4939 tests，含 2 处变异检查） | 未执行 | `b938b878`（merge `4743a282`） | ① 底座 TOOLBAR 一颗 + 虚拟化行 INDICATOR 轻量零滤镜（口径已裁决并写入契约 §4.1 G13 段）② 实测新事实：Router 仅 MOVE 帧转移 hover（BUTTON_DOWN/UP 不转移），测试据此定序 ③ PickerPanel 整合证据留 G14 |
| G13/MemberGrid | `MemberGrid.java`、新增 `MemberGridTest.java`（16 条） | `8efa5dfb` | 子代理（主代理复核） | 已验收 | 定向三类 57 项全绿 + 完整 build exit 0（4959 tests，含 4 处变异检查） | 未执行 | `8b273367`（merge `66919c80`） | ① 成员卡无选择/悬停交互态（`hitTestable=false` 点击穿透到卡内按钮）→ **裁决认可保持「无选中态」**，G14 确认不补 selectableSurface ② 图像协议底/占位底/无效徽章底保留显式（§4.1/§7.3，反向钉住）③ 底座复用 G07 ScrollContainer GROUP 不重复绑定 ④ 变异 M4 曾临时触碰白名单外 `SceneScrollContainer`、零残留未进提交：知悉，后续实例以「白名单内可验证形态」设计变异 |
| G13/SearchResultList | `SearchResultList.java`、`SearchResultListTest.java`（12→18 条） | `7f66493b` | 子代理（主代理复核） | 已验收 | 定向三类 59 项全绿 + 完整 build exit 0（4978 tests，含 2 处变异检查） | 未执行 | `524b3832`（merge `84445c62`） | ① 底座选 GROUP（内嵌内容底座，OVERLAY 归 G14 宿主浮层根，避免叠第二层玻璃）：认可 ② 单元轻量 `tint(selectionBackground,0x59)` > hover `0x1F`（Autocomplete 同口径）、零 BACKDROP ③ 图像占位底/图位圆角按渲染协议保留 ④ **G14 已知悉项**：中栏 `applyPanelChrome` 外壳与本列表 GROUP 底座两层玻璃语义叠加，整合时裁决保留层次 |
| G15/Theme | `ConfigTheme.java`、新增 `ConfigThemeTest.java`（11 条）、`ConfigThemeAlignmentTest.java`（仅 javadoc） | `84445c62` | 子代理（主代理复核） | 已验收 | 定向三包 138 项全绿 + 完整 build exit 0（5012 tests，含 2 处变异检查含双路径隔离反证） | 未执行 | `6d2b4f1e`（merge `67909f09`） | ① 形态：**纯委托单入口** `asFormTheme(rt)`→`FormThemes.resolve`（避免双真相源），认可 ② **书面警示转 G15/Shell/Fields 必须遵守**：禁止 `asFormTheme(rt).get()` 快照喂显式值版 build（丢重派生+半透明 tint 画到无滤镜节点），一律改 theme-aware 无参重载 ③ ROOT_BG=0xCC111318 世界遮罩语义归 Shell 裁决 ④ 未包装页壳语义色门面（避免双入口） |
| G15/Shell | `ConfigScreen.java`、`ConfigScreenTest.java`（56→62 条） | `67909f09` | 子代理（主代理复核） | 已验收 | 定向 config.ui 全域 393 项全绿 + 完整 build exit 0（5026 tests，M1/M2 变异三类用例同时变红） | 未执行（代码已验收，GPU/宿主待 G19） | `58ce1f5a`（merge `8348626a`） | 66 处扫描逐项销账：页壳 PANEL/操作条 TOOLBAR/反馈条 GROUP 均 binder 独占；`SceneChromeTokens` 取色引用清零；**遮罩移出页壳独立成根**（像素锁+先于 BACKDROP 绘制，不挡采样）；徽标 READOUT_BG/胶囊999/描边2 按 §7.3 保留并锁进守卫；`createActionBar` 静态设色遗留（G11 台账）本次销账。裁决：① 成功态用 accent（无 success 槽，不自造色板）→ 与 P-02 同批 G19 评估 ② 新探针 `__getPageRoot()` 符合规范，认可 ③ 既有断言 2 条改引页壳探针（结构断言语义不变，§10.1） |
| G13/PickerInfoBar | `PickerInfoBar.java`、`PickerInfoBarTest.java`（3→9 条） | `66919c80` | 子代理（主代理复核） | 已验收 | 定向三类 50 项全绿 + 完整 build exit 0（4994 tests，含 4 处变异检查含 P-04 口径） | 未执行 | `788e6116`（merge `b0bfef2b`） | ① 装表面取 TOOLBAR（自写实底+圆角=有自身底表面，FormActionBar 先例；非 GROUP/非不装路线）：认可 ② `SceneChromeTokens` 5→1（仅 PAD_SM 布局常量）③ 备忘 G14：宿主 center 实底叠薄玻璃观感留整合；文本 `\n` 只显首行为既有合同 |
| G13/VariantChooser | `VariantChooser.java`、`VariantChooserTest.java`（11→17 条） | `4743a282` | 子代理（主代理复核） | 已验收 | 定向 58 项 + search 全包扩跑 102 项全绿 + 完整 build exit 0（含 2 处变异检查，主题切换用例均参与变红） | 未执行 | `de297fbd`（merge `881e3f30`） | 三项裁决：① 「恰一颗」=面板自身（viewport GROUP 归 G07 自持），全树预算 G14 协调 ② 行 hover 取 INDICATOR 配方档（0x14）——**G13 口径优先于 Autocomplete 私有 0x1F 常量**，登记 ③ 未选中/禁用圆点改透明（RadioGroup dot 同口径）留 G19。scrim 遮罩/图像协议保留；圆点 8px 几何自持；过程披露（测试文件误编辑已回滚重写）核实无残留 |
| G17/Input | `ChatInputChrome.java`、`ChatInputChromeTest.java`（7→12 条） | `663bf691` | 子代理（主代理复核） | 已验收 | 定向 input 包+设置回归 113 项全绿 + 完整 build exit 0（2 处变异检查含 P-04） | 未执行 | `675831a5`（merge `e5da4edc`） | 旧聊天设置→`LocalStyle` 配方局部覆盖（玻璃开关=backdrop null/材质、blur/lens/底色/alpha/圆角/focus/占位逐项映射；未管辖属性主题供给，双向跟随实证）；9+4 处竞争绑定销账；ChatInputBar 零改动。三裁决：① 构造期同值播种**认可**（pre-flush 几何合同逼出；binder 初值能力归 G19 评估）② elevation=0 仍走浮雕路径**接受登记**（binder 统一语义，真机观感归 G19）③ `transitionMillis=0` 保持旧立即应用**接受**（G19 若要跟随主题过渡删一行） |
| G17/Container | `ChatContainer.java`、`ChatContainerTest.java`（8→13 条） | `b0bfef2b` | 子代理（主代理复核） | 已验收 | 定向 chat3.view 15 类 222 项全绿 + 完整 build exit 0（分支上 5031，含 2 处变异检查，切换类用例参与变红）；merge 后主树首跑 1 红系本测试越界断言，见裁决② | 未执行 | `bf2e0ec9`（merge `81db5791`；§10.1 修复 `70e07489`） | 外框五项表面属性（底色/backdrop/描边色/宽/圆角）= 主题 PANEL 档基线 + 聊天玻璃设置逐项局部覆盖（开=DARK_THIN·blur·lens 设置值，关=实心令牌逃生舱）；divider/hint 按 §7.3/§4.1 保留静态并说明；ChatHudWindow、ChatGlassWiring 零改动（前者无外观写入点）。四裁决：① **绕开 `SceneSurfaceBinder`**（其 elevation≥0 必进浮雕通道，违反「大面板暗边不回归」）→ `SceneSurfaceStyle` 配方载体 + 普通绘制唯一写入者（elevation 恒 -1）：**接受**，通道统一口径归 P-05 ② 测试 L775「全树无 ROUNDED_BAND」为**越界钉**（把后代 Input chrome 合法浮雕一并禁止）：主代理 §10.1 一次性修复为只钉容器自身 fragment 先序前缀——并行模型下旧基线盲区的真实代价，G19 主树 build 兜底有效 ③ 主题兜底落 **PANEL 字段档**（设置恒在场，「整档缺席」不可构造）：**认可** ④ 聊天栈基线 effect 水位归 P-06，dispose 用例已钉「增量恒定」不再恶化 |
| G14/PickerPanel | `ScenePickerPanel.java`、`ScenePickerPanelTest.java`（28→44 条） | `881e3f30` | 子代理（主代理复核） | 已验收 | 定向宿主+search 包 130 项全绿 + 完整 build exit 0（主树复跑 5064 tests，3 处变异含主题用例参与变红） | 未执行 | `a821920d`（merge `eb0b40ce`） | ① **预裁决 1 落实**：中栏 `applyPanelChrome` 实底与成员带外壳表面写入删除（两层玻璃叠加消除，表面归 GROUP 底座各一颗，clip/PAD 布局合同保留），宿主自身恰一颗 PANEL（FormPageShell 先例）② **预裁决 3 落实**：整树 BACKDROP 构成表（主树 6 颗/变体浮层 7 颗，「声明滤镜节点数==命令数」配对证明无叠无漏），任务单「每配件≥1 集成证据」五件齐 ③ §10.1 断言更新：`selectionAreaShellsCarryOuterBorders`→`navKeeps…SurfaceRemoved`（宿主实底三断言反转为零表面断言，nav TOOLBAR 两断言原样）——**预裁决的正式落地** ④ errorText 经 `SceneThemes.resolve` 消费主题字段（SceneToast 口径），归 P-02 收编批次一并复核 ⑤ 双 Nav 文件零 diff；剩余问题无 |
| G15/Support | `FieldShellBinder.java`、`SearchPickerFieldSupport.java` + 四测试类（binder 4→8、picker 29→34、support/fontSort 各 +1 守卫） | `8348626a` | 子代理（主代理复核） | 已验收 | 定向 config.ui 全量+uilib.config.modern 37 suites 404 项全绿 + 完整 build exit 0（主树复跑 5075 tests；2 处变异含 `.get()` 快照→3 红、竞争绑定→2 红） | 未执行 | `035ef067`（merge `9d45c380`） | ① binder 切 `FormFieldShell` theme-aware 重载（零 `.get()` 快照，落实 G15/Theme 警示）+ 行触发器 INPUT 配方（§4.1 Select 先例），12 token/8 写入点销账；`FieldRenderSupport/FontSortPresentation` 实扫零外观写入主文件不改（守卫钉「非视觉职责」）：**认可** ② `theme` 兼容占位形参：**裁决=7 个 Renderer 全并入后一次收口摘除**（新登记 G15/收口 小实例，Renderer 期间禁自行摘参）③ 占位图标底 mutedForeground 全不透明浅块（旧深灰）：**接受现状**（语义正确、未自建色板），观感轻重归 G19 校准（候选 disabled/border 档）④ `issues` 摘要保持中性色：**认可**（旧行为即中性；如需 warning 档并入 P-02 批次）⑤ 六条 Renderer 衔接要点（含 StructuredList 直接切 buildBorderless theme-aware 重载、listHeight 纯 int 安全）已随 Boolean 派发传递 |
| G14/VirtualGrid | `SceneVirtualGrid.java`、`SceneVirtualGridTest.java`（15→22 条） | `eb0b40ce` | 子代理（主代理复核） | 已验收 | 定向 VirtualGrid+Nav+search+PickerPanel 152 项全绿 + 完整 build exit 0（主树复跑 5082 tests；3 处变异：回收不重置色→6 红/标签静态替代→3 红/底座回退实底→3 红） | 未执行 | `c959bba8`（merge `47b74d80`） | ① 底座按 G07 先例自持恰一颗 GROUP（OVERLAY/PANEL 归宿主层次，不叠第二层玻璃）：**认可** ② 复用单元 §4.1 轻量档（选区 0x59>hover 0x1F>禁用/透明，零滤镜/边框/圆角）+ **回收不串三向证据**（滚出清零、滚回按 key 即时重派生、换位正反双向、换代随行复用）：**认可**，为 §4.1 口径迄今最强复用正确性证据 ③ 图位圆角/占位底 0xFF454B54 图像协议不改色保留（§7.3）④ `SceneVirtualGridNav` 实扫零外观写入判定**不需要接缝**：认可，保持只读 ⑤ VirtualGrid 暂不开 Props 显式覆盖入口（契约未要求），关闭此问 ⑥ §10.1 清单为空，既有 15 例零删弱。**G14 家族 2/2 收官** |
| G17/Bubble | `ChatMessageList.java`、`ChatMessageListTest.java`（79→88 条） | `e5da4edc` | 子代理（主代理复核） | 已验收 | 定向 88+3+markdown 全绿 + 完整 build exit 0（分支上 5046；3 处变异含设置切换类 P-04）；主树复跑 5091 tests | 未执行 | `22effa3b`（merge `be2dc62d`） | ① **列表容器判定本无底表面**（背景归 Container/HUD 宿主）→ 不新增 BACKDROP：认可，与 Container 侧「chrome 子项 0 采样」互证成闭环 ② `BubbleLocalStyle/BubbleSurface` 照抄 Input LocalStyle 口径、单采样点 `sampleChatBubbleStyle()`、帧采样只重派生、`MessageBake` 仍为底色唯一每帧链（hover 3% 白/淡出逐位不变）：**认可** ③ 12 写入点销账，语义色/accent 条/引用条/markdown 渲染色全不动（§7.3）④ 材质档 DARK_REGULAR（用户既有裁决）≠契约 §5 GROUP=DARK_ULTRA_THIN，且容器 DARK_THIN/气泡 alpha 递进是否统一收口 → **归 G19 聊天族材质校准**（登记）⑤ 测试注入 setter `__setBubbleLocalStyle` 合双下划线探针惯例：**认可** ⑥ 分支名 `wt-g17-toolbar` 为历史命名误差照用（交付物=Bubble，提交前缀正确） |
| G17/Toolbar | `ChatToolbarAppearance.java`、`ChatToolbar.java`、新增 `ChatToolbarThemeTest.java`（5 条） | `81db5791` | 子代理（主代理复核） | 已验收 | 定向 chat3.input+hud.api 19 类 141 项全绿（既有断言零删改）+ 完整 build 主树复跑 exit 0（**分支上唯一红=基线先存 ChatContainerTest L775，stash 干净基线复测证实非本实例回归，merge 含 `70e07489` 链后自动消解，实测 5096 全绿**；3 处变异 M1/M2/M3 全命中含既有用例参与变红） | 未执行 | `aecab769`（merge `0efa493f`） | ① 默认路径 `recipe(rt,role)`=主题 BUTTON_* 档基线+聊天设置**逐项只覆盖滤镜字段**（开=DARK_THIN 系按设置 blur/lens、关=null），`style()` 显式旧路径原样保留不订阅：**认可** ② `HudToolbarSpec`（值对象无 runtime 上下文，本身就是宿主显式配方载体）与 `HudToolbarLayer`（纯透传）**零改动按 §7.3 销账**：认可；「非聊天 HUD 也吃主题」需公共 API 语义扩展 → **关闭**（契约明文 Spec 保留，超范围）③ 全量物化 keyed 列表（≤4 项有界）判 **G09 非虚拟化 per-item 档保留**、不适用 §4.1 行轻量：判定正确，关闭 ④ 两族按钮 hover/缘色微差（0x90/0x50 vs 0x64/0x14、PRIMARY 0x20 vs 0x2A）：非回归（§5 既定语义），**归 G19 聊天族校准**（与 Bubble④ 同批）⑤ `recipe()` 无初值 Computed 归 G19 binder 初值评估（与 Container①/Input① 同批）⑥ **基线先存红两次主动披露（含祖先链证据）：并行模型该情形的标准处置范本，值得肯定**。**G17 家族 4/4 收官** |
| G15/Boolean | `BooleanFieldRenderer.java`（+12/−2）、新增 `BooleanFieldRendererThemeTest.java`（5 条） | `9d45c380` | 子代理（主代理复核） | 已验收 | 定向 config.ui 30 suites 336 项全绿 + 完整 build exit 0（主树复跑 5101 tests；3 处变异：快照复喂→守卫红、竞争写入→守卫红+绑定器仍末写、回退显式路径→行为+守卫共 4 红） | 未执行 | `7631df59`（merge `c4a57e81`） | ① 实扫本 Renderer **零直接外观写入**，唯一残留=死参数上 `ConfigTheme.asFormTheme()` 快照 → 改 `null`+归属注释+移 import：认可（「清残留」非「改默认」，§10.1 空）② **源码守卫成为 Renderer 标配**（M2 实证构造期抢写被 binder 末写吸收、行为用例不敏感，只能守卫兜底）——已扩围 P-03，后续派发一律要求 ③ 收口提示登记：余 6 员齐后统一删 `FieldShellBinder` theme 形参（两重载）+ 同步 `FieldShellBinderTest` 调用点 ④ 其注释含 ConfigTheme 字样属销账说明，守卫按去注释代码判定不误报（G19 扫描注意口径） |
| G15/Number | `NumberFieldRenderer.java`（+17/−1）、新增 `NumberFieldRendererThemeTest.java`（6 条） | `be2dc62d` | 子代理（主代理复核） | 已验收 | 定向 config.ui 30 suites 337 项全绿 + 完整 build exit 0（分支 5097；3 处变异：构造期快照/错绑 muted/ROW 叠第二层表面全命中，P-04 双向切换+守卫标配） | 未执行 | `48d22add`（merge `2076371e`） | ① 唯一真实写入点=slider 读数前景 → `rt.bind(SceneThemes.foreground(rt),...)`（无快照）：**认可**，确立前景口径先例 ② `FIELD_GAP/FONT_READOUT` 布局/排版常量保留注依据、`theme.listHeight()` 纯 int 安全（Support 要点 3 沿用）③ SceneSlider(G06)/SceneTextInput(G04) 只组 Props，零第二层表面（用例钉 ROW 无表面）④ `asFormTheme()` 占位形参按约定保留待收口：认可。**剩余待派仅 FontSort**；SimpleList 已派 wt4（基线 2076371e 含 Number） |
| G15/Choice | `ChoiceFieldRenderer.java` **零 diff**、新增 `ChoiceFieldRendererThemeTest.java`（4 条，359 行） | `47b74d80` | 子代理（主代理复核） | 已验收 | 定向 config.ui 30 suites 335 项全绿 + 完整 build exit 0（分支 5086；**4 处变异**：竞争写入→守卫红（P-03 复现）、**withTheme 固定快照→切换用例红**（新变异形态）、映射回退 2 红、enabled 硬编码 2 红） | 未执行 | `cedd1bec`（merge `2866966b`） | ① 实扫零有效外观写入 → **主文件零改+守卫钉**形态（FieldRenderSupport 先例的 Renderer 首用）：**认可** ② 两处 `asFormTheme()` 占位实参守卫**计数钉**（恰 2）+ 收口同步义务写入守卫 javadoc：认可，**收口形态确立**（摘参时计数 2→0、ConfigTheme 3→1）③ M1 复现 P-03「构造期抢写行为用例不敏感」，双实例实证后守卫标配不可动摇 ④ **过程披露（主代理裁决关闭）**：施工期误发一条 build 至 wt4（Number worktree），配置期即 kill、零 git/源码影响；Number 提交链完整、主树复跑权威，worktree build/ 非验收依据——不追责，同 VariantChooser 先例登记 ⑤ 披露「六条衔接要点原文不在仓库」属实：要点存于派发 prompt 与台账 Support 行摘要，Choice 按任务单复述逐条对账无遗漏（picker 路径对其不适用） |
| G15/String | `StringFieldRenderer.java` **零改动（diff 为空）**、新增 `StringFieldRendererLiquidGlassTest.java`（5 条，332 行） | `0efa493f` | 子代理（主代理复核） | 已验收 | 定向 336 项全绿 + 完整 build exit 0（分支 5101 恰 +5；3 处变异：绕 binder→4 红含 P-04、摘占位→守卫红、竞争写底色→守卫红） | 未执行 | `4a18d85b`（merge `c375270e`） | ① 薄适配层实扫零外观写入 → 零改+守卫钉（19 禁则+4 正向钉，**占位实参「必须保留」正向钉**首例）：**认可** ② `placeholder=""` 判为**语义决策**（helper 去重）非外观写入，保留+§依据：认可 ③ SceneTextInput 不传 placeholderColor（null→本体主题 muted/disabled）＝G04 双路径正确消费 ④ 过程披露两起（测试夹具 throws 编译修复、守卫误伤 import 行后改准钉法=钉更准非放宽）：白名单内解决，知悉 ⑤ checkstyleTest 目录级基线告警非本次引入：登记 G19 观察，不阻塞 |
| G15/StructuredList | `StructuredListFieldRenderer.java`（+36/−8）、新增 `StructuredListFieldRendererThemeTest.java`（5 条，421 行） | `c4a57e81` | 子代理（主代理复核） | 已验收 | 定向 37 suites 431 项全绿 + 完整 build exit 0（分支 5106 净增 5；3 处变异：快照喂壳→3 红、行缘色写死→2 红、L229 切回三参→3 红）；主树复跑见基线行 | 未执行 | `823d9204`（merge `7c4bb667`） | ① L85 显式快照喂壳切 theme-aware 无边框重载（Support 要点 4 点名路径）+ L231 error→主题 `errorText` 绑定：**认可** ② L229 `FormLabeledControl.vertical(rt,…)` **G11 台账遗留正式销账**（额外授权行，diff 证据在案）③ 行缘色 `resolve+bindComputed` 独占、轻量行零滤镜、几何纯 int 保留（§4.1+§4.2）——**深色档 borderDefault/errorText 与原常量逐字节同值 → 默认观感零变化**，切换才动：P-04 双档断言在案 ④ 行缘色未接动画轨道（与 FormFieldShell 默认路径同构）：**归 G19 MOTION 集中校准**登记 ⑤ 若收口时本文件改走 binder helper 属主代理决策 → 收口实例评估；过程披露（自测断言笔误白名单内即修）知悉 |
| G15/SimpleList | `SimpleListFieldRenderer.java`（**纯注释 +27/−0，可执行行零改动**）、新增 `SimpleListFieldRendererThemeTest.java`（5 条，410 行） | `2076371e` | 子代理（主代理复核） | 已验收 | 定向 34 suites 392 项全绿 + 完整 build exit 0（分支 5112 恰 +5；3 处变异：绕 binder→4 红、竞争写入→守卫红、**摘 listHeight→12 红**证明其真实读取） | 未执行 | `53bd7f43`（merge `6fe62800`） | ① 实扫零直接外观写入（外观全在 G15/Support binder + G12 控件本体）→ 注释销账+P-04 真实装配钉+守卫：**认可** ② **与 Boolean 的关键区分**：本文件 `theme` 非零消费（`listHeight()` 真实读取），不适用 null 变体、按 Number 先例原样传值——**收口时 L212-214 需改主题映射同源常量而非删参**，已并入收口清单 ③ 「G19 勿要求 renderer 层直消 SceneThemes 信号（会造双写入者）」：**采纳**为 G19 审查口径 ④ 两次 Gradle 配置缓存重算披露：环境噪音，知悉 |
| G15/FontSort | `FontSortFieldRenderer.java`（+37/−6）、新增 `FontSortFieldRendererThemeTest.java`（6 条，465 行） | `2866966b` | 子代理（主代理复核） | 已验收 | 定向 42 suites 456 项全绿 + 完整 build exit 0（分支 5117 恰 +6；**4 处变异**：快照→3 红、错绑→切换/install/守卫红、行包装叠色→**行为层真红**2、静态回退→2 红） | 未执行 | `5cbc3e07`（merge `81a02103`） | ① 实扫 2 处有效前景写入（行标签 textColor/空提示 mutedColor）→ Number 同形 `rt.bind(SceneThemes.foreground/mutedForeground)`，**非** Choice 零 diff 形态：判定准确，认可 ② 排版常量 `fontHelper/fontLabel/listHeight×2` 保留注依据 + 死 import 清理；「排版字号未来主题驱动」需契约先给 SceneTheme 字号维度 → **本目标外，登记为后续能力候选**（关闭于 G19 备忘）③ **收口总账（主代理汇总在册）**：`asFormTheme()` 占位实参 Choice 2/Number 1/String 1（正向钉）/SimpleList 1/FontSort 1、Boolean 0（null 化）、StructuredList 0（已删 import）、CharRule 待交付报数——收口实例按此清账 + 各守卫计数同步 ④ 过程披露：2 次 edit 参数错位瞬时损坏主文件、当场回滚 HEAD 从干净基线重建、最终验证在重建后：与 VariantChooser/Support 同先例，核实 diff 无异常残留，知悉 |
| G18/GlassLab | `GlassLabHost.java`（+180/−33）、新增 `GlassLabHostThemeTest.java`（8 条，504 行） | `c375270e` | 子代理（主代理复核） | 已验收（真机复测归 G19） | 定向 glass 13 条+devtools 全族 112 项全绿 + 完整 build exit 0（主树复跑 5140；3 处变异 M1/M2/M3 精准打红） | 未执行（实验室宿主，G19 真机重点：stage/probe 玻璃化后三档对照观感） | `cbd2e22e`（merge `dca4a89d`） | ① **ROOT_BG 0xFF 全屏不透明承托底保留显式**（诊断采样基色随主题漂移会毁三档可比性，G15/Shell 遮罩分层同构）+ 守卫白名单钉 1 处：**认可** ② **不申请扩口 PlaygroundKit 判定成立**（公开 `text(rt,…)` 重载 + `buildLabCard` 走 theme 公开 API，零双真相源；`card()` 无宿主回退分支原样）③ **撞色发现（变异暴露，登记 G19 主题校准候选）**：LIGHT PANEL 配方 idle tint 与样本 `GLASS_TINT=0x26FFFFFF` 同值 → 反向钉改用「drawSurface×backdrop 矩形几何配对」计数（方法学贡献：P-04 家族新检测技巧）④ 真机叠加层采样基底含主题玻璃面（理论上丰富素材、三档共基底不损可比）：G19 真机确认项 ⑤ 先例哈希勘误 `a7bc8765→2f15ee11`：仓库文档零误引（grep=0），系主代理派发 prompt 笔误，不记账 ⑥ `install` 先于建树 + `__runRoot` 包裹（P-01 先例同型） |
| G15/CharRule | `CharacterRuleFieldRenderer.java`（+73/−13）、新增 `CharacterRuleFieldRendererThemeTest.java`（6 条，509 行） | `7c4bb667` | 子代理（主代理复核） | 已验收 | 定向 435 项全绿 + 完整 build exit 0（分支 5127 恰 +6；4 处变异全命中含 M2「binder 不消费占位参、守卫系唯一防线」与 Choice 结论互证）；主树复跑 5146 | 未执行 | `4728ec14`（merge `6224f2da`） | **契约 §7.1 path 覆盖第 8 员**（任务单七员清单漏列，G19 预扫暴露补派，缘由注记提交/测试 javadoc）。① L96 无参 `asFormTheme()` 快照拆除三下游分治：行错误色→`resolve+bindComputed` 主题 `errorText`（StructuredList 同法）、fontError/listHeight→`defaultDark()` 纯 int 同源常量、占位参→`null`：**认可** ② **预研两处纠偏（该代理实扫正确）**：旧值实为 `defaultDark().errorColor()`=0xFFFFB4AB 与主题 `errorText` **同源同值→默认观感零变化、§10.1 空**（我派发 prompt 的「0xFFFF6B6B 变色预警」系过时记忆，撤回）；`FormThemes.FONT_ERROR/LIST_HEIGHT` 为 private+禁新增 API → 取 `FormTheme.defaultDark()` 公开权威源直读 int+守卫恰 2 钉防回潮：合规替代，**采纳为收口替换范式** ③ createTextButton 保持结构+零表面双档钉（主代理裁决执行）；其 label 默认白前景 G19 校准候选（登记）④ 过程披露（temp 自建 4 脚本未入库、runner 相对路径修正）：知悉 |
| G15/收口 | `FieldShellBinder.java` + 8 Renderer 触点文件 + 7 测试守卫（15 files，+250/−151） | `6224f2da` | 子代理（主代理复核） | 已验收 | 定向 45 suites 446 项全绿 + 完整 build exit 0（分支 5146；3 处变异：快照回喂→守卫红、整主题回潮→守卫红、**同源→字面量→计数钉红**证正向钉非虚设）；主树复跑见基线行 | 未执行 | `403e15d3`（merge `b71af0fd`） | ① binder 两重载 `theme` 形参**直删不留 @Deprecated**（4.0 施工期定案，G15/Support② 裁决执行完毕）② 9 调用点分治：纯占位直删 5 员、`defaultDark()` 同源常量替换（SimpleList×1/FontSort×3+私有链摘参）、StructuredList 零触点——**CharRule 范式成为标准替换模板** ③ 守卫翻转成套：「占位恰 N」→「ConfigTheme/asFormTheme 零出现+defaultDark 恰 N」反向钉；`FieldShellBinderTest` 追加 **`FormTheme` 整词类型级禁则**（封死整主题回潮，强于原计划）④ 全库 grep `FieldShellBinder.build(` 21 命中全在册 + main 侧 `asFormTheme()` 消费仅剩 `ConfigTheme.java:137` 方法本体（§4.2 兼容入口）：**收口总账闭环** ⑤ 行为不变：默认档断言原样绿（逐值相等）。**G15 家族 12/12 收官，G00–G18 全部代码实例完成** |
| G19/P02 | `SceneThemes.java`（+72 纯新增）+ 7 控件/渲染器删私有派生 + 新 `SceneThemesSemanticColorTest`（6 条）等 8 测试（16 files +484/−90） | `b71af0fd` | 子代理（主代理复核） | 已验收 | 定向 theme/控件/config 全绿 + 完整 build exit 0（主树复跑 479 类 5155 tests；3 处变异：错槽→**17 红**、退静态值→2 红、借道错槽→2 红） | 未执行 | `ac19c22f`（merge `7aeedc21`） | ① 公共入口 `warningText/errorText/danger/successText` 纯新增零签名破坏；**danger javadoc 载入主代理 1.43:1 禁文本警示**（警示→落地闭环范例）② success 委托 accent：代理自备 `g19p02_contrast_check.py` 实证「单一绿跨三档无解且不造新色板」——**采纳保守路线**，未来加分量单点可改 ③ 7 消费点逐点等值收编（Toast `typeColorSignal` 化后 sourceTheme 作用域语义保住）；`SceneTheme.java` 零 diff；`ConfigScreen` 不纳入（FormThemes 桥域外，差异披露）④ 白名单两处路径勘误（KeyValueMap 在 control 非 search；Toast 测试在 integration）+ 守卫收紧 2 token 登记 §10.1：按实扫处置，合规 ⑤ **P-05 reliefDisabled 与 binder seed 提案在册 → G19 裁决=采纳提案、排后续平台任务不在本目标实施**（理由：全线真机复验未执行，此刻再改 theme 共享通道会使已验收观感证据作废；登记待真机后专项）|
| G16/ReactivePage | `ReactivePage.java`、新增 `ReactivePageTest.java`（7 条） | `3132c2cb` | 子代理（主代理复核，多实例组） | 已验收 | 定向 79 项无回归 + 完整 build exit 0（覆盖四页 4943 tests，含变异检查） | 未执行 | `91330af0`（merge `f975b847`） | 保留显式样本：三枚状态徽标 ACCENT/DANGER/MUTED+文字 TEXT+999 几何——**裁决接受**（契约 §7.3 状态徽标 + 数值证据：主题 danger≠DANGER、浅色正文落高饱和深底不可读），守卫「各恰 1 处」计数钉死 |
| G16/RichTextPage | `RichTextPage.java`、新增 `RichTextPageTest.java`（7 条） | `3132c2cb` | 子代理（主代理复核，多实例组） | 已验收 | 同上 | 未执行 | `0be0e90a`（merge `f975b847`） | 容器前景 5 处走 §2.7 `Props.builder` 主题路径；标签内 `#FF5533/#4FC3F7/<mark>` 等渲染协议数据逐字保留（登记）；守卫零豁免 |
| G16/ControlCharPage | `ControlCharPage.java`、新增 `ControlCharPageTest.java`（5 条） | `3132c2cb` | 子代理（主代理复核，多实例组） | 已验收 | 同上 | 未执行 | `9fa4aed2`（merge `f975b847`） | 17 类控制字符/`§` 协议码点逐字保留（逐码点在场证明+切换前后相等）；容器层全部主题化，守卫零豁免 |
| G16/LatexPage | `LatexPage.java`（仅注释 17 行）、新增 `LatexPageTest.java`（5 条） | `3132c2cb` | 子代理（主代理复核，多实例组） | 已验收 | 同上 | 未执行 | `91f40f64`（merge `f975b847`） | 核实 L148=公式正文默认色（像素继承外层前景）→ **裁决接受保留显式**（改之摧毁 latex-compare 目检基线，§7.3）；54 条公式行反向钉住；守卫两处豁免计数钉死；卡标题/说明经公共构件自动跟随 |
| G16/MarkdownPage | `MarkdownPage.java`+`MarkdownPageContent.java`（纯注释 +52）、新增 `MarkdownPageTest.java`（7 条）、`MarkdownPageContentTest.java`（+1） | `f975b847` | 子代理（主代理复核） | 已验收 | 定向 18 类 104 项全绿 + 完整 build exit 0（4975 tests，含 2 处变异检查） | 未执行 | `fb540c80`（merge `663bf691`） | ① L124/L273 `base.setColor` 核实为**渲染正文默认色**（进 TextSegment 色通道）→ 保留显式，**裁决接受**（与 LatexPage 同理）② 数据驱动样本色（衬底/竖条/横线/表头/PaintCommand）全保留+反向钉住 ③ 跨引用块空行 quoteLevel=1 续行为渲染语义禁区，**另案登记**（如需修正开渲染缺陷任务，不属本目标） |

## 平台观察

（主代理记录，非本目标交付项；不阻塞实例派发。）
| 编号 | 观察 | 证据 | 处置 |
| --- | --- | --- | --- |
| P-01 | 无 `Owner` 上下文时 `SceneRuntime.bindComputed` 先 `Computed.create(derivation)`（无归属）再 `bind` 重入 `rootOwner`，派生信号不被任何作用域持有、`runtime.dispose()` 回收不到；`SceneThemes.surface` 的 `Computed.create` 同理 | G16/外壳构造期实测泄漏 12 个 effect，已用 `runtime.__runRoot(...)` 包裹宿主构建并由新用例钉死；`SceneSurfaceBinder.bind` 自带 `Owner.current()==null → __runRoot` 兜底 | 记为平台级观察：正常页面/控件构建都在 mount/show/forEach/portal 的 Owner 作用域内，无实际泄漏；G19 静态审查加「无 Owner 上下文构建点」扫描，是否改平台语义另行裁决 |
| P-02 | `SceneThemes` 缺语义色公共便捷派生：`warningText(rt)`（Toast/ObjectField 两处私有派生重复）、`errorText(rt)`/`danger(rt)`（KeyValueMap 以构造期 `resolve(rt)`+effect 内取值规避）；另 G15/Shell 反馈「成功」语义只能借 accent（`SceneTheme` 无 success 槽）；G15/Support 反馈 `issues`（无效/重复计数）摘要想染 warning 但无公共 `warningText` 入口（暂保持中性正文色，未擅自新增语义） | 四实例报告一致 + G15/Support 裁决点③；均无功能缺口、未越权加 API | G19 评估在 theme 包一次收编（`warningText/errorText/danger` 派生 + 是否增设 success 语义色，共享文件需主代理授权实例执行），随后删各控件私有版；届时复核 Support `issues` 是否改染 warning 档。**状态更新：已派专项实例（wt3 `wt-g19-themepack` @ `b71af0fd`，受权改 theme 包）**；主代理警示在册——`danger` 预算对比度 1.43:1 为底图角色禁作文字色，文字入口只出 `errorText/warningText`；success 无干净达标色则委托 accent |
| P-03 | 变异检查方法论：`SceneSurfaceBinder.bind` 之后追加一次性静态 `set*` 会被绑定 effect 末次写入吸收（测不出）；真实缺陷形态是「静态底色**替代**绑定」；**Boolean M2 实证对偶情形**：构造期抢写同样被 binder flush 后末写吸收，行为用例对同属性抢写不敏感 | G12/ObjectField 变异检查 M3 + G15/Boolean 变异 M2 实证 | G19 静态审查与后续实例变异检查按此区分；**源码守卫用例列为每个 Renderer 实例标配**（Boolean 先例：禁全部表面/前景 setter + 正向锁 helper 装配点，去注释判定） |
| P-04 | 深色档同值陷阱：`PlaygroundKit.TEXT/MUTED` 与 `liquidGlassDark` 的 `foreground/mutedForeground` 恰好同值，仅静态断言无法区分「主题路径」与「构件回退/样本保留」 | G16 四页组 B 变异检查实证（深色下注入静态色不红，切换断言才红） | **审查口径（主代理认可）**：主题跟随证据必须含「切换后更新 + `assertNotEquals` 两档前提 + 真实宿主装配路径」；G19 按此复核全部页面/控件实例证据 |
| P-05 | 浮雕通道统一口径：`SceneSurfaceBinder` 在 idle.elevation≥0 时必走 `ROUNDED_BAND` 浮雕通道（Input 裁决②：elevation=0 亦进），而聊天大面板既有观感为普通绘制（Container 被迫绕 binder，elevation 恒 -1）。同一「表面」语义存在两条绘制通道且无显式豁免开关 | G17/Input `675831a5`、G17/Container `bf2e0ec9`、§10.1 修复 `70e07489` | G19 裁决统一口径：给 binder/配方增设「禁浮雕」豁免位，或按 Role 校准 elevation 默认档；涉及 theme 包共享文件，届时授权专项实例执行 |
| P-06 | 聊天栈基线 effect 水位：每轮「挂载→卸载」残留约 14 个 effect（`ChatContainer` 内 2 个滚动合同旧 `Computed` + `ChatMessageList/ChatInputBar/ChatScrollbar` 约 12 个），非迁移引入 | G17/Container 交付报告 dispose 水位实测；其用例已钉「增量恒定」 | 不阻塞本目标；G19 登记为后续技术债观察项（收编与否另行裁决） |
| P-07 | `ui/scene/paint/ScenePalette.java` 全类**零消费者死代码**（main+test 均无引用，其 BG_PRESSED 引用属定义体自身） | G19 预扫分析（只读代理）实证 | 非本目标缺陷；登记为后续清理候选（删除需公共 API 承诺评估，与 P-02 同批平台治理） |
| P-08 | Picker 族 4 文件（`ScenePickerPanel/ScenePickerPanelNav/search/MemberGrid/search/VariantChooser`）import `club.heiqi.config.ui.editor.*`：control→config **向上依赖**，违反「scene core 不依赖业务包」的架构方向 | 审查代理 diff 扫描；基线 `4549c8dc` 即存在（import 行数 5/3/3/2），本目标未新增亦未清偿 | 既有欠账，另立清理实例（editor provider 接口下沉 scene 层或经回调注入），不并入本目标 |
| P-09 | `SceneSurfaceBinder.bind` 文档声明「同一节点只调用一次」但**无运行时防重入标记**，双 binder 竞争只能靠测试/源码守卫兜底 | 审查代理观察（`SceneSurfaceBinder.java:19-20`） | 与「binder 初值 seed」同批评估：节点属性槽加一次性标记或 debug 断言；涉及 theme 共享文件，需主代理授权专项实例 |
| P-10 | **历史 WCAG 数字未随记录披露输入模型，致外审不可复现**（外审第一轮 F-3 触发）：本表下方「G19 预演数值档案」与 v1 审计包的比值各自有模型（名义底=各 role `FALLBACK_BG*` 兜底色 / 未声明），但审计包 v1 压缩时丢了模型声明；且部分代理上报输入值与 `SceneTheme` 现行权威值不符（如 warningText light 实为 `0xFF8B5000` 非 `0xFFA65300`；PANEL tint 实为 `0x14EAF7FF/0x33FFFFFF`）；主代理当时验算了「比值算得对不对」未复核「底与输入是什么」。**【第二轮·审查者B F-01/F-02 追加坐实】**v2 附录 A 本身又含两处手抄残留（候选绿表、ΔL 行未过脚本）：`#A5D6A7` 实际 M1 双档通过（6.946799/4.565249），v2「列举域内无解」错误结论；「selected-vs-hover ΔL +129%」手抄错（实 −59.4%）；主代理脚本复算与 B 逐位一致 | 审计包 **v2.1** 全附录 A 改为 `auditA_v21_full.py` 脚本输出直接嵌入；successText 委托论证由「无解」降格为「M2 世界包络内无稳定解（#A5D6A7 对纯白 1.64 塌方）+ 扩色板属治理保留权」；V-3 判据改用无歧义 CR 三值（1.10/1.59/1.45），废弃无单位「ΔL<2」表述。**纪律固化：审计文档数值表只允许脚本输出直接嵌入，模型与输入随数字同档** | 契约 §4.3 与 V 档案旧比值以 v2.1 附录 A 为订正链（不改契约正文冻结）；M2 对称风险维持 §6.1 第 0 条默认启用门槛。**【第五轮】审查者 B 通道终止（无法再访问）**：F-01/F-02 整改交付至 v2.1+、主代理独立重算与其逐位一致，但无评审者回签——数值链闭环状态如实记为「整改完成、回签缺席、脚本直出纪律兜底」，不伪称双签 |
| P-11 | **GitHub 远端 4.0 快照落后本地全部施工（未 push 为规范行为）**：审查者 A 经远端链接审查，所引「SceneButton 直写 setBackgroundColor、SceneControlChrome 生产写入路径、命题 B 架构分叉」均为**施工前旧状**——本地 HEAD 实证：`SceneButton:121` 走 `SceneSurfaceBinder.bind`；`bindSelectableBackground(` 全 main 生产调用点 0（仅定义体+2 注释）；`SceneStateColors` 类 javadoc 自述「纯静态查表非主题引擎」属兼容入口显式语义（契约 §6 保留）。其概念建议（producer ownership test、SurfaceRole×ActionVariant 拆分、材质模式显式化、ownership 统一术语、特权 renderer 命名）对现行代码仍有效 | 审计包 v2.1 头部事实校正块 + §6.5 登记五项待裁决提案（不阻塞真机验收，属下阶段架构演化待用户裁决）；外审材料规约更新：一律以自包含审计包为准或主代理代查现行代码；如需远端审由用户决定 push。**【第三轮更新】**A 已撤回事实层攻击并接受本地基线声明，其「零点三层强度（调用/全引用/语义）」口径升级采纳：`audit_zero_proof.py` 实证 L2 全引用表（兼容入口 REF=0；Chrome→StateColors 定义体内部链、SceneChromeTokens 体内自调、ConfigScreen 桥接豁免三处语义 REF 均披露归属）；L3 揭示 **v2.1「唯一写入者」确属过度表述**——元素级轻量通道（行覆写/caret/thumb/dot/scrim）直绑信号到控件独占子节点，架构不变式订正为 **node 级单一写入者**（契约 §7.3 第三轮补录 + 审计包 v2.2 §3.1/§3.6「零使用≠零能力」边界表）；A 提案优先级重排为 §6.5 A~E（B 项已随 v2.2 完成）；现行源码 zip 已备（工作站 temp `qz-uilib-src-main-4.0-c389ac42.zip`，1.9MB）供 A 独立零点核验 | 教训：给外审的「仓库链接」在本仓是失效证据源（远端≠本地），今后外审话术必须显式声明证据基线。**【第四轮】**A 基于 c389ac42 全量源码 zip 独立核验：L1/L2/L3 全 PASS；逐节点抽查 VirtualGrid/SearchResultList/VariantChooser/StructuredListRenderer/DataTable 五样本，**无同节点 binder+轻量双写**（外壳/viewport=binder，行/cell=轻量槽，icon/label=图像/文本协议分层），node 级单写者前提获外部独立证据；正式签 L-C（限定「生产使用为零」，拒签「零能力」偷换版，v2.2 措辞已合规）；§6.5-A 交付测试骨架+P1~P4 precedence 矩阵+RULE-1~4 注册制守护（不正则全覆盖防误杀合法例外族），状态=形态已交付待立项；审计包 v2.3 落签署记录 |**【2026-09-10 主代理重审补录】差距实测订正**：本行「远端落后本地全部施工」为 v2.1 时点表述；实测 `origin/4.0`=`c389ac42`（即 v2.1，第二轮整改版），本地 4.0 `0856d8b1` **领先 3 个文档提交**（`9ef8156f` v2.2 / `4c1fdd72` v2.3 / `0856d8b1` v2.4），`git rev-list --count origin/4.0..4.0`=3、`4.0..origin/4.0`=0，零代码改动 |

## G19 预演数值档案（主代理 Python 实算，脚本 `Qz工作站\temp\g19_contrast_calc.py`，2026-09-11）

半透明合成按 `src.rgb·a + dst.rgb·(1-a)`，WCAG 相对亮度公式；「名义基底」取各 role `FALLBACK_BG*` 不透明兜底色。

| # | 结论 | 数据 | 用途 |
| --- | --- | --- | --- |
| V-1 | **选区文字裁决稳固**：dark 档选区（accent@0x59）上 `foreground` 与 `onAccentForeground` 近乎无差，均远超 4.5:1；白字反例无收益 | INPUT 10.72 vs 10.73、PANEL 9.71 vs 9.72；light 8.58~9.26 | 维持「选区文本用 foreground」不变 |
| V-2 | **`danger=0xFF7F1D1D` 于 PANEL 底仅 1.43:1 → 底图/徽章角色，禁作文字**；文字语义 `errorText 8.46:1`、`warningText 8.60:1` 达标 | dark 档名义底 | 已警示 G19/P02 实例；契约 §5 注记 danger 语义 |
| V-3 | **轻量行态 alpha 在暗实底上亮度近不可辨**：idle↔hover(0x14) ΔL=0.0022、hover↔selected(0x59) ΔL=0.0091（≪0.02 可辨阈）——区分度实际依赖色度(紫调)+世界背景采样差异+描边/文字伴随信号 | GROUP 合成底 | **G19 真机目检重点**：暗环境下 hover/selected 可辨性；若不达观感，候选提 selected alpha 或伴随左侧 accent 条（G12 已有法） |
| V-4 | **玻璃对比度随世界亮度包络漂移**：dark 选区上前景对比 1.46~11.94（世界 0x000000→0xFFFFFF） | PANEL idle tint+选区双层合成包络 | 结构性结论：可读性不得单靠色对比，配方自带 edge 描边（0x24~0x40FFFFFF）满足双通道；亮世界场景列真机验收项 |
| V-5 | disabled 前景 3.15~3.77:1（三档名义底） | dark 档 | 达 WCAG UI 组件 3:1，文本级降级语义可接受；亮档复核列真机 |
| P-12 | **全量测试基线 5155/0F/0E/6skip 与「6 跳过的身份」此前只以「项目方报告」在册，未经主代理独立实测**（审计包 §8 补证清单长期挂着该项，外审 A/B 无法据此独立判断） | 主代理重审实测 `gradlew.bat --offline --console=plain test` @ `0856d8b1`：BUILD SUCCESSFUL，`build/test-results/test/*.xml` 统计 **5155/0/0/6**，与报告逐位一致；6 项身份实证=2 项 `LatexReferenceComparisonTest`（需 `qz.latex.reference.jar` / `QZ_LATEX_REFERENCE_JAR`）+4 项 `C9DualFontProbeTest` 字体探针，均 `AssumptionViolatedException`；另实测 95 处 surface sink（46 文件）节点归属，无「同节点 binder+轻量双写」，与 A 五样本结论一致（覆盖面更宽） | 审计包 §2/§4.4/§8 落实测行；「构建报告随包」仍需用户或 CI 决定，台账 L13 本地 artifact 指针保留 |
| P-13 | **文档证据此前靠人工保证**：v2.1 整改后仍有一行手抄漏网（`onAccent-over-selBg` 不在脚本输出内），且「347 闭合」为静态快照、新增 surface 写入者无人拦 | P1/P2 施工（2026-09-10，`4b9b83e6`/`4fafa034`/`8f5d4e4f`）：数值直出反向门禁 `tools/audit/check_numeric_traceability.py`（§7 每数必须可定位 + 11 浮点/9 色值基准）；surface sink 注册制守护 `SurfaceSinkOwnershipRegistryTest`（60 条目 / 65 处写入，未登记即红灯）；变异验证 P1×3 + P2×3 全部按要求红/绿 | 门槛：主树 `gradlew build` exit 0（5158 / 0F / 0E / 6 skip）；审计包 v2.6 增证据矩阵；纪律：改审计文档先跑门禁（默认仅报告，--strict 才阻断） | **【2026-09-10 减负退役】**外审取消后复核成本收益：近 60 个提交仅 4 个触碰 sink 且多为合法新增，39/60 条目属永远合法的 light-slot，注册制税负 > 收益 → **退役** SurfaceSinkOwnershipRegistryTest 与 surface_sink_registry.json（守护测试 3 例删除，测试基线回落 5155），surface_sink_ownership.py 降级为按需诊断工具；node 级单写者此后无自动化守护（依赖 binder 契约 + 代码审查；需要时用 scanner 人工巡检），已在 tools/audit/README.md 如实记录 |
