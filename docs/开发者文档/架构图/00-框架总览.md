# L0 框架总览：club.heiqi 包树地图

一句话定位：全仓一级包树与子系统关系，标注 scene 新栈与旧栈并存及 scene core 依赖边界。

> 素材基线：源码实时状态（2026-08-13）。涉及类名可在 `src/main/java` 核对。

## 包树与子系统关系

```mermaid
flowchart TD
  ROOT["club.heiqi<br>根包"]
  CFG["club.heiqi.config<br>配置核心（新栈）"]
  UI["club.heiqi.uilib<br>UI 框架主体"]

  ROOT --> CFG
  ROOT --> UI

  CFG --> CFG_R["config.runtime<br>权威配置运行时"]
  CFG --> CFG_S["config.schema<br>配置模式定义"]
  CFG --> CFG_U["config.ui<br>配置页界面"]

  UI --> UM["config.modern<br>新旧栈桥接"]
  UI --> FONT["font<br>字体系统"]
  UI --> GL["gl<br>GL 支撑"]
  UI --> CLIENT["client<br>客户端集成（含 hud）"]
  UI --> INTERNAL["internal<br>内部工具"]
  UI --> MIXIN["mixin<br>原版注入"]
  UI --> NET["net<br>网络层"]
  UI --> UISUB["ui<br>控件层"]

  UISUB --> SCENE["scene<br>scene 新栈"]
  UISUB --> OLD["旧栈<br>widget / text / screen / slot / inventory"]

  SCENE --> SCN_CORE["node / runtime / layout / paint<br>核心数据与计算"]
  SCENE --> SCN_IN["input / overlay<br>输入与浮层"]
  SCENE --> SCN_WIDGET["control / form / host<br>控件与宿主"]

  UM -- 依赖 --> CFG
  UM -- 依赖 --> FONT
  CLIENT -- 依赖 --> SCENE
```

## 关键边界（真值锚点）

- scene 新栈与旧栈（`widget` / `text` / `screen` / `slot` / `inventory`）并存，互不替代；新增界面默认走新栈。
- scene core（`node` / `runtime` / `layout` / `paint` / `input` / `overlay`）禁止依赖旧栈包，也禁止依赖 MC / GL 类型；平台认知只经 `ui.scene` 顶层 `UiSurface` 与 `ui.render.UiRenderBackend` 抽象进入。
- 配置新栈在 `club.heiqi.config`（core 层），`club.heiqi.uilib.config.modern` 只做桥接，不反向依赖 core。
- 网络层业务门面是 `club.heiqi.uilib.net.api.NetService`，与 UI 无依赖环。
