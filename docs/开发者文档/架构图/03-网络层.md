# L1 网络层：出站入站数据流与信封格式

一句话定位：`NetService` 单例门面下的出站调度 / 入站路由两条数据流，及 `NetEnvelope` 信封与编解码。

> 素材基线：源码实时状态（2026-08-13）。涉及类名可在 `src/main/java` 核对。

## 出站数据流

```mermaid
flowchart TD
  API["业务 API<br>NetService 单例门面"]
  REG["注册表<br>NetChannel.Sender / NetFetchEndpoint<br>NetRealtimeChannel / NetStore / NetStreamEndpoint"]
  CH["物理 channel<br>qz:0"]
  ENV["NetEnvelope<br>MAGIC QZNL v2<br>Kind 枚举 CHANNEL / FETCH / STORE / STREAM<br>route key + contentType + headers + requestId + statusCode + payload"]
  SCH["NetOutboundScheduler<br>优先级调度"]
  LIM["超限分片<br>NetPayloadLimits 校验<br>经 NetChunkAssembler 拆分 CHUNK 帧"]
  SPI["ITransport SPI"]
  FT["forge.ForgeTransport"]
  VT["vanilla.VanillaMixinTransport"]

  API --> REG
  REG --> ENV
  API --> CH
  CH --> ENV
  ENV --> SCH
  SCH --> LIM
  LIM --> SPI
  SPI --> FT
  SPI --> VT
```

## 入站数据流

```mermaid
flowchart TD
  FH["FrameHandler.handleFrame<br>传输适配器上报原始帧"]
  HF["NetService.handleInboundFrame<br>分片重组（NetChunkAssembler.accept）"]
  DEC["NetEnvelope.decode<br>MAGIC 校验 + 反序列化"]
  DISP["dispatcher 路由<br>NetEnvelopeDispatcher / NetRealtimeDispatcher<br>按 Kind 与 route key 分发"]
  HND["handler<br>Channel handler / Fetch / Store / Stream / Realtime 回调"]
  MT["MainThreadDispatcher<br>fetch 完成等回调回主线程（CLIENT 侧）"]

  FH --> HF
  HF --> DEC
  DEC --> DISP
  DISP --> HND
  HND --> MT
```

## 编解码

```mermaid
flowchart TD
  C["NetCodec<br>Schema 驱动的对象编解码"]
  F["NetField<br>字段声明"]
  SR["SchemaRegistry<br>注册表与校验"]
  PR["PrimitiveCodecs / Varint<br>基础类型与变长整数"]
  C --> F
  C --> SR
  C --> PR
```

## 图注（真值锚点）

- `NetService` 是单例门面，注册表在 freeze 后拒绝新增；物理 channel 常量 `PHYSICAL_CHANNEL` 为 "qz:0"。
- `NetEnvelope` 位于 `club.heiqi.uilib.net.core`（无独立 envelope 子包）；`Kind` 枚举覆盖 CHANNEL、FETCH_REQUEST / FETCH_RESPONSE / FETCH_ERROR、STORE_SNAPSHOT / STORE_DELTA、STREAM_REQUEST / STREAM_START / STREAM_CHUNK / STREAM_ERROR / STREAM_CANCEL 等 wire 类型。
- 超限 payload 出站时经 `NetChunkAssembler` 分片为 CHUNK 帧，入站先重组再解码。
- 传输层通过 `ITransport` SPI 隔离；Forge 与原版（vanilla mixin）各一份实现。
- 网络消息不直接建 Java 类型：body 语义为 HTTP-like 的 JSON / 文本 / 二进制，信封携带完整路由与元数据。
