# 原版物品渲染流程（参照图）

以 1.7.10 原版 `RenderItem` 为参照基线，梳理 GUI 物品渲染的入口链、三条绘制分支与 GL 状态自净情况，供 Qz-UILib 围栏逐项对照。

> 素材基线：build/rfg/minecraft-src RenderItem.java（1.7.10 反编译，2026-08-13）

## 图 1 总览：renderItemAndEffectIntoGUI 全链

```mermaid
flowchart TD
    A["renderItemAndEffectIntoGUI 入口 ItemStack 非空"] --> B["zLevel 加 50"]
    B --> C{"ForgeHooksClient.renderInventoryItem"}
    C -- "返回 false" --> D["renderItemIntoGUI 携带 renderEffect true"]
    C -- "返回 true 接管" --> E["跳过原版绘制"]
    D --> F{"物品渲染三分支"}
    F -- "3D block spriteNumber 0 且 renderItemIn3d" --> G["3D block 分支 pushMatrix 姿态链"]
    F -- "requiresMultipleRenderPasses" --> H["多 pass 分支 alpha 模板 quad"]
    F -- "其余" --> I["2D 图标分支 renderIcon"]
    G --> J["尾部 glEnable GL_CULL_FACE"]
    H --> J
    I --> J
    J --> K["zLevel 减 50"]
    K --> L["返回"]
    E --> K
    C -- "hook 抛异常" --> M["catch Throwable 组装 CrashReport"]
    F -- "三分支内异常" --> M
    M --> N["throw ReportedException zLevel 不回滚"]
```

## 图 2 2D 图标分支细节（:528-565）

```mermaid
flowchart TD
    A["disable LIGHTING"] --> B["enable BLEND"]
    B --> C["blendFunc 770 771 1 0"]
    C --> D["bindTexture items sheet"]
    D --> E["color4f 染色 renderWithColor 时"]
    E --> F["disable LIGHTING 防 renderEffect 污染"]
    F --> G["enable ALPHA_TEST"]
    G --> H["enable BLEND"]
    H --> I["renderIcon 16x16 z 等于 zLevel"]
    I --> J["enable LIGHTING"]
    J --> K["disable ALPHA_TEST"]
    K --> L["disable BLEND"]
    L --> M{"hasEffect 0"}
    M -- "有附魔光效" --> N["renderEffect glint"]
    M -- "无" --> O["enable LIGHTING 结束 此分支 BLEND 与 ALPHA_TEST 已回 disable"]
    N --> O
```

## 图 3 3D block 分支细节（:428-471）

```mermaid
flowchart TD
    A["bindTexture blocks atlas"] --> B["enable ALPHA_TEST"]
    B --> C{"renderBlockPass"}
    C -- "pass 大于 0" --> D["alphaFunc GREATER 0.1 加 enable BLEND 加 blendFunc 770 771 1 0"]
    C -- "pass 等于 0" --> E["alphaFunc GREATER 0.5 加 disable BLEND"]
    D --> F["glPushMatrix"]
    E --> F
    F --> G["translate x-2 y 加 3 z 为 -3 加 zLevel"]
    G --> H["scale 10 10 10"]
    H --> I["translate 1 0.5 1"]
    I --> J["scale 1 1 -1 镜像"]
    J --> K["rotate 210 绕 X"]
    K --> L["rotate 45 绕 Y"]
    L --> M["color4f 染色 renderWithColor 时"]
    M --> N["rotate -90 绕 Y"]
    N --> O["renderBlockAsItem"]
    O --> P{"pass 等于 0"}
    P -- "是" --> Q["alphaFunc GREATER 0.1 遗留 不自净"]
    P -- "否" --> R["glPopMatrix"]
    Q --> R
```

## 图 4 多 pass 分支与 glint（:473-526 + renderEffect）

```mermaid
flowchart TD
    A["disable LIGHTING 加 enable ALPHA_TEST"] --> B["bindTexture items atlas"]
    B --> C["disable ALPHA_TEST 加 disable TEXTURE_2D"]
    C --> D["enable BLEND 加 blendFunc 0 0 0 0"]
    D --> E["colorMask 只写 alpha 加 color 1 1 1 1"]
    E --> F["20x20 全 alpha 模板 quad"]
    F --> G["colorMask 复位全通道"]
    G --> H["enable TEXTURE_2D 加 enable ALPHA_TEST"]
    H --> I["逐 pass 开头 blendFunc 770 771 1 0 加 bindTexture 加 color 染色"]
    I --> J["disable LIGHTING 加 enable ALPHA_TEST 加 renderIcon"]
    J --> K["disable ALPHA_TEST 加 enable LIGHTING"]
    K --> L{"hasEffect 该 pass"}
    L -- "有" --> M["renderEffect 进入 depthFunc EQUAL 加 disable LIGHTING 加 depthMask false 加 bind glint"]
    M --> N["enable ALPHA_TEST 加 enable BLEND 加 color 染色 加 renderGlint 两层 quad"]
    N --> O["renderEffect 退出 depthMask true 加 disable BLEND 加 disable ALPHA_TEST 加 enable LIGHTING 加 depthFunc LEQUAL"]
    O --> P{"还有 pass"}
    L -- "无" --> P
    P -- "继续" --> I
    P -- "结束" --> Q["enable LIGHTING 结束 BLEND 保持 enable 不自净"]
```

## 图 5 overlay：数量与耐久条（:700-739）

```mermaid
flowchart TD
    A["renderItemOverlayIntoGUI ItemStack 非空"] --> B{"stackSize 大于 1 或自定义文本"}
    B -- "是" --> C["disable LIGHTING 加 disable DEPTH_TEST 加 disable BLEND"]
    C --> D["drawStringWithShadow 数量 字体自绘"]
    D --> E["enable LIGHTING 加 enable DEPTH_TEST"]
    B -- "否" --> F{"showDurabilityBar"}
    E --> F
    F -- "是" --> G["disable LIGHTING 加 disable DEPTH_TEST 加 disable TEXTURE_2D 加 disable ALPHA_TEST 加 disable BLEND"]
    G --> H["renderQuad 三连 z 等于 0 非 zLevel"]
    H --> I["BLEND 刻意不恢复 Forge 注释"]
    I --> J["enable ALPHA_TEST 加 enable TEXTURE_2D 加 enable LIGHTING 加 enable DEPTH_TEST"]
    J --> K["color 1 1 1 1"]
    K --> L["结束"]
    F -- "否" --> L
```

## 图注：自净 vs 不自净

| 状态项 | 原版行为 | 自净? |
|---|---|---|
| zLevel | 正常路径 :579 加 50 与 :644 减 50 成对；异常路径 catch 后抛 ReportedException 不回滚 | 部分（异常不自净） |
| BLEND | 2D 图标分支成对 disable 回滚；多 pass 分支 :480 enable 后结束不回滚；overlay 耐久条 :731 刻意不恢复 | 不自净（2D 分支除外） |
| ALPHA_TEST | 2D 与多 pass 分支成对 disable 回滚；3D block pass0 :468 遗留 alphaFunc GREATER 0.1 不还原 | 部分（3D pass0 不自净） |
| LIGHTING | 三个分支结束均 enable 回滚 | 自净 |
| CULL_FACE | :568 恒 enable 永不恢复 | 不自净 |
| TEXTURE_2D | 多 pass 模板段与 overlay 耐久条段均成对 enable 回滚 | 自净 |
| colorMask | 多 pass 模板段 :482 只写 alpha 后 :492 复位全通道 | 自净 |
| color | overlay 耐久条末 :736 显式 color 1 1 1 1 | 自净 |
| DEPTH_TEST | overlay 数量段 :708 禁用 :712 恢复成对 | 自净 |
| depthFunc 与 depthMask | renderEffect :650-662 进入与退出对称成对 | 自净 |
| 矩阵栈 | 3D block 分支 :444 pushMatrix 与 :471 popMatrix 成对 | 自净 |

原版不自净项依赖宿主每帧重设（GuiContainer.drawScreen），Qz 围栏以 attrib 帧+texture binding 恢复替代。
