# 验收记录 — 全库默认液态玻璃样式（G19）

> 主代理编制。配套：《规划-全库默认液态玻璃样式》《任务单-全库默认液态玻璃样式》《契约-全库默认液态玻璃样式》《台账-全库默认液态玻璃样式》《施工手册-液态玻璃控件迁移》。
> 分支 `4.0`，验收基线头 `7aeedc21`（G19/P02 并入后）。编制日期 2026-09-11。

## 1. 总体结论

- **代码交付：完成并验收。** G00–G18 全部任务族（含 G14/G15/G16/G17 多实例族）与 G19 前置收编（P-02 语义色）共 **60+ 个代码实例**全部走「白名单实现 → 定向测试 + ≥2 变异检查（P-04 口径）→ 完整 `gradlew.bat build` exit 0 → 本地提交 → worktree merge → 主树复跑」流水线，逐实例记录于台账。
- **最终构建证据（主代理实跑）**：`gradlew.bat build`（含 checkstyle+test 全链）**exit 0**；**479 测试类 / 5155 tests / 0 failures / 0 errors / 6 skipped**（起点基线 4685 tests；全程只增不减，6 skipped 恒定基线项）。
- **静态覆盖审查：全量销账**（§3）。**行为审查：矩阵全绿**（§4）。
- **视觉/宿主/性能审查：未执行 → 整体状态 = 待验**（任务单 §G19「有必需实例待验时整体状态仍为待验」）。本地无 Minecraft/GPU 宿主环境，全线实例如实登记「未执行」，零伪造。真机验收清单见 §5，移交用户或 CI 宿主环境执行。

## 2. 交付链摘要（家族 → 关键提交）

| 任务族 | 状态 | 提交链尾（merge 头） |
| --- | --- | --- |
| G00 契约冻结 | ✅ | `8e669f79`（+G13 滤镜口径补录 `42be2ab5`、§7.3 HUD 补录 `75e0f066`） |
| G01–G03 主题/表面/按钮 | ✅ | `0a4484e5` |
| G04 文本族（Label/TextInput/TextArea/Autocomplete） | ✅ | `162b3b62`（m `fcf6bf2c`） |
| G05 三态控件（Toggle/Checkbox/RadioGroup） | ✅ | `4c1387fd`（m `7afa1062`） |
| G06 Slider / G07 滚动对 / G08 Select | ✅ | `ca463fc6` / `b3764565`（m `5dfd96e0`） / `86381424`（m `a5438bee`） |
| G09 导航四员 | ✅ | `dc0696bc` 等（m `efc7c26c`/`e6bc6f6b`） |
| G10 浮层四员 | ✅ | `65b878cb`+`75619c23`（m `a1162e93`） |
| G11 表单模板五员 | ✅ | `64173080`（m `55edefeb`） |
| G12 复合五员 | ✅ | `cef6537d`（m `7f66493b`） |
| G13 搜索配件五员 | ✅ | `de297fbd`（m `881e3f30`） |
| G14 PickerPanel+VirtualGrid | ✅ 2/2 | `c959bba8`（m `47b74d80`） |
| G15 配置页 12 员（含收口） | ✅ 12/12 | `403e15d3`（m `b71af0fd`） |
| G16 Playground 11 员 | ✅ | `fb540c80`（m `663bf691`） |
| G17 聊天/HUD 4 员 | ✅ | `aecab769`（m `0efa493f`） |
| G18 GlassLab | ✅ | `cbd2e22e`（m `dca4a89d`） |
| G19/P02 语义色收编 | ✅ | `ac19c22f`（m `7aeedc21`） |

并行交付采用 4 worktree 白名单互斥 + `merge --no-ff`；先存红/基线盲区两例（Container 越界断言、Toolbar 先存红）均按 §10.1 收敛（`70e07489`），主树复跑兜底机制有效。

## 3. 静态覆盖审查（销账结论）

- 手段：`qz_scan_appearance.py` 首扫（`6fe62800`）→ 只读分析代理**去注释逐类销账表** → 主代理裁决 → 收口/G18/P02 后**终扫复核**（`7aeedc21`）。
- 口径与结论：
  1. 去注释后 347 处（首扫时点）：**在册豁免 322**（定义体/§4.2 布局常量/§7.3 样本/兼容入口/台账裁决保留）+ **在途管辖 23**（现已全部落地销账）+ **真漏网 2**——`SceneHudHost.HUD_SHELL_BG`、`UiHudRenderListener` debug 文字色，均已补**契约 §7.3 保留条目**（遮罩/诊断色语义）关闭。
  2. 四类兼容入口（`applyPanelChrome/applyOuterShell/bindStandardBorder/bindSelectableBackground`）去注释后**活跃调用方=0**，仅定义体保留（§4.2）；G14 销账声称经逐行核实属实。
  3. `ConfigTheme.` 68→44（去注释）：ConfigScreen 34 处=桥 1（非快照）+§7.3 样本 2+纯 int 门面 31；Renderer 占位实参经**收口清零**，main 侧 `asFormTheme()` 消费仅剩 `ConfigTheme.java:137` 方法本体（兼容入口）。
  4. 终扫 `FormTheme.defaultDark` 命中行 2→20（含注释命中；main 代码行恰 8、注释 12）：**收口「同源纯 int 直读」范式的预期计数**（每文件由源码守卫「恰 N」钉死，非快照回潮）；其余模式增减与 §2 交付链逐笔对账一致。
  5. 存疑三组关账：RadioGroup option 行静态缘色（双固化，台账补注）；Chat 同族零改动文件（家族口径补注）；`ScenePalette` 死代码（P-07 后续清理候选）。
  6. G19 预演数值档案 V-1~V-5（Python 实算，台账）：选区 foreground 裁决稳固；`danger` 1.43:1=底图角色（P02 javadoc 已载入禁文本警示）；errorText/warningText/disabled 达标。

## 4. 行为审查（矩阵证据指针）

每实例按契约 §9 交付六类证据，台账逐实例行可溯：

| 维度 | 覆盖面 | 代表证据 |
| --- | --- | --- |
| 默认工厂路径 | 全部 22 控件/模板/配件：无参/无 Props 样式入参=主题派生信号 | G03 SceneButton Props.surface(null)、G04–G13 各「默认档逐项配方断言」 |
| 显式覆盖优先 | 各控件显式配方/solidDark/旧参数路径 | 契约 §3 优先级链；G17 聊天设置局部覆盖（Input/Container/Toolbar/Bubble 四员逐项映射断言） |
| 主题更新重派生 | 全线「换主题→颜色变」（P-04 口径：切换断言+assertNotEquals 两档前提+真实装配） | 各 `*ThemeTest`；PickerPanel `themeSwitchKeepsSelectionIdentityAndEffects`；ConfigTheme/ConfigScreen 桥切换用例 |
| 样本/协议不变（反向） | 「换主题→不变」 | G16 四页协议、MemberGrid/SearchResultList 图像协议、GlassLab `themeSwitchNeverTouchesDiagnosticSamples`（几何配对法） |
| 延迟构建 | portal/show/forEach 内构建源 Owner 作用域继承 | G08 Select 弹层、G10 Dialog/ContextMenu/Tooltip/Toast、G13 VariantChooser、G14 PickerPanel 浮层树（主树 6 颗/浮层 7 颗构成表） |
| 生命周期 | 卸载 effect 回基线、鬼写入封死、身份保持 | 各 dispose 用例；PickerPanel `closeReclaimsPortalTreeAndDisposeReturnsEffectsToBaseline`；聊天栈水位「增量恒定」钉（P-06） |
| PaintPlan | BACKDROP 先于底色/无不透明盖、每表面恰一颗、轻量行零滤镜 | G04–G14 命令级断言；G15/Shell 遮罩绘制顺序证明；VirtualGrid/SearchResultList §4.1 轻量档 |

**行为红线全线遵守**：未触碰 markdown/LaTeX/字体渲染/协议解析/草稿事务/HUD 布局预算（各实例报告零越界，抽查无「直连原版实现」倾向——全部落在 scene/信号/PaintCommand 自有抽象内）。

## 5. 视觉与性能：待验清单（移交用户/CI 宿主）

**状态：未执行（本地无宿主环境），本目标不伪造。** 真机验收建议按下列清单执行（记录真实制品版本+环境，对比同场景迁移前后）：

- **表面谱系**：大面板（G15 页壳/ChatContainer）→ 中表面（TOOLBAR/GROUP/信息条/操作条）→ 小控件（按钮四态/输入/开关/滑条）→ 浮层嵌套（Dialog 内 ContextMenu/Tooltip；Select 弹层；PickerPanel portal）。
- **背景谱系**：亮/暗世界、无世界背景（菜单内页）、半透明遮罩下层（ConfigScreen 掩蔽+玻璃共存）。
- **重点核对项（源自施工登记）**：
  1. **V-3/V-4**：暗实底与亮世界下轻量行 hover(0x14)/selected(0x59) 可辨性与选区文字可读性（若不足：候选提 selected alpha 或伴随 accent 条，需新实例）。
  2. Container「大面板暗边不回归」+ Input elevation=0 浮雕通道真机观感（关联 P-05 豁免位提案）。
  3. 聊天族材质档 DARK_THIN(容器)/DARK_REGULAR(气泡)/alpha 递进 与两族按钮 hover 档微差（0x90/0x50 vs 0x64/0x14、PRIMARY 0x20 vs 0x2A）是否可感（GlassLab 三档对照页为工具）。
  4. `0x26FFFFFF` LIGHT PANEL tint 与 GLASS_TINT 撞色在实验室页的可读性。
  5. 无滤镜档全线（`withoutBackdrop`/聊天玻璃开关=关/ConfigScreen 逃生舱）实心兜底可读性。
  6. HUD 缩放 50%/100%/200% + 工具栏（缩放语义保留项）与小视口。
  7. 行圆角直角带（G12 条带）、拖拽把手 idle 档、Toast SUCCESS 借 accent 点、徽标三件套、`SceneTextArea` 选区旧 token（在册挂账）。
  8. Motion 一致性：`transitionMillis=0` 保留项（Input/聊天）与主题过渡档并存是否协调。
- **性能审查（未执行）**：节点数/重建次数/快照/帧耗时需在真机以固定场景对比同制品测量；测量方案与阈值由主代理届时明确（任务单 §G19：不编数字）。代码级已有预算证据：每表面恰一颗采样、虚拟化轻量档、`声明滤镜节点数==BACKDROP 命令数` 配对断言。

## 6. 平台观察处置总表

| 编号 | 处置 | 状态 |
| --- | --- | --- |
| P-01 无 Owner 构建点 | 宿主构建点已 `__runRoot`/mount 包裹并测试钉死（G16 外壳/G18）；不改平台语义 | 关账（观察保留） |
| P-02 语义色派生缺失 | **已收编**（`ac19c22f`）：`warningText/errorText/danger/successText` 公共入口+删 7 私有版；success 委托 accent；Support issues 改染为后续候选（观感变化需批准） | 关账 |
| P-03 变异吸收论 | 方法论采纳；源码守卫成 Renderer 标配 | 关账 |
| P-04 同值陷阱 | 审查口径全线执行 | 关账 |
| P-05 浮雕通道 | **提案采纳（`reliefDisabled` 位最小改法在案）、实施排后**：全线真机复验前不再改 theme 共享通道；Container/Input 回归 binder 列真机后专项 | 后续任务 |
| P-06 聊天栈 effect 水位 | 基线债（非迁移引入），「增量恒定」已钉 | 后续任务 |
| P-07 ScenePalette 死代码 | 清理候选（公共 API 承诺评估） | 后续任务 |
| binder 初值 seed | 提案在册（与 P-05 同批排后） | 后续任务 |

## 7. 剩余风险

1. **全部视觉/性能结论未经真机**（本文 §5）——主题默认切换、玻璃叠层、HUD 缩放组合的真实观感是唯一未闭环风险；代码级不变量（预算/采样/生命周期/等值收编）已尽可能以 PaintPlan 断言前移。
2. 兼容入口（§4.2）保留但零调用，若外部 Mod 仓仍消费旧路径，行为=旧显式语义（设计如此）；业务仓接入验证不在本仓目标内。
3. `installRuntime` 保持 playground 包级；未来更多非 playground 宿主需主题构件时再议公共化（G18 已示范无扩口路径）。
4. 6 个既有 skipped 测试与 checkstyleTest 目录级告警为施工期前既有基线项。

## 8. 验收状态声明

- 代码状态：**已验收**（G00–G19/P02 全家族）。
- 视觉/GPU/Minecraft 宿主状态：**待验**（§5 清单移交）。
- 按任务单 §G19 交付定义，**本目标整体状态 = 待验**，待 §5 真机清单执行完毕并由主代理在本记录追加「真机验收」章节后转「已验收」。

### 8.0 外部评审第一轮注记（网页模型，基于审计包 v1；整改至 v2）

外审裁定：**不得签「整体交付通过」，可签「代码阶段已完成（项目方报告），外部验收未完成」**——本记录 §1 结论域按此收窄。三处实质缺陷坐实并订正（审计包 v2）：① 347 分类 v1 转录算术错（343 不闭合→v2 闭合 141+108+76+20+2）；② v1 §3.4 Role 名单凭记忆错引（正确为契约 §2.3 九值 PANEL/TOOLBAR/INPUT/GROUP/OVERLAY/INDICATOR/BUTTON_*，源码实证）；③ v1 WCAG 表按申报模型不可复现→整表作废，v2 附录 A 以 M1/M2 声明模型重算，登记平台观察 **P-10**（数字必须连模型带输入留档；PANEL tint 0x14EAF7FF、warningText light 0xFF8B5000 等权威值订正；M2 暴露两档对称不可读角升级为**默认启用门槛**）。方法类意见（覆盖矩阵/跳过项/未杀变异补证、守护测试边界、motionRoot 所有权披露、successText「无解」断言收窄、P-05 理由重述、性能判据先冻结）全部吸收进 v2；补证清单见 v2 §8。外审继续对仓库代码做独立攻击式复审中，其后续 findings 与本注记同层登记。

### 8.1 复核注记（审查主代理，HEAD `86a1bbe5`）

- 三组独立只读审查（红线合规 R01–R12／既往缺陷 §3.1 九项／覆盖销账 §5 十行）+ 主代理抽查一致结论：**当轮无红线违规、既往缺陷全部保住、覆盖表十行全部已落**；主代理实跑完整 `gradlew.bat build` exit 0（479 类 / 5155 tests / 0F / 0E / 6 skipped）复现本文 §1 数字。
- 新登记平台观察 **P-08**（Picker 族 control→config 既有向上依赖，基线先存）与 **P-09**（binder 无防重入标记），见台账；均为后续专项，不阻塞本目标代码状态。
- 文档同步修复：开发者文档 README 索引补录四件套、稳定 API 清单收录 `ui.scene.theme` 包与 `SceneToast.Entry`/`FieldShellBinder` 破坏性变更登记、台账 G19 双行合并 + G00 逐实例行补录 + 基线时间戳措辞校准、契约 EOF 空白清除、本文 §3.4 defaultDark 计数口径注明含注释行。

### 8.2 外部评审第二轮注记（审查者 A＋B，基于审计包 v2；整改至 v2.1）

- **审查者 B（数值攻击，两项全部坐实）**：F-01 候选绿表与 F-02 ΔL 行系 v2 编写时**手抄未经脚本**——`#A5D6A7` 实际 M1 双档通过（6.946799/4.565249），v2「列举域内无解」结论作废；「+129%」手抄错（实 −59.4%，口径 `(L_A−L_B)/L_B`）。整改：附录 A 整表改为 `auditA_v21_full.py` 脚本输出直接嵌入；successText 委托论证降格重建为「M2 世界包络内无稳定解（该绿对纯白 1.64 塌方）+ 扩色板属治理保留权」；V-3 判据改用 CR 三值（sel/idle 1.095、sel/hover 1.592、hover/idle 1.454），废弃无单位「ΔL<2」。P-10 闭环，纪律固化「数值表只许脚本直出」。
- **审查者 A（架构攻击，证据基线错位但概念建议有效）**：其「命题 B（Chrome 并行写入路径）」「SceneButton 特权直写」引用自 **GitHub 远端 4.0 旧快照——远端落后本地全部施工（未 push 为规范行为），登记 P-11**。本地 HEAD 实证：`SceneButton:121` 走 binder，`bindSelectableBackground(` 生产调用点 0（兼容入口保留=契约 §6 明文）。其五项架构建议（producer ownership test 矩阵、SurfaceRole×ActionVariant 拆分、材质模式显式化 LIQUID_GLASS/OPAQUE/REDUCED、ownership 统一术语、Reference Renderer 命名治理）登记审计包 v2.1 §6.5 **待用户裁决**，不阻塞真机验收；§3.1「唯一写入者」措辞已按契约原文（唯一通用实现 + 属性表 + 调用点 0 实证）精确化。
- **本轮后状态**：附录 A 数值链脚本化闭环；放行门槛维持 v2 表述（M2 默认启用门槛 + §6.1 真机清单 + §8 补证清单）；「代码阶段已完成（项目方报告），外部验收未完成」判定不变。

### 8.3 外部评审第三轮注记（审查者 A 复审，基于 v2.1 事实校正；整改至 v2.2）

A 撤回事实层攻击（远端快照≠本地基线，登记 P-11 教训），其概念层升级全部有效吸收：① 「零点」拆调用/全引用/语义三层强度，`audit_zero_proof.py` 实证后**承认 v2.1「唯一写入者」确属过度表述**——元素级轻量通道（行覆写/caret/thumb/dot/scrim 绑到控件独占子节点）真实存在，架构不变式订正为 **node 级单一写入者**（契约 §7.3 第三轮补录，审计包 v2.2 §3.1 三层 producer 画像 + 零点表脚本直出）；② 兼容入口「零使用≠零能力」三问三答立 §3.6（能力保留属兼容本体，退役路径待裁决）；③ §6.5 优先级重排：A(ownership contract 测试化)建议优先落地、B(双层零点证明)**已随 v2.2 完成**、C(术语)保留、D(Role×Variant)A 自行暂缓、E(材质模式)轻量吸收。现行源码 `src/main/java` 全量 zip 已备供 A 独立核验。判定不变：代码阶段已完成（项目方报告），外部验收未完成。

### 8.4 外部评审第四轮注记（审查者 A 源码级独立核验，签署 L-C；审计包 v2.3）A 基于 `c389ac42` 全量 `src/main/java` zip 重做核验：**L1 调用点/L2 生产引用/L3 语义归属全 PASS**；逐节点抽查 5 个虚拟化/动态行样本无同节点双写，**「node 级单一写入者」前提获外部独立证据**（含 icon 占位协议、StructuredList 无壳外框合法形态甄别）；正式签署 L-C，限定语「旧 selectable-background producer **生产使用**为零、旧能力作兼容本体保留」并明确拒签两种偷换版（零能力/全库 setter 唯一来源），v2.2 措辞经其核对合规。§6.5-A 获交付形态：JUnit4 骨架 + P1~P4 precedence 矩阵（P4 显式恒定=优先级与跟随性并钉）+ RULE-1~4 注册制守护（其明确反对正则全覆盖防误杀）——**待用户裁决立项**。外审四轮累计：数值链（B）与架构边界链（A）均已闭环至「项目方证据获独立复算/复核」水平；剩余关口不变 = §6.1 真机验收 + §8 补证清单 + L-C 退役路径/§6.5 治理裁决。

### 8.5 外部评审第五轮注记（收口；审计包 v2.4）

- **审查者 A 正式收口架构边界线**：事实基线原则（不再以落后远端反推现行代码）、L-C 限定签署、node 级单写者五样本证据、§6.5-A 立项形态锁定（四行为矩阵 + P1–P4 + RULE-1~4 注册制，特别保留 P4「显式恒定」）、Role×Variant/Material Mode/ownership 术语确认为演进项「不得错误提升为当前阻塞缺陷」；认可「唯一通用实现（实现归属）vs node 级单一写入者（运行时不变量）」为最终口径；确认总体状态维持「代码完成、真机/GPU/宿主验收待验」，不因签署提前放行。
- **审查者 B 通道终止**（评审者无法再访问）：其 F-01/F-02 整改交付至 v2.1+（脚本直出、主代理逐位一致复算）但**未获回签**——数值链状态如实记为「整改完成、评审者缺席回签、脚本化纪律与独立重算兜底」，不伪称双签闭环。
- **下一轮评审焦点（A 定义）**：§6.5-A 若立项施工，复审对象为「测试实现是否真的证明了契约，而不是把契约重新写了一遍」。
- 本记录状态声明（§8）不变：**代码阶段已完成（项目方报告，外审 A 线签署至 L-C 边界），外部验收未完成**；待 §6.1 真机门槛执行。
## 8.5 主代理独立重审（2026-09-10，HEAD `0856d8b1`）

非外审轮。目的 = 复核「外部攻击未抓到正式代码 bug」这一结论的前提确实可由本地实证支撑，而非报告转述。

| 项 | 方法 | 结果 |
| --- | --- | --- |
| 全量测试基线 | 独立跑 `gradlew.bat --offline --console=plain test` + XML 逐类统计（不引用项目方报告） | **5155 / 0F / 0E / 6 skip**，BUILD SUCCESSFUL，与报告逐位一致 |
| 6 跳过项身份 | XML `skipped` 节点 message 逐条读取 | 2 项 LaTeX 参考渲染（需外部 jar）+ 4 项 `C9DualFontProbeTest` 字体探针；原「待补」项闭环 |
| 附录 A 数值链 | 重跑 `auditA_v21_full.py` 并逐值对账 | 主表 14 值 + success 20 值 + CR/ΔL 6 值 + fg/M2 6 值全一致；**1 处脚本溯源缺口**（`onAccent-over-selBg` 不在脚本输出内，数值本身重算正确：dark 7.2322 / light 6.4412） |
| L-C 零点前提 | 独立扫 `src/main`（不复用被审方脚本）：L1 调用点 / L2 全引用 / L3 语义 sink 归属 | `bindSelectableBackground`/`bindStandardBorder`/`inputBackground`/`listItemBackground`/`applyPanelChrome`/`dangerBackground` **REF=0**；仅 3 处已披露语义 REF（Chrome→StateColors、ChromeTokens 体内自调、ConfigScreen 桥接） |
| node 级单写者 | 全部 46 文件 95 处 surface sink 的归属节点分类（宽于 A 的五样本抽查） | 无同节点 binder+轻量双写；DataTable 表头行（binder）与数据行（轻量）为不同节点实例 |
| 代码基线声明 | `git diff 7aeedc21..HEAD -- src` + `src/main` 内容 SHA256 | 差异为空（`b1a3e530…`），审计包「代码头 7aeedc21」属实 |
| push 差距 | `git rev-list --count origin/4.0..4.0` | 本地领先 **3** 个文档提交（非「远端落后 90+」），零代码改动 |

**未做（不得写成通过）**：GPU/Minecraft 宿主视觉与性能验收、`gradlew build` 全链（本次仅 `test` 任务；台账 L13 的 build exit 0 属既往记录、非本轮复核）、§6.5-A 的 `SceneSurfaceOwnershipContractTest` 实现（形态已锁定，待立项裁决）。
