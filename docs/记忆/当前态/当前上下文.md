# 当前上下文

本文件记录「当前真实状态」，服务下一位接手的 Agent 或未来会话恢复现场。只保留当前真相，信息过时直接覆盖，不写阶段流水账。

## 架构主线：scene 新栈 strangler 重写

项目已引入 UI 系统架构宪章 `NORTH_STAR.md`（仓库根目录，最高准绳），并按宪章模型全新搭建声明式新栈 `club.heiqi.uilib.ui.scene`，用 strangler fig 模式渐进绞杀旧 HTML-like / `ui.dom` 栈。

- **新栈与旧栈物理隔离共存**：新栈 `ui.scene`（node / layout / paint / component / input 五子包）与旧栈 `ui.dom` + `ui.document.HtmlLikeDocumentWidget` 完整并存，共用 `ui.reactive`（Signal/Effect/Computed/Owner/ReactiveScheduler，保留不动）与 `ui.render`（UiRenderContext，保留不动）。
- **旧栈仍是对外主路径**：当前所有对外 facade、生产页面仍走旧 dom 栈；scene 新栈目前只作为内部 devtools demo 验证栈（入口 `/qzuilib test` → SCENE_DEMO 组）。
- **strangler 5 Phase 路线图**：Phase 0 契约地基 → Phase 1 端到端最小可渲染切片 → Phase 2 三声明式基石（bindText/show/forEach + keyed 列表）→ Phase 3 分级失效全链路 + 合成级动画 → Phase 4 控件层重建（qzuilib test 分组逐组切换到 scene 栈）→ Phase 5 宿主粘合层迁移 + 旧栈退役。

## 当前进度（Phase 0-3 已合回 4.0）

- **Phase 0 + Phase 1**（地基 + 最小可渲染切片）：已合回 `4.0`。scene 七层骨架落地，SceneNode 双标记模型（脏向上冒、`descendantXxxDirty` 路标，绝不向下递归）根除旧 DOM 的 I7 粗粒度标脏债。
- **Phase 2**（三声明式基石）：已合回 `4.0`。`SceneKeyedListReconciler`（路 B 批量 + LIS）、`SceneConditionalRenderer`、`SceneRuntime` 的 bindText/show/forEach 三方法，I7 终审双铁证（列表插项稳定项零重算）。
- **Phase 3**（分级失效全链路 + 合成级动画）：已合回 `4.0`（合并提交 `89e7c1ed`）。LAYOUT/PAINT/COMPOSITE 三级解耦传导；transform/opacity 走 composite-only 通路（PUSH_OPACITY/POP_OPACITY 边界命令 + ScenePaintReplayer 配对），纯 composite 帧零重排零重建，坐实信条五铁律。
  - 已知边界（已在 NORTH_STAR 偏离登记）：`Transform` 仅 translateX/translateY 两字段，只支持整数像素 translate，rotation/scale/skew/亚像素未实现；深层 fill 子节点 top-down 约束传播未实现（仅 root fill）。
- **scene 输入半环**（I1-I4）：已合回 `4.0`。`PlatformInputSource` 入口契约线、`SceneInputRouter` 权威交互状态机、`SceneInteractionState` 懒创建 signal，守 I10（核心包零平台 import）/I11（handler 只写 signal）。

## Phase 4 控件层重建（进行中，分支 `refactor/scene-phase4-control-layer`）

- **战略路线（oracle 裁决）**：两段式混合——先补「迁任何有外观控件都绕不开」的最小地基骨架，再切片驱动逐控件迁移，严禁提前造全集。8 批路线图：第 0 段（地基，Button 试金石）→ 批1 Checkbox/Toggle（首个真实替换+真机+真实字体度量）→ 批2 Radio/Segmented/Breadcrumb → 批3 TextInput/Slider → 批4 Tab/Table（+滚动裁剪）→ 批5 Select/TextArea/CodeEditor（+绝对定位+top-layer）→ 批6 ColorPicker/Auto/DataTable/KeyValue/Tree → 批7 Overlay/Tooltip/Cursor 基础设施 → 批8 Slot/HostImage（MC 桥接仅宿主层）。每组切换是 `/qzuilib test` 入口单点开关，可一键回退，绝不半迁混用。
- **第 0 段地基已完整收口**（两提交：`a33d80d8` 地基 + `9fca7d38` 偏离 2 修复）。四轮 oracle 审查（含两次反证法验真），I7/I4/I6 三条命根均经反证验证为真，无「性能假装优化」或「测试技巧掩盖缺陷」的雷。scene 全 296 测试绿，输入半环 130 测试零回归。具体落地：
  - **flex 布局**：SceneNode 加 flexDirection/padding(4边)/gap/mainAxisAlign/crossAxisAlign（全 LAYOUT 级），枚举 `FlexDirection{ROW,COLUMN}`/`MainAxisAlign{START,CENTER,END}`/`CrossAxisAlign{START,CENTER,END,STRETCH}`（layout 包）。默认 COLUMN+STRETCH+START 零漂移等价旧垂直堆叠。I7 命门：padding/gap/对齐只是 performLayout 内定位纯计算，子节点重排仍由 `newBox.equals` 几何闸门裁决，绝不向下递归标脏。
  - **PaintCommand 扩展**：新增 BORDER/CLIP_PUSH/CLIP_POP 命令 + cornerRadius/borderWidth 字段。**UiRenderContext 零改动**（已有 pushClip/drawBorder/drawSurface 含圆角，守 I6）。CLIP 像 opacity 一样在 paintNode 骨架产出绝对坐标不进 fragment；BORDER/圆角进 fragment（纯 paint 属性复用）。圆角是 int 像素零控件语义。
  - **文本色修正**：SceneNode 加 textColor（默认 0xFFFFFFFF，PAINT 级），修掉 ScenePaintEngine 写死白色的正确性 bug。
  - **控件契约骨架**：新建 `ui.scene.control` 包（与旧 ui.control 对仗）。控件=纯静态工厂（private 构造+static create 返回 `Supplier<SceneNode>`，零实例字段），Props 用 @Desugar record（全 ReadableSignal+回调）。SceneButton 作试金石撞齐 7 能力点（居中/padding/边框/胶囊圆角/裁剪/非白文字/四态），R-D1 验真四态切换只 PAINT 不 LAYOUT（relayoutCount==0）。package-info 写死契约红线 R1-R6。
  - **hitTestable 命中穿透（偏离 2 修复，方案 B）**：SceneNode 加 `hitTestable`（默认 true，**不标脏**，cursor 同款例外）+ SceneHitTester 自身命中前跳过。装饰子节点（label/图标/滑轨）setHitTestable(false) 使命中穿透到控件根节点（交互单元），交互态绑 root。根治「容器挂交互态/命中叶节点」拓扑错配——否则批1-2 所有复合控件会系统性复制「点文字按钮不亮」缺陷。中间节点语义：hitTestable=false 只剔除「叶命中目标」资格，有子命中时仍作链路径锚点出现（为 Segmented 段内穿透预留）。
- **第 0 段已登记/交接的约束**：
  - 偏离 1（已解除）：原 ROW+CENTER 主轴偏移恒 0，根因真实字体度量缺失。**批 1 前置②真实字体度量已落地解除**（见下「批 1 前置②」），叶节点已能 shrink-to-fit 到文字实际宽度，ROW+CENTER 偏移恢复非 0。
  - R6 边界待批 1 补一句：交互态 signal 必须绑在 hitTestable=true 的命中单元节点上（绑装饰节点永远收不到 pressed）。

## 批 1 前置②真实字体度量（已完成并提交 `5cb593fd`）

- **目标**：解除偏离 1，为后续控件视觉验收提供可信字体度量基础。已落地并提交 `5cb593fd`，scene 全套 292 测试绿、`jetbrainsBuildProject` 通过。
- **方案（oracle 裁决，新开 session）**：scene 核心新造窄端口 `SceneTextMeasurer`（measureWidth/lineHeight/epoch 三方法，零依赖），装配层 adapter `TextMeasureServiceSceneAdapter` 委托既有 `ui.text.TextMeasureService`（守 I6 不重造度量 + strangler 卫生，scene 核心 import 图 100% 内部）。
- **落地**：
  - 新建 `ui/scene/text/SceneTextMeasurer.java` + `TextMeasureServiceSceneAdapter.java`。
  - `SceneLayoutEngine` 构造注入 measurer；删 `DEFAULT_LINE_HEIGHT`/`DEFAULT_CHAR_WIDTH`；`computeWidth` 对文本叶 shrink-to-fit（`min(outerWidth, measureMaxLineWidth+padH)`，多行取最大行宽）；行高改 `measurer.lineHeight`；新增 epoch 失效链（`lastMeasureEpoch` + `measuredTextNodes` IdentityHashMap，epoch 变时逐个 `markSelfLayout` **只向上冒泡，零向下递归**，守 I7）。
  - `SceneNode` 加 `fontSizePx`（默认 16）+ `getFontSize/setFontSize`（去重后 markSelfLayout+markSelfPaint，与 setText 同级）；fillParentHeight 注释更新（hack 已解除）。
  - `ScenePaintEngine` 清除「height-当-字号」hack，fontSize 改读 `node.getFontSize()`，删 `DEFAULT_FONT_SIZE`。
  - 装配点 `internal/devtools/pages/SceneHostWidget` 注入 `TextMeasureServiceSceneAdapter(DefaultTextMeasureService.getInstance())`，删旧无参构造器（6 个测试调用点统一注入确定性 `FixedTextMeasurer` 替身 charW=8/lineHeight=16）。
  - `ScenePackageIsolationTest` 渲染纯度断言扩到 layout/paint/**node** 三子包（禁 UiRenderContext/FontRenderer/ui.text.*，按文件名排除合法回放接缝 ScenePaintReplayer）。
- **I6/I7/I10 三命门已主 Agent 独立验真**（非仅信 fixer 报告）：epoch 失效链确认零向下递归、shrink-to-fit 走 `newBox.equals` 几何闸门、node 包扫描确认实际零平台/渲染污染。
- **D5 不加 measured 缓存**：现有 cachedLayout 已防热路径 measure（叶 performLayout 仅在 selfDirty/无缓存时跑），再加重复缓存违反 NORTH_STAR §6 判据。
- **本轮未做（保持 YAGNI）**：R6 补句（批 1 控件落地时顺手）、Checkbox/Toggle 迁移本体、窄端口 trim/wrap（controls 需要时再扩）、容器 shrink-to-fit（与已登记「深层 fill 约束下传」偏离同源）。

## 批 1 前置：固定尺寸地基能力（已完成并提交 `237e8a33`）

- **背景**：oracle 评估批 1 迁移契约时抛出前置阻塞——scene 布局引擎原无显式宽入口（宽走 shrink-to-fit/STRETCH 派生），Checkbox box(16×16)/Toggle track(48×24)/thumb(18) 等固定尺寸无法表达。用户拍板「先正式评估再实现」。
- **方案（oracle 评估，复用固定尺寸评估 session）**：新增 `preferredWidth` 字段（与既有 `preferredHeight` 对称，0=不约束），严禁 min/max/flexGrow（YAGNI）。`preferredWidth/Height` 语义=最终盒外尺寸（含 padding），与 LayoutBox 一致。**判定属 flex 模型合理补全，不算宪章偏离、无需登记**（纯 bottom-up 内在尺寸，不触碰任何信条/不变量，与「深层 fill 约束下传」偏离正交）。
- **落地**：
  - `SceneNode` 加 `preferredWidth`（默认 0）+ `setPreferredWidth`（去重 markSelfLayout，只向上冒）+ `getPreferredWidth`。
  - `SceneLayoutEngine.computeWidth` 加最高优先级分支 `preferredWidth > 0 → 返回 preferredWidth`，优先级 **preferredWidth > 容器fill > 文本shrink-to-fit > 无文本fill**。
  - `computeContentHeight` 容器分支也吃 preferredHeight 作下限（外尺寸语义，padV 不重复加）。
  - **STRETCH 豁免（灵魂）**：performLayout cross 维度 STRETCH（默认值）时，子节点在 cross 维有显式 preferred（row 看 preferredHeight、column 看 preferredWidth）则保内在尺寸不被拉满——否则固定小方块放进默认 STRETCH 容器会被拉满，固定尺寸形同虚设。Checkbox box/Toggle thumb 落地全依赖此豁免。
  - **基准对齐（oracle 复核闭环的缺陷修复）**：performLayout 排子用 `computeWidth(node)` 解析盒宽，layoutInternal 给子约束也须用同一基准（条件化 `if(!children.isEmpty())`，仅容器进、叶零开销）。两端基准必须统一，否则固定宽容器的「依赖约束宽子节点」按裸约束宽布局而溢出父盒。两处加耦合不变式注释固化。
- **oracle 复核抓到的真实缺陷（已闭环）**：fixer 首版只改 performLayout 一端、漏改 layoutInternal 另一端，基准分裂致溢出。300 测试全绿是**假绿**（批 1 控件凑巧免疫：check 走 shrink、thumb 自带 preferredWidth）。补 T9 回归（先复现红 D.width==200 溢出、修后转绿 D.width==48）+ 对齐基准，现 301 测试真绿。
- **I7 三命门主 Agent 独立验真**：computeWidth 纯读不标脏、调用在双 false 跳过判定之后（干净子树仍整棵跳过）、几何变更仍走 `newBox.equals` 闸门、无向下递归标脏。
- **本轮未做（保持 YAGNI）**：批 1 控件实现本体（Checkbox/Toggle，地基已就绪可正式开工）、indeterminate 三态、Toggle 滑块平滑动画（依赖 transform 通路完善，与偏离 2026-06-20 同源）、R7 受控双向范式红线（用户拍板批 1 先按范式实现暂不写 package-info）。

## 下一步

- **批 1：Checkbox/Toggle 迁移**（strangler 首个真实替换，地基已全部就绪可开工）。两条前置均已清：前置②真实字体度量已提交 `5cb593fd`（偏离 1 解除）；固定尺寸地基已提交 `237e8a33`（preferredWidth + STRETCH 豁免）。**oracle 已产出批 1 迁移契约方案**（待执行）：checked 双向走「受控组件」`ReadableSignal<Boolean> checked` 入 + `Consumer<Boolean> onChange`（传期望新值）出，控件零内部状态守 R1/R2/R4/I11；批 1 只做二态 Checkbox + 二态 Toggle（indeterminate/滑块动画排后续批）；demo 用独立屏幕托管（新建 `SceneControlsDemoScreen`，不在旧 Controls 组原地混用，旧控件留回退基线）。前置①补 R6 边界一句仍待批 1 第一个控件落地时顺手加。完成后进真机帧率/上屏验收（交用户跑）。
- **oracle 复用纪律**：第 0 段 ora-1 与前置②的 oracle session 上下文均已用过，批 1 起需 oracle 时**新开 session**（传当前态记忆+成果摘要重建上下文），不复用旧 session。

## 待用户验收（非阻塞）

- **scene Phase 1-3 真机帧率/上屏验收待用户跑**（沙箱无 GUI）：入口 `/qzuilib test` → SCENE_DEMO 组 → 打开 demo 页。验收点：opacity/transform 改动真正刷上屏、纯 composite 帧零重排零重建、空格切背景（PAINT）、T 切文本（LAYOUT）。真机后派子代理查 `run/client/logs/fml-client-latest.log`，`club.heiqi.uilib` 应零异常。

## 当前分支与发布

- 当前工作分支：`refactor/scene-phase4-control-layer`（从 `4.0` 切出，Phase 4 控件层重建）。已落地四提交：`a33d80d8` 第 0 段地基 + `9fca7d38` 偏离 2 修复 + `5cb593fd` 批 1 前置②真实字体度量 + `237e8a33` 固定尺寸地基（preferredWidth + STRETCH 豁免）。工作树对源码干净（仅未跟踪 `.slim/`、`opencode.json`）。Phase 4 完成后按 AGENTS.md 用 `git merge --no-ff` 合回 `4.0`。
- 主分支 `4.0`：scene Phase 0-3 全部已合入。
- 发布回合以 `release/4.x` 系列分支为目标；`4.2.x` 已发布，Release 正文来自 `.changelogs/<version>.md`，本地验证结果不写入远端 Release 正文。

## 当前约束

- 具体实现细节必须以当前源码、Gradle 配置和专题文档为准，不从历史对话推断。
- 涉及架构性改动，开工前先对照 `NORTH_STAR.md`《核心信条》《关键不变量》《决策检查清单》，确认未踩《反模式》；偏离须按《修订纪律》显式登记。
- 涉及对外接入能力变化时，同步更新 `docs/使用文档/`；可复现错误或审查结论写入 `docs/开发者文档/errors/` 或 `docs/开发者文档/reviews/`。
- 不把临时调试记录、一次性试验结论、阶段流水账写入本文件或 `docs/AI记忆文档.md`。

## 当前阻塞

- 无源码级硬阻塞。Phase 4 批 1（Checkbox/Toggle 真实迁移）可开工，第 0 段所有阻断前置已清（偏离 2 修复 + R6 写入 + 测试 6 翻转均完成）。
- 批 1 一条非阻断交接提醒：① R6 补一句「交互态 signal 必须绑 hitTestable=true 命中单元节点」边界（第一个控件落地时顺手加）。前置②真实字体度量已落地（偏离 1 已解除，label 已能 shrink-to-fit 到真实宽度），原「文字仍占位度量、居中偏移再现」的提醒已失效。
- Phase 5（旧栈退役）依赖 dom 旧栈在 Phase 4 完成后才能退役，当前不可启动。
