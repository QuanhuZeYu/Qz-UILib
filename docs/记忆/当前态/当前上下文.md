# 当前上下文

本文件记录“当前真实状态”，服务下一位接手的 Agent 或未来会话恢复现场。

## 当前阶段

- `/qzuilib test` 页面系统性重构已进入视觉优先矩阵阶段，详细矩阵见 `docs/开发者文档/specs/qzuilib-test-page-visual-matrix-plan.md`
- Phase 1 已收口合并回 `4.0`
- Phase 2 审查已完成：系统性检查全子系统，发现 28 处语义不一致（详见 `docs/开发者文档/reviews/REVIEW-20260601-browser-semantics-phase2-audit.md`）
- Phase 2 首批 P0/P1 浏览器语义修复已通过完整测试并合并回 `4.0`
- Phase 2 第二批浏览器语义修复已完成，合并回 `4.0`，并通过独立代码审查（详见 `docs/开发者文档/reviews/REVIEW-20260601-browser-semantics-phase2-followup.md`）
- Phase 2 第三批 P2 事件语义修复已完成并合并回 `4.0`，覆盖 `wheel` DOM-like 事件默认滚动前分发
- Phase 2 第四批 fixed clip chain 修复已完成，覆盖 transform 祖先作为 fixed containing block 时的布局、绘制/命中共享 clip 语义
- 已完成浏览器能力缺口复核（详见 `docs/开发者文档/reviews/REVIEW-20260601-capability-gap-recheck.md`）：确认 `REVIEW-20260518` 正文结论失效，当前真实剩余缺口约 23 主项，并按"有意边界 / 性价比待评估真缺口"分级
- 当前推进策略已切换：暂不继续推进新功能、新样例扩展或 B 类缺口填补，阶段重点改为查找 bug、代码审查和可控优化建议

## 当前目标

- 短期内不推进新功能、新样例或 B 类缺口填补，当前目标以查找 bug、代码审查和优化建议整理为主
- 审查重点优先覆盖 `/qzuilib test` 视觉/语义矩阵、HTML-like 文档控件、浏览器语义实现、测试组织和已知人工确认边界
- 发现问题时先记录复现条件、影响面、验证方式和修复优先级；只有用户明确要求时再进入修复实现
- 对优化建议优先区分真实 bug、行为风险、测试缺口、性能/可维护性改进与有意能力边界
- 继续沿用现有 `docs/记忆/`、`docs/使用文档/`、`docs/开发者文档/` 分层沉淀体系

## 当前状态

- 当前工作分支：`fix/production-code-round6-findings`，从 `4.0` 创建；生产代码第六轮审查 5 个 findings 已完成源码修复、文档同步、定向测试、`compileJava`、`git diff --check` 和分支提交
- 当前主分支：`4.0`；生产代码第五轮修复已在 `fix/production-code-round5-findings` 验证通过后按规范合并回 `4.0`
- 生产代码第六轮审查修复已留档到 `docs/开发者文档/reviews/REVIEW-20260609-production-code-round6.md`；对应修复分支 `fix/production-code-round6-findings` 已完成 5 个 findings 的最小修复：keyframe forwards fill 按 `animation-direction` 与最终迭代奇偶写入终值、transform 后滚轮/滚动条命中复用 hit-test inverse transform、`RemoteHudSubmitEvent.dismiss()` session 精确关闭、远程页面 screen session/generation 生命周期绑定并避免手动关闭后旧 expired 弹错误页、`display:none` 中断运行中 transition 派发 `transitioncancel`
- 生产代码第三轮审查已留档到 `docs/开发者文档/reviews/REVIEW-20260608-production-code-round3.md`；对应修复分支 `fix/production-code-round3-findings` 已完成 4 个 findings 的最小修复：`textInput.preventDefault()` 阻止内置文本改值、HUD top-layer 后代命中预过滤补齐、mounted `UiStyleSheet` 变更触发缓存失效、移除展开 select 子树后清理 top-layer 注册
- 生产代码第四轮审查已留档到 `docs/开发者文档/reviews/REVIEW-20260608-production-code-round4.md`；对应修复分支 `fix/production-code-round4-findings` 已完成 5 个 findings 的最小修复：select DOM detach 触发控件级关闭并恢复 popup inline 样式、公开 `UiStyleDeclaration.copyFrom(...)` 触发 layout/paint 失效且 clone 走静默复制、select top-layer popup 按 transform/动画后的视觉边界锚定、HUD 宿主隐藏改用 `display:none` 抑制整棵注册子树、popup 关闭后按最新 pointer 立即刷新 hover/cursor
- 生产代码第五轮审查修复已留档到 `docs/开发者文档/reviews/REVIEW-20260608-production-code-round5.md`；对应修复分支 `fix/production-code-round5-findings` 已完成 3 个 findings 的最小修复：HUD dismiss 非空 session 精确匹配、远程页面旧 stream 回调按当前 session/generation 丢弃、远程 HTML session TTL 过期对页面显示错误页并对 HUD 发送 session-scoped dismiss；TTL 口径决策见 `docs/记忆/决策/DECISION-20260608-remote-html-session-ttl.md`
- 当前协作工具：仓库 `.opencode/opencode.json` 已显式注册 `codegraph` MCP；Windows 下通过 `cmd.exe /d /s /c` 启动 `npx -y @astudioplus/codegraph-mcp --graph-only`，避免 npm `.cmd` shim 无法被 opencode 直接执行和重复 `--mcp` 参数；本机全局安装只作为个人加速，后续代码关系探索优先用 CodeGraph 缩小范围，再用源码和测试确认
- `/qzuilib test` 当前状态：旧菜单、旧子页和旧卡片矩阵仍保持清空；入口保留 DOM / CSS / Layout / Paint / Input / Controls / TextFont / Animation / RuntimeHost / RemoteNet 十组导航
- P0 视觉矩阵首页框架已落地：`UiTestDocumentPageController` 仅保留页面生命周期、导航和环境刷新；`UiTestMatrixRegistry` 记录十组规格、首轮计划数量和功能画廊；`UiTestGroupVisualBuilder` 生成首页/二级页框架；`UiTestSemanticChecker` 描述自动/人工边界；`UiTestMatrixState` 维护视觉状态、语义状态和汇总状态
- 当前 registry 已接入 53 张 DOM / CSS / Layout / Paint / Input / Controls / TextFont / Animation / RuntimeHost 核心视觉样例，首轮计划数量按规格表合计 59 张，当前已接入 53 张、剩余缺口 6 张；具体视觉舞台由 `UiTestSampleVisualFactory` 负责分派，DOM 专项舞台由 `UiTestDomVisualFactory` 承载，Input 专项舞台由 `UiTestInputVisualFactory` 承载，Controls 专项舞台由 `UiTestControlsVisualFactory` 承载，TextFont 专项舞台由 `UiTestTextVisualFactory` 承载，Animation 专项舞台由 `UiTestAnimationVisualFactory` 承载，RuntimeHost 专项舞台由 `UiTestRuntimeHostVisualFactory` 承载；首页已压缩为总览、分组导航、最近失败、人工确认和环境信息，二级页为单样例翻页视图并只显示短状态/短日志
- 当前 `/qzuilib test` 新增运行时断言首批链路：`UiTestAssertionRunner` 负责当前样例自动断言，`UiTestAssertionLogger` 记录结构化完整日志，`UiTestMatrixState` 支持样例结果回写和分组状态刷新；自动断言已覆盖 DOM 全部 7 张样例，RuntimeHost 的 runtime stats 摘要 1 张样例，以及 CSS / Layout / Paint / Input / Controls / TextFont / Animation 中除 7 张人工待确认样例（inline-block baseline、transform 相关、host image fallback、focus-visible 游戏内观感、TextFont fallback 字形观感、Animation layout-vs-paint 运动节奏）外的 34 张基础样例，合计自动 42 张、人工 11 张；DOM 专项断言由 `UiTestDomAssertionRunner` 承载，Input 专项断言由 `UiTestInputAssertionRunner` 承载，Controls 专项断言由 `UiTestControlsAssertionRunner` 承载，TextFont 专项断言由 `UiTestTextAssertionRunner` 承载，Animation 专项断言由 `UiTestAnimationAssertionRunner` 承载，RuntimeHost 专项断言由 `UiTestRuntimeHostAssertionRunner` 承载；页面提供“一键测试全部”，会逐个渲染并执行所有已接入样例，完整 computed style/布局盒/事件日志/差异仍保存在结果和日志中但不再铺满页面；RuntimeHost 的 4 张人工样例已完成游戏内简单确认且未发现问题
- `VIS-CTRL-003` 已自动断言 textarea Ctrl+A 替换、value、change 日志和布局盒；`VIS-CTRL-005` 已自动断言 select top-layer、真实鼠标命中选项选择、value、change 日志和 table 同步；`VIS-CTRL-007` 已覆盖 Tab roving `tabindex`；`VIS-PAINT-006` 已自动断言 overflow、scroll range 和 scrollTo 偏移变化；三者真实 caret/selection 观感、popup 位置/遮挡和 scrollbar track/thumb 几何/拖拽仍可作为截图观察项，但不再占用矩阵人工待确认状态
- `HtmlLikeDocumentWidgetTest` 已完成组织拆分并收口为基础后端契约测试：共享 helper/fixture 抽到同包 `HtmlLikeDocumentWidgetTestSupport`，拖拽相关测试迁移到 `HtmlLikeDocumentWidgetDragTest`，滚动/滚轮/滚动条/ElementNode scroll API 相关测试迁移到 `HtmlLikeDocumentWidgetScrollTest`，焦点、focusin/focusout、Tab 遍历、textInput/key event、disabled focus、raw button/input 键盘行为相关测试迁移到 `HtmlLikeDocumentWidgetFocusKeyboardTest`，静态布局缓存复用、block/flex/table 子树平移复用和 DOM/样式/文本测量 epoch 布局缓存失效相关测试迁移到 `HtmlLikeDocumentWidgetLayoutCacheTest`，transition/keyframe/declared keyframe、animation/transition end、runtime geometry、paint-only refresh 与 top-layer popup animation 相关测试迁移到 `HtmlLikeDocumentWidgetAnimationRuntimeTest`，渲染/文本/图片 fallback 迁移到 `HtmlLikeDocumentWidgetRenderingTest`，基础命中迁移到 `HtmlLikeDocumentWidgetHitTest`，HUD 风格运行态布局迁移到 `HtmlLikeDocumentWidgetHudRuntimeTest`，click/dblclick/contextmenu/active/hover/mouseup/link activation 迁移到 `HtmlLikeDocumentWidgetEventDispatchTest`；本阶段未改生产代码语义
- 当前字体日志策略已收敛：新增 `Config.fontRuntimeDebug` 开关，高频字体运行时日志默认关闭；`FontService`、`GlyphGenerationDispatcher`、`GlyphPageManager`、`FontBatchRenderer` 与 `FontRuntimeDiagnostics` 仅在该开关启用时输出高频诊断信息，避免淹没运行时断言日志
- 表单控件可见性已增强：`DocumentLayoutEngine` 为原生 `input height:auto` 保留一行 line-height 内容高度，避免空值输入框高度塌缩；`DocumentTextAreaControl` 使用独立高对比 caret 颜色、2px 宽度和 `setCaretColor(...)`，并修正 selection/caret 自定义渲染的文档坐标到屏幕坐标转换，避免 widget 非零偏移时光标被画到错误位置
- 已修复游戏内运行日志暴露的 textarea 删除换行崩溃：删除换行后旧 `visualLineMetrics` 可能引用已不存在的逻辑行，`isSoftWrappedVisualLine` 现会校验 `logicalLineIndex` 边界；错误沉淀见 `docs/开发者文档/errors/ERROR-20260602-textarea-stale-visual-line-cache.md`
- 新 test 页面后续规划要求：先保持语义分组导航，不恢复旧页面名结构；运行时用例字段与状态不沿用当前已清空矩阵，需重新设计
- 首批 P0/P1 修复已落地：
  - `DocumentNode.insertBefore`/`replaceChild` 同父移动时先移除再重算参考索引，并补齐 `appendChild`/`insertBefore` 返回插入节点语义
  - `DocumentLayoutEngine` 按 max 再 min 应用尺寸约束，确保 `min-width/min-height > max-*` 时 min 胜出，并修正正负/全负相邻 margin collapse
  - `DocumentEffectChain` 将 `position:fixed` 视为 CSS-like stacking context，并让 overflow 子内容 clip 可达性按 padding box 圆角判断
  - `ElementSemantics` 将 `disabled` 作为 HTML 布尔属性处理，`disabled="false"` 仍禁用
  - `FlexLayoutHelper` 支持 row flex item 默认 `min-width:auto` 与 `flex-basis` 的 border-box 换算
- 第二批浏览器语义修复已在当前工作分支落地：
  - `DocumentNode.removeChild` 改为返回被移除节点，非直接子节点报错；`DocumentQuerySupport.querySelector*` 不再把内部根节点作为 document 查询结果
  - `DocumentFocusManager` 将 `focusin` 限定为获得焦点，并新增独立 `focusout` 冒泡事件；焦点切换按 `focusout` → `focusin` → `blur` → `focus` 分发
  - `DocumentMouseEventDispatcher` 将 hover/active 作为状态通知，不再被目标 handler 返回值截断祖先通知
  - `UiStyleProperty` 修正 `border-collapse` 继承与 `font-style` 布局失效；复核确认 `text-shadow` 按 CSS 标准应保持继承
  - `DocumentPaintEngine` 将 inset box-shadow 绘制在背景之上、边框之下
- 第三批浏览器语义修复已合并回 `4.0`：
  - 新增 `DocumentElementWheelEvent` / `DocumentElementWheelHandler` 与 `setWheelHandler` / `setCaptureWheelHandler`
  - `HtmlLikeDocumentWidget.onMouseScroll` 先分发 wheel DOM-like 事件，再执行默认滚动；`preventDefault()` 阻止默认滚动，handler 返回 `true` 仅停止传播
- 第四批浏览器语义修复已合并回 `4.0`：
  - `transform` 祖先会建立 fixed containing block，fixed 后代改为相对该祖先 padding box 定位
  - `DocumentVisualTraversal` 在 fixed 后代位于 transform containing block 内时保留祖先 scroll/clip 传播，不再无条件清空 clip chain
- Phase 2 剩余语义工程化修复已完成：已补空块/递归 margin collapse、row flex `align-content`、flex 交叉轴 auto margin、absolute auto margin 居中、table auto 内容列宽、textInput capture 阶段、transform fixed containing block 下滚动范围一致性；sticky stacking context 复核为审查误报；`inline-block baseline` 仍需后续单独改行内布局盒落位模型
- 已完成运行时页面浏览器语义回归复查与修复：row flex item `min-width:auto` 改用 CSS-like min-content 测量；诊断页、运行时自检、配置模板和字体排序中确需等分收缩的 row flex 子项显式声明 `min-width:0`（详见 `docs/开发者文档/reviews/REVIEW-20260602-flex-min-content-runtime-pages.md`）
- 已修复 `PAINT-005` 暴露的 transform 内文本绘制语义：`DocumentPaintRenderer` 在 transform 栈内禁用延迟文本批处理，避免字体 batch flush 使用屏幕坐标绕过父元素矩阵；错误沉淀见 `docs/开发者文档/errors/ERROR-20260602-transform-text-deferred-batch.md`
- 已修复 `VIS-PAINT-007` 暴露的宿主图片缺失资源 fallback 问题：`MinecraftHostImageRenderer` 在绑定纹理前检查 `ResourceLocation` 是否存在，缺失时跳过贴图绘制并保留 UILib 元素底色，避免 Minecraft 默认紫黑 missing texture 泄漏；错误沉淀见 `docs/开发者文档/errors/ERROR-20260606-host-image-missing-texture-fallback.md`
- 当前最新实现：HTML-like 脏子树布局缓存已支持静态 `display:block` / `display:flex` / `display:table` / `display:inline-block` / `display:none` 未脏子树复用，并在 containing 宽高、forced size、节点版本和文本测量 epoch 不变时允许 `containingLeft` / `flowTop` 变化，通过整体平移复用上一轮盒树；仍保守排除 absolute/fixed 盒、transform fixed containing block 子树、display:inline fragment/text run 级 inline formatting 和 layout 动画运行态复用；本阶段已评估 display:inline fragment/run 级缓存，因需要独立行盒/fragment 协议而暂不打开
- 当前最新测试：`HtmlLikeDocumentWidgetTest` 聚焦基础后端绘制与空尺寸契约；`HtmlLikeDocumentWidgetRenderingTest` 聚焦 wrapped text、UILib raw/Minecraft formatted 文本、图片 alt fallback 和字体样式绘制；`HtmlLikeDocumentWidgetHitTest` 聚焦基础命中；`HtmlLikeDocumentWidgetHudRuntimeTest` 聚焦 HUD 动态文本和固定高度 flex-grow 滚动区运行态布局；`HtmlLikeDocumentWidgetEventDispatchTest` 聚焦 click、dblclick、contextmenu、link activation、active、hover 和 mouseup 事件分发；`HtmlLikeDocumentWidgetAnimationRuntimeTest` 聚焦 paint-only/effect/layout transition、layout runtime diagnostics、runtime geometry hit-test/click、declared keyframe、forwards fill、animation/transition end 与 top-layer popup animation；`HtmlLikeDocumentWidgetLayoutCacheTest` 聚焦静态布局缓存复用、block/flex/table 子树平移复用、脱流后代保守重排，以及 DOM 移动、继承样式、样式表、`textMeasure` epoch 触发的布局缓存失效；`HtmlLikeDocumentWidgetDragTest` 聚焦 DocumentDraggableSupport fixed/relative/static/right-bottom 锚定、拖拽阈值、click 保留和 HTML-like draggable 事件坐标；`HtmlLikeDocumentWidgetScrollTest` 聚焦 overflow/wheel、wheel DOM-like 分发、HUD 内部滚动、根/嵌套滚动条、滚动命中/hover 刷新和 ElementNode scroll API；`HtmlLikeDocumentWidgetFocusKeyboardTest` 聚焦 focus、focusin/focusout、Tab traversal、Tab 触发滚动、raw button/input keyboard、textInput/key event 和 disabled focus 行为；`HtmlLikeDocumentWidgetInlineLayoutCacheTest` 聚焦 inline-block 在 inline formatting 中的换行平移、落位/baseline 后续文本、文本测量 epoch 失效和脱流后代边界，并覆盖 display:inline 多行 fragment、嵌套 inline、text-indent、text-align、文本测量 epoch、继承样式变化和脱流定位边界回归，确认当前保持重算且不误报复用
- 当前最新验证：生产代码第六轮 findings 修复已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.animation.DocumentAnimationTimelineTest"`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.document.HtmlLikeDocumentWidgetAnimationRuntimeTest"`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.layout.DocumentScrollStateTest"`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.document.HtmlLikeDocumentWidgetScrollTest"`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.remote.*"`、`./gradlew.bat --no-configuration-cache compileJava`；生产代码第五轮 findings 修复此前已通过 `git diff --check`、远程包测试与 `compileJava`；更早还通过第三轮/第二轮 findings 定向回归、`DocumentPaintEngineTest`、`UiDocumentTest`、`DocumentEventPropagationTest`、`UiStyleResolverTest`、`HtmlLikeDocumentWidget*Test`；RuntimeHost 的 `VIS-HOST-001` / `VIS-HOST-002` / `VIS-HOST-004` / `VIS-HOST-005` 已经 `runClient21` 简单人工确认未发现问题，未提供截图或专门日志路径
- 曾通过完整 `./gradlew.bat --no-configuration-cache test`；后续视觉样例批次需继续按批验证
- 已知有意能力边界（非待修复项）：keyframe per-stop timing、完整 Web Animations API、CSS `var(...)` 声明级解析、`align-items:baseline` 等价 START、CSS Grid、transform 矩阵堆栈、gradient、float
- B 类缺口当前进度：input type（password/number）已完成；font-family 因底层无字体族维度跳过并记录决策；textContent 已完成；textarea 软换行已通过“逻辑行 + 视觉行”两级模型完成；background-image 的 CSS `url(...)` 解析已完成，远程 CSS 支持 `ResourceLocation` 单图 URL 与 `none`，仍不扩展 HTTP 背景图、渐变、多背景或 repeat/position/size

## 当前约束

- 具体实现细节必须以当前源码、Gradle 配置和专题文档为准，不从历史对话推断
- CodeGraph MCP 查询结果只作为定位和影响分析辅助，不能替代源码读取、定向测试和 `compileJava`
- 不把临时调试记录、一次性试验结论、阶段流水账写入 `docs/AI记忆文档.md`
- 涉及对外接入能力时，仍必须同步更新 `docs/使用文档/`
- 涉及可复现错误或审查结论时，仍必须写入 `docs/开发者文档/errors/` 或 `docs/开发者文档/reviews/`

## 当前阻塞

- 无源码级硬阻塞；当前主动暂停新功能推进，除非用户明确要求修复，否则优先输出审查结论、风险点和优化建议；脏子树布局缓存阶段已对 display:inline fragment/text run 级缓存做保守收口，后续除非先设计独立 fragment/run 缓存协议，否则不要直接打开；`inline-block baseline` 仍属更完整浏览器语义工程项，需单独重构行内布局盒落位模型

## 更新规则

- 只保留当前真相，不写阶段流水账
- 信息过时后直接覆盖，不做按日期追加
