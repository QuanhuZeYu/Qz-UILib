# 当前上下文

本文件记录「当前真实状态」，服务下一位接手的 Agent 或未来会话恢复现场。
只保留当前真相，信息过时直接覆盖，不写阶段流水账。
已完成事项的 commit 级流水归档在 `docs/记忆/当前态/历史/`，不在本文件堆积。

## 架构主线：scene 新栈 strangler 重写

项目按 `NORTH_STAR.md` 宪章搭建声明式新栈 `club.heiqi.uilib.ui.scene`，
用 strangler fig 模式渐进绞杀旧 HTML-like / `ui.dom` 栈。

- 新旧栈物理隔离共存，共用 `ui.reactive` 与 `ui.render`；旧栈已退出业务接入，保留作废弃参考
- 业务接入默认走 scene 新栈：`/qzuilib test` → scene hub，`/qzuilib legacy_test` 保留旧栈参考回归
- strangler 路线图：Phase 0-3 已合回 4.0；Phase 4 控件层已大批完成并合回 4.0；Phase 5 旧栈退役阻塞中

## 当前阶段状态

- **现代化配置页全新思维模型已落定**（2026-06-28 第 131 次交接）
  - 确立三态四层软依赖架构（Authority/DraftBuffer/Persistence 三态 +
    核心层/UI层 四层 + config 零硬依赖 uilib 软依赖）
  - 废弃 DECISION-20260623，不参考旧栈完全重新设计
  - 详见 `docs/记忆/决策/DECISION-20260628-modern-config-new-mental-model.md`
  - 下一步落 Schema/Authority/DraftBuffer/Persistence/EventBus/UI 层适配（独立实现任务）
- **L1 嵌套 grow 子容器缺陷已修复**（2026-06-28 第 130 次交接）
  - 从"有意边界"重新定性为"不对称判定缺陷"（Oracle 裁决，CSS §9.8 definite 语义）
  - priorKnownInnerHeight 闸门放宽对齐 computeHeight 三合流口径 + 排除 scrollable
  - 144 测试全绿（含 11 新增回归测试），守 I7
  - 详见 `docs/记忆/决策/DECISION-20260628-scene-l1-grow-prior-asymmetry.md`
- **scene 技术债伪债务清理完成**（2026-06-28 第 129 次交接）
  - B5/B8 已还清未标记 → 改"已还清"（commit 23bf3a94/16dd6d56）
  - A6 口径过时 → 改"已裁决保留为有意设计"（commit 16dd6d56）
  - L3 有意边界误登 → 移出债务清单到"有意边界"分区
  - L2≡B4 重复登记 → 合并去重
  - 详见 `docs/记忆/长期事实/scene技术债.md`
- **SceneSlider 缺陷 D 已根治并合回 4.0**（2026-06-28，commit c37b1b3c）
  - 详见 `docs/记忆/决策/DECISION-20260628-scene-slider-defect-d-fix.md`
- **scene 布局引擎四期 deepwork 全部完成并合回 4.0**（2026-06-28 第 126 次交接）
  - 详见 `docs/记忆/决策/DECISION-20260628-scene-min-max-clamp.md`
- **scene 控件层 Phase 4 已大批完成**：Checkbox/Radio/Slider/TextInput/Select/Tab/DataTable/
  TextArea/SimpleList/KeyValueMap/ObjectField/Breadcrumb + primitive/wrapper 范式 + 统一 chrome
  + 滚动地基 + overlay 保护节点机制
- **overlay P0 地基已落地并转正为宪章 §4/§4.5 一等能力**

## 当前分支与发布

- 当前工作分支：`feat/modern-config-p0-core`（HEAD `b90eb53e`）
- 工作区状态：干净，全量测试绿
- 最近进展：ArchUnit 自动守卫移除（`7b20c36c`）+ L2 纯数学边界转评审纪律文档化
  （`539c0843`）+ 全仓「保留自建」旧注释统一为 §7.1 归类格式（`b90eb53e`）
- 发布回合以 `release/4.x` 系列分支为目标；`4.2.x` 已发布，
  Release 正文来自 `.changelogs/<version>.md`

## 当前阻塞与待办

- **Phase 5 旧栈退役**：依赖 Phase 4 完成后才能退役，当前不可启动
- **配置页核心层实现待启动**：架构模型已定，下一步落 Schema/Authority/DraftBuffer 等
- **可选收尾**：push 分支到远端 / 清理本地已合并分支 / 启动非布局债某项 / 用户自定新方向
- **待真机验收**：B6 FBO 批 3 纹理缓存性能取舍 + L1 修复可选真机验证

## 仍登记的技术债

**权威清单见 `docs/记忆/长期事实/scene技术债.md`**，本文件不复制清单内容。
其他文档提到 scene 技术债时统一指向该权威源。

## 当前约束

- 具体实现细节必须以当前源码、Gradle 配置和专题文档为准，不从历史对话推断
- 涉及架构性改动，开工前先对照 `NORTH_STAR.md`《核心信条》《关键不变量》《决策检查清单》，
  确认未踩《反模式》；偏离须按《修订纪律》显式登记
- 涉及对外接入能力变化时，同步更新 `docs/使用文档/`；
  可复现错误或审查结论写入 `docs/开发者文档/errors/` 或 `docs/开发者文档/reviews/`
- 不把临时调试记录、一次性试验结论、阶段流水账写入本文件或 `docs/AI记忆文档.md`
- **fixer 必须串行执行**（不得并行多个 fixer，读与写不得同批并行）：避免 Gradle 竞争 build/ 目录误报，fixer 自行串行编译/测试验证（见 ERROR-20260624 / 20260518）
- **Markdown 正文不写超长行**（≤200 字符），表格行结构例外但控制单格长度
