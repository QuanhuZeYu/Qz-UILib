# 当前上下文

本文件记录“当前真实状态”，服务下一位接手的 Agent 或未来会话恢复现场。

## 当前阶段

- `/qzuilib test` 页面系统性重构已启动：旧 test 子页和页面控制器已清空，命令入口已进入 P0 语义首页，详细矩阵见 `docs/开发者文档/specs/qzuilib-test-page-rebuild-plan.md`
- Phase 1 已收口合并回 `4.0`
- Phase 2 审查已完成：系统性检查全子系统，发现 28 处语义不一致（详见 `docs/开发者文档/reviews/REVIEW-20260601-browser-semantics-phase2-audit.md`）
- Phase 2 首批 P0/P1 浏览器语义修复已通过完整测试并合并回 `4.0`
- Phase 2 第二批浏览器语义修复已完成，合并回 `4.0`，并通过独立代码审查（详见 `docs/开发者文档/reviews/REVIEW-20260601-browser-semantics-phase2-followup.md`）
- Phase 2 第三批 P2 事件语义修复已完成并合并回 `4.0`，覆盖 `wheel` DOM-like 事件默认滚动前分发
- Phase 2 第四批 fixed clip chain 修复已完成，覆盖 transform 祖先作为 fixed containing block 时的布局、绘制/命中共享 clip 语义
- 已完成浏览器能力缺口复核（详见 `docs/开发者文档/reviews/REVIEW-20260601-capability-gap-recheck.md`）：确认 `REVIEW-20260518` 正文结论失效，当前真实剩余缺口约 23 主项，并按"有意边界 / 性价比待评估真缺口"分级
- B 类缺口填补线已启动：按"每项先核实复杂度、独立分支、定向测试、提交、`--no-ff` 合并"节奏逐项推进

## 当前目标

- 重建 `/qzuilib test` 为覆盖 DOM、CSS、布局、绘制、事件、控件、文本字体、动画、宿主运行时、远程与网络语义的测试系统
- 运行时测试内容已清空，下一轮需要重新规划卡片字段、状态、样例展示和自动断言后再接入
- 逐步修复 UI 框架中不符合浏览器语义的事件、命中测试、布局和绘制行为
- 每轮修复先补回归测试，再做最小实现修改，避免后续语义回退
- 对 B 类真实缺口做小步填补，优先选择可沿用现有模型、影响面可控的能力
- 继续沿用现有 `docs/记忆/`、`docs/使用文档/`、`docs/开发者文档/` 分层沉淀体系

## 当前状态

- 当前主分支：`4.0`
- 当前工作分支：`4.0`
- `/qzuilib test` 当前状态：旧菜单、布局诊断、字体基线、HTML-like Smoke、Glass、背包概览、列表拖拽、浏览器语义展示、动画展示、结构审查、运行时自检、网络自检等旧子页已删除；入口保留 DOM / CSS / Layout / Paint / Input / Controls / TextFont / Animation / RuntimeHost / RemoteNet 语义分组导航
- 运行时测试矩阵已清空：`createRuntimeTestCases()` 返回空列表，所有分组规格/接入计数归零，二级页只显示空态说明，不再展示旧用例卡片、`执行自动测试`、`人工通过` 或 `人工失败`；旧矩阵列表、旧 demo 样式表、旧用例样例分发和旧自动断言分发已删除/替换为空态 stub
- 下一轮 `/qzuilib test` 需要先重新规划运行时用例设计，再决定是否恢复统一字段、状态、自动断言和人工确认入口
- 表单控件可见性已增强：`DocumentLayoutEngine` 为原生 `input height:auto` 保留一行 line-height 内容高度，避免空值输入框高度塌缩；`DocumentTextAreaControl` 使用独立高对比 caret 颜色、2px 宽度和 `setCaretColor(...)`，并修正 selection/caret 自定义渲染的文档坐标到屏幕坐标转换，避免 widget 非零偏移时光标被画到错误位置
- 已修复游戏内运行日志暴露的 textarea 删除换行崩溃：删除换行后旧 `visualLineMetrics` 可能引用已不存在的逻辑行，`isSoftWrappedVisualLine` 现会校验 `logicalLineIndex` 边界；错误沉淀见 `docs/开发者文档/errors/ERROR-20260602-textarea-stale-visual-line-cache.md`
- 新 test 页面后续规划要求：先保持语义分组导航，不恢复旧页面名结构；运行时用例字段与状态不沿用当前已清空矩阵，需重新设计
- 首批 P0/P1 修复已落地：
  - `DocumentNode.insertBefore`/`replaceChild` 同父移动时先移除再重算参考索引，并补齐 `appendChild`/`insertBefore` 返回插入节点语义
  - `DocumentLayoutEngine` 按 max 再 min 应用尺寸约束，确保 `min-width/min-height > max-*` 时 min 胜出，并修正正负/全负相邻 margin collapse
  - `DocumentEffectChain` 将 `position:fixed` 视为 CSS-like stacking context，并让 overflow 子内容 clip 可达性按 padding box 圆角判断
  - `ElementSemantics` 将 `disabled` 作为 HTML 布尔属性处理，`disabled="false"` 仍禁用
  - `FlexLayoutHelper` 支持 row flex item 默认 `min-width:auto` 与 `flex-basis` 的 border-box 换算
- 第二批浏览器语义修复已在当前工作分支落地：
  - `DocumentNode.removeChild` 改为返回被移除节点，非直接子节点报错；`DocumentQuerySupport.querySelector*` 不再把内部根节点作为 document 查询结果
  - `DocumentFocusManager` 将 `focusin` 限定为获得焦点，并新增独立 `focusout` 冒泡事件；焦点切换按 `focusout` → `focusin` → `blur` → `focus` 分发
  - `DocumentMouseEventDispatcher` 将 hover/active 作为状态通知，不再被目标 handler 返回值截断祖先通知
  - `UiStyleProperty` 修正 `border-collapse` 继承与 `font-style` 布局失效；复核确认 `text-shadow` 按 CSS 标准应保持继承
  - `DocumentPaintEngine` 将 inset box-shadow 绘制在背景之上、边框之下
- 第三批浏览器语义修复已合并回 `4.0`：
  - 新增 `DocumentElementWheelEvent` / `DocumentElementWheelHandler` 与 `setWheelHandler` / `setCaptureWheelHandler`
  - `HtmlLikeDocumentWidget.onMouseScroll` 先分发 wheel DOM-like 事件，再执行默认滚动；`preventDefault()` 阻止默认滚动，handler 返回 `true` 仅停止传播
- 第四批浏览器语义修复已合并回 `4.0`：
  - `transform` 祖先会建立 fixed containing block，fixed 后代改为相对该祖先 padding box 定位
  - `DocumentVisualTraversal` 在 fixed 后代位于 transform containing block 内时保留祖先 scroll/clip 传播，不再无条件清空 clip chain
- Phase 2 剩余语义工程化修复已完成：已补空块/递归 margin collapse、row flex `align-content`、flex 交叉轴 auto margin、absolute auto margin 居中、table auto 内容列宽、textInput capture 阶段、transform fixed containing block 下滚动范围一致性；sticky stacking context 复核为审查误报；`inline-block baseline` 仍需后续单独改行内布局盒落位模型
- 已完成运行时页面浏览器语义回归复查与修复：row flex item `min-width:auto` 改用 CSS-like min-content 测量；诊断页、运行时自检、配置模板和字体排序中确需等分收缩的 row flex 子项显式声明 `min-width:0`（详见 `docs/开发者文档/reviews/REVIEW-20260602-flex-min-content-runtime-pages.md`）
- 已修复 `PAINT-005` 暴露的 transform 内文本绘制语义：`DocumentPaintRenderer` 在 transform 栈内禁用延迟文本批处理，避免字体 batch flush 使用屏幕坐标绕过父元素矩阵；错误沉淀见 `docs/开发者文档/errors/ERROR-20260602-transform-text-deferred-batch.md`
- 当前测试全部通过；已通过 `compileJava`、`git diff --check`、相关 DOM / layout / flex / effect / hit-test / paint / visual traversal 测试集；第四批已通过在线 `DocumentLayoutEngineTest` / `DocumentVisualTraversalTest` / `DocumentHitTestEngineTest` / `DocumentPaintEngineTest` 与在线 `compileJava`；flex min-content 修复已通过 `FlexLayoutHelperBoundaryTest`、`DocumentLayoutEngineTest` 与 `compileJava`；剩余语义工程化修复已通过离线 `FlexLayoutHelperBoundaryTest` / `PositionedLayoutHelperBoundaryTest` / `TableLayoutHelperBoundaryTest` / `DocumentLayoutEngineTest` / `DocumentScrollStateTest` / `HtmlLikeDocumentWidgetTest`、离线 `compileJava` 与 `git diff --check`；test 页面重构规划分支已通过目标页面测试、离线 `compileJava`、`git diff --check` 与游戏内人工打开验证；P0 首页分支已通过 `UiTestDocumentPageControllerTest` / `UiDocumentScreensTest`、离线 `compileJava` 与 `git diff --check`，最后复跑阶段遇到已知 `ERROR-20260601` GTNH GitHub manifest 外部波动；运行时卡片分支已通过离线 `UiTestDocumentPageControllerTest`；运行时二级页分支已通过离线 `compileJava` 与目标 `UiTestDocumentPageControllerTest`；截图复查修复已通过目标 `UiTestDocumentPageControllerTest`；Input 运行时卡片分支已通过离线目标 `UiTestDocumentPageControllerTest`、离线 `compileJava` 与 `git diff --check`；全分组运行时二级页补齐分支已通过离线目标 `UiTestDocumentPageControllerTest` 与 `git diff --check`；textarea 崩溃修复已通过离线目标 `DocumentTextAreaControlTest`；运行时自动化增强已通过离线 `compileJava --rerun-tasks`、离线目标 `DocumentTextAreaControlTest` / `UiTestDocumentPageControllerTest --rerun-tasks` 与 `git diff --check`；input 空值高度与 textarea 光标坐标修复已通过在线目标 `DocumentTextInputControlTest` / `DocumentTextAreaControlTest`、在线目标 `DocumentLayoutEngineTest`、在线 `compileJava` 与 `git diff --check`；运行时测试矩阵补全已通过 `git diff --check`、离线 `compileJava` 与离线目标 `UiTestDocumentPageControllerTest --rerun-tasks`；DOM 测试页可观察性优化已通过 `git diff --check` 与离线目标 `UiTestDocumentPageControllerTest`
- 运行时矩阵清空已通过离线目标 `UiTestDocumentPageControllerTest`（该命令已执行 `compileJava`）与最终 `git diff --check`
- 曾通过完整 `./gradlew.bat --no-configuration-cache test`；第三、四批尚未重跑完整 test
- 已知有意能力边界（非待修复项）：keyframe per-stop timing、完整 Web Animations API、CSS `var(...)` 声明级解析、`align-items:baseline` 等价 START、CSS Grid、transform 矩阵堆栈、gradient、float
- B 类缺口当前进度：input type（password/number）已完成；font-family 因底层无字体族维度跳过并记录决策；textContent 已完成；textarea 软换行已通过“逻辑行 + 视觉行”两级模型完成；background-image 的 CSS `url(...)` 解析已完成，远程 CSS 支持 `ResourceLocation` 单图 URL 与 `none`，仍不扩展 HTTP 背景图、渐变、多背景或 repeat/position/size

## 当前约束

- 具体实现细节必须以当前源码、Gradle 配置和专题文档为准，不从历史对话推断
- 不把临时调试记录、一次性试验结论、阶段流水账写入 `docs/AI记忆文档.md`
- 涉及对外接入能力时，仍必须同步更新 `docs/使用文档/`
- 涉及可复现错误或审查结论时，仍必须写入 `docs/开发者文档/errors/` 或 `docs/开发者文档/reviews/`

## 当前阻塞

- 无源码级硬阻塞；`inline-block baseline` 属剩余工程项，需单独重构行内布局盒落位模型

## 更新规则

- 只保留当前真相，不写阶段流水账
- 信息过时后直接覆盖，不做按日期追加
