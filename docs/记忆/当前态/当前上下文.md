# 当前上下文

本文件记录“当前真实状态”，服务下一位接手的 Agent 或未来会话恢复现场。

## 当前阶段

- 阶段名称：浏览器语义修复进行中，`fix/browser-semantics-phase1` 的 `top-layer` 共享语义收口已完成

## 当前目标

- 逐步修复 UI 框架中不符合浏览器语义的事件、命中测试、布局和绘制行为
- 每轮修复先补回归测试，再做最小实现修改，避免后续语义回退
- 继续沿用现有 `docs/记忆/`、`docs/使用文档/`、`docs/开发者文档/` 分层沉淀体系

## 当前状态

- 当前工作分支：`fix/browser-semantics-phase1`
- 已完成第一批浏览器语义修复：`pointer-events` 改为默认继承；`visibility:hidden` 改为“隐藏当前元素自身但允许 `visibility:visible` 后代恢复绘制与命中”
- 已补回归测试：`UiStyleResolverTest`、`DocumentHitTestEngineTest`、`DocumentPaintEngineTest`
- 已验证：`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.style.UiStyleResolverTest" --tests "club.heiqi.uilib.ui.layout.DocumentHitTestEngineTest" --tests "club.heiqi.uilib.ui.paint.DocumentPaintEngineTest"`
- 已验证：`./gradlew.bat --no-configuration-cache compileJava`
- 已完成第二批事件语义修复：鼠标 `click` 现在按 down/up 最近公共祖先解析 target，`mouseup` 改为按释放位置命中目标，raw button 键盘默认激活改为走完整 click 分发链
- 已完成第三批事件语义修复：`AT_TARGET` 阶段改为 target capture 后仍执行 target handler（除非 `stopImmediatePropagation()`）；事件返回值与默认行为解耦，`return true` 只停止传播，默认行为仅受 `preventDefault()` 控制
- 已完成第四批事件与布局基础修复：`dblclick` / `contextmenu` 补齐 capture -> target -> bubble 三阶段链路；block 布局补齐 BFC 对 margin collapse 的阻断、普通父块与首个子块顶部 margin collapse
- 已补事件链端到端回归测试：`HtmlLikeDocumentWidgetTest`
- 已补布局回归测试：`DocumentLayoutEngineTest` 新增 BFC sibling collapse 阻断、普通父子顶部 collapse、overflow 阻断父子 collapse 用例
- 已补运行时自检与日志：`UiRuntimeSelfTestDocumentPageController` 新增“浏览器事件语义”断言项，游戏内可直接触发 AT_TARGET 顺序、raw button 默认 click、`preventDefault()` 与控件去重验证，并同步写入页面日志与 Log4j
- 已验证：`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.document.HtmlLikeDocumentWidgetTest" --tests "club.heiqi.uilib.ui.control.DocumentButtonControlTest" --tests "club.heiqi.uilib.ui.control.DocumentSlotControlTest"`
- 已验证：`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.control.DocumentToggleSwitchControlTest" --tests "club.heiqi.uilib.ui.control.DocumentSegmentedSelectorControlTest"`
- 已验证：`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.document.HtmlLikeDocumentWidgetTest" --tests "club.heiqi.uilib.ui.control.DocumentButtonControlTest" --tests "club.heiqi.uilib.internal.devtools.pages.UiRuntimeSelfTestDocumentPageControllerTest"`
- 已验证：`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.document.HtmlLikeDocumentWidgetTest" --tests "club.heiqi.uilib.ui.dom.DocumentEventPropagationTest" --tests "club.heiqi.uilib.ui.layout.DocumentLayoutEngineTest"`
- 已验证：`./gradlew.bat --no-configuration-cache compileJava`
- 已完成第五批绘制/命中/滚动统一语义修复：新增共享视觉遍历层 `DocumentVisualTraversal`，统一 `fixed/sticky` 偏移、scroll offset、overflow clip 链与 stacking phase 收集；`DocumentHitTestEngine`、`DocumentScrollState`、`DocumentPaintEngine` 改为复用该层，不再各自维护独立遍历逻辑
- 已明确当前浏览器语义边界：`overflow:hidden/auto` 祖先提供裁剪链，但不等同于 CSS 规范意义上的 stacking context；`fixed` 后代脱离祖先 scroll/clip 影响，按视口定位；高 `z-index` positioned 后代仍可参与祖先 stacking phase 排序，但其像素输出与命中受祖先 clip 链裁剪
- 已补共享层与回归测试：新增 `DocumentVisualTraversalTest`，并更新 `DocumentPaintEngineTest` 的 `fixed` / `overflow clip` 浏览器契约预期
- 已验证：`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.layout.DocumentVisualTraversalTest" --tests "club.heiqi.uilib.ui.layout.DocumentHitTestEngineTest" --tests "club.heiqi.uilib.ui.layout.DocumentScrollStateTest" --tests "club.heiqi.uilib.ui.paint.DocumentPaintEngineTest"`
- 已验证：`./gradlew.bat --no-configuration-cache compileJava`
- 已完成第六批共享视觉场景修复：`DocumentVisualTraversal` 新增普通树 + `top-layer` 根盒统一场景 `VisualScene` 与共享定位查询；`DocumentPaintEngine`、`DocumentHitTestEngine`、`DocumentScrollState`、`HtmlLikeDocumentWidget` 改为复用同一套根级顺序与元素定位，不再由 widget 单独维护 `top-layer` 命中/绘制循环
- 已补共享场景回归测试：`DocumentVisualTraversalTest` 新增根级顺序/定位用例，`DocumentPaintEngineTest`、`DocumentHitTestEngineTest`、`DocumentScrollStateTest` 新增 `top-layer` 引擎级契约
- 已验证：`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.layout.DocumentVisualTraversalTest" --tests "club.heiqi.uilib.ui.layout.DocumentHitTestEngineTest" --tests "club.heiqi.uilib.ui.layout.DocumentScrollStateTest" --tests "club.heiqi.uilib.ui.paint.DocumentPaintEngineTest"`
- 说明：本轮收口时 `compileJava` 曾成功一次；末次重跑偶发卡在 `gtnhconvention` 加载 GitHub manifest，属外部构建脚本波动，非本轮源码编译错误
- 已完成第七批 `top-layer` 运行态补齐：修正 HUD 共享文档在 `pointer-events` 继承语义下的交互挂载根契约，避免交互 HUD 因 mount root 继承 `none` 而整体失去命中；同时保持 HUD 根空白默认不拦截
- 已完成：`HtmlLikeDocumentWidget` 的动画时间线更新与 `requestAnimation(...)` 现已覆盖普通树 + `top-layer` 根盒，顶层 popup 的 transition / keyframe / animate() 不再遗漏
- 已补集成回归：`UiHudDocumentHostTest` 新增交互 mount root 命中契约，`HtmlLikeDocumentWidgetTest` 新增 `top-layer popup animate()` 端到端用例
- 已验证：`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.layout.DocumentVisualTraversalTest" --tests "club.heiqi.uilib.ui.layout.DocumentHitTestEngineTest" --tests "club.heiqi.uilib.ui.layout.DocumentScrollStateTest" --tests "club.heiqi.uilib.ui.paint.DocumentPaintEngineTest" --tests "club.heiqi.uilib.ui.document.HtmlLikeDocumentWidgetTest" --tests "club.heiqi.uilib.ui.control.DocumentSelectControlTest" --tests "club.heiqi.uilib.ui.hud.UiHudDocumentHostTest"`
- 已验证：`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.remote.RemoteHudOverlayClientBridgeTest" --tests "club.heiqi.uilib.ui.control.DocumentTextAreaControlTest" --tests "club.heiqi.uilib.ui.control.DocumentSliderControlTest"`
- 已验证：`./gradlew.bat --no-configuration-cache compileJava`
- 说明：本轮中途曾偶发遇到 `gtnhconvention` 加载 GitHub manifest 失败；重试后测试与 `compileJava` 均已成功，已沉淀错误记录供后续排障

## 当前约束

- 具体实现细节必须以当前源码、Gradle 配置和专题文档为准，不从历史对话推断
- 不把临时调试记录、一次性试验结论、阶段流水账写入 `docs/AI记忆文档.md`
- 涉及对外接入能力时，仍必须同步更新 `docs/使用文档/`
- 涉及可复现错误或审查结论时，仍必须写入 `docs/开发者文档/errors/` 或 `docs/开发者文档/reviews/`

## 当前阻塞

- 无源码级硬阻塞
- 当前分支目标已收口；若继续推进，重点转向更细绘制顺序差异与高耦合运行时 smoke 迁移

## 更新规则

- 只保留当前真相，不写阶段流水账
- 信息过时后直接覆盖，不做按日期追加
