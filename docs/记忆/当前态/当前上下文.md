# 当前上下文

本文件记录“当前真实状态”，服务下一位接手的 Agent 或未来会话恢复现场。

## 当前阶段

- 阶段名称：已引入分层 AI 协作记忆框架，后续任务按分层入口读取和写回

## 当前目标

- 保持 `docs/AI记忆文档.md` 只承担总导航职责
- 将当前态、长期事实、关键决策分别写入 `docs/记忆/当前态/`、`docs/记忆/长期事实/`、`docs/记忆/决策/`
- 继续沿用现有 `docs/使用文档/`、`docs/开发者文档/errors/`、`docs/开发者文档/reviews/`、`docs/开发者文档/specs/` 专项沉淀体系
- 代码开发仍以本项目 Java / Gradle / Minecraft 1.7.10 约束为准，不迁移 Harmes-Rust 的 Rust / egui 产品架构

## 当前状态

- 根目录 `AGENTS.md` 已允许按 Harmes-Rust 的分层记忆框架改造
- `docs/记忆/` 已作为 AI 协作记忆层入口引入
- `docs/AI记忆文档.md` 已从“大而全记忆文件”调整为总导航角色
- 长期稳定的项目定位、入口边界、运行命令已迁移到 `docs/记忆/长期事实/`
- 关键协作框架选择已写入 `docs/记忆/决策/DECISION-20260531-记忆框架.md`

## 当前约束

- 具体实现细节必须以当前源码、Gradle 配置和专题文档为准，不从历史对话推断
- 不把临时调试记录、一次性试验结论、阶段流水账写入 `docs/AI记忆文档.md`
- 涉及对外接入能力时，仍必须同步更新 `docs/使用文档/`
- 涉及可复现错误或审查结论时，仍必须写入 `docs/开发者文档/errors/` 或 `docs/开发者文档/reviews/`

## 当前阻塞

- 无硬阻塞

## 更新规则

- 只保留当前真相，不写阶段流水账
- 信息过时后直接覆盖，不做按日期追加
