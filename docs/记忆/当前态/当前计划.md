# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. `4.2.0` 发布准备已完成，本地 `4.0` 已包含 GTNH `2.9.0-beta-1` 依赖基线、发布说明和 `/qzuilib test` `VIS-PAINT-005` 样例生命周期修复
2. 发布前最终验证已通过：`git diff --check`、`./gradlew.bat --no-configuration-cache test`、`./gradlew.bat --no-configuration-cache --no-daemon -x test publishToMavenLocal`
3. 下一步如继续发布维护，只需关注远端 `4.2.0` tag 触发的 GitHub release workflow 是否完成；若 workflow 失败，优先按 Actions 日志定位构建或凭据问题
4. `runClient21` 已解除 `BytePatternMatcher` 缺类，但本地 GTNH 2.9 beta smoke 仍受 `ServerUtilities 2.3.0` 与 `Et-Futurum-Requiem 2.6.40-GTNH` 第三方 mixin 冲突阻塞
5. 若继续追求本地游戏内 smoke，优先决定是否在忽略的 `run/client/config/etfuturum/mixins.cfg` 中禁用 `enablePlayersSleepingPecentageGamerule`，或等待/查找 GTNH 上游配套版本

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
