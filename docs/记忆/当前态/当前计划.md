# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 当前工作分支为 `4.0`，`fix/hud-menu-native-scan` 已通过 `git merge --no-ff` 合并回主分支
2. 已将 `UiHudDocumentHost.createInputContext(...)` 收窄为仅在 HUD 交互输入启用时探测原生文本框焦点，`MENU` / `INGAME` 不再触发 `UiNativeTextInputInspector`
3. 已补 `UiHudDocumentHostTest.shouldInspectNativeTextInputOnlyWhenHudInputIsInteractive`，覆盖菜单页不会触发原生输入探测
4. 已沉淀错误记录到 `docs/开发者文档/errors/ERROR-20260610-hud-menu-native-scan.md`，并新增 `.changelogs/4.2.1.md`
5. 已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.hud.UiHudDocumentHostTest"`、`./gradlew.bat --no-configuration-cache compileJava`、`./gradlew.bat --no-configuration-cache --no-daemon -x test publishToMavenLocal`；后续远端发布由推送 `4.0` 和 `4.2.1` tag 触发

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
