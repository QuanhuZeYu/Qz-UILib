# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。
计划变化时直接改写，不保留历史版本。

## 当前计划（控件审核 P0+P1 批次已合回 4.0，2026-06-25）

> I6 + B2 已合回 4.0。
> 控件审核批1-2 + P1-B 已合回 4.0：SceneTable 退役 + disabled 焦点兑 R9 + Tab 越界校验 + TextInput caret 兜底 + Breadcrumb/Select chrome 收口 + SimpleList 按钮 focusable 对齐 + TextArea caret/文本色范式对齐。

1. ~~**控件审核**：四组 explorer 并行审核 25 个控件~~ ✅（2026-06-25）
2. ~~**批1 P0+P1**：SceneTable 退役 + disabled 焦点 + Tab 校验 + caret 兜底~~ ✅（`488af5a4`）
3. ~~**批2 P1**：Breadcrumb/Select chrome 收口 + SimpleList 按钮 focusable 对齐~~ ✅（`65d07c09`）
4. ~~**P1-B**：TextArea caret 上色对齐 TextInput + 文本三态色补齐~~ ✅（`b52f77a3`）
5. **剩余 P1**：行内编辑器 enabled/readOnly 全栈写死 / "谁负责 signal.set"文档 / Owner 回归断言 / 3 primitive 独立测试
6. **P2 改进项**：Props Builder 一致性 / chrome 收口（复合控件）/ 行高常量收口 / DataTable O(n) 优化
7. **已知 bug**：forEach 与 show 共享容器 anchor 误删（TextArea placeholder 占位节点未插入树）
8. **嵌套分类树导航** / **字段引擎最小切片**（待用户拍板）
9. **真机验收**：B2 帧率 + disabled 焦点视觉 + Breadcrumb focus 态 + TextArea 文本三态色

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）
  并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I1/I4/I6/I7/I10/I11）自检，
  偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，
  Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，
  不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 控件完成后必须建 demo 页接入 hub，否则用户无法真机验证。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
