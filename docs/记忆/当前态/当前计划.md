# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。
计划变化时直接改写，不保留历史版本。

## 当前计划（统一 chrome 配色 + hover/focus 视觉态强化，2026-06-25）

> Tab 聚焦视觉强化（框架级方案）+ 统一 chrome 配色（合并一轮推进）已完成。
> SceneChromeTokens + SceneStateColors 框架基础设施已落地，
> 3 个 primitive Result 改签名 + 9 个 wrapper 适配已通过 reviewer 审核和全量编译。
> 待真机验收视觉无回归。

1. ~~**Oracle 评估**：框架级方案（不是小补丁）~~ ✅
2. ~~**用户拍板**：5 项架构决策 + 5 项配色决策~~ ✅
3. ~~**第 0 步**：SceneChromeTokens + SceneStateColors + 单测~~ ✅（`af89a10d`）
4. ~~**第 1+2 步**：3 primitive 改签名 + 9 wrapper 适配 + 8 测试更新~~ ✅（`61f0fead`）
5. ~~**reviewer 审核**：有条件通过，P1 已修复~~ ✅
6. **真机验收**：focus ring 视觉效果 + hover/focus 共存 + Tab/Segmented 新增 border 密度
7. **合回 4.0**：`refactor/scene-primitives-batch-2` 分支用 `git merge --no-ff` 合回
8. **后续恢复主线**：嵌套分类树导航 / 字段引擎最小切片；高优先级缺口 TextArea

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）
  并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I1/I4/I6/I7/I10/I11）自检，
  偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，
  Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，
  不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 控件完成后必须建 demo 页接入 hub，否则用户无法真机验证。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
