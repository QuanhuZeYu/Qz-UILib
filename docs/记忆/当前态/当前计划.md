# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。
计划变化时直接改写，不保留历史版本。

## 当前计划（技术债批3+4已合回 4.0，2026-06-26）

> 控件审核 P0+P1 批次 + 技术债批3+4 已合回 4.0。

1. ~~**批3**：行高收口 + primitive 独立测试 + 文档/Owner 断言 + anchor bug 修复~~ ✅（`36d8d1ef`）
2. ~~**批4**：DataTable O(n) + 行内编辑器 enabled + Props Builder P0 + KeyValueMap chrome 收口~~ ✅（`acfd8daa`）
3. **剩余 P1**：无（全部已清）
4. **剩余 P2**：chrome 收口第2-4批（ObjectField/SimpleList/DataTable，需视觉决策）/ Props Builder P1（SimpleList/ObjectField/KeyValueMap class->record 回填）
5. **真机验收**：B2 帧率 + disabled 焦点视觉 + Breadcrumb focus 态 + TextArea 文本三态色 + 行内编辑器 enabled + placeholder 修复
6. **嵌套分类树导航** / **字段引擎最小切片**（待用户拍板）

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）
  并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I1/I4/I6/I7/I10/I11）自检，
  偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，
  Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，
  不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 控件完成后必须建 demo 页接入 hub，否则用户无法真机验证。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
