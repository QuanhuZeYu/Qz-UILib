# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。计划变化时直接改写，不保留历史版本。

## 当前计划（批 4 Tab/Table + 滚动裁剪，oracle 已重评，2026-06-22）

> 批 1/2/3 全部完成提交（Checkbox/Toggle、Radio/Segmented/Breadcrumb、Slider/TextInput）。批 4 步骤 A（Tab `5f649640`）、步骤 B（滚动地基 `97d214fb` + demo `f67e110d`）完成。P0 解耦层收尾 `d2bccac8`、McScreenBridge 真机诊断插桩 `1924e889`、Slider/TextInput 真机前缺陷修复 `905461d0` 已完成；Demo2 文本桥接与 `SceneTextInput` B1 字符级重设计已完成并通过定向/组合回归测试，待真机复验。**下一焦点：真机闸门验收（oracle 裁决从可选 P2 升级为硬前置，三 demo 全在 McScreenBridge 新壳下无降级路径）→ 闸门全绿后步骤 C Table 迁移。**

1. **✅ P0 解耦层收尾（已完成提交 `d2bccac8`）**：oracle 重评发现层2 UiSurface 渲染面虽脱 MC，但 render ctx 参数仍持具体 `UiRenderContext`（焊 GL），UiSurface 位于 scene 核心顶层包，import 即违 I6。已改 ctx 参数为抽象 `UiRenderBackend` 接口（replayer 早已只认此接口）；三 host render 签名同步改（drawSelf 传 UiRenderContext 自动向上转型）；`ScenePackageIsolationTest` 新增 `sceneCoreTopLevelShouldNotReferenceConcreteRenderBackend()` 扫顶层 `ui.scene` 禁 UiRenderContext。验证：五文件零 errors、ScenePackageIsolationTest 3/3、scene 全包 `--rerun-tasks` 32 类 379 用例零失败。坐实「层1 UiRenderBackend 契约 + 层2 UiSurface 渲染面 + replayer」全链守信条六（换渲染后端 scene 核心零改动）。
2. **真机闸门（硬前置 P0，动 Table 本体前必过，沙箱无 GUI 用户跑）**：oracle 裁决从可选升级为硬前置——三 demo 全 `extends McScreenBridge` 无旧壳降级路径，新壳引入跨帧复用 compositor/snapshotService + onGuiClosed 双 close（修 FBO 泄漏）。入口 `/qzuilib test` → SCENE_DEMO 组三 demo。重点：①三 demo 开关/ESC 返回 ②opacity/transform 上屏 ③FBO/快照池不持续增长 ④TextInput 主键盘/小键盘输入、点击定位、方向键/Home/End、Backspace/Delete、PASSWORD 掩码编辑正确 ⑤Slider fill 与 thumb 同步 ⑥滚轮裁剪跟手 ⑦GUI Scale 命中 ⑧日志零异常（搜 `QzUiLib/McScreenBridge` 与 `club.heiqi.uilib`）。任一失败阻断 Table 优先修新壳或控件缺陷。
3. **步骤 C：Table 迁移（真机闸门全绿后启动，在滚动地基上，本期最小范围）**：scrollable viewport → content(COLUMN) → 表头行 + 数据行(ROW，单元格 preferredWidth 表达固定列宽)。本期**静态行数据全量建树**（旧栈 refreshTable 也全量重建，不做 keyed diff）、固定列宽、表头+数据行、纵向滚动复用步骤 B 地基、单元格静态样式。Table 复用 UiSurface/UiRenderBackend 抽象零改动。**排后续批**：keyed 动态行 forEach、排序、行内编辑、横向滚动、滚动条、DataTable 工具栏。开工前 oracle **新开 session** 产 Table 迁移契约 R11 + explorer 复用 exp-1（已侦察旧栈 DocumentTable/DataTable）。
4. **维持总策略**：暂停旧栈新功能/新样例，重点 scene 新栈迁移；发现旧栈问题先记录复现条件/影响面/验证方式/优先级，用户明确要求时再修。

> 已完成的批 4 步骤 A（Tab `5f649640`）、步骤 B（滚动地基 `97d214fb` + demo `f67e110d`）、新壳与解耦层、真机诊断插桩、Slider/TextInput 修复落地细节见 `docs/记忆/当前态/当前上下文.md` 与 `docs/记忆/当前态/交接记录.md`，不在本计划堆叠。

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，偏离按《修订纪律》登记。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
