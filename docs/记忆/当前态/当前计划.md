# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. `/qzuilib test` RuntimeHost 首轮 5 张视觉样例已恢复：open timing、resize、runtime stats、HUD/container input、exception panel 分别接入 `VIS-HOST-001` 到 `VIS-HOST-005`
2. RuntimeHost 视觉舞台与断言已拆为 `UiTestRuntimeHostVisualFactory` / `UiTestRuntimeHostAssertionRunner`；其中 `VIS-HOST-003` 为自动断言，其他 4 张保留游戏内人工确认诊断
3. 当前矩阵统计已更新为首轮计划 59 张、已接入 53 张、缺口 6 张；自动样例 39 张、人工确认样例 14 张
4. 下一步优先恢复 `/qzuilib test` 的 RemoteNet 首轮视觉矩阵，建议按 channel、fetch、stream、store、remote page/HUD、config sync/transport mode 推进 6 张样例
5. 如继续做测试结构整理，先评估其他接近千行的测试类职责边界，不再把新主题塞回 `HtmlLikeDocumentWidgetTest` 或继续膨胀 `UiTestDocumentPageControllerTest`
6. 后续涉及具体类、调用链、依赖图或影响面时，按 `docs/记忆/长期事实/CodeGraph协作.md` 先用 `codegraph` MCP 缩小范围，再读源码和跑定向验证

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
