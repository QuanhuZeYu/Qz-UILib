# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. `HtmlLikeDocumentWidgetTest` 已完成拖拽、滚动、Focus/Keyboard、LayoutCache、AnimationRuntime 五批拆分：共享 helper 位于 `HtmlLikeDocumentWidgetTestSupport`，主题测试位于 `HtmlLikeDocumentWidgetDragTest`、`HtmlLikeDocumentWidgetScrollTest`、`HtmlLikeDocumentWidgetFocusKeyboardTest`、`HtmlLikeDocumentWidgetLayoutCacheTest` 与 `HtmlLikeDocumentWidgetAnimationRuntimeTest`
2. AnimationRuntime 拆分已通过 `git diff --check`、原测试类、新测试类和 `compileJava` 并合回 `4.0`
3. 下一批如继续拆测试，优先选择 `EventDispatch` 主题；click/double-click/context menu/active/hover/mouseup 等纯事件分发测试不要混入其他主题
4. 后续测试拆分仍只做组织搬家，不改生产代码语义，不引入新断言目标；以“搬家后行为完全一致”为完成标准
5. 每批拆分后串行运行 `git diff --check`、原测试类、新拆出的测试类和 `compileJava`；不要并行执行多个 Gradle 命令
6. `/qzuilib test` 下一批 TextFont 视觉矩阵可在测试拆分阶段性收口后恢复

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
