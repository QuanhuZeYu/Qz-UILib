# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. `fix/lwjgl2-text-input` 已完成 Issue #62 审查优化：initialized guard、`keyCode` 注释、HUD 边界文档和核心单测。
2. 本轮不移除 `handleHostTypedCharacter(..., keyCode)` 参数，不改现有控制字符过滤逻辑，不展开 HUD 文本输入桥接实现。
3. 保持 `RemoteSelfCheckPages.java` 非本任务变更不动，不纳入输入修复一致性提交。
4. 将 `fix/lwjgl2-text-input` 合回 `release/4.2.4`，合并时使用 `git merge --no-ff`。
5. 回到 `release/4.2.4` 后继续推送分支与 `4.2.4` tag，触发 Release workflow。

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准
- 发布说明必须在 tag 指向提交中存在，且文件名与 tag 完全一致；Issue #62 修复说明已写入 `.changelogs/4.2.4.md`
- 本地验证命令和结果只记录在协作交接、提交说明或开发者文档中，不写入远端 GitHub Release 正文

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
