# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 暂停推进新功能、新样例和 B 类缺口填补，当前以查找 bug、代码审查和优化建议复核为主
2. 生产代码第三轮审查 4 个 findings 已在 `fix/production-code-round3-findings` 完成最小修复并通过定向验证
3. 后续如继续生产代码修复，优先基于新的审查报告或用户明确问题重新建分支，不复用本轮修复分支
4. 若继续审查，重点关注事件默认行为、top-layer 生命周期、动态样式表缓存失效和 HUD 输入预过滤的新增回归面

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
