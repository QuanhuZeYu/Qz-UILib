# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 继续按 B 类缺口清单逐项推进，每项动手前先核实隐藏工程量，避免把重构项当轻量项处理
2. textarea 软换行完成后，下一项优先评估属性选择器 `[attr]` / `[attr=value]`，确认 `UiSelector` 解析、匹配与远程样式表回归范围
3. 后续按顺序评估兄弟组合器、结构伪类细分、表单校验扩展、拖拽 `dragenter/dragleave/drop`
4. 每轮继续执行 `git diff --check`、相关测试和 `compileJava`，必要时再做完整 `test`

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
