# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 当前分支 `chore/local-release-flow` 正在迁移 Qz-Miner 的本地发布说明覆盖流程：`.changelogs/<tag>.md` 作为 GitHub Release 正文源，workflow 后置步骤统一改写 Release 标题和正文
2. `4.2.0` 首次远端发布已成功生成资产，但自动 notes 只列 PR `#60` / `#61` 且 compare 基线为 `4.0.4...4.2.0`，不符合本项目完整发布说明预期
3. 本轮需提交 `.changelogs/4.2.0.md`、`docs/开发者文档/发布流程.md`、release workflow 后置覆盖步骤和相关长期约定，再移动 `4.2.0` tag 到新提交重新触发发布
4. 发布前最终验证已通过：`git diff --check`、`./gradlew.bat --no-configuration-cache test`、`./gradlew.bat --no-configuration-cache --no-daemon -x test publishToMavenLocal`
5. `runClient21` 已解除 `BytePatternMatcher` 缺类，但本地 GTNH 2.9 beta smoke 仍受 `ServerUtilities 2.3.0` 与 `Et-Futurum-Requiem 2.6.40-GTNH` 第三方 mixin 冲突阻塞

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
