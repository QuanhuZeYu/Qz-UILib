# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 当前工作分支为 `4.0`，`add/input-dropdown-autocomplete` 已通过 `git merge --no-ff` 合并回主分支
2. `DocumentAutocompleteInputControl` 已补候选选择后的状态刷新、候选数量上限和带内部标记的 top-layer listbox 锚定，避免长列表同步创建过多节点和误锚定普通 listbox
3. 已补 autocomplete 纯 JVM 回归，覆盖外部失焦关闭、top-layer detach、transform 锚定、长列表键盘滚动、选择后状态一致性和候选数量上限
4. 已通过 `git diff --check`、autocomplete/select 定向测试和 `compileJava`
5. 后续若接入字体名补全或其他配置输入，可直接复用该通用控件，不需要再扩展字体专用 API

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
