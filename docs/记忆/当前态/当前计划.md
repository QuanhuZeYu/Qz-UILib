# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。
计划变化时直接改写，不保留历史版本。

## 当前计划（基础控件 primitive 化 + 统一 primitive 样式，2026-06-25）

> 无样式 primitive 基础层重构（TextInput/Select/DataTable 迁移/flat 回收/overlay 保护节点治本）
> 已全部完成并真机验收，已合回 4.0。
> 基础控件 primitive 化 5 批已全部完成，全量 scene 测试通过。

1. ~~**评估方案**：oracle 评估"最基础控件 primitive 化 + 统一 primitive 样式"~~ ✅
2. ~~**批 A 双向布尔**：Checkbox/Toggle → SceneToggleablePrimitive~~ ✅（`26d221d`）
3. ~~**批 B 按钮**：Button → SceneButtonPrimitive~~ ✅（`4c7bac8`）
4. ~~**批 C N选1**：RadioGroup/Segmented → SceneSingleSelectPrimitive~~ ✅（`82f278f`）
5. ~~**批 D 连续值**：Slider → SceneSliderPrimitive~~ ✅（`edd286e`）
6. ~~**批 E Tab**：复用 SceneSingleSelectPrimitive~~ ✅（`e9c83db`）
7. **真机验收**：5 批 primitive 化需真机验收视觉无回归
8. **统一 chrome（独立轨）**：designer 配色方案已存档（`scene-chrome-color-spec.md`），
   等用户拍板 6 项待定后执行。扩展 SceneChromeTokens，全部 wrapper 改引用 token。
9. **后续恢复主线**：嵌套分类树导航 / 字段引擎最小切片；高优先级缺口 TextArea。

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）
  并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I1/I4/I6/I7/I10/I11）自检，
  偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，
  Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，
  不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 控件完成后必须建 demo 页接入 hub，否则用户无法真机验证。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
