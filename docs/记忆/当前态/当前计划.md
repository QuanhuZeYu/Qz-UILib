# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. UI 背景模糊开发者快捷配置方案 A+C 已完成实现、使用文档、决策记录和定向验证，下一步进入提交或合并前复核。
2. 提交前只 stage 背景模糊实现、测试、使用文档和记忆文档，避开用户已有 specs 未提交改动。
3. 如需更高信心，使用 `runClient21` 在游戏内 smoke `BackdropBlurPreset.DISABLED` / `QUALITY` 页面效果。
4. 若用户要求提交，先复查 `git status`、`git diff`、`git log --oneline -10`，再按仓库提交规范提交。

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准
- 背景模糊策略改动至少通过 `git diff --check`、相关定向测试和 `compileJava`
- 纯 JVM 测试不要直接调用会触发 Minecraft 默认运行时静态初始化的快捷工厂

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
