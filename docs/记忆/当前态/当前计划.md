# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 暂停推进新功能、新样例和 B 类缺口填补，当前以查找 bug、代码审查和优化建议复核为主
2. 生产代码第四轮审查已完成并留档，发现 5 个待修 findings：select detach open 状态残留、`UiStyleDeclaration.copyFrom(...)` 失效缺口、transform select popup 锚点、HUD hidden entry 绘制泄漏、popup close hover/cursor 残留
3. 后续如进入修复，基于 `docs/开发者文档/reviews/REVIEW-20260608-production-code-round4.md` 新建修复分支，不复用第三轮修复分支
4. 若继续审查，优先避开已记录 findings，转向网络/远程 HUD 生命周期、animation runtime 与 layout/paint cache 的更深层交互

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
