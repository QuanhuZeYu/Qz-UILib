# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 暂停推进新功能、新样例和 B 类缺口填补，当前以查找 bug、代码审查和优化建议复核为主
2. 第二轮 `/qzuilib test` 修复分支已通过 `--no-ff` 合并回 `4.0`；当前审查分支为 `review/production-code-round3`
3. 本轮全项目生产代码审查已完成并留档，后续如用户要求修复，优先按严重度处理 `REVIEW-20260608-production-code-round3.md` 的 4 个 findings
4. 修复时建议顺序：先收口 `textInput.preventDefault()` 默认取消语义与 HUD top-layer 后代命中预过滤，再处理 mounted stylesheet 缓存失效与 top-layer detach 生命周期泄漏
5. 只有用户明确要求修复时，再按独立修复分支、最小回归测试、最小实现修改和定向验证推进

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
