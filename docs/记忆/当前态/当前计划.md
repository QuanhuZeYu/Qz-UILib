# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 当前工作分支为 `4.0`；`4.2.2` 已发布并推送 tag，tag 目标提交为 `6419aa60`
2. `4.2.2` 范围已收口：包含字符字体规则、字体名自动完成输入、远程配置字段级校验和相关文档
3. 发布前已通过 `git diff --check`、字符规则/FontMatcher/配置页绑定/远程同步/autocomplete 定向测试、`compileJava` 与 `publishToMavenLocal`
4. GitHub Release 页面正文来自 `.changelogs/4.2.2.md`，页面显示 5 个资产；Release workflow #67 最终 `Status Success`
5. 如需进一步确认配置页真实交互，可运行 `runClient21` 从模组配置入口手动检查；本次未处理历史 `stash@{0}`

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
