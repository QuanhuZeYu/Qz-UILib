# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 继续修复事件分发剩余核心语义：AT_TARGET 顺序、返回值传播语义、dblclick/contextmenu 捕获阶段
2. 开始布局基础语义修复：BFC 创建条件、margin collapse、overflow 对布局与滚动容器的影响
3. 评估 `visibility` / `pointer-events` 修复是否需要补充到更多控件与 smoke 页场景
4. 每轮改动补对应 JVM 回归测试，并至少执行相关 `test --tests ...` 与 `compileJava`
5. 每轮收口后更新 `当前态/` 与 `交接记录.md`

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
