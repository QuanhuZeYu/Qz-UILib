# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。
计划变化时直接改写，不保留历史版本。

## 当前计划（scene chrome 收口第2-4批已合回 4.0，2026-06-26 第 111 次交接）

> P2 chrome 第2-4批已合入 4.0。三复合控件 25 处硬编码 token 化完成。

1. **真机验收**：SimpleList 暖灰→冷灰 3 个色变化
   （BUTTON_BG / BUTTON_BG_DISABLED / TEXT_DISABLED，用户跑）
2. **Props Builder P1 待裁决**：是只给 SimpleList 加 `final` 修饰后宣告 P1 完成
   还是接受 explorer 建议（class→record 价值低，加 final 是最低成本收尾）
   或全部三控件保留现状不再追进度
3. **跨控件 chrome 兜底**：grep 检查 scene 包内是否还有其他控件硬编码
   与本批 token 同值但未收口的常量（reviewer 在审核末尾提议）
4. **嵌套分类树导航** / **字段引擎最小切片**（待用户拍板主线方向）

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）
  并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I1/I4/I6/I7/I10/I11）自检，
  偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，
  Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，
  不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 控件完成后必须建 demo 页接入 hub，否则用户无法真机验证。
- **并行 fixer 只写代码不跑 build**，编译/测试统一由主 Agent 串行单次跑，
  避免并行 Gradle 竞争 build/ 目录误报（ERROR-20260624 已立项）。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。