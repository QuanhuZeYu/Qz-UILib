# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. CSS / Layout / Paint / Input 首轮 24 张视觉样例已补齐，页面已改为精简首页、短日志和一键全量断言；下一批优先 Controls 视觉样例，每批 7-12 张
2. 扩展剩余样例的自动断言或机器诊断覆盖，完整诊断保留在 state/log 中，页面只显示短状态与短差异；不可机器验证的视觉/交互项保持人工待确认
3. 如果后续仍需要更细排障，再增加“展开详情”或页内视图切换，不要默认回到长文本铺满页面
4. 每批保持 `git diff --check`、目标 `UiTestDocumentPageControllerTest` 和 `compileJava` 通过；涉及游戏内 UI、HUD、输入、命令入口、远程页面或网络 smoke 时再用 `runClient21` 验证

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
