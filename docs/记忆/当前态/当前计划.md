# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。计划变化时直接改写，不保留历史版本。

## 当前计划

1. **scene 新栈 Phase 1-3 真机验收**（待用户跑）：入口 `/qzuilib test` → SCENE_DEMO 组 → demo 页，确认 opacity/transform 上屏、纯 composite 帧零重排零重建、空格切背景（PAINT）、T 切文本（LAYOUT）。真机后派子代理查 `run/client/logs/fml-client-latest.log` 确认 `club.heiqi.uilib` 零异常。
2. **Phase 4 批 1 Checkbox/Toggle 迁移（契约已定，待执行）**：前置全部就绪——前置②真实字体度量已提交 `5cb593fd`（偏离 1 解除）、固定尺寸地基已提交 `237e8a33`（preferredWidth + STRETCH 豁免，scene 全套 301 测试绿）。oracle 已产出批 1 迁移契约：checked 双向走「受控组件」`ReadableSignal<Boolean> checked` 入 + `Consumer<Boolean> onChange`（传期望新值）出，控件零内部状态守 R1/R2/R4/I11；批 1 只做二态 Checkbox + 二态 Toggle（indeterminate/滑块平滑动画排后续批）；demo 用独立屏幕托管（新建 `SceneControlsDemoScreen`，不在旧 Controls 组原地混用，旧控件留回退基线）。落地时顺手补前置① R6「交互态须绑 hitTestable=true 节点」边界一句。需 oracle 时新开 session（旧 session 不复用）。完成后真机验收交用户跑。
3. **维持当前总策略**：暂停旧栈新功能/新样例，重点查 bug、代码审查、可控优化建议；发现问题先记录复现条件、影响面、验证方式、修复优先级，用户明确要求时再进入修复实现。

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，偏离按《修订纪律》登记。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
