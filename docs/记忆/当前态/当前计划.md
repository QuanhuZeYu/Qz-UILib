# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 当前文档分支 `docs/remote-ui-runtime-lease-decision` 只记录远程 UI 网络/会话层生产级重构研究，不改生产代码
2. 已新增决策 `docs/记忆/决策/DECISION-20260609-remote-ui-runtime-lease-protocol.md`，并更新决策索引与交接记录
3. 后续如进入生产代码重构，应按 Remote UI Runtime + 显式 Lease 协议先搭建内部等价骨架和测试，再收敛 `RemoteDocumentPages` / `RemoteHudOverlays` 为薄 facade
4. 文档分支完成后按仓库规范提交；是否合回 `4.0` 由用户后续确认

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
