# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 当前分支 `chore/gtnh-290-beta` 已完成 GTNH `2.9.0-beta-1` 依赖基线与 GTNH Gradle `2.0.25` modern Java run 链路同步
2. 收口前必须保持 `git diff --check`、`compileJava` 和输入、字体、vanilla/Forge 网络相关定向验证通过或明确记录外部依赖波动
3. `runClient21` 已解除 `BytePatternMatcher` 缺类，但仍受 `ServerUtilities 2.3.0` 与 `Et-Futurum-Requiem 2.6.40-GTNH` 第三方 mixin 冲突阻塞
4. 若继续追求本地游戏内 smoke，优先决定是否在忽略的 `run/client/config/etfuturum/mixins.cfg` 中禁用 `enablePlayersSleepingPecentageGamerule`，或等待/查找 GTNH 上游配套版本
5. 本次提交只收口依赖与运行链路基线，不扩大公开 API、不顺带修复非升级阻塞的生产代码问题

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
