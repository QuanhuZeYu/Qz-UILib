# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. `/qzuilib test` Controls 首轮 7 张视觉样例已完成；下一批继续视觉矩阵，优先推进 TextFont 样例，若需保持 7-12 张批量可合并少量 DOM 样例
2. 每批继续沿用 registry / visual builder / assertion runner / matrix state 模型，不恢复旧测试页结构
3. 每批保持 `git diff --check`、`UiTestDocumentPageControllerTest`、相关测试和 `compileJava` 通过；不要并行执行多个 Gradle 命令
4. 涉及游戏内 UI、HUD、输入、命令入口、远程页面或网络 smoke 的能力，再使用 `runClient21` 验证
5. HTML-like 脏子树布局缓存阶段已收口；未来若重新扩展纯 inline formatting，必须先设计并记录独立 fragment/run 缓存协议；layout 动画运行态仍不复用，除非补齐完整时间相关布局值测试

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
