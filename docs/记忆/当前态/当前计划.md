# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 完成本轮 `fix/browser-semantics-phase2-followup` 分支验证与提交
2. 将本轮 DOM / 事件 / 样式 / 绘制语义修复按 `--no-ff` 合并回 `4.0`
3. 继续从 Phase 2 审查报告中挑选有明确浏览器对标依据、影响面可控的剩余 P2/P3 项
4. 下一批优先评估 wheel DOM 事件、fixed clip chain、父子 margin collapse 递归等仍未收口项
5. 每轮继续执行 `git diff --check`、相关测试和 `compileJava`，必要时再做完整 `test`

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
