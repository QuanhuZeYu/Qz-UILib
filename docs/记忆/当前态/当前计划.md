# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。
计划变化时直接改写，不保留历史版本。

## 当前计划（SceneDataTable 行内编辑完成，2026-06-24）

> 滚动 API 封装 SceneScrolls.attach 已真机通过。
> SceneDataTable 行内编辑三 commit 渐进落地完成：
> text（只读）/ textInput（文本编辑）/ select（下拉编辑）三种列就绪，
> 14 测试全绿，reviewer 3 轮通过。

1. **真机验证 SceneDataTable**（用户跑）：
   Select 展开后滚动表格 anchor 跟随、cell 值不在 options 边界、
   多 Select 同列同时展开、大表格性能（200+ 行）。
2. **嵌套分类树导航**：SceneTreeView 从零建（>1000 行，一期暂缓项，
   需评估是否启动）。
3. **字段引擎最小切片**：三个嵌套控件 + SceneDataTable 已就绪，
   可接入字段引擎。
4. **继续暂缓深水区**：搜索、多行文本、高级 picker、raw editor、
   远程同步、完整 CSS/token/theme 和 Phase 5 均暂缓。

> 完成细节见 `docs/记忆/当前态/当前上下文.md` 与 `docs/记忆/当前态/交接记录.md`，
> 不在本计划堆叠。

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）
  并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，
  偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，
  Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，
  不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
