# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 先降低当前 UILib 字体引擎 debug 日志噪声，优先收敛 `FontService`、字体批次与字形生成链路的高频 debug 输出，避免后续 runtime 断言日志被淹没
2. 为已接入的 9 张 CSS / Layout / Paint 核心视觉样例补运行时断言日志与结果 state 回写，日志至少包含 caseId、阶段、expected、actual、difference 与关键诊断摘要
3. 分组页尝试改为按样例翻页的低密度展示，优先采用“每页 1 张样例 + 上一张/下一张 + 当前页状态摘要”模式
4. 继续补齐 CSS / Layout / Paint 剩余视觉样例，每批 8 到 12 张；具体舞台继续放在 `UiTestSampleVisualFactory`，`UiTestGroupVisualBuilder` 只负责统一装配
5. 每批保持 `git diff --check`、目标 `UiTestDocumentPageControllerTest` 和 `compileJava` 通过；涉及游戏内 UI、HUD、输入、命令入口、远程页面或网络 smoke 时再用 `runClient21` 验证

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
