# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 提交当前 `test/runtime-tests` 分支的运行时矩阵补全、目标测试和规格/记忆文档更新
2. 用 `runClient21` 做一次游戏内关闭复测，确认关闭阶段不再新增 `run\client\hs_err_pid*.log`
3. 对 `/qzuilib test` 全 10 个运行时二级页完整矩阵做游戏内人工复查，重点确认 Paint/Controls/Animation 视觉项、TextFont fallback/reload、RuntimeHost 行为、RemoteNet 服务端链路，以及待实现缺口标记是否清晰
4. 若人工复查发现页面文字、状态或视觉输出不清晰，先更新规格文本，再做最小页面调整
5. 单独评估 `inline-block baseline`：需要行内布局盒延迟落位或可重定位盒模型，不与属性选择器等 B 类缺口混做
6. 继续 B 类缺口清单，下一项优先评估属性选择器 `[attr]` / `[attr=value]`，当前运行时矩阵已将该项明确标记为待实现缺口
7. 每轮继续执行 `git diff --check`、相关测试和 `compileJava`，必要时再做完整 `test`

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
