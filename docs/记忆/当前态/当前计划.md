# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 复查 `fix/autocomplete-virtualized-options` 分支 diff，确认只包含本轮虚拟化修复、测试、使用文档和必要当前态记录。
2. 如需提交，提交标题按 `[Fix]: 修复自动完成大量候选卡顿`，正文用中文说明 helper 抽取、autocomplete 虚拟化、测试与文档影响。
3. 如需合回 `4.0`，先确认工作区干净，再按协作规范使用 `git merge --no-ff` 保留显式合并提交。

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
