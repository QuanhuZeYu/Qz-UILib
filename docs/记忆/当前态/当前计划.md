# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 当前工作分支为 `add/config-template-qz-sync`，目标是为 Forge 配置模板补齐 Qz 网络同步便捷接入
2. 已新增 `Spec.enableQzNetworkSync(...)`、`Spec.createQzNetworkSyncTarget(...)`，并将 Qz-UILib 自身 `ModConfigGui` 切换到新入口
3. 已新增配置同步 close fetch 端点，模板页关闭或异步打开后立即关闭时会通知服务端释放配置会话
4. 已同步更新配置模板使用文档、稳定 API 清单与长期架构边界
5. 已通过 `git diff --check` 和配置同步相关定向测试；本轮尚未提交，若需要提交需用户明确要求

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
