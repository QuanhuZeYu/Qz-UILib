# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。计划变化时直接改写，不保留历史版本。

## 当前计划（旧栈拔线与 qzui test 新栈承载，2026-06-22）

> 用户已下达最高指示：旧 HTML-like / `ui.dom` 栈彻底退出实际业务接入，暂不删除，仅作为废弃参考代码；后续真实入口、新功能和业务接入默认走 scene 新栈。第一批新栈 qzui test 首页/导航容器已通过真机验收，`/qzuilib test` 默认入口已切到 `SceneTestHubScreen`，`/qzuilib scene_test` 作为过渡别名继续打开同一新栈 hub，`/qzuilib legacy_test` 保留旧 HTML-like 视觉矩阵，仅供 legacy 参考回归。

1. **入口切换提交**：由 git agent 提交本轮 `/qzuilib test` 默认入口切换与文档同步改动。
2. **旧栈冻结执行**：旧 `UiTestDocumentPageController`、旧视觉矩阵和旧断言 runner 只保留为 legacy 参考/回归资料，不再作为实际业务接入路径或扩展目标。
3. **后续 Table 能力预研**：横向滚动、自适应列宽、动态 keyed 行、排序、行内编辑、滚动条、DataTable 工具栏均未做；启动前新开 oracle 评估，不复用旧 session。
4. **维持总策略**：暂停旧栈新功能/新样例，重点 scene 新栈迁移；发现旧栈问题先记录复现条件/影响面/验证方式/优先级，用户明确要求时再修。

> 已完成的批 4 步骤 A（Tab `5f649640`）、步骤 B（滚动地基 `97d214fb` + demo `f67e110d`）、步骤 C（SceneTable 最小版）、新壳与解耦层、真机诊断插桩、Slider/TextInput 修复落地细节见 `docs/记忆/当前态/当前上下文.md` 与 `docs/记忆/当前态/交接记录.md`，不在本计划堆叠。

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，偏离按《修订纪律》登记。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
