# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. `HtmlLikeDocumentWidgetTest` 剩余主题拆分已完成：主类只保留基础后端契约，渲染、命中、HUD 运行态和事件分发分别落到 `HtmlLikeDocumentWidgetRenderingTest`、`HtmlLikeDocumentWidgetHitTest`、`HtmlLikeDocumentWidgetHudRuntimeTest`、`HtmlLikeDocumentWidgetEventDispatchTest`
2. 本轮拆分只做测试组织搬家，未改生产代码语义，已通过 `git diff --check`、`HtmlLikeDocumentWidget*Test` 和 `compileJava`
3. `/qzuilib test` 下一步优先恢复 TextFont 视觉矩阵，继续按视觉优先矩阵规格补样例与断言边界
4. 如继续做测试结构整理，先评估其他接近千行的测试类职责边界，不再把新主题塞回 `HtmlLikeDocumentWidgetTest`
5. 后续涉及具体类、调用链、依赖图或影响面时，按 `docs/记忆/长期事实/CodeGraph协作.md` 先用 `codegraph` MCP 缩小范围，再读源码和跑定向验证

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
