# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。计划变化时直接改写，不保留历史版本。

## 当前计划（批 4 Tab/Table + 滚动裁剪，oracle 已裁决，2026-06-21）

> 批 1/2/3 全部完成提交（Checkbox/Toggle、Radio/Segmented/Breadcrumb、Slider/TextInput）。批 4 oracle 裁决：立即开工、与真机验收并行、滚动地基独立前置、Table 本体前设真机闸门。切入顺序 **Tab → 滚动地基 → Table**。**步骤 A（Tab `5f649640`）、步骤 B（滚动地基 `97d214fb` + demo 接入 `f67e110d`）均已完成提交**，滚动 demo 已接入 VIS-SCENE-003 可真机验收。下一焦点为**真机闸门验收（Phase 1-3 + 批 1/2/3 全控件 + 新滚动 demo）** + 闸门通过后步骤 C Table 迁移。

1. **✅ 步骤 A：Tab 迁移（已完成提交 `5f649640`）**：新建 `SceneTab` 纯静态工厂控件——root(COLUMN) → tabBar(ROW，照 SceneSegmented R8 N 选 1 受控范式) + contentPanel(单内容区)。内容切换落成 **N 个独立 `rt.show(contentPanel, Computed(activeIndex==i), tabPanels.get(i))`**（不照搬旧栈命令式 clearChildren）。半受控 activeIndex（`ReadableSignal<Integer>` + `onActivate` Consumer，控件零状态不自改）。基础键盘 ←/→/Home/End/Enter/Space。补契约 **R10**（条件渲染内容区切换必须经 show，双重禁止建树期 if 分支 + 命令式重挂；R1-R8→R1-R10）。接入 SceneControlsHostWidget（activeTabSignal 受控源 + 3 页签独立 builder）+ 复用 VIS-SCENE-002。三命门主 Agent 独立验真（R3/R10 N 个独立 show 无建树期 if、R8 handler 只 onActivate 零自改、受控不自改测试真反证）。SceneTabTest 7/7 + scene 全包 370 测试零回归 + 编译零 problems。**排除（后续批）**：可关闭页签、页签溢出滚动、roving tabindex 之外复杂键盘、内容缓存优化。
2. **真机闸门（步骤 B 完成、动 Table 本体前必过）**：Phase 1-3 + 批 1/2/3 全控件 + 新滚动 demo 一起验。沙箱无 GUI，用户跑。入口 `/qzuilib test` → SCENE_DEMO 组 →「打开 Scene demo 页」/「打开 Scene 控件 demo」。重点：单测 stub 字体度量（FixedTextMeasurer charW=8）vs 真机度量系统偏差、Breadcrumb 段宽裁剪风险、滚动上屏。真机后派子代理查 `run/client/logs/fml-client-latest.log`，`club.heiqi.uilib` 应零异常。
3. **✅ 步骤 B：滚动/视口基础设施地基（已完成提交）**：复刻「深层 fill 约束下传」成功模式，从 Table 剥离成独立步骤。事实先行——exp-2 核实确认①生产滚轮桥接已存在（`LwjglInputSource.drainFrame()` 帧间差分×120 push SCROLL，守 I10，B5 可关闭）、②`ScenePaintEngine.paintNode` 无顶层剪枝守卫（每帧无条件全树 DFS）、③`SceneHostWidget.drawSelf` 每帧无条件 layout×2+paint 无守卫——故 oracle「需 geometry 路标放行下沉」假设被证伪但比假设更宽松（paint 本无剪枝，无需放行）。落地结论：
   - B1：SceneNode 新增 `scrollOffsetY`(int，**geometry 级**，setter 去重后只 `markGeometryDirty()` 绝不标 layout/paint) + `scrollable`(boolean，**LAYOUT 级**，setter `markSelfLayout()`) 两字段，两者级别严格分开；contentSize/viewportSize/maxScroll **全派生不存**。横向 `scrollOffsetX` 本期不加（YAGNI）。
   - B2：`SceneLayoutEngine.computeHeight` 顶部加 scrollable 钉死分支——scrollable=true 时**不走 `max(natural, preferredHeight)`**，直接返回视口高（优先级 preferredHeight > fill 约束高 > 安全回退内容高），首次解耦 viewport/content；content 子复用 COLUMN 主轴 UNCONSTRAINED 现状（**未改 buildChildConstraints**）。
   - B3：`ScenePaintEngine.paintNode` 子递归前 `childOffsetY = isScrollable() ? nodeAbsY - scrollOffsetY : nodeAbsY`（**只 paint 骨架注入**）；CLIP 条件加 `|| isScrollable()`，CLIP_PUSH 用 viewport 自己 `nodeAbsY`（**不含 offset**）裁固定窗口，复用现有 clipChildren 通路零新命令。非 scrollable 节点行为零变化。
   - B4：滚轮 handler 经 `rt.on(viewport, SCROLL)` 读 wheelDelta → `step=-wheelDelta`（向下滚 wheelDelta<0→offset 增大→内容上移）→ clamp [0, maxScroll] → 写 signal → bind 推给 setScrollOffsetY。
   - 三命门主 Agent 独立验真（亲读源码）：① scrollOffsetY seter 只 markGeometryDirty 零 layout/paint；② paint 偏移注入正确 + CLIP 不含 offset + 非 scrollable 通路不变；③ 测试真反证——`scrollShouldNotTriggerRelayout`（滚动帧 relayoutCount==0）/`scrollShouldReuseFragmentsWithoutRegeneration`（regeneratedFragmentCount==0）/`scrollOffsetShouldTranslateChildAbsoluteY`（top 0→-100）/`clipShouldStayFixedWhileContentScrolls`（CLIP 坐标恒定）全真断言非假绿。
   - 验证：jetbrainsBuildProject 零 problems；SceneScrollViewportTest 7/7 全绿；scene 全包 377 测试零回归。**NORTH_STAR 偏离已登记**（2026-06-21，scrollable 打破 bottom-up「容器高=内容高」模型，建议下版评估转正为一等公民）。诚实标注：`Invalidation` 枚举无 geometry 级，bind impact 仅文档用，真正失效级别由 seter 内 markGeometryDirty 决定。
4. **✅ 步骤 B 真机接入：SceneScroll 滚动 demo（已完成提交 `f67e110d`）**：用户拍板「长列表条目形态 + 新建独立屏幕不删旧的」。新建 `SceneScrollHostWidget`（root COLUMN fillParentHeight → title + viewport scrollable+preferredHeight=240+圆角 → content COLUMN 非 scrollable → 20 条斑马纹条目总高 640>240 触发滚动）+ `SceneScrollDemoScreen`（照 SceneControlsDemoScreen 范式 BaseScreen+openDemo+ESC 返回+dispose+onResize）。SCROLL handler **signal-first**（只 `scrollSignal.set(clamped)` 零直接 setScrollOffsetY，maxScroll 每帧重读 cachedLayout 重算）+ bind 推给 setScrollOffsetY。新增 VIS-SCENE-003 入口（UiTestMatrixRegistry + UiTestSampleVisualFactory `appendSceneScrollDemoStage`，绿色系按钮区分控件 demo）。**未碰**引擎核心 + 现有 SceneDemo/SceneControlsDemo 全部不动（零回退风险）。主 Agent 双命门独立验真（亲读 host + 测试全文）：① signal-first handler 内零 setScrollOffsetY；② **bubble 盲区补测** `scrollOnContentItemShouldBubbleToViewportHandler` 填验收7 盲区——命中第一条目几何中心（非 viewport 中心）+ `hitTestDeepest` 探针 assertSame 最深命中是 item + 正式 SCROLL 后断言 offset 0→120，真证 SCROLL 从 item 沿父链 bubble 到 viewport handler。SceneScrollDemoTest 3/3 + scene+demo 全包 380 测试零回归 + 编译零 problems。真机视觉（滚轮手感/裁剪上屏/ESC/按钮跳转）交用户跑。
5. **步骤 C：Table 迁移（在滚动地基上，本期最小范围）**：scrollable viewport → content(COLUMN) → 表头行 + 数据行(ROW，单元格 preferredWidth 表达固定列宽)。本期**静态行数据全量建树**（旧栈 refreshTable 也全量重建，不做 keyed diff）、固定列宽、表头+数据行、纵向滚动、单元格静态样式。**排后续批**：keyed 动态行 forEach、排序、行内编辑、横向滚动、滚动条、DataTable 工具栏。开工前 oracle 新开 session 产 Table 迁移契约。
6. **维持总策略**：暂停旧栈新功能/新样例，重点 scene 新栈迁移；发现旧栈问题先记录复现条件/影响面/验证方式/优先级，用户明确要求时再修。

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，偏离按《修订纪律》登记。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
