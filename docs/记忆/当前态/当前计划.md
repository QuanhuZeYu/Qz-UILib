# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 在 `fix/browser-semantics-phase2` 上保留首批 P0/P1 修复提交，暂不直接污染 `4.0`
2. 等 `gtnhconvention` GitHub manifest 外部波动恢复后，重跑完整 `./gradlew.bat --no-configuration-cache test`
3. 完整测试通过后，按仓库规范将 `fix/browser-semantics-phase2` 合并回 `4.0`
4. 继续处理 Phase 2 审查报告剩余 P2/P3 项，优先选择有明确回归测试的低风险语义修复

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
