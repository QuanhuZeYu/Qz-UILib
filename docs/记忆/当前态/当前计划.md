# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。计划变化时直接改写，不保留历史版本。

## 当前计划（批 4 Tab/Table + 滚动裁剪，oracle 已重评，2026-06-22）

> 批 1/2/3 全部完成提交（Checkbox/Toggle、Radio/Segmented/Breadcrumb、Slider/TextInput）。批 4 步骤 A（Tab `5f649640`）、步骤 B（滚动地基 `97d214fb` + demo `f67e110d`）完成。P0 解耦层收尾 `d2bccac8`、McScreenBridge 真机诊断插桩 `1924e889`、Slider/TextInput 真机前缺陷修复 `905461d0` 已完成；Demo2 文本桥接、`SceneTextInput` B1 字符级重设计、长文本 caret 偏移修复、中文 IME 同帧多 TEXT 合并修复均已完成并通过定向/组合回归测试。用户反馈真机测试没有异常后，步骤 C `SceneTable` 最小版已完成；本轮已完成独立 `SceneTable` demo 接入 `/qzuilib test` → SCENE_DEMO → `VIS-SCENE-004`。**下一焦点：真机验收 SceneTable demo，或进入后续 Table 能力预研（横向滚动/自适应列宽/DataTable 前需重新评估）。**

1. **SceneTable demo 真机复验**：入口 `/qzuilib test` → SCENE_DEMO → `VIS-SCENE-004` → 「打开 Scene Table demo 页」；重点复验从手型按钮进入页后指针恢复默认箭头，同时确认列对齐、固定列宽、固定行高、长文本裁剪、纵向滚动跟手、日志零异常。
2. **后续 Table 能力预研**：横向滚动、自适应列宽、动态 keyed 行、排序、行内编辑、滚动条、DataTable 工具栏均未做；启动前新开 oracle 评估，不复用旧 session。横向滚动对 Table 因固定列宽总和可算，难度低于通用双轴滚动；自适应列宽需列宽求解器和缓存策略，不能直接搬旧栈 `TableLayoutHelper`。
3. **维持总策略**：暂停旧栈新功能/新样例，重点 scene 新栈迁移；发现旧栈问题先记录复现条件/影响面/验证方式/优先级，用户明确要求时再修。

> 已完成的批 4 步骤 A（Tab `5f649640`）、步骤 B（滚动地基 `97d214fb` + demo `f67e110d`）、步骤 C（SceneTable 最小版）、新壳与解耦层、真机诊断插桩、Slider/TextInput 修复落地细节见 `docs/记忆/当前态/当前上下文.md` 与 `docs/记忆/当前态/交接记录.md`，不在本计划堆叠。

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，偏离按《修订纪律》登记。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
