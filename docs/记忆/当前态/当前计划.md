# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 继续评估绘制顺序中更细的浏览器语义缺口，优先处理普通树与 `top-layer` 混排时的 phase/clip 细节
2. 继续补 `top-layer` 内部 scrollbars、bounds、runtime layout 值与 animation event 的细粒度回归
3. 继续评估哪些高耦合交互测试迁到 `/qzuilib test` 运行时断言，同时保留最小 JVM 构建级契约
4. 后续每轮改动补对应 JVM 回归测试，并至少执行相关 `test --tests ...` 与 `compileJava`
5. 下次开始新阶段前，先基于当前共享场景实现重新审查 `RemoteHudOverlayClientBridge`、runtime smoke 页与 top-layer 相关运行时日志

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
