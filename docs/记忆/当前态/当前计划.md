# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。计划变化时直接改写，不保留历史版本。

## 当前计划（Scene 现代配置页地基，2026-06-23）

> 默认 `/qzuilib test` 已切到 `SceneTestHubScreen` 并由 git agent 提交为 `b482ba61`（`[Refactor]: 切换 qzuilib test 到 scene 新栈入口`）。硬编码隔离的 scene 配置表单 demo 已提交为 `343dae10`（`[Add]: 新增 scene 配置表单演示页`），用户测试反馈“没什么大问题”。用户最高指示仍然有效：旧 HTML-like / `ui.dom` 栈退出实际业务接入，暂不删除，仅作为废弃参考代码。下一步不直接开真实完整配置页，应先补 scene modern config 一期地基。

1. **实现 SceneSelect 首个消费者**：基于 top-layer 做浮空选项列表，选中/展开/关闭只写 signal，长列表先限高滚动；补 R11 契约。
2. **补 SceneSelect demo**：新增或扩展 demo 页面验证 top-layer 与 SceneSelect P0，不接真实 modern config 字段引擎。
3. **实现字段引擎最小切片**：新增 scene-native 字段 spec、draft/current、校验、dirty、canSave/canCancel 派生与保存语义。
4. **接真实配置数据源并补 demo**：优先接 `club.heiqi.config` / `MutableConfig`，验证一期字段类型、分类、校验、保存、取消和恢复默认。
5. **继续暂缓深水区**：搜索、树、多行文本、列表编辑、表格行内编辑、高级 picker、raw editor、远程同步、完整 CSS/token/theme 和 Phase 5 均暂缓。

> overlay P0 地基四段已完成并通过定向验证：①overlay 纯数据类+anchor resolver ②SceneRuntime.portal ③SceneInputRouter overlay 优先命中+dismiss ④SceneHostWidget 多 root layout/paint/replay（per-root layout engine 防串味）。详见 `docs/开发者文档/specs/scene-overlay-p0-plan.md`。

> 已完成的批 4 步骤 A（Tab `5f649640`）、步骤 B（滚动地基 `97d214fb` + demo `f67e110d`）、步骤 C（SceneTable 最小版）、新壳与解耦层、真机诊断插桩、Slider/TextInput 修复落地细节见 `docs/记忆/当前态/当前上下文.md` 与 `docs/记忆/当前态/交接记录.md`，不在本计划堆叠。

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
