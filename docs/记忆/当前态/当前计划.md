# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。计划变化时直接改写，不保留历史版本。

## 当前计划（Scene Layout demo，2026-06-23）

> 默认 `/qzuilib test` 已切到 `SceneTestHubScreen` 并由 git agent 提交为 `b482ba61`（`[Refactor]: 切换 qzuilib test 到 scene 新栈入口`）。用户最高指示仍然有效：旧 HTML-like / `ui.dom` 栈退出实际业务接入，暂不删除，仅作为废弃参考代码。用户已明确：颜色/外观属性直接写可接受，token/theme 不是当前主线，CSS/排版引擎更重要。P0 容器内容宽与 P1-a COLUMN 唯一 fill 子吃剩余高度均已完成，下一步回到独立 Scene Layout demo。

1. **新增 Layout demo 宿主**：实现 `SceneLayoutHostWidget` + `SceneLayoutDemoScreen`，复用现有 scene host pipeline，不抽公共基类。
2. **展示 P0/P1-a 能力**：页面顶部固定标题条，下方 scrollable viewport 用 `fillParentHeight` 吃剩余高度；内容展示 `FILL` vs `SHRINK`、ROW/COLUMN、padding/gap、preferredWidth 优先级、Breadcrumb 真实宽度。
3. **接入 test hub**：在 `SceneTestHubHostWidget` / `SceneTestHubScreen` 增加 Layout demo 入口；在旧 visual matrix 中登记 `VIS-SCENE-005`。
4. **补入口测试**：更新 `UiTestDocumentPageControllerTest`，断言 `VIS-SCENE-005` 入口、标题和说明卡片存在。
5. **继续暂缓深水区**：完整 CSS selector/cascade/stylesheet、token/theme、wrap、position、百分比单位、margin、box-sizing、多 fill 子 flex-grow 权重、Table 横向滚动/自适应列宽、旧视觉矩阵替代和 Phase 5 均暂缓。

> 已完成的批 4 步骤 A（Tab `5f649640`）、步骤 B（滚动地基 `97d214fb` + demo `f67e110d`）、步骤 C（SceneTable 最小版）、新壳与解耦层、真机诊断插桩、Slider/TextInput 修复落地细节见 `docs/记忆/当前态/当前上下文.md` 与 `docs/记忆/当前态/交接记录.md`，不在本计划堆叠。

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
