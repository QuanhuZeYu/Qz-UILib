# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 当前优先任务切换为拆分 `src/test/java/club/heiqi/uilib/ui/document/HtmlLikeDocumentWidgetTest.java`，该文件约 5896 行、122 个测试，已超过单文件职责边界
2. 第一批建议只做测试组织拆分：抽 `HtmlLikeDocumentWidgetTestSupport` 共享 helper，再迁移拖拽相关测试到 `HtmlLikeDocumentWidgetDragTest`
3. 第一批不要改生产代码语义，不引入新断言目标；以“测试搬家后行为完全一致”为完成标准
4. 拆分后串行运行 `git diff --check`、`HtmlLikeDocumentWidgetTest`、新拆出的测试类和 `compileJava`；不要并行执行多个 Gradle 命令
5. 后续再按风险递增拆 `FocusAndKeyboard`、`Scroll`、`LayoutCache`、`AnimationRuntime`、`EventDispatch`，不要一次性拆完整 5896 行文件
6. `/qzuilib test` 下一批 TextFont 视觉矩阵可暂缓，等本次大测试文件拆分第一批收口后再恢复

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
