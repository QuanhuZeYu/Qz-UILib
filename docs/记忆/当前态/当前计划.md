# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 创建 `fix/browser-semantics-phase2` 分支，开始 P0 修复
2. P0：修复 `insertBefore`/`replaceChild` 同父节点索引偏移 bug（`DocumentNode.java:204-209, 238-244`）
3. P0：修复 min/max 约束应用顺序（`DocumentLayoutEngine.java:569-578, 649-661`）
4. P0：修复 `position:fixed` 不创建 stacking context（`DocumentEffectChain.java:68-69`）
5. P1：修复 disabled 布尔属性语义、flex item min-width、flex-basis box-sizing、overflow+border-radius 裁剪、负 margin collapse
6. 每项修复补回归测试，执行 `compileJava` 和相关 `test --tests ...` 验证
7. 全部修复后合并回 `4.0`

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
