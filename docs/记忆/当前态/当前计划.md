# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 当前工作分支为 `4.0`；`4.2.3` 已发布并推送 tag，tag 目标提交为 `b2b76495`
2. `4.2.3` 范围已收口：包含 select 大列表虚拟化、默认滚轮/滚动条拖拽后的元素 `scroll` 事件分发、相关测试和文档
3. GitHub Release 页面正文来自 `.changelogs/4.2.3.md`，不包含本地验证板块；Release workflow #68 最终 `Status Success`
4. 后续发版继续沿用 `.changelogs/<tag>.md` 作为远端正文，只写本次更新和兼容性说明，本地验证结果不要写进远端 Release
5. 当前无发布阻塞；如需进一步确认大列表真实游戏内观感，可运行 `runClient21` 构造超长 select 页面手动观察

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
