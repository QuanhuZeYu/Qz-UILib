# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 生产代码第六轮审查的 5 个 findings 已完成修复、文档同步、定向验证和分支提交
2. 当前等待用户决定是否按规范将 `fix/production-code-round6-findings` 以 `git merge --no-ff` 合并回 `4.0`
3. 如继续审查，优先避开第三至第六轮已修点，转向尚未覆盖的 animation runtime cache、layout/paint cache 与远程 UI 后续交互续期设计

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
