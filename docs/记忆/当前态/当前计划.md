# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。
计划变化时直接改写，不保留历史版本。

## 当前计划（文本垂直对齐重构待真机验收，2026-06-25）

> 文本垂直/水平对齐 + ascent/descent/lineGap 度量链路 + half-leading 模型 + lineHeight 字体驱动 + lineSpacing 删除均已完成并提交。
> 待用户真机验收后合回 4.0。

1. ~~**文本垂直对齐**：TextVerticalAlign + half-leading 模型~~ ✅（`a05ea1c8` + `31c7201f`）
2. ~~**文本水平对齐**：TextHorizontalAlign + calculateTextLeft~~ ✅（`d1556934`）
3. ~~**ascent/descent/lineGap 链路 + lineHeight 字体驱动 + lineSpacing 删除**~~ ✅（`9be117c1` + `31c7201f`）
4. **真机验收**：用户跑 runClient21 确认垂直位置和行距，通过后合回 4.0
5. **高优先级缺口 TextArea**：多行文本输入，primitive 化 + chrome 适配
6. **嵌套分类树导航**：分类树导航组件
7. **字段引擎最小切片**：字段引擎最小切片

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）
  并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I1/I4/I6/I7/I10/I11）自检，
  偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，
  Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，
  不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 控件完成后必须建 demo 页接入 hub，否则用户无法真机验证。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
