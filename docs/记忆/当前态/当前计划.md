# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 当前分支 `refactor/remote-ui-runtime-lease` 正在完成远程 UI Runtime + Lease Protocol 第一阶段等价重构
2. 已搭建内部协议、asset store、session manager、server/client runtime 骨架，并让页面/HUD adapter 先接入现有公开入口
3. 收口前必须保持 `git diff --check`、`compileJava`、`club.heiqi.uilib.ui.remote.*` 相关测试和必要网络 Stream 回归通过
4. 本阶段不做 renew/resume 默认启用，不改变公开 API 默认语义；后续再继续把 `RemoteDocumentPages` / `RemoteHudOverlays` 瘦身为更薄 facade
5. 任务结束时按仓库规范提交，并更新 `docs/记忆/当前态/交接记录.md`

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
