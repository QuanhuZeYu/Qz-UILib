# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。
计划变化时直接改写，不保留历史版本。

## 当前计划（注释统一收口完成，2026-07-01，HEAD `b90eb53e`）

> ArchUnit 移除 + L2 边界文档化 + 全仓注释统一收口三段已提交，工作区干净、全量测试绿。
> 当前分支 `feat/modern-config-p0-core`，主线转向现代化配置页 P0 核心层实现
> （思维模型见 DECISION-20260628-modern-config-new-mental-model）。

1. **启动配置页 P0 核心层实现**：落 Schema/Authority/DraftBuffer/Persistence/EventBus/UI 层适配
   （架构模型已定，独立实现任务）
2. **可选：L2 边界补零依赖 grep 脚本**（替代已移除的 ArchUnit，断言 `scene/layout/**` 无四包 import）
3. **可选：§7.1 补第六类「多帧批次」/ 第七类「drag 序列」白盒场景**（待 Oracle 裁决，见交接记录遗留）
4. **可选：布局引擎收口 P2 批次**（setter 链式化 + 容器工厂糖 / bind impact 去留，需用户拍板，见 DECISION-20260701-scene-container-factory-and-chain-setter）
5. **可选：push 分支到远端 / 清理本地已合并分支**
6. **可选：用户自定新方向**

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）
  并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I1/I4/I6/I7/I10/I11）自检，
  偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，
  Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，
  不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 控件完成后必须建 demo 页接入 hub，否则用户无法真机验证。
- **fixer 必须串行执行**（不得并行多个 fixer，读与写不得同批并行）；fixer 自行串行编译/测试验证，避免 Gradle 竞争 build/ 目录误报（ERROR-20260624 已立项）。
- **token 实际值以 SceneChromeTokens.java 源码为准**，不依赖过期文档清单。
- **oracle 裁决架构方案后**，如方案涉及长期可引用的设计决策，
  建议补写 `docs/记忆/决策/` 文档沉淀。
- **I11 逃生舱②扩张纪律**：扩之前证明它确实是一类新的权威交互真值，
  而非可以用现有机制表达的东西。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。