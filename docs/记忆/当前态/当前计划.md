# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。
计划变化时直接改写，不保留历史版本。

## 当前计划（I6 + B2 已合回 4.0，2026-06-25）

> 文本垂直/水平对齐 + 度量链路 + half-leading 模型 + lineHeight 字体驱动 + lineSpacing 删除均已完成，真机验收通过并合回 4.0。
> SceneTextArea 基础多行文本组件已合回 4.0，真机验收通过（含 caret 撑满 bug 修复）。
> scene 架构审核 P0 两项（B1+B3）+ TextArea caret 撑满 bug 已合回 4.0。
> I6 replayer 反向依赖 ui.style 切断 + B2 TextArea O(N²) 文本几何消除已合回 4.0。

1. ~~**I6 replayer 反向依赖 ui.style**（P1）~~：scene 内自建圆角值对象切断依赖 → 实际方案：drawSurface 第 7 参降级为 int，render 层保留 ResolvedCornerRadii 重载 ✅（`c9c53208`）
2. ~~**B2 TextArea O(N²) 文本几何**（P1）~~：Step1 行结构前缀和缓存 + Step2 点击前缀宽数组缓存，oracle 裁决方案 B(R1) ✅（`ae263575` + `7b7b1722`）
3. **嵌套分类树导航** / **字段引擎最小切片**（新功能，待用户拍板范围）
4. 其余温床（B4/B5/B6/B7/B8/A1/A6）按需处置，chrome 主题层单独立项
5. 真机验收 B2 文本几何性能收益（O(N²)→O(N) 帧率实测，交用户跑）

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）
  并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I1/I4/I6/I7/I10/I11）自检，
  偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，
  Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，
  不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 控件完成后必须建 demo 页接入 hub，否则用户无法真机验证。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
