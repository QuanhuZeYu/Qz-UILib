# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. Modern Config 模板配置页已完成 Batch 0/1/2，下一步从 Batch 3 嵌套结构模板开始。
2. Batch 3 需要新增 `DocumentTreeViewControl` 与 `DocumentBreadcrumbControl`，并实现 `NestedCategoryBinding`、`ObjectPropertyBinding`。
3. 普通 map 可卡片内联编辑，递归深度默认限制为 5；超深层级通过展开编辑或导航处理。
4. 继续保持可选 `club.heiqi.config` 模块主用入口，检测不到模块时回退现有 `ForgeConfigTemplateScreen`。
5. 不做 Forge 到 config 模块的配置迁移；复杂结构的 Forge 回退兼容由接入方自行设计，建议用 JSON 字符串 cfg 承载。
6. 每个 Batch 结束必须运行 `git diff --check`、相关测试或 `compileJava`，提交 git，并更新 `docs/记忆/当前态/交接记录.md` 给出下一位 Agent 的接力提示词。

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准
- 每批 Modern Config 施工至少通过 `git diff --check`；涉及生产 Java 代码时至少补相关测试或运行 `compileJava`
- 本地验证命令和结果只记录在协作交接、提交说明或开发者文档中，不写入远端 GitHub Release 正文

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
