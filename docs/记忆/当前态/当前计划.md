# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 当前工作分支 `add/character-font-rules` 已完成字符字体规则实现、审查修复和定向验证，准备按 `git merge --no-ff` 合并回 `4.0`
2. 字符字体规则实现包含 `FontCharacterRule` / `FontCharacterRuleSet`、`fontSystem.characterFontRules`、`FontMatcher` 规则优先匹配、配置页专用编辑器和远程同步字段级校验
3. `FontCharacterRuleControl` 字体名输入复用 `DocumentAutocompleteInputControl`，候选来源为 `FontConfig.getFontSortSnapshot()`，空输入时展示最多 128 个当前字体目录候选
4. 重叠启用规则不再阻断保存，只在配置页提示；运行时继续按首个启用且命中的规则优先
5. 已通过 `git diff --check` 和字符规则、FontMatcher、配置页绑定、远程同步、autocomplete 定向测试；合并后需确认工作区干净

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
