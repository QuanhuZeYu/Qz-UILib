# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 对 `/qzuilib test` 全 10 个运行时二级页做游戏内人工复查，重点确认 selection/caret、step、软换行、fallback、reload、动画事件/视觉、宿主运行时和 RemoteNet 服务端链路
2. 若人工复查发现页面文字、状态或视觉输出不清晰，先更新规格文本，再做最小页面调整
3. 单独评估 `inline-block baseline`：需要行内布局盒延迟落位或可重定位盒模型，不与属性选择器等 B 类缺口混做
4. 继续 B 类缺口清单，下一项优先评估属性选择器 `[attr]` / `[attr=value]`
5. 每轮继续执行 `git diff --check`、相关测试和 `compileJava`，必要时再做完整 `test`

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
