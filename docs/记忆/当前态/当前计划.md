# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。计划变化时直接改写，不保留历史版本。

## 当前计划（真实配置页新栈垂直切片，2026-06-22）

> 默认 `/qzuilib test` 已切到 `SceneTestHubScreen` 并由 git agent 提交为 `b482ba61`（`[Refactor]: 切换 qzuilib test 到 scene 新栈入口`）。用户最高指示仍然有效：旧 HTML-like / `ui.dom` 栈退出实际业务接入，暂不删除，仅作为废弃参考代码。提交后 oracle 已评估下一步：优先做一个最小真实配置页新栈垂直切片，Table 深水区、旧视觉矩阵替代和 Phase 5 暂缓。

1. **选择真实配置页切片**：读 `docs/记忆/长期事实/Config模块.md` 与实际 `club.heiqi.config` 源码，筛选 5–8 个字段、只需 STRING/BOOLEAN/NUMBER/单选枚举的最小真实配置页，并用 question 向用户拍板。
2. **复用现有新栈宿主**：优先复用 `SceneControlsHostWidget` / `SceneControlsDemoScreen` 的输入桥和 scene pipeline，不新造宿主；实现前确认 TextInput/Toggle/Segmented/Radio Props 与 signal 绑定方式。
3. **打通编辑保存闭环**：实现 load → signal → 控件编辑 → 显式保存 → 回读验证，handler 只写 signal，保存走受控命令，不绕过 NORTH_STAR I1/I2/I9/I11。
4. **搭车复验 POINTER**：切片真机验收时顺带确认从手型入口进入新栈页后鼠标恢复默认箭头；若失败再单独立项。
5. **暂缓深水区**：Table 横向滚动/自适应列宽/动态 keyed 行、旧视觉矩阵替代、Phase 5 均等真实切片暴露需求后再新开 oracle 评估。

> 已完成的批 4 步骤 A（Tab `5f649640`）、步骤 B（滚动地基 `97d214fb` + demo `f67e110d`）、步骤 C（SceneTable 最小版）、新壳与解耦层、真机诊断插桩、Slider/TextInput 修复落地细节见 `docs/记忆/当前态/当前上下文.md` 与 `docs/记忆/当前态/交接记录.md`，不在本计划堆叠。

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，偏离按《修订纪律》登记。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
