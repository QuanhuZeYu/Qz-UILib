# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 重新规划 `/qzuilib test` 运行时测试设计：先定义目标用户、卡片字段、状态模型、自动/人工边界和分组矩阵
2. 规划前不要继续修补旧运行时卡片；先重新定义字段、状态、样例展示和自动/人工边界
3. 重新接入前保持当前空态页面：分组导航可打开，二级页显示清空说明，不展示旧用例、执行入口或人工确认按钮
4. 用 `runClient21` 做一次游戏内打开和关闭复测，确认 test 空态可打开且关闭阶段不再新增 `run\client\hs_err_pid*.log`
5. 每轮继续执行 `git diff --check`、相关测试和 `compileJava`，必要时再做完整 `test`

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
