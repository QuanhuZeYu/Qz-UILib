# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 以 `docs/开发者文档/specs/qzuilib-test-page-visual-matrix-plan.md` 为入口重建 `/qzuilib test`：视觉化展示功能优先，浏览器语义验证为重要目标
2. 保持现有 DOM / CSS / Layout / Paint / Input / Controls / TextFont / Animation / RuntimeHost / RemoteNet 十组导航，不恢复旧页面名结构
3. 实现前先设计新的 registry、分组视觉 builder、语义 checker 和视觉/语义双状态模型；不要继续修补或恢复旧运行时卡片矩阵
4. 重建时避免继续向 `UiTestDocumentPageController` 堆叠逻辑，单个代码文件接近或超过 1000 行时必须评估职责拆分
5. 每批保持 `git diff --check`、目标测试和 `compileJava` 通过；涉及游戏内 UI、HUD、输入、命令入口、远程页面或网络 smoke 时再用 `runClient21` 验证

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
