# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。
计划变化时直接改写，不保留历史版本。

## 当前计划（文本垂直对齐待真机 debug 验证，2026-06-25）

> 垂直对齐（lineHeight 行框居中）+ 水平对齐 + ascent/descent 度量链路暴露 + 量纲债 javadoc 标注均已实现并提交。
> 下一步：用户真机看三线 debug 确诊行框偏移方向，再决定 calculateTextTop 公式如何用 ascent/descent 升级。

1. ~~**文本垂直对齐**：TextVerticalAlign + 行框居中~~ ✅（`a05ea1c8`）
2. ~~**文本水平对齐**：TextHorizontalAlign + calculateTextLeft~~ ✅（`d1556934`）
3. ~~**ascent/descent 链路暴露 + 量纲债**：font-level 标量 + SceneTextMeasurer default 方法 + javadoc~~ ✅（`9be117c1`）
4. **真机三线 debug 验证**：用户改 DEBUG_TEXT_LAYOUT=true 跑 runClient21，看行框偏上还是偏下有空白
5. **calculateTextTop 公式升级**：根据 debug 结果用 ascent/descent 做 baseline-aware 居中
6. **高优先级缺口 TextArea**：多行文本输入，primitive 化 + chrome 适配
7. **嵌套分类树导航**：分类树导航组件
8. **字段引擎最小切片**：字段引擎最小切片

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）
  并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I1/I4/I6/I7/I10/I11）自检，
  偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，
  Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，
  不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 控件完成后必须建 demo 页接入 hub，否则用户无法真机验证。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
