# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 生产代码第六轮审查的 5 个 findings 已完成修复、文档同步、定向验证和分支提交；当前分支仍为 `fix/production-code-round6-findings`，尚未合并回 `4.0`
2. 生产代码第七轮只读审查已完成并留档到 `docs/开发者文档/reviews/REVIEW-20260609-production-code-round7.md`
3. 下一步优先修复第七轮 2 个 findings：运行态 transform fixed/clip/scroll metrics 语义、hit-test passthrough / `pointer-events:none` 与默认滚动/滚动条命中一致性
4. 修复后需补对应回归测试，并至少执行 `DocumentVisualTraversalTest`、`DocumentHitTestEngineTest`、`DocumentScrollStateTest`、`HtmlLikeDocumentWidgetScrollTest`、`HtmlLikeDocumentWidgetAnimationRuntimeTest` 与 `compileJava`

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
