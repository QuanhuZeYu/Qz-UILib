# 当前计划

本文件记录「接下来做什么」，只保留 3 到 7 个短期内真正会执行的步骤。计划变化时直接改写，不保留历史版本。

## 当前计划（提交 Form demo 与规划现代配置页地基，2026-06-23）

> 默认 `/qzuilib test` 已切到 `SceneTestHubScreen` 并由 git agent 提交为 `b482ba61`（`[Refactor]: 切换 qzuilib test 到 scene 新栈入口`）。用户最高指示仍然有效：旧 HTML-like / `ui.dom` 栈退出实际业务接入，暂不删除，仅作为废弃参考代码。硬编码隔离的 scene 配置表单 demo 已实现并通过审核/定向测试，用户测试反馈“没什么大问题”。用户明确暂不直接开始真实配置页，应先评估/补齐现代配置页所需的新栈地基。

1. **提交保存 Form demo**：按 AGENTS.md 使用 `[English]: 中文标题` 提交本轮 Form demo、测试和记忆文档改动。
2. **编写地基补全设计文档**：先做现代配置页 scene 迁移/地基补全设计，不直接写真实配置页。
3. **拍板一期边界**：建议一期只覆盖 `STRING/NUMBER/BOOLEAN/CHOICE`、扁平分类、草稿保存，暂缓搜索、树、多行、列表编辑、高级 picker、远程同步。
4. **单独深评 Select/top-layer**：这是现代配置页一期最大地基缺口，需先定 scene 浮层提升、命中和失效模型。
5. **继续暂缓深水区**：完整 CSS selector/cascade/stylesheet、token/theme、wrap、position、百分比单位、margin、box-sizing、多 fill 子 flex-grow 权重、多行文本、Tab 焦点遍历、Ctrl+S/Enter 提交、远程同步和 Phase 5 均暂缓。

> 已完成的批 4 步骤 A（Tab `5f649640`）、步骤 B（滚动地基 `97d214fb` + demo `f67e110d`）、步骤 C（SceneTable 最小版）、新壳与解耦层、真机诊断插桩、Slider/TextInput 修复落地细节见 `docs/记忆/当前态/当前上下文.md` 与 `docs/记忆/当前态/交接记录.md`，不在本计划堆叠。

## 每步完成标准

- 涉及生产 Java 代码改动时至少通过 `compileJava`（优先 `jetbrainsBuildProject`）并补相关测试；提交前过 `git diff --check`。
- scene 新栈改动须对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，偏离按《修订纪律》登记。
- 实现完成后必须经一次独立子代理审核；代码实现审核默认使用 @reviewer，Oracle 仅用于架构方案裁决/复杂边界复核且默认新开 session。
- 帧率/真机实测交用户跑，本地验证结果只记录在交接、提交说明或开发者文档中，不写入远端 Release 正文。
- 完成独立任务后，检查是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。

## 更新规则

- 只保留 3 到 7 个短期有效步骤。
- 计划变化时直接改写，不保留历史版本。
