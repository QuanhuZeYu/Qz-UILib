# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 下一步从 `overflow/scroll` 语义开始：布局容器、滚动范围、clip 与交互的一致性
2. 然后处理 `stacking context`：创建条件、绘制顺序、命中顺序与 top-layer 边界
3. 继续评估哪些高耦合交互测试迁到 `/qzuilib test` 运行时断言，同时保留最小 JVM 构建级契约
4. 每轮改动补对应 JVM 回归测试，并至少执行相关 `test --tests ...` 与 `compileJava`
5. 每轮收口后更新 `当前态/`、`交接记录.md` 与必要的决策/使用文档

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
