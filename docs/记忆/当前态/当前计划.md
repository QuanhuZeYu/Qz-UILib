# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 继续推进 HTML-like 脏子树布局缓存：在已补 DOM 移动、继承样式、样式表和 `textMeasure` epoch 回归基础上，优先设计可平移复用测试
2. 评估是否放宽当前 `containingLeft` / `flowTop` 完全一致限制；若放宽，必须覆盖 margin collapse、auto margin、relative/sticky 和 containing block 坐标
3. 可平移复用稳定后，再按独立批次评估 flex/table/inline 缓存扩展，不要一次性大改或打开 layout 动画运行态复用
4. CSS / Layout / Paint / Input 首轮 24 张视觉样例已补齐；脏子树阶段收口后，下一批继续 Controls 视觉样例，每批 7-12 张
5. 每批保持 `git diff --check`、相关测试和 `compileJava` 通过；不要并行执行多个 Gradle 命令；涉及游戏内 UI、HUD、输入、命令入口、远程页面或网络 smoke 时再用 `runClient21` 验证

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
