# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 生产代码第七轮修复已提交并通过 `git merge --no-ff` 合并回 `4.0`
2. 当前只剩文档状态同步分支 `docs/round7-merge-state` 需要提交并合回 `4.0`
3. 主线分支检查结果：本地未合入 `4.0` 的分支只有 `codex/birthday-remote-trigger-practice`；如需清理已合并分支，先征得用户确认

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
