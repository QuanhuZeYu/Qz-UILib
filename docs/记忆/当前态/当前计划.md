# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. `4.2.0` 已按本地发布说明覆盖流程重新发布：tag 指向 `8d086ab`，GitHub Release 标题为 `4.2.0`，正文来自 `.changelogs/4.2.0.md`
2. 已迁移 Qz-Miner 的本地发布流程约束：`.changelogs/<tag>.md` 作为 GitHub Release 正文源，workflow 后置步骤统一改写 Release 标题和正文
3. 发布前最终验证已通过：`git diff --check`、`./gradlew.bat --no-configuration-cache test`、`./gradlew.bat --no-configuration-cache --no-daemon -x test publishToMavenLocal`
4. 本机已安装 GitHub CLI `gh` 2.93.0，安装目录 `C:\Program Files\GitHub CLI\` 已加入系统 `Path`；当前 shell 需刷新 `Path` 或重启终端后直接使用
5. `runClient21` 已解除 `BytePatternMatcher` 缺类，但本地 GTNH 2.9 beta smoke 仍受 `ServerUtilities 2.3.0` 与 `Et-Futurum-Requiem 2.6.40-GTNH` 第三方 mixin 冲突阻塞

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
