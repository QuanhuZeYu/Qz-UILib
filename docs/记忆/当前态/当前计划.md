# 当前计划

本文件记录“接下来做什么”，只保留短期内真正会执行的步骤。

## 当前计划

1. 完成 `refactor/decouple-lwjgl3ify` 加固验证：新增输入后端测试、`git diff --check`、输入/控件/HUD 定向测试、`compileJava`。
2. 如验证发现输入后端编译或行为问题，优先修 `UiInputService` facade 与 `UiInputBackend` 内部实现，不改调用点。
3. 发布依赖已降级为 `devOnlyNonPublishable`；如需确认 POM，可额外跑 `publishToMavenLocal` 并检查元数据不再声明 `lwjgl3ify`。
4. 第一阶段不重构 OpenGL、字体 atlas、`org.lwjgl` / `org.lwjglx` 基础客户端链路；纯 JVM 测试若碰到 GL/SDL 类加载，应改测试替身而不是加运行依赖。

## 每步完成标准

- 总导航保持精简，不重新堆积长期事实和进度流水账
- 长期事实只记录中长期仍有效的目录、边界、命令和项目约定
- 当前态可频繁覆盖，但必须服务下一位接手者快速恢复现场
- 决策记录写清背景、候选方案、最终选择、影响范围和后续注意事项
- 验证命令和运行前提以 `docs/记忆/长期事实/稳定命令.md` 为准
- 输入解耦至少通过新增输入后端测试、`git diff --check`、相关输入/控件/HUD 定向测试和 `compileJava`
- 发布依赖降级后如涉及发布验收，需验证 `publishToMavenLocal` 产物元数据中不再强制声明 `lwjgl3ify`

## 更新规则

- 只保留 3 到 7 个短期有效步骤
- 计划变化时直接改写，不保留历史版本
