# 布局引擎范式统一收口任务清单

> 创建：2026-07-01 ｜ HEAD：`92a6fd05` ｜ 分支：`feat/modern-config-p0-core`
> 依据：研究编排 2 Oracle（容器工厂糖评估 + 范式统一性普查）交叉结论

## 总判定

**当前范式其实相当统一**，已被 `package-info.java` R1-R11 十一条契约红线 + `NORTH_STAR.md` I1-I12 不变量强约束焊死。颜色 token 收口干净、flush 零泄漏、JUnit4 统一、create 签名统一。真正需要收口的点少而集中——主要是"两派范式缺一句话说清边界"和几处死代码/命名残留。

## 关键事实（决策依据）

- **SceneNode setter 全部返回 void（不链式）**：`SceneNode.java:1293/1050/1074/1093/1116/1138/1221/1334` 全 void，ast_grep 搜 `public SceneNode set$NAME` 零匹配。容器工厂糖只能压方向设置一行，后续配置仍是散的 void setter。
- **容器创建样板量**：211 处 `new SceneNode()` + 143 处 `setFlexDirection`，但典型容器 4-6 行，高度重复。
- **两派分层是隐性的**：组件层（16 个，`Supplier<SceneNode>` + record Props）vs Primitive 层（9 个，`Result` + 无样式）。package-info 未成文。
- **bindDerived 是死代码**：`SceneRuntime.java:221/247/249` 三重载全库 0 调用（我们上一轮加的，但从未用过）。
- **bind 的 Invalidation impact 参数运行时丢弃**：刻意保留作 I4 审查双轨锚点，每个 bind 调用点写一个不生效参数（认知税）。
- **宽高轴已基本对称**：fillParent/preferred/max/percent 全对称（偏离 2026-07-01 已还清）。唯一剩余：`WidthSizing{FILL,SHRINK}` 无 `HeightSizing` 对偶——但这是 NORTH_STAR §4:111-113 明示的正当例外（高轴天然 shrink）。

## 任务清单（按优先级）

### P0 —— 纯清理，零风险，立即可做

| # | 任务 | 成本 | 证据 |
|---|---|---|---|
| P0-1 | **删死代码 `bindDerived` 三重载** | 极低（删 ~50 行 + Javadoc） | `SceneRuntime.java:221/247/249` 全库 0 调用 |
| P0-2 | **统一 create 参数名 `runtime`→`rt`** | 低（改 1 处签名+调用） | `SceneDataTable.java:687` 唯一用 `runtime` |

### P1 —— 文档收口，把隐性范式显性化（防未来散乱）

| # | 任务 | 成本 | 收益 |
|---|---|---|---|
| P1-1 | **package-info 补 R12「两派分层规则」** | 极低（加一段） | 明文规定"无样式行为核心=`XxxPrimitive` 返回 `Result`；有样式组件=`Xxx` 返回 `Supplier<SceneNode>`；字段>6 或多可选参上 class Props+Builder"。杜绝后人建控件走错派 |
| P1-2 | **WidthSizing 补「高轴无对偶」注释** | 极低（1 行注释） | 指向 NORTH_STAR.md:111-113，说明高轴天然 shrink 故无 HeightSizing。消除"是否漏了"的困惑 |

### P2 —— 需用户拍板（架构取舍）

| # | 任务 | 成本 | 取舍 |
|---|---|---|---|
| P2-1 | **setter 链式化 + 容器工厂糖** | 中 | **Oracle 1 完整评估后的推荐方案**（见下"P2-1 完整方案"） |
| P2-2 | **bind 的 Invalidation impact 参数去留** | 中（100+ 调用点） | 保留=接受认知税换审查锚点；砍掉=简化 API 但失 I4 双轨核对。**需用户拍板** |

### P2-1 完整方案（Oracle 1 完整评估后推荐）

**核心洞察**：SceneNode 37 个 setter 全 void（不链式）。纯工厂 `column()` 只能压"方向"一行，后续配置仍散。**真正的杠杆是 setter 链式化**（`void setXxx` → `SceneNode setXxx` 返回 this），让工厂+链式 = 真正流畅 DSL。

**推荐组合：链式化 37 setter + 4 个静态工厂**

链式化后效果（5 行塌成 1 行）：
```java
// 改前
SceneNode row = new SceneNode();
row.setFlexDirection(FlexDirection.ROW);
row.setGap(8);
row.setPadding(12);
row.setFillParentHeight(true);

// 改后
SceneNode row = SceneNode.row().setGap(8).setPadding(12).setFillParentHeight(true);
```

**工厂糖 API（只做零参+单参，拒绝双参重载）**：
```java
public static SceneNode column() { ... }
public static SceneNode row() { ... }
public static SceneNode column(int gap) { ... }
public static SceneNode row(int gap) { ... }
```

**兼容性**：void→引用类型对丢弃返回值的调用点透明（`node.setX();` 照常编译）。**唯一风险**：`::setX` 方法引用从 `Consumer` 变 `Function`，必须先 grep `::set` 扫全仓核实。

**迁移**：链式化天然零迁移（存量调用点不动）；工厂糖不强迁存量 211 处（YAGNI，自然演进）。

**排除的方案**：
- ❌ Builder（C）：与 void setter 体系冲突，与项目 Props-Builder 语义错位
- ❌ HeightSizing 补全：高轴天然 shrink（默认态），无需显式 SHRINK 开关，补了制造语义模糊

**实施步骤**：
1. 先 grep `::set\w+` 扫全仓确认无方法引用阻断
2. AST 批量改 37 setter 链式化（`void`→`SceneNode` + `return this`）
3. 加 4 个静态工厂
4. 全量测试回归

### P3 —— 独立范畴，不属本次收口

| # | 任务 | 说明 |
|---|---|---|
| P3-1 | 旧栈 `Document*Test`/`HtmlLike*Test` 清退 | 独立任务，旧 HTML-like 栈残留 |
| P3-2 | 测试方法命名 `should` vs `test` 统一 | should 主导（313 处）但混 test（83 处），非阻断 |

## 实施建议

**批次 1（P0 + P1，零风险清理 + 文档）**：4 项全做，一个 fixer 一次完成，单 commit。
- 删 bindDerived
- 统一 create 参数名
- package-info 补 R12
- WidthSizing 补注释

**批次 2（P2，需用户拍板）**：容器工厂糖 + bind impact 去留，等用户决策后实施。

## 不变量守恒

- P0/P1 全部零行为变化（删死代码 + 注释 + 命名），I1-I12 无触碰
- P2-1 容器工厂糖若走 (a) 纯工厂路径，也是纯增量，零破坏
- P2-1 若走 (b) 链式 setter 路径，需回归全量布局测试（setter 返回类型变化）
