# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-21 第 47 次）—— 批 3 Slider + TextInput 档位 A 全部完成提交

### 一句话现状
Phase 4 控件层重建：**批 3（TextInput/Slider 迁移）两控件全部落地、主 Agent 独立验真、已提交**。SceneSlider 确立 R7 连续版契约、SceneTextInput 确立 R9 受控文本输入契约（档位 A 全声明式，还清旧栈命令式 render caret 范式债）。工作树干净，下一会话可开工**批 4（Tab/Table + 滚动裁剪）**，但建议先过真机验收硬闸门（Phase 1-3 + 全控件，沙箱无 GUI 须用户跑）。

### 当前分支与已提交基线
- 分支 `refactor/scene-phase4-control-layer`（从 `4.0` 切出）。最近提交（新→旧）：
  - `15eb3e4a` 批 3 TextInput 完成态记忆同步
  - `8e179b94` **批 3** SceneTextInput 档位 A 迁移（确立 R9 + 声明式 caret + 决策记录）
  - `e9df2ac7` **批 3** SceneSlider 迁移（确立 R7 连续版 + committing 双语义 + draggingValue 瞬态 signal + 修 root 隐形命中区缺陷 A）
  - `7c0d33d6` 人工调整
  - 更早：`a6288dfe`/`93469b05` 批 2 + `98a655c5` 深层 fill + `89bb9e6d` 批 1 + `4721b6a5` C 任务 transform + `237e8a33` 固定尺寸地基 + `5cb593fd` 批1前置②字体度量 + `9fca7d38` 偏离2修复 + `a33d80d8` 第0段地基
- 工作树对源码干净（仅未跟踪 `.slim/`、`opencode.json`，永不提交）。

### 本会话两大成果（均已主 Agent 独立验真，非仅信 fixer/oracle 报告）

**① SceneSlider 受控连续滑块（确立 R7 连续版，提交 `e9df2ac7`）**
- R7 连续版：value 受控（`ReadableSignal<Double>` + `onChange(double, committing)`），`draggingValue` 控件本地瞬态 signal（`create()` 体内局部、闭包捕获归 Owner，非实例字段守 R1），仅 pointerCapture 生命周期存活，渲染读 `effectiveValue = draggingValue ?? value`，松手清 null 回落外部值。committing 双语义（拖拽预览 false / 释放+键盘提交 true）映射旧栈两 notify。
- thumb/fill 用 preferredWidth + ROW + MainAxisAlign 喂连续数值（既有能力，不构成 flex-only 偏离）；fillBox 宽 `clamp(round(W*progress)-thumb/2, 1, W)`，FILL_MIN_WIDTH 下限钉 1 而非 0（setPreferredWidth(0)=不约束回退 fill 父宽语义）。拖拽走 LAYOUT 本批接受（离散输入非自走动画，不违信条五，I7 锁死局部重排）。
- **主 Agent 亲读源码揪出真实 UX 缺陷 A**（fixer 未察觉）：root 无 preferredWidth 被 fill 拉满可用宽，track 仅 200 左对齐 → root 右侧空白成隐形命中区，点它 valueFromPointerX ratio clamp 1 → 值跳 max。ora-2 复核确认后修复 `root.setPreferredWidth(TRACK_WIDTH)`，补两条真回归断言（`pointerDownRightOfTrackDoesNotJumpToMax` 点 left+300 断言 onChange 不触发 + `rootHitWidthEqualsTrackWidth` COLUMN+STRETCH 下断言 root 宽==200）。同时清理 fixer 关于 rootAbsX 偏移的误判注释（实测 pointerX 与 absoluteX 同基准无偏差）。
- 验证：SceneSliderTest 11 测试全绿，scene 全包 `--rerun-tasks` 零回归。不构成新偏离。

**② SceneTextInput 受控文本输入（确立 R9，档位 A 全声明式，提交 `8e179b94`）**
- **三轮 oracle 逐层纠偏定范围**：ora-1 裁档位 B + 方案 β → ora-2 纠正「β 在 paint 层算前缀宽落不了地（paint 引擎拿不到 measurer）」提方案 γ → ora-3 终审纠正最关键认知误区：**用户要的是「靠近信条范式」不是「功能多」，二者正交**。caret 定位/选区是 feature 非 paradigm，超出 strangler 行为等价范畴。最终锁定**档位 A 行为等价 + 全声明式范式升级**，零偏离、消解 β/γ 之争。
- 确立契约 **R9 受控文本输入零状态**（R7 从布尔/连续到 String 推广）：value 外部 `ReadableSignal<String>` 唯一驱动，输入只 `onChange.accept(真实 String)` 上抛，控件绝不自缓存/自改。package-info 红线 8→9 补 R9。
- **关键洞察：caret 位置不是状态，是 f(value 长度) 布局派生**——caret 作为 textNode 兄弟叶节点（preferredWidth=1 + preferredHeight=16）靠 ROW 逐子定位自然排到文本末尾右侧，零本地 signal、零度量（比 Slider 还简单，无 draggingValue）。caret 可见性 bind `is.focused()` 切 CARET_COLOR↔透明（PAINT 级，照搬 Toggle 读 is 范式）。**范式升级：旧栈命令式 fillRect 画 caret → 声明式 caret 子节点 + signal 驱动**。
- 行为等价旧栈红线全复刻：字符过滤（拒 ISOControl/\n/\r/\t、NUMBER 字符集）、maxLength 码点数填满拒绝不截断、退格 offsetByCodePoints 删整码点防代理对、密码掩码 displayText 按码点替换但 onChange 上抛真实值、placeholder、readOnly vs disabled、onChange 每次真实变化触发无内置 onEnter。新建 SceneInputType 枚举不 import 旧栈。
- 四命门独立验真（亲读源码）：R9 命门测试真断言（输入 a 后 onChange 收 a 且 valueSignal 仍空）、I1/I11 handler 只 onChange+读 signal 零 setXxx、声明式 caret 定位真成立（test 断言 caretX>=text.x+width 且 value 变长右移）、行为等价红线全测。
- 一处 fixer 诚实标注的能力缺口（非本控件 bug）：cursor 用 POINTER 而非 TEXT/I-beam，因 SceneCursor 枚举暂无 TEXT 值，不在档位 A 范围。
- 验证：SceneTextInputTest 10 测试全绿，scene 全包 `--rerun-tasks` 零回归。**不构成偏离**，沉淀决策记录 `DECISION-20260621-scene-textinput-tier-a-declarative-caret.md`（功能维度 vs 范式维度辨析 + β 否决/γ 回填方向 + 字符级度量缺口）。

### 下一步（批 4 + 悬空硬闸门）
- **悬空硬闸门（建议先做）**：ora-2 提出 Phase 1-3 真机验收 + 全控件（含 Slider/TextInput）真机外观交互全靠单测、未经真机。沙箱无 GUI，只有用户能跑。建议开批 4 前先过此闸门——TextInput/Slider 真机度量与单测 stub（FixedTextMeasurer charW=8）可能有系统偏差，Breadcrumb 段宽偏离登记的裁剪风险也只有真机能验。
- **批 4：Tab/Table（+ 滚动裁剪）**（按 8 批路线图）。强依赖深层 fill（已还清）+ 滚动视口裁剪（clipChildren 已具备）。Table 是首个数据密集型控件，可能撞 I5 keyed 动态列表（forEach）+ 滚动态边界。需 oracle **新开 session** 产批 4 契约（ora-1/ora-2/ora-3 上下文均已用尽，不复用）。

### 控件迁移可复用范本（批 4 照抄）
- 控件范本：`SceneButton`（无值）/ `SceneCheckbox`/`SceneToggle`（R7 受控双向）/ `SceneRadioGroup`/`SceneSegmented`（R8 多选项单选）/ `SceneBreadcrumb`（纯展示+回调）/ `SceneSlider`（R7 连续版 + 拖拽 pointerCapture + 瞬态 signal）/ `SceneTextInput`（R9 受控文本 + 声明式 caret 子节点）。
- 测试范本：`SceneSliderTest`（受控瞬态命门 + 命中区=视觉区回归 + 值像素映射）/ `SceneTextInputTest`（R9 受控不自改 + caret ROW 自然定位 + 行为等价红线）。
- demo 范本：`SceneControlsHostWidget`（已含 Checkbox/Toggle/Radio/Segmented/Breadcrumb/Slider/TextInput×2）+ `SceneControlsDemoScreen`，入口 `UiTestMatrixRegistry` + `UiTestSampleVisualFactory` 复用 VIS-SCENE-002。
- API 表面：`SceneRuntime`（mount/bind/bindText/on/focusable/interactionState/requestFocus/requestPointerCapture/show/forEach）；`SceneInteractionState`（hovered/pressed/focused 三只读 signal，focused 真写入由 FocusManager 驱动，已亲验非占位）。
- caret/字符级度量缺口（批 4 不涉及，档位 B 才需）：scene 无字符级前缀宽 API，回填走方案 γ（SceneRuntime 注入 measurer + 薄委托 measureTextWidth），见 DECISION-20260621。

### 待用户验收（非阻塞，沙箱无 GUI）
- scene Phase 1-3 + C 任务 transform + 批 1/2/3 全控件真机帧率/上屏验收待用户跑：入口 `/qzuilib test` → SCENE_DEMO 组 →「打开 Scene demo 页」/「打开 Scene 控件 demo」（批 1/2/3 控件均已加进同一 host）。真机后派子代理查 `run/client/logs/fml-client-latest.log`，`club.heiqi.uilib` 应零异常。验收重点：Slider 拖拽跟手/松手回落、TextInput 输入/退格/caret 跟随文本末尾/密码掩码、Breadcrumb 段宽非等宽字体下有无裁剪。

### 本会话纪律切换（用户指令，长期沿用）
- 用户无法实时拍板，**决策点改派 @oracle 自动裁决并采纳**，不再用 question 等待用户响应。需架构裁决时直接派 oracle，新开 session（既有 ora session 上下文均已用尽）。
- 用户偏好：对旧代码范式不持兼容意见，优先改造到靠近核心宪章/信条的范式，接受高风险改动但要保证最终符合宪章信条。
- **派发纪律（本会话强化）**：决策/侦察/实现一律走前台 task 派发，不空停、不只发文字不调用工具；orchestrator 不亲自全量读代码做侦察，派 explorer。

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- 编译/测试优先走 JetBrains MCP（见 `AGENTS.md` 第 8 节，shell gradle 曾卡死）；仍需 shell 时用 `$env:GRADLE_USER_HOME="D:\.MyApps\.ENV\gradle-home"; ./gradlew.bat ...`（.MyApps 双 p）。**注意：jetbrainsBuildProject 因 gtnhconvention 插件配置阶段需联网拉 GitHub manifest，沙箱 DNS 污染时会失败；编译验证优先用 jetbrainsBuildProject（IDE 内置 classpath 不走该插件配置）。**
- PowerShell 不支持 `&&` 与 heredoc（`<<`）；链式用 `;` 或 `cmd1; if ($?) { cmd2 }`；提交信息必须先 write 到临时文件再 `git commit -F <文件>`。
- 提交/合并用 UTF8 无 BOM；PowerShell 5.1 `Set-Content -Encoding utf8` 会写 BOM，必须用 .NET `[System.IO.File]::WriteAllText($f,$c,(New-Object System.Text.UTF8Encoding $false))` 写无 BOM。
- git 操作必须如实读取 EXIT 码 + 真实 stdout，不预判、不编造命令成功输出。
- LSP 报 `java.lang.Record cannot be resolved` 是 IDE 用 Java8 解析 Jabel record 的固有误报，jetbrainsBuildProject 编译通过即可忽略。

## 更新规则
- 只保留最近一次交接的当前真相，历史交接直接覆盖，不按次数累积。
- 施工逐行细节沉淀到决策/审查/错误文档，本文件只留「现状 + 下一步 + 边界」。
