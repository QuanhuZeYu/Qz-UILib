# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-27 第 122 次）—— deepwork 阶段 2 第二批完成（ForkJoinPool 引入）

### 一句话现状

deepwork 阶段 2 第二批（引入 ForkJoinPool，高风险）**全部完成**。
步骤 2.3（SceneParallelExecutor 单例 pool + 回退开关）+ 2.4（layout
并行化）+ 2.5（paint 并行化）已落地，determinism 闸门测试绿（并行
开/关结果完全一致，含混合 fork/串行场景）。P0 z-order 错乱 bug 被
reviewer 抓出并修复（两遍遍历保证 appendAll 按 children 顺序）。
NORTH_STAR §10.2 已补注。**PARALLEL_ENABLED 默认 false，真机校准后
才开**。下一步第三批真机实测（交用户跑三组对照）。当前在
`add/b6-fbo-perf-baseline` 分支，16 个 commit。

### 本会话累计 commit（按时间序）

| commit | 内容 |
|--------|------|
| `96c3d884` | [Add]: 步骤 2.0——measurer 并发底座加固 + 宪章 I6/I7 并行强化补注 |
| `ca73ce8c` | [Add]: 步骤 2.1——subtreeNodeCount 增量维护（fork 阈值铺路）|
| `c0c9f1fe` | [Refactor]: 步骤 2.2——几何变化传播改返回值归并+join 点点亮（D1 命门）|
| `8fecc48f` | [Docs]: 第 121 次交接——第一批完成 |
| `2ef214dc` | [Add]: 步骤 2.3+2.4——SceneParallelExecutor 单例 pool + layout 并行化 |
| `dcbed405` | [Add]: 步骤 2.5——paint 并行化（paintSubtree 产独立 plan + appendAll）|
| `a4406d5d` | [Fix]: P0 z-order 错乱——两遍遍历保证 appendAll 按 children 顺序（paint+layout）|
| `212d4978` | [Docs]: 宪章 §10.2 新增——阶段 2 子树并行 fork-join 已落地 |

### 本会话任务链路

**阶段 E：阶段 2 计划审查 + 行业调研（上轮会话延续，纯研究）**
1. 读三份必读文件恢复进度
2. explorer 侦察 7 项设计硬约束 + 补查 TextLayoutService 缓存并发安全
3. oracle 起草阶段 2 计划 + 3 处关键纠偏（measurer「全无锁」过度承诺 +
   D1 问题错置 + 双缓冲与子树并行解耦）
4. 用户指示「派 LIB 研究行业解法，再让 Oracle 推荐」
5. librarian 行业调研（Servo/Blink/WebKit/Flutter/Bevy/Rayon/Unity/Unreal）
   → 方案 1 双背书（Servo + Bevy），方案 2 零先例
6. oracle 基于调研给最终推荐（6 拍板项 + 3 宪章修订项）
7. explorer 补查 3 个未验证项：reload 命门已解除（线程守卫硬拦 worker +
   scene 管线零 reload 触发）、稳态无锁确认、PaintCommand 不可变确认

**阶段 F：用户拍板 + 阶段 2 第一批实施**
8. 用户全盘采纳：D1 方案1 / D2 方案A 阈值64 / D3 专用常驻 pool cores-1 /
   D4 双缓冲预留 / D5 宪章全盘修订 / worker render-scoped 不变量登记
9. 步骤 2.0（fixer→reviewer 通过）：
   - measuredTextNodes 换 ConcurrentHashMap.newKeySet()
   - SceneNode 类注释锚定禁止重写 equals/hashCode
   - ConcurrentMeasureWidthIdempotenceTest（N 线程并发 measureWidth 幂等
     断言，冷启动 miss + 稳态命中两组）
   - NORTH_STAR 宪章修订落地（I6 精确化 + I7 并行强化 + worker render-scoped）
10. 步骤 2.1（fixer→reviewer 有条件通过→fixer 补整改→通过）：
    - SceneNode 加 cachedSubtreeNodeCount + subtreeCountDirty
    - markSubtreeCountDirty() 沿祖先链冒泡（遇已点亮即停，O(深度)）
    - 4 结构变化入口调用（appendChild/removeChild/insertBefore/applyChildReconcile）
    - layoutInternal 后序顺带重算（dirty 时重算，干净时跳过守 I7）
    - SubtreeNodeCountTest 10 用例
11. 步骤 2.2（explorer 精确侦察→oracle 裁决返回值形态与时序→fixer→reviewer 通过）：
    - SceneNode 新增 4 方法（拆 self 置位与 bubble）
    - SceneLayoutEngine 新增 2 record（SubtreeLayoutResult + SelfBubbleSignal）
    - performLayout 步骤 D 改置位不 bubble，**步骤 C 零改动**
    - layoutInternal 子循环 join 点补 child bubble
    - 纯重构单线程逐位等价，D1 命门消除

**阶段 G：阶段 2 第二批实施（引入 ForkJoinPool，高风险）**
12. 步骤 2.3（fixer→reviewer 通过）：SceneParallelExecutor 单例 pool
    （static final ForkJoinPool，cores-1，scene-layout-worker-N 命名，
    PARALLEL_ENABLED 默认 false 回退开关）
13. 步骤 2.4（explorer 侦察 2.4 事实基础→fixer→主 Agent 核实 D1 命门）：
    - LayoutSubtreeTask + ParallelLayoutOutcome（worker 局部探针片段）
    - layoutInternal 子循环 fork-join（达阈值 child fork，join 点归并探针+补 bubble）
    - layout() 入口集成开关 + 整树门槛 256 + 单子树门槛 64
    - determinism 闸门测试 5 用例绿
14. 步骤 2.5（fixer→reviewer 抓出 P0 bug→fixer 修复）：
    - PaintPlan 新增 appendAll（纯搬运不平移 offset）
    - paintNode → paintSubtree（产独立 localPlan，子循环 fork-join）
    - paint() 入口集成开关 + 整树门槛
    - determinism 闸门测试 5 用例绿
    - **P0 修复**：reviewer 抓出混合 fork/串行 children 时 appendAll 顺序错乱
      （z-order 失序），两遍遍历修复（第一遍 fork，第二遍按 children 顺序
      join+appendAll）。layout 2.4 同款 LinkedHashSet 潜在顺序问题一并修复

### 关键架构决策（详见决策文档）

- **DECISION-20260627-display-list-contract-line.md**：Display List 契约线
  阶段 1 落地 + 并发框架方向。含完整阶段路径、行业调研背书、P0 命门分析
- **NORTH_STAR 三处补注**：信条六（两阶段已落地）+ I6（并行强化 + measurer
  缺口已还清，**本轮 oracle 提议精确化见下**）+ 第 10 节新增 10.1
  （已落地的契约线切分，双缓冲仍标预留）
- **来源 A vs 来源 B 裁决**：epoch 外置用来源 A（保留 measuredTextNodes 集合
  + epoch 下放节点），来源 B（侵入式链表）业界无先例否决

### Oracle 阶段 2 最终推荐（基于行业调研，待用户拍板）

**三个拍板项最终推荐**：
- **D1 脏标记冒泡并发 → 方案 1**（返回值归并 + join 点串行点亮）：
  Servo + Bevy 双背书，方案 2 业界零先例。**Oracle 关键纠正**：阶段 2
  并行边界内不写脏标记冒泡（冒泡在 flush 后、layout 前串行完成），
  真正并发写是 performLayout 内的 markGeometryDirty/markSelfPaint 向上冒泡。
  方案 1 落点 = worker 内不即时冒泡，返回值携带几何变化信号，父在 join
  点按子返回值补点亮（复用现有 anyChildGeometryChanged 机制，:238,247）
- **D2 fork 阈值 → 方案 A**（subtreeNodeCount 精确判断）：
  Rayon/Java 用精确规模；Servo 用 chunk size 但场景不同（SceneNode 树高度
  不均衡）。阈值：**整树 <256 全串行，单子树 ≥64 才 fork**（起步值，真机校准）
  subtreeNodeCount 增量维护：复用现有脏标记冒泡 O(深度) + layoutInternal
  后序遍历顺带重算（零额外遍历成本）
- **D3 线程池 → 专用常驻 ForkJoinPool，cores-1，进程级单例**：
  新增 SceneParallelExecutor 持 static final ForkJoinPool，懒初始化。
  常驻是行业共识（Servo/Flutter/Unity/Unreal/Rayon）；cores-1 游戏引擎强
  共识（主线程跑 MC 循环 + GL replay）；专用 pool（Servo layout 单独池）
  **不挂 AbstractSceneHostWidget**（devtools demo 宿主，多实例多 pool）

**D4/D5/D6 最终推荐**：
- **D4 双缓冲**：确认阶段 2 不做，预留阶段 3/4。同步 fork-join 无双缓冲需求
- **D5 宪章修订**（重大架构变更，需用户拍板）：
  - **I6 measurer 表述精确化**（Oracle 关键纠正）：从「全无锁」降级为
    「稳态无锁读 + 幂等填充假设（待测试验证）+ reload 不与 worker 并发前提」
  - I7 新增「并行强化」条目（并行不改变跳过语义，脏标记并行前已定稿只读，
    几何变化经返回值归并 join 点串行点亮）
  - §10.1 补注阶段 2 已落地子树并行 fork-join（帧内），双缓冲仍预留阶段 3
- **D6 真机实测**（必交用户跑，沙箱无 GUI）：三组对照——
  小树负优化验证 / 大树并行收益（500-2000 节点）/ 阈值扫描 {32,64,128,256}

### Oracle 关键纠正（高于"让方案看起来顺"）

1. **D1 问题被原表述错置**：阶段 2 并行边界内**不写脏标记冒泡**
   （冒泡在 flush 后 layout 前串行完成）。真正的并发写是 performLayout
   内的 markGeometryDirty/markSelfPaint 向上冒泡。方案 1 正确，但落点是
   「几何变化传播」而非「脏标记冒泡」
2. **I6「全无锁」是过度承诺**：决策文档自己登记了"幂等性假设未经压测验证"
   （上轮交接:126），但 I6 补注却写成既成事实。Oracle 把它精确化为
   「假设 + 待测试」+ 新增「reload 不与 worker 并发」前提

### Oracle 标注的未验证项（下个会话启动前必须补查）

- 【需补查】TextLayoutService.measureCodepointWidth / FontMatcher.writeMatchedFont
  / GlyphRuntimeTables 当前实现（本轮 oracle 未读 ui.text/ui.font 包），
  I6 精确化的「稳态无锁读」依赖这些实际锁状态
- 【需补查，隐藏命门】**字体 reload 触发点是否确实在主线程 render 循环之外**
  （I6 精确化新增的「reload 不与 worker 并发」前提依赖此）—— 这是阶段 2
  并行安全的隐藏命门，必须在阶段 2 第一批 2.0 步前确认
- 【推测】PaintCommand.translatedBy 返回新对象（不可变）—— 从 I6 补注
  「PaintCommand 全 final」推测成立，本轮未读 PaintCommand.java，并行 paint
  拼接依赖此不可变性，2.5 步前确认

### 阶段 2 实施路径（Oracle 最终建议，待用户拍板后启动）

**第一批：并行前置配套（低风险，不引入线程，约 1 周）**
- 步骤 2.0：measuredTextNodes 换 ConcurrentHashMap.newKeySet() + 幂等断言测试
  （N 线程并发 measureWidth 同串结果全等，含冷启动 miss 路径）
- 步骤 2.1：subtreeNodeCount 增量维护（SceneNode 加 cachedSubtreeNodeCount
  + subtreeCountDirty，冒泡复用现有祖先链，layoutInternal 后序顺带重算）
- 步骤 2.2：几何变化传播改「返回值归并 + join 点点亮」（D1 方案 1 落地，
  worker 内不即时 markGeometryDirty/markSelfPaint，改返回值携带信号，
  父 join 点补点亮）—— **此步仍单线程做并验证**，确保挪动冒泡时机
  不破坏 I7 现有测试
- 第一批结束：代码仍串行，但已具备并行所需的全部无锁地基。全量跑测试
  + 提交 + 真机验收一次后进第二批

**第二批：引入并行（高风险，带回退开关 PARALLEL_ENABLED，约 1.5-3 周）**
- 步骤 2.3：SceneParallelExecutor 单例 pool（static final ForkJoinPool，
  cores-1，带全局开关 PARALLEL_ENABLED 默认 false，出问题一键回退串行）
- 步骤 2.4：layout 并行化（layoutInternal 子循环：child.cachedSubtreeNodeCount
  >= FORK_THRESHOLD && children.size() >= 2 的子 fork 为 RecursiveTask，
  探针改线程安全累加：worker 各产 LayoutResult 片段，join 点归并）
- 步骤 2.5：paint 并行化（每个 worker 产独立子 PaintPlan，join 点按 DFS
  前序确定性顺序拼接；PUSH/POP 边界命令整段落在同一 worker 内；
  PaintPlan 新增 appendAll API）—— **比 layout 更棘手，单列**
- 验收：并行开/关两种模式下 SceneLayoutEngineTest/ScenePaintEngineTest
  全套结果完全一致（determinism 闸门）

**第三批：真机校准（交用户）**
- 步骤 2.6：按 D6 三组对照真机跑 → 用户回贴 → Oracle 校准 FORK_THRESHOLD
  → 决定 PARALLEL_ENABLED 默认值

### 验证状态

- compileJava + compileTestJava 通过
- **全套测试零回归**（PARALLEL_ENABLED 默认 false 串行路径不变）
- determinism 闸门测试全绿：
  - layout：parallelOnOffProducesSameResult + mixedForkSerialChildrenPreservesConstraintRelayoutedOrder
  - paint：parallelOnOffProducesSameCommandSequence + mixedForkSerialChildrenPreservesZOrder
- 幂等断言测试绿（measurer 并发安全）
- SubtreeNodeCountTest 10 用例绿
- 重点脏标记测试通过（D1 命门逐位等价）
- reviewer 审核全部通过（2.5 P0 bug 抓出并修复）
- **真机帧率实测未跑**（沙箱无 GUI，第三批必跑）

### 剩余债务全量状态

**真未还（代码债）**：
- B6 FBO I7 合成阶段偏离：FBO 纹理每帧重栅格化，待真机性能基线数据决定是否启动
- B6 hit-test 对偶偏离：SceneHitTester 对 transform 零感知，待交互需求触发
- UiPerformanceMonitor 单例 scope 错配（远期，采集深埋 Widget.java 硬编码 getInstance）
- A1 effect 内 set 慢一帧：残留语义待确认
- B4 COLUMN fill O(n²)：缓做
- chrome 主题层：P2 大工程未立项
- Phase 5 旧栈退役：阻塞中

**有意边界（非债）**：
- B6 FBO 全屏尺寸：批 3 局部 FBO 优化待性能暴露后启动
- B6 降级路径：FBO 不可用时保留 clip 放弃 transform（best-effort）
- 圆角 clip+transform：stencil+T 交互未真机验证
- SceneTextArea 功能限制 / DataTable 嵌入式深色槽

**真机验收待办（非 B6）**：
- B8 滚动后 hover 切换 / B2 TextArea 压力测试页帧率 / Breadcrumb ROOT_GAP
- Phase 1-3 真机帧率/上屏 / 改名后真机 GL 路径
- B6 FBO 性能基线页三档对照真机跑 + fps 拐点记录

### 当前分支与工作区

- 当前工作分支：`add/b6-fbo-perf-baseline`，16 个 commit
- 工作区干净，第二批完成

### 下个会话任务表

1. **第三批：真机校准（交用户跑，沙箱无 GUI）**
   - 步骤 2.6：三组对照真机跑
     - 小树负优化验证（<64 节点，全串行 vs 强行 fork，fps 对比）
     - 大树并行收益（500-2000 节点，串行 vs fork 阈值=64，fps + 加速比）
     - 阈值扫描 {32,64,128,256} 找帧率拐点
   - 用户回贴 → Oracle 校准 FORK_THRESHOLD/WHOLE_TREE_THRESHOLD
   - 决定 PARALLEL_ENABLED 默认值（true or false）
   - FrameRateProbe 已就绪，SceneStressTestHostWidget 可复用
2. **合并准备**：16 commit 待真机验证后决定合回 4.0（--no-ff）
3. **B6 FBO 性能基线真机实测**（可选，用户跑 /qzuilib scene_test →
   "FBO Perf Baseline" 三档对照）
4. **B6 hit-test 对偶 / A1 残留确认**（可选，待触发）

### 已知边界

- deepwork 进度文件：`.slim/deepwork/concurrent-framework.md`（必读，含完整
  Oracle 阶段 2 计划审查结论 + 行业调研背书 + 第一批实施记录）
- 决策文档：`docs/记忆/决策/DECISION-20260627-display-list-contract-line.md`
- NORTH_STAR 宪章修订已落地：I6 精确化（稳态无锁 + miss 两处锁 + 幂等已验证
  + reload 不与 worker 并发）+ I7 并行强化（不改变跳过语义，返回值归并 join 点
  串行点亮）+ worker render-scoped 不变量（render 内 fork、返回前 join）
- FBO 性能基线页：`/qzuilib scene_test` → "FBO Perf Baseline"
- FrameRateProbe：`ui/diagnostic/FrameRateProbe.java`，组合式帧率探针
- measurer 调用路径：稳态无锁（DCL → final → volatile 表读），miss 路径
  有 DerivedFontCache + CodepointTextCache 两处 synchronized（预热消除）
- 阶段 2 并行边界内不写脏标记冒泡（冒泡在 flush 后 layout 前串行完成），
  真正并发写是 performLayout 内 markGeometryDirty/markSelfPaint 向上冒泡
  ——**已由步骤 2.2 改造消除**（self 置位留 worker 内，bubble 延迟到父 join 点）
- TextLayoutService.getStringWidth 缓存：32 位 float[] direct-index，
  并发正确性安全，偶发重复度量（性能损耗非正确性错误）
- SceneNode equals 引用相等（未重写，类注释已锚定禁止重写）
- 字体 reload 只能主线程执行（线程守卫硬拦 worker），scene 管线零 reload
  触发路径，worker render-scoped 前提下 reload 与 worker 严格时序隔离
- **第二批已完成**：ForkJoinPool 已引入，layout/paint 并行化已落地，
  determinism 闸门测试绿（含混合 fork/串行场景），PARALLEL_ENABLED
  默认 false 待真机校准后开
- fork 阈值（用户拍板）：整树<256 全串行 + 单子树≥64 才 fork（起步值，真机校准）
- **P0 bug 教训**：paint/layout 子循环混合 fork/串行 children 时，
  appendAll/探针归并必须两遍遍历（第一遍 fork，第二遍按 children 顺序
  join+appendAll），否则 z-order/LinkedHashSet 顺序错乱。reviewer 抓出
  fixer 照搬 layout 模式未识别 paint 顺序敏感性
- 第三批关键纪律：worker render-scoped（不跨帧存活）+ 真机三组对照
  （小树负优化/大树收益/阈值扫描）→ 校准阈值 → 决定 PARALLEL_ENABLED 默认值

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑，避免并行 Gradle 竞争 build/ 目录。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，
  改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能
  登记到正确 Owner。
- token 实际值以 SceneChromeTokens.java 源码为准，不依赖过期文档清单。
- oracle 裁决架构方案后，如方案涉及长期可引用的设计决策，建议补写
  `docs/记忆/决策/` 文档沉淀。
- I11 逃生舱②扩张纪律：扩之前证明它确实是一类新的权威交互真值，
  而非可以用现有机制表达的东西（B8 验证了 hover 重算不需要扩②）。