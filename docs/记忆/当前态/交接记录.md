# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-26 第 114 次）—— scene 新栈技术债深度清理合回 4.0

### 一句话现状

scene 新栈技术债深度清理完成。本会话从 chrome 收口开始，
经全量债务盘点，完成 N1/B5/B8/A6 修复 + B6/B4 推迟决策 + B2 压力测试页 +
oracle 审核原文落盘 + 3 项过期文档回写。
**真未还的代码债仅剩 A1 残留确认 + B4（缓做）+ B6（推迟）+
chrome 主题层（未立项）+ Phase 5（阻塞）**。

### 本会话累计 commit（已合回 4.0，按时间序）

| commit | 内容 |
|--------|------|
| `fae6b53e` | [Refactor]: scene chrome 收口第2-4批（ObjectField/SimpleList/DataTable token 化） |
| `c5acdeb8` | [Refactor]: Props Builder P1 收尾 + 跨控件 chrome 几何兜底第1批 |
| `1e5d4e55` | [Refactor]: scene chrome 颜色通道扩展 + 几何缺口收口（DANGER/WARNING/BUTTON_HEIGHT） |
| `d5d6d3d9` | [Docs]: 落盘 oracle 架构审核原文 + 回写 3 项过期文档条目 |
| `23bf3a94` | [Fix]: N1 capture 期隐式聚焦守卫 + B5 paint LEFT 惰性测量 |
| `534dcf65` | [Docs+Add]: 保存 B6 FBO 方案研究信息 + 扩展 B2 TextArea 压力测试页 |
| `16dd6d56` | [Fix+Docs]: A6 bind impact Javadoc 正名 + B8 滚动后 hover 滞留修复 + B4 缓做决策 |

### 本会话第三次任务（A6 + B8 + B4 决策）

**A6 bind impact Javadoc 正名**（用户拍板保留参数）：
- oracle 发现实际 123 处调用点（生产 104 + 测试 19）
- impact 是「声明式失效意图标注，I4 审查锚点」：调用方声明意图 vs setter 实际行为双轨交叉核对
- 改 Javadoc 措辞，零代码逻辑改动

**B8 滚动后 hover 滞留修复**（用户拍板方案 Y'，不扩 I11）：
- oracle 二次评估关键洞察：hover 重算是 Router 内部职责（与 MOVE 同源），不是外部 handler 请求
- 方案 Y'：Router 加 `reconcileHoverAfterScroll` 内部协议方法（handler 碰不到，与 route 同级）
- host render 在 flush + layout 之后调用，用末次指针坐标重做 hit-test + hover 切换
- 关键时序：route → flush（scrollOffsetY 生效）→ layout → reconcileHoverAfterScroll
- I11 不扩：方法不在 EventContext 上，handler 碰不到
- 4 个回归测试（滚动后 hover 切换 / 无 SCROLL no-op / 标记清零 / 移出整树）

**B4 COLUMN fill O(n²) 缓做**（用户拍板缓做 + 留原因）：
- 决策文档：`docs/记忆/决策/DECISION-20260626-b4-column-fill-on2-deferred.md`
- 缓做原因：99% 干净帧短路，仅 COLUMN + fill 子 + 约束真变帧触发，非热路径
- 未来改进：首子短路 + 预计算缓存，O(n²)→O(n)，等价化简
- 触发条件：大 n COLUMN fill 容器频繁改约束场景出现

### 验证状态

- jetbrainsBuildProject 全量编译通过
- 串行 gradlew test 49 用例 0 失败
  （SceneRouterScrollHoverTest 4 / SceneRouterInteractionTest 15 /
  SceneInputRouterCaptureCancelTest 18 / SceneRuntimeTest 12）
- reviewer session 失效空返回，主 Agent 直接审核关键点通过
- **待真机验收**：
  - B8 滚动后 hover 切换视觉态
  - B2 TextArea 压力测试页帧率（500+ 行滚动/编辑）
  - Breadcrumb ROOT_GAP 间距 6→4px

### 剩余债务全量状态（2026-06-26 explorer 盘点 + 本会话清理后）

**已清零**：
- chrome 收口全部完成（第2-5批 + 颜色通道扩展）
- N1 capture 守卫 ✓
- B5 paint LEFT 惰性测量 ✓
- B8 滚动后 hover 滞留 ✓
- A6 bind impact 正名 ✓
- B7 caret 高度 ✓（隐式还清）
- I6 replayer ui.style ✓
- B1 绝对坐标统一 ✓
- B3 clip 口径统一 ✓
- B2 文本几何 O(N²)→O(N) ✓（代码已修，待真机帧率）
- oracle 审核原文落盘 ✓
- 3 项过期文档回写 ✓

**真未还（代码债）**：
- A1 effect 内 set 慢一帧：大部分被 ReactiveScheduler 不动点覆盖，残留语义待原报告确认
- B4 COLUMN fill O(n²)：缓做，决策文档已落盘
- B6 transform+clip 坐标错位：推迟，决策文档已落盘
- chrome 主题层：P2 大工程未立项
- Phase 5 旧栈退役：阻塞中（依赖 Phase 4 完成）
- N2 capturedNode onCleanup：随 capture 生产化
- I4c-O1 跨栈光标单例：Phase 5 自然收口

**有意边界（非债）**：
- SceneTextArea 功能限制（选区/剪贴板/IME/caret 闪烁/自动换行/横向滚动/caret 跟随）
- DataTable 嵌入式深色槽 12 个保留+注释
- NORTH_STAR 两条偏离（COLUMN 多 fill 子 / transform 矩阵已完整化但登记未更新）

**真机验收待办**：
- B8 滚动后 hover 切换
- B2 TextArea 压力测试页帧率
- Breadcrumb ROOT_GAP 6→4px
- Phase 1-3 真机帧率/上屏（composite 帧零重排）
- Select/Slider/TextInput 滚动内点击
- onTextEvent emoji 路径

### 当前分支与工作区

- 当前工作分支：`4.0`，本会话所有合入已完成，工作区干净
- 开发分支均已合并删除

### 下个会话任务表

1. **真机验收**：B8 hover + B2 帧率 + Breadcrumb GAP（用户跑）
2. **主线拍板**：用户已选「暂不起主线」，下个会话先问是否启动新切片
3. **NORTH_STAR 偏离登记更新**：transform 矩阵已完整化（2026-06-20 失同步）+
  补登记 B6 主动禁止偏离（待用户确认宪章措辞）
4. **A1 残留确认**：需翻会话记录或重新核 ReactiveScheduler.flush 收敛终止条件
5. **可选**：reviewer P2-1 建议补 SceneStateColors.errorRowBackground 独立查表测试

### 已知边界

- B6 FBO 方案完整评估存于 `docs/记忆/决策/DECISION-20260626-b6-transform-clip-fbo-deferred.md`，
  待真实 rotate+矩形clip 需求触发后单独开专栏
- B4 缓做原因与改进点存于 `docs/记忆/决策/DECISION-20260626-b4-column-fill-on2-deferred.md`
- oracle 审核原文存于 `docs/开发者文档/reviews/REVIEW-20260625-scene-oracle-architecture-audit.md`
- I11 逃生舱现状：三类（只读测量/受控命令/宿主桥接）正交且各自物理必需，
  ②的三个命令无冗余。B8 验证了「扩之前证明它是一类新的权威交互真值」纪律有效
- 并行 Gradle build 竞争误报已立项 ERROR-20260624 / 20260518
- token 实际值以 SceneChromeTokens.java 源码为准

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑，避免并行 Gradle 竞争 build/ 目录。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，
  改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能
  登记到正确 Owner。
- token 实际值以 SceneChromeTokens.java 源码为准，不依赖过期文档清单。
- oracle 裁决架构方案后，如方案涉及长期可引用的设计决策，建议补写
  `docs/记忆/决策/` 文档沉淀。
- I11 逃生舱②扩张纪律：扩之前证明它确实是一类新的权威交互真值，
  而非可以用现有机制表达的东西（B8 验证了 hover 重算不需要扩②）。