# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-21 第 46 次）—— 还清深层 fill 偏离 + 批 2 三控件完成，批 3 待开工

### 一句话现状
Phase 4 控件层重建：**深层 fill 高度约束 top-down 下传（还清偏离1主条）已完成提交，批 2（Radio/Segmented/Breadcrumb 迁移，确立 R8 多选项单选受控契约）已完成提交**。工作树对源码干净，下一会话可直接开工**批 3（TextInput/Slider）**。

### 当前分支与已提交基线
- 分支 `refactor/scene-phase4-control-layer`（从 `4.0` 切出）。最近提交（新→旧）：
  - `d1ec1feb` 批 2 完成态记忆同步
  - `93469b05` **批 2 收尾**：修 SceneRadioGroup 重复 import + 补 Breadcrumb 容器 shrink-to-fit 偏离登记
  - `a6288dfe` **批 2** RadioGroup/Segmented/Breadcrumb 三控件迁移（fixer 自提）
  - `98a655c5` **深层 fill** 高度约束 top-down 下传（还清偏离1主条 + 修两角落缺陷 + COLUMN YAGNI 登记）
  - 更早：`89bb9e6d` 批 1 Checkbox/Toggle + `4721b6a5` C 任务 transform + `237e8a33` 固定尺寸地基 + `5cb593fd` 批1前置②字体度量 + `9fca7d38` 偏离2修复 + `a33d80d8` 第0段地基
- 工作树对源码干净（仅未跟踪 `.slim/`、`opencode.json`，永不提交）。

### 本会话两大成果（均已主 Agent 独立验真，非仅信 fixer/oracle 报告）

**① 深层 fill 高度约束 top-down 下传（还清偏离1主条，方案甲）**
- oracle 裁决暂停批 2 横向铺量，优先还清布局引擎地基债（批 4 Table/批 5 编辑区填满强依赖深层 fill，现在是改布局核心最干净窗口）。关键澄清：脏标记传播（I7 管，向上冒）与约束传播（布局输入）正交，约束变化触发重算靠「布局遍历局部比较缓存约束」而非向下标脏。
- 落地：`SceneNode` per-node `lastConstraints` 快照（不参与脏冒泡）；`SceneLayoutEngine` 两段式闸门 `childConstraintsWouldChange` + `selfConsumesConstraint`；`buildChildConstraints` 仅 ROW 交叉轴下传、COLUMN 主轴恒 UNCONSTRAINED；`priorKnownInnerHeight` 不回看子 cache（无循环依赖）。同轮修两角落缺陷：约束失高深层叶 fill 回退 shrink（防陈旧）、fill+大 preferredHeight 下传口径对齐（防留白）。
- 三命门独立验真：grep 确认新增代码区零 markSelfLayout/markGeometryDirty；priorKnownInnerHeight 不回看子 cache；COLUMN 禁主轴下传正确。`SceneLayoutEngineTest` 47 测试（4 主干+5 边界反证）。
- 偏离处置（方案甲）：偏离表 2026-06-17 深层 fill 主条改「✅ 已还清」+ 新增 2026-06-20 COLUMN 主轴 fill YAGNI 登记（javadoc 固化防误修）。

**② 批 2 Radio/Segmented/Breadcrumb 迁移（确立 R8）**
- 工作流：exp-1+ora-1 并行侦察/产契约 → 主 Agent 用 explorer 事实校验 oracle 契约 → 代 fixer 预读 SceneInteractionState 确认 focus signal → fix-1 串行实现 → 主 Agent 四命门独立验真 → ora-2 独立代码审查。
- 确立契约 **R8 多选项单选受控零状态**（R7 从 boolean 推广到 index）：选中由外部 `ReadableSignal<Integer> selectedIndex` 唯一驱动，激活只 `onSelect.accept(targetIndex)`，控件绝不自改。Radio/Segmented Props 真同构。package-info 红线 7→8 补 R8。
- `SceneRadioGroup`（VERTICAL，dot 显隐用透明背景纯 PAINT 零重排）、`SceneSegmented`（固定段宽替代 flex-grow，R6 段穿透权威落地）、`SceneBreadcrumb`（纯展示+回调，不用 forEach 绕开 I5）。
- 四命门独立验真：I6 隔离（三文件零平台/渲染 import）、R8 受控不自改（handler 只 onSelect/读 signal）、命中穿透+零重排（relayoutCount==0）、R8 红线写入。
- 测试 14 条（Radio 5 + Segmented 5 + Breadcrumb 4），scene 全套 342 绿，ScenePackageIsolationTest 2/2 守 I6。
- ora-2 审查收尾两项必做（已完成）：删 SceneRadioGroup 重复 import（jetbrainsBuildProject 零 problems 确认）；Breadcrumb segBtn 字符估算宽（`label.length()*9`）转 NORTH_STAR 偏离登记——oracle 论证它与 Segmented 固定段宽不同性质（是真实文本宽的估算替代，有文字裁剪风险），根因 scene 容器无 shrink-to-fit + 控件层访问不到 SceneTextMeasurer，属隐性偏离须显式登记。

### 下一步（批 3）
- **批 3：TextInput/Slider 迁移**（按 8 批路线图）。首次涉及文本输入态（光标/选区/IME）与拖拽连续值（Slider thumb 拖动），比批 1-2 复杂度高。
- 需 oracle 产批 3 迁移契约时**新开 session**（旧 ora-1/ora-2 session 上下文已用过，不复用）。
- 批 3 可能暴露新地基缺口：文本输入需 caret/选区渲染、键盘文本事件路由；Slider 拖拽需 pointer capture（输入半环已预留接口但 Phase 1 未实现，见 NORTH_STAR §4.5）。开工前 oracle 须先评估前置阻塞。

### 控件迁移可复用范本（批 3 照抄）
- 控件范本：`SceneButton`（无值）/ `SceneCheckbox`/`SceneToggle`（R7 受控双向）/ `SceneRadioGroup`/`SceneSegmented`（R8 多选项单选）/ `SceneBreadcrumb`（纯展示+回调）。
- 测试范本：`SceneRadioGroupTest`（受控不自改反证 + absCenter 沿父链累加算绝对坐标 + 零重排 relayoutCount==0 + 命中穿透 + 键盘/方向键）。
- demo 范本：`SceneControlsHostWidget` + `SceneControlsDemoScreen`，入口走 `UiTestMatrixRegistry` + `UiTestSampleVisualFactory`，复用 VIS-SCENE-002。
- API 表面：`SceneRuntime`（mount/bind/bindText/on/focusable/interactionState/requestFocus/show/forEach）；`SceneInteractionState`（hovered/pressed/focused 三只读 signal，focused 真写入由 FocusManager.requestFocus 驱动）。

### 待用户验收（非阻塞，沙箱无 GUI）
- scene Phase 1-3 + C 任务 transform + 批 1/批 2 控件真机帧率/上屏验收待用户跑：入口 `/qzuilib test` → SCENE_DEMO 组 →「打开 Scene demo 页」/「打开 Scene 控件 demo」（批 2 三控件已加进同一 host）。真机后派子代理查 `run/client/logs/fml-client-latest.log`，`club.heiqi.uilib` 应零异常。

### 本会话纪律切换（用户指令，长期沿用）
- 用户无法实时拍板，**决策点改派 @oracle 自动裁决并采纳**，不再用 question 等待用户响应。批 3 起需架构裁决时直接派 oracle。
- 用户偏好：对旧代码范式不持兼容意见，优先改造到靠近核心宪章/信条的范式，接受高风险改动但要保证最终符合宪章信条。

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- 编译/测试优先走 JetBrains MCP（见 `AGENTS.md` 第 8 节，shell gradle 曾卡死）；仍需 shell 时用 `$env:GRADLE_USER_HOME="D:\.MyApps\.ENV\gradle-home"; ./gradlew.bat ...`（.MyApps 双 p）。**注意：jetbrainsBuildProject 因 gtnhconvention 插件配置阶段需联网拉 GitHub manifest，沙箱 DNS 污染时会失败；编译验证优先用 jetbrainsBuildProject（IDE 内置 classpath 不走该插件配置）。**
- PowerShell 不支持 `&&` 与 heredoc（`<<`）；链式用 `;` 或 `cmd1; if ($?) { cmd2 }`；提交信息必须先 write 到临时文件再 `git commit -F <文件>`。
- 提交/合并用 UTF8 无 BOM；PowerShell 5.1 `Set-Content -Encoding utf8` 会写 BOM，必须用 .NET `[System.IO.File]::WriteAllText($f,$c,(New-Object System.Text.UTF8Encoding $false))` 写无 BOM。
- git 操作必须如实读取 EXIT 码 + 真实 stdout，不预判、不编造命令成功输出。
- LSP 报 `java.lang.Record cannot be resolved` 是 IDE 用 Java8 解析 Jabel record 的固有误报，jetbrainsBuildProject 编译通过即可忽略。

## 更新规则
- 只保留最近一次交接的当前真相，历史交接直接覆盖，不按次数累积。
- 施工逐行细节沉淀到决策/审查/错误文档，本文件只留「现状 + 下一步 + 边界」。
