# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- **当前分支**：`add/qzuilib-test-modern-config-demo`
- **当前任务**：`/qzuilib test` 现代配置模板 demo 接入已完成。已新增 MODCFG 分组与 `VIS-MODCFG-001`，可从 test 页跳转到完整 `ModernConfigTemplateScreen` 12 入口 demo。
- **工作区状态**：代码实现已提交：`733ecec8`（规划）、`b24a2391`（功能 + 测试）。本次还需要提交文档收口（规划同步、矩阵文档、指令文档、错误记录、交接记录）。
- **验证状态**：已通过 `git diff --check`、`compileJava`、`compileTestJava`、`club.heiqi.uilib.internal.devtools.pages.*` 测试、`club.heiqi.uilib.config.*` + `club.heiqi.config.*` 全套 **25 套件 197 项全过**。`runClient21` 游戏内人工确认尚未执行。

### 本轮核心变更

- **MODCFG 分组**：`UiTestMatrixRegistry` 新增第 11 组 `MODCFG`，新增 `VIS-MODCFG-001` 人工确认样例；矩阵计数从 59/53 更新到 60/54，已接入自动/人工从 42/11 更新到 42/12。
- **demo 入口**：`UiTestSampleVisualFactory` 为 `VIS-MODCFG-001` 渲染「打开完整现代配置模板 demo 页」按钮、`club.heiqi.config` 模块状态牌和 12 入口预览卡片。
- **可选依赖边界**：`UiTestModernConfigDemoLauncher` 只做 `Class.forName` 检测和安全入口；`UiTestModernConfigDemoBridge` 只在检测通过后加载，负责构造 `MutableConfig`、14 个 `FieldSpec` 和 `ModernConfigTemplateScreen`。
- **demo 数据**：内存配置覆盖 STRING/NUMBER/BOOLEAN/CHOICE/LONG_TEXT/SIMPLE_LIST/TABLE/OBJECT/KEY_VALUE_MAP/PRESET_SELECTOR/RAW_EDITOR/ENHANCED_PICKER 12 个用户入口；demo `SaveHandler` 只 `markClean()`，不落盘。
- **测试覆盖**：新增 `UiTestModernConfigDemoBridgeTest` 纯 JVM 测试，覆盖模块检测、demo 配置结构和 FieldSpec 声明；`UiTestDocumentPageControllerTest` 覆盖 MODCFG 组页文本与矩阵计数。
- **顺手修复**：`UiTestControlsAssertionRunner` 的 `VIS-CTRL-005` select top-layer option 自动断言改为扫描真实 hit-test 命中，避免全量运行时 `selectOptionHit=false`。

### 修改文件

- 新增（main）：`UiTestModernConfigDemoLauncher.java`、`UiTestModernConfigDemoBridge.java`。
- 修改（main）：`UiTestMatrixRegistry.java`、`UiTestSampleVisualFactory.java`、`UiTestControlsAssertionRunner.java`。
- 新增（test）：`UiTestModernConfigDemoBridgeTest.java`。
- 修改（test）：`UiTestDocumentPageControllerTest.java`、`UiTestRuntimeHostVisualMatrixTest.java`。
- 新增（docs/errors）：`ERROR-20260614-uitest-top-layer-option-hit.md`。
- 修改（docs）：`qzuilib-test-modern-config-demo-plan.md`、`qzuilib-test-page-visual-matrix-plan.md`、`qzuilib-test-page-rebuild-plan.md`、`指令触发方案.md`、`errors/README.md`、本文件。

### 重要约束与后续注意

- 不改动 `ModernConfigTemplateScreen` / `ModernConfigBridge` / `ModConfigGui` / config 包，demo 只复用 public API。
- 可选依赖检测必须继续与 `ModConfigGui` 保持一致：`Class.forName("club.heiqi.config.Config", false, classLoader)` 与 `Class.forName("club.heiqi.config.MutableConfig", false, classLoader)`，不检测配置文件。
- 纯 JVM 测试仍禁止 `new ModernConfigTemplateScreen`；屏幕跳转、12 入口真实可见性、搜索/草稿/保存/返回链路必须用 `runClient21` 游戏内人工确认。
- 当前 `club.heiqi.config` 与 UILib 同 main sourceSet，降级路径无法在当前同 jar 环境实测；代码路径已保留为未来拆分准备。

## 交接要求

- 控制在一屏内，优先写"下一位现在最需要知道什么"
- 不复制长篇背景，详细信息放回对应文档
