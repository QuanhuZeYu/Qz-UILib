# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-25 第 99 次）—— 文本垂直对齐修复实现完成待真机验证

### 一句话现状

文本垂直对齐修复已实现并提交（commit `a05ea1c8`，分支 fix/text-vertical-alignment）。
新建 TextVerticalAlign 枚举（TOP/CENTER/BOTTOM，默认 CENTER）+ paint 层行框居中偏移。
编译通过，scene 测试全绿，reviewer 审核通过。待真机验证视觉居中效果后合回 4.0。

### 本会话关键 commit

| commit | 分支 | 内容 |
|--------|------|------|
| `9448d53a` | 4.0 | [Merge] 合回字体引擎坐标系修复与 ink 字段补全 |
| `3b393a34` | 4.0 | [Docs] 字体任务收尾——交接第98次 + 当前态 |
| `a05ea1c8` | fix/text-vertical-alignment | [Fix] scene 文本垂直对齐修复——TextVerticalAlign + 行框居中 |

### 实现要点

- 新建 `TextVerticalAlign` 枚举（`ui.scene.node` 包，TOP/CENTER/BOTTOM，默认 CENTER）
- SceneNode 加 `textVerticalAlign` 字段（PAINT 级，setter 只调 markSelfPaint 不调 markSelfLayout，I4 守住）
- ScenePaintEngine 注入 SceneTextMeasurer，calculateTextTop 按 align 计算：
  - TOP: paddingTop
  - CENTER: paddingTop + max(0, (innerHeight - lineHeight)/2)
  - BOTTOM: paddingTop + max(0, innerHeight - lineHeight)
  - 盒高≤lineHeight 钳到 paddingTop
  - switch default 抛 UnsupportedOperationException（未来扩展忘补 case 立即报错）
- AbstractSceneHostWidget + 14 处测试构造点改传 measurer（无残留无参构造）
- PaintCommand.text javadoc 修正：基线对齐点 → 文本行框左上角
- 新增 TOP/CENTER/BOTTOM/钳零/默认 CENTER 五个测试

### Oracle 裁决要点（DECISION-20260625-text-vertical-alignment-research.md）

- 扩展点放节点字段层（A变体），枚举放 ui.scene.node 避免环形依赖
- 首版 TOP/CENTER/BOTTOM 三种全实现（零成本不留 TODO 债），BASELINE 推迟
- 水平对齐本次不做，结构对称预留（未来加 TextHorizontalAlign 同模式）
- 光标无耦合（caret 是独立背景节点，y 由 FlexLayout CENTER 决定）

### 验证状态

- 编译通过，scene 测试全绿
- reviewer 审核通过，I4/I6/I7/I8 不变量均守住
- 待真机验证视觉居中效果（按钮/TextInput 文字是否居中）

### 下个会话任务表

1. **真机验证文本垂直对齐**：用户跑 runClient21 看按钮/TextInput 文字是否视觉居中
2. **若通过则合回 4.0**：按 AGENTS.md 用 `git merge --no-ff` 合回
3. **恢复主线**：TextArea 高优先级缺口 / 嵌套分类树导航 / 字段引擎最小切片

### 当前分支

- `fix/text-vertical-alignment`：1 commit，工作区干净，待真机验证后合回 4.0
- `4.0`：主分支，字体引擎修复已合入
- `fix/glyph-coordinate-system-mismatch`：已合回，保留作参考，可删

### 已知边界

- 多行文本（`\n`）未处理，当前用单 lineHeight（单行行框），属后续扩展
- 水平对齐 textLeft 恒 0，本次不做
- 真机行高随字号变化未由 stub 覆盖，待真机确认

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能登记到正确 Owner。
