# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-23 第 85 次）—— docs 剩余超长表格行已改 XML

### 一句话现状
`docs/` 下指定剩余超长表格行已治理：命中表格整体改为 XML，非表格超长行按自然断点换行；
目标文件复查均无 `>200` 字符物理行，`git diff --check` 通过（仅 CRLF 提示）。

### 本次处理范围

- `docs/使用文档/v4.x-LTS-稳定API清单.md`：3 张含超长行表格改 XML。
- `docs/开发者文档/reviews/REVIEW-20260518-browser-capability-gap-audit.md`：
  “已完整实现的能力”表格改 XML。
- `docs/开发者文档/specs/network-layer-plan.md`：“关键文件”表格改 XML。
- `docs/开发者文档/specs/net-codec-wire-format.md`：“Envelope v2”表格改 XML。
- `docs/开发者文档/errors/ERROR-20260424-gradle-zulu8-toolchain.md`：
  PowerShell 长命令改为 `$toolchains` 数组拼接。
- `docs/开发者文档/reviews/REVIEW-20260613-lwjgl3ify-decouple-plan.md`：
  fallback 键盘事件语义说明按自然断点换行。

### 跳过项

- 未处理 `NORTH_STAR.md`：用户指定由另一个 fixer 处理。
- 未处理 `docs/记忆/决策/DECISION-20260609-remote-ui-runtime-lease-protocol.md`：
  用户指定代码块内行跳过。

### 验证

- 指定 6 个文件逐行复查：均无 `>200` 字符物理行。
- `git diff --check` 通过；输出仅提示部分文件 CRLF 将被 Git 触碰时转 LF。
- 本次是文档格式治理，未运行代码构建或测试。

### 下一步

1. 如继续文档治理，可对全仓执行一次超长行扫描确认是否还有非目标残留。
2. SceneSelect 真机复验仍按上一轮交接要求交用户跑。
3. 后续进入字段引擎最小切片前，先确认当前文档治理分支已合并或工作区干净。

### 已知边界

- XML 只是文档格式承载，未引入解析流程或运行时语义。
- 本次只处理用户指定文件；未全量治理 `NORTH_STAR.md` 与指定跳过的代码块长行。
- SceneSelect 真机复验状态未因本次文档治理改变。

---

## 重要纪律（长期沿用）

- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
