# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；当前工作分支：`fix/layout-case-observation`
- 已完成：旧 test 子页仍保持清空；`/qzuilib test` 首页保留 DOM / CSS / Layout / Paint / Input / Controls / TextFont / Animation / RuntimeHost / RemoteNet 语义分组导航；当前运行时测试内容已清空：`createRuntimeTestCases()` 返回空列表，分组计数归零，二级页只显示空态说明，不再展示旧用例卡片、执行入口或人工确认按钮；旧矩阵列表、旧 demo 样式表、旧用例样例分发和旧自动断言分发已删除/替换为空态 stub
- 当前设计：新 test 体系只保留语义分组壳；运行时用例字段、状态、样例展示、自动断言和人工确认入口都需要下一轮重新规划，不沿用当前已清空矩阵；textarea 删除换行崩溃已修复，见 `docs/开发者文档/errors/ERROR-20260602-textarea-stale-visual-line-cache.md`；input 空值高度与 textarea 光标坐标混用已修复，见 `docs/开发者文档/errors/ERROR-20260602-form-control-input-textarea-caret.md`；游戏关闭 native 崩溃已定位为 `QzUiLibShutdown` 非渲染线程释放字体 GL 资源，修复与日志路径见 `docs/开发者文档/errors/ERROR-20260603-font-shutdown-hook-gl-dispose.md`
- 验证状态：运行时矩阵清空已通过离线目标 `UiTestDocumentPageControllerTest`（该命令已执行 `compileJava`）与最终 `git diff --check`；字体 shutdown hook 修复仍需下一次游戏内关闭时确认不再生成新的 `run\client\hs_err_pid*.log`
- 下一步：交给下一个 Agent 重新规划 `/qzuilib test` 运行时测试设计；规划前不要继续修补旧卡片矩阵；随后再用 `runClient21` 做游戏内打开和关闭复测
- 文档流程补充：审查误报或结论失效时，必须在原始审查正文添加 `后续复核（YYYY-MM-DD）` 批注，并同步更新 reviews 索引，避免后续 Agent 只读原报告重复审核

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
