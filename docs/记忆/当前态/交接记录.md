# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次工作分支：`add/config-template-qz-sync`；目标是给 Forge 配置模板补齐 Qz 网络同步便捷接入
- 已完成范围：`ForgeConfigTemplateScreen.Spec` 新增 `enableQzNetworkSync(...)` 与 `createQzNetworkSyncTarget(...)`；`ModConfigGui` 改用新入口；`ConfigTemplateSyncManager` 新增 `config_template_sync_close` fetch 端点；`ConfigTemplateRemoteSyncController` 在页面关闭和异步打开后已关闭场景通知服务端清理会话
- 文档状态：已更新 `docs/使用文档/02-控件/Forge配置模板.md`、`docs/使用文档/v4.x-LTS-稳定API清单.md` 与 `docs/记忆/长期事实/架构边界.md`
- 验证状态：已通过 `git diff --check`；已通过 `./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.config.ConfigTemplateSyncManagerLifecycleTest" --tests "club.heiqi.uilib.config.ConfigTemplateRemoteSessionTest" --tests "club.heiqi.uilib.config.RemoteConfigDocumentPagesLifecycleTest" --tests "club.heiqi.uilib.config.ConfigSyncModelsTest"`
- 当前仍需注意：本轮改动尚未提交；若需要提交或合并回 `4.0`，需用户明确要求后再执行
- 后续建议：需要游戏内完整确认时，再用 `runClient21` 从模组配置页验证 Qz 同步握手、编辑、保存和关闭释放会话

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
