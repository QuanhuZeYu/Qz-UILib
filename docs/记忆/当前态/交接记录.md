# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次工作分支：`chore/local-release-flow`；目标是迁移 Qz-Miner 的 `.changelogs/<tag>.md` 本地发布说明覆盖流程，并重新发布 `4.2.0`
- 已完成范围：已补 `.changelogs/4.2.0.md`、`docs/开发者文档/发布流程.md`、release workflow 后置覆盖步骤、开发者文档索引和长期项目约定；`gh` 2.93.0 已安装到 `C:\Program Files\GitHub CLI\`
- 首次发布状态：`4.2.0` 远端 release workflow 成功，资产已生成，但自动 notes 只列 PR `#60` / `#61` 且 compare 基线为 `4.0.4...4.2.0`，不符合完整发布说明预期
- 验证状态：发布前已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test`、`./gradlew.bat --no-configuration-cache --no-daemon -x test publishToMavenLocal`；本轮文档 / workflow 迁移提交前仍需再跑 `git diff --check`
- 发布待办：提交并 `--no-ff` 合回 `4.0` 后，把本地 annotated tag `4.2.0` 移到新合并提交，并用 `git push --force-with-lease origin 4.2.0` 重触发发布
- 后续验收：确认 GitHub Release 标题为 `4.2.0`、正文来自 `.changelogs/4.2.0.md`、资产重新生成且工作区干净

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
