# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；本批分支 `add/qzuilib-test-controls-visual-samples` 用于 `/qzuilib test` Controls 视觉样例，完成后按项目规则合并回 `4.0`
- 已完成：Controls 首轮 7 张视觉样例 `VIS-CTRL-001` 到 `VIS-CTRL-007`，覆盖 button、text/password/number input、textarea、checkbox/radio、select/table、slider/toggle、tab/focus/disabled
- 当前矩阵：已接入 31 / 59 张，剩余缺口 28 张；自动样例 23 张、人工待确认 8 张；Controls 中 `VIS-CTRL-003` textarea caret/selection 与 `VIS-CTRL-005` select popup/table 视觉对齐保留人工诊断
- 新增实现：`UiTestControlsVisualFactory` 和 `UiTestControlsAssertionRunner`，由现有 `UiTestSampleVisualFactory` / `UiTestAssertionRunner` 委托，不恢复旧测试页结构
- 验证：已通过 `git diff --check`、`UiTestDocumentPageControllerTest`、`HtmlLikeDocumentWidgetInlineLayoutCacheTest`、`HtmlLikeDocumentWidgetTest`、`DocumentPaintEngineTest`、`UiDocumentTest`、`DocumentEventPropagationTest`、`UiStyleResolverTest`、`compileJava`；本批未跑完整 `test` 或 `runClient21`
- 下一步：继续 `/qzuilib test` 视觉矩阵，优先 TextFont 样例；同一工作区 Gradle 命令必须串行，禁止并行跑 `compileJava` / `test`

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
