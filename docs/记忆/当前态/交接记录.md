# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-24 第 89 次）—— 嵌套字段组件三件套完成 + mount/show Owner 归属 bug 修复

### 一句话现状
SceneSimpleList、SceneKeyValueMap、SceneObjectField 三个嵌套字段控件全部完成（含测试 + demo 页 + 压力测试页），
mount/show Owner 归属 bug 已修复，所有 reviewer 非阻断债务已清。
下一步：真机验证或继续 TABLE 行内编辑 / 嵌套分类树导航。

### 本会话全部 commit（按时序）

**债务清理（4 次 commit）**
- `a924d07d` 清理 4 项 reviewer 快胜债务（B2 cachedLayout 强转守卫 / B5 反射改公开方法 / B6 contentBox 重命名 / B7 maxScrollY 守卫）
- `750b3ca3` AnchorRect 包归属下沉 + hitTest 零分配（B3/B4）
- `5f58c9a2` B1 oracle 裁决接受不修（retained-mode 固有延迟）
- `e7f82604` A1/A2 偏离登记合并转正为 §4/§4.5 正式能力

**嵌套字段控件（5 次 commit）**
- `874c3588` SceneSimpleList + SceneKeyValueMap 控件 + 测试（9+11 项）
- `d65b609f` 控件 demo 页 + 压力测试页接入 hub + SimpleList 滚动修复
- `4100fd3d` SceneObjectField 递归组件 + 测试（7 项）
- `83f8f7c5` 清理 ObjectField 死代码 + Javadoc 残留
- `df6e9299` 修正 mount 与 show 的 Owner 归属为当前作用域（bug-fix）

**demo 页接入（1 次 commit）**
- `9415d3e4` ObjectField demo 页接入 hub 并扩展压力测试 Tab

### 控件能力一览

| 控件 | 能力 | 测试 | demo | 压力 Tab |
|------|------|------|------|---------|
| SceneSimpleList | 动态字符串列表（增删编辑 + id 稳定 key + caret 保持 + 滚动） | 9 项 | ✓ | ✓ |
| SceneKeyValueMap | 动态键值对（key/value/type 三列 + 校验反馈 + 边界 + 滚动） | 11 项 | ✓ | ✓ |
| SceneObjectField | 递归复合对象（递归建树 + 折叠展开 + 深度限制 + 不可变重建链） | 7 项 | ✓ | ✓ |

### oracle 裁决记录（2 次）
- **B1 anchor 时序窗口**：replay 无视觉错位，真实窗口在 hit-test 滞后一帧（retained-mode 固有延迟），接受不修
  → `docs/记忆/决策/DECISION-20260624-overlay-anchor-hit-test-frame-delay.md`
- **OBJECT 递归设计 4 边界**：方式 A 递归建树不递归 mount / 方案 A 单根 Signal /
  折叠展开外部 signal 受控 / 深度限制原地内联展开 + 超限占位
  → oracle 复核四条裁决全部遵守，I1/I3/I5/I7/I11 守住

### mount/show Owner 归属 bug 修复
- 根因：`SceneRuntime.mount`(:134) 和 `show`(:279) 写死 `rootOwner.createChild()`，
  不读 `Owner.current()`，导致嵌套调 mount/show 的子作用域挂 rootOwner 不级联回收
- 修复：改为 `(Owner.current() != null ? Owner.current() : rootOwner).createChild()`
- 修复 SceneKeyValueMap 行删除 Owner 泄漏（buildRow 内 3 个 rt.mount）
- 三控件测试 27 项全绿，零回归

### 嵌套字段组件研究报告
explorer 产出完整研究报告，推荐实施顺序：
1. ✅ SimpleList（forEach 已就绪）
2. ✅ KeyValueMap（复用 forEach + SceneSelect/Segmented）
3. ✅ OBJECT 递归（方向 B 建树期同步递归展开）
4. ⬜ TABLE 行内编辑（SceneTable 当前静态只读，需大改或新建）
5. ⬜ 嵌套分类树导航（SceneTreeView 从零建，>1000 行）

### `/qzuilib test` hub 现有 12 个入口
Scene → Controls → Scroll → Table → Layout → Form → Select →
SimpleList → KeyValueMap → Stress Test → ObjectField

### 错误记录
- `ERROR-20260624-parallel-fixer-gradle-build-race.md`：并行 fixer 各自跑 build 导致 gradle 竞争
  （教训：并行 fixer 只并行写代码，build 统一由主 Agent 串行跑）

### 下一步
1. **真机验证**（用户跑）：200 字段 + 嵌套对象下 ObjectField 布局/绘制成本、
   Tab 切换 mount/unmount 泄漏、深度占位渲染、文本旁路在嵌套 TextInput 中的传递
2. **TABLE 行内编辑**：SceneTable 当前静态只读，需改造为支持行内编辑
   （explorer 建议需 oracle 裁决 SceneTable 扩展 vs 新建 SceneDataTable）
3. **嵌套分类树导航**：SceneTreeView 从零建（>1000 行，一期暂缓项）
4. **字段引擎最小切片**：三个嵌套控件已就绪，可接入字段引擎

### 已知边界
- reviewer 9 项建议已全部处置（6 还清 + 1 接受 + 2 无需处理）
- A1/A2 偏离登记已转正为 §4/§4.5 正式能力
- mount/show Owner 归属已修复
- 旧对外现代配置页文档仍描述 DOM 12 模板能力；scene 一期完成前不得宣称等价
- 旧栈 ui.dom 仍完整存在（65+ 源文件），scene 包零引用，Phase 5 暂缓

### 当前分支
`refactor/scene-phase4-control-layer`（从 `4.0` 切出）
本会话 10 次 commit 均在此分支，未合回 `4.0`

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- **并行 fixer 只并行写代码，build 统一由主 Agent 串行跑**（ERROR-20260624 教训）。
