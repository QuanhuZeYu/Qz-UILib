# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；本批分支 `refactor/style-declaration-slots` 用于 `UiStyleDeclaration` 第二批低风险属性 slot 化，完成后按项目规则合并回 `4.0`
- 已完成：`UiStyleDeclaration` 将更多 paint-only 属性从独立字段迁入 `StyleDeclarationSlot<T>`，真实声明值统一落到 `declaredValues`；覆盖 `border-radius`、backdrop、visibility、box/border/text paint、pointer/cursor、scrollbar-color 与 transform 等低风险绘制属性
- 已完成：保留 public getter/setter/clear 方法不变；`copyFrom` 继续通过 `declaredValues` 复制 slot 属性；`setKeyword(...)` 覆盖 slot 属性时可正确清除具体值并保留关键字
- 本批新增测试：`UiStyleResolverTest.slotBackedPaintPropertiesCopyAndClearWithKeyword` 覆盖 slot 属性复制与 keyword 覆盖清除
- 后续边界：layout-affecting 属性、animation/transition 属性和复合 layout 属性暂未迁入 slot；后续仍应按属性族小批次迁移，不要一次性重写 `UiStyleDeclaration`
- 验证：已通过 `UiStyleResolverTest`、`compileJava` 与 `git diff --check`；本批未跑完整 `test` 或 `runClient21`
- 注意：同一工作区内 Gradle 命令必须串行，禁止并行跑 `compileJava` / `test`

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
