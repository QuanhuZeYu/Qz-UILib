# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；`HtmlLikeDocumentWidgetTest` 第三批 Focus/Keyboard 拆分已完成并合并回主分支
- 已落地：同包 `HtmlLikeDocumentWidgetTestSupport` 承载共享 helper/fixture；`HtmlLikeDocumentWidgetDragTest` 承载拖拽主题测试；`HtmlLikeDocumentWidgetScrollTest` 承载滚动主题测试；`HtmlLikeDocumentWidgetFocusKeyboardTest` 承载焦点与键盘主题测试
- 已迁移 Focus/Keyboard 范围：focus、focusin/focusout、Tab traversal、Tab 触发滚动、raw button/input keyboard、textInput/key event 和 disabled focus 行为，共 18 个测试；测试内容和断言语义保持不变
- 本轮验证已通过：`git diff --check`、`HtmlLikeDocumentWidgetTest`、`HtmlLikeDocumentWidgetFocusKeyboardTest`、`compileJava`
- 后续建议：继续按主题小批量拆 `LayoutCache`、`AnimationRuntime`、`EventDispatch`，每批只做测试组织搬家，不改生产代码语义
- 背景状态：Controls 视觉样例已接入并修复 `VIS-CTRL-005` table 同步；`/qzuilib test` TextFont 下一批可在测试拆分阶段性收口后恢复

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
