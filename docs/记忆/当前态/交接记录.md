# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-28 第 130 次）—— L1 嵌套 grow 子容器缺陷修复

### 一句话现状

L1 从"有意边界"重新定性为"不对称判定缺陷"并修复：priorKnownInnerHeight 闸门放宽对齐
computeHeight 三合流口径 + 排除 scrollable，守 I7。经 Oracle 测试充分性评估补齐 5 个
遗漏项后，共 11 回归测试全绿（144 tests 0 failed）。
经 librarian 行业研究 + explorer 源码侦察 + oracle 裁决 + 2 fixer 实现 + reviewer 审核
+ oracle 测试评估 + fixer 补测。工作区含改动未提交，待用户确认后 commit。

### 本会话改动清单（含第128-130次）

**第128次已 commit（6b485c17）**：Slider 缺陷 D 文档同步
**第129次已 commit（ef9bcaa3）**：scene 技术债伪债务清理
**第130次未 commit**：L1 嵌套 grow 子容器缺陷修复
- `ConstraintResolver.java`：priorKnownInnerHeight 闸门从 `isFillParentHeight && hasHeightConstraint`
  放宽为 `(isFillParentHeight || getFlexGrow>0 || getPercentHeight>0) && !isScrollable && hasHeightConstraint`
  + Javadoc 同步三处同步关系（+32 / -6）
- `SceneLayoutEngineTest.java`：11 回归测试（+408）
  - L1-1~6（主修复反证 / percent 变体 / 两层嵌套 / scrollable 排除 / I7 干净帧 / maxHeight 撞顶）
  - T1~5（Oracle 评估补齐：padding 扣减 / preferredHeight 下界 / ROW cross 高 / percent 嵌套 / scrollable+fill 变体）
- 新建 `DECISION-20260628-scene-l1-grow-prior-asymmetry.md`：L1 修复决策
- `DECISION-20260628-scene-min-max-clamp.md`：L1 重新定性为缺陷
- `scene技术债.md`：L1 改为已还清
- `决策/README.md`：补 L1 决策索引
- 当前态三件套刷新

### 任务链路

1. 用户要求处理 L1 → 派 librarian 研究 Qt/Flutter/CSS 行业解法 + explorer 侦察源码现状
2. librarian：主流框架都不解嵌套 grow，YAGNI；explorer：L1 机理与主流不同（X 已收到确定高但不传）
3. 用户拍板"派 Oracle 再核实主流框架为何有意设计此边界"
4. Oracle 裁决：L1 是缺陷非有意边界（CSS §9.8 definite 语义），修正 explorer 补丁（漏 percent + 需排除 scrollable）
5. Oracle 建议补查 FlexLayouter:366 → 派 explorer 确认非对称点
6. 并行派 2 fixer：代码改动 + 6 回归测试
7. 主 Agent 串行跑 build + test → 编译零问题，139 测试全绿
8. 派 reviewer 审核 → 有条件通过（P1 测试 4 反证力度不足）
9. 主 Agent 修 P1：测试 4 改用 grow 子 + 断言 X 自身高=16（viewportHeight）→ 重跑 test 全绿
10. 用户问"测试范围是否足够" → 派 oracle 评估测试充分性
11. oracle：临界偏不足，2 高风险（padding/preferredHeight）+ 3 中风险（ROW/percent 嵌套/scrollable+fill）遗漏
12. 派 fixer 补 T1-T5 → 主 Agent 串行跑 build+test → 144 tests 全绿
13. 主 Agent 更新记忆文档

### 当前分支与工作区

- 当前工作分支：`4.0`
- 工作区状态：含第130次 L1 修复改动未提交（2 代码/测试 + 5 文档）
- 4.0 比 origin/4.0 领先 13 commits（第128-129次已 commit，第130次尚未 commit）
- 游离分支 `codex/birthday-remote-trigger-practice`（个人彩蛋，保留游离不合并不删除）
- 本地残留已合并分支：`docs/north-star-slim`、`refactor/scene-slider-defect-d-fix`（可清理）

### 下个会话任务表（用户决定优先级）

1. **待用户确认后 commit 第130次 L1 修复改动**
2. **可选：push 4.0 到远端**（含第128-130次 + 之前 11 commits）
3. **可选：清理本地已合并分支**
4. **可选：启动非布局债某项**（B6 FBO 纹理缓存 / B6 hit-test 对偶 / A1 残留确认 / chrome 主题层）
5. **可选：用户自定新方向**

### 验证状态

- jetbrainsBuildProject 编译零问题
- gradle test --tests SceneLayoutEngineTest BUILD SUCCESSFUL（144 tests，0 failed）
- reviewer 有条件通过，P1 已修复（测试 4 改用 grow 子真正反证 scrollable 排除）
- oracle 测试充分性评估：补齐 T1-T5 后从"临界偏不足"提升到"足够"
- 未跑真机验证（布局算法改动，沙箱无 GUI，真机验证可选）

### 待真机验收清单（沙箱无 GUI，交用户跑）

- ~~B2 TextArea O(N²)→O(N) 帧率收益~~ → **2026-06-28 用户确认无异常**
- ~~SceneTestHub 滚动视口效果~~ → **2026-06-28 用户确认无异常**
- ~~glyph 坐标系修复~~ → **2026-06-28 用户确认无异常**
- B6 FBO 真机渲染：**有效避免裁切问题，但性能压力太大**（2026-06-28 用户反馈），
  批 3 纹理缓存需重新评估性能取舍
- ~~文本垂直对齐 em-box 居中模型真机视觉~~ → **2026-06-28 用户确认无异常**
- ~~lwjgl3ify onTextEvent emoji 真机路径 + Bridge remove API 内存泄漏隐患~~
  → **2026-06-28 用户确认无异常**
- ~~SceneSlider 缺陷 D 松手提交~~ → **2026-06-28 随 c37b1b3c 合并已真机验证**
- ~~B5 paint LEFT 惰性测量~~ → **随 23bf3a94 已落地**
- ~~B8 滚动后 hover 滞留~~ → **随 16dd6d56 已落地**
- **L1 嵌套 grow 子容器修复**：布局算法改动，可选真机验证（如有嵌套 grow 容器的 demo 页）
- network Forge 回退 + BungeeCord/Velocity 代理联机实测：**网络层暂时搁置，
  集中处理 UI 层**（2026-06-28 用户指示）

### 已废弃方向（不再维护）

- 旧 HTML-like / `ui.dom` 栈相关任务（Phase 5 退役清理）：旧栈已废弃，
  退役清理不纳入当前工作主线

### 待决策/待实现项（未排期，待用户拍板）

- N1 capture 生产化前置必修项：requestPointerCapture 持有期 POINTER_DOWN
  焦点/指针归属相反（REVIEW-20260618-scene-input-i4-merge:91-93）
- Config scene 版 8 种字段类型未落地：LONG_TEXT/SIMPLE_LIST/TABLE/OBJECT/
  KEY_VALUE_MAP/PRESET_SELECTOR/RAW_EDITOR/ENHANCED_PICKER（Config模块.md:141）
- datatable 编辑列视觉 D1/D2 拍板项（datatable-editable-cell-visual-spec:257-263）
- scene-overlay-p0-plan：NORTH_STAR 是否把 top-layer 写入正文/偏离登记待用户确认
- net_self_check 页待恢复（network-layer-plan:16）
- wheel 事件 DOM 分发（旧栈 3.4 P2，REVIEW-20260601-browser-semantics-phase2-followup:192）
- A1 effect 内 set 慢一帧残留待确认（scene技术债.md）
- font-family 长期暂缓（有意边界，DECISION-20260601）
- L1 reviewer P3 建议补测：grow+shrink 混合 / grow+preferredHeight / percent 双层 / padding 非零

---

## 重要纪律（长期沿用）

- 帧率/真机实测必交用户跑，沙箱无 GUI
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做；
  但 opencode 重启后原 session 失效，按"调度开销明显大于收益 + 信息已在上下文"
  白名单主 Agent 可亲自做单点排错
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑，避免并行 Gradle 竞争 build/ 目录
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，
  改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能
  登记到正确 Owner
- token 实际值以 SceneChromeTokens.java 源码为准，不依赖过期文档清单
- oracle 裁决架构方案后，如方案涉及长期可引用的设计决策，建议补写
  `docs/记忆/决策/` 文档沉淀
- I11 逃生舱②扩张纪律：扩之前证明它确实是一类新的权威交互真值，
  而非可以用现有机制表达的东西（B8 验证了 hover 重算不需要扩②）
- 诚实记录 oracle 方案缺口：oracle 不是神谕，发现 bug 诚实记录修复
- **I7 数值求解器边界**：父级数值求解器内多轮迭代（freeze do-while）≠ 多 pass，
  单 pass 约束的是父→子约束下沉次数；判据是「求解器若需要先 layout 子、读子结果、
  再回头改父分配即违反 I7；若全程父级数值迭代、子在收到最终约束后才首次 layout，则守 I7」
- **margin 五处联动纪律**：改 margin 任一处需同步审视 SizingCalculator/
  ConstraintResolver/FlexLayouter/SceneGeometry 四协作者
