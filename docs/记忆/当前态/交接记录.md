# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前分支：`add/runtime-test-subpages`
- 已完成：旧 test 子页仍保持清空；`/qzuilib test` 首页改为语义分组导航；运行时测试卡片迁移到类型二级页；DOM / CSS / Layout / Paint 二级页各已接入 5 张卡片和 `执行自动测试`、`人工通过`、`人工失败` 按钮，结果会写回卡片状态、总览计数和最近失败摘要
- 当前设计：新 test 体系按 DOM、CSS、Layout、Paint、Input、Controls、TextFont、Animation、RuntimeHost、RemoteNet 分组；首页只做导航和摘要；运行时用例必须直接展示 `预期结果：...` 人工验收文本；Paint 视觉/命中类和 `LAYOUT-005` baseline 自动按钮只做结构或已知缺口标记并进入 `执行中`，最终需人工确认
- 验证状态：当前测试全部通过；本轮已通过离线 `compileJava`、离线 `UiTestDocumentPageControllerTest` 与 `git diff --check`
- 下一步：按规格启动 Input / Controls / TextFont / Animation / RuntimeHost / RemoteNet 运行时二级页恢复；每页至少恢复 5 个运行时卡片，先写规格再实现
- 文档流程补充：审查误报或结论失效时，必须在原始审查正文添加 `后续复核（YYYY-MM-DD）` 批注，并同步更新 reviews 索引，避免后续 Agent 只读原报告重复审核

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
