# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前分支：`refactor/decouple-lwjgl3ify`；输入后端加固已提交 `cf1bf055 [Fix]: 加固 lwjgl3ify 输入后端降级路径`，工作区在交接前应保持干净。
- 已完成第一阶段：`UiInputService` facade 不变，内部 `UiInputBackend` / `Lwjgl3ifyInputBackend` / `LwjglxPollingInputBackend` 已落地；`InputEvents` 反射订阅，注册失败时启用键盘轮询兜底；`lwjgl3ify` 发布硬依赖已移除，仅保留 `devOnlyNonPublishable` 与 `testCompileOnly`。
- 已验证第一阶段：`git diff --check`、`Lwjgl3ifyInputBackendTest`、输入/HUD/控件定向测试、`compileJava`、`publishToMavenLocal` 均通过；发布 POM/module 未声明 `lwjgl3ify`。
- `lwjglx` 续研结论：渲染业务未发现直接使用 `org.lwjglx`，渲染链主要是 `org.lwjgl.opengl.*` / `org.lwjgl.BufferUtils`；`org.lwjglx` 源码耦合集中在输入和光标。
- 下一步建议：先拆 `Keyboard.KEY_*` 常量扩散，涉及控件、文档、remote、config、devtools 等只为键码常量 import `org.lwjglx.input.Keyboard` 的文件；新增内部 `UiKeyCodes` 后替换，不改变 `UiKeyEvent.keyCode` 数值语义。
- 暂缓项：`LwjglxPollingInputBackend` 与 `UiHostInputCoordinator` 的真实 `Keyboard/Mouse` 轮询调用先保留在客户端宿主输入边界；系统光标 `SystemDocumentCursorHost` 仍有 `Display` 静态 import 和反射 `Mouse/Cursor`，可单独立项；OpenGL/字体 atlas 不与本轮键码常量解耦混做。

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
