# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 用户确认后续路线：先扩展字体引擎的字号能力，再实现数学公式渲染和排版能力；决策记录为 `docs/记忆/决策/DECISION-20260611-font-size-before-latex-math.md`
- 字号能力应进入测量、布局、裁剪、绘制与 glyph 缓存，不只是在最终绘制阶段套 `GL11.glScalef(...)`
- 对外建议按 UI 像素 `font-size` 表达；内部应按目标像素尺寸提交 glyph quad，并在后续需要高清大字号时让 glyph atlas 按字号区分缓存
- 数学公式阶段以 LaTeX 风格数学子集为输入，优先自研公式 AST + box layout，复用字体系统保证普通文本、希腊字母、符号和上下标字体一致
- 当前尚未实现源码改动；下一步应先设计字体字号 API、字体度量模型、布局缓存影响和兼容默认字号映射
- 用户已授权后续自动 Git 收口：除非用户明确说明“不提交”或“不合并”，Agent 应按任务完成情况自动判断是否提交、是否合并回主分支，并可自动执行

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
