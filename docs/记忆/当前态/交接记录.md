# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接（2026-06-17 第 34 次）—— 重大方向变更：彻底废弃 HTML-like，按 NORTH_STAR 模型全新搭建 `ui.scene` 场景树（第一阶段地基 T1-T5c 已落地提交，T6 真机切片待启动）

- **当前分支**：`feat/scene-model-foundation`（从 `4.0` 切出，`4.0` HEAD = `9740c5e9`）。**已 2 笔提交**：源 `db6fa3b5`（16 文件 1946 行）+ 测试 `b238fd4a`（5 文件 1801 行）。工作树干净，compileJava + test 全绿（用正确路径 `D:\.MyApps\.ENV\gradle-home` 双 p）。**尚未合回 4.0**（第一阶段 T6 真机通过后再 `--no-ff` 合并）。
- **⚠️ 重大方向变更（用户下达，覆盖原方向 A 5 批路线图）**：用户宣布**彻底废弃 HTML-like 实现，完全用 NORTH_STAR.md 模型搭建新式 UI**。原话："新模型兼容旧案搞得我要疯了，HTMLlike现在我宣布彻底废弃，完全使用新模型搭建新式UI；清理qzuilib test中所有旧式HTMLlike；参考HTMLlike做完全新数据模型的实现；彻底不还旧债"。**作废**：方向 A 5 批控件迁移路线图、批次 0/1 全部框架（Tooltip/Tab/RadioGroup 不再迁，批次 0 Breadcrumb 已合入代码作可行性实证保留）。
- **deepwork 进度文件 `.slim/deepwork/control-reactive-refactor.md`（git 忽略，新会话续作必读）**：顶部"重大方向变更"节 + "新模型重写战略规划"节含全部规划/裁决/施工进度。下方原批次 1 内容已冻结仅作历史参考。
- **新模型战略规划（ora-2 / ses_12b33ef82ffenYRqDrtNaKGiIt 定稿）**：
  - **可保留不重写**：`ui.reactive`（纯响应式原语零项目依赖，宪章①②层完美落地，原样复用）+ `ui.render`（UiRenderContext 公共 API 纯几何+颜色+文本+效果栈，零 DOM/signal 概念守 I6，新 paint 产语义等价命令即可对接，render 一行不改）。
  - **必须重写/废弃（约 3.5 万行垂直栈）**：`ui.dom`(5900，I7 标脏债的根)→`ui.layout`(8215)→`ui.paint`(3703)→`ui.component`(797，焊死 ElementNode 是旧 DOM 响应式壳)→`ui.control`(13292)→`HtmlLikeDocumentWidget`(3741)。
  - **新包 `ui.scene`（与旧 `ui.dom` 物理隔离支持 strangler 共存）**，按 NORTH_STAR 七层：`ui.scene.node`(SceneNode 灵魂)/`ui.scene.layout`/`ui.scene.paint`/`ui.scene.component`(SceneRuntime)。
  - **核心反转（根除 I7 债）**：旧 DOM `markSubtreeLayoutMutation` 把脏**向下推**（递归刷全部后代 version → 全量重算）；新 SceneNode 把"有脏"**向上冒**（祖先链点路标 O(深度)，绝不碰后代）。`descendantXxxDirty` 是"路标"非脏标记（只告诉遍历"下面有脏别整棵跳过"，不强制自身重算）。`applyChildReconcile` 批量提交稳定子项零标脏。
  - **属性系统**：B 方案——SceneNode 强类型属性槽（setText→markSelfLayout、setBackgroundColor→markSelfPaint、setOpacity/setTransform→markComposite，写入自动打级降低 I4 风险），ui.style 只借鉴算法不复用本体。
  - **5 Phase 路线图（strangler fig 渐进绞杀，不一刀切）**：Phase 0 契约地基→Phase 1 端到端最小可渲染切片→Phase 2 三声明式基石+keyed 列表→Phase 3 分级失效全链路+合成级动画→Phase 4 控件层重建（qzuilib test 7 分组逐组切换）→Phase 5 宿主粘合层迁移+旧栈退役（4 条退役条件全满足才删 3.5 万行）。
- **第一阶段（Phase 0 + Phase 1 = T1-T6）施工进度，全部过 oracle 审计门**：
  - **T1 SceneNode 双标记** ✅ oracle 关口 PASS（I7 债地基层正面根除）。**T1b** 跨父迁移漏标修复 + 复合 reconcile 测试 ✅。
  - **T2 PaintCommand 纯数据契约** ✅（零节点引用守 I6 契约线）。
  - **T3 SceneRuntime + bind 骨架** ✅（I3 mount 只跑一次 + I4 属性槽自动打级）。
  - **T4 SceneLayoutEngine 增量布局** ✅ ora-2 审查 PASS。**T4b** 修下沉节点几何上传缺陷（方案 A：layoutInternal 返回布尔沿祖先链上传，仍 O(脏链)）✅。
  - **T5 ScenePaintEngine + Replayer** ✅（I8 fragment assertSame 复用）。
  - **ora-2 整体审查（T1/T2/T3/T5+接缝）= 有条件 PASS**，发现阻断项 **B1 坐标接缝 bug**（PaintCommand 固化绝对坐标、fragment 复用不感知 layout 位置变化→父子裂开）。
  - **T5b** B1 修复（用户拍板方案 A 相对坐标：fragment 存相对坐标、组装期叠加 offset）→ **ora-2 复审 FAIL**：fixer 为修 B1 让 layout 复用 `compositeDirty` 表"位置变化"，**语义双载**违反信条五"三级语义不混"+反模式"万能脏标记"，Phase 3 composite 回放会吞掉位置变化。
  - **T5c** 修复（用户拍板新增独立 geometryDirty 标记）→ **ora-2 复审 PASS，埋雷彻底清除**：compositeDirty 恢复纯净（仅 setOpacity/setTransform 置位）、geometryDirty 与 bubbleDescendantLayout 同构守 I7、机制2/3 合并删 lastLayoutSnapshot、四类标记正交。17 单测全绿。
- **下一步（T6，未启动，第一阶段走通判据兑现点）**：① qzuilib test 加 **scene_demo 分组**（碰 devtools 路由 UiTestMatrixRegistry/UiTestGroupVisualBuilder/QzUiLibClientCommand）；② 建最小宿主把 SceneRuntime 接入 ui.render 帧循环，**固化 `flush(reactive)→layout→paint→replay` 顺序**（geometryDirty 清除依赖 layout 后必 paint 的同帧成对契约）；③ 构建 signal 驱动的背景色矩形 + bindText 文本场景；④ 沙箱单测覆盖 replay 序列；⑤ **真机交用户跑**（沙箱无 GUI）肉眼确认上屏 + 改 signal 后只该节点重绘兄弟缓存命中，真机后派子代理查 `run/client/logs/fml-client-latest.log` `club.heiqi.uilib` 零异常。**建议复用 fix-1**（T3 SceneRuntime 作者，最熟 mount/bind API）。
- **Phase 3 遗留备忘（不阻断 T6，勿现在动）**：① bind impact 参数未使用（删或 debug 校验）；② mount 异常路径泄漏空 childOwner（rootOwner.dispose 兜底）；③ markComposite 仍走 bubbleDescendantPaint（Phase 5 compositor 独立线程时需独立 descendantCompositeDirty 路标）；④ 补 composite/geometry 隔离回归测试；⑤ layout→paint 同帧成对契约需宿主固化。
- **重要纪律（沿用）**：决策点优先中文 `question`；帧率/真机实测必交用户跑；编译命令 `$env:GRADLE_USER_HOME="D:\.MyApps\.ENV\gradle-home"; ./gradlew.bat --no-configuration-cache compileJava`（**.MyApps 双 p 才对**，接力提示词曾误写单 p）；PowerShell 提交/合并信息用 `git commit -F <文件>`/`git merge --no-ff <分支> -F <文件>` + `[IO.File]::WriteAllText(path,msg,(New-Object Text.UTF8Encoding $false))`（heredoc/多行 `-m`/`Set-Content`/`--no-ff` 不带消息文件均会炸）。
- **可复用 session**（task_id 显式传别名才生效）：ora-2 `ses_12b33ef82ffenYRqDrtNaKGiIt`（新模型战略规划+全部地基审计，**架构裁决/审计首选**）；fix-1 `ses_12b0c5540ffeifr19Vh2318fUn`（T3 SceneRuntime 作者，**T6 实施首选**）；fix-2 `ses_12b0bb936ffeKczcOXYknfBL3W`（T4/T4b/T5/T5b/T5c 作者，熟 layout+paint 引擎）；exp-1 `ses_12b4ac494ffeB8HPwmuwC3bnYP`（HTML-like 全景+新模型就绪度，qzuilib test 入口结构）。

## 最近一次交接（2026-06-17 第 33 次）—— 方向 A 批次 0 落地：Breadcrumb 响应式重写 + 暴露 DOM 粗粒度标脏地基债（叫停关口已解除，批次 0 有条件通过）

- **当前分支**：`4.0`，已合入。merge `79e7c354`（`--no-ff` 合 `refactor/breadcrumb-reactive`）+ 源 `9b45e943` + 测试 `8d21572b` + 文档 `33533f17`。**已 `git show HEAD:` 双验** `DocumentBreadcrumbControl.java`（runtime/pathSignal/segmentsSignal/forEach）+ 债文档在 HEAD 树。工作树干净，完整 compileJava+test 通过（20s）。领先 origin/4.0 191 提交未推送。
- **本轮主线（deepwork 编排，control 层响应式长周期重构启动）**：阶段 B 架构诊断 → 用户拍板方向 A + 5 批路线图 → 阶段 D 用 deepwork 启动批次 0（Breadcrumb 切口）。**deepwork 进度文件 `.slim/deepwork/control-reactive-refactor.md`（git 忽略，含全部研究上下文/裁决/路线图/前置事实，新会话续作必读）。**
- **阶段 B oracle 架构诊断（全新 session，已沉淀 deepwork）**：control 层根因 = **根本没有视图更新抽象，控件作者除命令式重建无路可走**。逐条违反 I1（2/26 覆盖）/I3/I5/I7。方向裁决：**方向 A（SolidJS 控件作者协议层）= 唯一主轴**，B（文本共享设施）并入批次 3，C（Flutter canUpdate）排除。**5 批路线图**：批0 Breadcrumb 切口 → 批1 简单展示控件+协议固化 → 批2 列表密集型(TreeView/Table) → 批3 文本控件(TextArea/CodeEditor)+signal化 → 批4 Autocomplete收尾。
- **批次 0 交付（Breadcrumb 响应式重写）**：构造器加参 `(UiDocument, UiComponentRuntime)`；path signal 化（`Signal<String> pathSignal` + `Computed<List<Segment>> segmentsSignal` 派生 + 影子字段 `path` 保同步契约 getPath）；构造器内 `runtime.forEach(element, segmentsSignal, seg->seg.path, renderSegment)` 替代 `clearChildren+for 重建`；separator 数据驱动（每非首段 wrapper 前置 ">"，判定 `!seg.path.isEmpty()`，不用 index）；点击事件 `ReactiveControlBindings.onAction` 归属项作用域自动退订。**删除 refresh()/clearChildren。** runtime 注入链：ModernConfigTemplateScreen(documentWidget.getComponentRuntime) → ModernConfigPropertyBindings.createBindings(+runtime) → ModernNestedCategoryBinding(+runtime) → new DocumentBreadcrumbControl(document, runtime)。
- **⚠️ 批次 0 叫停关口触发并诊断解除（关键架构发现）**：I7 局部性验收断言**失败**——根段 wrapper 被 forEach 复用（assertSame 全过）但 `__getSubtreeLayoutMutationVersion` 90→112。**oracle 终审（解读 3，真实地基债非度量误差）**：`DocumentNode.recordStructuralMutation→markSubtreeLayoutMutation`（DocumentNode.java:517/540）对容器 append/removeChild 时**无条件向下递归**把未变兄弟及全部后代的 layout/subtree 版本刷新 → layout 层 `resolveReusableLayoutBox` 唯一复用闸门（DocumentLayoutEngine.java:404-434 version 门）判定复用失败 → 未变兄弟被真实重算。**无下层兜底缓存**（LayoutContext 6 缓存都是 pass 内 memo 不跨帧）。**reconciler 已最优**（KeyedListReconciler LIS 稳定项零 DOM 操作），债**完全在 DOM 层标脏粒度**。
- **裁决：批次 0 有条件通过，方向 A 继续**。依据：① 债不在 control 层（forEach 复用/声明式事件/reconciler 都做对了，失败在 control 层无法触及的 DOM 层）；② 债**先验存在**（原全量重建同样整树标脏，被"反正都要重建"淹没；forEach 复用后才暴露）；③ 正确性无损（重算结果与跳过一致），属性能局部债；④ 用下游地基债阻断上游协议化 = ROI 错配。**I7 断言降级**（选项 2）：保留 assertSame 复用断言（必过）+ 新增 `documentsKnownCoarseSubtreeDirtyMarkingDebt` 断言"债当前存在"作回归锚点（DOM 修复后翻转提示）。**债已登记**：`docs/开发者文档/errors/ERROR-20260617-dom-coarse-subtree-dirty-marking.md` + README 索引 + NORTH_STAR 偏离登记（I7，回填计划＝reconciler 批量提交 API 绕过粗粒度标脏）。
- **协议设计输入（批次 0 真正产出，给后续批次）**：① forEach 单节点约束 → 需 wrapper 包裹（DOM 深度+1）；② separator 只能 prepend（与原 append 顺序相反，影响 `:first/:last-child`）；③ UiDisplay 缺 INLINE_FLEX（wrapper 用 FLEX，非 flex 父容器下换行有差异）；④ onAction 在 forEach 项回调内自动归属项作用域、自动退订——**值得固化为协议**；⑤ 影子字段保同步契约（getPath）= signal 化控件对外同步读 getter 的通用配方。
- **DOM 地基债修复方向（oracle，待批次 2 真机实测定优先级）**：方向 1（推荐，中等工程量 200-400 行）= 新增 `ElementNode.reconcileChildren(List<ChildOperation>)` 批量提交 + reconciler 收集 ops 一次提交 + recordStructuralMutation 精确判定"结构变化项必标脏 / 引用复用稳定项只标容器自身不递归"。layout 层不改。方向 2（重构 markSubtreeLayoutMutation 按需传播，跨层耦合撞 I6）不推荐。**后续批次受影响**：批1 低（项少<10 帧预算内淹没）；批2 **中高需真机帧率实测**（TreeView 100 项增删一项→99 项重算，但仍比原全量重建轻：复用对象+重算 layout vs 销毁重建+new+GC）；批3/4 低中。
- **下一步（需用户中文 question 拍板后用 deepwork 续编排）**：① **批次 0 真机验证交用户跑**（MODCFG 嵌套分类页面包屑导航点击回跳正常 + 帧率，沙箱无 GUI；入口 runClient21 → `/qzuilib` 进配置页 → 嵌套 map 分类用面包屑导航；真机后派子代理查 `run/client/logs/fml-client-latest.log`、`club.heiqi.uilib` 零异常）；② 真机通过后**启动批次 1**（简单展示控件 Tooltip/Tab/KeyValueEditor 批量迁移 + 协议固化，连续 3 控件无需改协议签名 = 协议稳定，扩展守护测试"control 层禁止 clearChildren+重建"）；③ **批次 2 前加真机帧率实测门**决定 DOM 地基债是否插队修复。
- **重要纪律（沿用）**：决策点优先中文 `question`；帧率/真机实测必交用户跑（沙箱无 GUI），真机后派子代理查日志；PowerShell 提交/合并信息用 `git commit -F <文件>`/`git merge -F <文件>` + UTF8 无 BOM（heredoc/多行 `-m`/`Set-Content`/直接 `--no-ff` 不带消息文件均会炸）。deepwork 长周期重构进度在 `.slim/deepwork/control-reactive-refactor.md`。

## 最近一次交接（2026-06-17 第 32 次）—— 控件层架构重构研究启动（阶段 A 备料完成，阶段 B 待开新 oracle session 诊断）

- **当前分支**：`4.0`，HEAD `663565f3`。工作树干净。领先 `origin/4.0` 188 提交，未推送。`refactor/textarea-incremental-sync` 空分支（TextArea 战术补丁已取消，无提交）。
- **上下文转移**：本会话上下文成本已过高（35 万+ tokens）。新会话从本交接条目 + 下方完整接力提示词启动，**直接开新 oracle session 做架构诊断**，省去前期讨论和备料调查的 token 重复开销。
- **关键转折（用户判定）**：第 30 次 oracle 指出 TextArea 卡顿根因是控件库层 `DocumentTextAreaControl.syncContent` 每 keystroke 全量 DOM 重建。最初尝试做战术补丁（行级 setText 增量 + value lazy-flush），**用户否决了当前抽象**——"每个控件都像这样来点自己特殊的东西透到整树上后期维护压力太大，并不认可当前抽象，开长周期重构"。
- **阶段 A 备料（本轮完成）**：两条并行 lane 产出内部现状画像 + 外部框架参照系：
  1. **explorer 全景画像**：control 层 74 文件 / ~13,267 行 / 26 个主体控件零继承零共享接口 / 9 个控件走 clearChildren + 全量重建 / markSubtreeMutated 在 ElementNode 层自动触发 / ReactiveControlBindings 仅桥接 2 控件 / CodeEditor(1268行) + TextArea(999行) 合计 2267 行有大量逐字复制。最痛点三重叠加：TextArea、CodeEditor、Autocomplete。
  2. **librarian 外部框架研究**：对比浏览器原生/React/SolidJS/Flutter/SwiftUI/Compose。推荐方向 = **SolidJS 模式（一次创建 + 细粒度绑定） + Flutter canUpdate 决策（配置层 vs 实例层分离）**。核心发现：我们架构距离 SolidJS 只差"控件层统一采用已有 signal/effect + forEach/show/bindText 原语"这一步。不推荐引入 VDOM diff。
- **阶段 B 待做**：开**全新 oracle session**（不复用 ora-1，它沉浸在 TextArea 战术细节里，架构建诊断需要高视角），基于两份报告 + NORTH_STAR 产出：① 当前 control 层抽象的精确缺陷诊断（对照宪章 I1-I9 + 反模式）；② 重构方向选项与 tradeoff 分析；③ 推荐方向的第一步切口（最小可验证形态）；④ 分期路线图与风险控制。
- **阶段 C/D**：把 oracle 方向选项整理成交用户的中文 question 拍板 → 方向定后用 deepwork 编排分批实施。
- **本条目附加完整接力提示词**（见下方分隔线后），可直接复制给新会话的 Agent 启动阶段 B。提示词含项目环境、架构结论、两份报告的精华压缩、NORTH_STAR 关键条款对照、以及给新 Agent 的明确指示。

---

## 最近一次交接（2026-06-17 第 31 次）—— 核心不变量自动化守护测试落地（I6 依赖方向绿灯 + 契约线提纯度量仪）

- **当前分支**：`4.0`，已合入。merge `9f6b227f`（`--no-ff` 合 `test/layer-contract-guard`）+ test 源 `97f16361`。**已 `git show HEAD:` + `git ls-tree` 双验** `src/test/java/club/heiqi/uilib/ui/LayerContractGuardTest.java` 在 HEAD 树。工作树干净，`compileTestJava` 通过（26s），测试类 2 方法实跑（XML 实证 `tests=2 failures=0`，非空跑）。本轮**零生产源码改动**（一个新测试 + 本交接更新）。
- **本轮（用户拍板「按高价值目标第一项开工」）**：经 ora-1（复用 session）战略排序，数据层主战场①②③核心已补完、无大鱼级新缺口，价值重心从「补缺口」转向「守护已建成地基不退化」。第一项 = 把 I1-I9 里反复靠人工 grep + 交接口述守护的分层铁律资产化为可执行、可 CI 阻断的源码扫描断言。低成本、零风险、为后续候选 C（I6 契约线提纯）铺路。
- **交付 `LayerContractGuardTest`（复用 `Lwjgl3ifyInputBackendTest` 的 `Files.walk("src/main/java")` 静态扫描范式，JUnit4，无真机/GUI 依赖）**：
  - **方法一 `shouldKeepComponentReactiveDomLayersFreeOfControlLayerImports`（绿灯基线）**：扫 `ui/component`、`ui/reactive`、`ui/dom` 三包，断言**无** `import club.heiqi.uilib.ui.control.`。这正是历次交接反复手工 grep 的 I6 依赖方向铁律（控件层在三层下游，反向 import 即契约穿透），资产化为绿灯。当前为真。
  - **方法二 `shouldTrackPaintLayerElementNodeViolationBaseline`（契约线提纯度量仪）**：扫 `ui/paint` 包 import `ElementNode` 的文件集合，与已知违反基线 `KNOWN_PAINT_ELEMENT_NODE_VIOLATIONS`（当前 **7 个**：DocumentCustomRenderBounds/DocumentCustomRenderSurface/DocumentPaintCommand/DocumentPaintEngine/DocumentPaintPlan/DocumentPaintRenderer/DocumentScrollbarThumbReplay）做**精确集合相等**双向断言——新增违反 → 红（阻断 I6 债扩散）；提纯移除某文件 → 也红（提示从基线删除、向下收敛直至清零）。**这条测试即候选 C 的进度度量仪**。
  - **健壮性**：路径正斜杠归一化（消除 Win/*nix 分隔符差异）；`listJavaFiles` 空扫描自检 `assertFalse`（防包重命名后静默通过）。已知弱点：源码文本扫描对包重命名敏感会误报，相对人工口述守护可接受。
- **⚠️ ora-1 点出的关键前置（未确认）**：守护测试要真正进 **CI 持续跑**才有阻断意义；若 CI 不跑 `test`，它退化为「一次性核对」、价值打折。**需用户确认 CI 是否执行 `test` 任务**。
- **下一步候选（ora-1 战略排序，需用户中文 question 拍板）**：① **候选 C / I6 契约线提纯**（宪章唯一明确点名的债，第 10 节）：`DocumentPaintCommand` 字段+构造器直接持 `ElementNode`、`getElement()` 公开给渲染层，`DocumentPaintRenderer.java:1107-1204` 十余处 `getParent()` 上溯做 table/row 关系遍历——把这些关系在 Paint 阶段预解析进命令的平台无关字段，渲染层不再回查 DOM。**高成本、回归面大、须切薄片渐进，本轮守护网已就位可兜底**；切入前需对 DocumentPaintRenderer/Engine 那约 30 处 `getElement()` 用法做专门侦察确认提纯路径可行。② forEach 生产扩面（候选 B，`ModernSimpleListPropertyBinding.refreshListView` 迁 forEach，注意保焦点）。③ 切口 1 真机帧率验证（第 29 次优化收益实测，仍待用户跑）。
- **此刻不值得做**（ora-1 判定）：候选 A 的 dynamic/switch、TextInput/Slider bridge（无消费者 YAGNI）、batch（flush 已是隐式事务边界、无人喊痛）、候选 D 二次性能优化（真机 profile 前置未满足）；C4 / focus 完整 signal 化 / 切口 2 已沉淀决策文档判定否决，不再提。
- **重要纪律（沿用）**：决策点优先中文 `question`；帧率/真机实测必交用户跑（沙箱无 GUI），真机后派子代理查 `run/client/logs/fml-client-latest.log`、`club.heiqi.uilib` 零异常；PowerShell 提交信息用 `git commit -F <文件>`（heredoc/多行 `-m`/`Set-Content` BOM 均会炸）。

## 最近一次交接（2026-06-17 第 30 次）—— C4/focus signal 决策沉淀 + 切口2/listbox bump 两观察项结案（纯文档/纯评估，零源码）

- **当前分支**：`4.0`，已合入。merge（`--no-ff` 合 `docs/c4-focus-signal-decision`）+ docs 源 `4a27a2a8`。**已 `git show HEAD:` 双验** `docs/记忆/决策/DECISION-20260617-scroll-focus-no-signalization.md` 在 HEAD 树、README 索引项已合入。本轮**零源码改动**（一篇决策文档 + 本交接更新）。工作树干净。
- **本轮三项交付（用户三项全做）**：
  1. **C4/focus signal 决策沉淀（明确待办，第 28 次留的尾）**：新增 `DECISION-20260617-scroll-focus-no-signalization.md`，把第 28/29 次反复评估的两条结论固化进决策层，避免后续会话重复走裁决——① **C4（scrollState signal 化）否决，方案 A（全 DOM 改坐标）不回退**：滚动偏移不改 DOM 节点属性、是渲染层视口态（信条一下半句），I1 无缺口；signal 化撞 I6/帧末批处理铁律、倒退 I7/I8、与第 10 节「滚动归合成线程」相悖；方案 A 是 pre-RenderingNG 淘汰模型，方案 B 虚拟视口才是 Blink/WebKit 真实实现；② **focus 完整 signal 化不做**：`focusEpochSignal` 唯一消费者 cursor 上零收益，维持过渡桥，**触发升级的唯一条件 = 出现 cursor 之外的第二个 focus 投影消费者**（如 `:focus` 边框高亮）；③ YAGNI 边界：不预建空桥，真需响应 scroll 的数据层 effect 时按 `focusEpochSignal` 模式即时建桥。
  2. **切口 2（input 热路径进阶缓存）评估 —— oracle 裁定不做（YAGNI）**：那两次 hitTest 在 `onMouseUp` **离散事件路径**（频率=点击，非 60fps 帧热路径），中间隔着可改文档态的用户回调，第二次 `refreshHover` 语义本就是「回调跑完后据最新态重评估 hover」、刻意重算。提案缓存键 `(x, y, rootBox 实例, scrollVersion, 动画态时间)` **漏三个失效维度**：top-layer 版本（`resolveTopLayerLayoutBoxes` 集合可因 active/hover 增删）、不改布局的 DOM 结构/可命中性变更（`rootBox` 实例相同 ≠ 命中态相同）、hover 影子态本身（与第二次 hitTest 的存在目的直接冲突）→ 边界 case 静默返回陈旧命中，撞反模式「缓存不失效」。撞宪章第 6 节判据（答不上「跳过哪层帧级重算」，跳过的只是事件级一次遍历）；误把 I7（渲染三阶段脏标记）套到查询路径。**结论：切口 1 真机数据出来、profile 指认 hitTest 为热点前，按 YAGNI 搁置，不做坐标键控缓存。**
  3. **`syncAnchoredListboxTopLayerPlacement` bump 观察项核查 —— 不是真债，观察项关闭**：追到根因 `UiStyleDeclaration.updateProperty`（cascade/UiStyleDeclaration.java:1834）`boolean changed = !Objects.equals(previousValue, nextValue)`，**仅 `changed` 为真才 `recordChange(impact)`**。`setLeft/setTop/setWidth/setPosition`/`clearZIndex` 全部经 `updateProperty`，**对相同值去重**。稳态下下拉打开、anchor 不动时重复写入相同 left/top/width/position 一律被去重 → **不 bump layoutVersion → 不会每帧 relayout**。只有 anchor 几何真变化或 `position` 首次从非 FIXED 变 FIXED 时 bump 一次（正确且必要）。**无需立任务。**
- **subagent 协作记录**：ora-1（oracle）复用做切口 2 评估，结论完整可信；候选 3 排查派 explorer 三次均只回半截中间状态（session 不可靠），改由**主 Agent 直接追完代码链路**（有界追踪、自己读更可靠），未浪费更多 lane。
- **未动**：本轮零源码、零测试改动，所有铁律自然保持。第 29 次的真机帧率验证**仍待用户跑**（沙箱无 GUI，入口 `/qzuilib test`，真机后派子代理查 `run/client/logs/fml-client-latest.log`、`club.heiqi.uilib` 零异常）。
- **下一步候选（需用户中文 question 拍板）**：① **切口 1 真机帧率验证**（最高优先，第 29 次优化的收益尚未实测，是后续是否再优化的决策前提）；② 若真机后 profile 仍指 hitTest 为热点，再评估 `onMouseUp` 内条件复用第一次 `releasedElement`（坐标+版本未变时）——但**不是**加坐标键控缓存（已判 YAGNI）；③ 其余历史候选（focus 完整 signal 化）已沉淀进决策层判定零收益，除非出现第二消费者不再提起。
- **重要纪律（沿用）**：决策点优先中文 `question`；帧率/真机实测必交用户跑（沙箱无 GUI），真机后派子代理查日志；PowerShell 提交信息用 `git commit -F <文件>`（heredoc/多行 `-m`/`Set-Content` BOM 均会炸）。

## 最近一次交接（2026-06-17 第 29 次）—— input 热路径命中查询复用 bounds query 快路径（I7 兑现，性能优化）

- **当前分支**：`4.0`，已合入。merge `c94e5991`（`--no-ff` 合 `perf/input-hotpath-bounds-query-reuse`）+ perf 源 `2960d026` + test `57fc7737`。**已 `git ls-tree -r HEAD` + `git show HEAD:` 双验** `HtmlLikeDocumentWidget.java` 的两处 `resolveLayoutBoxForBoundsQuery()` 改动 + 新测试 `HtmlLikeDocumentWidgetInputHotPathTest.java`（11 方法）均在 HEAD 树。工作树干净，compileJava 通过（19s）。
- **本轮决策过程（候选 B，经 oracle 战略裁决选定 + 三轮深评后落地）**：第 28 次列的下一步两候选，经 oracle 战略评估排序 **B（input 热路径优化）> A（focus 完整 signal 化）> C（paint 层 ElementNode 契约线提纯）**：
  - **A 应抵制**：`focusEpochSignal` 不是 C4 那种空桥（它有 cursor effect 真实消费者、注释已登记为过渡桥、现状合规），升级成完整 focus signal 化在唯一消费者 cursor 上**零收益**（effect 无论订阅 int bump 还是 ElementNode 都要回查 `focusManager.getFocusedElement()`），属与刚否决的 C4 同性质的「为响应式而响应式」、YAGNI。等真出现 cursor 之外的第二个 focus 投影消费者（如 `:focus` 边框高亮）再升级。
  - **B 是真实债非提前优化**：宪章第 9 节「重算起点太高」+ I7 未兑现 + 信条五铁律（input 高频路径全树 relayout = 热路径全量操作 = bug），有第 25 次真机实测 ~60× 放大坐实；前置条件「基础链路够完整」在第 27/28 次后（输入→状态→视图→渲染闭环完整）已满足。
- **本轮交付（用户拍板候选 B、切口 1、接受动画事件时机微调、listbox bump 单列观察项）**：oracle 三轮深评把切口 1 核死为**最小形态**——
  - **关键成本模型修正**：relayout **早已被 `resolveLayoutBox:1127` 版本缓存兜底**，input 热路径真实重复成本不在 relayout，而在 `resolveInteractiveLayoutBox`（widget:1202）每次无条件做的 `animationTimeline.updateFromLayout`（全树递归）+ `flushCompletedAnimationEvents`（带副作用，非幂等，会消费/剪除已完成动画）+ `resolveTopLayerLayoutBoxes`。而已上线的 `resolveLayoutBoxForBoundsQuery`（widget:1232，原为 bounds query 稳态快路径，其 Javadoc 实测旧实现占稳态渲染 CPU ~99.9%）稳态下本就跳过这两项。
  - **改动（仅 main 2 行 + 注释）**：`findElementAt`（widget:493→495）与 `findElementAtWithin`（widget:514→517）的 rootBox 来源从 `resolveInteractiveLayoutBox()` 改为 `resolveLayoutBoxForBoundsQuery()`。**零新增缓存字段**（relayout 版本缓存兜底，无需缓存 rootBox）。`findElementAtWithin` 内 `topLayerBoxes = resolveTopLayerLayoutBoxes(rootBox, null)` 获取行保留（518/523 用到，编辑时一度误删已补回）。**topLayerBoxes 每次重算不缓存**（`syncAnchoredListboxTopLayerPlacement` 的 style 写入是 anchored 下拉定位正确性的必要副作用）。
  - **行为微调（用户知情接受）**：稳态下 findElementAt 不再 `flushCompletedAnimationEvents`，动画完成事件派发**唯一化到绘制管线** `resolvePaintCommands`（widget:981，每帧 drawSelf 已派一次）。极端场景「某帧只有 input 无 drawSelf」完成事件延迟到下一帧——与已上线的 `resolveLayoutBoxForBoundsQuery` 同款权衡，真实运行 drawSelf 每帧都跑、不构成回归。运行态（有 LAYOUT/transform 动画）`hasLayoutRuntimeValue()` 为真时**回退完整 `resolveInteractiveLayoutBox`，行为完全不变**。
- **过 NORTH_STAR 第 8 节**：**I7 兑现**（input 命中查询稳态摊销为 O(1) 版本比对，跳过重复全树动画推进）✓ I1/I3（未碰 signal/effect/cursor 路径）✓ I6（未碰渲染层契约）✓。命中语义零改动（稳态两路径返回同一 `resolvePaintLayoutBox(false)` 结果，hitTest 输入等价）。
- **验证**：compileJava 通过；新增 `HtmlLikeDocumentWidgetInputHotPathTest` **11 个全绿**（A 命中等价×3：稳态同坐标返回同元素/多层嵌套命中最深+边界外 null/运行态回退完整路径；B 回调内改 DOM×3：removeChild 自身/`display:none`/删兄弟后命中正确；C 滚动后命中×3：滚轮滚动/连续查询一致/`scrollIntoView` API 读最新 scrollState；E 动画完成事件×2：findElementAt 不派+render 恰派一次/多次查询不重复派）。**D 计数探针按约束跳过**（`animationTimeline` 无公开调用计数 API，需侵入生产代码加计数字段，按「不为测试污染生产」原则放弃）。广范围定向回归（document/layout/paint）**327 tests 唯一失败 `shouldDegradeSystemCursorHostWhenBackendFails`（预存环境敏感，零回归基准内，勿动）**，**零新增回归**。
- **未动（按铁律）**：scroll→hover 同步管线（790-798，第 28 次测试守护）、`dispatchLatestScrollIfChanged` 同步语义、enter/leave 同步配对、hover/press 影子契约、`commitInteractionSignals` 帧首时序、`resolveInteractiveLayoutBox` 本体（运行态仍走它）、`Signal.set` 去重守卫。
- **⚠️ 真机帧率待用户跑 runClient 验证**（沙箱无 GUI）：入口 `/qzuilib test`；预期 input 帧 render 从第 25 次实测的 ~12.9ms 回落。真机后派子代理查 `run/client/logs/fml-client-latest.log`，关注 input 帧 render、`club.heiqi.uilib` 零异常。
- **后续观察项（本轮单列、未处理）**：`syncAnchoredListboxTopLayerPlacement`（widget:1302-1325）每次写 anchored listbox 的 `setLeft/setTop/setWidth/setPosition`（LAYOUT 级 style），**若 `UiStyleDeclaration` 对相同值不去重**则有下拉打开时每次 `resolveTopLayerLayoutBoxes` 反复 bump layoutVersion → 每帧 relayout，是独立于切口 1 的潜在热路径债。需先确认 `setLeft/setTop/setWidth` 相同值去重语义，确认是真债再单列任务（YAGNI，本轮未扩大切口）。
- **下一步候选（需用户中文 question 拍板，勿自行大改）**：① **切口 2**（input 热路径进阶）：给 hitTest 加 `(documentX, documentY, rootBox 实例, scrollVersion, 动画态时间)` 坐标键控单帧缓存，消除 onMouseUp 内同坐标重复 hitTest（866 + 1460 两次）——**须切口 1 真机验证收益达标后再评估是否值其风险**（可能 YAGNI）；② 上述 `syncAnchoredListboxTopLayerPlacement` bump 观察项核查；③ focus 完整 signal 化（已三方判定 cursor 路径零收益，除非出现新消费者，不建议）。
- **重要纪律（沿用）**：决策点优先中文 `question`；帧率/真机实测必交用户跑（沙箱无 GUI），真机后派子代理查日志。

## 最近一次交接（2026-06-17 第 28 次）—— C4 经三方裁决否决 + 补 scroll→hover 脆弱管线测试守护

- **当前分支**：`4.0`，已合入。merge `7a85013a`（`--no-ff` 合 `test/scroll-hover-refresh-coverage`）+ test `9de008db`。**已 `git ls-tree -r HEAD` + `git show HEAD:` 双验** `HtmlLikeDocumentWidgetScrollTest.java` 在 HEAD 树，且新测试方法 `shouldDeriveCursorFromRehoveredElementAfterMouseWheelScroll` 与私有内部类 `RecordingCursorHost` 均在 HEAD。工作树干净，compileJava/compileTestJava 通过（4s）。
- **本轮决策过程（关键：候选②C4 经三方裁决否决，方向改为补测试）**：用户初选候选② `scrollState version++ → signal 依赖（C4）`，但经 explorer 侦察 + oracle 宪章裁决 + librarian 查 Chromium RenderingNG 官方文档**三方印证后否决了 C4 主体**，理由如下：
  - **I1 本就无缺口**：`DocumentScrollState.java:25` 注释「布局盒本身仍表达未滚动的文档坐标」——scroll offset **不改任何 DOM 节点属性**，是信条一**下半句（渲染层职责）**而非上半句（数据层）。这条链路里唯一真界面投影 cursor **第 27 次已走 signal**；滚动后的 `updateHoveredElement` 与 enter/leave 同类，属「必须同步配对的事件派发」，本就该命令式。**不存在第二条改 UI 路径，I1 没缺口**。
  - **撞 I6**：`DocumentScrollState` 在 `ui.layout`（渲染层⑤），`Signal` 在 `ui.reactive`（数据层）。scrollState 持 signal = 契约穿透。唯一合规形态是 widget 持桥（同 `focusEpochSignal`），**但这是座空桥**——scroll 影响 cursor 已被 hovered 影子覆盖，无第二个需订阅 scroll 的 effect，建了无消费者。
  - **倒退 I7/I8**：`cachedPaintScrollVersion`/`cachedBoundsIndexScrollVersion` 是脏标记模范实现，`scrollDependenciesSatisfied()`（widget:1030）做**逐元素 offset 比对**，比 signal 更精细。signal 化反而降低失效精度。
  - **撞铁律 + 与第 10 节相悖**：`dispatchLatestScrollIfChanged`（widget:1476）同步读 version 判「是否滚动」，signal 帧末批处理会让同帧 get 取旧值→永远 `return false`→断裂 scroll→hover。且宪章第 10 节明确把**滚动归合成线程**，signal 化等于往 UI 线程依赖图焊一个宪章想推到合成线程的东西。
  - **用户追问「回退到最初的全 DOM 坐标方案 A 是否更合适、哪个更像浏览器」**：oracle + librarian（Chromium RenderingNG 官方文档）一致裁决 **方案 B（虚拟视口）= Blink/WebKit 真实实现，方案 A（全 DOM 改坐标）是 pre-RenderingNG 已淘汰模型**（O(n) 全量 reflow，Slimming Paint 项目专门删了 22000 行此类代码）。原文佐证：「Scroll: update the scroll offset... by mutating property trees」（layout tree 不动）、「scroll offset... lives in a paired transform-tree node」（存 transform 节点非 layout）、滚动跑 compositor thread。**用户当年 A→B 是向浏览器正确实现靠拢，方案 A signal 化是「用四条信条（信条五/I7/I8/第10节）的实质违反，换一条本就不缺的 I1 形式合规」，巨亏，不回退**。
- **本轮交付（用户拍板「补 scroll→hover 集成测试」）**：oracle 指出真正的债是那条交接反复强调「勿历史回退」、却**无测试守护**的 scroll→hover 脆弱管线。亲自核对发现 `HtmlLikeDocumentWidgetScrollTest.java:915` 已有 `shouldRefreshHoverAfterMouseWheelScrollMovesContent`（覆盖重命中 + enter/leave 配对前两段），**正缺第 27 次新引入的脆弱点：flush 后 cursor effect 据新 hovered 派生**（scroll→影子→signal→cursor 闭环）。故新增聚焦 cursor 闭环的测试，而非从零写。
  - **新增测试 `shouldDeriveCursorFromRehoveredElementAfterMouseWheelScroll`**（ScrollTest，单文件）：可滚容器内 first（无 cursor）+ second（`setCursor(POINTER)`），鼠标固定 (10,10) 初始 hover first → flush 后 cursor=DEFAULT；滚轮 -120 使 second 移到鼠标下 → flush 后 cursor=POINTER。钉死「滚动后同步影子→signal→cursor 闭环未被破坏」。复用 CursorTest 的 `RecordingCursorHost` 模式（`applyCursor` null 归一 DEFAULT、`getLatestCursor()` 取末项）。补 `DocumentCursorHost`/`UiCursor` import。
- **验证**：compileJava/compileTestJava 通过；ScrollTest + CursorTest 共 **38 tests 唯一失败 `shouldDegradeSystemCursorHostWhenBackendFails`（预存环境敏感，零回归基准 8 项之内，勿动）**，新测试通过。**零新增回归**。
- **未动（按铁律）**：scrollState 保持命令式 version++ + version 影子同步契约（按铁律不可 signal 化）、`dispatchLatestScrollIfChanged` 同步管线、`cachedPaintScrollVersion`/`cachedBoundsIndexScrollVersion` 脏标记签名、`Signal.set` 去重守卫、`UiComponentRuntime` 零 `ui.control` import（I6）、enter/leave 同步配对。**本轮零 main 源码改动，纯新增测试**。
- **架构结论沉淀（重要，避免后续重复评估）**：① **C4（scrollState signal 化）已三方否决，不做**；② **方案 A（全 DOM 改坐标）已淘汰，不回退**；③ 方案 B 虚拟视口 = 浏览器正确实现，滚动是渲染层视口态/合成线程职责，不归数据层 signal 图；④ 若未来真出现「需响应 scroll 的数据层 effect」，按 `focusEpochSignal` 模式即时建桥即可，不预建空桥（YAGNI + 宪章第 6 节判据）。建议把此结论同步进 `docs/记忆/决策/`。
- **下一步候选（需用户中文 question 拍板，勿自行大改）**：① **focus 完整 signal 化**（第 27 次约定的未来预案，epoch 桥已就位）：把 `DocumentFocusManager`（527 行，含 focusin/out 派发、Tab 遍历、scrollIntoView）的 focus 态 signal 化，替掉 `focusEpochSignal` 过渡桥。注意 focus 派发同样需区分「该走 effect」vs「必须同步配对的 DOM 事件」；② **input 热路径单帧缓存优化**（第 25 次坐实根因：`onMouseUp` 单事件 `findElementAt`/`resolveInteractiveLayoutBox` 被调多次、每次全树 hitTest+relayout；与响应式化正交，属性能优化，须先确认「基础链路已够完整可优化」）。
- **重要纪律（沿用）**：决策点优先中文 `question`；帧率/真机实测必交用户跑（沙箱无 GUI），真机后派子代理查 `run/client/logs/fml-client-latest.log`。

## 最近一次交接（2026-06-17 第 27 次）—— 输入层交互态 signal 化：hover + press 最小切片（cursor 全走 effect 派生，I1 落地）

- **当前分支**：`4.0`，已合入。merge `66e910a3`（`--no-ff` 合 `feat/reactive-interaction-state`）+ refactor 源 `8e414241` + test `584ab97b`。**已 `git ls-tree -r HEAD` + `git show HEAD:` 双验** `HtmlLikeDocumentWidget.java`/`DocumentFocusManager.java` 在 `4.0` HEAD 树，且 `hoveredSignal`/`pressedSignal`/`focusEpochSignal`/`interactionOwner`/cursor effect/`commitInteractionSignals`/`bumpFocusEpoch`/`onFocusChangedForCursor` 均在 HEAD。工作树干净，compileJava 通过（27s）。
- **本轮（用户拍板方向，仅差落地）**：输入层交互态 signal 化最小切片（hover + press）。红线：不照搬现有命令式 dispatch，用本项目 signal/effect 范式重新表达交互态；cursor 全走 effect 派生（消除散落命令式 `syncCursorFromHoveredElement` 改 UI 路径，这才是 I1 真落地）；enter/leave DOM 事件派发保留同步逐次配对；focus 暂留裸字段、走 epoch 桥（未来预案）；cursor 接受一帧延迟、只在 `drawSelf` flush。
- **设计要点（已想透时序陷阱）**：
  - **保留** `hoveredElement`/`pressedElement` 裸字段作**同步影子状态**——服务「单事件内同步取当前值」（enter/leave 配对读上一个 hover、`beginDragIfNeeded` 同帧读 pressedElement、`resolveClickTarget` 读 previousPressed）。**不能 signal 化**：`Signal.set` 帧末批处理 + 对已应用值去重，set 后同帧 get 取旧值，破坏同步契约。
  - **新增** `hoveredSignal`/`pressedSignal`（`Signal<ElementNode>`）作 cursor 投影的响应式事实源；`focusEpochSignal`（`Signal<Integer>`）作 focus 变化的反应式桥（`focusElement` 末尾改 bump epoch 而非命令式 applyCursor）。
  - **新增** `interactionOwner`（`Owner`），构造期建**唯一 cursor effect**：`interactionOwner.createEffect(裸 Runnable)`（读三 signal 建订阅 → `cursorHost.applyCursor(DocumentCursorResolver.resolve(...))`）。**关键**：用 `interactionOwner.createEffect` 而非 `componentRuntime.createEffect`——后者按 impact `markXxxDirty` 会污染 document 脏标记触发重排/重绘，cursor 投影不该如此。`close()` 里 `interactionOwner.dispose()`。
  - **规避时序陷阱**：单帧 hover 可能 A→B→A（一帧多 MOVE 事件），逐次 set 会被去重拦截致 signal 错停在中间值。**正解**：`drawSelf` 帧首 `commitInteractionSignals()`（把影子终值提交进 signal）**先于** `componentRuntime.flush()`（同一全局 `ReactiveScheduler` 单例，flush 一并应用 hover/press signal 并重跑 cursor effect）。
  - **删除散落命令式 cursor 调用**（原 6 处 `syncCursorFromHoveredElement`）：hover 路径（`updateHoveredElement` 内 2 处）、press 路径（`onMouseDown`/`onMouseUp`/`releasePressedElement` 各 1 处）改为只更新影子字段，cursor 交 effect；focus 路径（`DocumentFocusManager.Host` 回调）改 `bumpFocusEpoch`，接口方法 `syncCursorFromHoveredElement` → `onFocusChangedForCursor`（带 javadoc 说明「仅 bump 反应式桥、勿命令式 applyCursor」）。`setCursorHost` 保留**一次同步应用** `applyCursorSynchronouslyForHostSwap()`（首帧 DEFAULT 语义，javadoc 标注「仅用于换宿主、勿用它改 UI 态」）。
- **过 NORTH_STAR 第 8 节**：**I1 真落地**（cursor 只经 signal/effect 派生，不存在第二条改 cursor 命令式路径）✓ I3（cursor effect 归属 interactionOwner、close 时随 owner dispose 不泄漏）✓ I6（未碰渲染层）✓ I9（单帧多 MOVE 影子终值帧首提交一次、flush 合并重跑）✓。cursor effect **不打 document 脏标记**（不走 componentRuntime.createEffect），cursor 投影不触发重排/重绘。内存：仅多 3 个 signal + 1 个 Owner + 1 个 cursor effect 订阅，换来 cursor 响应式派生。
- **验证**：compileJava 通过；`HtmlLikeDocumentWidgetCursorTest` 7/8 过（唯一失败 `shouldDegradeSystemCursorHostWhenBackendFails` 是**预存环境敏感失败**、勿动）。**测试断言口径调整**：cursor 改 effect 驱动后只在 flush 应用 + Signal 去重合并中间值 → 原索引断言（`get(0)/get(1)/get(2)`）须在事件与断言间插帧绘制触发 flush。新增 package-private 测试 helper `flushInteractionFrameForTest()`（仅 `commitInteractionSignals()` + `flush()`，**不走 render 路径**、规避 LWJGL native 沙箱缺失），CursorTest 受影响方法均在事件后插帧。**enter/leave 同步断言（`HtmlLikeDocumentWidgetEventDispatchTest`）全过**——验证 enter/leave 保持同步配对未被 signal 化波及。定向回归（document/reactive/control/layout 范围）568 tests 仅 1 预存环境失败，**零新增回归**（DocumentPaintRendererTest 7 字体后端失败本机未触发、属环境差异）。
- **未动（按铁律）**：`Signal.set` 去重守卫与同步契约（正是保留同步影子字段的根本原因）、`UiComponentRuntime` 零 `ui.control` import（I6）、enter/leave 等需严格配对的 DOM 事件派发保持同步、`scrollVersion` 产生与消费、`UiStyleDeclaration`/`ElementNode` 核心写入路径、`resolveBoxContext`/坐标语义。
- **⚠️ 真机视觉/帧率待用户跑 runClient 验证**（沙箱无 GUI）：入口 `/qzuilib test`；验证 hover/press/focus 切换时 cursor 正确（一帧延迟可接受、只在 drawSelf flush）；select popup 关闭后 cursor 刷新正常。真机后派子代理查 `run/client/logs/fml-client-latest.log`，`club.heiqi.uilib` 零异常。
- **下一步候选（需用户中文 question 拍板）**：① 把 focus 完整 signal 化（本轮预案，已约定随时准备进行）；② scrollState `version++` → signal 依赖（C4，注意保留「滚动后指针刷新」管线、勿历史回退）；③ input 热路径单帧缓存优化（`onMouseUp` 单事件 `findElementAt`/`resolveInteractiveLayoutBox` 被调 2 次，第 25 次单列观察项，与响应式化正交）。
- **重要纪律（沿用）**：决策点优先中文 `question`；帧率/真机实测必交用户跑，真机后派子代理查日志。

## 最近一次交接（2026-06-16 第 25 次）—— 候选①事件绑定封装：补数据流「输入半环」（DOM 事件 → signal）+ 控件 bridge

- **当前分支**：`4.0`，已合入。merge `c448aaf2`（`--no-ff` 合 `feat/reactive-event-binding`）+ feature `2f5848d3`（源）+ test `f980b0f5`（测试）。**已 `git ls-tree -r HEAD` + `git show HEAD:` 双验** `ReactiveControlBindings.java` / `UiComponentRuntimeOnTest.java` / `ReactiveControlBindingsTest.java` 确实在 `4.0` HEAD 树，且 `on/onClick/Binding`（runtime）+ `onAction/onToggle`（bridge）API 均在 HEAD，**I6 守住**（`UiComponentRuntime` 零 `ui.control` import，grep 检查为空）。工作树干净，compileJava 通过。
- **开工前真机日志诊断（关键，推翻了「泄漏」假说）**：用户先让查最新 `fml-client-latest.log`（23:00-23:03 会话）。子代理证实 demo **真机跑过、`club.heiqi.uilib` 零异常、优雅停服**，但单次会话内 **render 累积 ~10×（0.7ms→7-8ms）、交互帧 input 累积 ~60×（0.2ms→12.9ms），结构 widgets/depth/hitTests 恒定不变**——初判疑似 effect/监听器泄漏。**但深度静态诊断子代理逐一证伪了「按 elementUid/节点引用只增不减的缓存」假说**：scrollState/animationTimeline/cachedBoundsIndex/topLayer + reactive 的 subscribers/dependencies/Owner.children/effects **全部有正确失效、无泄漏、不违反 I7/I8**。**真因是两条工程效率问题（非泄漏）**：① **demo 的 `addTask` 加行从不回收**，活 DOM 节点数随「添加」单调增长 → 布局/paint/hitTest 全 O(活节点数) → render 10×；② **input 热路径 `onMouseUp` 单次事件里 `findElementAt` 被调多次**（releasedElement + updateHoveredElement + finally refreshHover），每次都 `resolveInteractiveLayoutBox` + 全场景 hitTest 递归整棵布局树，点击刚改 signal 致 layoutVersion 变、每次 findElementAt 可能全量 relayout → input 单帧全树遍历次数是 render 的 3-6 倍 → input 60×。`hitTests=2`/`widgets=2` 恒定不矛盾：那是 widget 层指标，文档内部 DOM 的 hitTest 递归未计入。**结论：地基生命周期管理（dispose 链、缓存失效）干净，符合宪章；累积是 O(N) 规模 + input 热路径重复全树遍历的工程效率问题。**
- **本轮（用户拍板候选①）**：补数据流目前缺失的「输入半环」（DOM 事件 → signal，此前 demo 里 toggle/button 仍命令式直调 `setActionHandler`）。设计取舍均经中文 `question` 拍板：① 主交付=**事件绑定封装**，input 热路径优化单列为观察项（本轮不动）；② API=**通用注册-注销对抽象 + 薄便利糖**；③ 分层落点=**通用 `on` 入 `UiComponentRuntime`，控件糖入新 bridge 类**（因 `ui.control` 单向依赖 `ui.dom`、`UiComponentRuntime` 受 I6 约束绝不能 import `ui.control`，故控件糖不能进 runtime）；④ 注销机制=**`on` 内部 `Owner.current().onCleanup` 自动注销**；⑤ demo **一并迁为新 API 并补测试**。
- **改动（component 1 改源 + control 1 新源 + devtools.pages 1 改源 + 2 新测试，零核心 reactive/DOM 改动）**：
  - **`UiComponentRuntime` 新增 `on(Runnable register, Runnable unregister)`（组件层，只碰 dom+reactive）**：`register.run()` 立即订阅 → `Owner scope = Owner.current()`（null 取 rootOwner）→ `scope.onCleanup(binding::dispose)` 登记退订。**作用域卸载时自动退订，订阅-退订成对出现，从构造上杜绝悬挂监听器**（无论订阅在构造期根作用域还是 forEach/show 的项/内容作用域内建立）。+ DOM 级便利糖 `onClick(ElementNode, Runnable)`（只碰 ui.dom，留组件层守 I6，左键 button==0 触发并消费、其余冒泡）。+ 内部类 `Binding`（持「最多执行一次」的退订回调，`boolean disposed` 守卫保证幂等——自动退订 onCleanup 与手动 `dispose()` 可任意先后/重复，只退订一次）。
  - **新增 `ReactiveControlBindings`（`ui.control` 控件 bridge 工具类）**：`onAction(runtime, DocumentButtonControl, Runnable)` + `onToggle(runtime, DocumentToggleSwitchControl, Consumer<Boolean>)`，把控件命令式 `setActionHandler/setChangeHandler` 包装进 `runtime.on`，使退订随 Owner 作用域自动发生。**这是一条新引入的单向依赖 control → component，无环**（component 受 I6 不能反向 import control，故糖落控件层）。
  - **`ReactiveTriadDemoView` 迁移**：4 个构造期按钮/开关（add/remove/shuffle/detailsToggle）改用 `ReactiveControlBindings.onAction/onToggle`，归属 rootOwner，由 `constructionBindings` 列表做防御性显式退订（幂等，与 rootOwner 自动退订不冲突）；**行内 `toggleButton` 的 onAction 归属 item owner——行被移除时该行 owner dispose 自动 `setActionHandler(null)`，根除潜在悬挂监听器**（这是「在新 API 里解决泄漏」的关键落点，虽然诊断表明本就无真实泄漏，新 API 从构造上杜绝了未来悬挂）。
- **过 NORTH_STAR 第 8 节**：**I1 强化**（事件回调内只改 signal，输入半环纳入 signal 驱动，无第二条改 UI 路径）✓ I3（on 经 `Owner.current()` 归属作用域、不在组件函数体外乱建副作用）✓ **I6 守住**（runtime 零 `ui.control` import；onClick 只碰 ui.dom；控件糖落控件层 bridge）✓ I7/I8（未碰缓存失效逻辑）✓。内存：每个绑定仅多一个 Binding 句柄 + 一个 onCleanup 回调，换来「卸载即注销」的确定性。
- **验证**：compileJava 通过（15s）；新增 `UiComponentRuntimeOnTest` **11 个**（on 订阅/退订/作用域归属/onCleanup 自动退订/Binding 幂等双向/onClick 左键消费其余冒泡/forEach 项作用域退订等）+ `ReactiveControlBindingsTest` **5 个**（onAction/onToggle 桥接 + 退订）全绿；`ReactiveTriadDemoViewTest` 既有 **9 个**未削弱、无需适配。定向回归（reactive/component/control/config/devtools.pages + 全量 test）**913 tests 仅 8 预存环境失败**（7×`DocumentPaintRendererTest` 字体后端 + 1×`HtmlLikeDocumentWidgetCursorTest`），**零回归**。
- **未动（按铁律）**：`Signal.set` 去重守卫与同步契约、`UiStyleDeclaration`/`ElementNode` 核心写入路径、`resolveBoxContext`/坐标语义、scrollVersion、动画 timeline、forEach/show 的 untrack 红线、`ReactiveScheduler`/`Effect`/`Owner` 核心实现（本轮诊断确认它们干净、无需改）。
- **数据流完整度现状（本轮后）**：①数据模型 ✓ ②主干链路 ✓ ③声明式三基石（show/forEach/bindText）✓ **+输入半环（事件 → signal，on/onClick + 控件 bridge）✓**。数据流闭环：输入（事件→signal）→ 中央事务 → effect → DOM → 渲染，**输出半环 + 输入半环齐备**。
- **⚠️ 真机视觉/帧率仍待用户跑 runClient 验证**（沙箱无 GUI）：入口同第 24 次（`/qzuilib test` → REACTIVE 分组 → 打开 demo），操作 add/remove/shuffle/toggle/开关说明，验证功能正确 + 事件经新 API 仍正常。读日志关注 `club.heiqi.uilib` 零异常。
- **下一步候选（需用户中文提问拍板，勿自行大改）**：
  - **候选①·input 热路径优化（本轮单列的观察项，已坐实根因）**：`onMouseUp`/`onMouseDown` 单次输入事件内 `findElementAt`/`resolveInteractiveLayoutBox` 被调多次、每次全量 relayout + 全场景 hitTest 重建。可在单次输入事件内缓存一次布局盒/场景（同 `cachedBoundsIndex` 签名缓存思路），把单帧多次全树遍历摊销为一次——直接收益 input 帧时。**这是性能优化，按用户「先搭完整链路再优化」纪律，需确认是否到了可优化时点。**
  - **候选②·便利糖补完**：`batch(Runnable)` 显式事务边界（配 `labelNextTransaction` 命名事务）、动态组件切换 `dynamic/switch`、更多控件 bridge（TextInput/Slider 等 → signal）。
  - **候选③·方向乙扩面**：更多命令式列表迁 forEach（`ModernSimpleListPropertyBinding.refreshListView` 含输入控件焦点态 → `ModernNestedCategoryBinding.refreshContent` 异构项）。
  - **候选④·方向丙（I6 契约线提纯）**：paint 层约 138 处持 `ElementNode`，阻碍双缓冲跨线程，相对独立、风险中等、收益偏长期。
- **建议**：输入半环补完后，数据流主干（输入→状态→视图→渲染）**链路已完整闭环**。下一步**倾向先做候选①的 input 热路径优化**（根因已坐实、收益明确、与真机观测直接对应），把 demo 真机帧时拉回健康；或按纪律继续候选②补便利糖。**但 input 优化属性能优化，须先与用户确认是否「基础链路已够完整、可以开始优化」**。
- **重要纪律（沿用）**：不要陷入提前优化陷阱，先把基础数据模型/数据流链路搭完整；决策点优先中文 `question` 工具；帧率/真机实测必交用户跑，真机后派子代理查日志。

## 最近一次交接（2026-06-16 第 24 次）—— 声明式三基石真机接入试点：新建响应式 demo 页（show/forEach/bindText 纯 signal 驱动）

- **当前分支**：`4.0`，已合入。merge `b0137ab0`（`--no-ff` 合 `feat/reactive-triad-demo-screen`）+ feature `1aba4b87`（源）+ test `4373af6d`。**已 `git ls-tree -r HEAD` + `git show HEAD:` 双验** `ReactiveTriadDemoScreen.java`/`ReactiveTriadDemoView.java`/测试确实在 `4.0` HEAD 树，且 view 内 `runtime.show`/`runtime.forEach`/`runtime.bindText`/`Computed.create` 与 registry 的 `REACTIVE`/`VIS-REACTIVE-001` 均在 HEAD（杜绝「只合文档没合代码」），工作树干净，compileJava 通过。
- **本轮（用户拍板候选①）**：用户选「声明式三基石真机接入试点」。开工前用 explore 子代理盘点承载方式，**关键发现：现有页面里没有理想的「条件显隐+计数+列表」三合一面板**；`/qzuilib test` 各 demo 在共享文档内 `clearChildren` 全量重建切换（不适合演示 forEach/show 的「稳定不重建」语义），而 MODCFG demo 是独立 Screen（`ModernConfigTemplateScreen`，运行时生命周期跟随 Screen，是已验证 forEach 可用的范本）。用户拍板**新建专门响应式 demo 页**（而非改生产搜索过滤器——后者风险高且 show/bindText 用途牵强）。
- **改动（2 新源 + 2 改源 + 1 新测试 + 2 改测试，纯 demo/devtools 侧，零核心层/widget/DOM/reactive 改动）**：
  - **新增 `ReactiveTriadDemoView`（包私有，`internal.devtools.pages`）**：声明式三基石的**纯组件层**——只依赖 `UiDocument`(④) + `UiComponentRuntime`(③) + 控件，**不认识 GuiScreen**，故可脱离 Minecraft 宿主单元测试（与 `ModernConfigSearchFilter` 同样的「组件而非屏幕」定位）。一个纯 signal 驱动的任务清单：① **forEach**（任务列表，key=`任务id + 完成态`，增删/打乱/切换完成只动变化行）；② **bindText**（底部「共 N 项 · 已完成 M 项」由 `Computed` 从 `tasksSignal` 派生）；③ **show**（开关 `detailsVisibleSignal` 控说明区块显隐 + 派生 `emptyStateCondition` 控空态提示）。全部界面变化只经由改 signal 触发（信条一/I1）。
  - **新增 `ReactiveTriadDemoScreen`（包私有，`extends BaseScreen`）**：GuiScreen 宿主壳，只管宿主生命周期（构造 widget、注入 view、resize、ESC/返回、关闭收口）。`onGuiClosed → cleanupResources`：`view.clearHandlers()` + `documentWidget.close()`（回收三基石 effect + 各作用域）+ `view.dispose()`（清理构造期创建的两个 `Computed`——它们不在任何 Owner 作用域内，否则 recompute effect 残留全局调度器被持续重跑）。静态 `openDemo()` 仿 MODCFG bridge 经 `UiScreenManager.enqueue` 切屏。
  - **注册进 `/qzuilib test`**：`UiTestMatrixRegistry` 加 `REACTIVE` 分组（plannedCase=1/auto=0/manual=1）+ `VIS-REACTIVE-001` 样例；`UiTestSampleVisualFactory` 加 `appendReactiveTriadDemoStage`（仿 `appendModernConfigDemoStage`，画「打开 demo」按钮 + 三基石说明卡片，按钮回调 `ReactiveTriadDemoScreen.openDemo()`）。响应式 demo **无可选模块依赖**（运行时随框架常驻），故不需 MODCFG 那种 `Class.forName` 可用性检测/launcher 间接层。
- **关键设计点**：① **View/Screen 拆分**是为可测性——三基石逻辑全在不依赖 GuiScreen 的 View 里，测试可直接 `new UiComponentRuntime(document)` + 手动 flush 验证数据侧，与 `ModernConfigSearchFilter` 同源（**这不是过度设计，是「组件 vs 屏幕」的正确分层**）；② **forEach 与空态 show 共用同一 `listContainer`**——forEach 行 + show 锚点 + 空态内容并存，各订各自的源（tasksSignal vs emptyStateCondition）；③ key 含完成态使「切换完成」只重建该行（与搜索过滤器 path+dirty 同手法）；④ 派生 `Computed` 必须在 bindText/show 之前创建（注册顺序即粗略拓扑序，Computed 先于下游消费方）。
- **过 NORTH_STAR 第 8 节**：**I1 实证**（纯 signal 驱动完整界面，三基石齐用，无第二条改 UI 路径）✓ I3（行/内容组件每 key 只跑一次、effect 自动归属）✓ **I5（forEach/show reconcile 只订各自源、untrack 隔离，DOM 收窄容器内）✓** **I7（打乱时 LIS 稳定项零重建、稳定条件不重建，测试 assertSame/assertNotSame 坐实）✓** I6（demo 纯 `internal.devtools.pages`，零 GL）✓ I4（bindText→LAYOUT、show 结构变更→LAYOUT）✓。内存：demo 仅多任务 signal/2 Computed/各 show 锚点。
- **验证**：compileJava 通过；新增 `ReactiveTriadDemoViewTest` **9 个全绿**（forEach 首帧渲染初始 3 行 / 增行复用未变行 assertSame / 删末项 / **打乱稳定项零重建** / **切换完成只重建该行 assertNotSame 其余 assertSame** / bindText 计数随派生刷新 / show 说明区块显隐+稳定不重建 assertSame / show 空态出现消失 / dispose 拆除三基石并停协调）。**测试期发现并修正的认知点（非 bug）**：变更方法读 `tasksSignal.get()` 取的是**已应用值**（signal「set 后未 flush 时 get 取旧值」契约），故同帧多次 addTask/removeLastTask 会被 I9 合并、净变化按最后一次——测试改为**每次变更后 flush 一帧**（与真机每输入帧 drawSelf 都 flush 一致）。定向回归（reactive/component/config/internal.devtools.pages/paint/document/layout）**697 tests 仅 8 预存环境失败**（7 `DocumentPaintRendererTest` 字体后端 + 1 `HtmlLikeDocumentWidgetCursorTest`），**零回归**。
  - **矩阵计数对齐（非回归，是新增分组/样例致快照位移）**：新增 REACTIVE 分组+样例使 groups 11→12、cases 54→55、planned 60→61、implemented 54→55、manual 计数 +1（auto/gap 不变）。已更新 `UiTestDocumentPageControllerTest`（首页 hero「已接入 55 个，自动 42 个，人工 13 个」/ 计划 61 / 自动/人工 42/13 / 全量完成 55；矩阵模型计数；补 REACTIVE 分组/样例/「打开 REACTIVE」/VIS-REACTIVE-001 可见性断言）与 `UiTestRuntimeHostVisualMatrixTest`（cases/implemented 54→55）。
- **未动（按铁律）**：`Signal.set` 去重守卫与同步契约、`UiStyleDeclaration`/`ElementNode` 核心写入路径、`resolveBoxContext`/坐标语义、scrollVersion、动画 timeline、forEach/show 的 untrack 红线、`UiComponentRuntime`/`KeyedListReconciler`/`ConditionalRenderer` 任何改动（demo 是 API 使用方，未碰组件层实现）。
- **生产侧现状**：本轮把 show/bindText 首次接入真实业务路径（demo 也是真机页面）。**show/forEach/bindText 三基石数据侧已测试覆盖；真机视觉/帧率/无泄漏仍待用户跑 runClient 验证**（沙箱无 GUI）。
- **⚠️ 真机实测待用户跑（沙箱无 GUI）**：
  - **入口**：`$env:GRADLE_USER_HOME="D:\.MyApps\.ENV\gradle-home"; ./gradlew.bat --no-configuration-cache runClient21` → 游戏内 `/qzuilib test` → **REACTIVE 分组** → 点「打开声明式三基石 demo 页」。
  - **测什么**：① **forEach**——点「添加任务」/「移除末项」看列表只增删变化行；点「打乱顺序」（末项移首位）看其余行不重建只移动；点行内「标记完成/未完成」看仅该行刷新、计数同步；② **bindText**——底部「共 N 项 · 已完成 M 项」随上述操作刷新；③ **show**——拨「显示说明区块」开关看说明区块挂载/卸载；删光所有任务看空态提示出现、再添加看其消失；④ ESC/返回回到 REACTIVE 组页。
  - **读日志**：真机后派子代理查 `run/client/logs/fml-client-latest.log`——`club.heiqi.uilib` 零异常（`at club.heiqi` 0 / IllegalState/NPE/ConcurrentMod 0 / `响应式 flush 超 1000 轮` 未触发）；反复开关 demo 多轮确认 effect 无泄漏累积；render 帧时正常。
- **下一步候选（需用户中文提问拍板，勿自行大改）**：
  - **候选①·便利封装补完**（数据流链路已通、demo 已证三基石真机可用，差糖）：**事件绑定封装（DOM 事件→signal，补数据流「输入半环」**，目前 demo 里 toggle/button 仍命令式直调方法改 signal，可封装成声明式 `on(event, signal-updater)`）、`batch(Runnable)` 显式事务边界（配 `labelNextTransaction` 命名事务）、动态组件切换 `dynamic/switch`。
  - **候选②·方向乙扩面**：把更多命令式列表迁 forEach（`ModernSimpleListPropertyBinding.refreshListView` 含输入控件焦点态 → `ModernNestedCategoryBinding.refreshContent` 异构项，难度递增）。
  - **候选③·方向丙（I6 契约线提纯）**：paint 层约 138 处持 `ElementNode`，阻碍双缓冲跨线程，相对独立、风险中等、收益偏长期。
- **建议**：三基石（show/forEach/bindText）数据侧已齐备且本轮新增端到端 demo 测试覆盖，真机验证一跑通即与 forEach 同基准线。**倾向真机通过后做候选①的「事件绑定封装」**——它补的是数据流目前缺失的「输入半环」（DOM 事件→signal 仍命令式），属「把基础数据流链路搭完整」而非纯糖，与本系列主线一致；之后再扩面（候选②）或提纯契约线（候选③）。
- **重要纪律（沿用）**：不要陷入提前优化陷阱，先把基础数据模型/数据流链路搭完整；决策点优先中文 `question` 工具；帧率/真机实测必交用户跑，真机后派子代理查日志。

## 最近一次交接（2026-06-16 第 23 次）—— 声明式三基石补全：条件渲染 show + 文本绑定 bindText


- **当前分支**：`4.0`，已合入。merge `ae26569a`（`--no-ff` 合 `refactor/conditional-render-bindtext`）+ feature `93d3f9f1`（源）+ test `6e5f0b77`。**已 `git ls-tree -r HEAD` + `git show HEAD:` 双验 `ConditionalRenderer.java`/`UiComponentRuntime` 的 `show/bindText/ConditionHandle` + 测试确实在 `4.0` HEAD 树**，工作树干净，compileJava 通过。
- **前置：第 22 次中央事务的真机实测已通过（本轮开工前用户跑了 runClient，沙箱无 GUI 改派子代理查日志）**。最新会话 `fml-client-latest.log`（21:21，含事务日志新代码）：① `club.heiqi.uilib` 零异常（`at club.heiqi` 0、IllegalState/NPE/ConcurrentMod 各 0、`响应式 flush 超 1000 轮` 未触发）；② **稳态 render 中位 4.66ms 与上一会话基准（~4.76ms）同档——事务日志每帧开销可忽略（idle 帧零抬升即铁证，日志记录在 flush 热路径但 idle 帧无变化）**；③ 打字帧 render 上限 25ms（基准 8.2ms）但**经亲自核对原始帧坐实：尖刺只在 input 帧、且与 `input=` 字段（同步 `searchIndex.search` 全索引扫描 + 变化行重绘）线性相关，与事务日志无关，是本会话打字更密集所致**；④ 优雅停服、关闭/dispose 阶段零 UI 报错。**结论：事务日志数据模型真机有效、无性能回退**。
- **本轮（用户拍板）**：用户明确「不要陷入提前优化陷阱，先把基础数据模型和数据流链路搭建完毕」。据此**废弃了一个刚起头的 lazy-alloc 提前优化分支**（`flush` 每帧空 LinkedHashMap，idle 帧已证可忽略），回到主线。派 explore 子代理盘点数据层（①②③）完整度，结论：**数据模型①+主干链路②已完整自洽**（signal/computed/readable 三件套、中央事务批处理+审计+时间旅行闭环、signal→effect→DOM 十跳分级失效无断点），**唯一「半真断链」是组件层③缺条件渲染原语**——声明式三基石（条件/列表/文本）里 forEach（列表）+mount 已有，独缺条件渲染，缺它则「按状态显隐子树」必退回命令式 append/removeChild（顶撞信条一/I1）。用户选「show/when + bindText 一起补」。
- **改动（component 1 新源 + 1 改源 + 1 新测试，纯组件层③，零 widget/DOM 核心改动）**：
  - **新增 `ConditionalRenderer`（包私有，`ui.component`）**：条件渲染协调器，与 `KeyedListReconciler` 同源（条件渲染本质 = 0/1 项 keyed 列表）。`update(boolean)`：真且未挂载→构建内容子树并 `insertBefore` 到占位锚点之前；假且已挂载→`contentOwner.dispose()`（onCleanup 摘 DOM）。**条件值未跨真假边界时直接 return、不动 DOM（守 I7：稳定子树不重建）**。全部 DOM 操作收窄在 `parent` 内（守 I5）。每次挂载内容一个独立子 `Owner`（复用 mount 生命周期语义）。
  - **`UiComponentRuntime.show(parent, condition, component)`**：调用时立即在 `parent` 末尾 append 一个 **`display:none` 占位锚点**（`data-ui-show-anchor`，标记内容声明顺序位置，内容总插到锚点之前——SolidJS `<Show>` 的 marker node 方案）+ 条件作用域子 Owner。reconcile effect **只订阅 condition 本身**，`update` 在 `Effect.untrack` 内执行——内容内部 signal 不回流成条件依赖（I5 红线，与 forEach 同手法）。`condition` 为 null 视为 false。返回 `ConditionHandle` 可单独 dispose（清条件 effect + 当前内容 + 锚点）。
  - **`UiComponentRuntime.bindText(textNode, source)`**：文本节点内容绑定，委托通用 `bind`，按 **LAYOUT 级**失效（`TextNode.setText`→`markMutated`→`recordLayoutMutation`，文本变化影响尺寸）。null 值跳过（与其它 bind 一致）。注：`setText` 内部本就 `markMutated`，bind 的 LAYOUT 标记是幂等叠加（同 `bindOpacity` 双 bump 性质，无害）。
  - **关键设计点**：① 锚点方案解决「条件 false→true 时内容插回正确位置」——无需追踪兄弟下标，锚点常驻标记位置；② 锚点 `display:none` 不参与布局；③ 锚点随 `condOwner` 的 onCleanup 一并摘除（整块 dispose 干净）。
- **过 NORTH_STAR 第 8 节**：**I1 强化**（声明式条件渲染替代命令式增删子树，补全三基石→可纯 signal 驱动「条件+列表+文本」完整界面）✓ I3（内容组件每次挂载只跑一次、effect 自动归属内容作用域）✓ **I5（条件 effect 只订阅 condition、untrack 隔离内容内部 signal、DOM 收窄 parent 内）✓** **I7（稳定条件不重建、assertSame 复用同一 DOM）✓** I6（`ConditionalRenderer` 纯 `ui.component`，零 GL/widget）✓ I4（bindText→LAYOUT、show 结构变更→LAYOUT）✓。内存：每个 show 块仅多一个锚点元素 + 内容 Owner，换来条件渲染能力。
- **验证**：compileJava 通过；新增 `UiComponentRuntimeShowTest` **18 个全绿**（show 15：真假首帧挂载/null 视假/翻转增删/稳定不重建 assertSame/翻转一轮重建新节点/卸载 dispose 内容 effect/Handle dispose 摘内容+锚点/runtime.dispose 清块/声明顺序位置 header-body-anchor-footer/嵌套随外层卸载递归清理/I5 红线内容内部 signal 不重算条件；bindText 3：首帧应用/signal 驱动/null 跳过）；component 包全绿。定向回归（reactive/component/config/paint/document/layout）**664 tests 仅 8 预存环境失败**（7 `DocumentPaintRendererTest` 字体后端 + 1 `HtmlLikeDocumentWidgetCursorTest`），**零回归**。
- **未动（按铁律）**：`Signal.set` 去重守卫与同步契约、`UiStyleDeclaration`/`ElementNode` 核心写入路径、`resolveBoxContext`/坐标语义、scrollVersion、动画 timeline、forEach 的 untrack 红线、各控件命令式增删（show/bindText 是新增 API，未替换任何现有控件）。
- **数据层完整度现状（本轮后）**：①数据模型 ✓ ②主干链路 ✓ **③声明式三基石（条件 show / 列表 forEach / 文本 bindText）✓ 全部齐备**。组件层现可纯声明式表达含条件+列表+文本的完整界面。生产侧目前仍仅搜索列表用 forEach + resultsSignal.set；show/bindText 是新增 API、零生产调用方、未经真机验证（与历次地基增量一致）。
- **下一步可选（需用户拍板，勿自行大改）**：① **声明式三基石真机接入试点**：选一个真实页面（如某个有条件显隐区块 + 文本计数的面板）用 show/bindText/forEach 纯声明式重写，真机验证「纯 signal 驱动条件+列表+文本」端到端有效；② **便利封装补完**（子代理盘点的次要项，链路已通只差糖）：事件绑定封装（数据流输入半环：事件→signal）、`batch(Runnable)` 显式事务边界（配合 `labelNextTransaction` 做命名事务）、动态组件切换 `dynamic/switch`；③ **方向乙·扩面**：把更多命令式列表迁到 forEach（`ModernSimpleListPropertyBinding`/`ModernNestedCategoryBinding`）；④ **方向丙（I6 契约线提纯）**：paint 层约 138 处持 `ElementNode`，阻碍双缓冲跨线程。
- **建议**：数据层①②③基础链路至此完整自洽。下一步**倾向先做声明式三基石的真机接入试点（候选①）**，把「纯 signal 驱动完整界面」拉到真机验证基准线——此前 forEach 已验证、show/bindText 尚未，凑齐三基石的端到端实证后，再决定扩面（候选③）或补便利糖（候选②）。

## 最近一次交接（2026-06-16 第 22 次）—— 信条四中央事务：事务日志 + 游标时间旅行

- **当前分支**：`4.0`，已合入。merge `745044e6`（`--no-ff` 合 `refactor/central-transaction-log`）+ feature `5a806746`（reactive 源）+ test `75028ce3`。**已 `git ls-tree -r HEAD` + `git show HEAD:` 双验 `TransactionLog.java`/`ReactiveScheduler` 的 `undo/redo/transactionLog/labelNextTransaction` + `TransactionLog` 核心 API 确实在 `4.0` HEAD 树**，工作树干净，compileJava 通过。
- **前置：真机实测已通过（第 21 次试点的遗留待办，本轮开工前先清）**。沙箱无 GUI，改由派 2 个子代理只读分析最新会话日志 `run/client/logs/fml-client-latest.log`（20:37 会话，20358 行）交叉验证：
  - **功能/异常**：`club.heiqi.uilib` 响应式/组件/搜索类**零异常**，`at club.heiqi` 堆栈帧 0 处，`IllegalStateException`/`NullPointerException`/`ConcurrentModificationException`（响应式 bug 三典型）全 0，调度器循环保护文案 `响应式 flush 阶段2 超过 1000 轮` 与 `重复 key` 从未触发。260 处 ERROR/WARN 经逐条 logger tag 核验全为 GTNH 第三方噪声（GregTech 89/mixin 53/AE2 31…），无一属本项目。会话优雅停服退出，**关闭/dispose 阶段零 UI 报错**（`documentWidget.close()`→`componentRuntime.dispose()` 真机无异常）。
  - **性能**：ModernConfigTemplateScreen 稳态 render 中位 **4.76ms**，fps 普遍 160-275，phases 全程 `<none>`。6 个打字帧 render 全 ≤8.2ms，与静置帧（中位 4.69ms）同档——**搜索输入不再触发全列表重建，试点目标达成**。>15ms 尖刺全是重开页面的开屏首帧（`slowFrames=1/1`、`input=0`），次帧立即回落 ~4-5ms。
  - **泄漏**：反复开关 12 段，各段次帧 render 一直 ~4-6ms 不随开关累积升高，**稳态基线无泄漏**。唯一观察项：3 次重开的开屏首帧成本轻微上行 93→97→102ms（样本仅 3 次，判为 GC/资源抖动而非泄漏，稳态平稳佐证），**留作后续多开几轮复测的观察项，非阻断**。
- **本轮（用户拍板方向 = 信条四中央事务）**：在 `ReactiveScheduler` 上补齐信条四「所有状态写入收口到中央事务」的**审计③ + 时间旅行②**（此前只实现了批处理①）。两项设计取舍均经中文提问拍板：① 事务日志**默认开启 + 有界环形缓冲**（审计为固有环节，内存可控）；② **仅日志 + 游标时间旅行**模型（不引入 signal 全局注册表，最小且足够，Redux DevTools 同模型）。
- **改动（reactive 1 新源 + 1 改源 + 1 新测试，纯数据层，零 widget/DOM/GL）**：
  - **新增 `TransactionLog`（`ui.reactive`）**：有界环形缓冲（`DEFAULT_CAPACITY=256`，超出丢最老、缩容立即淘汰）。`Transaction`（单调序号 + 时间戳 + 可选标签 + 不可变 `List<Entry>`）/ `Entry`（`signal`+`before`+`after`）记录结构。**游标 `cursor`** 把日志分为已应用段 `[0,cursor)` 与可重做未来段 `[cursor,size)`，游标位置即逻辑状态快照。`canUndo/canRedo/stepBack/stepForward`（包私有供调度器）、`commit`（截断未来段→追加→前移游标→按容量淘汰）、`transactions()` 不可变视图（审计）、`setEnabled/setCapacity/clear`、`resetForTest`。
  - **`ReactiveScheduler` 改造**：`flush` 阶段1 按 signal 用 `LinkedHashMap` 合并捕获 before/after（首次 `before=signal.peek()`，同帧再写仅更 after），提交为一个原子事务（**仅记净变化的源 signal**，`Computed` 派生值不入日志、由源 signal 重放后自动重算）。日志关闭时**不分配 changes map、本段零开销**。抽出 `runEffectsToFixpoint()`（原阶段2 不动点循环）供 flush + undo/redo 复用。新增 public `undo()/redo()`（游标后退/前进 + `applyAndRerun` 反向/正向重放 Entry + 重跑 effect 到不动点；**纯导航、绕过队列与日志、不产生新事务**；用 `flushing` 守可重入）、`transactionLog()`、`labelNextTransaction(String)`（为下一事务附审计标签，一次性，提交后清空）。
  - **关键正确性点**：① 在游标中段提交新写入→截断未来分支（标准 undo/redo 语义，测试 `newWriteTruncatesFutureBranch` 坐实）；② 多 signal 事务原子回退（`multiSignalTransactionUndoneAtomically`）；③ undo 触发 effect 与 computed 双双重算（`undoRerunsEffects`/`undoRerunsComputed`）；④ `Signal<?>` 作 map key 走对象身份（无 equals 覆盖），不同 signal 实例天然不撞。
  - **测试期发现并修正的认知点（非 bug）**：原想测「同帧改回原值→净变化 0→不记事务」，但 `Signal.set` 去重守卫是对**已应用值**判等（`value` 直到 flush 才变），故 `set(7);set(5)`（现值 5）中 `set(5)` 被守卫拦下不入队，同帧净零经公共 API 不可达。改写为 `inFrameDedupRecordsFinalValueOnly` 记录真实可达语义（记 5→7）。`commitTransaction` 里的 `before==after` 净变化过滤**保留为防御性代码**（保障日志不变量），未动 `Signal.set` 铁律路径。
- **过 NORTH_STAR 第 8 节**：**I2/I9 强化**（中央事务的审计+时间旅行，正是信条四②③的落地）✓ I1（undo/redo 经 `applyAndNotify`，与 signal 既有改值路径同路）✓ I3/I4/I5/I7/I8 未触碰 ✓ I6（`TransactionLog` 纯 `ui.reactive`，零 GL/widget/DOM）✓。**第 6 节内存预算**：有界环形缓冲（256 上限）明确换来「时间旅行调试 + 可追溯」，对齐宪章信条四预算行 ✓。
- **验证**：compileJava 通过；新增 `TransactionLogTest` **23 个全绿**（日志记录 6 / 游标时间旅行 8 / 环形缓冲 3 / 标签 3 / 开关 3）；reactive 包 48 tests 全绿（既有 25 + 新增 23）。定向回归（reactive/component/config/paint/document/layout）**649 tests 仅 8 预存环境失败**（与基准同集：7 `DocumentPaintRendererTest` 字体后端 + 1 `HtmlLikeDocumentWidgetCursorTest`），**零回归**。
- **未动（按铁律）**：`Signal.set` 去重守卫与同步契约、`UiStyleDeclaration`/`ElementNode` 核心写入路径、`resolveBoxContext`/坐标语义、scrollVersion、动画 timeline、forEach 的 untrack 红线、各控件命令式增删。
- **生产侧现状**：事务日志/时间旅行**仅数据层 + 测试覆盖，未接入任何真实页面、未经真机验证**（与历次地基增量一致）。`undo/redo/labelNextTransaction` 是新增 API，零调用方。日志默认开启但目前生产无 signal 写入热路径（试点仅搜索列表用 forEach + `resultsSignal.set`），实际内存占用极小。
- **下一步可选（需用户拍板，勿自行大改）**：① **审计/时间旅行接入真实页面试点**：给搜索列表的 `resultsSignal.set` 加 `labelNextTransaction("search.query")`，或给某可撤销操作接 undo/redo，真机验证审计日志可读性与时间旅行体感；② **方向乙·扩面**：把更多命令式列表迁到 forEach（`ModernSimpleListPropertyBinding.refreshListView` 含输入控件焦点态 → `ModernNestedCategoryBinding.refreshContent` 异构项 → 控件级列表，难度递增）；③ **方向丙（I6 契约线提纯）**：paint 层约 138 处持 `ElementNode`、命令自身约 21 处 → 阻碍双缓冲跨线程，相对独立、风险中等、收益偏长期；④ 时间旅行进阶：当前 undo/redo 是「单事务步进」，可选做「跳到任意序号」「日志可视化/导出」等 DevTools 能力（按需）。
- **建议**：信条四核心收益（审计+时间旅行）数据层已成体系。下一步**倾向先做一个最小的审计/时间旅行真机接入试点**（候选①，给搜索 set 打标签 + 真机读日志），把信条四也拉到「真机验证有效」的同一基准线，再决定扩面（候选②）或提纯契约线（候选③）。

## 最近一次交接（2026-06-16 第 21 次·续）—— 响应式真机试点（方向乙）：搜索结果列表接入 forEach

- **当前分支**：`4.0`，已合入。merge `09a3c504`（`--no-ff` 合 `refactor/reactive-pilot-search`）+ feature `e77eb777`。**已 `git ls-tree`/`git show HEAD:` 验证 `runtime.forEach`/`documentWidget.close()`/响应式测试确实在 `4.0` HEAD 树**，工作树干净，compileJava 通过。
- **本轮（用户授权方向乙）**：把响应式运行时（forEach + signal）**首次接入一个真实页面**，做端到端试点。试点目标 = `ModernConfigSearchFilter` 搜索结果列表（原本纯 `clearChildren()`+循环 append 全量重建，无局部 diff，数据驱动，每项有唯一 key=配置路径——最干净的「全量重建反模式」样本）。
- **开工前 explore 子代理调研（关键事实，已逐条亲自核实）**：① 承载类 `ModernConfigTemplateScreen extends BaseScreen extends GuiScreen`，构造期 `new HtmlLikeDocumentWidget(...)` + `new ModernConfigSearchFilter(...)`；② widget 在 `ui/document/`（非 `ui/widget/`），`getComponentRuntime()`(:313)、`close()`(:322 委托 `componentRuntime.dispose()`)、`drawSelf`(:726 帧初 `componentRuntime.flush()`) 管线已全铺好；③ **坐实交接所述隐患**：`ModernConfigTemplateScreen.cleanupResources()`（onGuiClosed 调用链）原**不调** `documentWidget.close()` → componentRuntime 永不 dispose；④ `componentRuntime.flush()` 走**全局单例** `ReactiveScheduler`，泄漏 effect 会被其它页帧循环持续重跑（接 dispose 的根本原因）；⑤ 现有 `ModernConfigSearchFilterTest` 只验数据层（`getResultCount`/`getResults`/`jumpTo`）、不查 DOM、不调 flush → 响应式改造不破坏它们；⑥ 测试用 3 参构造器 → 必须向后兼容。
- **改动（config 2 源 + 1 测试）**：
  - `ModernConfigSearchFilter` 新增 **4 参构造器**（透传 `UiComponentRuntime`），保留 3 参构造器委托（`runtime=null`）。`renderResults` **双路**：runtime≠null → 把可见结果 `resultsSignal.set(...)`、由构造期登记的 `forEach(resultsContainer, resultsSignal, keyFn, createResultRow)` 协调（**删掉 clearChildren**，否则整列表 `markSubtreeMutated` 全量失效抵消 forEach 收益）；runtime=null → 原命令式整列表重建（测试走此路，构造期同步渲染无需 flush）。
  - **keyFn = `path + (dirty?"#1":"#0")`**（用户拍板「key 含 dirty」）：path 唯一标识配置项；`refreshDirtyMarkers` 原地换 SearchEntry 但 path 不变，附带 dirty 使「●已修改」变化时 key 变、forEach **只重建该行**（脏状态属行内展示，path 不变时 forEach 默认复用旧节点不会刷新它）——只动变化行，不退化整列表（守 I5/I7）。
  - **`createResultRow` 去 index 化**：签名去掉 `index` 参数（forEach 复用节点时下标失真），点击改 `jumpToPath(entry.getPath())`；原 `jumpTo(int)` 保留供既有测试；去掉 `data-modern-config-search-index` 属性（全局仅内部使用，无外部依赖）。
  - `ModernConfigTemplateScreen`：构造期 `searchFilter = new ModernConfigSearchFilter(..., documentWidget.getComponentRuntime())`；`cleanupResources()` **接上 `documentWidget.close()`**（生产侧 widget.close 此前从不被调用，本轮补上，防 effect 泄漏全局调度器）。
- **过 NORTH_STAR 第 8 节**：I1/I2/I9（结果经 signal/scheduler）✓ I3（行组件每 key 只跑一次）✓ **I5（forEach 协调收窄列表内、keyed）✓** **I7（未变行复用 DOM、不重建）✓** I6（试点未碰渲染层）✓。dispose 链路补全（信条三组件卸载）。
- **验证**：compileJava 通过；新增 `ModernConfigSearchFilterReactiveTest` **5 个全绿**（首帧 flush 渲染全部 / 缩小查询复用未变行**同一 DOM 节点**`assertSame` / 清空恢复持续复用 / **dispose 拆除全部行并停止协调** / 3 参构造器命令式向后兼容）；既有 `ModernConfigSearchFilterTest` 16 个全绿（数据层不变）。定向回归（config + reactive + component + paint + widget + layout）**411 tests 仅 8 预存环境失败**（7 `DocumentPaintRendererTest` 字体后端 + 1 `HtmlLikeDocumentWidgetCursorTest` 环境敏感），**零回归**。
- **⚠️ 真机实测待用户跑（沙箱无 GUI，本轮未验证真机帧率/视觉）**：
  - **入口**：`$env:GRADLE_USER_HOME="D:\.MyApps\.ENV\gradle-home"; ./gradlew.bat --no-configuration-cache runClient21` → 游戏内 `/qzuilib test` → MODCFG 分组 → 打开 ModernConfig demo 页。
  - **测什么**：在搜索框输入/删字符、切类型分段、切「只看已修改」→ 看结果列表是否正常增删（功能正确性优先）；读 `run/client/logs/fml-client-latest.log` 的 `UI 运行统计[ModernConfigTemplateScreen]`，对比输入帧 `render=`/`slowFrames=`（预期：单项变化不再整列表重建尖刺）。
  - **回归校验**：点击结果行应正确跳转到对应卡片（去 index 化后改按 path 跳）；反复开关 demo 多次确认无 effect 泄漏累积变慢；「●已修改」标记在 save/restore 后应正确出现/消失（key 含 dirty 驱动该行重建）。
- **试点已知局限/观察**：① flush 是全局单例，多文档不隔离（试点页 dispose 已接，安全）；② 其它命令式列表（`ModernSimpleListPropertyBinding.refreshListView` 已有手写 tryDeltaUpdate、虚拟列表 view 池、`ModernNestedCategoryBinding` 等）**本轮未动**，是后续可继续替换的对象；③ 配置卡片分批构建走 `UiScreenManager.enqueue`，与 reactive 无关，未动。
- **下一步可选**：① **真机实测通过后**，继续把更多命令式列表迁到 forEach（搜索列表是最简样本，难度递增：simple-list 含输入控件焦点态、nested-category 异构项）；② 信条四中央事务的状态快照/时间旅行（scheduler 目前只批处理、无日志快照）；③ **方向丙（I6 契约线提纯）**：paint 层约 138 处持 ElementNode，阻碍双缓冲跨线程；④ 若真机暴露问题（功能/性能/泄漏），回到本试点修正——这正是试点的目的。

## 最近一次交接（2026-06-16 第 21 次）—— forEach keyed 列表协调（信条三，I5）

- **当前分支**：`4.0`，已合入。merge `4a0a0979`（`--no-ff` 合 `refactor/keyed-list-foreach`）+ feature `5457b70c`。**已用 `git ls-tree -r HEAD` 验证 `KeyedListReconciler.java`/测试/`forEach`/`untrack` 确实在 `4.0` HEAD 树**（杜绝第20次「只合文档没合代码」事故重演），工作树干净，compileJava 通过。
- **本轮主攻（用户拍板方向甲 = 信条三 keyed 列表 I5）**：在第19/20次 mount + Owner 树地基上建 keyed 动态列表协调。数据变化时按 key 对齐新旧子节点、只增删移动变化项，每项复用 mount 生命周期作用域。
- **改动（reactive 2 源 + component 2 源 + 1 测试）**：
  - **新增 `KeyedListReconciler<T>`（包私有，`ui.component`）**：按 key 对齐复用 / 删除消失项 / **LIS（最长递增子序列）求稳定项做最小移动**（稳定节点零移动零重建，守 I7/I8）。全部 DOM 操作严格收窄在 `container` 子节点范围（`insertBefore`/`removeChild`，referenceChild=null 退化 append，守 I5 红线）。重复 key 抛 `IllegalStateException`。每项一个子 `Owner`（`listOwner.createChild()`）+ `onCleanup` 摘 DOM。
  - **`UiComponentRuntime.forEach(container, itemsSignal, keyFn, itemComponent)` + `ListHandle`**：reconcile effect 在 listOwner 作用域内创建、**只 `itemsSignal.get()`（唯一追踪点）**，协调全程在 `Effect.untrack` 内执行——**item 内部读取的 signal 不会回流成列表依赖**，单项变化只重跑该项 effect、不触发整列表重协调（I5 红线的机制保证，已被测试 `itemInternalSignalChangeDoesNotReReconcileList` 坐实）。`forEach` 零 widget/render/GL 引用，分层干净。
  - **`Effect.untrack(Runnable)`**：SolidJS 等价物，`setCurrent(null)` 隔离追踪上下文。
  - **`ReactiveScheduler.flush` 阶段2 改为迭代到不动点**（带 `MAX_FLUSH_PASSES=1000` 循环依赖保护）：**测试逼出的真 bug**——mount/forEach 在协调 effect（阶段2 内运行）里动态挂载子组件时，其 `bind` effect 本帧才诞生，原单遍快照 `new ArrayList<>(effects)` 会漏跑 → 新挂载项首帧显示默认值、下一帧才跳变。迭代到不动点使同帧首跑。**按注册顺序扫描，保留 Computed 先于下游的拓扑序契约**。是单遍的严格超集（收敛场景第二遍无脏、行为等价）。
  - **`Effect.dispose` 清 `dirty=false` + 已 dispose 的 effect 不再 `registerEffect`**：配合不动点循环——否则「在已 dispose owner 上建的 effect」残留调度器列表且 dirty 恒 true，被循环误判为脏空转触发上限异常（`OwnerTreeTest.effectCreatedOnDisposedOwnerIsImmediatelyDisposed` 坐实）。
- **过 NORTH_STAR 第 8 节**：I1/I2/I9（经 signal/scheduler）✓ **I3（item 组件每 key 只跑一次、effect 自动归属项作用域）✓** I4（bind 各级别失效）✓ **I5（diff 严格收窄列表内、keyed、untrack 杜绝全列表 diff）✓** **I7/I8（LIS 稳定项零移动零重建、复用 DOM 节点与作用域）✓** I6（forEach 零 GL/widget）✓。
- **验证**：compileJava 通过；新增 `UiComponentRuntimeForEachTest` **12 个全绿**（初次按序建项 / append 复用 / 删除摘 DOM / 逆序重排不重建 / 最小移动稳定项零移动 / 增删移动混合 / 移除项作用域 dispose / **I5 红线 item signal 不重协调** / 重复 key 抛异常 / 清空 / ListHandle dispose / 嵌套 mount 内列表随父卸载）；reactive + component 全套绿。定向回归（reactive/component/paint/HtmlLikeDocumentWidget/layout）**297 tests 仅 7 个 `DocumentPaintRendererTest` 预存环境失败**（缺 LWJGL native），**零回归**。
- **预存失败集（不变）**：`DocumentPaintRendererTest` 7 个字体后端 `NoClassDefFoundError`/`NoSuchMethodException`（缺 LWJGL native）、`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`（环境敏感，本轮该选择未跑到/偶发通过）。
- **未动（按铁律）**：`UiStyleDeclaration`/`ElementNode` 核心写入路径、`resolveBoxContext`/坐标语义、scrollVersion、动画 timeline、各控件命令式增删（forEach 是新增 API，未替换任何现有控件的 `clearChildren`+循环 append）。
- **生产侧仍零使用**：forEach 同样仅测试覆盖，**未接入任何真实页面、未经真机验证**（与地基现状一致）。
- **下一步可选（需用户拍板）**：① **方向乙（真机试点 + 验证）**：响应式数据层（signal/mount/forEach）至今未经真机端到端验证，是最大未知。选一个真实页面（如 ModernConfig 某列表/卡片）用 forEach/mount 接入，runClient 实测「signal 改值 → composite-only/paint 局部更新」「列表增删 → 只动变化项」的真实收益。**注意：生产侧 `widget.close()` 从不被调用 → `componentRuntime` 永不 dispose，接入时须在 Screen `onGuiClosed` 接上 `getComponentRuntime().dispose()`，否则 effect 订阅泄漏**。② 把 forEach 逐步替代各控件手写的 `clearChildren()`+循环 append（虚拟列表/DocumentSelectControl 等）。③ **方向丙（I6 契约线提纯）**：paint 层约 138 处持 `ElementNode`、命令自身约 21 处 → 阻碍双缓冲跨线程，相对独立、收益偏长期。④ 信条四：中央事务的状态快照历史 / 时间旅行调试（目前 scheduler 只批处理、无日志快照）。
- **建议**：强烈倾向**先乙（真机试点验证地基真实有效）**——signal/computed/bind/mount/forEach 五件套地基已成体系，但端到端真机收益一次都没验证过；继续叠层（如信条四中央事务）前应先证明地基在真实页面有效，否则风险累积。

## 最近一次交接（2026-06-16 第 20 次）—— 组件运行时与 widget 解耦（抽出 UiComponentRuntime）

- **当前分支**：`refactor/component-runtime-decouple`（从 `4.0` 切出），已提交 `3da8c8b8`（新建运行时+测试）+ `1d8f6e07`（widget 解耦），**待 `--no-ff` 合回 `4.0`**。
- **动因（用户提问引出）**：用户问「旧 HtmlLike 模型是要替换还是保留」。澄清结论：「HtmlLike 模型」分三层，命运不同——① **DOM 树（UiDocument/ElementNode/TextNode）不删**，是宪章④层交汇点、整个 retained 管线的地基；② **命令式 setter（setOpacity/append 等）不删，但"下沉"**为框架/渲染层底层指令集（业务侧不再直接调，由 effect 内部调用，正是第 17 次选岗路 B 的依据）；③ **业务侧命令式"构建用法"（clearChildren+循环 append）才是要替换的**，bind/mount 是其替代。结论：bind/mount 不是"套在要删的旧模型上加负担"，而是"新编程模型 → 保留的 DOM 层"之间的必需桥。
- **真问题（用户直觉点中，位置在 widget 而非 DOM）**：第 19 次把 `mount/bind*/createEffect/根 Owner` 做成了 `HtmlLikeDocumentWidget` 实例方法，**组件层（宪章③）耦合到了 widget**。组件层本应只依赖 DOM(④)+reactive(①②)，不该认识 widget。趁调用方仍为零（生产侧 reactive 未铺开、零迁移成本）解耦，否则日后成本剧增。
- **本轮成果**：新建 `club.heiqi.uilib.ui.component.UiComponentRuntime`（宪章③组件层运行时）：
  - 持有 `UiDocument` + 根 `Owner`，**只 import dom + reactive + style，零 widget/render/GL 引用**（分层干净，可独立构造/测试/复用，不依赖任何渲染后端）。
  - 迁入全部组件 API：`flush()`（委托 `ReactiveScheduler`）、`createEffect(impact,body)`、通用 `bind(impact,ReadableSignal,Consumer)`、便捷封装 `bindOpacity/bindTransform/bindBackgroundColor/bindTextColor/bindVisibility`、`mount(parent,Function)`、`dispose()`、内部类 `MountHandle`。
  - `HtmlLikeDocumentWidget` **删除**全部组件 API（用户拍板「直接删除」，调用方为零），改为持有一个 `UiComponentRuntime componentRuntime`（构造时 `new UiComponentRuntime(this.document)`）；新增 `getComponentRuntime()` 入口；`drawSelf` 帧初 `flush` 改委托 `componentRuntime.flush()`；`close()` 改委托 `componentRuntime.dispose()`。widget 退回本职（帧循环 flush + 渲染挂接），净删 ~374 行。清理无用 import（Effect/Owner/ReactiveScheduler/ReadableSignal/Signal/Consumer/Function/UiVisibility/UiStyleChangeImpact）。
- **过 NORTH_STAR 第 8 节**：I6（组件层不碰渲染层、运行时零 GL/widget 引用）✓；信条六长期回报（契约线干净才能换层/双缓冲）方向对齐；行为等价（纯重构，API 搬家，逻辑零改）。
- **测试迁移**：旧 3 个 widget 测试（`HtmlLikeDocumentWidget{StyleSignalBinding,StyleBindExpand,Mount}Test`）**删除**，重写为 `ui.component` 包下 3 个（`UiComponentRuntime{Binding,BindExpand,Mount}Test`），直接 `new UiComponentRuntime(document)`，不再依赖 widget/TextMeasureService——印证「运行时可独立测试」。
- **验证**：compileJava 通过；定向回归 316 tests 仅 8 预存失败，**零回归**。
- **预存失败集（不变）**：`DocumentPaintRendererTest` 7 个字体后端 `NoClassDefFoundError`（缺 LWJGL native）、`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`（环境敏感）。
- **未动（按铁律）**：DOM 树、命令式 setter、`resolveBoxContext`/坐标语义、scrollVersion、动画 timeline 路径。
- **下一步可选**：① **信条三 keyed 列表（I5）**：在 `UiComponentRuntime` 上加 `forEach(itemsSignal,keyFn,itemComponent)`，按 key 对齐新旧子节点、只增删移动变化项，每项复用 mount 作用域——这是本系列的下一主线，地基已全部就位（mount + Owner 树 + computed）；② 真机试点：选一个页面用 runtime 接入，runClient 实测；③ **生产接线隐患**：`widget.close()` 生产代码从不调用 → `componentRuntime` 永不 dispose；未来接入响应式时须在 Screen `onGuiClosed` 接上 `widget.close()`，否则 effect 订阅泄漏。

## 最近一次交接（2026-06-16 第 19 次）—— 组件挂载模型（信条二/三，I3：组件函数只跑一次）

- **当前分支**：`refactor/component-mount-model`（从 `4.0` 切出），已提交 `7d6f70eb`（reactive 层）+ `5359e6b2`（widget 层），**待 `--no-ff` 合回 `4.0`**。
- **本轮主攻（用户授权 agent 决策，明确「接受更长工期，框架一定要优秀」）**：在 signal/computed/bind 地基上，补信条二「组件函数只跑一次、动态行为落在 effect 里」（I3）的挂载模型。**这是信条三 keyed 列表（I5）的前置地基**——列表每一项本身就是一个「只跑一次、signal 驱动」的小组件。
- **决策依据（agent 定，已向用户说明）**：先信条二再信条三，因列表 diff 本质是「数据驱动地创建/增删一批子组件」，无组件模型则无法做好列表。与差距评估 REVIEW 第 5 节第 2 步一致。
- **开工前调研（explore 子代理，关键事实）**：① 节点创建必经 `UiDocument` 工厂（`document.element(tag)`，构造包私有）；② **生产侧 reactive 零使用**——管线已铺（`drawSelf` 帧初 flush、Owner 在 close dispose），但无任何业务真用 → **绿地无历史包袱，可定义干净 API**；③ `widget.close()` 生产侧从不被调用（隐患：reactiveOwner 永不 dispose，接入点是各 Screen 的 `onGuiClosed`）；④ 动态渲染统一是 `clearChildren()` + 循环 append + 各控件手写局部 diff（`tryDeltaUpdate`/虚拟列表 view 池）——**本轮不动它们**，留给信条三。
- **改动（reactive 3 源 + widget 1 源 + 4 测试）**：
  - `ReactiveContext`：新增 `CURRENT_OWNER` ThreadLocal（与现有 current-effect 并列）+ `getCurrentOwner/setCurrentOwner`。
  - `Owner`：扁平 `List<Effect>` → **树形**（父子 + 递归 dispose）。新增 `createChild()`、`run(body)`（在自身 owner 上下文执行，内部新建 effect/子 owner 自动归属）、`onCleanup(cleanup)`（dispose 时在子树清理后执行，组件用它摘 DOM）、静态 `current()`、`isDisposed()`。dispose 顺序：子 owner → 本 effect → cleanup 回调 → 从父摘除。
  - `Effect`：构造时读 `ReactiveContext` current-owner **自动归属**（SolidJS 风格）；`Owner.createEffect` 改为在自身上下文里 `new Effect`，构造器单次 attach（避免双重 attach）。
  - `HtmlLikeDocumentWidget`：① **修复 `createEffect` 归属**——原绑死 `reactiveOwner`，改为「有组件作用域→归属它，无→widget 根 owner」（否则组件内 effect 泄漏到 widget 根、卸载漏清）；② 新增 `mount(parent, Function<UiDocument,ElementNode> component)` → 建**当前作用域**的子 owner、`run` 内只跑一次 component 得根节点、append、`onCleanup` 登记 removeChild、返回 `MountHandle`；③ `MountHandle.unmount()` dispose 子 owner（递归清理 effect + 摘 DOM）。
- **测试逼出的真 bug（已修，体现「框架要优秀」价值）**：mount 原硬编码 `reactiveOwner.createChild()` → 嵌套 mount 时内层 owner 错误挂到 widget 根而非外层组件 owner → 父组件 unmount 漏清子组件 effect。改用 `Owner.current()`（嵌套时是外层组件 owner，顶层为 null 退回 widget 根）。`nestedComponentDisposedWhenParentUnmounts` 直接覆盖。
- **关键语义点**：mount 后组件内 effect **不立即跑**，需等 `flush()` 首跑（标准 SolidJS：挂载后下一帧 flush）。widget `drawSelf` 帧初已 flush，行为正确。
- **过 NORTH_STAR 第 8 节**：I1（signal 唯一数据源）✓ I2/I9（中央事务批处理）✓ **I3（组件只跑一次、effect 自动归属不泄漏）✓** I4（bind 各级别失效）✓。
- **验证**：compileJava 通过；新增 `OwnerTreeTest`（嵌套/递归 dispose/自动归属/cleanup 顺序）+ `HtmlLikeDocumentWidgetMountTest` 6 个（只跑一次/effect 归属/unmount 摘 DOM/幂等/嵌套递归清理/close 清全部）+ 扩展 `ReactiveSystemTest`。定向回归 458 tests 仅 8 预存失败，**零回归**。测试走纯数据层路径（手动 flush），不调 render（LWJGL native 沙箱缺失）。
- **未动（按铁律）**：`UiStyleDeclaration`/`ElementNode` 核心写入路径、`resolveBoxContext`/坐标语义、scrollVersion、动画 timeline、各控件命令式增删（留给信条三）。
- **预存失败集（不变）**：`DocumentPaintRendererTest` 7 个字体后端 `NoClassDefFoundError`、`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`。
- **下一步（强烈建议方向丙 = 信条三 keyed 列表 I5，本轮地基已就绪）**：在 mount 之上建 `forEach(container, itemsSignal, keyFn, itemComponent)` 之类的 keyed 列表协调——每项一个子 owner（复用本轮 mount），按 key 对齐新旧、只增删移动变化项，diff 范围**严格收窄在列表节点内**（I5 红线）。可逐步替代各控件手写的 `clearChildren()`+循环 append。**其它可选**：① 真机 runClient 实测组件挂载/signal 驱动；② 把 reactive 接进一个真实页面（如 ModernConfig 某区块）作端到端试点，验证 close/卸载链路（注意生产侧 `widget.close()` 当前不被调用，需在 Screen `onGuiClosed` 接上）。

## 最近一次交接（2026-06-16 第 18 次）—— 深化方向甲：通用 bind 推广 + Computed 派生原语

- **当前分支**：`refactor/style-signal-binding-expand`（从 `4.0` 第 17 次 merge 切出），已提交 `35bfed96`（reactive 层）+ `079abd20`（widget 层），**待 `--no-ff` 合回 `4.0`**。
- **本轮主攻（用户拍板「深化甲：推广 bind 到更多属性 + computed」）**：在第 17 次 `bindOpacity/bindTransform` 闭环基础上，把 bind 推广为通用 API、覆盖任意属性/级别，并补齐响应式 `Computed` 派生原语。
- **设计取舍（三项均与用户确认，全选推荐）**：
  1. **抽 `ReadableSignal<T>` 只读接口**：`Signal`（可写）与 `Computed`（只读派生）都实现它，使 computed 可直接喂入 bind（不必先 `.get()` 解包）。
  2. **通用 bind + 少量便捷封装**：不为每个属性写专用 `bindXxx`（属性几十个会爆炸）。核心是通用 `bind(UiStyleChangeImpact impact, ReadableSignal<T> source, Consumer<T> applier)`；另加 3 个高频便捷封装 `bindBackgroundColor`/`bindTextColor`/`bindVisibility`（均 PAINT 级）。`bindOpacity`/`bindTransform` 改为接受 `ReadableSignal` 并委托通用 bind（向后兼容，Signal 实现了该接口）。
  3. **通用 bind 支持任意级别（含 LAYOUT）**：`impact` 参数决定失效级别，调用方自行保证与目标属性在 `UiStyleProperty` 的标注一致。LAYOUT 级绑定走全量重排（无 composite-only 优化，符合预期）。
- **改动**：
  - **reactive 层（`35bfed96`）**：新增 `ReadableSignal.java`（只读接口）、`Computed.java`（派生原语）；`Signal` 实现 `ReadableSignal`、新增包内 `peek()`（不追踪读，供 Computed 相等判断避免自订阅）。
  - **widget 层（`079abd20`）**：`HtmlLikeDocumentWidget` 新增通用 `bind` + 3 便捷封装；`bindOpacity/bindTransform` 改签名为 `ReadableSignal` 并委托 bind。
- **Computed 实现关键（正确性要点）**：effect 驱动 + 输出 cell 模式——内部一个 recompute `Effect` 追踪上游、一个输出 `Signal<T>` cell 承载结果。recompute 在 flush 阶段2 **同步**（`cell.applyAndNotify`，不走队列）推入 cell，并做 `Objects.equals` 记忆化（仅输出变化才向下游传播）。**单次 flush 内链式传播（signal → computed → effect）依赖调度器「注册顺序即粗略拓扑序」契约**：Computed 必先于其下游消费方注册（要先建 computed 才能 bind 它），故 recompute 必先于下游 effect 在阶段2 运行。flush 前 `get()` 返回 null 占位（与 Effect 首跑时机一致）。
- **过 NORTH_STAR 第 8 节检查清单**：I1/I2/I9（signal/computed 经 ReactiveScheduler）✓ I3（动态行为在 effect/computed 里）✓ I4（bind 按 impact 打对应级别，LAYOUT/PAINT/COMPOSITE 均支持）✓ 无新增常驻缓存（computed 仅多一个 cell + recompute effect 订阅，属信条二既定内存）✓。
- **验证**：compileJava 通过；新增 `ComputedTest`（reactive 层派生/记忆化/链式传播/dispose）+ `HtmlLikeDocumentWidgetStyleBindExpandTest`（通用 bind PAINT/LAYOUT 级、computed 喂入 bind、3 便捷封装、close 停止应用）全绿，`--rerun-tasks` 强制重跑确认非缓存跳过。定向回归 303 tests 仅 8 预存失败，**零回归**。
- **未动（按铁律）**：`UiStyleDeclaration`/`ElementNode` 核心写入路径、`resolveBoxContext`/`DocumentCustomRenderBounds` 坐标语义、scrollVersion 产生与交互侧消费、动画驱动 transform/opacity 仍走 timeline 全量重建。
- **预存失败集（不变）**：`DocumentPaintRendererTest` 7 个字体后端 `NoClassDefFoundError`（缺 LWJGL native）、`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`（环境敏感）。
- **下一步可选（需用户拍板）**：① 真机 runClient 实测 bind 闭环帧率；② **方向乙（I6 契约线提纯）**：paint 层约 138 处持 `ElementNode`、命令自身 21 处 → 阻碍双缓冲跨线程；③ **方向丙（I5 keyed 列表 diff）**：可承接本轮 signal/computed 地基；④ 继续深化甲：signal 驱动的组件「只跑一次」挂载模型（信条三 I3），或更多属性便捷封装。

## 最近一次交接（2026-06-16 第 17 次）—— signal → style 绑定（方向甲，岗路 B：effect 桥接）

- **当前分支**：`refactor/style-signal-binding`（从 `4.0` HEAD `711dda19` 切出），已提交源码 `6dcab7e2`，**待 `--no-ff` 合回 `4.0`**。
- **开工前两项确认（交接遗留已自动消解）**：
  1. `.opencode/opencode.json` 遗留**已由用户独立提交 `711dda19 人工调整` 干净删除**（删 20 行，与第 16 次重构 merge 隔离）。无需再问、未混入本批次。
  2. **方向丁（createEffect javadoc）已先行修正并合回 `4.0`**：javadoc 原写 `PAINT/COMPOSITE → markPaintDirty()`，实现实为三路分支（`COMPOSITE → markCompositeDirty()`）。提交 `62a8c133`，纯注释、无需回归。
- **本轮主攻（用户拍板方向甲 + 岗路 B）**：把 signal 通过 effect 桥接到现有命令式 `setOpacity/setTransform`，打通「signal → effect → COMPOSITE 失效 → 第 16 次 composite-only 回放」完整闭环。
  - **关键背景**：`HtmlLikeDocumentWidget.createEffect` 自第 14 次建立后**零调用方**，signal → composite-only 端到端**从未被验证**。下游（effect→失效→回放）第 16 次已验证，上游（signal→effect→style）此前是空的。本轮补齐上游。
  - **岗路抉择（重要，已与用户确认）**：交接提示词字面写「把 setOpacity/setTransform 写入 signal 化」（= 岗路 A：`opacitySlot/transformSlot` 内部换成 `Signal`）。**调研后否决岗路 A**——`Signal.set` 是异步排队（帧末 flush），会破坏现有「`setX` 后立即 `getX` 拿到新值」的同步契约，且 `UiStyleDeclaration` 2608 行被整个级联系统依赖，改动面极大、几乎必然回归。**改走岗路 B：effect 桥接**——signal 停在数据层，effect body 内仍调命令式 setter，**完全不动 `UiStyleDeclaration`/`ElementNode` 核心写入路径**。这才是信条二「signal → 节点属性细粒度绑定(effect)」的原意（effect 是 signal 与 setter 之间的桥）。
  - **改动（仅 1 源 + 1 测试，+54/源）**：`HtmlLikeDocumentWidget` 新增 `bindOpacity(ElementNode, Signal<Float>)` / `bindTransform(ElementNode, Signal<UiTransform>)`，基于已有 `createEffect(COMPOSITE, body)`，body 内 `element.style().setOpacity/​setTransform(source.get())`；`source.get()` 在追踪上下文自动订阅。null 值跳过。effect 绑定 widget 生命周期，`close()` 自动 dispose。
- **过 NORTH_STAR 第 8 节检查清单**：I1（signal 唯一数据源）✓ I2/I9（经 ReactiveScheduler 批处理）✓ I3（动态行为在 effect 里）✓ I4（走 COMPOSITE 级）✓ I7/I8（composite-only 回放，第 16 次守住）✓ 无新增常驻缓存（只多 effect 订阅）✓。
- **实现细节坑（测试中坐实）**：**每次 effect 重跑产生 2 次 COMPOSITE bump**——① `setOpacity()` 内部经 style change listener → `markCompositeMutated` → `compositeVersion++`；② `createEffect` body 跑完后按声明 impact 显式 `markCompositeDirty()` → 再 `compositeVersion++`。二者**都走 composite-only 回放，对正确性无害**。批处理测试里 3 次 set / 1 次 flush = delta 2（非 6），正好反证 effect 只重跑一次（I9 生效）。
- **验证**：compileJava 通过；新增 `HtmlLikeDocumentWidgetStyleSignalBindingTest` 9 个纯 JVM 测试全绿（首次 flush 应用初值、signal 变重新应用、COMPOSITE 而非 PAINT 失效、I9 批处理、close/dispose 停止应用、单独 dispose effect）。定向回归 431 tests 仅 8 预存失败，**零回归**。
  - **测试走纯数据层路径**（手动 `ReactiveScheduler.flush()`），不调 `render`（依赖 LWJGL native 沙箱缺失）。composite-only 渲染侧由第 16 次引擎/widget 测试覆盖。
- **未动（按铁律）**：`UiStyleDeclaration`/`ElementNode` 核心写入路径、`resolveBoxContext`/`DocumentCustomRenderBounds` 坐标语义、scrollVersion 产生与交互侧消费、动画驱动 transform/opacity 仍走 timeline 全量重建。
- **预存失败集（不变）**：`DocumentPaintRendererTest` 7 个字体后端 `NoClassDefFoundError`（缺 LWJGL native）、`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`（环境敏感）。
- **下一步可选（需用户拍板）**：① 真机 runClient 实测：用 `bindOpacity/bindTransform` 绑定 signal，hover/逻辑改 signal 值，确认走 composite-only（render 段无重建尖刺）；② **方向乙（I6 契约线提纯）**：paint 层约 138 处持 `ElementNode`、命令自身 21 处 → 阻碍双缓冲跨线程；③ **方向丙（I5 keyed 列表 diff）**：依赖响应式视图层，可承接本轮 signal 地基；④ 继续深化方向甲：把更多样式属性（PAINT 级 color 等）纳入 bind 体系，或建立 signal 驱动的组件「只跑一次」挂载模型（信条三 I3）。

## 最近一次交接（2026-06-16 第 16 次）—— composite-only 回放路径

- **当前分支**：`refactor/composite-replay-path`（从 `4.0` HEAD `bc120f2f` 切出），已提交 `21bb60cf`，**待 `--no-ff` 合回 `4.0`**。
- **本轮成果**：实现 composite-only 就地回放，transform/opacity 直接 setX（非动画）不再触发 ~720 条命令全量重建（信条五 COMPOSITE 级，I7/I8）：
  - `DocumentPaintCommand`：`paintContextOpacity` 由 final 改为非 final；新增包内 `updateTransform(UiTransform)`、`updatePaintContextOpacity(float)`。
  - `DocumentPaintEngine`：新增 public `tryApplyCompositeReplay(rootBox, commands)`——按盒树最新 computed style 就地更新 `TRANSFORM_START/END` 的 transform 与 `PAINT_CONTEXT_START` 的 opacity；新增私有 `indexBoxesByElement`（element→box 索引，缺失时登记，保留第一个）。
  - `HtmlLikeDocumentWidget`：`resolvePaintLayoutBox` 拆两路径——paintVersion 变 → 原全量重建；仅 compositeVersion 变 → `tryApplyCompositeReplayOnCache` 就地回放，**不重置** `cachedPaintScrollVersion`。新增私有 `tryApplyCompositeReplayOnCache`、实例字段 `compositeReplayAppliedThisResolve`。
- **关键正确性边界（交接提示词未强调，本轮发现并守住）**：
  1. **结构守卫**：transform identity↔非identity 翻转会增删 `TRANSFORM` 命令对并翻转 transform stacking context；opacity 跨 `0.999` 阈值（`DocumentEffectChain.createsPaintContext` / `DocumentPaintEngine:293`）会增删 `PAINT_CONTEXT` 命令对并翻转 opacity stacking context，二者都改 stacking 排序。任一翻转 `tryApplyCompositeReplay` 返回 false → 回退全量重建（守 I8）。
  2. **opacity 烘焙**：opacity≥0.999 不建 paint context、opacity 烘焙进子命令颜色，无命令可就地更新 → 必须回退（结构守卫已覆盖）。opacity<0.999 时 opacity 经回放期 `fallbackOpacity` 应用、不烘焙进颜色（`DocumentPaintEngine:295` `boxOpacity` 计算），故可就地更新。
  3. **top-layer**：弹窗/select/HUD 子树不在 `rootBox` 内，命令含其盒，无法仅凭 rootBox 比对/更新 → 存在 top-layer 即回退全量重建。
  4. **animationStateChanged 假信号（本轮最大坑）**：`animationTimeline.updateFromLayout`（`updateElementState` FLOAT_PROPERTIES 含 OPACITY/transform 子属性）会把 opacity/transform 基准值变化记为 `animationStateChanged=true`（仅记 transition 起点，**并非有动画在跑**）。这会让 `resolvePaintCommands` 命中检查误判而全量重建，吞掉就地更新。修复：新增 `compositeReplayAppliedThisResolve` 标志，命中检查改为 `animationBlocksReuse = animationWork || (animationStateChanged && !compositeReplayAppliedThisResolve)`——本帧 composite-only 成功且无运行动画（`!animationWork`）时放行复用；有运行动画仍按铁律全量重建。
- **诊断方法论复用**：用 `[CR-probe]`/`[CR-probe2]` stderr 探针定位「composite-only 在 `resolvePaintLayoutBox` 成功了，却被 `resolvePaintCommands` 命中检查的 `animationStateChanged` 重建吞掉」。探针读数从 `build/test-results/test/TEST-*.xml` 的 `<system-err>` CDATA 取。**探针已全部回退**。
- **验证**：compileJava BUILD SUCCESSFUL；新增守护测试——引擎层 5 个（`DocumentPaintEngineTest`：opacity/transform 就地更新成功、opacity 跨阈值回退、transform 变 identity 回退、空命令回退）、widget 层 3 个（`HtmlLikeDocumentWidgetAnimationRuntimeTest`：仅 opacity 变不重建、PAINT 级仍重建、opacity 跨阈值回退重建；widget 级 transform 端到端因 `pushTransform` 依赖 LWJGL native 沙箱缺失，改由引擎层覆盖）。定向回归 422 tests（paint/HtmlLikeDocumentWidget*/layout/reactive）仅 8 预存失败，零回归。
- **预存失败集（不变）**：`DocumentPaintRendererTest` 7 个字体后端 `NoClassDefFoundError`（缺 LWJGL native）、`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`（环境敏感）。
- **未动（按铁律）**：`resolveBoxContext`/`DocumentCustomRenderBounds` 坐标语义、scrollVersion 产生与交互侧消费、动画驱动 transform/opacity 仍走 timeline 全量重建。
- **工作区遗留（与本任务无关，未提交）**：`.opencode/opencode.json` 被删除（工具/环境行为），本轮提交**未纳入**，需用户决定是否恢复或单独处理。
- **下一步可选**：① 用户进 `runClient21` 实测 transform/opacity hover 直改场景帧率（确认就地回放消除重建尖刺）；② 将 `ElementNode` style 改为 signal 化，让 effect 体内响应式逻辑驱动 setOpacity/setTransform（替代命令式调用），打通信条二/三与 composite-only 的完整链路。

## 最近一次交接（2026-06-16 第 15 次）—— compositeVersion 独立版本路径

- **当前分支**：`4.0`，干净，HEAD = `1b1d3c46`。
- **本轮成果**：完成 compositeVersion 独立版本基础设施（信条五，I5），COMPOSITE 失效不再 bump `paintVersion`/`mutationVersion`：
  - `UiDocument`：新增 `compositeVersion` 字段、`recordCompositeMutation()`、`getCompositeVersion()`、`markCompositeDirty()` public API
  - `DocumentNode`：新增 `markCompositeMutated()` protected final
  - `ElementNode.onStyleChanged`：COMPOSITE → `markCompositeMutated()`（独立路径，彻底解耦）
  - `UiDocument.recordStyleSheetMutation`：COMPOSITE → `recordCompositeMutation()`（不再降级为 PAINT）
  - `HtmlLikeDocumentWidget`：新增 `cachedCompositeVersion` 缓存字段，`resolvePaintLayoutBox` 同时检查 paintVersion 和 compositeVersion，`createEffect(COMPOSITE, ...)` 走 `markCompositeDirty()`
- **当前阶段语义**：compositeVersion 变化目前仍触发 `refreshComputedStyles + cachedPaintScrollVersion = -1`（即仍走命令重建）。版本分离已到位；行为等价现状，零功能风险。
- **下一步（composite-only 回放路径）**：
  - 当 compositeVersion 变但 paintVersion 未变时，跳过 `cachedPaintScrollVersion = -1` 重置，改为就地更新命令列表中 TRANSFORM_START 命令的 `transform` 字段和 PAINT_CONTEXT_START 命令的 `paintContextOpacity` 字段
  - `DocumentPaintCommand.paintContextOpacity` 需从 `final` 改为可变，加就地更新方法
  - 此路径完成后，transform/opacity 直接改值（非动画）将不再触发 ~720 条命令全量重建
- **验证**：compileJava 通过；249 tests，仅预存 CursorTest 1 失败（零回归）。
- **预存失败集（不变）**：`DocumentPaintRendererTest` 7 个字体后端失败、`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`

## 最近一次交接（2026-06-16 第 14 次）—— COMPOSITE 失效级 + Owner/Effect 接入 DOM 失效链

- **当前分支**：`4.0`，干净，HEAD = `e481043a`（本批次已合入）。
- **本轮成果**：
  - **补 `COMPOSITE` 失效级**（信条五，I5）：`UiStyleChangeImpact` 新增 `COMPOSITE` 枚举值（优先级 LAYOUT > PAINT > COMPOSITE）。`UiStyleProperty` 中 `OPACITY`/`TRANSFORM` 从 `PAINT` 改为 `COMPOSITE`；`UiStyleDeclaration` 中 `opacitySlot`/`transformSlot` slot impact 同步改为 `COMPOSITE`。
  - **COMPOSITE 降级处理（过渡期）**：`ElementNode.onStyleChanged` 与 `UiDocument.recordStyleSheetMutation` 中 COMPOSITE 目前降级 → `markPaintMutated()`/`recordPaintMutation()`，行为暂同 PAINT。枚举已就位，待后续补 `compositeVersion` 后可独立走 composite-only 回放路径（彻底避免 transform/opacity 变化重建 ~720 条命令）。
  - **UiDocument 公开失效 API**：新增 `markPaintDirty()` / `markLayoutDirty()` public 方法，供响应式 effect 触达 DOM 失效。
  - **HtmlLikeDocumentWidget 接入 Owner/Effect**（信条三，I3，信条四）：
    - 字段 `reactiveOwner = new Owner()`，widget 生命周期内统一管理 effect 订阅
    - `createEffect(UiStyleChangeImpact, Runnable)` 公开 API：body 执行后按 impact 自动触发文档失效（LAYOUT → `markLayoutDirty()`，PAINT/COMPOSITE → `markPaintDirty()`）
    - `drawSelf` 帧初调 `ReactiveScheduler.get().flush()`，保证每帧 signal 变化先于版本检查生效
    - `close()` 方法 dispose 全部 effect 订阅
- **验证**：`compileJava` BUILD SUCCESSFUL；`ReactiveSystemTest` 11 个全绿；`HtmlLikeDocumentWidget` 系列 170 tests，仅预存 CursorTest 环境敏感 1 失败（零回归）；Paint/Layout/Animation 定向回归仅预存 DocumentPaintRendererTest 7 个字体后端失败（零回归）。
- **架构状态更新**：
  - 信条五分级失效：LAYOUT/PAINT 两级 → 三级（新增 COMPOSITE），枚举已就位；COMPOSITE 真正免重建待后续实现
  - 信条三/四：Effect → DOM 失效链已连通，外部可通过 `widget.createEffect(impact, body)` 在响应式上下文中驱动文档失效
  - 响应式数据层与命令式 DOM 已初步接轨；signal 更新 → effect 触发 → document dirty → paintVersion bump → 渲染流水线感知变化
- **重构下一批次**：
  - 实现 COMPOSITE 真正独立路径：在 `UiDocument`/`HtmlLikeDocumentWidget` 引入 `compositeVersion`，COMPOSITE 失效不 bump `paintVersion`，回放期只重新应用 transform/opacity 参数，避免全量重建
  - 将 `ElementNode` 的 style signal 化：把 `element.style().setOpacity()` / `setTransform()` 改为写 signal，让 effect 体内的响应式逻辑驱动样式变化（替代命令式调用路径）
- **预存失败集（不变）**：`DocumentPaintRendererTest` 7 个字体后端 `NoClassDefFoundError`（缺 LWJGL native）、`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`（环境敏感）

## 最近一次交接（2026-06-16 第 13 次）—— 响应式数据层地基试点完成

- **当前分支**：`4.0`，干净，HEAD = `8046a1d8`（响应式试点已合入）。
- **本轮成果**：新增 `club.heiqi.uilib.ui.reactive` 包（数据层地基第一批，零 Minecraft 依赖，零现有代码改动）：
  - `Signal<T>`：可写 signal 原子，`get()` 自动追踪依赖，`set()` 经调度器批处理（I1/I2）
  - `Effect`：自动追踪依赖的副作用单元，支持动态依赖重连（I3）
  - `ReactiveScheduler`：中央事务调度器，signal 写入唯一收口，帧末统一 flush（I2/I9）
  - `Owner`：生命周期作用域，`dispose()` 一次性清理全部子 effect（信条三）
  - `ReactiveContext`（包内）：线程局部追踪上下文
  - `ReactiveSystemTest`：11 个纯 JVM 测试，全部通过（0 failure/0 error）
- **重构下一批次**（待立项）：将 Effect 接入 DOM 失效层
  - 在 `UiStyleChangeImpact` 补 `COMPOSITE` 级
  - Effect 触发时按失效级别标记对应 `ElementNode`，替代现有 `layoutVersion/paintVersion` 命令式版本号
  - 这是信条四（中央事务 → DOM 失效路径）与信条五（分级脏标记）的交汇点
- **协作规范**：架构改动前先对照 `NORTH_STAR.md`（宪章 §0/§8）；决策点优先中文提问。

## 最近一次交接（2026-06-16 第 12 次）—— 引入 NORTH_STAR 架构宪章（仅文档，不动源码）

- **当前分支**：`docs/introduce-north-star-charter`（从 `4.0` 切出，HEAD 起点 `3699d11c`）。本轮**只动文档、零源码改动**，待用户确认后 `--no-ff` 合回 `4.0`。
- **任务**：把外部 `D:\CodeSpace\Blank\NORTH_STAR.md`（UI 系统设计宪章）引入本项目作为最高架构准绳；用户明确「此后再据此做大型重构」，故本轮不重构。
- **已完成（全部为文档）**：
  - 宪章正本复制到仓库根目录 `NORTH_STAR.md`（内容与源一致，仅去尾部空行；已 SHA/逐行核对）。
  - `AGENTS.md` + 镜像 `CLAUDE.md` 新增第 0 节「架构宪章」：架构改动前必先对照宪章、评审用 I1-I9 核对、冲突时「先改文件再改代码」、信条/不变量改动须经用户确认。**（AGENTS.md 改动已经用户确认）**
  - 文档导航接入：`docs/README.md`（顶部 callout + 路线表 + 职责边界）、`docs/AI记忆文档.md`（导航表 + 新增「架构决策前置」）、`docs/记忆/README.md`（读取建议第 7 条）、`docs/记忆/长期事实/架构边界.md`（新增「最高准绳」节）。
  - 新增决策 `docs/记忆/决策/DECISION-20260616-north-star-charter.md` 并登记到决策索引。
  - 新增现状差距评估 `docs/开发者文档/reviews/REVIEW-20260616-north-star-alignment-gap.md` 并登记到审查索引。
- **差距评估核心结论（基于源码，非乐观口径，供下一步重构用）**：
  - **渲染层已大体对齐**：`DocumentPaintCommand` 是事实上的 Display List；渲染层 `ui/render/*` 对 `ElementNode` 引用 = 0、GL 收敛于此（~323 处）→ I6 渲染侧达成；脏子树布局缓存、样式 LAYOUT/PAINT 分级标注、合成层/字形图集/滚动免重建可作重构地基。
  - **数据层基本不存在**：无 signal/effect/依赖图（全包搜索无业务响应式类）、无中央事务与状态快照（仅有构建期 `WidgetBuildAttachmentTransaction`，与状态写入无关）、无 keyed 列表协调。当前更新链路 = 命令式改 DOM → `markSubtreeMutated()` → `layoutVersion++/paintVersion++`。→ 信条一/二/四、I1/I2/I3/I5 未满足。**这是大型重构主战场。**
  - **分级失效缺 COMPOSITE**：`UiStyleChangeImpact` 仅 `LAYOUT`/`PAINT`；`TRANSFORM`/`OPACITY` 归 `PAINT` → 与前几轮交接「transform/hover 触发 paintVersion bump、~720 命令全量重建」掉帧同源。补 COMPOSITE 级相对独立且能直接缓解。
  - **契约线提纯项**：Paint 层 `ui/paint/*` 仍约 135 处持有 `ElementNode`、命令自身约 21 处 → I6 数据侧未达成，阻碍宪章第 10 节双缓冲跨线程。
- **重构 6 步优先级建议（已写入 REVIEW 第 5 节，待用户立项）**：① 数据层地基（signal + 中央事务）② 响应式绑定与组件只跑一次 + keyed 列表 ③ 补 COMPOSITE 失效级 ④ 提纯 Display List 契约线 ⑤ 失效模型迁移到分级脏标记 ⑥ 保留式 GPU 场景 + 脏矩形。
- **验证**：本轮无源码改动，未跑 compileJava（无必要）。文档链接与索引已自查。
- **待办**：① 用户确认后将 `docs/introduce-north-star-charter` `--no-ff` 合回 `4.0`；② 据宪章对大型重构分批立项（建议从第 1 步数据层地基的隔离试点开始）；③ 重构每完成一批，回填 `REVIEW-20260616`（活地图）。
- **协作规范**：决策点优先中文提问工具；据宪章重构前过第 8 节《决策检查清单》，过渡期若需违反信条/不变量须在宪章第 11 节《偏离登记》显式登记。

## 最近一次交接（2026-06-16 第 11 次）—— 方向F（方案2 收益未兑现）治本完成并合回 4.0

- **当前分支**：`4.0`，干净，HEAD = merge `098cb9a6`（方向F 已合入）。开发分支 `perf/scroll-relative-descendant-eligible`（commit `e0342b58`），`--no-ff` 合回。**待用户重启调试会话实测帧率确认收益**（IDE HotSwap 不支持本次的新增方法/改签名，必须重启 `1d. Run Client (Java 21, Hotswap)` 带新代码）。
- **真凶（探针坐实，与方向F 提示词假设1 完全吻合）**：ModernConfig 含 PRESET_SELECTOR 字段 → 引入 `DocumentSelectControl` → 其触发器元素恒设 `position:relative`（`DocumentSelectControl.java:205`，给 absolute 弹窗当定位锚点，弹窗关闭 display:none 不入树、但触发器 relative 常驻）→ 根滚动容器 `hasPositionedDescendant()` 返 true → 判 ineligible → 登记进 scrollDependencies 快照 → 每次滚动 `scrollDependenciesSatisfied()` 返 false → 全量重建。探针读数：滚轮帧 `phases=F.inv.scrollDep`、render 20~21ms；静置帧 `F.cacheHit`、render 5~9ms。`[F-probe]` 直接点名 ineligible 容器=`<document>`、第一个 positioned 后代=`<select> pos=RELATIVE`。**假设3（hover/paintVersion bump）已排除**（滚轮帧标签是 scrollDep 不是 fresh）。
- **关键陷阱（推翻提示词假设2 的「relative 可简单放行」）**：relative + 无 z-index 的盒 → `getStackingPhase()` 归 `POSITIONED_AUTO_OR_ZERO`（`DocumentLayoutBox.java:509`），其命令在 `appendBoxCommands` 的 positioned 阶段生成、**落在 normal-flow 段 SCROLL_OFFSET 作用域之外**（line 352-364 在 END 之后）。单纯放行 relative → 容器 eligible 免重建，但 select 命令固化构建期 scroll、作用域外无 delta 补偿 → **滚动后 select 卡原位错位**。
- **治本方案（用户拍板，全在 `DocumentPaintEngine.java` +95 净）**：
  - **收窄 eligibility**：`isReplayScrollOffsetEligible` 加 `collectsPositionedDescendants` 参数。盒**自身收集 positioned 后代**（建 stacking context 或为绘制根，如 ModernConfig 根容器走 if 分支）→ 用新 `hasScrollEscapingDescendant` 只拦 absolute/fixed/sticky、**放行 relative**；盒**不收集**（非 stacking context 的 else 分支，relative 后代会逃逸到祖先 stacking context 收集、脱离本作用域）→ 退回原 `hasPositionedDescendant` 全拦，保守正确。命令生成（line 319 传 `resolvedPaintStackingContext`）与快照收集（`collectScrollDependencies` 加 `paintRoot` 参数 + 静态 `createsStackingContext()` 判据，静置帧无动画运行值故静态判据安全）两个调用点判据统一。
  - **positioned 阶段按需补作用域**：`appendStackingPhaseItems` 加 `wrapScrollOffset` 参数，NEGATIVE/AUTO/POSITIVE 三阶段在 phase **非空**时各自补一对 `SCROLL_OFFSET_START/END`，使 relative 后代回放期同样叠加 -scroll；其内文本同步 `deferTextClip`。**空阶段不 emit**（否则污染命令序列、增加回放开销——中途曾因无条件 emit 引入 4 个命令序列回归，已修）。
  - **为何不能整段一对作用域**：内容 clip 框（`ClipContext` 用 `boxOffset` 不含自身 scroll，是视口框）必须在作用域外，否则被 delta 平移错位；故 START 不能提到内容 clip push 之前，positioned 阶段须单独补对。
- **未动（按铁律）**：`resolveBoxContext`/`DocumentCustomRenderBounds` 坐标语义、scrollVersion 产生与交互侧消费。
- **验证**：compileJava 通过。新增 3 守护测试全 PASS（`HtmlLikeDocumentWidgetScrollTest`：`shouldNotRebuildPaintCommandsWhenScrollingContainerWithRelativeDescendant` 免重建+位置随滚动线性上移、`shouldRebuildPaintCommandsWhenScrollingContainerWithAbsoluteDescendant`、`shouldRebuildPaintCommandsWhenScrollingNonStackingContainerWithRelativeDescendant`）。`DocumentPaintEngineTest` 52 / `HtmlLikeDocumentWidgetScrollTest` 29 全绿。定向回归（paint.* / HtmlLikeDocumentWidget* / 文本控件 / layout.*）零回归，仅余预存失败（`DocumentPaintRendererTest` 7 字体后端 + `HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`）。
- **探针法复用**：`HtmlLikeDocumentWidget.resolvePaintCommands` 拆 `F.cacheHit`/`F.inv.anim`/`F.inv.fresh`/`F.inv.transient`/`F.inv.scrollDep` 走 `UiPerformanceMonitor.recordPhase`；`DocumentPaintEngine.collectScrollDependencies` 一次性 dump ineligible 容器 + 第一个 positioned 后代（`MyMod.LOG`）。**探针已全部干净回退**（先 edit 误重复，最后用 `git checkout` 恢复 HEAD 起步施工）。
- **待办**：① 用户重启调试实测帧率（滚轮帧 render 是否从 20~51ms 降到静置 ~3.5~5ms、select 下拉框滚动后位置正确）；② 实测通过后视情况补 CodeGraph 记忆（architectural_decision「relative 后代放行 + positioned 阶段补 SCROLL_OFFSET 作用域」）+ 决策文档；③ 用户原疑问「文本裁切是否该放回放期」：ModernConfig 走 eligible 后文本走回放期延迟裁剪，可后续评估 `renderDeferredClipTextCommand` 占回放比重再答。
- **协作规范**：决策点优先中文提问工具；帧率/探针实测必交用户跑（IDE 已接入，结构性改动需重启、纯方法体改动可 HotSwap）。

## 最近一次交接（2026-06-15 第 10 次）—— 方向E（方案2）施工完成，待用户实测+提交

- **当前分支**：`4.0`（**尚未开命名分支、未提交**）。任务：方向E「滚动期绘制命令免重建 = 命令坐标解绑滚动偏移、回放期叠加」**全部 5 阶段已实施并通过验证**，等用户实测帧率后再开分支提交、`--no-ff` 合回 `4.0`。
- **方案2 最终实现路线（与第 9 次提示词的差异，重要）**：未采用「`resolveBoxContext` 不再减 scrollOffset、回放期重算全部坐标」的激进路线。**命令坐标保持现状（照常烘焙构建期 scroll，`resolveBoxContext`/`DocumentCustomRenderBounds` 坐标语义零改动）**，改用 **delta 模型**：
  - eligible 滚动容器的 flow content 段外层包一对 `SCROLL_OFFSET_START/END` 命令；`SCROLL_OFFSET_START` 的 left/top 携带「构建期 scroll 快照」。
  - 回放期 `delta = 构建期scroll - 当前scroll`，叠加进 `RenderReplayState` 累计偏移栈；最终视觉位置 = 命令坐标 + delta。
  - provider 为 `NONE`（测试/旧调用方）时 delta 恒为 0 → **逐像素等价现状**，现有测试零改动（除命令序列断言）。
  - 好处：完全不碰 hit-test/sticky/CUSTOM 共用的坐标烘焙，正确性风险面最小。
- **eligibility**：可滚动（任一方向 maxScroll>0）且子树无 positioned 后代（`hasPositionedDescendant`），保证 SCROLL_OFFSET 作用域精确等于 flow content 段。sticky/fixed/absolute/relative 后代都使容器 ineligible → 回退重建。
- **缓存失效改为「滚动依赖快照」**（不碰 scrollVersion 模型）：`DocumentPaintEngine.buildPaintPlan` 返回 `DocumentPaintPlan`（命令 + `Map<ElementNode,int[]{scrollLeft,scrollTop}>` 快照）。快照登记**所有 ineligible 可滚动容器**（用户决策放宽：不只 sticky/fixed，含 absolute/relative，堵脏渲染）。`HtmlLikeDocumentWidget.resolvePaintCommands` 缓存命中条件改为：`cachedPaintScrollVersion>=0`（曾构建过哨兵）且 transientScrollbar 未变且（`scrollVersion` 未变 ‖ `scrollDependenciesSatisfied()`：快照空恒满足、否则各容器当前偏移==构建期快照）。**普通页面（含 ModernConfig，全 eligible）快照为空 → 滚动永不重建。**
- **scrollbar thumb 回放期实时重算**：thumb 命令落在 SCROLL_OFFSET 作用域外（不随滚动平移），故 `DocumentScrollbarThumbReplay`（element+vertical+trackStart+travel+thumbSize+maxScroll）附在 THUMB 命令上，回放期 `renderScrollbarThumb` 按当前 scrollState 实时重算主轴起点。track 不重算（视口框）。
- **文本裁剪搬到回放期**：eligible 容器内文本构建期全量生成（打 `clipDeferred` 标记、跳过构建期裁剪），回放期 `renderDeferredClipTextCommand` 按 clip 矩形交集做整 run 剔除 + 超长单行（≥64 字符）横向二分裁切。
- **drawSelf**：`DocumentPaintRenderer.render(..., scrollOffsetProvider)`，provider 委托 `scrollState.getScrollLeft/Top`。
- **改动文件**：源码 `DocumentPaintCommand`(thumbReplay 字段)、`DocumentPaintCommandType`(SCROLL_OFFSET_START/END)、`DocumentPaintEngine`(emit+eligibility+buildPaintPlan+快照收集)、`DocumentPaintRenderer`(偏移栈+clip栈+延迟裁剪+thumb 重算)、`HtmlLikeDocumentWidget`(缓存改造+provider)、新增 `DocumentPaintPlan.java`/`DocumentScrollbarThumbReplay.java`。
- **必修 BUG 已修**：`appendScrollOffsetStartCommand` 原传 0,0，已改为携带 `scrollState.getScrollLeft/Top` 构建期快照（否则 delta 漏 +构建期scroll 项、滚动后错位）。
- **验证**：`compileJava` 通过。新增测试全绿——`DocumentPaintRendererTest`：`shouldCarryBuildScrollSnapshotInScrollOffsetStartCommand`、`shouldDeferScrollOffsetToReplayPixelIdentical`、`shouldReplayScrollbarThumbFollowingLiveScroll`；`HtmlLikeDocumentWidgetScrollTest`：`shouldNotRebuildPaintCommandsWhenScrollingEligibleContainer`（滚动后 `getPaintCacheGenerationForDiagnostics()` 不变）、`shouldKeepHitTestAccurateAfterRebuildFreeScroll`、`shouldRebuildPaintCommandsWhenScrollingContainerWithStickyDescendant`。**更新了 6 个既有 `DocumentPaintEngineTest` 断言**（4 个命令序列加入 SCROLL_OFFSET 对：opacity/custom-color size4→6、offset-content/custom-cmd size6→8；2 个文本裁剪测试改写为验证 `clipDeferred` 延迟语义：`shouldDeferTextClipForReplayInEligibleScrollContainer`、`shouldKeepBuildTimeTextClipWithoutScrollState`）。
- **完整套件**：1254 tests，9 failed。**已用 `git stash` 在干净基线复验：这 9 个失败完全相同**（`DocumentPaintRendererTest` 7 个字体后端 `NoClassDefFoundError`/`NoSuchMethodException` + `TextLayoutServiceTextContentModeTest` + `HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`），均为测试环境缺 Minecraft 字体 natives 的固有失败，**零回归**。
- **未修改**：`resolveBoxContext`、`DocumentCustomRenderBounds.boundsOf/scrollOf` 坐标语义、scrollVersion 产生与交互侧消费（指针更新管线）——全部按铁律保持。
- **待办**：① 用户实测帧率比对（滚轮滚动 ModernConfig ~10s，看 `render=`/`fps=`/`slowFrames=` 是否消除重建尖刺）；② 实测通过后开命名分支（建议 `perf/scroll-offset-replay-time`），标题 `[English]: 中文标题`，`--no-ff` 合回 `4.0`；③ 视情况补 CodeGraph 记忆（architectural_decision「方案2 delta 模型 + 滚动依赖快照」）。
- **协作规范**：决策点优先中文提问工具；帧率实测必交用户跑（沙箱无 GUI）。

---

## 历史交接（2026-06-15 第 9 次）

- **当前分支**：`4.0`，干净（源码无改动，仅 `docs/记忆/当前态/` 两个文件有上轮遗留预修改，与本任务无关）。HEAD 仍是方向C 的 merge `fadb8a00`。任务：方向D（输入帧 render 段全量命令重建）**已完成诊断定性，未动手实施**；用户已选定走「方案2 架构性调整」，施工提示词见文末。
- **方向D 真凶（七轮外科探针逐层坐实，全程数据驱动，推翻原提示词三处预设）**：
  - **提示词预设①「hover → paintVersion bump → `refreshComputedStyles` 整树刷样式是大头」→ 证伪**：探针 `D.refreshStyles` 在所有帧几乎不出现，paintVersion 输入态基本不 bump，refreshComputedStyles 不是真凶。
  - **提示词预设②「鼠标移动 hover 触发伪失效」→ 证伪**：纯移动鼠标（无滚轮）全程 `D.cacheHit`（render ~3.5~5ms），分类计数器 `D.scBump.nonWheel` 恒为 0。之前几轮误判，是因为测试时"移动鼠标"夹带了滚轮滚动。
  - **真凶坐实 = 滚轮滚动**：堆栈采样 100% 指向 `DocumentScrollState.handleWheel → updateOffsets → version++` → 下一帧 `resolvePaintCommands` 发现 `scrollState.getVersion()` 变化（`cachedPaintScrollVersion != scrollVersion`）→ `DocumentPaintEngine.buildPaintCommands` 全量重建 ~740 条命令、~16ms → fps 跌到 60~110、slowFrames 飙到 50~90/120。打字帧另叠加 `D.relayout`（文本变更 bump layoutVersion 全量重排，固有成本，本方向不处理）。
- **机制坐实**：滚动偏移在 `DocumentVisualTraversal.resolveBoxContext`（约 292~293 行 `childOffsetX/Y = boxOffsetX/Y - getScrollLeft/Top(scrollState, box)`）减入，经 `createEntry`（约 483 行）向整棵后代子树传播，最终被 `DocumentPaintEngine` 固化进每条命令的**绝对坐标**。offset 一变全部命令坐标失效 → 只能整批重建。
- **探针法复用**：本轮探针通道走 `UiPerformanceMonitor.recordPhase`（render 段 `drawSelf` 期 FrameSession 活跃，稳态 `phases=` 空可独占）；`resolvePaintCommands` 拆 `D.cacheHit`/`D.relayout`/`D.refreshStyles`/`D.buildCmd` 四段 + 失效原因细分（`D.inv.animState`/`D.inv.scrollBump`/`D.inv.transient`）+ 命令数差值（`D.cmdN<总数>d<差值>`，坐实滚动帧 740 条只差 0~4 条）+ `DocumentScrollState` 加 wheel/nonWheel 分类计数器 + 一次性堆栈采样（每秒限一行）锁定调用链。**全部探针已回退，工作区干净**。
- **方向选择（用户决策）**：选「方案2 命令坐标解绑滚动、回放期叠加偏移」做架构性根治（滚动是 UI 最高频操作，主流引擎都把滚动做成回放期偏移而非重建）。已评估排除方案1（子树命令平移，栈配对高风险）、方案3（buildPaintCommands 内部提速，治标仍每帧重建）、方案4（节流，会回退"滚动时指针刷新"）。
- **指针更新回退风险（已坐实可控）**：用户特别关注"滚动时指针样式不更新"的历史脆弱区。坐实结论：指针更新走**交互管线**（`onMouseScroll → findElementAt → hitTest`，line 689~715 + `dispatchLatestScrollIfChanged` line 1309~1316 检测 `scrollState.getVersion()` 变化后 `updateHoveredElement`），与**绘制管线**（命令生成/回放）分离。方案2 只解绑「绘制命令缓存 ↔ scrollVersion」，**严禁动 scrollVersion 的递增与交互侧消费**，则指针更新零回退。必须加专门回归测试覆盖"滚动后指针刷新"。
- **预存失败集（7 个，与本任务无关勿误判）**：`DocumentPaintRendererTest` 6 个 GL11 `NoClassDefFoundError`（缺 LWJGL native）+ 1 个 `shouldTreatTableSectionBoundaryAsSharedLastRowOnlyForFinalRow` `NoSuchMethodException`；`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails` 环境敏感。
- **协作规范**：决策点优先用中文提问工具（用户不读英文）；帧率/探针实测必交用户跑（沙箱无 GUI）。

---

## 历史交接（2026-06-15 第 8 次）

- **当前分支**：`4.0`，干净。任务：ModernConfig 配置页**方向C（输入期 FPS 暴跌）已治本完成并合回 `4.0`**（开发分支 `perf/hit-test-reuse-computed-style` 提交 `81509818`，`--no-ff` 合并 `fadb8a00`，HEAD）。
- **方向C 真凶（三级外科探针逐层坐实，全程无假设）**：输入态（鼠标移动/打字）`onMouseMove → findElementAt` 高频触发，input 段 4~9ms。三级探针逐层下钻：①一级拆 input 段 → `in.hitTest`（命中测试本体）最大头、`in.resolveBox`（`resolveInteractiveLayoutBox` 全树推进动画时间线）次之约 35%、`in.hover` 可忽略；②二级拆 `in.hitTest` → `in.ht.traverse`（递归命中遍历）几乎吃满、`in.ht.scene`（`resolveVisualScene` 场景重建）≈0；③三级拆 traverse → **`in.ht.compute`（命中遍历对每个盒重复调无缓存 `UiStyleResolver.compute()` 算 visibility/pointer-events）占 traverse 75~80%（~6ms）**。**原提示词疑似 `resolveInteractiveLayoutBox` 全树推进是真凶——坐实为否**，真凶是命中遍历里的重复全量级联（无缓存、祖先递归、选择器匹配遍历所有规则，每盒 ×2），而布局期早已把同一份 computed style 缓存在 `box.getComputedStyle()` 里。与方向B 真凶不同源。
- **修复（已合入，单文件 `DocumentHitTestEngine.java` +71/-5）**：命中测试热路径复用 `box.getComputedStyle()` 替代 `UiStyleResolver.compute(element)`。新增带 `ComputedStyle` 参数的重载 `isSelfHitTestSuppressed/isSelfHitTestVisible/isPointerEventsEnabled` 与私有 `isVisibilityHidden(ComputedStyle)`；`hitTestBox` self 检查传 `box.getComputedStyle()`；`hitTextRuns/hitInlineFragments` 在 `ownerElement == box.getElement()`（直接文本/内联子，覆盖绝大多数）时复用 box style，否则回退原 `compute`。**正确性保障已坐实**：`VISIBILITY` 与 `POINTER_EVENTS` 在 `UiStyleProperty` 均标记 `UiStyleChangeImpact.PAINT`，变化触发 paint 失效 → `refreshComputedStyles()` 刷新 box style；且布局期 `computeStyleWithCache` 与命中测试用的 `compute(element)`（均不传 activeStates）语义一致（DOM 父级联、无激活伪类），缓存样式与即时计算零差异。
- **实测收益（用户跑客户端）**：input 段 `in.ht.compute` ~6ms 已消除，input 段从 4~9ms（峰值 11~12ms）降到 ~1.6~5ms。
- **验证**：`compileJava` 通过；定向回归 167 tests 仅 1 失败 = 预存环境敏感 `HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`（与本任务无关），其余含 `DocumentHitTestEngineTest`/事件分发/滚动/拖拽/文本控件全过，零回归。**三级探针已全部回退**（`DocumentHitTestEngine` 3 个 `debug*Nanos`、`HtmlLikeDocumentWidget` findElementAt/onMouseMove 计时、`UiPerformanceMonitor` 的 `recordInputPhase`+`InputSession.phaseSamples`+applyInput merge+limit 全部恢复 HEAD）。
- **遗留新瓶颈（本轮范围外，用户已决策本轮先收口）**：实测暴露——凡 `events=1` 的输入帧，**render 段**（非 input）从静置 ~3.5ms 跳到 ~20ms，slowFrames 飙升。强烈提示输入（hover 状态变更）触发 `paintVersion` bump → 命令全量重建（~720 条 paint 命令）。这是**方向A（增量命令重建）**，前几轮交接已识别但判定偶发未做；现确认输入态会稳定触发，是独立于方向C 的下一个瓶颈。下一 agent 可接力：嫌疑在 hover/active 伪类变更使 `document.getPaintVersion()` bump 触发 `resolvePaintLayoutBox → refreshComputedStyles` + `DocumentPaintEngine.buildPaintCommands` 全量重建。
- **预存失败集（8 个，与本任务无关勿误判）**：`DocumentPaintRendererTest` 6 个 GL11 `NoClassDefFoundError`（缺 LWJGL native）+ 1 个 `shouldTreatTableSectionBoundaryAsSharedLastRowOnlyForFinalRow` `NoSuchMethodException`；`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails` 环境敏感。
- **诊断方法论复用（本轮再次验证有效）**：input 段计时走独立 `UiPerformanceMonitor.InputSession`（ThreadLocal，与帧 `FrameSession` 分离），探针子段需挂 InputSession 并 merge 进帧 `phaseSamples` 复用 `phases=` 日志通道（稳态该字段空，正好独占）；三级逐层下钻而非一次拆到底，每层坐实再决定下钻方向，避免假设错真凶。
- **协作规范**：决策点优先用中文提问工具（用户不读英文）；帧率/探针实测必交用户跑（沙箱无 GUI）。

---

## 历史交接（2026-06-15 第 7 次）

- **当前分支**：`4.0`。任务：ModernConfig 配置页渲染**方向B（稳态 FPS 天花板）保底层已实测达标**；治本层待施工（下方有专门施工提示词）。
- **方向B 真凶（外科探针一击坐实，已推翻交接记录原假设）**：custom renderer 每帧调 `getDocumentBounds()`（只读）经 `HtmlLikeDocumentWidget.resolveInteractiveLayoutBox()` **每次无条件推进整棵动画时间线**（`animationTimeline.updateFromLayout` 全树递归 + `flushCompletedAnimationEvents`），而绘制管线每帧已推进过一次、bounds 查询又在回放期（paint 之后）——十余次重复推进。`BoundsProbe` 拆三段实测：稳态命中率 100%、`index`（保底层场景缓存）仅 0.02ms、**`interactive` 段占 99.9%**（~580ms/秒、~0.88ms/call）。**原假设「场景重建是大头」被推翻**——保底层缓存完美工作但单独零收益。
- **本轮已实施的修复（工作区、未提交）**：
  - **保底层缓存（上轮遗留，已验证有效）**：`DocumentVisualTraversal.indexBoxLocations(scene)`（+37，单趟 DFS 展开 `IdentityHashMap<ElementNode,BoxLocation>`）+ `HtmlLikeDocumentWidget` 按场景签名缓存定位索引（含 5 个 `cachedBoundsIndex*` 字段）。
  - **本轮新修复（真凶治理保底层）**：`HtmlLikeDocumentWidget.resolveLayoutBoxForBoundsQuery()`——bounds 只读查询稳态走此路径，只复用版本键控静态布局盒 + 幂等 `updateScrollStateFromCachedLayoutIfNeeded()`，**不推进时间线、不派发动画事件**；运行态动画（`hasLayoutRuntimeValue()`）回退完整 `resolveInteractiveLayoutBox()` 保实时正确。`resolveCachedBoxLocation` 改走此路径。
- **实测收益（用户跑客户端三轮）**：稳态 `frame ~20ms→~13ms`、`fps 46→75`（接近 vsync 上限）、单次 bounds 查询 0.88ms→0.37ms。卡顿问题解决。
- **验证**：`compileJava` 通过；定向回归 89 tests 零失败（`HtmlLikeDocumentWidgetBoundsIndexCacheTest`3 + `HtmlLikeDocumentWidgetAnimationRuntimeTest`31 + `DocumentTextAreaControlTest`14 + `DocumentCodeEditorControlTest`23 + `DocumentTextInputControlTest`18，含改动最敏感的动画运行态）。**探针已全部回退**（删 `BoundsProbe.java`、`resolveCachedBoxLocation` 4 个 nanoTime 移除），`git status` 仅剩 `HtmlLikeDocumentWidget.java`(+72) / `DocumentVisualTraversal.java`(+37) / 新增 `HtmlLikeDocumentWidgetBoundsIndexCacheTest.java`。
- **治本层（用户已决策「继续补」，待下一 agent 施工）**：残留 `interactive` 段仍占 ~99.8%（~0.37ms/call、每帧 ~4.7ms、占 13ms 帧约 35%），残因是 `hasLayoutRuntimeValue()` 每次 bounds 查询仍 6 次遍历 `animationTimeline.states`。治本：命令构建期（`DocumentPaintEngine.appendCustomCommand`，已持有 box）固化 viewport/text/layer bounds 传入 CUSTOM 命令，renderer 回放期完全不调 `getDocumentBounds()`，可清零这 4.7ms、帧时压到 ~8ms。**注意 `DocumentLayoutBox` 无 `getParent()`，父/兄弟 box 须从遍历调用栈传入，要改 `appendCustomCommand` 签名**。详见文末「给下一个 agent 的治本层施工提示词」。
- **预存失败集（8 个，与本任务无关勿误判）**：`DocumentPaintRendererTest` 6 个 GL11 `NoClassDefFoundError`（缺 LWJGL native）+ 1 个 `shouldTreatTableSectionBoundaryAsSharedLastRowOnlyForFinalRow` `NoSuchMethodException`；`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails` 环境敏感。
- **文档**：`ERROR-20260615-flex-anonymous-item-layout-self-pollution.md` 末尾已补「第四层瓶颈（方向B）」+「第四层诊断复用经验」（只读 getter 谨防副作用 / 探针拆到方法段粒度 / 零收益修复也要留探针证伪）。
- **协作规范**：决策点优先用中文提问工具（用户不读英文）；帧率/探针实测必交用户跑（沙箱无 GUI）。
- **待办**：① 治本层施工（见提示词）；② 经用户确认后提交到 `4.0`（保底层+真凶修复+测试 一个 commit；文档/交接 一个 commit；治本层完成后再一个 commit）；③ CodeGraph MCP 本轮持续超时未存记忆，恢复后补：convention「决策点优先用中文提问工具」+ debug_context「方向B 真凶=getDocumentBounds 每次推进动画时间线全树遍历，非场景重建」。

---

## 历史交接（2026-06-15 第 6 次）


- **当前分支**：`4.0`。任务：ModernConfig 配置页渲染第三层瓶颈——hover/paint 变更触发全文档命令重建（方向A，增量命令重建）。
- **三层修复均在工作区、未提交**：
  - **第一层**（见下方第 5 次交接）：`ElementNode.__putGeneratedAttribute` + `FlexLayoutHelper.flushAnonymousTextItem`，消除 flex 匿名文本项布局期自污染 layoutVersion。
  - **第二层**（固化样式）：`DocumentPaintCommand` 加 `elementStyle` + `withElementStyle()`/`getElementStyle()`；`DocumentPaintEngine` 生成 BORDER/OUTLINE/BOX_SHADOW/TEXT/inline-border 命令时固化样式；`DocumentPaintRenderer.resolveCommandStyle()` 优先读固化。**实测 `styleRecompute=0`、零回归，但 replay 几乎没降**——证明样式重算不是 replay 主成本（旧逐帧 styleMemo 已摊销同帧重复 compute）。第二层作为正确低风险清理保留。
  - **第三层（本次）**：`DocumentLayoutBox.refreshComputedStyles()` 从裸 `compute(element)` 改为自顶向下传 DOM 父样式 + 元素实例备忘的 `computeStyleWithCache`，O(N×D)→O(N)，与布局期 `LayoutContext.computeStyle` 同语义；匿名 flex item 分支保持沿用 `computedStyle`。diff 仅 `DocumentLayoutBox.java`（+40）。
- **第三层探针实测归因（数据驱动，已推翻静态假设）**：22 秒 hover 客户端实测——
  - `refreshStyles` 段实测仅 **8.42ms 且整轮只触发 1 次**（远非预想的「第一真凶」）；`buildCmd` 段 16~46ms 是 rebuild 大头，但 rebuild 本身偶发（仅 hover 切换新控件 / window resize 那几帧）。
  - **决定性发现**：连续 12 秒 `events=0` 稳态帧依旧 `replay 16~20ms`、FPS 卡 41~52。**FPS 天花板由每帧 replay（方向B，本任务范围外）决定，非命令重建（方向A）**。增量重建只能消偶发尖刺，无法提升稳态 FPS。
- **第二步增量重建（2a 脏子树 refresh / 2b 命令分段 splice）经用户决策不实施**：实测收益不在范围内（方向A只是偶发尖刺），高风险（栈配对）不值当，留待与方向B 一并评估。第三层只收第 1 步（refreshStyles O(N)）。
- **验证**：`compileJava` 通过；定向测试（`DocumentPaintEngineTest`/`DocumentPaintRendererTest`/`UiStyleResolverTest`/`HtmlLikeDocumentWidget*`/`DocumentLayoutEngineTest`）除 8 个预存失败外全过，与干净基线失败集完全一致（零回归）。第 1 步由 `HtmlLikeDocumentWidgetAnimationRuntimeTest.shouldRefreshPaintOnlyStyleWithoutRecomputingLayout` 直接覆盖。
- **预存失败集（8 个，与本任务无关）**：`DocumentPaintRendererTest` 6 个 GL11 `NoClassDefFoundError`（classpath 缺 LWJGL native，崩在 `drawText`）+ 1 个 `shouldTreatTableSectionBoundaryAsSharedLastRowOnlyForFinalRow` `NoSuchMethodException`；`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails` 环境敏感。
- **临时探针已全部回退**：本轮新加（`refreshStyles`/`buildCmd` 计时）+ 上轮遗留（`DocumentPaintRenderer.debugReplayStyleRecomputeCount`/`debugReplayBakedStyleHitCount`、`drawSelf` 分段 recordPhase）。`git diff` 确认只剩 6 文件真实修复，`HtmlLikeDocumentWidget.java` 已完全恢复 HEAD。
- **文档**：`ERROR-20260615-flex-anonymous-item-layout-self-pollution.md` 末尾已更新第二层（已修+结论）、第三层（记录+方案+未做项）、第三层诊断复用经验（探针采样口径、先拆段再投入、FPS 天花板 vs 偶发尖刺）。
- **协作规范（用户本轮新增）**：agent 遇到需决策问题优先用提问工具，除非用户明确不想被打扰。
- **待办**：三层修复待用户确认后**分层提交**到 `4.0`（提交信息分清三层，勿混）。CodeGraph 时通时断，记忆（协作规范 convention + 第三层 debug_context）待其恢复后补存。

---

## 历史交接（2026-06-15 第 5 次）

- **当前分支**：`4.0`。任务：ModernConfig 配置页仍卡顿（第 4 次文本测量重构后用户复测仍 ~3FPS），探针定位真实根因并修复。
- **根因（已坐实，堆栈 100% 一致）**：`FlexLayoutHelper.flushAnonymousTextItem` 在布局过程中为 flex 容器内裸文本创建临时匿名元素 `qz-anonymous-flex-item` 并 `setAttribute("data-hit-test-hidden")`，而 `ElementNode.setAttribute` 值变化时 `markSubtreeMutated → recordLayoutMutation → layoutVersion++`。该匿名元素从不挂入文档树，其 bump 是错误副作用，形成「布局→bump layoutVersion→下帧布局缓存失效→重排」死循环。配置页 ~20 个含裸文本 flex 容器 → 单帧 `layoutVersion` 被 bump 20 次。属布局引擎通用缺陷，非 ModernConfig 独有。
- **修复（已合入工作区，未提交）**：`ElementNode` 新增框架内部静默写属性入口 `__putGeneratedAttribute(name,value)`（不触发失效，与 `__appendGeneratedChild`/`__copyFromSilently` 同约定）；`FlexLayoutHelper.flushAnonymousTextItem` 改用它。diff 仅 2 文件（`ElementNode.java` +15、`FlexLayoutHelper.java` 改 1 行）。
- **实测收益**：稳态帧时间 340ms → 18~30ms，FPS 2.7 → 34~46（约 12 倍）；布局 `relayout x20` 消除，稳态 `resolve(cached)` 命中。
- **验证**：`compileJava` 通过；回归 `FlexLayoutHelperBoundaryTest`、`DocumentLayoutEngineTest`、`HtmlLikeDocumentWidgetLayoutCacheTest`、`HtmlLikeDocumentWidgetInlineLayoutCacheTest`、`HtmlLikeDocumentWidgetHitTest` 全过。临时诊断探针（drawSelf 分段 recordPhase、resolveLayoutBox 失效原因、DocumentNode.markMutated 堆栈采样）**已全部回退**，工作区只剩真实修复。
- **错误与诊断路线文档**：`docs/开发者文档/errors/ERROR-20260615-flex-anonymous-item-layout-self-pollution.md`，内含「UI 卡顿诊断可复用路线」（探针四步法：拿读数→recordPhase 区分重建/回放→定位失效缓存键→markMutated 堆栈采样）。
- **剩余第二层瓶颈（未处理，下一任务）**：稳态仍有 `replay(cmd~724) 约 17~27ms`，724 条 paint 命令回放本身偏慢。嫌疑：`DocumentPaintRenderer.render` 每帧 `new IdentityHashMap` 重建 styleMemo + 每个非文本命令 `UiStyleResolver.computeWithParentStyle` 全量重算 computed style；另有偶发 `resolve(rebuildCmd)`（命令数 720↔737 抖动，疑似 caret blink/hover 改 paintVersion）。详见下方"给下一个 agent 的提示词"。
- **待办**：本次修复尚未提交，需用户确认后提交到 `4.0`（或新分支）。

---

## 历史交接（2026-06-15 第 4 次）

- **当前分支**：`4.0`。任务：ModernConfig 大视图 ~3FPS 文本测量重构 **已完成并合回 `4.0`**。
- **成果**：抽出共享 `club.heiqi.uilib.ui.text.layout` 包（`TextMeasureFunction` / `LogicalTextLine` / `VisualLineLayout` / `TextLayoutEngine`），把 TextArea/CodeEditor 每帧 O(N²) 逐前缀 `measureTextWidth(substring)` 降为 O(N) 增量，按「内容指纹+可用宽度+字体测量纪元+行高+软换行」缓存稳态零测量，selection/caret 两层共享引擎同一次结果。
  - 底层新增：`TextLayoutService.prefixWidthsRaw`（单趟累加 raw advance、逐码点边界 ceil）、`UiRenderContext.measurePrefixWidths`（包装为 UI 像素，与逐次 `measureTextWidth(prefix)` 数值完全一致）、`FontRendererAdapter.getTextMeasureEpoch` 透传。
  - TextArea 删除私有 `VisualLineMetrics`/`LogicalLine` 改用共享引擎；CodeEditor 渲染层改查引擎前缀宽度并首获缓存；TextInput 整段测量已走共享入口、无重复实现故未改。
- **关键提交**：重构 `f36ad2d3 [Refactor]`、诊断回退 `16cf7f28 [Chore]`、合并 `79aab71c [Merge]`（`--no-ff`，开发分支 `refactor/shared-text-layout-engine`）。临时诊断埋点 `5b8e5b1f`/`5108a87c` 已回退完毕。
- **设计文档**：`docs/记忆/决策/DECISION-20260615-shared-text-layout-engine.md`。记忆：`architectural_decision` id `85476782-fd47-40ba-8cc5-e1a45ffb460e`。
- **验证**：`compileJava` 通过；`DocumentTextAreaControlTest`（含新增 CJK 多行/空行 caret 用例）、`DocumentCodeEditorControlTest`（**新增** caret/selection/CJK 像素级渲染用例，此前 CodeEditor 渲染零回归保护）、`DocumentTextInputControlTest`、`TextLayoutEngineTest`（13 用例）全过；`ui.control.*` 全过。
- **预存环境敏感失败（与本次无关，已 stash 复验在干净 4.0 上同样失败）**：`TextLayoutServiceTextContentModeTest.shouldTreatSectionCodesAsVisibleCharactersInUiLibRawTrimAndWrap`（真实 AWT Dialog 字体 advance 依赖本机）、`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`。记忆：`known_issue` id `8e794b4c-0f30-4137-82a6-a66ad346b655`。
- **剩余项（需用户）**：稳态帧率收益需进 `runClient21` 打开 ModernConfig 大视图实测确认（稳态 custom renderer 单帧 <2ms、整屏 render 个位数 ms）；沙箱无 GUI 无法自测帧率。

---

## 历史交接（2026-06-15 第 3 次，已收口）

- 任务：ModernConfig 大视图 ~3FPS，根因实测锁定为 TextArea/CodeEditor custom renderer 每帧 O(N²) 全文测量（与 GL/视口裁切/纹理上传无关）。该任务已由第 4 次交接的共享 `TextLayoutEngine` 重构完成并合回 `4.0`，临时诊断埋点 `5b8e5b1f`/`5108a87c` 已回退。
- 根因细节与设计取舍见 `docs/记忆/决策/DECISION-20260615-shared-text-layout-engine.md` 及记忆 `debug_context` id `8856391a-f9fe-4961-a9f5-8a1cc39b10b7`，不再在本文展开。
- 环境说明：本机 JDK25 可直接 `./gradlew.bat --no-configuration-cache compileJava`（约 20–42s）；游戏内运行/帧率实测需交用户（沙箱无 GUI 客户端）。

---

## 给下一个 agent 的施工提示词（方向E：滚动期绘制命令免重建 = 命令坐标解绑滚动偏移，回放期叠加）

> ModernConfigTemplateScreen 方向E 续接：方向A/B/C/D 已全部收口。**方向D（输入帧 render 段全量重建）已用七轮外科探针完成诊断（探针已干净回退，工作区从 HEAD 干净起步），真凶坐实为「滚轮滚动每帧全量重建 ~740 条绘制命令 ~16ms」**，详见上方「第 9 次交接」。用户已拍板做**架构性根治（方案2）**，非补丁。本提示词即方案2 的施工指引。
>
> **要解决的根因（已坐实，勿重新诊断绘制侧，可直接施工）**：
> - 滚动偏移当前在 `DocumentVisualTraversal.resolveBoxContext()`（约 292~293 行）`childOffsetX = boxOffsetX - getScrollLeft(...)` / `childOffsetY = boxOffsetY - getScrollTop(...)` 处减入，并经 `createEntry`（约 483 行）向滚动容器整棵后代子树传播，最终被 `DocumentPaintEngine` 固化进每条命令的**绝对坐标**（`box.getLeft() + offset` 形式）。
> - 于是滚动一改 offset → 全部后代命令坐标失效 → `HtmlLikeDocumentWidget.resolvePaintCommands()` 因 `scrollVersion` 变化使 `cachedPaintCommands` 失效 → `buildPaintCommands` 全量重建 740 条 ~16ms。
> - **scrollVersion 100% 由 `DocumentScrollState.updateOffsets()` 的 `version++` 驱动，且滚轮滚动是唯一来源（探针 `D.scBump.wheelN.nonWheel0`，nonWheel 恒 0）**。纯 hover/鼠标移动不触发（全程 cacheHit ~3.5ms）。
>
> **方案2 核心思路（架构性）**：让绘制命令携带「文档内容坐标」（**不含滚动偏移**），把滚动偏移移到**回放期**叠加。这样滚动只改回放参数、不使命令缓存失效、零重建。
> - **回放期已有天然注入点**：`DocumentPaintRenderer.render(context, commands, offsetX, offsetY)` 对**所有**命令类型统一做 `command.getLeft() + offsetX`（renderCommand/renderTextCommand/effect/transform/custom/clip 全部如此），当前 offsetX/offsetY 传的是 widget 屏幕绝对位置 `getAbsoluteX()/getAbsoluteY()`（全局平移）。方案2 要在此基础上叠加**每条命令所属滚动容器链的 -scrollOffset**。
>
> **关键技术约束（已坐实，务必遵守，否则破坏正确性）**：
> 1. **滚动是局部的，不是全局的**。一个文档可有任意多个并发滚动容器（`DocumentScrollState.entries` 是 `Map<ElementNode,ScrollEntry>`，每个 overflow 容器一个 entry），还可嵌套。所以**不能简单给整批命令加一个全局 -scrollY**，必须按命令所属的「滚动容器链」分别叠加各自的 scrollOffset。这是方案2 最大的工程难点。
> 2. **clip 矩形坐标当前不含自身 scrollOffset**（`ClipContext` 存 `boxOffsetX/boxOffsetY` 而非 childOffset，约 290 行），即视口框滚动时不动、只有被裁内容动——这个语义正确，方案2 改造后必须保持：clip 命令叠加「祖先滚动容器链」偏移、但不叠加自身的。
> 3. **sticky / fixed 定位**：`resolveBoxContext` 里 sticky（`DocumentStickyPositioning`）和 fixed（`fixedContainingBlock*`）与 scrollOffset 在同一处交织计算。方案2 把 scrollOffset 抽到回放期后，必须确认 sticky/fixed 的偏移语义不被破坏（sticky 本就是相对滚动容器的特殊偏移）。**这是高风险点，需专门测试**。
> 4. **CUSTOM 命令已有 `DocumentCustomRenderBounds` 固化滚动态**（方向B 的产物，`attachCustomRenderBounds` 约 171 行），方案2 改造滚动偏移来源后必须与之协调，别让文本控件选区/光标/行号层错位。
> 5. **命令缓存 key 解绑 scrollVersion**：`resolvePaintCommands()`（约 891 行）当前用 `cachedPaintScrollVersion` 判定失效，方案2 完成后滚动不应再使命令缓存失效（这是收益来源），但 scrollbar thumb 位置等仍需随滚动更新——评估 scrollbar 命令是否也要走回放期偏移、还是单独处理。
>
> **不会破坏指针更新（已与用户确认并坐实）**：用户曾遇到「滚动时指针样式不更新」回退，特别在意。已坐实指针更新走**交互管线**（`onMouseScroll → findElementAt → hitTest → dispatchLatestScrollIfChanged 检测 scrollVersion 变化 → updateHoveredElement`），与**绘制管线**分离。方案2 只改绘制侧命令坐标与回放偏移，**严禁改动 scrollVersion 的产生（updateOffsets 的 version++）与交互侧消费（dispatchLatestScrollIfChanged 约 1309 行）**，则指针更新零影响。**必须加专门回归测试覆盖「滚动后指针刷新」**。
>
> **施工建议步骤**：
> 1. 先补一轮探针坐实 `buildPaintCommands` 内部 16ms 的耗时分布（场景遍历 vs BoxContext 解析 vs 样式/动画解析 vs 文本测量各占多少），确认方案2 收益落点（绕开重建能省多少）值得这个改动面，再正式动手。子代理已分析：`resolveVisualScene` 是 O(根+topLayer) 常数级、大头在 `appendBoxCommands` 递归内的惰性 `resolveBoxContext` + stacking 排序 + 样式/动画解析 + 文本测量。
> 2. 设计命令的「滚动容器链」携带方式（命令上挂所属滚动容器引用栈，或回放期用 START/END 配对维护当前滚动偏移栈——后者与现有 CLIP/PAINT_CONTEXT/TRANSFORM 的栈式配对范式一致，可复用 `RenderReplayState`）。
> 3. `resolveBoxContext` 不再把 scrollOffset 减进 childOffset；改由回放期按滚动容器链叠加。
> 4. 命令缓存 key 解绑 scrollVersion。
> 5. 全程 `compileJava` + 定向回归 + 用户实测帧率比对。
>
> **硬约束（全程沿用）**：
> - 决策点（方案细节/签名变更/风险/范围）优先用**中文**提问工具确认（用户不读英文）；提问工具偶发 SchemaError，重试或改纯中文文字陈述+口头选。
> - 帧率/探针实测必交用户跑（沙箱无 GUI）：用户跑 `./gradlew.bat --no-configuration-cache runClient21` → 打开 ModernConfigTemplateScreen → **滚轮上下滚动浏览约 10s**（方向E 真凶是滚轮，不是鼠标移动）→ 关闭；你用 `Select-String -LiteralPath` 精确锁定 `run/client/logs/fml-client-latest.log` 读 `UI 运行统计[ModernConfigTemplateScreen]` 行看 `render=`/`fps=`/`slowFrames=`/`phases=`。
> - 探针拆到「方法段」粒度；定位完务必**回退全部探针**（方向C/D 的探针均已干净回退，可参考其增删点）。
> - 一个方法连试两次不收敛就停下诊断根因换思路。
> - 改完跑定向回归：`HtmlLikeDocumentWidgetScrollTest` / `HtmlLikeDocumentWidgetBoundsIndexCacheTest` / `HtmlLikeDocumentWidgetAnimationRuntimeTest` / `DocumentHitTestEngineTest` / `HtmlLikeDocumentWidgetEventDispatchTest` / `DocumentPaintEngineTest` / `DocumentPaintRendererTest` / `DocumentLayoutBox*` / `DocumentTextAreaControlTest` / `DocumentCodeEditorControlTest` / `DocumentTextInputControlTest`，外加**新增的滚动免重建测试 + 滚动后指针刷新测试**，比对预存失败集（下方 7 个）确认零回归。
> - 完成后开命名分支（`perf/scroll-offset-replay-time` 或类似），标题格式 `[English]: 中文标题`，正文中文 Markdown，再 `git merge --no-ff` 合回 `4.0`（前作 `perf/hit-test-reuse-computed-style`、`perf/custom-renderer-baked-bounds` 即此法）。
> - AGENTS.md 第 4 节：开工先读 `docs/AI记忆文档.md`；完成后更新本交接记录，视情况更新长期事实/决策/errors。
>
> **方向D 已坐实的可复用事实（直接用，勿重测）**：
> - 滚轮滚动每帧全量重建 740 条命令 ~16ms 是输入帧 render 段卡顿的唯一真凶；纯鼠标移动/hover 全程 cacheHit ~3.5ms，不触发重建。
> - scrollVersion bump 100% 来自 `DocumentScrollState.updateOffsets()` 的 `version++`（探针 `D.scBump.wheelN.nonWheel0`，purge/cleanup 路径恒 0）。
> - 命令是不可变 final 字段、坐标全绝对值（`box.getLeft()+offset`）；命令列表是扁平 list + CLIP/PAINT_CONTEXT/TRANSFORM 的 START/END 栈式配对，子树命令外层包着祖先 start、后面跟 end，切片复用必须维护跨边界栈配对。
> - 回放期 `DocumentPaintRenderer` 对所有命令类型统一 `getLeft()+offsetX`，是天然的偏移注入点。
> - 指针更新走交互管线（findElementAt→hitTest→dispatchLatestScrollIfChanged 检测 scrollVersion），与绘制管线分离，方案2 守住「不动 scrollVersion 产生与交互侧消费」即不回退。
>
> **预存失败集（7 个，与本任务无关勿误判）**：`DocumentPaintRendererTest` 6 个 GL11 `NoClassDefFoundError`（缺 LWJGL native）+ 1 个 `shouldTreatTableSectionBoundaryAsSharedLastRowOnlyForFinalRow` `NoSuchMethodException`；`HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails` 环境敏感。
>
> **当前工作区**：分支 `4.0`，干净（仅两个 docs 文件有上轮遗留预修改，与本任务无关）。HEAD = merge `fadb8a00`。从干净树起步施工。

---

## 给下一个 agent 的研究提示词（方向F：方案2 已落地但 ModernConfig 滚动收益未兑现，定位真凶）

> 方向E（方案2 delta 模型）**机制层已完成并合回 `4.0`**（merge `74c54454`，前置 commit `67db02f8` 源码+测试、`b533a1cb` 文档）。compileJava + 全部新增测试通过、零回归（9 个固有字体后端失败已用 stash 在干净基线复验相同）。**但用户实测 ModernConfig 滚动收益未兑现，这是方向F 要查的核心问题。**
>
> **实测关键数据（2026-06-15，用户跑 runClient21 滚轮滚动 ModernConfigTemplateScreen ~50s）**：
> | 帧类型 | render | 说明 |
> |---|---|---|
> | `events=0/0/0`（静置） | **3.2~5.3ms** | 稳态，命中缓存 |
> | `events=1/0/0`（滚轮帧） | **20~51ms**（input 3~6ms 另计） | **仍在全量重建，未降到静置水平** |
>
> 即：方案2 期望「eligible 容器滚动只改回放参数、render 维持 ~3.5ms」，但滚轮帧 render 仍 20~51ms。**机制就绪、单元测试证明 eligible 路径免重建，但 ModernConfig 实跑没走到免重建路径**。
>
> **最高优先级假设（按可能性排序，逐一用探针证伪/坐实，勿凭空改）**：
> 1. **ModernConfig 主滚动容器是 ineligible（子树含 positioned 后代）→ 走回退重建路径**。主滚动容器在 `ModernConfigDocumentBuilder.java` 59~60 行（`overflowX=HIDDEN/overflowY=AUTO`）。已初查：`club.heiqi.uilib.config` 包内无 `setPosition` 调用，但**底层控件（DocumentSelect/Tooltip/Overlay 等）或 document 构建层可能引入 absolute/fixed/sticky/relative 后代**，使 `hasPositionedDescendant` 为 true → 整容器 ineligible → 快照登记它 → 每次滚动 `scrollDependenciesSatisfied()` 返回 false → 全量重建。**先验证：在 `isReplayScrollOffsetEligible` 或 `collectScrollDependencies` 打探针，dump ModernConfig 实跑时主滚动容器是否 eligible、若否则是哪个后代 positioned。** 这是最可能的真凶。
> 2. **若主容器确实 ineligible**：需判断那些 positioned 后代是否真的「逃逸」出滚动容器作用域。若它们其实跟随内容滚动（只是 position:relative 之类未真正脱流），可考虑**收窄 `hasPositionedDescendant` 判定**（例如 relative 不脱流、仍在 flow content 段内、SCROLL_OFFSET 作用域可覆盖它 → 不应判 ineligible）。这能让更多真实页面走免重建。**但务必先坐实 absolute/fixed/sticky 的作用域逃逸语义**（见铁律），收窄判定有正确性风险，需配像素回归测试。
> 3. **`events=1` 帧的重建可能不是滚动触发，而是 hover/paintVersion bump**（方向C 第 8 次交接遗留的「方向A」）：滚轮帧同时带 `hitTests=2`，说明滚动后做了命中测试 → 可能 hover 元素变更 → paintVersion bump → `resolvePaintLayoutBox → refreshComputedStyles + buildPaintCommands` 全量重建。**这条与方案2 正交**：方案2 只解绑 scrollVersion，没解绑 paintVersion。若真凶在此，需另做 paintVersion 维度的增量。**用探针在 `resolvePaintCommands` 区分本帧重建是 scrollVersion 失效还是 paintVersion 失效（`cachedPaintVersion` vs `document.getPaintVersion()`）。**
>
> **探针落点建议（复用方向C/D 的 `UiPerformanceMonitor.recordPhase` 通道，稳态 `phases=` 空可独占）**：
> - `resolvePaintCommands`：拆 `F.cacheHit` / `F.rebuild`，且 rebuild 时细分失效原因——`F.inv.paintVer`（`cachedPaintVersion != paintVersion`）/ `F.inv.scrollDep`（`!scrollDependenciesSatisfied()`）/ `F.inv.transient`（transientScrollbar 变）/ `F.inv.anim`。一次滚轮 10s 即可看出滚轮帧重建主因。
> - 若是 `F.inv.scrollDep`：在 `collectScrollDependencies` dump 快照非空时登记了哪些元素 + 各自哪个 positioned 后代触发 ineligible（加一次性日志，每秒限一行，仿方向D 堆栈采样）。
> - **务必全部回退探针**（方向C/D 探针均干净回退，git diff 可参考）。
>
> **方案2 机制层关键事实（已落地，直接用，勿重写）**：
> - eligibility = `isReplayScrollOffsetEligible`（`DocumentPaintEngine` 约 330 行）= 可滚动 且 `!hasPositionedDescendant`（约 350 行，**任何非 static 后代都判 true**，含 relative/absolute/fixed/sticky）。
> - `buildPaintPlan`（约 178 行）返回 `DocumentPaintPlan`（命令 + `Map<ElementNode,int[]>` 快照）；`collectScrollDependencySnapshot`（约 200 行）登记**所有 ineligible 可滚动容器**的构建期偏移。
> - `HtmlLikeDocumentWidget.resolvePaintCommands`（约 907 行）缓存命中 = `!animationStateChanged && !animationWork && transientScrollbar 未变 && cachedPaintScrollVersion>=0 && (scrollVersion 未变 ‖ scrollDependenciesSatisfied())`。注意 `cachedPaintVersion` 失效走的是更上游 `resolvePaintLayoutBox`（约 944 行），**paintVersion bump 会强制 rebuild，与方案2 的 scrollVersion 解绑正交**。
> - delta 模型：`SCROLL_OFFSET_START` 命令 left/top = 构建期 scroll 快照；回放 `RenderReplayState.pushScrollOffset` 算 `delta=构建期-当前`；provider = `HtmlLikeDocumentWidget.scrollOffsetProvider`（委托 scrollState）。
> - 文本裁剪已搬回放期（eligible 容器 `clipDeferred` 全量生成、`renderDeferredClipTextCommand` 回放剔除）。**注意：ineligible 容器仍走构建期裁剪 + 重建路径**，所以若 ModernConfig 走 ineligible，文本裁剪在不在回放期对它无影响——用户「文本裁切是否该放回放期」的疑问，本质要先回答「ModernConfig 为何没走 eligible」。
>
> **用户原始疑问留档**：「文本裁切是不是不应该放在回放期？」——这个疑问的前提（文本裁切已影响 ModernConfig 性能）需先证伪/坐实：若 ModernConfig 走 ineligible 重建路径，则其文本根本没走回放期裁剪，疑问不成立；若走 eligible，则要评估回放期整 run 剔除 + 超长横向二分裁切的单帧成本是否反而比构建期裁剪贵（用探针测 `renderDeferredClipTextCommand` 占回放比重）。**先用方向F 探针定位 ModernConfig 滚轮帧重建主因，再回头回答这个疑问。**
>
> **硬约束（全程沿用）**：决策点优先中文提问工具；帧率/探针实测必交用户跑（runClient21 → 开 ModernConfigTemplateScreen → 滚轮滚 ~10s → 关，读 `run/client/logs/fml-client-latest.log` 的 `UI 运行统计[ModernConfigTemplateScreen]`）；探针拆方法段、定位完全回退；一个方法连试两次不收敛就停下换思路；不动 `resolveBoxContext`/`DocumentCustomRenderBounds` 坐标语义、不动 scrollVersion 产生与交互侧消费；完成开命名分支 `[English]: 中文标题` + `--no-ff` 合回 `4.0`。
>
> **预存失败集（9 个，与本任务无关勿误判，已在干净基线复验相同）**：`DocumentPaintRendererTest` 7 个（6 个字体后端 `NoClassDefFoundError` + `shouldTreatTableSectionBoundaryAsSharedLastRowOnlyForFinalRow` `NoSuchMethodException`）+ `TextLayoutServiceTextContentModeTest.shouldTreatSectionCodesAsVisibleCharactersInUiLibRawTrimAndWrap` + `HtmlLikeDocumentWidgetCursorTest.shouldDegradeSystemCursorHostWhenBackendFails`。
>
> **当前工作区**：分支 `4.0`，干净，HEAD = merge `74c54454`（方向E 已合入）。从干净树起步。
