# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-23 第 85 次）—— 三批真机缺陷全修复并通过真机验收，进入字段引擎

### 一句话现状
SceneSelect 三批真机缺陷已全部修复并通过用户真机验收；文档长行治理完成（表格行转 XML，全项目仅剩 1 条代码块内行）。下一步进入字段引擎最小切片。

### 三批修复概要（按提交时序）

**第一批：下拉不显示 / 范围太大 / resize 不更新**
- `590cee56` SceneSelectHostWidget 补全 overlay 布局与绘制管线
- `aaf7c661` 抽取 AbstractSceneHostWidget 统一 render pipeline（净减 929 行）
- `86f69d81` listbox 高度 shrink-to-fit + scrollable 分支3 maxHeight cap
- `9d588cb5` 补 selfConsumesConstraint I8 缺口
- `b444b883` 修复 I7 叶节点宽度约束闸门缺口导致 resize 不更新

**第二批：滚动后 hit/绘制不跟随 / 滚出不 dismiss**
根因：`scrollOffsetY` 是 paint-only 偏移，hit-test 与 `absoluteBox` 对滚动零感知。
- `a62da1d6` 对齐滚动偏移几何探针：抽 `childYBase` 单层注入原子
  （判 parent.isScrollable 而非 self），paint/hit-test/absoluteBox 三方共用
- `7c814926` 裁剪滚动视口命中区域：hit-test clip bounds 递归参数传递，类无状态
- `18143ecf` 关闭不可见锚点浮层：`dismissOverlaysWithInvisibleAnchor` 独立步骤，
  trigger 滚出零交集时 requestDismiss 走 signal（I11 逃生舱②）
- `d0e3445b` NORTH_STAR `:247` 补注几何探针对齐；AGENTS.md 新增中断恢复纪律

**第三批：矮窗滚不到底 / 底部下拉翻转**
根因1：maxScroll 少算 viewport 上下 padding。根因2：resolver 只有 resolveDown。
- `faefb192` 统一滚动最大偏移口径：`SceneGeometry.maxScrollY` 闭式
  `max(0, maxChildBottom + padBottom - boxH)`，外层/内层同一调用
- `e2d436d8` 支持锚定浮层上下择优展开：`resolveAuto` 四分支选侧 +
  `layoutOverlays` 二次布局（I7 安全，下游全透明）
- `3ca8fe62` NORTH_STAR 补注 maxScrollY 统一 + anchor 上下择优；
  项目约定新增「Markdown 正文不写超长行」规则

**文档长行治理**
- `3cdc8efc` 第 84 次交接保存
- 当前态文档已重写为短行（本次）
- 其他目录超长行待批量治理（73 个文件，派 fixer 处理中）

### 验证
- 第一批：Gradle 全套 scene 测试多次 rerun 通过
- 第二批：`*SceneHitTesterTest` `*SceneScrollViewportTest` `*ScenePaintEngineTest`
  `*SceneAnchoredOverlayPipelineTest` `*SceneSelectTest` 全部通过
- 第三批：`*SceneScrollViewportTest` `*SceneAnchorResolverTest`
  `*SceneAnchoredOverlayPipelineTest` 全部通过
- 三批均经 reviewer 审核无阻断，oracle 要点全 ✓，I7/I11/I6/I10 守住
- `git diff --check` 通过，工作区干净

### 下一步
1. **字段引擎最小切片**：scene-native 字段 spec、draft/current、校验、
   dirty、canSave/canCancel 派生与保存语义
2. **接真实配置数据源**：优先接 `MutableConfig`，验证一期字段类型、分类、校验、
   保存、取消和恢复默认
3. **处理剩余 reviewer 建议**（非阻断，累计 9 项，可在字段引擎间隙处理）
4. **暂缓**：搜索、树、多行文本、列表编辑、表格行内编辑、高级 picker、
   raw editor、远程同步、完整 CSS/token/theme 和 Phase 5

### 已知边界
- 三批缺陷已修复并**通过真机验收**（用户确认）
- 每帧 overlay root 4 次 layout，真机帧率已验收无问题
- trigger 在 flush 后位置变化的动态切换场景未专门测试（真机验收未反馈问题）
- reviewer 9 项建议改进非阻断
- 旧对外现代配置页文档仍描述 DOM 12 模板能力；scene 一期完成前不得宣称等价
- NORTH_STAR `:247`/`:248` 待 overlay/scrollable 稳定后经 oracle 评估转正
- **文档约定**：Markdown 正文不写超长行（≤200 字符），表格行转 XML 格式
- 全项目 md 文件仅剩 1 条 >200 字符行（DECISION-20260609 代码块内，按规则跳过）

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
