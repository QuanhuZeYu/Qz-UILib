# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-29 第 136 次）—— Oracle 深审 commit 69f71a7d 必修 4 项修复

### 一句话现状

Oracle 深审 commit `69f71a7d` 找出的 4 项必修问题已全部修复（commit `fb5746f4`）：删除每帧无条件 bumpLayoutEpoch+flush（C1）、拆 scrollbar bind 恢复 I4 双轨核对（M1）、重写测试用探针断言脏级别修假阳性（M2）、scrollbar 滚轮冒泡问题加 TODO 留真机验证（M3）。2512 测试全绿。分支 `feat/modern-config-p0-core`，9 个 commit 待 push。

### 本会话改动（commit `fb5746f4`）

**C1 Critical：删除每帧 bumpLayoutEpoch + flush（恢复 I7/I9/I2）**
- `SceneRuntime.java`：删除 `layoutEpochSignal` 字段 + `bumpLayoutEpoch()` + `layoutEpochSignal()` 访问器 + 未用 import
- `AbstractSceneHostWidget.java:121-125`：删除 render 流第二次 layout 后的 `bumpLayoutEpoch(); flush();` 两行

**M1 Major：拆 bind 恢复 I4 双轨核对**
- `SceneScrollbar.java`：原单 `bind(COMPOSITE)` 同时调 setPreferredHeight(LAYOUT)+setTransform(COMPOSITE) 失真，拆为：
  - `bind(LAYOUT, 只订阅 contentChangedSignal, setPreferredHeight)` —— height 不依赖 scrollOffset
  - `bind(COMPOSITE, 订阅 scrollSignal+contentChangedSignal, setTransform)` —— 滚动时只此 bind 跑

**C1 配套：scrollbar 订阅源切换**
- `SceneScrollbar.Props` 新增 `ReadableSignal<?> contentChangedSignal` 字段
- `ConfigScreen.java`：创建 scrollbar 时传 `activeSectionSignal` 作为 contentChangedSignal
- 已知边缘（javadoc 已登记）：section 切换后 content 高度变化滞后一帧生效（signal 立即 set 但 layout 下一帧才产出新几何）；窗口 resize 暂未覆盖留 TODO

**M2 Major：重写测试用探针断言脏级别**
- `SceneScrollbarTest.java`：`scrollShouldOnlyChangeTransformNotHeight` 改用 `__isCompositeDirty()=true` + `__isSelfLayoutDirty()=false` 探针断言，取代旧版仅断言值相等的假阳性
- doFrame 顺序改为 layout→bump→flush→layout（末尾 layout 清脏使树回干净态供探针断言）

**M3 Major：scrollbar hitTest 滚轮冒泡（加 TODO，留真机验证）**
- 侦察确认 Oracle 推测成立：column+thumb 均 hitTestable=false，鼠标在 4px 列上 hit-test 穿透命中 scrollContainer，SCROLL 冒泡向 root 不经过 viewport（viewport 是 column 兄弟非祖先）→「在滚动条上滚不动内容」
- 三种修法均需架构改动（破坏纯显示控件定位 / 需扩 layout 支持绝对定位叠加 / 污染 ConfigScreen），本批次不强行修，`SceneScrollbar.java` 加 TODO 注释标记问题 + 可行修法，留真机验证后定夺

### 不变量恢复证据
- I4：拆 bind 后声明级别与实际打级别一致，M2 探针测试断言 selfLayoutDirty=false + compositeDirty=true 通过
- I7：删除每帧 bump，effect 只在 signal 变化时重跑；滚动时只 COMPOSITE bind 跑
- I9：删除每帧二次 flush
- I2：删除每帧 bumpLayoutEpoch 的 signal.set
- 合规边界守住：uilib 通用组件包未 import config.ui.*，contentChangedSignal 由调用方注入

### 真机验证待项（交用户）
- 配置页能正常打开、渲染、交互（含上一次会话的 P1.5 改动）
- **新增**：section 切换时 scrollbar thumb 几何是否滞后一帧可感知（已知边缘，预期视觉无碍）
- **新增**：鼠标悬在 scrollbar 4px 列上滚轮是否能滚动内容（M3 推测不能，真机验证后选修法）
- 编辑字段后保存，重开游戏重打开 `config/qzuilib-modern.yaml` 确认值持久
- 帧率与交互响应正常
- SceneButton primary variant 蓝底视觉 / SceneNavList 侧栏视觉 / save 反馈条效果

### 下一步可选方向
1. 真机验证通过后合并 `feat/modern-config-p0-core` 回 `4.0`（用 `git merge --no-ff`）
2. M3 真机验证后选 scrollbar 滚轮修法
3. P2 完善：嵌套分类树 / 全局搜索 / 8 种复杂字段类型 / paint 视口剔除（真有瓶颈时）

### 分支与工作区
- 当前分支：`feat/modern-config-p0-core`
- 提交历史：9 次未 push commit（最新 `fb5746f4` Oracle 必修 4 项修复，其余 8 次见上一次交接）
- 工作区：干净
- 测试：2512 全绿