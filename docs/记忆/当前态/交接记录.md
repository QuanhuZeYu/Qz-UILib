# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；当前工作分支：`4.0`；本轮规划分支为 `docs/test-page-visual-plan`，按规则合并后生效
- 已完成：本轮已把 `/qzuilib test` 新运行时矩阵规划调整为“视觉化展示功能优先，浏览器语义验证为重要目标”，规划入口为 `docs/开发者文档/specs/qzuilib-test-page-visual-matrix-plan.md`；旧 `qzuilib-test-page-rebuild-plan.md` 已加后续复核提示，只作为历史语义编号和预期文本素材
- 当前设计：继续保留 DOM / CSS / Layout / Paint / Input / Controls / TextFont / Animation / RuntimeHost / RemoteNet 十组导航；首页后续应转为能力画廊、语义覆盖热力图、最近失败、人工任务和环境信息；二级页以真实视觉样例为主体，并使用视觉状态与语义状态双维度结果模型
- 实现约束：不要继续向已超大的 `UiTestDocumentPageController` 堆叠矩阵逻辑；后续应先拆出 registry、分组视觉 builder、语义 checker 和结果 state；AGENTS 已补充“单个代码文件接近或超过 1000 行时必须评估职责拆分”约束
- 下一步：按新规格重建 P0 数据模型和首页框架；规划前不要恢复旧运行时卡片矩阵；每批保持 `git diff --check`、目标测试和 `compileJava`，涉及游戏内 UI/HUD/输入/远程/网络时再用 `runClient21` 验证
- 文档流程补充：审查误报或结论失效时，必须在原始审查正文添加 `后续复核（YYYY-MM-DD）` 批注，并同步更新 reviews 索引，避免后续 Agent 只读原报告重复审核

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
