# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-27 第 120 次）—— deepwork 阶段 2 计划 oracle 审查 + 行业调研完成

### 一句话现状

deepwork 阶段 2（子树并行 layout/paint）**计划审查完成，待用户拍板启动实施**。
本轮派 explorer 侦察设计事实基础（7 项硬约束）+ explorer 补查 TextLayoutService
缓存并发安全（正确性安全，偶发重复度量）+ oracle 起草阶段 2 计划
+ librarian 行业调研（Servo/Blink/WebKit/Flutter/Bevy/Rayon/Unity/Unreal）
+ oracle 基于调研给最终推荐。**无代码改动**，纯计划审查。
下个会话开场用户拍板 6 个技术项 + 3 个宪章修订项后启动阶段 2 第一批。
当前在 `add/b6-fbo-perf-baseline` 分支，工作区干净，8 个 commit 待真机验收。

### 本会话累计 commit（按时间序）

| commit | 内容 |
|--------|------|
| （无代码改动）| 本轮纯计划审查，无 commit |

### 本会话任务链路

**阶段 D：阶段 2 计划起草 + 审查 + 行业调研（纯研究，无代码）**
1. 主 Agent 读三份必读文件恢复进度（交接/deepwork/决策文档）
2. 派 explorer 侦察阶段 2 设计事实基础（7 项任务）→ 7 项硬约束：
   - SceneNode 引用相等（未重写 equals/hashCode，可换并发集合）
   - layout 并行边界复杂（3 类隐性跨子树写共享：探针累加/父回写子/冒泡标记）
   - paint 头号改造点（PaintPlan 共享 ArrayList，DFS 前序顺序敏感，无 appendAll API）
   - GL 硬墙已现成（PaintPlan/PaintCommand/PaintFragment 全 final 不可变）
   - paint 期 measureWidth 稳态无锁（I6 缺口已还清）
   - 零 fork-join 历史（需新建线程池）
   - TextLayoutService 缓存并发安全（float[] direct-index，待补查）
3. 并行派两路：
   - explorer 补查 TextLayoutService.getStringWidth 内部缓存 → 32 位 float[]
     direct-index，并发正确性安全（单元素读写原子 + miss 幂等同值 + reload
     volatile 换引用），唯一代价偶发重复度量（性能损耗非正确性错误）
   - oracle 起草阶段 2 计划 + 宪章同步评估（新 session）
4. oracle 起草产出 + 关键纠偏：
   - **measurer「全无锁」是过度承诺**：miss 路径有 DerivedFontCache
     (synchronized this) + CodepointTextCache (synchronized) 两处锁，稳态命中
     无锁属实，冷启动/新字符首现串行，预热可消除
   - **双缓冲应与子树并行解耦**：阶段 2 只落地同步 fork-join 子树并行，
     双缓冲继续预留阶段 3/4（跨帧流水需 reactive 数据态快照）
   - **P0 命门**：脏标记冒泡并发写祖先（多 worker 并发 performLayout 时
     bubbleDescendant* 会并发写共享祖先 boolean）
   - 阶段拆分：第一批（底座线程安全化，单线程可验收，约 1 周）+
     第二批（上 fork-join，约 1.5-3 周）
   - 小树阈值：节点数 512 起、整树 <1024 全串行（后调整为整树<256 + 单子树≥64）
5. oracle 给 6 个拍板项，主 Agent 用 question 转达 → 用户指示：
   **「派 LIB 研究行业解法，再让 Oracle 推荐」**
6. 派 librarian 行业调研（3 个方向）→ 关键证据：
   - **方向 1（脏标记冒泡）**：Servo 用返回值归并 + join 点 post-order 串行点亮
     （对应方案 1）；Bevy 调度器互斥 + 明确拒绝 atomics；Blink/WebKit/Flutter
     不并行 layout；**方案 2 零先例**
   - **方向 2（fork 阈值）**：Rayon/Java 用精确数据规模（支持方案 A）；
     Servo 用 chunk size 16-64（children 维度）；阈值量级十百量级
   - **方向 3（线程池）**：常驻是行业共识；cores-1 是游戏引擎强共识
     （Unity/Unreal/Naughty Dog 留核给主线程）；专用 pool（Servo layout 单独池）
7. 派 oracle（新 session）基于调研给最终推荐 → 见下方「Oracle 最终推荐」

### 关键架构决策（详见决策文档）

- **DECISION-20260627-display-list-contract-line.md**：Display List 契约线
  阶段 1 落地 + 并发框架方向。含完整阶段路径、行业调研背书、P0 命门分析
- **NORTH_STAR 三处补注**：信条六（两阶段已落地）+ I6（并行强化 + measurer
  缺口已还清，**本轮 oracle 提议精确化见下**）+ 第 10 节新增 10.1
  （已落地的契约线切分，双缓冲仍标预留）
- **来源 A vs 来源 B 裁决**：epoch 外置用来源 A（保留 measuredTextNodes 集合
  + epoch 下放节点），来源 B（侵入式链表）业界无先例否决

### Oracle 阶段 2 最终推荐（基于行业调研，待用户拍板）

**三个拍板项最终推荐**：
- **D1 脏标记冒泡并发 → 方案 1**（返回值归并 + join 点串行点亮）：
  Servo + Bevy 双背书，方案 2 业界零先例。**Oracle 关键纠正**：阶段 2
  并行边界内不写脏标记冒泡（冒泡在 flush 后、layout 前串行完成），
  真正并发写是 performLayout 内的 markGeometryDirty/markSelfPaint 向上冒泡。
  方案 1 落点 = worker 内不即时冒泡，返回值携带几何变化信号，父在 join
  点按子返回值补点亮（复用现有 anyChildGeometryChanged 机制，:238,247）
- **D2 fork 阈值 → 方案 A**（subtreeNodeCount 精确判断）：
  Rayon/Java 用精确规模；Servo 用 chunk size 但场景不同（SceneNode 树高度
  不均衡）。阈值：**整树 <256 全串行，单子树 ≥64 才 fork**（起步值，真机校准）
  subtreeNodeCount 增量维护：复用现有脏标记冒泡 O(深度) + layoutInternal
  后序遍历顺带重算（零额外遍历成本）
- **D3 线程池 → 专用常驻 ForkJoinPool，cores-1，进程级单例**：
  新增 SceneParallelExecutor 持 static final ForkJoinPool，懒初始化。
  常驻是行业共识（Servo/Flutter/Unity/Unreal/Rayon）；cores-1 游戏引擎强
  共识（主线程跑 MC 循环 + GL replay）；专用 pool（Servo layout 单独池）
  **不挂 AbstractSceneHostWidget**（devtools demo 宿主，多实例多 pool）

**D4/D5/D6 最终推荐**：
- **D4 双缓冲**：确认阶段 2 不做，预留阶段 3/4。同步 fork-join 无双缓冲需求
- **D5 宪章修订**（重大架构变更，需用户拍板）：
  - **I6 measurer 表述精确化**（Oracle 关键纠正）：从「全无锁」降级为
    「稳态无锁读 + 幂等填充假设（待测试验证）+ reload 不与 worker 并发前提」
  - I7 新增「并行强化」条目（并行不改变跳过语义，脏标记并行前已定稿只读，
    几何变化经返回值归并 join 点串行点亮）
  - §10.1 补注阶段 2 已落地子树并行 fork-join（帧内），双缓冲仍预留阶段 3
- **D6 真机实测**（必交用户跑，沙箱无 GUI）：三组对照——
  小树负优化验证 / 大树并行收益（500-2000 节点）/ 阈值扫描 {32,64,128,256}

### Oracle 关键纠正（高于"让方案看起来顺"）

1. **D1 问题被原表述错置**：阶段 2 并行边界内**不写脏标记冒泡**
   （冒泡在 flush 后 layout 前串行完成）。真正的并发写是 performLayout
   内的 markGeometryDirty/markSelfPaint 向上冒泡。方案 1 正确，但落点是
   「几何变化传播」而非「脏标记冒泡」
2. **I6「全无锁」是过度承诺**：决策文档自己登记了"幂等性假设未经压测验证"
   （上轮交接:126），但 I6 补注却写成既成事实。Oracle 把它精确化为
   「假设 + 待测试」+ 新增「reload 不与 worker 并发」前提

### Oracle 标注的未验证项（下个会话启动前必须补查）

- 【需补查】TextLayoutService.measureCodepointWidth / FontMatcher.writeMatchedFont
  / GlyphRuntimeTables 当前实现（本轮 oracle 未读 ui.text/ui.font 包），
  I6 精确化的「稳态无锁读」依赖这些实际锁状态
- 【需补查，隐藏命门】**字体 reload 触发点是否确实在主线程 render 循环之外**
  （I6 精确化新增的「reload 不与 worker 并发」前提依赖此）—— 这是阶段 2
  并行安全的隐藏命门，必须在阶段 2 第一批 2.0 步前确认
- 【推测】PaintCommand.translatedBy 返回新对象（不可变）—— 从 I6 补注
  「PaintCommand 全 final」推测成立，本轮未读 PaintCommand.java，并行 paint
  拼接依赖此不可变性，2.5 步前确认

### 阶段 2 实施路径（Oracle 最终建议，待用户拍板后启动）

**第一批：并行前置配套（低风险，不引入线程，约 1 周）**
- 步骤 2.0：measuredTextNodes 换 ConcurrentHashMap.newKeySet() + 幂等断言测试
  （N 线程并发 measureWidth 同串结果全等，含冷启动 miss 路径）
- 步骤 2.1：subtreeNodeCount 增量维护（SceneNode 加 cachedSubtreeNodeCount
  + subtreeCountDirty，冒泡复用现有祖先链，layoutInternal 后序顺带重算）
- 步骤 2.2：几何变化传播改「返回值归并 + join 点点亮」（D1 方案 1 落地，
  worker 内不即时 markGeometryDirty/markSelfPaint，改返回值携带信号，
  父 join 点补点亮）—— **此步仍单线程做并验证**，确保挪动冒泡时机
  不破坏 I7 现有测试
- 第一批结束：代码仍串行，但已具备并行所需的全部无锁地基。全量跑测试
  + 提交 + 真机验收一次后进第二批

**第二批：引入并行（高风险，带回退开关 PARALLEL_ENABLED，约 1.5-3 周）**
- 步骤 2.3：SceneParallelExecutor 单例 pool（static final ForkJoinPool，
  cores-1，带全局开关 PARALLEL_ENABLED 默认 false，出问题一键回退串行）
- 步骤 2.4：layout 并行化（layoutInternal 子循环：child.cachedSubtreeNodeCount
  >= FORK_THRESHOLD && children.size() >= 2 的子 fork 为 RecursiveTask，
  探针改线程安全累加：worker 各产 LayoutResult 片段，join 点归并）
- 步骤 2.5：paint 并行化（每个 worker 产独立子 PaintPlan，join 点按 DFS
  前序确定性顺序拼接；PUSH/POP 边界命令整段落在同一 worker 内；
  PaintPlan 新增 appendAll API）—— **比 layout 更棘手，单列**
- 验收：并行开/关两种模式下 SceneLayoutEngineTest/ScenePaintEngineTest
  全套结果完全一致（determinism 闸门）

**第三批：真机校准（交用户）**
- 步骤 2.6：按 D6 三组对照真机跑 → 用户回贴 → Oracle 校准 FORK_THRESHOLD
  → 决定 PARALLEL_ENABLED 默认值

### 验证状态

- 本轮纯计划审查，无代码改动，无构建/测试
- 上一轮状态延续：compileJava + compileTestJava 通过，222 类 2215 测试全绿
- **真机帧率实测未跑**（沙箱无 GUI）

### 剩余债务全量状态

**真未还（代码债）**：
- B6 FBO I7 合成阶段偏离：FBO 纹理每帧重栅格化，待真机性能基线数据决定是否启动
- B6 hit-test 对偶偏离：SceneHitTester 对 transform 零感知，待交互需求触发
- UiPerformanceMonitor 单例 scope 错配（远期，采集深埋 Widget.java 硬编码 getInstance）
- A1 effect 内 set 慢一帧：残留语义待确认
- B4 COLUMN fill O(n²)：缓做
- chrome 主题层：P2 大工程未立项
- Phase 5 旧栈退役：阻塞中

**有意边界（非债）**：
- B6 FBO 全屏尺寸：批 3 局部 FBO 优化待性能暴露后启动
- B6 降级路径：FBO 不可用时保留 clip 放弃 transform（best-effort）
- 圆角 clip+transform：stencil+T 交互未真机验证
- SceneTextArea 功能限制 / DataTable 嵌入式深色槽

**真机验收待办（非 B6）**：
- B8 滚动后 hover 切换 / B2 TextArea 压力测试页帧率 / Breadcrumb ROOT_GAP
- Phase 1-3 真机帧率/上屏 / 改名后真机 GL 路径
- B6 FBO 性能基线页三档对照真机跑 + fps 拐点记录

### 当前分支与工作区

- 当前工作分支：`add/b6-fbo-perf-baseline`，8 个 commit（上一轮遗留）
- 工作区干净，本轮无代码改动，会话结束

### 下个会话任务表

1. **开场：用户拍板阶段 2 计划**（6 个技术项 + 3 个宪章修订项，详见
   上方「Oracle 阶段 2 最终推荐」+「Oracle 关键纠正」+「宪章修订」）
   - 行业背书强烈推荐：D1 方案 1 / D3 专用常驻 pool cores-1 / D4 双缓冲预留 /
     带 PARALLEL_ENABLED 回退开关 + 先无锁地基后并行
   - 需用户价值判断：D2 fork 判据（方案 A 推荐）/ D2 起步阈值（64 推荐）/
     D6 真机实测（必交用户跑）
   - 宪章修订（重大变更需确认）：I6 精确化 / I7 并行强化新增 / §10.1 补注
2. **拍板前：派 explorer 补查 3 个未验证项**（Oracle 标注，启动第一批前必做）：
   - TextLayoutService.measureCodepointWidth / FontMatcher.writeMatchedFont
     / GlyphRuntimeTables 当前实现锁状态
   - **字体 reload 触发点是否在主线程 render 循环之外**（隐藏命门）
   - PaintCommand.translatedBy 不可变性（2.5 步前确认，可推迟）
3. **拍板后：启动阶段 2 第一批**（步骤 2.0→2.1→2.2，按 deepwork 规则
   fixer→build→test→review→commit，每步独立验收）
4. **B6 FBO 性能基线真机实测**（可选，用户跑 /qzuilib scene_test →
   "FBO Perf Baseline" 三档对照）
5. **B6 hit-test 对偶 / A1 残留确认**（可选，待触发）

### 已知边界

- deepwork 进度文件：`.slim/deepwork/concurrent-framework.md`（必读，含完整
  Oracle 阶段 2 计划审查结论 + 行业调研背书）
- 决策文档：`docs/记忆/决策/DECISION-20260627-display-list-contract-line.md`
- NORTH_STAR 三处补注：信条六/I6/第 10 节 10.1
  （I6「全无锁」表述待精确化，见 Oracle 关键纠正）
- FBO 性能基线页：`/qzuilib scene_test` → "FBO Perf Baseline"
- FrameRateProbe：`ui/diagnostic/FrameRateProbe.java`，组合式帧率探针
- measurer 调用路径：稳态无锁（DCL 快速返回 → final 读 → volatile 表读），
  miss 路径有 DerivedFontCache + CodepointTextCache 两处 synchronized
  （Oracle 本轮纠偏，I6 表述需精确化）
- 阶段 2 并行边界内不写脏标记冒泡（冒泡在 flush 后 layout 前串行完成），
  真正并发写是 performLayout 内 markGeometryDirty/markSelfPaint 向上冒泡
- TextLayoutService.getStringWidth 缓存：32 位 float[] direct-index，
  并发正确性安全，偶发重复度量（性能损耗非正确性错误）
- SceneNode equals 引用相等（未重写），可换 ConcurrentHashMap.newKeySet()，
  前提 SceneNode 永不重写 equals
- 字体 reload 触发点是否在主线程 render 外 = 隐藏命门，启动第一批前必须补查

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑，避免并行 Gradle 竞争 build/ 目录。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，
  改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能
  登记到正确 Owner。
- token 实际值以 SceneChromeTokens.java 源码为准，不依赖过期文档清单。
- oracle 裁决架构方案后，如方案涉及长期可引用的设计决策，建议补写
  `docs/记忆/决策/` 文档沉淀。
- I11 逃生舱②扩张纪律：扩之前证明它确实是一类新的权威交互真值，
  而非可以用现有机制表达的东西（B8 验证了 hover 重算不需要扩②）。