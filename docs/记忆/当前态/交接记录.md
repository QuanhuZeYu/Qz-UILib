# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次工作分支：`chore/gtnh-290-beta`；本轮针对 `/qzuilib test` 的 `VIS-PAINT-005` 自动断言唯一失败排查与修复，未改生产层 `top-layer` 语义
- 根因结论：底层不需要修；`DocumentNode` 在结构变更后剪枝 detached top-layer 是防止 select/popup 泄漏的正确语义，失败来自测试页在样例卡尚未完整挂到文档根时提前调用内部 `__showTopLayerElement(...)`，随后继续追加字段触发剪枝，导致 `topLayerRegistered=false`
- 已改范围：`UiTestSampleVisualFactory` 将 `VIS-PAINT-005` 紫色弹层标记为延迟 top-layer，`UiTestGroupVisualBuilder` 在视觉样例区挂根后再激活；激活时先读取普通布局 `getDocumentBounds()`，再写入 fixed 坐标并注册 top-layer，保持样例内相对位置
- 验证状态：已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.internal.devtools.pages.UiTestDocumentPageControllerTest.shouldRunPaintTopLayerAndManualDiagnostics"`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.internal.devtools.pages.UiTestDocumentPageControllerTest"`、`./gradlew.bat --no-configuration-cache compileJava`
- 当前仍需注意：本分支原有 GTNH `2.9.0-beta-1` 依赖基线与 `gtnhsettingsconvention 2.0.25` 调整仍在；`runClient21` 的 `BytePatternMatcher` 缺类已解除，但本地 smoke 仍受 `ServerUtilities 2.3.0` 与 `Et-Futurum-Requiem 2.6.40-GTNH` 第三方 mixin 冲突阻塞
- 后续建议：若用户要求继续游戏内确认，可先重新打开 `/qzuilib test` 的 PAINT 第 5 张观察紫色 top-layer 是否覆盖红色高 `z-index` 层；若用户要求提交，先确认是否把本轮 devtools 修复与 GTNH 依赖基线改动放在同一提交

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
