# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次工作分支：`chore/gtnh-290-beta`；目标是把开发依赖基线同步到 GTNH `2.9.0-beta-1`
- 已改范围：`dependencies.gradle` 的 `setGtnhVersion("2.9.0-beta-1")`；将 `Angelica` 从手写 `2.1.15` 锁定改回 `elytraModpackVersion.gtnhdev("Angelica")`；按源码用途把非平台硬依赖的整合包兼容依赖改为 `devOnlyNonPublishable` / `runtimeOnlyNonPublishable`；`settings.gradle.kts` 将 `com.gtnewhorizons.gtnhsettingsconvention` 从 `2.0.20` 升到 `2.0.25`
- 调研结论：`2.9.0-beta-1` manifest 中本项目直接依赖的 beta `dev` artifact 可从 GTNH Nexus 解析；重点变化包括 `lwjgl3ify 3.0.23`、`GTNHLib 0.11.9`、`Hodgepodge 2.7.160`、`Angelica 2.1.32`
- `BytePatternMatcher` 根因已定位并修复：`gtnhgradle 2.0.20` 在 `ModernJavaModule` 中硬编码 `java17PatchDependencies` 为 `lwjgl3ify 3.0.10:forgePatches`，而普通 `java17Dependencies` 被 manifest 冲突解析到 `lwjgl3ify 3.0.23`；升级到 `2.0.25` 后 patch jar 变为 `lwjgl3ify-3.0.20-forgePatches.jar`，该 jar 包含缺失的 `com.gtnewhorizons.retrofuturabootstrap.api.BytePatternMatcher`；ExampleMod/Blowdryer 仍是 `0.2.2`，无需更新
- 验证状态：已通过 `git diff --check`、`compileJava`、输入/字体/网络传输定向测试、远程 UI/配置同步/net core 定向测试；本次还确认 `java17PatchDependencies` 解析为 `lwjgl3ify 3.0.20`，`runtimeClasspath`/`java17Dependencies` 中 `lwjgl3ify` 解析为 `3.0.23`
- 当前阻塞：`runClient21` 不再出现 `BytePatternMatcher` 缺类，但客户端仍在 `RFB-Main` 启动失败；日志关键段见 `run/client/logs/fml-client-latest.log`，`lwjgl3ify-3.0.20-forgePatches.jar` 已进入 classpath，随后 `ServerUtilities 2.3.0` 的 `mixins.serverutilities.early.json:minecraft.MixinWorldServer_SleepPercentage` 与 `Et-Futurum-Requiem 2.6.40-GTNH` 的 `playerssleepingpercentage.MixinWorldServer` 同时重定向 `WorldServer` 睡眠逻辑，触发 `InvalidInjectionException`
- 后续建议：若只需本地 `runClient21` smoke，可先在忽略的运行目录配置 `run/client/config/etfuturum/mixins.cfg` 将 `B:enablePlayersSleepingPecentageGamerule=false` 后重试；若要保持完整 2.9 beta 兼容环境，应优先等待/查找 GTNH 对 `ServerUtilities` 与 `Et-Futurum-Requiem` 的配套修复，不要把该第三方 mixin 冲突当作 Qz 源码回归

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
