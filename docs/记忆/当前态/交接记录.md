# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-23 第 81 次）—— SceneSelect 首个消费者已完成

### 一句话现状
overlay P0 地基四段 + 两个缺口补强 + SceneSelect 控件本体 + R11 契约 + demo 页面均已完成并通过 reviewer 审核与定向测试；下一步进入字段引擎最小切片或真机验收。

### 本次结论
- **P0 地基**（提交 `963257b2`）：overlay 纯数据类 + anchor resolver + SceneRuntime.portal + SceneInputRouter 优先命中/dismiss + SceneHostWidget 多 root layout/paint/replay。
- **P0 缺口补强**（提交 `238e220e`）：①portal 四参/portalAnchored 五参重载透传 dismissPolicy/dismissRequest ②AnchorProvider + SceneGeometry.absoluteBox + Entry 存 anchorX/Y + host layout/replay/hit-test 用 anchor 偏移；零回归（provider==null 逐位等价现状）。
- **SceneSelect 控件**（提交 `dbdf1c87`）：trigger 常驻主树，listbox 经 portalAnchored 提升为 overlay root；选中/展开/关闭只写 signal；键盘导航 + listbox 限高滚动；R11 契约写入 control/package-info.java。
- **Demo**（提交 `1f675ef4`）：SceneSelectHostWidget + SceneSelectDemoScreen（4 张卡片）+ Hub 入口 + VIS-SCENE-007 用例 + 测试。
- **宪章**：NORTH_STAR《偏离登记》新增 top-layer 多 paint root + 独立 hit-test 边界扩展（路径 A，待稳定后转正）。
- **审核**：reviewer 审核通过，无阻断问题；5 项建议改进均非阻断（时序窗口罕见、listbox fillParentHeight UX、强转守卫、测试路径差异、anchored 测试覆盖面）。
- **验证**：JetBrains 定向编译通过；Gradle 定向测试 `*SceneOverlay*`/`*SceneSelect*`/`*UiTestDocumentPageController*`/`*UiTestRuntimeHostVisualMatrix*` 全部通过；`git diff --check` 通过。

### 下一步
1. **真机验收**（交用户跑）：`/qzuilib test` → Select demo，验证 listbox 浮空定位、anchor 跟随、外部点击/ESC 关闭、键盘导航、listbox 滚动、帧率。
2. **字段引擎最小切片**：scene-native 字段 spec、draft/current、校验、dirty、canSave/canCancel 派生与保存语义。
3. **接真实配置数据源**：优先接 `MutableConfig`，验证一期字段类型、分类、校验、保存、取消和恢复默认。
4. **暂缓**：搜索、树、多行文本、列表编辑、表格行内编辑、高级 picker、raw editor、远程同步、完整 CSS/token/theme 和 Phase 5。

### 已知边界
- overlay + SceneSelect 全链路未经真机验证：listbox 浮空定位、anchor 跟随、dismiss 手感、帧率仍需 `runClient21` 人工确认。
- reviewer 建议改进未处理（非阻断）：①hit-test/replay anchor 时序窗口（罕见）②listbox fillParentHeight 占满 maxHeight 导致选项少时下方空白（UX，可改 shrink-to-fit）③cachedLayout 强转无 instanceof 守卫 ④SceneSelectTest 布局路径与生产差异 ⑤anchored overlay 测试覆盖面不足。
- 旧对外现代配置页文档仍描述 DOM 实现的 12 模板能力；scene 一期完成前不得宣称能力等价。
- `NORTH_STAR.md` 已登记 top-layer 边界偏离，待 overlay 能力稳定后转正。

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
