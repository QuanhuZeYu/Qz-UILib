# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- **当前分支**：`add/configurable-backdrop-blur`
- **当前任务**：UI 背景模糊开发者快捷配置方案 A+C 已完成实现并提交；正在收口审查修复
- **工作区状态**：实现提交为 `e8ad826f`；用户已有未提交改动 `docs/开发者文档/specs/README.md` 与未跟踪 `docs/开发者文档/specs/backdrop-blur-developer-api-plan.md` 不属于本轮可覆盖范围
- **验证状态**：实现提交前验证已通过；审查修复后重新通过 `git diff --check`、背景模糊相关定向测试和 `compileJava`

### 本轮进度详情

- 已新增页面级 `BackdropBlurPreset` / `BackdropBlurPolicy` / `BackdropBlurController` / `DefaultBackdropBlurController`，并接入 `UiDocument` 与渲染链路
- `UiDocumentScreens.DocumentScreenEnvironment` 已支持页面级策略，业务入口新增 preset / policy 快捷重载
- `DocumentPageController` 与内部 inline controller 已暴露有效背景模糊策略
- 已补充 `HtmlLikeDocumentWidgetRenderingTest` 和 `UiDocumentScreensTest`；纯 JVM 测试不再调用会触发 Minecraft 静态初始化的默认运行时工厂
- 已更新使用文档、架构边界和页面级背景模糊策略决策记录
- 审查修复已明确 `BackdropBlurPolicy.enabled` 是页面级强制总开关，具体字段未声明时仍继承全局配置；`DISABLED` 不再重复覆盖底层 shader/fallback 字段

### 下一步操作

1. 提交或合并前复查 `git status --short`，确认只剩用户 specs 未提交改动
2. 如需游戏内 smoke，再用 `runClient21` 打开使用 `BackdropBlurPreset.DISABLED` / `QUALITY` 的业务文档页面确认宿主背景模糊效果

### 重要约束

- 纯 JVM 测试不要调用 `DocumentScreenEnvironment.minecraftDefaults(...)` / `withoutBackgroundBlur()`，这类工厂会触发 Minecraft 默认运行时适配器静态初始化
- 本轮只实现页面级背景模糊策略与开发者快捷入口，不扩展新的全局配置 UI 或持久化配置项

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
