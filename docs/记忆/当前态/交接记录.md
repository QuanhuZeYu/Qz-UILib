# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次工作分支：`chore/4.2.0-release-finalize`；目标是补完 `4.2.0` 发布流程收尾
- 已完成范围：`4.0` 已合入 GTNH `2.9.0-beta-1` 依赖基线、GTNH Gradle `2.0.25`、`4.2.0` changelog/API 文档、`/qzuilib test` `VIS-PAINT-005` 延迟 top-layer 注册修复，并完成发布前最终验证记录
- 验证状态：已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test`、`./gradlew.bat --no-configuration-cache --no-daemon -x test publishToMavenLocal`；此前已通过 `UiTestDocumentPageControllerTest`、`VIS-PAINT-005` 定向断言和 `compileJava`
- 发布状态：本地准备创建并推送 `4.2.0` tag；远端发布由 `.github/workflows/release-tags.yml` 在 tag push 后触发
- 当前仍需注意：`runClient21` 的 `BytePatternMatcher` 缺类已解除，但本地 GTNH 2.9 beta smoke 仍受 `ServerUtilities 2.3.0` 与 `Et-Futurum-Requiem 2.6.40-GTNH` 第三方 mixin 冲突阻塞
- 后续建议：tag 推送后先看 GitHub Actions 的 release tagged build；若用户要求继续游戏内 smoke，再决定是否临时禁用 `run/client/config/etfuturum/mixins.cfg` 中的 `enablePlayersSleepingPecentageGamerule`

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
