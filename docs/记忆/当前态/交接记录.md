# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前分支：`refactor/decouple-lwjgl3ify`；输入后端加固已提交 `cf1bf055 [Fix]: 加固 lwjgl3ify 输入后端降级路径`，交接计划更新已提交 `b803f0b6 [Docs]: 更新 lwjglx 解耦交接计划`。
- 已完成第一阶段：`UiInputService` facade 不变，内部 `UiInputBackend` / `Lwjgl3ifyInputBackend` / `LwjglxPollingInputBackend` 已落地；`InputEvents` 反射订阅，注册失败时启用键盘轮询兜底；`lwjgl3ify` 发布硬依赖已移除，仅保留 `devOnlyNonPublishable` 与 `testCompileOnly`。
- 当前批次已收口：新增 `UiKeyCodes` 承载当前 LWJGL2/MC 键码数值，控件、HTML-like 文档默认行为、remote、config、devtools 和测试侧已改用该常量层，不改变 `UiKeyEvent.keyCode` 事件语义；对应提交标题为 `[Refactor]: 收拢 lwjglx 键码常量引用`。
- 仅保留边界：`LwjglxPollingInputBackend` 与 `UiHostInputCoordinator` 仍直接调用 `org.lwjglx.input.Keyboard` / `Mouse`，因为它们是客户端宿主输入事件源；普通业务代码不要重新引入 `Keyboard.KEY_*`。
- 已验证第一阶段：`git diff --check`、`Lwjgl3ifyInputBackendTest`、输入/HUD/控件定向测试、`compileJava`、`publishToMavenLocal` 均通过；当前 `UiKeyCodes` 批次已通过 `git diff --check`、输入/控件/HUD 定向测试和 `compileJava`。
- 暂缓项：系统光标 `SystemDocumentCursorHost` 仍有 `Display` 静态 import 和反射 `Mouse/Cursor`，可单独立项；渲染链主要是 `org.lwjgl.opengl.*` / `org.lwjgl.BufferUtils`，OpenGL/字体 atlas 不与本轮键码常量解耦混做。

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
