# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 已开始引入 AWT 字体研究结论：字体 atlas 改为 baseline + actual pixel bounds + 可变 slot 契约；决策记录为 `docs/记忆/决策/DECISION-20260611-awt-ink-bounds-atlas.md`
- 当前实现已扩展 `GlyphInfo` / `GlyphRuntimeTables`，`GlyphGenerator` 不再缩字号塞固定格，`GlyphPage` 使用 shelf packing，`DefaultFontRendererAdapter` / `FontBatchRenderer` 按 atlas baseline 生成 quad，渲染 UV 按完整可变 slot 采样，`TextLayoutService` 用 AWT advance 测量宽度
- 空白 glyph 现在保留 advance 但不上传 atlas bitmap；`FontMatcher` 对空白字符允许匹配字体，避免 outline 为空导致 advance-only glyph 无法生成
- 后续路线仍是先扩展字体引擎字号能力，再实现数学公式渲染和排版；当前 atlas 契约是字号能力的底层前置
- 下一步需继续跑字体相关定向测试、`compileJava` 和必要时 `runClient21` 观察真实字体基线、CJK、emoji、下划线/删除线观感
- 用户已授权后续自动 Git 收口：除非用户明确说明“不提交”或“不合并”，Agent 应按任务完成情况自动判断是否提交、是否合并回主分支，并可自动执行

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
