# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次工作分支：`4.0`；目标是修复 Qz-Team 点击多人游戏后 Qz-UILib HUD 输入链路反射深扫 Java 21 JDK 内部字段导致崩溃，并发布 `4.2.1`
- 已完成范围：`UiHudDocumentHost.createInputContext(...)` 先计算 `interactiveInputEnabled`，只有 `CONTAINER` 且 HUD 交互输入启用时才调用 `UiNativeTextInputInspector.hasFocusedTextInput(...)`；新增 `shouldInspectNativeTextInput(...)` 包内判断；补充 `UiHudDocumentHostTest.shouldInspectNativeTextInputOnlyWhenHudInputIsInteractive`
- 文档状态：已新增 `docs/开发者文档/errors/ERROR-20260610-hud-menu-native-scan.md` 与 `.changelogs/4.2.1.md`，并更新 `docs/开发者文档/errors/README.md`、`docs/记忆/当前态/当前上下文.md`、`docs/记忆/当前态/当前计划.md`
- 验证状态：已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.hud.UiHudDocumentHostTest"`、`./gradlew.bat --no-configuration-cache compileJava`、`./gradlew.bat --no-configuration-cache --no-daemon -x test publishToMavenLocal`
- 当前仍需注意：`4.0` 与 `4.2.1` tag 已推送到 `origin`，远端 tag 目标提交为 `1f707b94`；当前环境因 `gh` 未登录且 GitHub API 返回 403，尚未确认 GitHub Actions release workflow 是否成功生成资产和 release notes
- 后续建议：更新 Qz-Team 使用的 Qz-UILib `4.2.1` 后点击多人游戏，确认 `latest.log` / `fml-client-latest.log` 不再出现 `InaccessibleObjectException`

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
