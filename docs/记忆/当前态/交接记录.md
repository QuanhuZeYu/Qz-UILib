# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-20 第 43 次）—— Scene Phase 3（分级失效全链路 + 合成级动画）已 `--no-ff` 合回 4.0

- **当前分支**：`4.0`，已合入。Phase 3 单提交 `744482f5`（12 文件 +1053/-20）+ 合并提交 `89e7c1ed`（`--no-ff` 合 `feat/scene-phase3-graded-invalidation`）。工作树对源码干净，领先 `origin/4.0` 240 提交未推送。
- **Phase 3 交付要点**：
  - 3A：`SceneNode` composite 路标独立化——新增 `descendantCompositeDirty` + `bubbleDescendantComposite()`（从 parent 起、遇已点亮即停、绝不向下递归，守 I7），`markComposite` 不再借道 paint 路标，`clearPaintDirty`/`clearCompositeDirty` 双向解耦。
  - 3B：合成级动画走 `PUSH_OPACITY`/`POP_OPACITY` 边界命令（opacity 走渲染层 group 栈天然相乘、replayer 传局部值防双重衰减；transform 仅 translate 走 int offset 通路）。opacity/transform 绝不存进 `PaintFragment`，每帧从 node 实时读取，纯 composite 帧 fragment 零重建——落地信条五铁律「60fps 合成动画不触碰布局/绘制层」。
  - oracle 收口审查有条件 PASS、无 P0；已落实 transform 取整改 `Math.round()`、needGroup 纳入 `box != null` 前置、markComposite 已脏短路、补共存单测。
- **已登记边界（非 bug）**：`Transform` 仅 `translateX/translateY` 两字段，rotation/scale/亚像素不支持；整数量化是 int offset 通路固有约束。回填方向与依据见 `NORTH_STAR.md`《偏离登记》2026-06-20 条 + `docs/记忆/决策/DECISION-20260620-scene-composite-opacity-group-transform-offset.md`。
- **唯一剩余事项 = 真机帧率验收（交用户跑，沙箱无 GUI）**：入口 `/qzuilib test` → SCENE_DEMO 组 → 打开 demo 页。验收点：opacity/transform 改动真正刷上屏 + 纯 composite 帧零重排零重建。真机后派子代理查 `run/client/logs/fml-client-latest.log` 中 `club.heiqi.uilib` 零异常。
- **下一步（待用户拍板）**：strangler 路线图 Phase 4 = 控件层重建（`/qzuilib test` 各分组逐组切换到 scene 栈）；Phase 5 = 宿主粘合层迁移 + 旧栈退役。当前态总策略仍是「暂停新功能，重点查 bug / 代码审查 / 可控优化」，Phase 3/4 属用户明确点名推进的例外。

## 重要纪律（长期沿用）

- 决策点优先用中文 `question` 向用户拍板；帧率/真机实测必交用户跑，沙箱无 GUI。
- 编译/测试优先走 JetBrains MCP（见 `AGENTS.md` 第 8 节，shell gradle 曾卡死）；仍需 shell 时用 `$env:GRADLE_USER_HOME="D:\.MyApps\.ENV\gradle-home"; ./gradlew.bat ...`（.MyApps 双 p）。
- PowerShell 不支持 `&&`，链式用 `;` 或 `cmd1; if ($?) { cmd2 }`。
- 提交/合并用 `git commit -F <文件>` / `git merge --no-ff <分支> -F <文件>` + UTF8 无 BOM，消息文件写临时目录绝对路径（非仓库内 `.git\` 路径）。
- git 操作必须如实读取 EXIT 码 + 真实 stdout，不预判、不编造命令成功输出。

## 更新规则

- 只保留最近一次交接的当前真相，历史交接直接覆盖，不按次数累积。
- 施工逐行细节沉淀到决策/审查/错误文档，本文件只留「现状 + 下一步 + 边界」。
