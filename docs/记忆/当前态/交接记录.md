# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- **当前分支**：`release/4.2.4`
- **当前任务**：准备 4.2.4 小版本发布；版本号由最终 Git tag 推导，发布正文覆盖文件为 `.changelogs/4.2.4.md`
- **工作区状态**：`release/4.2.4` 本地发布准备收口后，下一步推送分支与 `4.2.4` tag
- **验证状态**：已通过 `git diff --check`；如进入正式发布前，建议再运行 `compileJava` 与 `publishToMavenLocal`

### 本轮进度详情

- 已确认仓库发布流程：`.changelogs/<tag>.md` 必须存在于 tag 指向提交中，Release 标题由 workflow 统一设为 tag 名
- 已确认源码没有固定 `modVersion` 需要手工递增；构建版本由 Git tag / GTNH Gradle 推导
- 已基于 `4.2.3..HEAD` 汇总 4.2.4 更新内容：字体像素字号与 atlas、共享虚拟候选列表、LWJGL3ify 输入后端可选化、背景模糊配置和页面级策略

### 下一步操作

1. 推送 `release/4.2.4` 分支
2. 推送 `4.2.4` tag，触发 Release workflow
3. 发布后检查 GitHub Release 标题是否为 `4.2.4`，正文是否来自 `.changelogs/4.2.4.md`

### 重要约束

- 不要在未包含 `.changelogs/4.2.4.md` 的提交上创建 tag，否则 Release workflow 读取不到覆盖正文
- 本地验证结果不写入 `.changelogs/4.2.4.md`，只记录在交接、提交说明或开发者文档中

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
