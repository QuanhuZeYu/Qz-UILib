# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- **当前分支**：`fix/host-background-blur-perf`（从 `4.0` 创建，完成后以 `git merge --no-ff` 合并回 `4.0`）
- **当前任务**：宿主级背景模糊全局默认关闭 + 修复 capture 无条件全屏快照（已完成）
- **背景**：打开 `/qzuilib test` MODCFG 现代配置 demo 后 FPS 仍低。既有 ModernConfig 性能优化只针对内容构建（DOM/Binding/索引），真正的每帧固定大头在屏幕渲染框架层——`UiScreenHostSession.render` 每帧无条件全屏 `glCopyTexSubImage2D` + 9 次全屏模糊绘制，所有 `BaseScreen` 共有。诊断与取舍见 `docs/记忆/决策/DECISION-20260614-host-background-blur-default-off.md`。
- **工作区状态**：4 个源码/测试文件 + 1 个决策文档 + 决策 README 索引 + 本交接记录。
- **验证状态**：本协作环境无 JDK 且 gradle 需联网，**未实跑** `compileJava`/测试；已通过完整 `git diff` review、静态一致性核查（签名/import/调用点/默认值依赖全部一致）和独立子 agent 审查（编译性/逻辑/回归/测试隔离四项通过）。本机需补跑 `git diff --check`、`compileJava` 与 `BackdropBlurPolicyTest`（及 render/screen 回归）最终确认。

### 已完成项

- `BackdropBlurConfig.hostBackgroundBlurEnabled` 字段默认与 `resetToDefaults()` 改为 `false`
- `applyPerformancePreset()` / `applyQualityPreset()` 显式置 `true`，避免“质量/性能预设却无模糊”
- `UiHostBackgroundBlurRenderer.captureCurrentFramebuffer` 增加 `BackdropBlurPolicy` 参数与启用判断，禁用时跳过全屏快照（与 `drawBlurredBackground` 对称）
- `UiScreenHostSession.render` 提前解析页面策略并传入 capture
- `BackdropBlurPolicyTest` 新增 3 项断言（默认关闭 / 显式策略保留 / 预设开启）

### 遗留与注意

- 需要宿主背景模糊的页面：override `getBackdropBlurPolicy()` 返回 `quality()`/`performance()` 或 `inheritGlobal().withHostBackgroundBlurEnabled(true)`，不受全局默认关闭影响
- 元素级 `backdrop-filter` 不受影响，本次只动宿主级默认开关
- 协作环境提示：本仓库工作树存在文件 stat-cache 现象，增量编辑工具的改动可能不被 git 感知甚至损坏 index，应优先用 shell 直接改盘并 `git status` 核实落盘

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
