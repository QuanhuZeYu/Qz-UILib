# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-25 第 96 次）—— 据 AWT 研究修正字体引擎坐标系 + trim 口径

### 一句话现状

插队任务完成：依据 `D:\CodeSpace\MC\JavaAWT研究` 的研究结论，统一字体烘焙字号与渲染缩放分母（消除 57.6/64 不自洽），并修复一个预先存在的 trim/getStringWidth 口径不一致 bug。45 个字体测试全绿，待真机验证。

### 本会话关键 commit（按主题）

- `cb786955` 步骤1治本（方案B）：DerivedFontCache + FontRegistry 去掉 `×fontScale`，烘焙字号 57.6→64；清理 FontConfig.fontScale 幽灵配置（5处）
- `007ac778` 修复 trimRawStringToWidth 与 getStringWidth 的 18/9 口径不一致（:783 传 charSize 而非 DEFAULT_FONT_SIZE_PX）

### 研究依据

- `D:\CodeSpace\MC\JavaAWT研究\docs\开发者文档\Qz-UILib字体渲染修复建议.md` — 系统性根因 + 4步修复路径
- `D:\CodeSpace\MC\JavaAWT研究\docs\开发者文档\AWT-Qz旧策略对比研究.md` — AWT 字体正确用法与错误用法对比
- 业界约定（ImGui/TextMeshPro/Bevy 等7库）：烘焙字号 = 渲染缩放分母

### 修复内容

**步骤1（治本·方案B·用户拍板）**：烘焙改 64，glyphSize 语义统一
- `DerivedFontCache.java:41` 去掉 `× FontConfig.fontScale`
- `FontRegistry.java:64,80` 注册派生字号去掉 `× fontScale`
- `FontConfig.java` 移除 fontScale 字段/配置读取/变更检测/快照刷新
- GlyphGenerator:69 与 FontBatchRenderer:244 无需改，量纲自洽后自动正确

**额外修复（预先存在 bug）**：
- `TextLayoutService.java:783` trimRawStringToWidth 无参重载从 DEFAULT_FONT_SIZE_PX(18) 改为 (int)FontConfig.charSize(9)
- 该测试在 4.0 主分支上本就失败（git stash 验证），与字号统一无关，但同批修好转绿

### 验证结论

- jetbrainsBuildProject 全量编译通过
- 45 个字体测试全绿（含此前一直失败的 shouldTreatSectionCodesAsVisibleCharactersInUiLibRawTrimAndWrap 转绿）
- reviewer 审核通过（I6/信条七未破坏）
- **真机验证待跑**：用户 runClient21 看偏上/偏右/偏小是否消除

### 下个会话任务表

1. **真机验证**：用户跑 runClient21 确认步骤1效果（偏上/偏右/偏小/缺边是否消除）
2. **若真机仍有下划线丢/缺边**：考虑叠加步骤2（关 mipmap + 放宽 smoothstep + slotGap≥4）
3. **若真机 emoji 仍偏右**：考虑步骤4（emoji advance 语义 + fallback 字体一致性，需真机排查）
4. **合回 4.0**：真机通过后按 AGENTS.md 用 `git merge --no-ff` 合回
5. **恢复主线**：嵌套分类树导航 / 字段引擎最小切片

### 当前分支

- `fix/glyph-coordinate-system-mismatch`：本次修复，工作区干净，待真机验证后合回 4.0
- `4.0`：主分支，scene Phase 0-4 + 统一 chrome 已合入

### 已知边界

- 步骤2/3/4 未做（用户拍板仅步骤1 + 预先存在 bug 同批修）
- fontScale 配置项已移除，配置同步测试（ConfigSyncModelsTest 等）用 general/render category 占位，不受影响
- 烘焙字号改 64 后 bitmap 变大约 11%，atlas 占用增加，但研究测算最高占用率 4.429%（1 页可容纳）

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能登记到正确 Owner。
