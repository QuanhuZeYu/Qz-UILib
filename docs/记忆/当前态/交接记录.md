# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-25 第 108 次）—— I6 + B2 技术债清理合回 4.0

### 一句话现状

完成两项 P1 技术债清理：I6 切断 scene replayer 对 ui.style 反向依赖 + B2 TextArea O(N²) 文本几何消除。两项均经 oracle 裁决/explorer 侦察 + fixer 实现 + reviewer 审核，已合回 4.0，工作区干净。

### 本会话关键 commit（已合回 4.0）

| commit | 内容 |
|--------|------|
| `c9c53208` | [Fix]: 切断 scene replayer 对 ui.style 的反向依赖（I6） |
| `ae263575` | [Refactor]: TextArea 行定位改用前缀和缓存消除 O(N²)（B2 Step1） |
| `7b7b1722` | [Refactor]: TextArea 点击前缀宽数组加跨帧缓存（B2 Step2） |
| merge | [Merge]: I6 切断 scene replayer 对 ui.style 反向依赖 |
| merge | [Merge]: B2 TextArea 文本几何 O(N²) 消除 |

### I6 关键结论

- 根因在 `UiRenderBackend.drawSurface` 接口第 7 参是 `ui.style` 的 `ResolvedCornerRadii`，不是 replayer 自身
- 方案：第 7 参降级为 `int cornerRadius`（replayer 只传 uniform 单值，零信息损失），render 层保留 ResolvedCornerRadii 重载供 backdrop-filter 用
- 守线增强：ScenePackageIsolationTest 新增 ui.style 禁令，覆盖四目录 + 顶层 + replayer import 行

### B2 关键结论（oracle 裁决）

- 选方案 B(R1) scene 内自建轻量缓存；否决整包复用旧栈 TextLayoutEngine（撞 I6/I10）和混合复用单行 PrefixWidthCache（不降单次复杂度）
- **关键陷阱**：scene measureWidth 含 ceil+round 双取整，"逐码点相加"会漂移且 FixedTextMeasurer 线性导致测试假绿——buildPrefixWidths 函数体禁改
- Step1：LineStructureCache（行结构前缀和，失效键 value.equals），7 个行定位方法改查表
- Step2：ClickPrefixWidthCache（点击前缀宽，失效键三元组含 epoch），buildPrefixWidths 函数体不动
- 决策文档：`docs/记忆/决策/DECISION-20260625-b2-textarea-text-geometry.md`

### 验证状态

- jetbrainsBuildProject 全量编译通过
- I6：ScenePaintEngineTest + ScenePackageIsolationTest 通过
- B2：SceneTextAreaTest 31 用例全绿（原 24 + Step1 新增 6 + Step2 新增 1）
- **B2 真机帧率验收待用户跑**（O(N²)→O(N) 收益为理论推导）

### 当前分支

- `4.0`：主分支，I6 + B2 已合入，工作区干净
- `fix/i6-replayer-ui-style-dep` / `refactor/b2-textarea-text-geometry`：已合并，可清理

### 下个会话任务表

1. **嵌套分类树导航** / **字段引擎最小切片**（新功能，待用户拍板范围）
2. 其余温床（B4/B5/B6/B7/B8/A1/A6）按需处置，chrome 主题层单独立项
3. B2 真机帧率验收（交用户跑）

### 已知边界

- B2 缓存只缓存最近点击行单行一份，跨行点击每行 miss 重建（设计如此，点击是离散事件）
- B2 单次 buildPrefixWidths 仍行内 O(L²)，跨帧缓存 O(1) 命中；真 O(N) 的 raw 累加端口（R2）本期不做，留待真机瓶颈
- I6 reviewer 提示 R1：input 包扫描未加 ui.style 禁令（既有缺口，当前无引用不触发）
- I6 reviewer 提示 R2：NORTH_STAR I6 字面未列 ui.style，依赖衍生解释，可考虑补登记

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能登记到正确 Owner。
