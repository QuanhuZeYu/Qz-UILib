# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-26 第 116 次）—— B6 FBO 离屏图层方案批 1 落地

### 一句话现状

B6 transform+clip 叠加坐标错位修复——FBO 离屏图层方案批 1 落地。
从决策文档附录接手，经 explorer 侦察 + oracle 实施前评估 + 用户三决策拍板
（有 clip 才 FBO / 降级保留 clip 放弃 transform / I7 偏离立即登记），
实现 PUSH_TRANSFORM_LAYER/POP_TRANSFORM_LAYER 命令 + PaintContextCompositor
pushTransformLayer/popTransformLayer（FBO 借还 + GL 状态管理）+ engine 门控判定。
reviewer 有条件通过（P0 偏离登记 + P2 glMatrixMode 修复 + 测试缺口补全均已处置）。
**待真机 rotate+clip 渲染验收 + 帧率实测**。

### 本会话累计 commit（待合并回 4.0）

| commit | 内容 |
|--------|------|
| （待提交） | [Add]: B6 FBO 离屏图层方案批 1——transform+clip 叠加正确处理 |

### 本会话任务（B6 FBO 方案批 1 实现）

**oracle 实施前评估（全新 session）**：
- 信条五/I7 成立性：命令层守 I7（fragment 引用不变），像素层批 1 破坏 I7 合成阶段
  （FBO 每帧重栅格化），须登记偏离，回填=FBO 纹理脏标记跨帧复用
- 矩阵基线选 C（MODELVIEW=I，保留 PROJECTION），非 drawHostImage 的重设 PROJECTION
- T 矩阵时序：段内纯 I，T 生命周期完全在 popTransformLayer 内
  （end→applyClipSnapshot→pushTransform(T)→composite→popTransform）
- 封装位置：复用同一 frameStack（共享 borrowedLayerCount/parentFramebufferId/disabledForFrame）
- inactive 降级语义是决策文档盲区：FBO 不可用时 transform+clip 物理无解
- stencil 代价几乎零（圆角clip走stencil与矩形clip走FBO互斥）
- 决策文档"数据层主动禁止"是措辞错误（代码只有注释无运行时强制）

**用户三决策拍板**：
1. 范围：有 clip 才 FBO（统一处理 transform+clip，无 clip 走 GL 矩阵纯顶点变换）
2. 降级：保留 clip 放弃 transform（FBO 不可用时不压 T，子树未变换坐标直画）
3. I7 偏离：批 1 落地时立即登记

**批 1 实现（9 生产文件 + 4 测试文件）**：
- PaintCommandType：新增 PUSH_TRANSFORM_LAYER/POP_TRANSFORM_LAYER
- PaintCommand：新增工厂方法 + translatedBy 防御 + toString
- PaintPlan：新增 addPushTransformLayer/addPopTransformLayer
- UiRenderBackend：接口新增 pushTransformLayer/popTransformLayer
- PaintContextCompositor：FrameKind 枚举 + PaintContextFrame 扩展
  kind/clipSnapshot/transform 分量 + pushTransformLayer/popTransformLayer
  + finishFrame 通用化 + currentScreenHeight 字段
- UiRenderContext：新增 pushTransformLayer/popTransformLayer 转发
- ScenePaintEngine：门控判定（needTransform&&needClip→LAYER）+ 注释改写
- ScenePaintReplayer：新增 PUSH_TRANSFORM_LAYER/POP_TRANSFORM_LAYER case
- RecordingRenderBackend/NoopBackend/RecordingBackend：适配新接口方法
- SceneBackendContractTest：白名单更新 + 新增场景 7/7b/7c
  （transform+clip / 三层嵌套 / 嵌套 transform+clip）

**reviewer 审核（有条件通过）**：
- P0 阻断（I7 偏离未登记）→ 已处置（NORTH_STAR 偏离登记新增 2026-06-26 条目）
- P2-2（:226 glMatrixMode 缺失）→ 已修复（glPopAttrib 副作用防御）
- P2-3/4（currentScreenHeight 单值/inactive kind）→ 记为后续优化
- 测试缺口 5/6（三层嵌套 + 嵌套 transform+clip）→ 已补全
- 测试缺口 7/8（inactive 降级/GL 状态时序）→ 架构限制，真机实测覆盖

### 验证状态

- compileJava + compileTestJava 通过
- scene 全包测试通过 + devtools pages 测试通过
- reviewer 有条件通过（P0+P2 处置完）
- **待真机验收**：
  - rotate + 矩形 clip 渲染正确性（FBO 离屏图层 + T 矩阵回贴）
  - transform+clip 帧率（FBO 每帧全屏 clear + 回贴 fillrate 代价）
  - 嵌套 transform⊃opacity⊃clip GL 状态正确性
  - 圆角 clip + transform（stencil 在 FBO 内 MODELVIEW=I 下正确性）

### 剩余债务全量状态

**已清零**：
- B6 transform+clip 叠加坐标错位 ✓（FBO 离屏图层方案批 1 落地）
- 契约层 GL 术语清零 ✓（pushPaintContext→pushGroupOpacity）
- 跨后端契约测试锚点 ✓
- chrome 收口全部完成
- 其余见第 115 次交接记录

**真未还（代码债）**：
- B6 FBO I7 合成阶段偏离：FBO 纹理每帧重栅格化，待脏标记跨帧复用回填
- A1 effect 内 set 慢一帧：残留语义待确认
- B4 COLUMN fill O(n²)：缓做
- chrome 主题层：P2 大工程未立项
- Phase 5 旧栈退役：阻塞中

**有意边界（非债）**：
- B6 hit-test 对偶：与 B6 正交，需单列登记遗留
- B6 FBO 全屏尺寸：批 3 局部 FBO 优化待性能暴露后启动
- SceneTextArea 功能限制（选区/剪贴板/IME 等）
- DataTable 嵌入式深色槽 12 个保留+注释

**真机验收待办**：
- B6 rotate+clip 渲染正确性 + 帧率
- B8 滚动后 hover 切换
- B2 TextArea 压力测试页帧率
- Breadcrumb ROOT_GAP 6→4px
- Phase 1-3 真机帧率/上屏
- 改名后真机 GL 路径（pushGroupOpacity FBO 离屏合成行为）

### 当前分支与工作区

- 当前工作分支：`add/b6-fbo-transform-layer`（待合并回 4.0）
- 4.0 工作区将保持干净

### 下个会话任务表

1. **真机验收**：B6 rotate+clip 渲染 + 帧率（用户跑）
2. **B6 FBO 纹理脏标记缓存**（批 3，性能暴露后启动）：还 I7 偏离
3. **B6 hit-test 对偶登记**：需单列登记遗留
4. **NORTH_STAR 偏离登记 B6 主动禁止措辞修正**：决策文档 :47/:154/:170 "主动禁止"→"注释声明约束无运行时强制"
5. **主线拍板**：是否启动新切片
6. **A1 残留确认**：核 ReactiveScheduler.flush 收敛终止条件

### 已知边界

- B6 FBO 方案完整评估存于 `docs/记忆/决策/DECISION-20260626-b6-transform-clip-fbo-deferred.md`
- oracle 实施前评估结论存于会话记录（信条五铁律 + 矩阵基线 + T 矩阵时序 + inactive 降级）
- I7 偏离登记存于 NORTH_STAR.md 偏离登记 2026-06-26 条目
- FBO 纹理跨帧缓存（还 I7 债）优先级高于局部尺寸 FBO（省 fillrate）
- inactive 降级语义：FBO 不可用时保留 clip 放弃 transform（不压 T，clip 正确 transform 失效）
- currentScreenHeight 单值字段：全屏 FBO 下安全，局部 FBO 需存入 frame
- B6 决策文档"主动禁止"措辞需修正（代码只有注释无运行时强制）

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑，避免并行 Gradle 竞争 build/ 目录。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，
  改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能
  登记到正确 Owner。
- token 实际值以 SceneChromeTokens.java 源码为准，不依赖过期文档清单。
- oracle 裁决架构方案后，如方案涉及长期可引用的设计决策，建议补写
  `docs/记忆/决策/` 文档沉淀。
- I11 逃生舱②扩张纪律：扩之前证明它确实是一类新的权威交互真值，
  而非可以用现有机制表达的东西（B8 验证了 hover 重算不需要扩②）。