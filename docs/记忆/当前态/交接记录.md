# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-28 第 127 次）—— docs 文档层清理 + SceneTestHub 滚动视口

### 一句话现状

完成 docs 文档层系统性清理：建立 scene 技术债权威源、归档当前上下文历史并重写为精简当前真相、
修架构边界 overlay/Phase4 过期描述、修 5 处死链、补 3 处索引遗漏、精简 reviews 索引超载段、
删空目录、specs 时态更新、Config 模块归并。前置小任务：SceneTestHub root 改滚动视口 + 半透明背景。
工作区干净，2 commits 已落（077e1102 + 1aa1cc80），4.0 领先 origin/4.0 3 commits。

### 本会话改动清单

**前置小任务**：
- `SceneTestHubHostWidget.java`：root 背景改半透明 `0xCC20242B`，加 `setScrollable(true)` + `SceneScrolls.attach`，整页可滚轮滚动

**docs 文档层清理（主 Agent 亲做 docs/记忆/ + 并行 fixer 处理非记忆文档）**：
- 新建 `docs/记忆/长期事实/scene技术债.md`（权威债务清单，整合 4 处副本）
- 新建 `docs/记忆/当前态/历史/当前上下文_2026-06-28.md`（历史归档，301行）
- 重写 `docs/记忆/当前态/当前上下文.md`（364行→约60行精简当前真相）
- 修 `docs/记忆/长期事实/架构边界.md:20/22`（overlay 已落地、Phase 4 已大批完成）
- 修 `docs/记忆/当前态/交接记录.md:117` + `docs/记忆/决策/DECISION-20260627:183`（.slim/deepwork→history 死链）
- `docs/记忆/决策/README.md` 补 2 处遗漏索引（modern-config-phase3、scene-composite-opacity）
- `docs/记忆/当前态/交接记录.md` 债务清单段改指针引用 scene技术债.md
- 修 4 处反引号死链（ERROR-20260618 / DECISION-20260618 / REVIEW-20260618）
- 删除空目录 `docs/设计方案/`
- `docs/开发者文档/reviews/README.md` 补 2 处遗漏索引 + 精简超载段（276→262行）
- `docs/开发者文档/specs/scene-overlay-p0-plan.md` 时态更新（已完工施工计划转施工记录）
- `docs/开发者文档/specs/scene-chrome-color-spec.md:395` 删除已退役 SceneTable 引用
- `docs/记忆/长期事实/Config模块.md` 归并阶段完成态为稳定能力描述

### 任务链路

1. 用户要求 test 页面容器做成滚动容器 + 背景半透明 → 主 Agent 亲改 SceneTestHubHostWidget
2. 用户要求清理 docs 文档层 → 派 explorer 全量侦察 docs/（213 文件）
3. explorer 产出事实清单：硬过期2处、超载流水账1处、死链5处、空目录1个、索引遗漏3处、重复4副本、归属错位3处
4. 用户拍板 4 个决策点：当前上下文归档+重写 / 新建 scene技术债.md 权威源 / SceneTable 侦察直接删 / 并行派 fixer
5. 并行派 fixer A（非记忆文档死链+空目录+reviews 索引）+ fixer B（specs 时态+Config 归并）
6. 主 Agent 亲做 docs/记忆/：建 scene技术债.md、归档+重写当前上下文、修架构边界、修死链、补决策索引
7. 派 reviewer 审核一致性与遗漏 → 5 项后续处理
8. 主 Agent 逐项修：B6 补 hit-test 对偶、交接记录债务改指针、spec 删 SceneTable、DECISION-20260627 死链、reviews 补 REVIEW-20260602 索引

### 当前分支与工作区

- 当前工作分支：`4.0`，工作区干净
- 4.0 领先 origin/4.0 3 commits（第126次 1 commit + 本会话 2 commits）
- 游离分支 `codex/birthday-remote-trigger-practice`（个人彩蛋，保留游离不合并不删除）
- 交接记录此前提到的"4 个 refactor/scene-* 开发分支"本地已不存在，远端无对应，属过时信息

### 下个会话任务表（用户决定优先级）

1. **可选：push 4.0 到远端**（含第126次 1 commit + 本会话 2 commits）
2. **可选：启动新方向**（详见 `docs/记忆/当前态/当前计划.md` 与 `docs/记忆/长期事实/scene技术债.md`）
3. **待真机验收项**（交用户跑，沙箱无 GUI）：见下方"待真机验收清单"

### 验证状态

- SceneTestHubHostWidget 改动经 jetbrainsBuildProject 编译零问题
- docs 清理经 reviewer 独立审核：5 项后续处理已全部修复
- 未跑测试（docs 纯文档改动 + SceneTestHub 改动需真机验收滚动效果）

### 待真机验收清单（沙箱无 GUI，交用户跑）

- ~~B2 TextArea O(N²)→O(N) 帧率收益~~ → **2026-06-28 用户确认无异常**
- ~~SceneTestHub 滚动视口效果~~ → **2026-06-28 用户确认无异常**
- ~~glyph 坐标系修复~~ → **2026-06-28 用户确认无异常**
- B6 FBO 真机渲染：**有效避免裁切问题，但性能压力太大**（2026-06-28 用户反馈），批 3 纹理缓存需重新评估性能取舍
- ~~文本垂直对齐 em-box 居中模型真机视觉~~ → **2026-06-28 用户确认无异常**
- ~~lwjgl3ify onTextEvent emoji 真机路径 + Bridge remove API 内存泄漏隐患~~ → **2026-06-28 用户确认无异常**
- network Forge 回退 + BungeeCord/Velocity 代理联机实测：**网络层暂时搁置，集中处理 UI 层**（2026-06-28 用户指示）

### 已废弃方向（不再维护）

- 旧 HTML-like / `ui.dom` 栈相关任务（Phase 5 退役清理）：旧栈已废弃，退役清理不纳入当前工作主线

### 待决策/待实现项（未排期，待用户拍板）

- N1 capture 生产化前置必修项：requestPointerCapture 持有期 POINTER_DOWN 焦点/指针归属相反（REVIEW-20260618-scene-input-i4-merge:91-93）
- Config scene 版 8 种字段类型未落地：LONG_TEXT/SIMPLE_LIST/TABLE/OBJECT/KEY_VALUE_MAP/PRESET_SELECTOR/RAW_EDITOR/ENHANCED_PICKER（Config模块.md:141）
- datatable 编辑列视觉 D1/D2 拍板项（datatable-editable-cell-visual-spec:257-263）
- scene-overlay-p0-plan：NORTH_STAR 是否把 top-layer 写入正文/偏离登记待用户确认（:149）
- net_self_check 页待恢复（network-layer-plan:16）
- wheel 事件 DOM 分发（旧栈 3.4 P2，REVIEW-20260601-browser-semantics-phase2-followup:192）
- A1 effect 内 set 慢一帧残留待确认（scene技术债.md）
- font-family 长期暂缓（有意边界，DECISION-20260601）

---

## 重要纪律（长期沿用）

- 帧率/真机实测必交用户跑，沙箱无 GUI
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做；
  但 opencode 重启后原 session 失效，按"调度开销明显大于收益 + 信息已在上下文"
  白名单主 Agent 可亲自做单点排错
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑，避免并行 Gradle 竞争 build/ 目录
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，
  改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能
  登记到正确 Owner
- token 实际值以 SceneChromeTokens.java 源码为准，不依赖过期文档清单
- oracle 裁决架构方案后，如方案涉及长期可引用的设计决策，建议补写
  `docs/记忆/决策/` 文档沉淀
- I11 逃生舱②扩张纪律：扩之前证明它确实是一类新的权威交互真值，
  而非可以用现有机制表达的东西（B8 验证了 hover 重算不需要扩②）
- 诚实记录 oracle 方案缺口：oracle 不是神谕，发现 bug 诚实记录修复
- **I7 数值求解器边界**：父级数值求解器内多轮迭代（freeze do-while）≠ 多 pass，
  单 pass 约束的是父→子约束下沉次数；判据是「求解器若需要先 layout 子、读子结果、
  再回头改父分配即违反 I7；若全程父级数值迭代、子在收到最终约束后才首次 layout，则守 I7」
- **margin 五处联动纪律**：改 margin 任一处需同步审视 SizingCalculator/
  ConstraintResolver/FlexLayouter/SceneGeometry 四协作者
