# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- **当前分支**：`4.0`（修复已合入）。修复在 `perf/modern-config-paint-style-cache`（从 `4.0` 创建）开发，已 `git merge --no-ff` 合回 `4.0`（合并提交 `f3cf1146`，修复提交 `4e56a612`）。
- **当前任务**：修复 `ModernConfigTemplateScreen`（`/qzuilib test` MODCFG demo）大视图 ~3 FPS。**根因修复已落地并合入 4.0，待本机编译 + 游戏内实测确认收益。**
- **改了什么**：`DocumentPaintRenderer` 绘制重放期引入「单趟绘制」ComputedStyle 备忘缓存。新增 `resolveStyle(ElementNode, Map)`：命中即返回；伪元素回退原始 `compute()`；普通元素递归解析父样式后走 `computeWithParentStyle` 并写入备忘。每次 `render(...)` 新建 `IdentityHashMap` 串入所有 `render*` / 表格 / `resolveCommandCornerRadii` 辅助方法，替换全部 8 处 `UiStyleResolver.compute(...)`。消除逐命令递归到文档根的祖先链级联重算。方案取舍见 `决策/DECISION-20260614-modern-config-paint-style-cache.md`。
- **为什么这么修**：根因已三轮实测确认＝重放期逐命令调无缓存、递归到根的 `compute()`（`replay`≈300–420ms、`replayStyleComputes`≈3800≈命令数 5.2×），非脏子树/布局（`resolve`≈33ms、`reusedSubtrees`=6520）。最小改动＝绘制期备忘缓存，语义等价、单文件、零接口变更。
- **验证状态**：沙箱无 JDK 未实跑编译；已过完整 diff review、brace/paren 平衡、方法作用域一致性核查与独立 adversarial review（编译性 / 语义等价 / 安全 / 表格路径四项 PASS）。**本机需补跑** `git diff --check`、`./gradlew.bat --no-configuration-cache compileJava`、ModernConfig/paint 定向测试与游戏内进 MODCFG 大视图实测。
- **怎么验收益**：① fps 直接看 `UiPerformanceMonitor`（4.0 已有，开 `Config.useDebug` 后 grep `UI 运行统计`），预期 fps 从 ~3 升到十几~几十、`render` 从 ~326ms 降到几十 ms。② 若要对比 `compute` 次数，诊断埋点只在 `perf/modern-config-paint-diagnostics`（临时提交 `50519f9f`/`ec6a7966`，未并入 4.0）：把含本修复的 `4.0` 合并进该分支再跑、grep `文档绘制诊断`；注意本修复主路径走 `computeWithParentStyle`（未计数），故 `replayStyleComputes` 会降到接近 0（仅伪元素回退计入），属预期，真正收益看 `replay` 毫秒数。
- **后续可选**：若要让重放完全不再 `c