# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次工作分支：`chore/4.2.0-release-postcheck`；目标是收口 `4.2.0` 重发后的状态记录
- 已完成范围：已迁移 Qz-Miner 的 `.changelogs/<tag>.md` 本地发布说明覆盖流程，补齐 `.changelogs/4.2.0.md`、`docs/开发者文档/发布流程.md`、release workflow 后置覆盖步骤、开发者文档索引和长期项目约定；`gh` 2.93.0 已安装到 `C:\Program Files\GitHub CLI\`
- 发布状态：`4.2.0` 已重新发布，tag 指向 `8d086ab`，GitHub Release 标题为 `4.2.0`，正文已确认来自 `.changelogs/4.2.0.md`，不再使用首次发布生成的简版 PR notes
- 验证状态：发布前已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test`、`./gradlew.bat --no-configuration-cache --no-daemon -x test publishToMavenLocal`；本轮状态记录提交前仍需再跑 `git diff --check`
- 当前仍需注意：`gh` 已安装但未登录，需执行 `gh auth login` 后才能用 CLI 读取或编辑 release；匿名 GitHub API 当前可能返回 `403`
- 后续建议：后续新版本必须先提交 `.changelogs/<tag>.md` 再打 tag；除非明确要求重新发布同一版本，否则不要再移动 `4.2.0` tag

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
