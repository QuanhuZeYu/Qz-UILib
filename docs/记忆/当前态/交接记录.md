# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-20 第 45 次）—— C 任务完成 + 批 1 Checkbox/Toggle 完成，批 2 待开工

### 一句话现状
Phase 4 控件层重建：**C 任务（transform 完整矩阵方案甲，还清偏离③）已完成提交，批 1（Checkbox/Toggle 受控双向迁移）已完成提交**。工作树对源码干净，下一会话可直接开工**批 2（Radio/Segmented/Breadcrumb）**。

### 当前分支与已提交基线
- 分支 `refactor/scene-phase4-control-layer`（从 `4.0` 切出）。已落地六提交：
  - `a33d80d8` 第 0 段地基 + `9fca7d38` 偏离 2 修复 + `5cb593fd` 批 1 前置②真实字体度量 + `237e8a33` 固定尺寸地基（preferredWidth + STRETCH 豁免）
  - `4721b6a5` **C 任务** transform 完整矩阵方案甲（还清偏离③）+ `a1c4841b` hash 回填
  - `89bb9e6d` **批 1** 受控双向控件 Checkbox/Toggle 迁移 + `9e6a24e7` 记忆文档同步
- 工作树对源码干净（仅未跟踪 `.slim/`、`opencode.json`，永不提交）。

### 本会话两大成果（均已主 Agent 独立验真，非仅信 fixer 报告）

**① C 任务 transform 完整矩阵（方案甲）**
- 接手 fix-3 编译断裂残局，新开 fix-1 修复并续完整 scope。落地：`Transform.java`（7 字段全 final 浮点）、`PaintCommandType.java`（PUSH/POP_TRANSFORM）、`PaintCommand.java`（7 个 transform primitive 字段 + 工厂）、`PaintPlan.java`（addPushTransform/addPopTransform）、`ScenePaintEngine.java`（删旧 D2 整数累加，改产边界命令最外层包 PUSH/POP_TRANSFORM）、`ScenePaintReplayer.java`（映射调 ctx 纯数值重载）、`UiRenderContext.java`（纯数值 pushTransform 重载，全 primitive，I6 让步已批准）。新建 `SceneTransformCompositeTest`（T1-T6）。
- 三命门已独立验真：transform 不进 fragment、setTransform 只走 markComposite、replayer 零 Transform import。还清偏离③两债（translate 仅整数量化 + 只 translate）。
- 本期约束：skew/L2 子树纹理缓存/FBO 留后续；非恒等 transform 节点本期不支持 clipChildren。

**② 批 1 Checkbox/Toggle 受控双向迁移**
- 新开 fix-2 实现。新建 `SceneCheckbox.java`、`SceneToggle.java`，确立契约 **R7 受控双向零内部状态**（`onChange.accept(!current.get())` 交还期望新值，控件绝不自缓存翻转）。`package-info` 红线 5→7 条补 R7。
- demo：`SceneControlsHostWidget` + `SceneControlsDemoScreen` + `/qzuilib test` VIS-SCENE-002 注册。
- 测试：`SceneCheckboxTest`(5) + `SceneToggleTest`(5)，受控命门测试到位（点击后 onChange 收 true + 外部 signal 仍 FALSE + 视觉未自翻转）。R1-R7 全守，命中穿透/四态零重排/键盘激活/disabled 阻断全覆盖。
- 本期约束：Checkbox 仅二态（indeterminate 排后续）；Toggle 滑块仅静态左右位置（transform 平滑动画排后续批，C 通路虽就绪本批不接）；Toggle on/off 切换走 LAYOUT 级（thumb 位置变重排，合理）。

### 下一步（批 2）
- **批 2：Radio/Segmented/Breadcrumb 迁移**（按 8 批路线图）。R7 受控双向范式已确立可照抄。
- Segmented 段式控件：每段独立交互单元，`package-info` R6 已预留边界「段本身 hitTestable=true，仅段内文字/图标 hitTestable=false 穿透到所属段」。
- 需 oracle 产批 2 迁移契约时**新开 session**（旧 ora session 上下文已用过，不复用）。

### 控件迁移可复用范本（批 2 照抄）
- 控件范本：`SceneButton.java`（试金石）/ `SceneCheckbox.java` / `SceneToggle.java`（受控双向）。
- 测试范本：`SceneButtonTest.java`（沙箱三件套 + mount + route + PaintPlan 断言）/ `SceneCheckboxTest.java`（受控双向命门）。
- demo 范本：`SceneControlsHostWidget` + `SceneControlsDemoScreen`，入口注册走 `UiTestMatrixRegistry`（组/用例 spec）+ `UiTestSampleVisualFactory`（按钮渲染 + openDemo 跳转）。
- API 表面：`SceneRuntime`（mount/bind/bindText/on/focusable/interactionState/requestFocus/show/forEach）；`SceneInteractionState`（hover/pressed/focused 三只读 signal）。

### 待用户验收（非阻塞，沙箱无 GUI）
- scene Phase 1-3 + C 任务 transform + 批 1 控件真机帧率/上屏验收待用户跑：入口 `/qzuilib test` → SCENE_DEMO 组 →「打开 Scene demo 页」/「打开 Scene 控件 demo」。真机后派子代理查 `run/client/logs/fml-client-latest.log`，`club.heiqi.uilib` 应零异常。

### 本会话纪律切换（用户指令）
- 本运行用户无法拍板，**决策点改派 @oracle 自动裁决并采纳**，不再用 question 等待用户响应。批 2 起需架构裁决时直接派 oracle。

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- 编译/测试优先走 JetBrains MCP（见 `AGENTS.md` 第 8 节，shell gradle 曾卡死）；仍需 shell 时用 `$env:GRADLE_USER_HOME="D:\.MyApps\.ENV\gradle-home"; ./gradlew.bat ...`（.MyApps 双 p）。**注意：jetbrainsBuildProject 因 gtnhconvention 插件配置阶段需联网拉 GitHub manifest，沙箱 DNS 污染时会失败；编译验证优先用 jetbrainsBuildProject（IDE 内置 classpath 不走该插件配置）。**
- PowerShell 不支持 `&&` 与 heredoc（`<<`）；链式用 `;` 或 `cmd1; if ($?) { cmd2 }`；提交信息必须先 write 到临时文件再 `git commit -F <文件>`。
- 提交/合并用 UTF8 无 BOM；PowerShell 5.1 `Set-Content -Encoding utf8` 会写 BOM，必须用 .NET `[System.IO.File]::WriteAllText($f,$c,(New-Object System.Text.UTF8Encoding $false))` 写无 BOM。
- git 操作必须如实读取 EXIT 码 + 真实 stdout，不预判、不编造命令成功输出。
- LSP 报 `java.lang.Record cannot be resolved` 是 IDE classpath 缓存问题，jetbrainsBuildProject 编译通过即可忽略。

## 更新规则
- 只保留最近一次交接的当前真相，历史交接直接覆盖，不按次数累积。
- 施工逐行细节沉淀到决策/审查/错误文档，本文件只留「现状 + 下一步 + 边界」。
