# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-23 第 82 次）—— SceneSelect 真机验收缺陷全修复，待复验

### 一句话现状
SceneSelect 首个消费者完成，真机验收暴露的三个缺陷（下拉不显示、下拉范围太大、resize 不更新）已全部修复并通过定向测试；下一步真机复验后进入字段引擎最小切片。

### 本次结论（按提交时序）
- **P0 地基四段**（`963257b2`，之前会话）：overlay 纯数据类 + anchor resolver + SceneRuntime.portal + SceneInputRouter 优先命中/dismiss + SceneHostWidget 多 root layout/paint/replay。
- **P0 两个缺口补强**（`238e220e`）：①portal 四参/portalAnchored 五参重载透传 dismissPolicy/dismissRequest ②AnchorProvider + SceneGeometry.absoluteBox + Entry 存 anchorX/Y + host layout/replay/hit-test 用 anchor 偏移；零回归根基（provider==null 逐位等价现状）。
- **SceneSelect 控件本体 + R11**（`dbdf1c87`）：trigger 常驻主树，listbox 经 portalAnchored 提升为 overlay root；选中/展开/关闭只写 signal；键盘导航 + listbox 限高滚动；R11 契约写入 control/package-info.java。
- **Demo**（`1f675ef4`）：SceneSelectHostWidget + SceneSelectDemoScreen（4 张卡片）+ Hub 入口 + VIS-SCENE-007 用例 + 测试。
- **记忆文档**（`ebaeaa3e`）：交接/计划/上下文同步。
- **真机缺陷1：下拉不显示**（`590cee56`）：SceneSelectHostWidget.render 仿 SceneFormHostWidget 缺 overlay 管线，补全 layoutOverlays + overlay paint/replay 循环。
- **host 层抽象债还清**（`aaf7c661`）：抽取 AbstractSceneHostWidget 基类统一 render + overlay 管线，8 个 demo 迁移，净减 929 行重复代码；所有 demo 自动支持 overlay。
- **真机缺陷2：下拉范围太大**（`86f69d81`）：listbox setFillParentHeight 吃满 anchor maxHeight；改为走 scrollable 分支3 shrink-to-fit 内容高，并补分支3 maxHeight cap（有约束时 min(内容高, 约束高)）。
- **I8 缺口**（`9d588cb5`）：selfConsumesConstraint 闸门只认 fill/scrollable 高度维度，不认 scrollable 分支3 节点消费约束高；扩展识别 + 约束变化重算测试 + NORTH_STAR 偏离登记更新。
- **真机缺陷3：resize 不更新**（`b444b883`）：I7 跳过闸门对无子叶节点宽度维度判定不全——selfConsumesConstraint 只看高度，childConstraintsWouldChange 对无子早退 false，宽度依赖约束宽的叶子（fill 装饰叶/clamp 文本叶）被跳过复用旧 box；方案 A′（oracle 裁决）按叶/容器分叉，叶节点补 widthChanged && preferredWidth<=0 判定，容器零改动；不破 I7 修复 I8。
- **宪章**：NORTH_STAR《偏离登记》新增 top-layer 多 paint root + 独立 hit-test 边界扩展（路径 A，待稳定后转正）；scrollable computeHeight 偏离登记更新分支3 cap 语义 + selfConsumesConstraint I8 修复。
- **验证**：JetBrains 定向编译通过；Gradle 全套 scene 测试 `--tests "*Scene*"` 多次 rerun 通过；`git diff --check` 通过。

### 下一步
1. **真机复验**（交用户跑）：`/qzuilib test` → Select demo，重点验证：①下拉列表正常显示且只包住选项（不延伸到底部）②调整窗口大小时内容立即跟随更新 ③anchor 跟随/外部点击/ESC 关闭/键盘导航/滚动。
2. **字段引擎最小切片**：scene-native 字段 spec、draft/current、校验、dirty、canSave/canCancel 派生与保存语义。
3. **接真实配置数据源**：优先接 `MutableConfig`，验证一期字段类型、分类、校验、保存、取消和恢复默认。
4. **暂缓**：搜索、树、多行文本、列表编辑、表格行内编辑、高级 picker、raw editor、远程同步、完整 CSS/token/theme 和 Phase 5。

### 已知边界
- 三个真机缺陷已修复但**未经真机复验**：下拉显示、listbox 高度、resize 跟随仍需 `runClient21` 人工确认。
- reviewer 5 项建议改进已处理 3 项关键（I8 缺口、listbox UX、测试覆盖），剩余 2 项非阻断（hit-test/replay anchor 时序窗口罕见、cachedLayout 强转守卫）。
- 旧对外现代配置页文档仍描述 DOM 实现的 12 模板能力；scene 一期完成前不得宣称能力等价。
- `NORTH_STAR.md` 已登记 top-layer 边界偏离 + scrollable 分支3 cap 语义扩展，待 overlay 能力稳定后转正。
- host 层已抽取 AbstractSceneHostWidget 基类，后续新建 demo 继承即可，不再可能漏 overlay 管线。

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
