# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-27 第 119 次）—— deepwork 并发框架阶段 1+1.5+阶段2前置完成

### 一句话现状

deepwork「研究重写并发框架」推进至阶段 2 前置全部完成。阶段 1 第一批
（探针 per-call + 契约边界）+ 阶段 1.5（epoch 外置）+ 阶段 2 前置
（measurer 加固三步）已落地，I6 缺口完全还清，measurer 调用路径全无锁。
下一步是阶段 2（子树并行 layout/paint），这是真正引入线程的阶段（风险最高），
按 deepwork 规则需起草计划交 oracle 审查。当前在 `add/b6-fbo-perf-baseline`
分支，8 个 commit 待真机验收 + 阶段 2 完成后决定合回 4.0。

### 本会话累计 commit（按时间序）

| commit | 内容 |
|--------|------|
| `74d98329` | [Add]: B6 FBO 性能基线实测页供真机 fillrate 拐点量化 |
| `736239ad` | [Fix]: B6 FBO 性能页 fps 不更新——绕开未驱动的 monitor 自建帧计时 |
| `57e0a40f` | [Refactor]: 帧统计组合式化——抽 FrameRateProbe + 基类默认采集 |
| `2fe32985` | [Add]: 并发框架阶段 1 第一批——探针 per-call + 契约边界固化 + 宪章三处补注 |
| `2ebc10e6` | [Refactor]: 并发框架阶段 1 第一批补迁移——彻底移除桥接 + 129 处测试断言迁移 |
| `479b6ade` | [Add]: 并发框架阶段 1.5——epoch 失效链外置（来源 A，经行业调研确认）|
| `32206403` | [Fix]: measurer 加固第一二步——volatile 发布 + 移除冗余原地清 |
| `ab868bc2` | [Fix]: measurer 加固第三步——拆 synchronized(fontService) 无锁化（I6 缺口完全还清）|

### 本会话任务链路

**阶段 A：B6 FBO 性能基线页 + 帧统计组合式化**
1. YAGNI 裁决：B6 FBO 纹理脏标记缓存属「性能暴露前」状态，转向性能基线实测
2. fixer 实现性能基线页（三档对照 + fps 采样）→ reviewer 审核通过
3. fps 不更新根因诊断（Oracle 确诊 McScreenBridge 不驱动 monitor）→ 自建帧计时修复
4. 帧统计组合式化：抽 FrameRateProbe + AbstractSceneHostWidget 默认采集

**阶段 B：deepwork 并发框架（用户提出 120tick 事件总线方向 → Oracle 两轮评估 → 认知翻转）**
5. 用户提出独立 120tick 事件总线线程分割数据态/渲染态
6. Oracle 第一轮：否决独立总线（reactive 内核单线程 + GL 主线程绑定 + 测试地基）
7. 用户拒绝"小型库不值得"论证，明确"喜欢并发框架，希望并发加速子树解析"
8. Oracle 第二轮：认知翻转——目标 A（多核加速）不依赖目标 C（reactive 并发化），
   正确落地形态是 NORTH_STAR 第 10 节预留的 Display List 契约线
9. 用户全盘采纳 Oracle 推荐：双缓冲推迟 + epoch 剥离阶段 1.5 + 三处宪章补注

**阶段 C：deepwork 阶段 1+1.5+阶段2前置 实施**
10. 阶段 1 第一批：探针 per-call 化 + 契约边界固化（fixer→reviewer→补迁移→reviewer）
11. 阶段 1.5：epoch 外置（explorer 侦察 P0 风险 → oracle 裁决方向 6 来源 A →
    librarian 行业调研确认 Blink/WebKit 双支柱同构 → oracle 最终推荐 →
    fixer 实施 P0 命门守住 → reviewer 通过）
12. 阶段 2 前置：measurer 加固三步（explorer 侦察 → fixer 三步 →
    reviewer 每步审核 → I6 缺口完全还清）

### 关键架构决策（详见决策文档）

- **DECISION-20260627-display-list-contract-line.md**：Display List 契约线
  阶段 1 落地 + 并发框架方向。含完整阶段路径、行业调研背书、P0 命门分析
- **NORTH_STAR 三处补注**：信条六（两阶段已落地）+ I6（并行强化 + measurer
  缺口已还清）+ 第 10 节新增 10.1（已落地的契约线切分，双缓冲仍标预留）
- **来源 A vs 来源 B 裁决**：epoch 外置用来源 A（保留 measuredTextNodes 集合
  + epoch 下放节点），来源 B（侵入式链表）业界无先例否决

### deepwork 进度文件
`.slim/deepwork/concurrent-framework.md`：持久进度文件，记录目标、研究发现、
计划、oracle 审查意见、阶段状态、验证结果、下一步选项。**下个会话必读**。

### 验证状态

- compileJava + compileTestJava 通过（多次构建）
- 222 类 2215 测试全绿（阶段 1 第一批补迁移后 reviewer 独立验证）
- 阶段 1.5 P0 命门测试通过（epochChangeBubblesThroughCleanMiddle）
- measurer 加固三步测试通过（FontService/TextLayoutService/SceneLayoutEngineTest）
- **真机帧率实测未跑**（沙箱无 GUI）

### 剩余债务全量状态

**真未还（代码债）**：
- B6 FBO I7 合成阶段偏离：FBO 纹理每帧重栅格化，待真机性能基线数据决定是否启动
- B6 hit-test 对偶偏离：SceneHitTester 对 transform 零感知，待交互需求触发
- UiPerformanceMonitor 单例 scope 错配（远期，采集深埋 Widget.java 硬编码 getInstance）
- A1 effect 内 set 慢一帧：残留语义待确认
- B4 COLUMN fill O(n²)：缓做
- chrome 主题层：P2 大工程未立项
- Phase 5 旧栈退役：阻塞中

**有意边界（非债）**：
- B6 FBO 全屏尺寸：批 3 局部 FBO 优化待性能暴露后启动
- B6 降级路径：FBO 不可用时保留 clip 放弃 transform（best-effort）
- 圆角 clip+transform：stencil+T 交互未真机验证
- SceneTextArea 功能限制 / DataTable 嵌入式深色槽

**真机验收待办（非 B6）**：
- B8 滚动后 hover 切换 / B2 TextArea 压力测试页帧率 / Breadcrumb ROOT_GAP
- Phase 1-3 真机帧率/上屏 / 改名后真机 GL 路径
- B6 FBO 性能基线页三档对照真机跑 + fps 拐点记录

### 当前分支与工作区

- 当前工作分支：`add/b6-fbo-perf-baseline`，8 个 commit
- 工作区干净，会话结束

### 下个会话任务表

1. **deepwork 阶段 2：子树并行 layout/paint**（首要任务）
   - 这是真正引入线程的阶段（风险最高），按 deepwork 规则需起草计划交 oracle 审查
   - ForkJoinPool 分治递归 layout/paint，兑现多核加速
   - 阶段 2 启动前配套：补幂等断言测试 + measuredTextNodes 换并发集合
   - GL 主线程绑定是硬墙：worker 生成 Display List，主线程 replay 上屏
   - 小树负优化风险：<100 节点 fork/join 开销可能 > 收益，必须设规模阈值
2. **B6 FBO 性能基线真机实测**：用户跑 `/qzuilib scene_test` → "FBO Perf Baseline"
3. **B6 hit-test 对偶**：待真实 rotate 交互需求触发
4. **A1 残留确认**：核 ReactiveScheduler.flush 收敛终止条件
5. **可选**：圆角 clip+transform 补充 demo 卡片真机验证
6. **可选**：NORTH_STAR 偏离登记 B6 主动禁止措辞修正

### 已知边界

- deepwork 进度文件：`.slim/deepwork/concurrent-framework.md`（必读）
- 决策文档：`docs/记忆/决策/DECISION-20260627-display-list-contract-line.md`
- NORTH_STAR 三处补注：信条六/I6/第 10 节 10.1
- FBO 性能基线页：`/qzuilib scene_test` → "FBO Perf Baseline"
- FrameRateProbe：`ui/diagnostic/FrameRateProbe.java`，组合式帧率探针
- measurer 调用路径全无锁：DCL 快速返回 → final 读 → volatile 表读
- 阶段 2 配套待做：幂等断言 + measuredTextNodes 换 ConcurrentHashMap.newKeySet()
- SceneNode equals 需核对引用相等才能直换并发集合
- widthCache 无锁并发写的幂等性假设未经并发压测验证

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑，避免并行 Gradle 竞争 build/ 目录。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，
  改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能
  登记到正确 Owner。
- token 实际值以 SceneChromeTokens.java 源码为准，不依赖过期文档清单。
- oracle 裁决架构方案后，如方案涉及长期可引用的设计决策，建议补写
  `docs/记忆/决策/` 文档沉淀。
- I11 逃生舱②扩张纪律：扩之前证明它确实是一类新的权威交互真值，
  而非可以用现有机制表达的东西（B8 验证了 hover 重算不需要扩②）。