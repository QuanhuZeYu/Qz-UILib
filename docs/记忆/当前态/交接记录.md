# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；本批分支 `add/dirty-subtree-inline-reuse` 用于 HTML-like 脏子树 inline-block 子树复用，完成后按项目规则合并回 `4.0`
- 已完成：`DocumentLayoutEngine` 将静态 `display:inline-block` 盒纳入既有脏子树复用白名单；仍沿用版本、文本测量 epoch、containing 宽高和 forced size 判断，并继续排除 absolute/fixed 后代、transform fixed containing block 与 layout 动画运行态
- 已完成：新增 `HtmlLikeDocumentWidgetInlineLayoutCacheTest`，覆盖 inline formatting 中 inline-block 换行平移、落位/baseline 后续文本、文本测量 epoch 失效和脱流后代边界；避免继续向 5896 行的 `HtmlLikeDocumentWidgetTest` 堆叠
- 后续边界：display:inline fragment/text run 级 inline formatting 仍未复用；若继续扩展，先补多行 inline fragment、嵌套 inline、text-indent/text-align、文本测量 epoch 与脱流边界测试
- 验证：已通过 `HtmlLikeDocumentWidgetInlineLayoutCacheTest`、`HtmlLikeDocumentWidgetTest`、`DocumentPaintEngineTest`、`UiDocumentTest`、`DocumentEventPropagationTest`、`UiStyleResolverTest`、`compileJava` 与 `git diff --check`；本批未跑完整 `test` 或 `runClient21`
- 注意：同一工作区内 Gradle 命令必须串行，禁止并行跑 `compileJava` / `test`

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
