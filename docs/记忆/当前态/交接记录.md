# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次新增项目级 OpenCode Anthropic 配置：`.opencode/opencode.json` 保留 CodeGraph MCP，新增 `model: "anthropic/claude-opus-4-8"` 与 `provider.anthropic`，只声明 `claude-opus-4-7`、`claude-opus-4-8`；`apiKey` 使用 `{env:ANTHROPIC_API_KEY}`，`baseURL` 预留为官方 `https://api.anthropic.com/v1`，如接 NewAPI Anthropic 协议只替换该端点。
- 已确认不要使用 `openai` provider 配 Claude 4.7/4.8 变体：OpenCode 内置 `openai` 会走 Responses API；NewAPI 的 Claude effort 后缀处理在 ChatCompletions 转 Claude 路径，二者不匹配。本次不写模型变体。
- 当前分支：`refactor/decouple-lwjgl3ify`；已完成提交：`e389d584`、`cf1bf055`、`b803f0b6`、`d502864a`、`323b7fbb`、`7ba6f3d3`。
- 已完成输入后端解耦：`UiInputService` facade 不变，`InputEvents` 继续反射订阅；新增 `UiKeyCodes` 后，业务、控件、remote、config、devtools 和测试侧不再为键码常量 import `org.lwjglx.input.Keyboard`。
- 新增 `LwjglInputRuntime`：`LwjglxPollingInputBackend` 与 `UiHostInputCoordinator` 通过包内反射桥访问键鼠运行时，优先 `org.lwjglx`，缺失时降级 legacy `org.lwjgl`，不新增 legacy LWJGL 显式运行依赖。
- `Lwjgl3ifyInputBackend` 已移除 `org.lwjglx.Sys`；`UiHudDocumentHost` 改走 `UiHostInputCoordinator.isNativeMouseGrabbed()`；`SystemDocumentCursorHost` 已移除 `Display` 静态 import，SDL 系统光标仍只在 lwjgl3ify / LWJGLX 光标桥可用时启用，否则 no-op。
- **已完成严格代码审查**，未发现阻断性问题。审查范围覆盖 4 个提交 11 个重点文件，确认主源码零静态依赖 `lwjgl3ify`/`lwjglx`，发布依赖正确降级为 `devOnlyNonPublishable`。
- **已创建两批次修复计划**：`docs/开发者文档/reviews/REVIEW-20260613-lwjgl3ify-decouple-plan.md`。批次 1（核心修复 M1-M4）待执行，批次 2（键码补全、运行时 API）待后续处理。执行指南见 `docs/开发者文档/reviews/AGENT-PROMPT-lwjgl3ify-decouple-fix.md`。
- **批次 1 执行 Agent 启动提示词**（复制给下一个 Agent）：
  ```请执行 refactor/decouple-lwjgl3ify 分支的代码质量优化修复（批次 1）。读取 docs/开发者文档/reviews/REVIEW-20260613-lwjgl3ify-decouple-plan.md 和 docs/开发者文档/reviews/AGENT-PROMPT-lwjgl3ify-decouple-fix.md，按计划完成任务 1.1 到 1.6。```
- 暂缓项：主渲染链仍是 `org.lwjgl.opengl.*` / `org.lwjgl.BufferUtils`，OpenGL、字体 atlas 和渲染依赖不要与本轮 `lwjglx` / `lwjgl3ify` 输入可选化混做。

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
