# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前分支：`docs/remote-ui-runtime-lease-decision`；本次只记录远程 UI 网络/会话层生产级重构研究，不改生产代码
- 新增决策：`docs/记忆/决策/DECISION-20260609-remote-ui-runtime-lease-protocol.md`
- 后续生产代码方向：新增内部 Remote UI Runtime + 显式 Lease 协议，区分 `sessionId`、`surfaceType/surfaceId`、`contentRevision`、`assetId`、`closeScope`；`NetService` 继续只做通用网络基础设施
- 默认行为约束：普通远程页面/HUD 仍固定 10 分钟 TTL、不隐式续期；keepalive / renew / resume 只能作为显式 opt-in 能力设计
- 下一位 Agent 如进入重构，应先做等价内部骨架和测试：`RemoteUiProtocol`、`RemoteUiAssetStore`、`RemoteUiSessionManager`、`RemoteUiServerRuntime`、`RemoteUiClientRuntime`，再逐步收敛 `RemoteDocumentPages` / `RemoteHudOverlays` 为薄 facade

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
