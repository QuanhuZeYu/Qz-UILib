# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-26 第 110 次）—— 技术债批3+4清理合回 4.0

### 一句话现状

控件审核 P0+P1 批次 + 技术债批3+4 全部合回 4.0。P1 全清，剩余 P2 chrome 收口第2-4批（需视觉决策）+ Props Builder P1 回填。

### 本会话关键 commit（已合回 4.0）

| commit | 内容 |
|--------|------|
| `36d8d1ef` | [Fix]: 批3（行高收口 + primitive 独立测试 + 文档/Owner 断言 + anchor bug 修复） |
| `acfd8daa` | [Fix]: 批4（DataTable O(n) + 行内编辑器 enabled + Props Builder P0 + KeyValueMap chrome 收口） |
| merge | [Merge]: 技术债批3+4清理 |

### 批3关键结论

- **anchor bug 修复**：forEach+show 共享 content 容器时 applyChildReconcile 误删 show 的 anchor → 引入独立 placeholderContainer
- **3 primitive 独立测试**：SingleSelect/Select/Slider primitive 各 10 测试，覆盖 NOOP_CHROME 路径
- **Owner 回归断言**：新增 ReactiveTestProbe 测试探针 + 3 个回归测试
- **行高收口**：ROW_HEIGHT_TABLE=28 + INPUT_HEIGHT=30 两个 token

### 批4关键结论

- **DataTable O(n)**：RowIndexCache HashMap 查表，O(n²*m)→O(n)+O(n*m)
- **行内编辑器 enabled/readOnly**：四个 Props 加可选 enabled+readOnly signal，9处写死替换
- **Props Builder P0**：Slider/TextInput/TextArea/DataTable 新增静态 Builder（oracle 裁决方案A）
- **KeyValueMap chrome 收口**：11 常量改引 token，buttonBackground 删改走 standardBackground

### 验证状态

- jetbrainsBuildProject 全量编译通过
- 全量 scene control 测试 198 项全绿
- **待真机验收**：disabled 焦点 + Breadcrumb focus + TextArea 文本三态色 + 行内编辑器 enabled + placeholder 修复

### 当前分支

- `4.0`：主分支，批3+4 已合入，工作区干净
- `fix/p1-p2-batch3-tech-debt`：已合并，可清理

### 下个会话任务表

1. **chrome 收口第2-4批**：ObjectField（低风险）→ SimpleList（需视觉决策暖灰→冷灰）→ DataTable（最高风险，22常量+8方法）
2. **Props Builder P1**：SimpleList/ObjectField/KeyValueMap class→record 回填 + 静态 Builder
3. **真机验收**：上述待验项目
4. **嵌套分类树导航** / **字段引擎最小切片**（待用户拍板）

### 已知边界

- chrome 收口第3批 SimpleList 暖灰→冷灰是可见变化，需 @designer/用户确认
- chrome 收口第4批 DataTable 有 3 处测试硬编码断言需同步改 + ITEM_BG_SELECTED 色值变化需决策
- Props Builder P1 回填需先读对应 test 的非法入参断言
- RowIndexCache 失效依赖 signal 写入约定（总是 set 新列表实例）

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能登记到正确 Owner。
