# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-23 第 75 次）—— Scene Layout demo 已完成并真机验收

### 一句话现状
Scene Layout demo 已完成：`/qzuilib test` 新栈 hub 已新增 Layout demo 入口，独立 `SceneLayoutHostWidget` 用固定标题条 + 唯一 `fillParentHeight` 滚动视口展示 P0/P1-a 排版能力；已通过审核、定向编译、测试和用户真机验收，待提交保存进度。

### 本次结论
- **默认入口已切换**：`/qzuilib test` 打开 `SceneTestHubScreen` + `SceneTestHubHostWidget` 新栈 test 首页。
- **保留过渡别名**：`/qzuilib scene_test` 继续打开同一新首页，减少已验收入口迁移摩擦。
- **保留 legacy 入口**：`/qzuilib legacy_test` 打开旧反射诊断菜单，仅供旧 HTML-like 视觉矩阵参考回归。
- **已挂接 5 个 demo**：Scene demo、Controls demo、Scroll demo、Table demo、Layout demo 仍作为独立 screen 打开，未内嵌、未大改原 demo。
- **约束已守住**：未依赖 `ui.dom` / `HtmlLikeDocumentWidget` / `BaseScreen` / 旧 page authoring API；导航按钮 handler 只写 `requestedDestinationSignal`，平台打开 screen 动作留在 `SceneTestHubScreen`。
- **oracle 已复核**：并行入口方向保留，scaleFactor 命中偏移不阻断并行入口；规划性裁决后续必须先派 oracle。
- **已修切屏约定**：`openRequestedDemo` 不在 `drawScreen` 内直接 `displayGuiScreen`，已改为 `UiScreenManager.enqueue` 帧外切屏。
- **已验证并提交**：JetBrains 定向编译通过，`git diff --check` 通过，git agent 已提交 `b482ba61`，提交后工作区干净。
- **真机已通过**：用户确认 `scene_test` 首页、4 个 demo、ESC 返回链和 GUI Scale != 1 命中均无问题。
- **POINTER 已复验通过**：用户确认进入 `SceneTable` 页后手型残留问题不再出现。
- **排版方向已拍板**：颜色/外观属性直接写可接受，token/theme 后置；真实配置页前优先补 scene layout 排版能力，不移植完整 CSS selector/cascade/stylesheet。
- **P0 已完成**：新增 `SceneNode.WidthSizing { FILL, SHRINK }`；`SceneLayoutEngine` 在子布局完成后回收容器内容宽；`SceneBreadcrumb` 删除 `APPROX_CHAR_WIDTH` 估算，segBtn 改用 `WidthSizing.SHRINK`。
- **P0 已审核验证**：实现后经全新 oracle session 审核，未发现阻断问题；已补 Javadoc 与 `SHRINK` 子容器 STRETCH 豁免测试；`jetbrains_build_project` 定向编译、Gradle `SceneLayoutEngineTest` + `SceneBreadcrumbTest`、`git diff --check` 均通过。
- **协作规范已更新**：`AGENTS.md` 写入 Oracle 除中断恢复外默认新开 session、不得复用超过 50K 上下文；实现后必须经独立子代理审核，代码实现审核默认交给 @reviewer。
- **P1-a 已完成**：`SceneLayoutEngine.layoutInternal` 改为按子节点构造约束；COLUMN 命中唯一 fill 子时下传 `priorKnownInnerHeight - 固定兄弟先验高 - gap`，父侧不直接改写子盒高。
- **回退边界已固化**：多个 COLUMN fill 子仍不做权重分配；无高度约束、固定兄弟为容器等不可先验场景继续 shrink-to-fit。
- **约束订阅已修正**：`childConstraintsWouldChange` 逐子比较实际下传约束，避免 COLUMN 剩余高变化漏重算唯一 fill 子。
- **文档已同步**：`SceneNode.setFillParentHeight` Javadoc 与 `NORTH_STAR.md` 偏离登记已更新为“单 fill 已部分还清，多 fill/flex-grow 仍为剩余债”。
- **已审核验证**：@reviewer 审核未发现阻断问题；已补 scrollable+fill 视口高度与 preferredHeight 优先测试。JetBrains 定向编译通过；Gradle 定向测试 `SceneLayoutEngineTest` 通过；`git diff --check` 通过。
- **Layout demo 已完成**：新增 `SceneLayoutHostWidget` + `SceneLayoutDemoScreen`，内容包括 `FILL` vs `SHRINK`、ROW/COLUMN、padding/gap、preferredWidth、SceneBreadcrumb 真实宽度、fillParentHeight 视口结构六张卡片。
- **Layout demo 接线完成**：`SceneTestHubHostWidget` / `SceneTestHubScreen` 已新增 Layout demo 按钮和分派；旧 visual matrix 登记 `VIS-SCENE-005`，stage 仅作为跳转预览。
- **测试硬计数已清理**：按用户要求删除 `UiTestDocumentPageControllerTest` 与 `UiTestRuntimeHostVisualMatrixTest` 中 registry/state 总数硬编码断言，不再维护 `59->60` 这类脆弱数字；保留入口、分组翻页和稳定文案断言。
- **审核发现并修复 H1**：独立审核指出标题条若是无固定高容器，P1-a 无法先验固定兄弟高度，viewport 会退回内容高且不可滚动；已给 titleBar 设置固定 `preferredHeight`，并把示意卡改成固定高度色条，避免嵌套 fill 误导。
- **Layout demo 验证**：新增 `SceneLayoutDemoTest` 覆盖 viewport 剩余高、content 溢出和滚轮 signal-first 链路；`UiTestDocumentPageControllerTest` 覆盖 `VIS-SCENE-005` 翻页入口；@reviewer 复审未发现阻断问题。`jetbrains_build_project` 定向编译通过；`git diff --check` 通过；Gradle 定向测试 `SceneLayoutDemoTest`、`UiTestDocumentPageControllerTest`、`UiTestRuntimeHostVisualMatrixTest` 通过。
- **Layout demo 真机通过**：用户确认 `/qzuilib test` → `Layout demo` 页面视觉、滚轮滚动、Breadcrumb 交互与 ESC 返回链未发现问题。

### 已确认入口链
- 当前 `/qzuilib test`：`QzUiLibClientCommand.openSceneTestHub` → `SceneTestHubScreen.openHub` → `SceneTestHubHostWidget`，首页按钮写 signal 后由 `SceneTestHubScreen.openRequestedDemo` 经 `UiScreenManager.enqueue` 打开既有 demo screen。
- 过渡 `/qzuilib scene_test`：同样进入 `QzUiLibClientCommand.openSceneTestHub`。
- legacy `/qzuilib legacy_test`：`QzUiLibClientCommand.openDiagnosticsMenu` → `DevToolsScreenLauncher.createDiagnosticsMenu` → `UiDiagnosticsScreens.createUiTest` → `InternalHostedScreenFactory.createScreen(UI_TEST_DEFINITION)` → `UiTestDocumentPageController` → `UiDocument + HtmlLikeDocumentWidget`。

### 下一步
1. 按 AGENTS.md 提交本轮改动；提交标题建议 `[Add]: 新增 scene layout 排版演示页`。
2. 提交后回到真实配置页候选筛选。
3. 若继续 P1，后续再评估真正的多 fill/flex-grow 分配语义和子项级 `align-self`，不要把 P1-a 特例扩成隐式权重系统。

### 文档写回
- `docs/记忆/当前态/当前上下文.md`：记录 scene 排版地基 P0/P1-a 已完成并通过审核/验证。
- `docs/记忆/当前态/当前计划.md`：短期计划已切换为 Layout demo 真机验收与真实配置页候选筛选。
- `docs/记忆/决策/DECISION-20260622-scene-layout-intrinsic-width-first.md`：记录排版地基优先于 token/theme、P0 `WidthSizing.SHRINK` 的取舍。
- `docs/记忆/长期事实/架构边界.md`：补 scene 排版边界与 `WidthSizing` 语义。
- `AGENTS.md`：补 Oracle 新 session 与实现后独立审核纪律，并明确代码实现审核默认使用 @reviewer。
- `docs/记忆/决策/DECISION-20260622-qzui-test-scene-hub.md`：记录默认入口已切换、`legacy_test` 保留旧链供参考回归。
- `NORTH_STAR.md`：更新 2026-06-20 COLUMN 主轴 fill 偏离登记，标明 P1-a 单 fill 剩余高已部分还清。
- `docs/记忆/当前态/交接记录.md`：本交接已更新为 P1-a 当前真相。
- `docs/记忆/当前态/交接记录.md`：本交接已更新为 Layout demo 已完成、待真机验收当前真相。
- `docs/记忆/长期事实/架构边界.md` 与 `docs/记忆/决策/DECISION-20260622-scene-layout-intrinsic-width-first.md`：补 P1-a 语义和剩余债边界。

### 已知边界
- 本次只新增 Layout demo 与 `VIS-SCENE-005` 预览入口，不迁旧视觉矩阵、不迁旧断言 runner、不做 Table 新能力、不启动 Phase 5。
- `scene_test` 真机 GUI 验收已通过；默认 `/qzuilib test` 复用同一新 hub，仍建议用户真机快速确认命令入口本身。
- 旧栈暂不删除，避免一次性破坏旧回归资料；但后续新入口、新功能、实际业务接入不得再依赖旧栈。
- 完整 CSS selector/cascade/stylesheet 引擎暂不迁入 scene；token/theme 后置，Layout demo 仅展示已落地 P0/P1-a 能力。
- P1-a 不新增属性，不实现完整 flex-grow、多权重分配、align-self、min/max、percent、margin、横向滚动。

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
