# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；当前工作分支：`optimize/dirty-subtree-layout`，尚未合并回 `4.0`
- 已完成：HTML-like 脏子树布局缓存第一阶段，`DocumentNode` 记录节点自身/子树 layout 变更版本，结构移动会标记新旧父级和移动子树；元素样式/class/属性变更按子树脏处理，文档级样式表/变量/top-layer 变化按全局脏处理
- 已完成：`DocumentLayoutBox` 保存布局输入参数、节点版本和 textMeasure epoch，`DocumentLayoutEngine` 静态布局 pass 可复用上一轮未脏 block 子树；`HtmlLikeDocumentWidget` 仅静态重排传入上一轮 `cachedLayoutBox`，layout 动画运行态不复用
- 当前边界：第一阶段只覆盖静态 `display:block` / `display:none` 且不含 absolute/fixed 盒的子树；flex/table/inline、可平移复用、TextNode 级换行缓存仍是后续任务，详见 `docs/记忆/决策/DECISION-20260606-dirty-subtree-layout-cache.md`
- 验证：已通过 `HtmlLikeDocumentWidgetTest`、`DocumentPaintEngineTest`、`UiDocumentTest`、`DocumentEventPropagationTest`、`UiStyleResolverTest`、`compileJava` 与 `git diff --check`；本批未跑 `runClient21`
- 注意：本轮曾误并行执行 Gradle 测试与编译，触发 `ChildProcess.kill`，顺序复跑已通过；后续同一工作区内 Gradle 命令必须串行

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
