# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；本轮在 `fix/codegraph-mcp-startup` 分支修复 CodeGraph MCP 启动失败
- 根因：Windows 下 opencode 非 shell 启动不能直接执行 `npx` / `codegraph-mcp` 的 `.cmd` shim；同时 `@astudioplus/codegraph-mcp` npm 启动器会自行进入 MCP 模式，配置里再传 `--mcp` 会变成重复参数
- 工具状态：项目 `.opencode/opencode.json` 已改为 `cmd.exe /d /s /c "npx -y @astudioplus/codegraph-mcp --graph-only ..."`；个人全局 opencode 配置也已同步改为 `cmd.exe /d /s /c "codegraph-mcp --graph-only ..."`
- 验证状态：已用换行 JSON-RPC 协议完成 CodeGraph MCP `initialize` + `tools/list` 握手，返回 42 个工具，首批工具包含 `codegraph_get_dependency_graph`、`codegraph_get_call_graph`、`codegraph_analyze_impact`
- 后续使用口径：具体类、方法、调用链、依赖图和影响面优先用 `codegraph` MCP 缩小范围，再用源码读取、定向测试和 `compileJava` 确认；不要把一次性 CodeGraph 查询输出写成静态索引
- 继续开发建议：`HtmlLikeDocumentWidgetTest` 后续仍可按主题拆 `EventDispatch`；每批只做测试组织搬家，不改生产代码语义

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
