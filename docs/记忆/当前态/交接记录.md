# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；最近实现分支：`add/test-visual-core-samples`
- 已完成：`/qzuilib test` 已接入首批 9 张 CSS / Layout / Paint 核心视觉样例：`VIS-CSS-001..003`、`VIS-LAYOUT-001..003`、`VIS-PAINT-001..003`
- 当前结构：控制器仍只负责生命周期/导航/环境刷新；registry 登记样例；`UiTestGroupVisualBuilder` 渲染统一卡片结构；`UiTestSampleVisualFactory` 负责具体视觉舞台；checker/state 仍保持视觉状态、语义状态和汇总状态分离
- 当前矩阵：首轮计划 59 张，已接入 9 张，剩余缺口 50 张；旧卡片矩阵和旧编号不恢复
- 验证：已通过目标 `UiTestDocumentPageControllerTest`、`compileJava` 与 `git diff --check`；本批未跑 `runClient21`
- 下一步：为这 9 张样例补 computed style、布局盒和绘制/clip/transform 自动语义检查及结果回写，再继续补 CSS / Layout / Paint 剩余样例
- 文档流程补充：审查误报或结论失效时，必须在原始审查正文添加 `后续复核（YYYY-MM-DD）` 批注，并同步更新 reviews 索引，避免后续 Agent 只读原报告重复审核

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
