# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；当前工作分支：`4.0`；最近实现分支：`fix/remove-test-runtime-throttle`（已 --no-ff 合并回 4.0）
- 已完成：HTML-like 文本绘制阶段新增保守裁剪，`DocumentPaintEngine` 将 active clip chain 传给文本命令生成，`DocumentTextPaintClipper` 跳过 overflow clip 外文本行，并对可安全判断的横向超长单行生成可见片段
- 已完成：`HtmlLikeDocumentWidget` 调用带 `TextMeasureService` 的 paint command 构建入口；为避免破坏 paint-only 缓存，文本测量只在存在 clip 且长文本可能需要横向切片时触发
- 已完成：已撤销 `/qzuilib test` 环境 frame/render 统计节流，测试页继续每帧把最新运行时统计写入普通 DOM 文本，用于暴露无使用方刻意优化下的框架表现
- 验证：已通过 `DocumentPaintEngineTest`、`HtmlLikeDocumentWidgetTest`、`UiTestDocumentPageControllerTest`、`compileJava` 与 `git diff --check`；本批未跑 `runClient21`
- 下一步：长文本剩余中长期方向是 TextNode 级换行缓存或 VirtualTextBlock / LogView；测试页重建计划仍可继续推进 Controls 首轮视觉样例

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
