# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-25 第 104 次）—— performLayout 步骤顺序重构补债完成

### 一句话现状

performLayout 五步骤重构为 A/B/C/D 四步骤（容器尺寸先定再定位子），
消除 ROW 交叉轴居中 bug 的结构性温床。已合回 4.0，全量 scene 测试零回归，
reviewer 通过。文本垂直对齐双修复 + 布局 bug 修复 + 步骤顺序重构全部落地。

### 本会话关键 commit

| commit | 内容 |
|--------|------|
| `819647cf` | [Refactor] 文本垂直对齐改用 em-box 居中模型，修复按钮文字偏上 |
| `d66fef49` | [Docs] 交接第102次——文本垂直对齐改用 em-box 居中模型 |
| `dc5b68d0` | [Fix] 测试主页导航按钮去掉固定行高，改为文本自适应高度 |
| `f9558ec9` | [Docs] 交接记录与当前态更新——文本垂直对齐真机验收通过 |
| `3fe1a1b7` | [Fix] ROW 交叉轴居中改用容器内高基准，修复固定高容器垂直对齐失效 |
| `697a8f3d` | [Test] 补嵌套ROW基准不串味与fill+CENTER回归测试，升级复用注释 |
| `5cf74837` | [Docs] 登记performLayout步骤顺序重构技术债 + 决策索引更新 |
| `4fbe69fe` | [Merge] 文本垂直对齐双修复 + ROW交叉轴居中布局bug修复（合回4.0） |
| `54974ec7` | [Refactor] performLayout步骤顺序重构为A/B/C/D四步骤 |

### 根因与修复要点

- **真根因（布局 bug，非 ink）**：`SceneLayoutEngine.java:530` ROW flex 交叉轴 crossAvail
  取 `crossMax`（子节点最大高），应取容器自身内高。固定 preferredHeight 只撑高 root 盒高，
  但子节点 CENTER 用 crossMax 定位，多出空间全沉盒底，子节点贴顶偏上 6px。
  oracle 复核确认系统性 bug，影响所有固定高 ROW + CENTER/END 场景。
- **layout 修复**（commit `3fe1a1b7`）：步骤 4 前用纯读函数 computeHeight 算 root 最终高度，
  ROW 的 crossAvail 改为 `max(0, rootFinalHeight - padV)`。步骤 5 复用变量。
  安全性：computeHeight 纯读无副作用，子节点后序递归已布局完，无循环依赖。
- **paint 修复**（commit `819647cf`，前一轮）：calculateTextTop 改 em-box 居中，
  与字体渲染器 em-box 顶锚点一致。这是另一层坐标系错配修复，与 layout bug 互补。
- **hub 按钮恢复固定 40px**：作为真机回归验证场景（layout 修复后应真正居中）。

### 验证状态

- JetBrains 编译通过，gradle test SceneLayoutEngineTest + club.heiqi.uilib.ui.scene.* 全绿
- 5 个回归测试：固定高 CENTER/END/STRETCH + 自适应回归 + 多子 CENTER
- reviewer 有条件通过（无阻断，S1 STRETCH 测试已补）
- **待真机最终验收**：layout + paint 双修复协同后，固定 40px 按钮文字应真正居中

### 下个会话任务表

1. **真机回归**：用户跑 runClient21 确认步骤顺序重构后布局零回归（数值单测已绿，真机建议过一遍）
2. **恢复主线**：TextArea / 嵌套分类树导航 / 字段引擎最小切片

### 当前分支

- `fix/text-vertical-alignment`：含 em-box 居中修复 commit `819647cf`，工作区干净
- `4.0`：主分支

### 已知边界

- **多行文本**：layout 层按 `countLines(\n)*lineHeight` 多行撑高，paint 层 calculateTextTop
  是单行模型（单个 emHeight=fontSize），paint 只发一条带原始 `\n` 的 TEXT 命令。
  既有不一致，非本次引入，多行垂直对齐属独立后续工作。
- **padding 失效**：`setPadding` 只调 `markSelfLayout` 不调 `markSelfPaint`，
  固定尺寸节点动态改 padding 时 textTop 不刷新。既有问题，非本次引入。
- **scene 核心 main 侧 SceneTextMeasurer.ascent/descent/lineGap 已无消费者**
  （lineHeight 仍用于 layout 多行高度），接口瘦身属独立架构决策。
- **TOP 测试用例**因 `FixedTextMeasurer` 的 `halfLeading=0` 恰不分新旧模型，
  区分能力靠 CENTER/BOTTOM（已知盲区，可接受）。

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能登记到正确 Owner。
