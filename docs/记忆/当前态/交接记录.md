# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；`HtmlLikeDocumentWidgetTest` 第四批 LayoutCache 拆分已完成并合并回主分支
- 已落地：同包 `HtmlLikeDocumentWidgetTestSupport` 承载共享 helper/fixture；`HtmlLikeDocumentWidgetDragTest` 承载拖拽主题测试；`HtmlLikeDocumentWidgetScrollTest` 承载滚动主题测试；`HtmlLikeDocumentWidgetFocusKeyboardTest` 承载焦点与键盘主题测试；`HtmlLikeDocumentWidgetLayoutCacheTest` 承载静态布局缓存主题测试
- 已迁移 LayoutCache 范围：静态布局缓存复用、block/flex/table 子树平移复用、脱流后代保守重排，以及 DOM 移动、继承样式、样式表、`textMeasure` epoch 布局缓存失效，共 23 个测试；测试内容和断言语义保持不变
- 本轮验证已通过：`git diff --check`、`HtmlLikeDocumentWidgetTest`、`HtmlLikeDocumentWidgetLayoutCacheTest`、`compileJava`
- 后续建议：继续按主题小批量拆 `AnimationRuntime`、`EventDispatch`，每批只做测试组织搬家，不改生产代码语义
- 背景状态：Controls 视觉样例已接入并修复 `VIS-CTRL-005` table 同步；`/qzuilib test` TextFont 下一批可在测试拆分阶段性收口后恢复

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
