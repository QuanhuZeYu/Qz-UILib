# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-25 第 107 次）—— scene 架构审核 + B1/B3 温床修复

### 一句话现状

oracle 对 scene 新栈做架构审核，产出 8 API 陷阱 + 10 BUG 温床 + 7 不变量核对清单。
用户拍板修 P0 两项：B1（3 primitive 绝对坐标统一 `SceneGeometry.absoluteBox`）+
B3/I7（`isClipWindow` 谓词统一 paint/hit-test clip 口径）。两 fixer 并行修复，
主 Agent 自审（reviewer 子代理两次空返回），569 scene 测试全绿，已 commit 在
`fix/scene-geometry-clip-bugbed` 分支，待合回 4.0。

### 本会话关键 commit

| commit | 内容 |
|--------|------|
| `4db556b4` | [Docs]: 更新当前计划为 TextArea 完成态（4.0 上） |
| `9ac1fd9c` | [Fix]: 统一 primitive 绝对坐标口径至 SceneGeometry.absoluteBox（B1） |
| `9e46759f` | [Fix]: 统一 paint 与 hit-test 的 CLIP 裁剪窗口口径（B3/I7） |

### oracle 审核关键结论

- **P0 B1**：absoluteX/Y 在 3 primitive 各自手写、不注入 scrollOffset，与 `SceneGeometry.absoluteBox`
  形成 4 套口径，primitive 进滚动容器即坐标漂移。**已修**（删私有 helper，统一走 absoluteBox）。
- **P0 B3/I7**：paint clipChildren+scrollable 都裁，hit-test 只裁 scrollable，口径分裂。
  **关键核实**：fixer-2 推测成立——纯 clipChildren 节点 clip bounds 与自身盒重合，
  父自身 bounds 检查先行挡住指针，B3 当前无独立可观测 bug，修复为防御性口径统一。
  **已修**（抽 `isClipWindow` 谓词共用）。
- **未修（后续候选）**：B2 TextArea O(N²) 文本几何、I6 replayer 反向依赖 ui.style、
  B4 COLUMN fill O(n²) 约束判定、B5 paint LEFT 无谓 measureWidth、B7 caret 高度不响应 fontSize、
  B6 transform+clip 叠加坐标错位、B8 滚动后 hover 滞留、A1 effect 内 set 慢一帧、
  A6 bind impact 死参数、chrome 主题层（P2 大工程，单独立项）。
- 完整报告由 oracle 产出，主 Agent 整合后存档（未单独写文件，关键结论见本交接 + review 报告）。

### 审核报告

- `docs/开发者文档/reviews/REVIEW-20260625-scene-geometry-clip-bugbed.md`（主 Agent 自审，
  reviewer 子代理两次空返回、原 session 失效，如实标注未经独立子代理复核）

### 验证状态

- `jetbrainsBuildProject` 全量编译通过，0 problems
- `./gradlew.bat test --tests "*scene*"` 569 测试 0 失败 0 错误
- B1：SceneSlider 11 + SceneTextArea 23 + SceneTextInput 18 = 52 测试全绿
- B3：SceneHitTesterTest 20（+4 新）、SceneNodeTest 35（+1 新）

### 当前分支

- `fix/scene-geometry-clip-bugbed`：B1+B3 修复，工作区干净，待合回 4.0
- 合回时用 `git merge --no-ff`

### 下个会话任务表

1. **合回 4.0**：`fix/scene-geometry-clip-bugbed` → 4.0（--no-ff）
2. **TextArea 真机验收**：用户跑 runClient21 确认多行输入/跨行 caret/滚动/textBridge
3. **B2 TextArea O(N²) 文本几何**（P1）：移植旧栈 TextLayoutEngine 思路，value→行起点前缀和缓存
4. **I6 replayer 反向依赖 ui.style**（P1）：scene 内自建圆角值对象切断依赖
5. **嵌套分类树导航** / **字段引擎最小切片**

### 已知边界

- B3 修复无行为变化（防御性口径统一），真机无需专门验收 clip，但顺带确认 Select/Slider/TextInput
  在滚动容器内点击定位无异常即可
- reviewer 子代理两次空返回原因未明，后续若再触发需排查 reviewer agent 配置

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能登记到正确 Owner。
