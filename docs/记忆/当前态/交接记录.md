# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；本轮在 `chore/codegraph-memory` 分支完成 CodeGraph 全局接入与记忆框架适配
- 全局工具状态：已安装 `@astudioplus/codegraph-mcp@0.18.5`；opencode 全局 `opencode.jsonc` 已新增 `codegraph` MCP，启动命令显式使用 `codegraph-mcp --mcp`，并设置 `CODEGRAPH_TELEMETRY=off`
- 仓库记忆状态：新增 `docs/记忆/长期事实/CodeGraph协作.md` 与 `docs/记忆/决策/DECISION-20260607-codegraph-memory-mode.md`；总导航、记忆 README、稳定命令、项目约定、当前态均已指向 CodeGraph 协作流程
- 后续使用口径：具体类、方法、调用链、依赖图和影响面优先用 `codegraph` MCP 缩小范围，再用源码读取、定向测试和 `compileJava` 确认；不要把一次性 CodeGraph 查询输出写成静态索引
- 继续开发建议：`HtmlLikeDocumentWidgetTest` 后续仍可按主题拆 `EventDispatch`；每批只做测试组织搬家，不改生产代码语义

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
