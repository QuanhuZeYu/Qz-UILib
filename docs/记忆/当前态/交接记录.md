# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；本轮在 `chore/project-codegraph-mcp` 分支补充项目级 CodeGraph MCP 配置
- 工具状态：已新增 `.opencode/opencode.json`，项目显式注册 `codegraph` MCP，使用 `npx -y @astudioplus/codegraph-mcp --mcp` 启动；协作者无需预先全局安装 CodeGraph 即可按项目配置加载
- 仓库记忆状态：`docs/记忆/长期事实/CodeGraph协作.md` 已更新为项目级配置优先、个人全局安装仅作为本机加速；稳定命令补充 `npx -y @astudioplus/codegraph-mcp --help`
- 后续使用口径：具体类、方法、调用链、依赖图和影响面优先用 `codegraph` MCP 缩小范围，再用源码读取、定向测试和 `compileJava` 确认；不要把一次性 CodeGraph 查询输出写成静态索引
- 继续开发建议：`HtmlLikeDocumentWidgetTest` 后续仍可按主题拆 `EventDispatch`；每批只做测试组织搬家，不改生产代码语义

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
