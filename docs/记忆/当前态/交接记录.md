# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；本轮在 `add/runtime-host-visual-matrix` 分支恢复 `/qzuilib test` RuntimeHost 首轮 5 张视觉样例，并按 `--no-ff` 合回主分支
- RuntimeHost 结果：新增 `VIS-HOST-001` 到 `VIS-HOST-005`，覆盖 open timing、resize、runtime stats、HUD/container input、exception panel；视觉舞台拆到 `UiTestRuntimeHostVisualFactory`，断言/诊断拆到 `UiTestRuntimeHostAssertionRunner`
- 当前矩阵统计：首轮计划 59 张，已接入 53 张，缺口 6 张；自动 39 张，人工 14 张；RuntimeHost 中 `VIS-HOST-003` 为自动断言，其余 4 张保留游戏内人工确认
- 验证状态：已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache --rerun-tasks test --tests "club.heiqi.uilib.internal.devtools.pages.UiTestDocumentPageControllerTest" --tests "club.heiqi.uilib.internal.devtools.pages.UiTestRuntimeHostVisualMatrixTest"`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.internal.devtools.pages.UiTestDocumentPageControllerTest" --tests "club.heiqi.uilib.internal.devtools.pages.UiTestRuntimeHostVisualMatrixTest"`、`./gradlew.bat --no-configuration-cache compileJava`
- 继续开发建议：下一轮从 `4.0` 切新分支，优先恢复 RemoteNet 首轮 6 张视觉矩阵；如继续整理测试结构，优先评估其他大测试类，不再向 `HtmlLikeDocumentWidgetTest` 或 `UiTestDocumentPageControllerTest` 堆叠新主题

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
