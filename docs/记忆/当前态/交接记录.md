# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-28 第 125 次）—— scene 布局算法清晰度重构 deepwork 全程完成

### 一句话现状

deepwork「scene 布局算法清晰度重构」**全部 4 阶段完成并合回 4.0**。
SceneLayoutEngine 1031→513 行，拆出 SizingCalculator / ConstraintResolver /
FlexLayouter 三个协作者；斩断自反馈从注释约束升格为类型约束；
还 NORTH_STAR §偏离 2026-06-20「COLUMN 主轴 flexGrow 权重分配」部分债。
工作区干净，所有 commit 已合回 4.0。

### 本会话累计 commit（按时间序，已合回 4.0）

| commit | 内容 |
|--------|------|
| `27303742` | [Docs]: 删除过期 ParallelPerf 设计方案 |
| `0ae15fb9` | [Docs]: 精校 NORTH_STAR §I6 measurer 并行契约措辞 |
| `8fb43f64` | [Refactor]: FontMatcher/DerivedFontCache 命中计数器 AtomicLong 改 LongAdder |
| `1282306e` | [Refactor]: SceneLayoutEngine scrollable 视口例外收口（阶段 1/4）|
| `c1b1281c` | [Refactor]: SceneLayoutEngine layoutInternal 拆方法（阶段 2/4）|
| `c7187e3d` | Merge branch 'refactor/scene-layout-clarity' into 4.0（阶段 1+2）|
| `89552d62` | [Refactor]: 拆出 SizingCalculator 协作者类（阶段 4.1/4）|
| `3abbd84b` | [Refactor]: 拆出 ConstraintResolver 协作者类（阶段 4.2/4）|
| `4e87afb6` | [Refactor]: 拆出 FlexLayouter 协作者类 + 跨类 Javadoc 回填（阶段 4.3/4）|
| `29493b4c` | Merge branch 'refactor/scene-layout-split' into 4.0（阶段 4.1-4.3）|
| `71004347` | [Add]: COLUMN 主轴 flexGrow 权重分配（阶段 3/4）|
| `4d885da3` | Merge branch 'refactor/scene-flex-grow' into 4.0（阶段 3）|

### 本会话任务链路

**阶段 A：基建研读 + 杂项清理（短任务）**
1. 用户「带我仔细研读当前的地基建设」→ 主 Agent 亲读 NORTH_STAR §I6/I7/§10.2
   + 3 个 explorer 并行侦察契约线阶段 1 / measurer 并发无锁底座 /
   分治几何归并 + 并行执行器
2. 用户指出 §I6「已还清」措辞与代码偏差 → 主 Agent 精校 §I6 三处
   （AtomicLong CAS → LongAdder / 行号 :305/:309 → :309/:313 /
   补「还清」口径脚注）
3. 删除过期 ParallelPerf 设计方案文档
4. 顺手做附加发现：FontMatcher/DerivedFontCache 命中计数器
   AtomicLong → LongAdder（与 §10.2 原意一致，收口 font 测量链路）

**阶段 B：deepwork scene 布局算法清晰度重构（长任务）**
5. 用户「参照 Flutter 和 Qt，本项目布局算法还能优化吗」→
   主 Agent 亲评清晰度（4 处塌陷区）+ librarian 行业调研
   （Qt qGeomCalc / Yoga / Flutter / CSS Flexbox / GPUI / Taffy 横向对比）+
   oracle 6 裁决（4 值得做 / 2 明确不做）
6. 用户「按照 deepwork 开始优化长任务」→ deepwork 工作流启动
7. 阶段 1 scrollable 收口（fixer → reviewer 通过）
8. 阶段 2 layoutInternal 拆方法（fixer SkipDecision 载体 → reviewer 通过）
9. 阶段 1+2 合回 4.0（merge c7187e3d）
10. 阶段 4.1 拆 SizingCalculator（fixer measuredTextNodes 注入矛盾处理
    + countLines 包级矛盾处理 → reviewer 通过）
11. 阶段 4.2 拆 ConstraintResolver（fixer measurer 双注入决策 +
    跨类契约 1 双向锚定 → reviewer 通过 + 主 Agent 顺手修 2 处文档过时）
12. 阶段 4.3 拆 FlexLayouter（fixer SelfBubbleSignal 选方案 D 包级嵌套
    record + performLayout 步骤 A 上移主引擎 → reviewer 有条件通过 +
    主 Agent 顺手回填 6 处 Javadoc 失效引用）
13. 阶段 4.1-4.3 合回 4.0（merge 29493b4c）
14. 用户拍板启动阶段 3 flexGrow → oracle 起草精化方案（6 裁决）
15. fixer 实施 4 步（含 opencode 重启后中断恢复一次）
16. **7 测试全红** → 主 Agent 亲补 oracle 方案 2 处缺口：
    - 缺口 1：ConstraintResolver:237 分配公式 getFlexGrow → effectiveGrow
    - 缺口 2：SizingCalculator.computeHeight fill 分支 + isHeightConsumingConstraint
      谓词对称扩展让 grow 子隐式 fill 主轴
17. 全套 scene 测试零回归 → reviewer 有条件通过（2 条建议级已知边界）
18. NORTH_STAR §偏离 2026-06-20 status 升级 + 长期事实文档同步
19. 阶段 3 合回 4.0（merge 4d885da3）

### 关键架构成果

1. **SceneLayoutEngine 1031→513 行**，拆出 3 协作者共 952 行
2. **斩断自反馈从注释约束升格为类型约束**：
   rootFinalHeight 作为 positionChildren 入参，FlexLayouter
   无 SizingCalculator 字段、构造器空、方法体内零 sizing 调用，
   物理上无法重算
3. **跨类契约 1（computeWidth 内宽基准权威）三向锚定**：
   SizingCalculator ↔ ConstraintResolver ↔ FlexLayouter
4. **flexGrow 权重分配落地**：
   - SceneNode.flexGrow（int 默认 0，COLUMN 主轴语义）
   - ConstraintResolver.computeColumnGrowHeights 一次性按权重分配
     freeH，余数补末位（Qt 语义）
   - effectiveGrow 收敛 fillParentHeight 隐式为 1（向后兼容桥）
   - SizingCalculator.computeHeight + isHeightConsumingConstraint
     对称扩展让 grow 子隐式 fill 主轴（主 Agent 亲补缺口）
   - 守单 pass I7，与现有唯一-fill 路径数学等价
5. **NORTH_STAR §偏离 2026-06-20 部分还清升级**：
   scope 列已支持/仍不支持清单，status 登记 3 项已知技术债

### 仍登记的债（非本次引入）

**布局算法剩余债**：
- COLUMN 主轴 min/max 高度 clamp + percent + margin + align-self
  （NORTH_STAR §偏离 2026-06-20 status 显式登记）
- 嵌套 grow 子容器场景（容器 X 是父的 grow 子但非 fill 时，
  X 自身 priorKnownInnerHeight 返 UNCONSTRAINED 致 X 内 grow 子
  回退 shrink）——reviewer 建议级已知边界，待真实需求触发再扩展
- childConstraintsWouldChange 逐子调 buildChildConstraints 叠加每子
  求解使脏判定为 O(n²)——本期接受（沿用 DECISION-20260626-b4 口径
  + 干净帧 Objects.equals 短路）
- 未来补 maxHeight 时撞顶重分配需重新评估 I7 单 pass 边界

**主引擎再瘦身（阶段 4.4，未启动）**：
- 主 Agent 评估当前 513 行已是清爽的 DFS 编排 + I7 跳过 + D1 命门
  结构，多出 213 行主要是必要 Javadoc + record 声明，
  继续瘦身可能切伤 I7 骨架，不建议启动

**预存非布局债**：
- B6 FBO 纹理脏标记跨帧复用（I7 合成阶段偏离，待性能暴露后启动）
- B6 hit-test 对偶（SceneHitTester 对 transform 零感知，待交互需求触发）
- A1 effect 内 set 慢一帧残留
- chrome 主题层（P2 大工程未立项）
- Phase 5 旧栈退役（阻塞中）

### 当前分支与工作区

- 当前工作分支：`4.0`，工作区干净
- 本会话 3 个开发分支已合回 4.0 并保留：
  - `refactor/scene-layout-clarity`（阶段 1+2）
  - `refactor/scene-layout-split`（阶段 4.1-4.3）
  - `refactor/scene-flex-grow`（阶段 3）
- 4.0 当前比 origin/4.0 领先 510 commits

### 下个会话任务表（用户决定优先级）

1. **可选：push 4.0 到远端**（用户拍板时机）
2. **可选：清理 3 个已合并的开发分支**（`git branch -d` 三分支）
3. **可选：启动主引擎再瘦身（阶段 4.4）**——主 Agent 评估不建议，
   但用户可派 oracle 复核 513 行能否继续瘦身
4. **可选：启动布局算法剩余债某项**（如 min/max clamp 或嵌套 grow
   容器场景扩展，待真实需求触发再启动）
5. **可选：启动非布局债某项**（B6 FBO 纹理缓存 / B6 hit-test 对偶 /
   A1 残留确认 / chrome 主题层）
6. **可选：用户自定新方向**

### 已知边界（沿用 + 本会话新增）

- 本会话新增文档：`.slim/deepwork/scene-layout-clarity-refactor.md`
  （deepwork 进度文件，记录 4 阶段状态 + oracle 裁决清单 + 委派纪律）
- 本会话新增协作者：SizingCalculator / ConstraintResolver /
  FlexLayouter 三个包级类（同 `club.heiqi.uilib.ui.scene.layout` 包）
- **跨类契约 1（computeWidth 内宽基准权威）三向锚定**：
  改 SizingCalculator.computeWidth 优先级链时必须同步检查
  ConstraintResolver.buildChildConstraints 与
  SceneLayoutEngine 主流程（传 FlexLayouter.positionChildren）
  两处调用
- **跨类契约 2（viewportHeight 与 priorKnownInnerHeight）**：
  改任一 fill 分支前必须同步审视另一处
- **斩断自反馈类型约束**：FlexLayouter 不持 SizingCalculator 引用，
  rootFinalHeight 作为入参传入，物理上无法重算
- **flexGrow 隐式 fill 主轴对称**：
  ConstraintResolver.effectiveGrow（fill→隐式 grow=1）↔
  SizingCalculator.computeHeight + isHeightConsumingConstraint
  （grow→隐式 fill 主轴），改一处必须同步另一处
- **`measuredTextNodes` 共享 Set 注入**：SizingCalculator 持 final Set
  引用与主引擎同一实例，computeWidth 内 add 与 layout 入口遍历同 Set
- **countLines 包级可见性**：因主引擎 priorKnownChildHeight 跨类调用
  而 SizingCalculator.countLines 改为包级（非 private 非 public）

### 验证状态

- 本会话每阶段 jetbrainsBuildProject 编译零问题
- 每阶段全套 scene 测试零回归（最后阶段 SceneLayoutEngineTest
  89 测试全绿 + 全套 scene 测试零回归）
- 每阶段独立 reviewer 审核通过（4.3 + 阶段 3 有条件通过 + 文档回填）
- oracle 裁决 2 处缺口主 Agent 亲补 + commit 正文留痕

---

## 重要纪律（长期沿用）

- 帧率/真机实测必交用户跑，沙箱无 GUI
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做；
  但 opencode 重启后原 session 失效，按"调度开销明显大于收益 + 信息已在上下文"
  白名单主 Agent 可亲自做单点排错
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑，避免并行 Gradle 竞争 build/ 目录
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，
  改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能
  登记到正确 Owner
- token 实际值以 SceneChromeTokens.java 源码为准，不依赖过期文档清单
- oracle 裁决架构方案后，如方案涉及长期可引用的设计决策，建议补写
  `docs/记忆/决策/` 文档沉淀
- I11 逃生舱②扩张纪律：扩之前证明它确实是一类新的权威交互真值，
  而非可以用现有机制表达的东西（B8 验证了 hover 重算不需要扩②）
- **诚实记录 oracle 方案缺口**：本会话阶段 3 oracle 方案有 2 处缺口
  （分配公式 + 接收端对称），主 Agent 亲补并在 commit 正文留痕，
  下次类似情况继续坚持"oracle 不是神谕，发现 bug 诚实记录修复"