# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前分支：`refactor/remote-ui-runtime-lease`；本次正在落地远程 UI Runtime + Lease Protocol 第一阶段等价重构
- 已新增内部类：`RemoteUiProtocol`、`RemoteUiAssetStore`、`RemoteUiSessionManager`、`RemoteUiServerRuntime`、`RemoteUiClientRuntime`
- 页面/HUD 公开入口未改默认语义；内部 Stream / submit / close 已携带并校验 `sessionId + surfaceId + contentRevision + assetId/localMountToken`，HUD close 使用显式 `closeScope`，不再由新协议依赖空 session sentinel
- `NetService` 未加入 keepalive / renew / remote UI 语义；默认仍固定 10 分钟 TTL 且不隐式续期，renew/resume 只保留 DTO 骨架
- 已通过远程包测试与网络 Stream 定向回归；收尾需确认最终 `git diff --check`、`compileJava` 和 git 提交状态

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
