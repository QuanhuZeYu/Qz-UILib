# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- **当前分支**：`docs/modern-config-template-plan`
- **当前任务**：Modern Config 模板配置页已完成 Batch 4 动态 map 与预设模板，下一位从 Batch 5 高级编辑与搜索模板开始施工
- **工作区状态**：Batch 4 实现提交 `9af90bce`；本轮结束需保持工作区干净
- **验证状态**：已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test --tests club.heiqi.uilib.config.ModernConfigDynamicMapPresetPropertyBindingTest --tests club.heiqi.uilib.config.ModernConfigTypeInferenceTest --tests club.heiqi.uilib.ui.control.DocumentKeyValueEditorControlTest`；该测试命令执行过程中 `compileJava` 已通过

### 本轮进度详情

- Batch 4 完成：新增 `DocumentKeyValueEditorControl`，支持 key/value/type 三列编辑、添加行、选择行、删除选中行和行快照事件。
- 新增 `ModernKeyValueMapPropertyBinding`，只处理 `FieldSpec.templateHint` 明确声明为动态 map 的路径；重复 key、空 key、点号 key 和数值/布尔解析错误会给出校验提示。
- 新增 `ModernPresetSelectorPropertyBinding`，识别同级 `_presets` 或 `presets` 预设定义；选择预设时批量写入预设声明字段，偏离预设时显示“自定义（已修改）”。
- `ModernConfigTypeInference` 新增 `KEY_VALUE_MAP` 与 `PRESET_SELECTOR` 模板类型；`ModernNestedCategoryBinding` / `ModernObjectPropertyBinding` 避免把动态 map 或预设对象继续当普通对象递归。
- Batch 4 未做 Forge→Config 迁移、源码编辑、增强选择器或搜索过滤；这些仍留给 Batch 5。

### 下一步操作

1. 从 Batch 5 开始：补齐源码编辑、增强选择器和搜索过滤模板。
2. 新增 `DocumentCodeEditorControl`，可基于 `DocumentTextAreaControl` 做行号、基础高亮、Tab 插入和错误行提示。
3. 实现 `RawEditorPropertyBinding`，支持 JSON/YAML 格式化、语法验证和表单/代码模式切换，语法错误不能污染当前草稿。
4. 实现 `EnhancedPickerPropertyBinding`，覆盖颜色、资源、声音三类字符串字段；颜色先做色块、HEX/RGB 输入和预览即可。
5. 新增 `ModernConfigSearchIndex` 与 `SearchFilterPropertyBinding`，支持路径/键名/值摘要索引、搜索、按类型过滤、只看已修改和结果跳转。
6. Batch 5 结束后运行 `git diff --check`、高级模板相关测试或 `compileJava`，提交 git，并更新本交接记录给出 Batch 6 提示词。

### 重要约束

- 入口检测的是 `club.heiqi.config` 模块能力是否存在，不是检测配置文件是否存在。
- `ModConfigGui` 不直接引用 `club.heiqi.config` 类型；现代页相关类型只在 `ModernConfigBridge` 之后加载。
- 不做 Forge 到 config 模块迁移工具，不在页面内添加迁移按钮。
- Forge 回退不需要理解复杂 config 模型；接入方自己决定复杂结构如何在 Forge cfg 下兼容，推荐 JSON 字符串属性。
- UI 风格沿用当前 Forge 配置模板页，不另起视觉体系。
- `MutableConfig` 裸值本身不包含 schema；choice、默认值、数值范围等 UI 语义依赖 `ModernConfigTemplateScreen.FieldSpec`。
- Batch 2 表格模板只覆盖稳定列 primitive map list；动态 key 集合已由 Batch 4 的 `ModernKeyValueMapPropertyBinding` 处理。
- Batch 3 普通 map 走 `ModernNestedCategoryBinding` / `ModernObjectPropertyBinding`，递归内联默认 5 层；超深层级通过树形导航或“展开编辑”处理。
- Batch 4 动态 map 必须由 `FieldSpec.templateHint` 明确声明，常用 hint 包括 `dynamic-map`、`key-value-map`、`kv-map`，不要自动吞掉普通对象。
- Batch 4 预设对象通过同级 `_presets` 或 `presets` 触发，预设选择只覆盖预设声明字段，并保留预设定义存储。

### 下一位 Agent 提示词

```text
你接手 Qz-UILib 的 ModernConfigTemplateScreen 分阶段施工。先阅读 AGENTS.md、docs/AI记忆文档.md、docs/开发者文档/specs/modern-config-template-screen-plan.md、docs/记忆/决策/DECISION-20260613-modern-config-template-optional-module.md、docs/记忆/当前态/交接记录.md。当前应从 Batch 5 开始，不要重做 Batch 0/1/2/3/4。Batch 5 只做高级编辑与搜索模板：新增 DocumentCodeEditorControl，基于 DocumentTextAreaControl 做行号、基础高亮、Tab 插入和错误行提示；实现 RawEditorPropertyBinding，支持 JSON/YAML 格式化、语法验证、表单/代码模式切换，语法错误不污染当前草稿；实现 EnhancedPickerPropertyBinding，覆盖颜色、资源、声音三类字符串字段，颜色先做色块、HEX/RGB 输入和预览；新增 ModernConfigSearchIndex 与 SearchFilterPropertyBinding，支持路径、键名、值摘要和类型索引，以及搜索、按类型过滤、只看已修改、结果跳转。不要做 Forge→Config 迁移，不要重做动态 map 或预设模板。保持 UI 风格与当前 ForgeConfigTemplateScreen 一致。完成后运行 git diff --check、高级模板相关测试或 compileJava，提交 git，并更新交接记录给出 Batch 6 的接力提示词。
```

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
