# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；最近实现分支：`add/test-visual-p0-home`
- 已完成：`/qzuilib test` P0 数据模型和首页框架已落地；控制器已收缩为生命周期/导航/环境刷新，矩阵逻辑拆到 `UiTestMatrixRegistry`、`UiTestGroupVisualBuilder`、`UiTestSemanticChecker`、`UiTestMatrixState` 和双状态模型
- 当前状态：继续保留 DOM / CSS / Layout / Paint / Input / Controls / TextFont / Animation / RuntimeHost / RemoteNet 十组导航；首页已有能力画廊、语义覆盖热力图、快速筛选、最近失败、人工任务、环境信息和状态模型；二级页已有分组说明、视觉样例区、语义检查区、操作区和诊断区壳
- 当前矩阵：registry 真实样例列表为空，不恢复旧卡片；首轮计划数量按规格表合计 59 张，下一批优先接入 CSS / Layout / Paint 真实视觉样例
- 验证：已通过目标 `UiTestDocumentPageControllerTest`、`compileJava` 与 `git diff --check`；本批未涉及游戏内 UI/HUD/输入/远程/网络 smoke，未跑 `runClient21`
- 文档流程补充：审查误报或结论失效时，必须在原始审查正文添加 `后续复核（YYYY-MM-DD）` 批注，并同步更新 reviews 索引，避免后续 Agent 只读原报告重复审核

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
