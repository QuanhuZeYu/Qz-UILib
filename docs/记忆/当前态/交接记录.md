# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；当前交接目标是拆分 `src/test/java/club/heiqi/uilib/ui/document/HtmlLikeDocumentWidgetTest.java`
- 已评估：该文件实际 5896 行、122 个测试，混合基础绘制、布局缓存、动画运行态、拖拽、滚动、事件分发、焦点和 raw 控件键盘语义，继续堆叠不符合单文件职责边界
- 现有拆分先例：同包已有 `HtmlLikeDocumentWidgetInlineLayoutCacheTest.java` 与 `HtmlLikeDocumentWidgetCursorTest.java`，说明按主题拆测试符合当前组织风格
- 推荐第一批：抽出包内测试支撑类 `HtmlLikeDocumentWidgetTestSupport`，迁移底部 helper/fixture；再把拖拽测试迁移到 `HtmlLikeDocumentWidgetDragTest`
- 第一批候选测试范围：`shouldDragFixedElementThroughDocumentDragSupport` 到 `shouldExposeNativeDocumentCoordinatesForHtmlLikeDragEvents`，大致位于旧文件 1785-2212 行
- 注意：第一批只做测试搬家，不改生产代码语义，不扩大断言目标；拆完需串行跑 `git diff --check`、`HtmlLikeDocumentWidgetTest`、`HtmlLikeDocumentWidgetDragTest`、`compileJava`
- 背景状态：Controls 视觉样例已接入并修复 `VIS-CTRL-005` table 同步；`/qzuilib test` TextFont 下一批可在测试拆分第一批后再恢复

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
