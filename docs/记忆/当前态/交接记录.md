# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-29 第 135 次）—— 旧栈清理 + P1.5 分类导航 + 决策红线精确化 + uilib 自身配置接入实验

### 一句话现状

现代化配置页推进到 P1.5：旧栈（ModernConfigTemplateScreen 及 21 个 Modern 簇类）已清理，分类导航 + save 反馈 + SceneButton primary variant + SceneNavList 已落地（commit `e92766d0`），决策红线经一次错误升级后精确化为三档（`6648ddb5` + `ee1e181d`），uilib 自身配置已接入新架构做端到端实验（commit `74d16ea1`）。2502 测试全绿。分支 `feat/modern-config-p0-core`，8 个 commit 待 push。

### 本会话累计改动

**旧栈清理（commit `0f4f2c93`）**
- 删除 21 个 Modern 簇主类 + 12 个测试 + 3 个 MODCFG demo 入口
- 连锁修改 7 文件（ModConfigGui / devtools registry 干掉 MODCFG 分组）
- 6 份旧决策归档到 `docs/开发者文档/legacy/`，2 份使用文档物理删除
- Config模块.md 旧栈段落加废弃横幅

**P1.5 分类导航（commit `e92766d0`，reviewer 审核通过无 critical/major）**
- `SceneButtonVariant`（新增枚举 STANDARD/PRIMARY）+ `SceneNavList`（新增纵向受控单选导航复用 SceneSingleSelectPrimitive）
- `DraftSignalAdapter` 新增 dirtyCountSignal/errorCountSignal/saveFeedbackSignal
- `ConfigScreen` 用 `rt.show(parent, condition, supplier)` 三参重构成"单 section 显示"，≤5 用 SceneSegmented，>5 用 SceneNavList 侧栏，自动按数量选形态
- `FieldShell` dot 三态优先级修正（error > dirty > normal）
- `StringFieldRenderer`/`NumberFieldRenderer` 去重 placeholder，`BooleanFieldRenderer` 去 toggle label
- `SaveFeedback`（新增 record）
- reviewer 4 个 minor 建议（actionBar 左右分区 / ConfigScreen 拆 SectionNavigator / SceneNavList 独立测试 / FieldShell dot 测试）均非阻断，留后续

**决策红线精确化（commit `6648ddb5` + `ee1e181d`）**
- 一次错误升级为"双向严父依赖"（`8bf5372f`）已 revert（`3f153ae8`），错误登记补记（`5760c816`）
- 现为三档精确红线：
  1. config 核心层（schema/runtime）严禁 import uilib 任何类
  2. config UI 层硬依赖 uilib scene/reactive，合法
  3. uilib 通用 UI 组件包（uilib.ui.*）严禁 import config.ui.*（库功能纯净红线）
- 豁免：uilib 作为 mod 自身接入方可 import config 全部包（schema/runtime/ui 含 ConfigUI.open），接入代码放专门包（如 `uilib.config.modern`），不污染通用组件包
- P0/P1 落地的 30+ import uilib 全部合规，**不重构**

**uilib 自身配置接入实验（commit `74d16ea1`，2502 测试全绿）**
- 新包 `club.heiqi.uilib.config.modern` 下 3 个主类：
  - `QzUiLibModernSchema.java`：ConfigSchema.builder DSL 声明 22 个字段（general 4 + fontSystem 16 + fontSizeSetting 2）
  - `ModernConfigEntry.java`：入口 bootstrap + ConfigUI.buildScreen + displayGuiScreen
  - `ModernConfigScreen.java`：MC GuiScreen 宿主 extends McScreenBridge，38 行，只覆写 doesGuiPauseGame 返回 false
- 命令 `/qzuilib modernconfig` 打开新配置页
- 配置文件独立 `config/qzuilib-modern.yaml`（YAML，不与 Forge .cfg 复用）
- `QzUiLibModernEndToEndTest.java` 11 个端到端测试（bootstrap/load/save/reload/cancel/校验失败回滚/CHOICE 校验/恢复默认 等）
- 字段未映射：`fontSort`（String[]）/ `characterFontRules`（String[]）—— P0 FieldType 暂不支持 SIMPLE_LIST，留 P2

### 关键裁决
- 分类导航形态自动阈值：≤5 SceneSegmented / >5 SceneNavList 侧栏
- SceneButton primary variant 允许补控件库扩展，navPane 同理
- 包集成度：config 与 uilib 心智上软分，**取消"config 可独立发布成无 uilib 纯数据 jar"目标**，单一 jar 发布（简化复杂度）
- YAML 核心（SnakeYAML 2.2 shadow）保持现状不放宽

### 已知限制 / 本会话新遗留
- 旧栈清理时残留 6 处对 Modern 类名的**注释文字引用**（HtmlLikeDocumentWidgetScrollTest / ConfigSerializer / DocumentCodeEditorControl / ReactiveTriadDemoView / ReactiveTriadDemoScreen / DocumentCodeEditorSyntaxSupport）不影响编译，未清理
- SceneNavList 缺独立单元测试（仅 ConfigScreenTest 间接覆盖）
- FieldShell dot 三态错误优先级测试未补
- ConfigScreen 604 行接近拆分阈值，建议拆 SectionNavigator 协作者（设计文档 §10.3 建议）
- actionBar 未做左右分区（设计 §7.1 未落地，主次靠 primary variant 区分）
- Oracle 裁决 paint 视口剔除 P2 第一优先（单文件 ScenePaintEngine 内加裁切），当前 ConfigScreen 因 content 容器无 fill 子根本不在 B4 O(n²) 范围，暂无瓶颈

### 真机验证待项（交用户）
- 配置页能正常打开、渲染、交互
- 编辑字段后保存，重开游戏重打开 `config/qzuilib-modern.yaml` 确认值持久
- 帧率与交互响应正常（Oracle 已确认 ConfigScreen 当前结构无 O(n²) 瓶颈）
- SceneButton primary variant 蓝底视觉 / SceneNavList 侧栏视觉 / save 反馈条效果

### 下一步可选方向
1. 真机验证通过后合并 `feat/modern-config-p0-core` 回 `4.0`（用 `git merge --no-ff`）
2. P2 完善：分类导航（已落地）/ 嵌套分类树 / 全局搜索 / 8 种复杂字段类型 / paint 视口剔除（真有瓶颈时）
3. 接入更多 mod 真实配置验证新架构扩展性

### 分支与工作区
- 当前分支：`feat/modern-config-p0-core`
- 提交历史：8 次未 push commit
  - `5760c816` 错误登记
  - `74d16ea1` uilib 自身配置接入
  - `ee1e181d` 决策豁免扩大
  - `6648ddb5` 决策精确化
  - `3f153ae8` revert 错误升级
  - `8bf5372f` 错误升级（已 revert）
  - `e92766d0` P1.5 分类导航
  - `0f4f2c93` 旧栈清理
- 工作区：干净
- 测试：2502 全绿（含新增 11 端到端测试）