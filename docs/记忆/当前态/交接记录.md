# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前分支：`test/manual-diagnostics-auto-assertions`；本轮将 `/qzuilib test` 中 textarea、select/table、scrollbar 的可机器诊断部分升级为自动断言
- 已改动：`VIS-CTRL-003` 自动检查 Ctrl+A 替换后的 value/change 日志/布局盒；`VIS-CTRL-005` 自动检查 select top-layer、选项选择、value/change 日志与 table 同步；`VIS-PAINT-006` 自动检查 overflow、scroll range 与 scrollTo 偏移变化
- 当前矩阵：首轮计划 59 张，已接入 53 张，缺口 6 张；自动样例 42 张，人工确认样例 11 张；真实 caret/selection 观感、popup 位置/遮挡和 scrollbar track/thumb 拖拽仍可截图观察，但不再占用人工待确认状态
- 继续开发建议：RemoteNet 暂不恢复；下一步可优先继续压缩非 Remote 人工项，或做剩余人工项游戏内截图确认

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
