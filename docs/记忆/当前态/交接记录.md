# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前分支：`docs/session-wrapup-phase2-review`（从 `4.0` 切出，待合并回 `4.0`）
- 已完成：phase2 首批 P0/P1 修复（`2d1bffa`）已合并回 `4.0`；本轮对该提交做独立复核，确认 8 项修复均向浏览器语义靠拢，离线复跑相关测试集全绿
- 本轮文档沉淀：reviews 索引补首批修复复核结论与三项后续未尽边界；errors manifest 波动文档补 `--offline` 绕过手段；稳定命令补离线测试命令
- 验证手段提醒：`test` 任务配置阶段需联网拉 gtnhconvention manifest，外部波动时用 `--offline --no-configuration-cache test --tests "<类名>"` 在依赖已缓存下复跑
- 下一步：将本文档分支 `git merge --no-ff` 回 `4.0`；再从 `4.0` 切新分支继续 Phase 2 审查剩余 P2/P3 项（含负 margin 父子折叠链路、fixed clip chain、focus 事件顺序等）

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
