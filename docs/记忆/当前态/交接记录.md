# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；当前工作分支：`test/runtime-tests`
- 已完成：旧 test 子页仍保持清空；`/qzuilib test` 首页改为语义分组导航；运行时测试卡片迁移到类型二级页；DOM / CSS / Layout / Paint / Input / Controls / TextFont / Animation / RuntimeHost / RemoteNet 全 10 个二级页已接入完整规格矩阵：DOM 13、CSS 15、Layout 16、Paint 9、Input 13、Controls 15、TextFont 7、Animation 8、RuntimeHost 7、RemoteNet 10，共 113 张卡片；每张卡片都有 `执行自动测试`、`人工通过`、`人工失败`，结果会写回卡片状态、总览计数和最近失败摘要
- 当前设计：新 test 体系按 DOM、CSS、Layout、Paint、Input、Controls、TextFont、Animation、RuntimeHost、RemoteNet 分组；首页只做导航和摘要；运行时用例必须直接展示 `预期结果：...` 文本；可由当前模型判断的用例接入自动断言；视觉、宿主、服务端链路和明确未实现能力保持执行中/人工确认或待实现缺口状态，不伪造通过；当前明确缺口包括属性选择器 `[attr]` / `[attr=value]`、分组选择器完整支持、contextmenu、drag/drop 事件链；textarea 删除换行崩溃已修复，见 `docs/开发者文档/errors/ERROR-20260602-textarea-stale-visual-line-cache.md`；input 空值高度与 textarea 光标坐标混用已修复，见 `docs/开发者文档/errors/ERROR-20260602-form-control-input-textarea-caret.md`；游戏关闭 native 崩溃已定位为 `QzUiLibShutdown` 非渲染线程释放字体 GL 资源，修复与日志路径见 `docs/开发者文档/errors/ERROR-20260603-font-shutdown-hook-gl-dispose.md`
- 验证状态：当前测试全部通过；运行时测试矩阵补全已通过 `git diff --check`、离线 `compileJava` 与离线目标 `UiTestDocumentPageControllerTest --rerun-tasks`；字体 shutdown hook 修复已通过目标 `FontServiceLayoutRuntimeSmokeTest`，仍需下一次游戏内关闭时确认不再生成新的 `run\client\hs_err_pid*.log`
- 下一步：先跑 `git diff --check` 并按规范提交/合并；随后用 `runClient21` 做一次游戏内打开和关闭复测，确认关闭阶段不再新增 `hs_err_pid*.log`；再进入游戏内复查完整 `/qzuilib test` 运行时矩阵，重点关注人工确认项和待实现缺口展示是否清晰；随后继续 `inline-block baseline` 或属性选择器 `[attr]` / `[attr=value]` 等 B 类缺口
- 文档流程补充：审查误报或结论失效时，必须在原始审查正文添加 `后续复核（YYYY-MM-DD）` 批注，并同步更新 reviews 索引，避免后续 Agent 只读原报告重复审核

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
