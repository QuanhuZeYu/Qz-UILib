# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-25 第 97 次）—— 据 AWT 研究修正字体引擎 + ink 字段补全

### 一句话现状

插队任务主体完成：字体坐标系统一（57.6→64）+ 下划线裁切修复 + ink 字段补全架构升级。5 个 commit 待真机验证后合回 4.0。Oracle 已产出文本垂直对齐研究方案待后续实施。

### 本会话关键 commit（分支 fix/glyph-coordinate-system-mismatch）

| commit | 内容 |
|--------|------|
| `cb786955` | 步骤1：烘焙字号 57.6→64 统一 + 清理 fontScale 幽灵配置 |
| `007ac778` | 修复 trim/getStringWidth 的 18/9 口径不一致（预先存在 bug） |
| `b1d70468` | 记忆文档：两条错误记录 + 交接记录 + 当前态 |
| `393b1fa2` | 步骤A：补回 GlyphGenerator slot 尺寸 bearing 偏移，修下划线裁切 |
| `c4091bb7` | ink 字段补全：quad 改纯 ink 宽 + ink UV（架构升级） |

### 修复内容

**步骤1（治本·方案B）**：烘焙字号 57.6→64，消除坐标系不自洽（偏上/偏右/偏小）。已真机验证通过。

**步骤A（下划线裁切）**：Oracle 裁决发现真正根因在烘焙端 slot 尺寸公式漏算 bearing（不是研究文档说的 mipmap/smoothstep）。`GlyphGenerator.java:101-104` 补回 bearing 偏移。已真机验证下划线恢复。

**ink 字段补全（架构升级）**：GlyphRuntimeTables 新增 8 个 short[]（inkWidth/inkHeight/bearingX/bearingY × NORMAL+BOLD，约17MB）。resolveGlyphQuadMetrics 几何公式重写：quad 从覆盖整个 slot（含 INK_PADDING）改为只覆盖纯 ink 区域，UV 只采样 ink 区域。字间重叠回归正常 ink 重叠量。待真机验证。

**trim 口径修复**：TextLayoutService.java:783 trimRawStringToWidth 无参重载从 DEFAULT_FONT_SIZE_PX(18) 改为 (int)FontConfig.charSize(9)，与 getStringWidth 口径统一。45 字体测试全绿。

### Oracle 文本垂直对齐研究（已保存，待实施）

研究文件：`docs/记忆/决策/DECISION-20260625-text-vertical-alignment-research.md`

问题：字符串在 UI 组件（按钮、TextInput）中贴组件顶部。以前字体偏上掩盖，坐标修正后暴露。

根因：`ScenePaintEngine.java:245` 文本绘制点恒为局部 (0,0)（盒顶），paint 层从未实现盒内文字垂直对齐。

方案（待用户拍板实施）：
- 步骤1：paint 层补行框居中偏移 `textTop = paddingTop + max(0, (innerHeight - lineHeight)/2)`
- 步骤2：给 ScenePaintEngine 注入 SceneTextMeasurer 拿 lineHeight
- 步骤3：修正 PaintCommand.text 过时 javadoc

与 ink 升级正交，ink 升级已为其铺好"文本垂直对齐有专门计算点"的结构前提。

### 验证状态

- 全量编译通过，font 包 45 测试全绿，reviewer 审核全部通过
- 步骤1 + 步骤A 已真机验证通过
- ink 字段补全待真机验证（字间重叠、边缘锐度、下划线、正常字母）

### 下个会话任务表

1. **真机验证 ink 升级**：用户跑 runClient21 看字体渲染整体效果
2. **若通过则合回 4.0**：按 AGENTS.md 用 `git merge --no-ff` 合回
3. **文本垂直对齐修复**：按 Oracle 方案实施（DECISION-20260625-text-vertical-alignment-research.md），需用户拍板居中口径
4. **恢复主线**：嵌套分类树导航 / 字段引擎最小切片

### 当前分支

- `fix/glyph-coordinate-system-mismatch`：5 commit，工作区干净，待真机验证后合回 4.0
- `4.0`：主分支

### 配置变更

- `~/.config/opencode/oh-my-opencode-slim/orchestrator_append.md` 补充"任务中断恢复"段落，明确同会话内子代理空结果必须用原 task_id 恢复（不是跨会话复用）

### 已知边界

- mipmap 保留（用户拍板，下划线对症在烘焙端不在采样链路）
- 步骤2/3/4（smoothstep/slotGap/UV inset/shader 容差）未做，按需可选
- fontScale 配置项已移除，配置同步测试不受影响
- ink 字段补全内存增量约 17MB（全码点预分配）
- reviewer 建议测试缺口：bearingY>0 下划线场景、空白字形 inkWidth=0 场景待补回归测试

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能登记到正确 Owner。
