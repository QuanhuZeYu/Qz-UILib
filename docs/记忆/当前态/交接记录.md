# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；当前工作分支：`add/runtime-test-pages-followup`
- 已完成：旧 test 子页仍保持清空；`/qzuilib test` 首页改为语义分组导航；运行时测试卡片迁移到类型二级页；DOM / CSS / Layout / Paint / Input / Controls / TextFont / Animation / RuntimeHost / RemoteNet 全 10 个二级页各已接入 5 张卡片和 `执行自动测试`、`人工通过`、`人工失败` 按钮，结果会写回卡片状态、总览计数和最近失败摘要
- 当前设计：新 test 体系按 DOM、CSS、Layout、Paint、Input、Controls、TextFont、Animation、RuntimeHost、RemoteNet 分组；首页只做导航和摘要；运行时用例必须直接展示 `预期结果：...` 人工验收文本；Paint 视觉/命中类、`LAYOUT-005` baseline、Controls selection/caret/step/软换行、TextFont fallback/reload、Animation 事件/视觉、RuntimeHost 和 RemoteNet 服务端链路类用例只做结构或可计算状态检查并进入 `执行中`，最终需人工确认；textarea 删除换行崩溃已修复，见 `docs/开发者文档/errors/ERROR-20260602-textarea-stale-visual-line-cache.md`
- 验证状态：当前测试全部通过；全分组运行时二级页补齐已通过离线目标 `UiTestDocumentPageControllerTest` 与 `git diff --check`；textarea 崩溃修复已通过离线目标 `DocumentTextAreaControlTest`
- 下一步：重新进入游戏内复查 Controls / CTRL-005 textarea 删除换行不再崩溃，并继续全 10 个二级页人工项；随后继续 `inline-block baseline` 或属性选择器 `[attr]` / `[attr=value]` 等 B 类缺口
- 文档流程补充：审查误报或结论失效时，必须在原始审查正文添加 `后续复核（YYYY-MM-DD）` 批注，并同步更新 reviews 索引，避免后续 Agent 只读原报告重复审核

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
