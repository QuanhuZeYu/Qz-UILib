# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-24 第 88 次）—— A1/A2 偏离登记合并转正，债务清理收官

### 一句话现状
reviewer 9 项建议全部处置 + A1/A2 两条偏离登记合并转正为 §4/§4.5 正式能力。
scene 新栈债务清理收官，下一步进入字段引擎最小切片。

### 本轮全部清理概要

**commit `a924d07d`** — 快胜档 4 项（B2/B5/B6/B7）
- B7: maxScrollY 加 !isScrollable() + instanceof LayoutBox 守卫
- B2: 3 处 cachedLayout 强转加 instanceof 守卫 + 延伸清理 2 处
- B6: contentBox → contentNodeBox 消除术语混淆
- B5: 测试反射改用包内方法 __getOverlayLayoutEngine

**commit `750b3ca3`** — 架构小重构 2 项（B3/B4）
- B3: AnchorRect 从 overlay 内部类迁移为 layout 顶层类，消除反向依赖
- B4: SceneHitTester 递归改传 int 参数，零对象分配

**commit `5f58c9a2`** — B1 oracle 裁决接受不修
- 原始前提有误：replay 无视觉错位，真实窗口在 hit-test 滞后一帧
- retained-mode 固有延迟，不破 I7/I8/I11

**本轮 commit** — A1/A2 合并转正
- A1 scrollable 视口：升为 §4 正式布局能力
- A2 overlay 多 paint root：升为 §4/§4.5 正式能力
- 不新增不变量，不改代码，纯文档对齐
- 修正 A2 scope 类名 SceneHostWidget → AbstractSceneHostWidget
- 偏离登记原文加历史标注分隔结案标记

### 验证
- compileJava + compileTestJava 通过（B2/B5/B6/B7 + B3/B4）
- 6 个测试类全绿
- 三轮 reviewer 审核 PASS + 一轮 oracle 裁决
- NORTH_STAR I7/I8/I10/I11 守住
- git diff --check 通过

### 剩余债务（全部 P3/P4，无需处理）
- B8 测试命名略误导（P3，未定位）
- B9 CRLF warning（P3，git 自动处理）
- reviewer 建议3：守卫失败路径单测缺口（P3，防御性）
- reviewer 建议4：测试方法遗留 throws Exception（P4）

### 下一步
1. **字段引擎最小切片**：scene-native 字段 spec、draft/current、校验、
   dirty、canSave/canCancel 派生与保存语义
2. **接真实配置数据源**：优先接 MutableConfig
3. **暂缓**：搜索、树、多行文本、列表编辑、表格行内编辑、高级 picker、
   raw editor、远程同步、完整 CSS/token/theme 和 Phase 5

### 已知边界
- SceneSelect 三批缺陷已修复并通过真机验收
- reviewer 9 项建议已全部处置（6 还清 + 1 接受 + 2 无需处理）
- A1/A2 偏离登记已转正，NORTH_STAR §4/§4.5 追加正式能力描述
- 旧对外现代配置页文档仍描述 DOM 12 模板能力；scene 一期完成前不得宣称等价
- 旧栈 ui.dom 仍完整存在（65+ 源文件），scene 包零引用，Phase 5 暂缓

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
