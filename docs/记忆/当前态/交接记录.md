# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前分支：`fix/runclient21-codechicken-mappings`，本轮修复 `runClient21` 启动时 CodeChickenLib/CoFHCore 弹出 MCP 映射目录选择窗口的问题
- 根因：`run/client/config/CodeChickenLib.cfg` 指向旧 `C:\Users\QuanHu\.gradle` 缓存，而项目稳定命令使用 `D:\.MyApps\.ENV\gradle-home`
- 已改动：`build.gradle.kts` 新增 `configureCodeChickenMappings`，所有 `runClient*` 启动前自动写入当前 `GRADLE_USER_HOME` 对应的 Forge `unpacked/conf` 映射目录；错误记录见 `docs/开发者文档/errors/ERROR-20260608-runclient21-codechicken-mapping-dir.md`
- 验证状态：已通过 `configureCodeChickenMappings` 生成当前 `GRADLE_USER_HOME` 下的 `mappingDir`、`git diff --check`、`compileJava`、`runClient21 --dry-run` 任务链检查
- 继续开发建议：修复验证后再回到 `/qzuilib test` RemoteNet 首轮视觉矩阵恢复

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
