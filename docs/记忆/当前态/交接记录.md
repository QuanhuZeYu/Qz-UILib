# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-23 第 77 次）—— Scene 配置表单 demo 已完成，下一步评估现代配置页地基

### 一句话现状
已新增硬编码隔离的 `SceneFormHostWidget` + `SceneFormDemoScreen`，并接入 scene hub 与 visual matrix `VIS-SCENE-006`；JetBrains 定向编译、Gradle 定向测试和 @reviewer 审核均通过，用户测试反馈“没什么大问题”，待提交保存进度。

### 本次结论
- **新增独立表单页**：`SceneFormHostWidget` 采用 scene 新栈独立 screen，不接真实 Forge Configuration，不复用旧 `PropertyBinding`，不扩旧 `ui.dom` / `HtmlLikeDocumentWidget`。
- **双副本状态已落地**：`name/distance/fancy` 均有 `current` 与 `draft` signal，保存写回 current，取消回滚 draft，恢复默认只写 draft。
- **响应式派生已落地**：`nameError`、`distanceError`、字段 dirty、`isDirty`、`hasError`、`canSave`、`canCancel` 均由 `Computed` 派生，UI 经 `runtime.bind` 消费。
- **布局骨架已落地**：root 为 COLUMN + 深蓝灰背景，固定 titleBar、固定 statusSummary、唯一 `fillParentHeight` viewport、固定 actionBar；viewport 内三张字段卡片并复用 signal-first 滚动链路。
- **控件范围守住**：仅使用 `SceneTextInput`、`SceneToggle`、`SceneButton`；未做 Select、多行文本、Tab 焦点遍历额外能力、Ctrl+S/Enter 提交或远程同步。
- **hub 接线完成**：`SceneTestHubHostWidget` 新增 `Form demo` 按钮与 destination；`SceneTestHubScreen` 分派到 `SceneFormDemoScreen`。
- **visual matrix 接线完成**：`SCENE_DEMO` 计数更新为 6，新增 `VIS-SCENE-006` 文案；`UiTestSampleVisualFactory` 新增表单 demo stage 与跳转按钮。
- **验证结果**：JetBrains 定向编译通过；Gradle 定向测试 `SceneFormDemoTest`、`UiTestDocumentPageControllerTest`、`UiTestRuntimeHostVisualMatrixTest` 通过；全量测试与真机视觉验收未跑。
- **用户测试反馈**：用户测试 Form demo 后反馈“没什么大问题”。
- **下一步方向已调整**：用户明确暂不直接开始真实配置页；旧配置页原计划舍弃并换成现代化配置页，因旧 DOM 性能瓶颈才切 scene 新栈。下一步应先评估/补齐 modern config 所需的新栈地基。
- **地基缺口初评**：现代配置页不是“能显示表单”，而是要承载旧栈已完成的 12 模板入口、分类/搜索、复杂控件、草稿校验、保存回滚和远程同步等能力。scene 当前主干健康，但中间层缺口明显：Select/top-layer、字段引擎、真实配置数据适配、TreeView/搜索/复杂编辑器/虚拟化等。

### 已改文件
- `src/main/java/club/heiqi/uilib/internal/devtools/pages/SceneFormHostWidget.java`
- `src/main/java/club/heiqi/uilib/internal/devtools/pages/SceneFormDemoScreen.java`
- `src/main/java/club/heiqi/uilib/internal/devtools/pages/SceneTestHubHostWidget.java`
- `src/main/java/club/heiqi/uilib/internal/devtools/pages/SceneTestHubScreen.java`
- `src/main/java/club/heiqi/uilib/internal/devtools/pages/UiTestMatrixRegistry.java`
- `src/main/java/club/heiqi/uilib/internal/devtools/pages/UiTestSampleVisualFactory.java`
- `docs/记忆/当前态/交接记录.md`

### 下一步
1. 先提交保存本轮 Form demo、测试与记忆文档改动；提交标题建议 `[Add]: 新增 scene 配置表单演示页`。
2. 提交后优先产出“scene 现代配置页地基补全设计文档”，不要直接开始真实配置页实现。
3. 设计文档需明确一期能力边界（建议 `STRING/NUMBER/BOOLEAN/CHOICE` + 扁平分类 + 草稿保存）、Select/top-layer 方案、字段引擎接口、真实配置数据源选择与旧栈能力差距表。
4. Select/top-layer 是当前最大架构风险点，建议后续单独派 oracle 深评方案后再实现。

### 已知边界
- 表单 demo 为硬编码样例，不写真实配置、不做持久化和远程同步；它只验证 scene form-state 地基，不代表现代配置页已经可开工。
- `SceneTextInput` 当前是单行输入；本任务未扩多行、select、Ctrl+S/Enter 提交和额外焦点遍历。
- 真机视觉与输入手感仍需 `runClient21` 人工确认。

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
