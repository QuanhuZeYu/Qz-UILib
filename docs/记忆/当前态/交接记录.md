# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-28 第 126 次）—— scene min/max clamp + percent + margin + align-self deepwork 全部完成

### 一句话现状

deepwork「scene min/max clamp + percent + margin + align-self」**全部四期完成并合回 4.0**。
NORTH_STAR §偏离 2026-06-20 剩余债**全部还清**。新增 maxHeight/maxWidth、percentHeight/percentWidth、
margin 四向、alignSelf 四个字段族；ConstraintResolver grow 求解器加 freeze do-while（上界+下界对称）
+ percent 分支；SizingCalculator 加 clamp 出口 + percent 分支；FlexLayouter 加 effectiveCrossAlign
+ STRETCH 尊重 maxWidth/percentWidth + margin 偏移定位；SceneGeometry maxScrollY 含 marginBottom。
I7 数值求解器边界澄清补注落地。工作区干净，所有 commit 已合回 4.0。

### 本会话累计 commit（按时间序，已合回 4.0）

| commit | 内容 |
|--------|------|
| `1f7d3b05` | [Docs]: NORTH_STAR I7 数值求解器边界澄清补注 + 偏离 2026-06-20 status 更新 |
| `8f3cb20b` | [Add]: scene 布局引擎一期 min/max 高度 clamp + freeze do-while 撞顶重分配 |
| `db85c936` | [Docs]: NORTH_STAR 偏离 2026-06-20 scope 更新 + 边界 2 已知限制登记 |
| `c1c7a37a` | [Docs]: 修正一期 2 处注释精确性 + 补 4 类关键测试 M9-M12 |
| `060c6aa3` | Merge: scene min/max clamp 一期合回 4.0 |
| `0ecfab24` | [Add]: scene align-self 二期 + 边界 2 回填（STRETCH 尊重 maxWidth）|
| `fadf36f0` | Merge: scene align-self 二期合回 4.0 |
| `fefd6bd3` | Add: SceneNode margin 四向外边距字段与方法（三期 commit 1）|
| `156317ab` | Add: margin 五处联动改动 + G1-G6 测试（三期 commit 2）|
| `59f3e219` | [Fix]: 修复三期 margin reviewer 三问题并补 G7-G11 关键测试 |
| `6911faa2` | Merge: scene margin 三期合回 4.0 |
| `993894f0` | [Add]: scene 布局引擎四期 percent（百分比高度/宽度）|
| `0c1c93fd` | [Fix]: 四期 percent reviewer 建议级修复 + NORTH_STAR 偏离 2026-06-20 全部还清 |
| `ba3635c3` | Merge: scene percent 四期合回 4.0 |

### 本会话任务链路

**阶段 A：进度恢复 + 轻量收尾（短任务）**
1. 读三件套恢复进度（第 125 次交接 + deepwork 进度文件）
2. 用户选「先收尾再开工」→ 提交第 125 次交接记录 + push 4.0 + 清理 3 个已合并分支
3. 用户选「布局债：min/max clamp」启动 deepwork

**阶段 B：deepwork scene min/max clamp + percent + margin + align-self（长任务）**
4. 主 Agent 亲读 NORTH_STAR §偏离 2026-06-20 确认债范围
5. 并行派 explorer（侦察代码现状）+ librarian（调研行业 min/max/percent/margin/align-self 实现）
6. explorer 确认 min/max/percent/margin/alignSelf 全是空白区；路径甲（父级数值求解器内闭环）可守 I7
7. librarian 调研 Qt/Yoga/Flutter/CSS/Taffy/GPUI；核心判定：maxHeight 是声明式元数据 → 路径甲可行
8. 派 oracle 起草精化方案（8 项裁决）+ 用户拍板 5 个决策点
9. 用户拍板：I7 补注纳入 / min 赢 CSS / 上界+下界对称 freeze / 容器 maxHeight 收窄 / percent 也排期
10. 开 refactor/scene-min-max-clamp 分支 + 建 deepwork 进度文件
11. 主 Agent 亲改 NORTH_STAR（I7 补注 + 偏离登记）→ 提交
12. **一期 min/max clamp**：fixer 5 项代码 + 12 测试 M1-M12 → reviewer 有条件通过
    → 6 建议级处理（注释修正 + NORTH_STAR scope + 边界 2 登记 + 补测试）→ 合回 4.0
13. **二期 align-self**：fixer AlignSelf 枚举 + effectiveCrossAlign + STRETCH maxWidth clamp
    （边界 2 回填）+ 9 测试 A1-A9 → reviewer 有条件通过 → 守卫精度修复 + 补测试 → 合回 4.0
14. **三期 margin**：fixer 两个 commit（字段 + 五处联动）+ 11 测试 G1-G11
    → reviewer 有条件通过 → 3 修复（computeShrinkContainerWidth 含 marginH / null 守卫 /
    maxScrollY 含 marginBottom）+ 补测试 → 合回 4.0
15. **四期 percent**：fixer percent 字段 + grow 求解器 percent 分支 + computeHeight/Width
    + STRETCH 豁免 + 12 测试 P1-P12 → reviewer 有条件通过 → 2 修复（maxHeight clamp +
    内容撑大下界）+ 注释精确化 + 补测试 → NORTH_STAR 全部还清 → 合回 4.0

### 关键架构成果

1. **NORTH_STAR §偏离 2026-06-20 剩余债全部还清**：四期落地，status 升级为「已还清」
2. **I7 数值求解器边界澄清补注**：明确「父级数值求解器内多轮迭代 ≠ 多 pass，
   单 pass 约束的是父→子约束下沉次数」，为 freeze do-while 守 I7 提供宪章依据
3. **freeze do-while 撞顶重分配（路径甲）**：computeColumnGrowHeights 内 freeze do-while
   （上界+下界对称，Qt qGeomCalc 语义），全程只读静态元数据，不读子 cache，守 I7
4. **四个新字段族**：maxHeight/maxWidth、percentHeight/percentWidth、margin 四向、alignSelf
5. **clamp 优先级**：`max(preferredHeight, min(natural, maxHeight))`（min 赢 CSS 语义）
6. **align-self 独立枚举方案 B**：AlignSelf（AUTO/START/CENTER/END/STRETCH），
   effectiveCrossAlign 回退在 FlexLayouter
7. **margin 五处联动**：SizingCalculator + ConstraintResolver + FlexLayouter + SceneGeometry
   （maxScrollY 含 marginBottom）
8. **percent 子隐式 fill**：与 grow 子隐式 fill 对称（isHeightConsumingConstraint + computeHeight
   条件同步扩展），grow 优先 + maxHeight clamp + 内容撑大下界

### 仍登记的债（非本次引入）

**布局算法剩余债**：
- 嵌套 grow 子容器场景（容器 X 是父的 grow 子但非 fill 时，X 自身 priorKnownInnerHeight
  返 UNCONSTRAINED 致 X 内 grow 子回退 shrink）——reviewer 建议级已知边界，待真实需求触发再扩展
- childConstraintsWouldChange 逐子调 buildChildConstraints 叠加每子求解使脏判定为 O(n²)
  ——freeze do-while 会进一步加重，待性能暴露再评估记忆化
- percentHeight 在 ROW 容器下不生效（仅 COLUMN 主轴生效，ROW 下被当作普通 fill 子处理）
  ——有意边界，字段 Javadoc 已明确

**预存非布局债**：
- B6 FBO 纹理脏标记跨帧复用（I7 合成阶段偏离，待性能暴露后启动）
- B6 hit-test 对偶（SceneHitTester 对 transform 零感知，待交互需求触发）
- A1 effect 内 set 慢一帧残留
- chrome 主题层（P2 大工程未立项）
- Phase 5 旧栈退役（阻塞中）

### 当前分支与工作区

- 当前工作分支：`4.0`，工作区干净
- 4.0 当前比 origin/4.0 领先 14 commits（本会话新增）
- 本会话 4 个开发分支已合回 4.0 并保留：
  - `refactor/scene-min-max-clamp`（一期）
  - `refactor/scene-align-self`（二期）
  - `refactor/scene-margin`（三期）
  - `refactor/scene-percent`（四期）

### 下个会话任务表（用户决定优先级）

1. **可选：push 4.0 到远端**（用户拍板时机）
2. **可选：清理 4 个已合并的开发分支**（`git branch -d` 四分支）
3. **可选：启动嵌套 grow 子容器场景扩展**（待真实需求触发）
4. **可选：启动非布局债某项**（B6 FBO 纹理缓存 / B6 hit-test 对偶 /
   A1 残留确认 / chrome 主题层）
5. **可选：用户自定新方向**

### 已知边界（沿用 + 本会话新增）

- 本会话新增文档：`.slim/deepwork/scene-min-max-clamp.md`（deepwork 进度文件）
- 本会话新增字段族：maxHeight/maxWidth、percentHeight/percentWidth、margin 四向、alignSelf
- 本会话新增枚举：AlignSelf（`club.heiqi.uilib.ui.scene.layout` 包）
- **freeze do-while 收敛性**：三重退出条件（!newlyFrozen.isEmpty && !active.isEmpty
  && remainingW > 0），每轮至少冻结 1 个，最多 n 轮，数学上必收敛
- **clamp 优先级**：max(preferredHeight, min(natural, maxHeight))，min 赢 CSS 语义
- **percent 子作固定子**：grow 优先 + maxHeight clamp + 内容撑大下界 max(pctH, priorKnownChildHeight)
- **percent 子隐式 fill**：isHeightConsumingConstraint + computeHeight fill 分支条件含 percentHeight>0
- **percentHeight 仅 COLUMN 主轴生效**：ROW 下不生效，被当作普通 fill 子处理
- **margin 五处联动**：改任一处需同步审视其他四处（SizingCalculator computeContentHeight/
  computeShrinkContainerWidth + ConstraintResolver computeColumnGrowHeights/buildChildConstraints
  + FlexLayouter positionChildren + SceneGeometry maxScrollY）
- **STRETCH 豁免**：preferredWidth>0 / SHRINK / percentWidth>0 三类豁免 + maxWidth clamp
  （stretched boolean 守卫精确区分被改写 vs 豁免子）
- **align-self 不进约束下传**：effectiveCrossAlign 只在 FlexLayouter.positionChildren 消费，
  不影响 buildChildConstraints / childConstraintsWouldChange

### 验证状态

- 本会话每期 jetbrainsBuildProject 编译零问题
- 每期全套 scene 测试零回归（最终 133 SceneLayoutEngineTest + 714 全套 scene 测试全绿）
- 每期独立 reviewer 审核通过（四期均有条件通过 + 建议级全部处理）
- oracle 裁决 8 项 + 用户拍板 5 项，方案可落地性已验证

---

## 重要纪律（长期沿用）

- 帧率/真机实测必交用户跑，沙箱无 GUI
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做；
  但 opencode 重启后原 session 失效，按"调度开销明显大于收益 + 信息已在上下文"
  白名单主 Agent 可亲自做单点排错
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑，避免并行 Gradle 竞争 build/ 目录
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，
  改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能
  登记到正确 Owner
- token 实际值以 SceneChromeTokens.java 源码为准，不依赖过期文档清单
- oracle 裁决架构方案后，如方案涉及长期可引用的设计决策，建议补写
  `docs/记忆/决策/` 文档沉淀
- I11 逃生舱②扩张纪律：扩之前证明它确实是一类新的权威交互真值，
  而非可以用现有机制表达的东西（B8 验证了 hover 重算不需要扩②）
- 诚实记录 oracle 方案缺口：oracle 不是神谕，发现 bug 诚实记录修复
- **I7 数值求解器边界**：父级数值求解器内多轮迭代（freeze do-while）≠ 多 pass，
  单 pass 约束的是父→子约束下沉次数；判据是「求解器若需要先 layout 子、读子结果、
  再回头改父分配即违反 I7；若全程父级数值迭代、子在收到最终约束后才首次 layout，则守 I7」
- **margin 五处联动纪律**：改 margin 任一处需同步审视 SizingCalculator/
  ConstraintResolver/FlexLayouter/SceneGeometry 四协作者
