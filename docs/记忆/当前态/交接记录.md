# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；最近批次分支 `add/dirty-subtree-flex-reuse` 已用于 HTML-like 脏子树 flex 子树复用并合并回 `4.0`
- 已完成：HTML-like 脏子树布局缓存可复用静态 `display:block` / `display:flex` / `display:none` 未脏子树；节点版本、子树版本、文本测量 epoch、containing 宽高和 forced size 不变时，`containingLeft` / `flowTop` 可变化并通过整体平移复用上一轮盒树
- 已完成：flex 子树复用沿用现有可平移协议与 absolute/fixed 后代排除规则；`HtmlLikeDocumentWidget` 仍只在静态重排传入上一轮 `cachedLayoutBox`，layout 动画运行态不复用
- 本批新增测试：`HtmlLikeDocumentWidgetTest` 覆盖 row flex 主轴 grow 与交叉轴居中、column flex item 主轴尺寸分配、flex 主轴/交叉轴 auto margin，以及 absolute/fixed 后代保守边界；row flex 用例还覆盖匿名文本 flex item 不重复测量
- 当前边界：仍不复用含 absolute/fixed 盒的子树、transform fixed containing block 子树、table/inline 子树和 layout 动画运行态；后续 table/inline 必须独立补测试后再扩展，详见 `docs/记忆/决策/DECISION-20260606-dirty-subtree-layout-cache.md`
- 验证：已通过 `HtmlLikeDocumentWidgetTest`、`DocumentPaintEngineTest`、`UiDocumentTest`、`DocumentEventPropagationTest`、`UiStyleResolverTest`、`compileJava` 与 `git diff --check`；本批未跑 `runClient21`
- 注意：同一工作区内 Gradle 命令必须串行，禁止并行跑 `compileJava` / `test`

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
