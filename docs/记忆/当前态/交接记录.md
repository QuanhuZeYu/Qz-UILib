# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-20 第 45 次）—— C 任务完成，待提交，下一步批 1 Checkbox/Toggle

### 一句话现状
**C 任务（transform 完整矩阵方案甲，偏离③还债）已实现并通过三命门验真，工作树干净，等待 git 提交后即可开始批 1 Checkbox/Toggle 迁移。**

### 当前分支与已提交基线
- 分支 `refactor/scene-phase4-control-layer`。已提交 5 个干净提交（含本次 C 任务，hash 见 git log）。
- **工作树**：文档改动尚未提交（记忆文档 + fixer 改动源码 + 测试），git commit 后将全部入库。

### C 任务实现摘要（三命门已主 Agent 独立验真）
- `Transform.java`：7 字段全 final 浮点 + isIdentity + equals/hashCode + 工厂。
- `PaintCommandType.java`：PUSH_TRANSFORM / POP_TRANSFORM + Javadoc。
- `PaintCommand.java`：7 个 transform primitive 字段（含 translateX/Y）+ pushTransform / popTransform 工厂 + equals/hashCode + 7 个 getter。
- `PaintPlan.java`：addPushTransform / addPopTransform 方法。
- `ScenePaintEngine.java`：删旧 D2 整数量化通路，改产 PUSH/POP_TRANSFORM 边界命令（最外层包 opacity/clip，严格配对）。
- `ScenePaintReplayer.java`：映射两命令调 ctx 纯数值重载，修订类注释红线。
- `UiRenderContext.java`：新增纯数值 pushTransform 重载（全 primitive，I6 让步）。
- `SceneTransformCompositeTest.java`（新建）：T1-T6 全绿。
- `ScenePaintCompositeReplayTest.java`：改写适配方案甲语义（translate 不进命令坐标，走 PUSH_TRANSFORM）。
- **验证**：编译 0 problems；scene 全套 316 测试零回归；9 个旧栈预存失败（DocumentPaintRendererTest 等）与本改动无关（基线对比确认）。

### 接手会话执行顺序（批 1 Checkbox/Toggle 迁移）
1. 确认 C 任务提交已入库（`git log --oneline -6`），分支 `refactor/scene-phase4-control-layer`。
2. **批 1 oracle 契约**（已由前期 oracle 产出，开工无需重新评估）：
   - 控件模式：受控组件，`ReadableSignal<Boolean> checked` 入 + `Consumer<Boolean> onChange`（传期望新值）出，零内部状态，守 R1/R2/R4/I11。
   - 本期只做二态 Checkbox + 二态 Toggle（indeterminate/滑块动画排后续批）。
   - demo 用新建 `SceneControlsDemoScreen`，不在旧 Controls 组原地混用。
3. 落地 `SceneCheckbox`（check box 16×16 固定尺寸 + 文字 label + hitTestable 穿透 + 四态 PAINT 切换）。
4. 落地 `SceneToggle`（track 48×24 + thumb 18×18 + 四态）。
5. 顺手补 R6 边界一句（第一个控件落地时）：交互态 signal 必须绑 hitTestable=true 命中单元节点。
6. 新建 `SceneControlsDemoScreen`，切换入口到 scene 栈（单点开关），旧控件留回退基线。
7. `jetbrainsBuildProject` + `cleanTest test` 全绿后交用户真机验收（帧率/上屏）。

### 重要纪律（长期沿用）
- 决策点优先用中文 `question` 向用户拍板；帧率/真机实测必交用户跑，沙箱无 GUI。
- 编译/测试优先走 JetBrains MCP（见 `AGENTS.md` 第 8 节，shell gradle 曾卡死）；仍需 shell 时用 `$env:GRADLE_USER_HOME="D:\.MyApps\.ENV\gradle-home"; ./gradlew.bat ...`（.MyApps 双 p）。
- PowerShell 不支持 `&&`，链式用 `;` 或 `cmd1; if ($?) { cmd2 }`。
- 提交/合并用 `git commit -F <文件>` / `git merge --no-ff <分支> -F <文件>` + UTF8 无 BOM。**注意：PowerShell 5.1 的 `Set-Content -Encoding utf8` 会写 BOM，必须用 .NET `[System.IO.File]::WriteAllText($f,$c,(New-Object System.Text.UTF8Encoding $false))` 写无 BOM；本会话两次提交都因 Set-Content BOM 被 amend 修过**。
- git 操作必须如实读取 EXIT 码 + 真实 stdout，不预判、不编造命令成功输出。

## 更新规则
- 只保留最近一次交接的当前真相，历史交接直接覆盖，不按次数累积。
- 施工逐行细节沉淀到决策/审查/错误文档，本文件只留「现状 + 下一步 + 边界」。
