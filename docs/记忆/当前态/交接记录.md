# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；本轮在 `refactor/html-widget-test-remaining-splits` 分支完成 `HtmlLikeDocumentWidgetTest` 剩余主题拆分，并按 `--no-ff` 合回主分支
- 拆分结果：主类只保留基础后端契约；渲染/文本/图片 fallback 迁移到 `HtmlLikeDocumentWidgetRenderingTest`，基础命中迁移到 `HtmlLikeDocumentWidgetHitTest`，HUD 风格运行态布局迁移到 `HtmlLikeDocumentWidgetHudRuntimeTest`，click/dblclick/contextmenu/link activation/active/hover/mouseup 迁移到 `HtmlLikeDocumentWidgetEventDispatchTest`
- 变更边界：只做测试组织搬家，未修改生产代码语义，未新增断言目标
- 验证状态：已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.document.HtmlLikeDocumentWidget*Test"`、`./gradlew.bat --no-configuration-cache compileJava`
- 继续开发建议：`/qzuilib test` 下一步可恢复 TextFont 视觉矩阵；如继续整理测试结构，优先评估其他大测试类，不再向 `HtmlLikeDocumentWidgetTest` 堆叠新主题

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
