# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 已完成：参考 `D:\CodeSpace\Rust\Harmes-Rust` 的仓库内分层记忆框架，确认其核心为 `AGENTS.md` 规则层、`docs/记忆/当前态/` 当前态层、`docs/记忆/长期事实/` 长期事实层、`docs/记忆/决策/` 决策层
- 已完成：本项目已迁移同类 `docs/记忆/` 目录结构，并保留原有 `docs/使用文档/`、`docs/开发者文档/errors/`、`docs/开发者文档/reviews/` 专项沉淀职责
- 已完成：`docs/AI记忆文档.md` 已调整为总导航，不再承载大段项目事实和阶段流水账
- 已完成：本项目长期事实已拆入 `长期事实/项目结构.md`、`长期事实/架构边界.md`、`长期事实/稳定命令.md`、`长期事实/项目约定.md`
- 已验证：本次为文档与协作规则迁移，已运行 `git diff --check`，未触发 Java/Gradle 编译
- 已确认：本次只迁移 Agent 协作记忆框架，不迁移 Harmes-Rust 的 Rust / egui 桌面 Agent 产品形态
- 下一步：后续开发任务按 `docs/AI记忆文档.md` 的读取顺序进入分层记忆文件，并在任务结束时检查是否需要写回对应层级

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
