# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；当前工作分支：`add/runtime-test-pages-followup`
- 已完成：旧 test 子页仍保持清空；`/qzuilib test` 首页改为语义分组导航；运行时测试卡片迁移到类型二级页；DOM / CSS / Layout / Paint / Input / Controls / TextFont / Animation / RuntimeHost / RemoteNet 全 10 个二级页各已接入 5 张卡片和 `执行自动测试`、`人工通过`、`人工失败` 按钮，结果会写回卡片状态、总览计数和最近失败摘要；Controls 和 Animation 卡片已增强为操作页面上的真实演示控件/动画，不再只新建离屏对象读摘要
- 当前设计：新 test 体系按 DOM、CSS、Layout、Paint、Input、Controls、TextFont、Animation、RuntimeHost、RemoteNet 分组；首页只做导航和摘要；运行时用例必须直接展示 `预期结果：...` 文本；Controls text/password/number/textarea 会自动断言 value、caret 可见性、非法字符过滤和 textarea 软换行；Animation 用例会启动真实 `animate(...)` 并断言运行态；Paint 视觉/命中类、`LAYOUT-005` baseline、TextFont fallback/reload、RuntimeHost 和 RemoteNet 服务端链路仍保留人工确认；textarea 删除换行崩溃已修复，见 `docs/开发者文档/errors/ERROR-20260602-textarea-stale-visual-line-cache.md`；input 空值高度与 textarea 光标坐标混用已修复，见 `docs/开发者文档/errors/ERROR-20260602-form-control-input-textarea-caret.md`
- 验证状态：当前测试全部通过；input 空值高度与 textarea 光标坐标修复已通过在线目标 `DocumentTextInputControlTest` / `DocumentTextAreaControlTest`、在线目标 `DocumentLayoutEngineTest`、在线 `compileJava` 与 `git diff --check`
- 下一步：进入游戏内复查 `/qzuilib test` Controls / Animation 二级页视觉效果是否与自动状态一致，并继续 TextFont fallback/reload、RuntimeHost、RemoteNet 服务端链路人工项；随后继续 `inline-block baseline` 或属性选择器 `[attr]` / `[attr=value]` 等 B 类缺口
- 文档流程补充：审查误报或结论失效时，必须在原始审查正文添加 `后续复核（YYYY-MM-DD）` 批注，并同步更新 reviews 索引，避免后续 Agent 只读原报告重复审核

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
