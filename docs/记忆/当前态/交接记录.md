# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-25 第 94 次）—— 基础控件 primitive 化 5 批完成 + 真机验收通过

### 一句话现状
无样式 primitive 基础层重构（第一轮 TextInput/Select/DataTable/flat/overlay 保护节点）+
基础控件 primitive 化 5 批（Toggleable/Button/SingleSelect/Slider/Tab）全部完成，
第一轮已合回 4.0，第二轮在 `refactor/scene-primitives-batch-2` 分支待合回。
5 批真机验收无异常。真机反馈：Tab 聚焦组件缺视觉信号，需强化（下个会话处理）。

### 本会话关键 commit（按主题）

**第一轮（已合回 4.0，merge commit `4ad4ca49`）**
- `401e0ba7` SceneTextInputPrimitive 抽离
- `d51f79d0` SceneSelectPrimitive 抽离（Oracle 回调注入方案）
- `4046a27e` DataTable 编辑列迁移 primitive + 输入槽视觉提示
- `bf3a96ba` Select toggle 跨帧修复（primitive 补丁，后回退）
- `7dfdd692` overlay 保护节点治本（一次性解决整类问题）
- `55e4ed6` flat 战术过渡补丁回收
- `70695fe3` 跳过 review 纪律错误记录

**第二轮（分支 refactor/scene-primitives-batch-2，未合回 4.0）**
- `38ac2e67` Primitive API 规范文档
- `1be811a0` scene 统一配色方案设计文档
- `26d221d` 批 A SceneToggleablePrimitive（Checkbox+Toggle）
- `4c7bac8` 批 B SceneButtonPrimitive（Button）
- `82f278f0` 批 C SceneSingleSelectPrimitive（RadioGroup+Segmented）
- `edd286e8` 批 D SceneSliderPrimitive（Slider）
- `e9c83db9` 批 E Tab 复用 SingleSelectPrimitive
- `c0763b29` 记忆文档更新

### primitive 化全景（7 个 primitive + 1 个消费者）
- `SceneTextInputPrimitive` / `SceneSelectPrimitive`（第一轮）
- `SceneToggleablePrimitive`（批 A，Checkbox/Toggle 共享双向布尔行为）
- `SceneButtonPrimitive`（批 B，Button 交互行为，支撑未来 IconButton）
- `SceneSingleSelectPrimitive`（批 C，RadioGroup/Segmented 共享 N选1 受控头，含 orientation + HOME/END）
- `SceneSliderPrimitive`（批 D，拖拽/键盘步进/progress 派生）
- Tab（批 E，消费者，复用 SingleSelectPrimitive 做 tabBar + 自己管内容区 show）

### 真机验收结论
- 5 批 primitive 化真机无异常，视觉与改动前一致，无交互回归
- **Tab 聚焦视觉信号缺失**：tab 聚焦的组件没有视觉信号，需强化（下个会话处理，本轮不做）

### 下个会话任务表
1. **Tab 聚焦视觉强化**：真机反馈 tab 聚焦组件缺视觉信号。需 designer 出方案 + fixer 实现。
   SceneTab 当前 tab 段三态（active/inactive/pressed）无 hover/focus 视觉，
   聚焦时无高亮边框/caret/底色变化。考虑加 focus 边框或底色。
2. **合回 4.0**：第二轮 `refactor/scene-primitives-batch-2` 分支待合回（用 `git merge --no-ff`）
3. **统一 chrome（独立轨）**：designer 配色方案已存档
   (`docs/开发者文档/specs/scene-chrome-color-spec.md`)，6 项待拍板后执行。
   扩展 SceneChromeTokens，全部 wrapper 改引用 token。
4. **后续恢复主线**：嵌套分类树导航 / 字段引擎最小切片；高优先级缺口 TextArea。

### 已知边界
- 基础控件 primitive 化仅覆盖 7 个基础控件 + Tab 消费者，Breadcrumb/DataTable/KeyValueMap/ObjectField/SimpleList 维持单文件（复合控件，不适合再抽 primitive）
- primitive API 规范已落地 (`docs/开发者文档/specs/scene-primitive-api-spec.md`)
- wrapper 延迟建树模式（return () -> {}）是 B1 教训硬约束，所有 wrapper 必须遵守
- SceneSelectPrimitive 的 ListboxChrome 回调注入是动态节点（overlay 延迟创建）的必要偏差，与静态节点模式 A 互补
- 旧栈 ui.dom 仍完整存在，Phase 5 暂缓

### 当前分支
- `4.0`：第一轮已合回（merge commit `4ad4ca49`）
- `refactor/scene-primitives-batch-2`：第二轮 8 个 commit，未合回 4.0

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能登记到正确 Owner。
