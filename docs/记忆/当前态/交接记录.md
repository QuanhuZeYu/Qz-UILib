# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- **当前分支**：`docs/modern-config-template-plan`
- **当前任务**：Modern Config 模板配置页已完成 Batch 3 嵌套结构模板，下一位从 Batch 4 动态 map 与预设模板开始施工
- **工作区状态**：Batch 3 实现随本轮提交；本轮结束需保持工作区干净
- **验证状态**：已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test --tests club.heiqi.uilib.config.ModernConfigNestedPropertyBindingTest --tests club.heiqi.uilib.ui.control.DocumentTreeViewControlTest --tests club.heiqi.uilib.ui.control.DocumentBreadcrumbControlTest`、`./gradlew.bat --no-configuration-cache compileJava`

### 本轮进度详情

- Batch 3 完成：新增 `DocumentTreeViewControl`，支持树节点折叠、展开、当前节点高亮和路径选择回调。
- 新增 `DocumentBreadcrumbControl`，按当前路径渲染根配置到目标节点的路径段，并支持点击路径段回跳。
- 新增 `ModernNestedCategoryBinding`，把现代配置根 map 组织为树形导航 + 面包屑 + 当前节点内容区，并聚合子绑定的脏状态、校验、恢复和保存。
- 新增 `ModernObjectPropertyBinding`，普通 map 以卡片内联渲染直接子项，默认内联深度限制为 5；超深层级显示“展开编辑”并交由树形导航处理。
- Batch 3 同步把 `ModernConfigTypeInference` 的 map 推断为 `OBJECT`；动态 map、预设、源码编辑、增强选择器和搜索过滤仍未实现。

### 下一步操作

1. 从 Batch 4 开始：实现动态 map 与预设模板。
2. 新增 `DocumentKeyValueEditorControl`，支持 key/value/type 三列编辑。
3. 实现 `KeyValueMapPropertyBinding`，只处理明确声明为动态 map 的路径，避免和 Batch 3 普通对象模板冲突。
4. 实现 `PresetSelectorPropertyBinding`，约定 `_presets` 或 `presets` 存储预设定义；选择预设时批量写入目标字段，偏离预设时显示已修改标记。
5. Batch 4 不要做 Forge→Config 迁移、源码编辑、增强选择器或搜索过滤；动态 map 和普通 map 的识别边界要保持清晰。
6. Batch 4 结束后运行 `git diff --check`、动态 map/预设相关测试或 `compileJava`，提交 git，并更新本交接记录给出 Batch 5 提示词。

### 重要约束

- 入口检测的是 `club.heiqi.config` 模块能力是否存在，不是检测配置文件是否存在。
- `ModConfigGui` 不直接引用 `club.heiqi.config` 类型；现代页相关类型只在 `ModernConfigBridge` 之后加载。
- 不做 Forge 到 config 模块迁移工具，不在页面内添加迁移按钮。
- Forge 回退不需要理解复杂 config 模型；接入方自己决定复杂结构如何在 Forge cfg 下兼容，推荐 JSON 字符串属性。
- UI 风格沿用当前 Forge 配置模板页，不另起视觉体系。
- `MutableConfig` 裸值本身不包含 schema；choice、默认值、数值范围等 UI 语义依赖 `ModernConfigTemplateScreen.FieldSpec`。
- Batch 2 表格模板只覆盖稳定列 primitive map list；动态 key 集合仍留给 Batch 4。
- Batch 3 普通 map 走 `ModernNestedCategoryBinding` / `ModernObjectPropertyBinding`，递归内联默认 5 层；超深层级通过树形导航或“展开编辑”处理。

### 下一位 Agent 提示词

```text
你接手 Qz-UILib 的 ModernConfigTemplateScreen 分阶段施工。先阅读 AGENTS.md、docs/AI记忆文档.md、docs/开发者文档/specs/modern-config-template-screen-plan.md、docs/记忆/决策/DECISION-20260613-modern-config-template-optional-module.md、docs/记忆/当前态/交接记录.md。当前应从 Batch 4 开始，不要重做 Batch 0/1/2/3。Batch 4 只做动态 map 与预设模板：新增 DocumentKeyValueEditorControl，支持 key/value/type 三列编辑；实现 KeyValueMapPropertyBinding，只处理明确声明为动态 map 的路径，避免和普通对象模板冲突；实现 PresetSelectorPropertyBinding，约定 _presets 或 presets 存储预设定义，选择预设时批量写入目标字段，偏离预设时显示已修改标记。不要做 Forge→Config 迁移，不要实现源码编辑、增强选择器或搜索过滤。保持 UI 风格与当前 ForgeConfigTemplateScreen 一致。完成后运行 git diff --check、动态 map/预设相关测试或 compileJava，提交 git，并更新交接记录给出 Batch 5 的接力提示词。
```

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
