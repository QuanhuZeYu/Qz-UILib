# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-23 第 79 次）—— Scene overlay P0 地基四段已完成

### 一句话现状
overlay P0 地基四段（纯数据类+anchor resolver、SceneRuntime.portal、SceneInputRouter overlay 优先命中+dismiss、SceneHostWidget 多 root layout/paint/replay）均已完成并通过 JetBrains 定向编译与 Gradle 定向测试；下一步实现 `SceneSelect` 首个消费者。

### 本次结论
- **P0 第一段**：新增 `ui.scene.overlay` 包（`SceneOverlayHost`/`OverlayHandle`/`SceneAnchorResolver`/`OverlayDismissPolicy`/`package-info`），扩展 `ScenePackageIsolationTest`，新增 `SceneAnchorResolverTest`/`SceneOverlayHostTest`。
- **P0 第二段**：`SceneRuntime` 新增 `portal(visible, content)` + `ScenePortalHandle`；`SceneOverlayPortalTest` 覆盖生命周期。
- **P0 第三段**：`SceneInputRouter` 新增 overlay top-first 优先命中、outside-click dismiss、ESC 栈顶 dismiss；新增 `SceneOverlayHitTestTest`/`SceneOverlayDismissTest`。
- **P0 第四段**：`SceneHostWidget` 接入多 root overlay layout/paint/replay，per-root layout engine 防串味；`SceneOverlayPipelineTest` 覆盖跨 clip 可见、overlay 后绘制、主树稳定兄弟不污染、双 overlay 独立 engine。
- **验证**：JetBrains 定向编译通过；Gradle 定向测试全部通过（8 套件）；`git diff --check` 通过。
- **未完成**：`SceneSelect` 首个消费者、R11 控件契约、demo 页面、anchor 定位接入 host pipeline。
- **暂缓范围**：搜索、树、多行文本、列表编辑、表格行内编辑、复杂 picker、raw editor、远程同步、完整 CSS/token/theme 和 Phase 5 继续暂缓。

### 已改文件
- `src/main/java/club/heiqi/uilib/ui/scene/overlay/SceneOverlayHost.java`
- `src/main/java/club/heiqi/uilib/ui/scene/overlay/OverlayHandle.java`
- `src/main/java/club/heiqi/uilib/ui/scene/overlay/SceneAnchorResolver.java`
- `src/main/java/club/heiqi/uilib/ui/scene/overlay/OverlayDismissPolicy.java`
- `src/main/java/club/heiqi/uilib/ui/scene/overlay/package-info.java`
- `src/main/java/club/heiqi/uilib/ui/scene/component/SceneRuntime.java`
- `src/main/java/club/heiqi/uilib/ui/scene/component/ScenePortalHandle.java`
- `src/main/java/club/heiqi/uilib/ui/scene/input/SceneInputRouter.java`
- `src/main/java/club/heiqi/uilib/internal/devtools/pages/SceneHostWidget.java`
- `src/test/java/club/heiqi/uilib/ui/scene/overlay/SceneAnchorResolverTest.java`
- `src/test/java/club/heiqi/uilib/ui/scene/overlay/SceneOverlayHostTest.java`
- `src/test/java/club/heiqi/uilib/ui/scene/component/SceneOverlayPortalTest.java`
- `src/test/java/club/heiqi/uilib/ui/scene/input/SceneOverlayHitTestTest.java`
- `src/test/java/club/heiqi/uilib/ui/scene/input/SceneOverlayDismissTest.java`
- `src/test/java/club/heiqi/uilib/internal/devtools/pages/SceneOverlayPipelineTest.java`
- `src/test/java/club/heiqi/uilib/ui/scene/input/ScenePackageIsolationTest.java`
- `docs/记忆/决策/DECISION-20260623-scene-overlay-foundation.md`
- `docs/记忆/决策/DECISION-20260623-scene-modern-config-foundation.md`
- `docs/记忆/决策/README.md`
- `docs/记忆/当前态/当前计划.md`
- `docs/记忆/当前态/当前上下文.md`
- `docs/记忆/当前态/交接记录.md`
- `docs/记忆/长期事实/Config模块.md`
- `docs/记忆/长期事实/架构边界.md`
- `docs/开发者文档/specs/scene-overlay-p0-plan.md`
- `docs/开发者文档/specs/README.md`

### 下一步
1. 实现 `SceneSelect` 首个消费者：trigger 在主树，listbox 经 portal 提升为 overlay root；选中/展开/关闭只写 signal；补 R11 契约。
2. 补 `SceneSelect` demo 页面验证 top-layer 与 P0 交互。
3. 再进入字段引擎最小切片与真实 `MutableConfig` 数据适配。

### 已知边界
- overlay P0 地基已完成但未提交：当前工作区有 4 个 main 改动 + 6 个新增 main/test 文件 + 3 个新增决策/spec 文档 + 7 个文档更新，均未 commit。
- `SceneSelect` 首个消费者尚未实现；R11 控件契约尚未写入 `package-info.java`。
- host pipeline 当前 overlay replay 用 host `absX/absY`，未接 `SceneAnchorResolver` 算 trigger 绝对坐标；anchor 定位接入留到 `SceneSelect` 实现时一起做。
- overlay P0 未经真机验证：跨 clip 可见、优先命中、dismiss 手感和帧率仍需 `runClient21` 人工确认。
- 旧对外现代配置页文档仍描述 DOM 实现的 12 模板能力；scene 一期完成前不得宣称能力等价。
- `NORTH_STAR.md` 是否新增 top-layer 作为"额外 paint root + 独立 hit-test 入口"的说明，涉及宪章边界扩展，需经用户确认后再改。

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
