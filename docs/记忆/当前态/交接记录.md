# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-24 第 86 次）—— 清理 reviewer 快胜档 4 项债务

### 一句话现状
reviewer 9 项非阻断建议中的快胜档 4 项（B2/B5/B6/B7）已还清并通过 reviewer 审核，
剩余 5 项（B1/B3/B4/B8/B9）可在字段引擎间隙处理。下一步进入字段引擎最小切片。

### 本轮清理概要（commit `a924d07d`）

**B7 maxScrollY 守卫**：`SceneGeometry.maxScrollY` 入口加 `!isScrollable()` 返回 0
守卫，自身与子节点 cachedLayout 加 `instanceof LayoutBox` 守卫。

**B2 cachedLayout 强转守卫**：`SceneGeometry`/`SceneHitTester`/`SceneTable` 三处
`(LayoutBox) getCachedLayout()` 改为 `instanceof LayoutBox` 守卫，失败返回 0/空命中链/跳过。
延伸清理 `SceneSelect:231` 死代码强转 + `SceneSelectHostWidget:83` 同类强转守卫。

**B6 contentBox 重命名**：`SceneTable` 中 `contentBox` → `contentNodeBox`，
消除与"内容盒 vs 视口盒"术语混淆。

**B5 反射改公开方法**：`SceneAnchoredOverlayPipelineTest` 移除
`getDeclaredField("overlayLayoutEngines")` 反射，改用已有包内方法
`AbstractSceneHostWidget.__getOverlayLayoutEngine`。

**测试注释修正**：`SceneScrollViewportTest` maxScrollY 断言描述改为反映
`!isScrollable()` 真实命中路径，补 `scrollable=true` 但 `box==null` 用例。

### 验证
- `compileJava` + `compileTestJava` 通过
- `SceneScrollViewportTest`/`SceneAnchoredOverlayPipelineTest`/
  `SceneOverlayPipelineTest`/`SceneHitTesterTest`/`SceneTableTest`/
  `SceneSelectTest` 全绿
- reviewer 审核 PASS，NORTH_STAR I7/I8/I10/I11 守住
- `git diff --check` 通过

### 剩余债务（5 项 + 2 项非阻断建议）
- B1 hit-test/replay anchor 时序窗口（P2，10-50 行）
- B3 AnchorRect 包归属下沉（P2，架构小重构，layout/input 反向依赖 overlay）
- B4 hitTest 递归内 AnchorRect 分配（P3）
- B8 测试命名略误导（P3，未定位具体方法）
- B9 CRLF warning（P3，判定无需处理）
- reviewer 建议3：守卫失败路径单测缺口（P3，防御性）
- reviewer 建议4：测试方法遗留 `throws Exception`（P4）
- A1/A2 NORTH_STAR 偏离登记待转正（需 oracle + 用户确认）

### 下一步
1. **字段引擎最小切片**：scene-native 字段 spec、draft/current、校验、
   dirty、canSave/canCancel 派生与保存语义
2. **接真实配置数据源**：优先接 `MutableConfig`
3. **处理剩余债务**（B1/B3 可在字段引擎间隙或单独一轮清理）
4. **暂缓**：搜索、树、多行文本、列表编辑、表格行内编辑、高级 picker、
   raw editor、远程同步、完整 CSS/token/theme 和 Phase 5

### 已知边界
- SceneSelect 三批缺陷已修复并通过真机验收（用户确认）
- reviewer 4 项快胜债务已还清，剩余 5 项 + 2 项非阻断建议
- 旧对外现代配置页文档仍描述 DOM 12 模板能力；scene 一期完成前不得宣称等价
- NORTH_STAR `:247`/`:248` 待 overlay/scrollable 稳定后经 oracle 评估转正
- 旧栈 ui.dom 仍完整存在（65+ 源文件），scene 包零引用，Phase 5 暂缓

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
