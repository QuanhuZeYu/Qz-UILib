# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-21 第 50 次）—— 脱离 MC 移植专项 A 组：scene 渲染出口接口化已合入母分支

### 一句话现状
为还清「脱离 MC 移植」结构债，先于批 4 Table 做了**渲染出口接口化 A 组（S1-S4）**：新建渲染后端接口 `UiRenderBackend`（10 个纯数值方法、零平台 import），`UiRenderContext implements` 它（方法体一行未动），`ScenePaintReplayer` 改持有接口类型，隔离测试收紧。**纯结构调整、零行为变更、scene 全套 378 测试零回归**，已 `merge --no-ff` 合入母分支 `refactor/scene-phase4-control-layer`（合并提交 `0e4de261`）。非宪章偏离，是信条六「换后端只重写渲染层」的正向兑现，无需登记《偏离登记》。**下一步：真机闸门验收（P2，含 fix 分支输入桥接，沙箱无 GUI 须用户跑）+ 步骤 C Table 迁移契约（P1）。**

### 当前分支与拓扑
- **母分支 `refactor/scene-phase4-control-layer`** HEAD = `0e4de261`（A 组合并提交）。提交图：
  - `0e4de261` [Merge] 合入 scene 渲染出口接口化 A 组
  - `d46aa928` [Refactor] scene 渲染出口接口化（专项 A 组，5 文件 +188/-22）
  - `835c37fb` 及更早 = 批 4 步骤 A/B 全部基线（详见 deepwork 文件）
- **A 组独立分支 `refactor/scene-render-backend-interface`**（从母分支切出，已合并回母分支，可删可留）。
- **fix 分支 `fix/scene-controls-input-bridge`** HEAD = `694618e7`（含第 49 次交接草稿 + `2b84bf67` 输入桥接修复）。**尚未合回母分支**，待真机验收输入桥接（GUI Scale=2 拖拽跟手 + 键盘输入）后按规范 `merge --no-ff` 合入。fix 分支与 A 组文件零交集（fix 改 demo pages + SceneTextInput，A 组改 render/replayer/test）。
- 工作树干净（仅未跟踪 `.slim/`、`opencode.json`，永不提交）。

### A 组成果（主 Agent 独立验真，非仅信 fixer）
- **三命门亲验通过**：① 接口纯净——亲读 `UiRenderBackend.java` 全文，10 方法签名与 UiRenderContext 逐字一致、零平台 import（只引平台无关的 `UiBorderRadiusResolver.ResolvedCornerRadii`）；② 零行为变更——`UiRenderContext implements UiRenderBackend`（:36）方法体未动，XML 实证 ScenePaintEngineTest 27/27、SceneTransformCompositeTest 8/8、ScenePackageIsolationTest 3/3；③ 隔离测试真守住——移除 ScenePaintReplayer 整文件豁免（paint 包统一扫描禁 `UiRenderContext` 子串）+ 新增 `replayerShouldDependOnRenderBackendInterfaceNotConcreteClass` 真反证断言（扫 import 行断言不含 UiRenderContext 且必须含 UiRenderBackend）。
- **接口 10 方法**：fillRect / drawSurface(含 ResolvedCornerRadii) / drawBorder / pushClip(5参含 cornerRadius) / popClip / drawText(4参) / pushPaintContext / popPaintContext / pushTransform(11参纯数值) / popTransform。只收录 replayer 实际消费的方法，未收旧栈其它重载。
- **fixer 主动改动已接受**：PaintCommand.java 2 处 javadoc link 由 UiRenderContext 对齐到 UiRenderBackend（注释同步维护，不影响编译/测试）。
- **专项规划**：见 `.slim/deepwork/scene-render-port-decouple.md`。A 组（S1-S4）已完成；S5（非 MC 参考后端演示验收）可排后；B 组（scene 平台无关图片句柄 SceneImageHandle）归**批 8** 新建工作，依赖 A 组，旧栈 HostImageSource 原样不动随 Phase 5 退役（**「HostImageSource 去 MC 化」这个提法已被 oracle 推翻**——它深嵌旧栈 86 处且即将退役，不改造）。

### 解耦现状（exp 全景侦察结论）
scene 八子包 + reactive 已实测零平台 import，有 ScenePackageIsolationTest 守红线。脱 MC 移植真实阻塞清单：
- **#1 UiRenderContext 焊 GL**（出口层最重）→ **A 组接口化已收口，scene 核心现只认 UiRenderBackend 接口**；换后端只需另写一份接口实现，scene 核心零改动。剩 S5 提供非 MC 参考后端做移植演示（可选、非阻塞）。
- **#2 HostImageSource 把 ItemStack/ResourceLocation 写进公共 API** → 归批 8 在 scene 侧新建平台无关句柄，不改旧类。
- **#3 输入源 / #4 host 壳 + BaseScreen** → 已天然隔离在适配层 `internal.devtools.pages`，按 PlatformInputSource 契约重写即可，归 Phase 5 宿主迁移。

### 下一步
1. **步骤 C Table 迁移契约（P1，可立即启动，与真机验收正交）**：oracle **新开 session** 产 Table 迁移契约 R11（exp-2 侦察料已备齐：旧栈 DocumentTableControl 命令式建树 + DocumentDataTableControl 全量重建无 keyed diff）。本期最小范围——scrollable viewport → content(COLUMN) → 表头行 + 数据行(ROW，单元格 preferredWidth 表达固定列宽)、静态行全量建树、纵向滚动复用步骤 B 地基。**排后续批**：keyed 动态行、排序、行内编辑、横向滚动、滚动条、DataTable 工具栏。**灰区待 oracle 拍板**：列宽是否只支持固定 px（旧栈支持 px/percent/auto）、单元格是否本期就支持任意子节点、borderCollapse 是否进本期。
2. **真机闸门验收（P2，沙箱无 GUI 交用户跑，不阻塞 P1）**：入口 `/qzuilib test` → SCENE_DEMO 组三 demo。重点：VIS-SCENE-002 输入框打字/退格 + Tab 切焦 + Slider 拖拽跟手(GUI Scale=2) + I-beam 光标；VIS-SCENE-003 滚轮跟手/方向/裁剪；VIS-SCENE-001 phase1-3。真机后查 `run/client/logs/fml-client-latest.log`，`club.heiqi.uilib` 应零异常。验收通过后合并 fix 分支。

### 本会话纪律（长期沿用）
- 决策点派 @oracle **新开 session** 自动裁决，不复用旧 ora session（本会话已用 ora-2 规划解耦位置 / ora-3 裁决先做解耦，下一主会话不复用）。
- 编译走 `jetbrainsBuildProject`；测试走 shell gradle `--rerun-tasks` 强制非缓存，读 test-results XML 实证（`tests=N failures=0`），不以 BUILD SUCCESSFUL 代测试计数。
- 主 Agent 独立验真命门（I6/I7/I1/I11/受控不自改/测试真反证），亲读源码，不只信 fixer/oracle 报告。
- PowerShell 不支持 `&&`；链式用 `;` 或 `if ($?)`；**注意 git 正常信息（如 merge "stopped before committing"、checkout "Switched to branch"）走 stderr 会让 `$?` 变 false**，链式 `if ($?)` 会误判跳过后续——git 编排避免依赖 `$?`，分步执行或显式判断真实状态。提交信息含中文/特殊字符用无 BOM UTF8 临时文件 `git commit -F`。
- LSP `java.lang.Record cannot be resolved` 是 Jabel record IDE 误报，jetbrainsBuildProject 通过即忽略。
- session 复用隔离：只复用当前主会话 Background Job Board 列出的 completed task_id，跨会话旧 session 一律新建。

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量（尤其 I4/I6/I7/I10/I11）自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
