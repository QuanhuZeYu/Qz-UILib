# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；当前工作分支：`4.0`；最近实现分支：`add/input-visual-tests`（已 --no-ff 合并回 4.0）
- 已完成：`/qzuilib test` Input 首轮 5 张视觉样例已接入：`VIS-INPUT-001` capture/bubble、`VIS-INPUT-002` preventDefault、`VIS-INPUT-003` wheel 默认滚动、`VIS-INPUT-004` focus-visible、`VIS-INPUT-005` keyboard/textInput
- 已完成：新增 `UiTestInputVisualFactory` 承载 Input 视觉舞台，新增 `UiTestInputAssertionRunner` 承载 Input 自动断言与人工诊断；主 runner/factory 只保留委托入口，避免继续堆叠到接近千行文件
- 当前矩阵：首轮计划 59 张，已接入 24 张，剩余缺口 35 张；CSS/LAYOUT/PAINT/INPUT 现已满覆盖（gap0）；自动 18 张、人工 6 张，focus-visible 真实焦点框和键鼠切换手感仍保留游戏内人工确认
- 验证：已通过 `./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.internal.devtools.pages.UiTestDocumentPageControllerTest"`、`compileJava` 与 `git diff --check`；本批仍未跑 `runClient21`
- 下一步：按计划继续 Controls 首轮视觉样例；如果需要确认 Input 手感，运行 `runClient21` 打开 `/qzuilib test` 的 `INPUT/VIS-INPUT-004` 做 focus-visible 截图和 Tab/鼠标切换确认
- 文档流程补充：审查误报或结论失效时，必须在原始审查正文添加 `后续复核（YYYY-MM-DD）` 批注，并同步更新 reviews 索引，避免后续 Agent 只读原报告重复审核

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
