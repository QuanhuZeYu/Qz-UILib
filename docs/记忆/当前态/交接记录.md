# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-07-01 滚动API全链路改进 P0-P4 完成，分支 `refactor/scroll-api-slim` HEAD）

### 一句话现状

滚动API全链路改进（P0-P4，C1-C6）6 commit 串行完成，作者层从 7 概念 + 手传 layout 通知降到 5 概念 + 零 layout 通知，对齐 Compose/SolidJS/Slint。Oracle 复审通过，守 I6/I1-I12/R1-R12。待真机验证 thumb 视觉 + 合回 4.0。

### 主线：滚动API全链路改进（refactor/scroll-api-slim 分支）

**6 commit 串行**（b197bc7a → f98f8480 → 本次文档收尾）：
1. **P0 layoutDoneSignal 下移 runtime**：SceneRuntime 新增 layoutDoneSignal + __bridgeLayoutEpoch，host 委托 runtime。epoch 仍归引擎（纯 int），signal 归 runtime，host 桥接。守 I6。
2. **P1 SceneScrollbar.Props 删 contentChangedSignal**：create 内改读 rt.layoutDoneSignal()，作者不再手传 layout 通知。
3. **P0 外泄消除闭环**：SceneScrollContainer attach 去 contentChangedSignal 参数，ScrollbarSpec 同步删。
4. **C1-C4 4 控件 scrollbarContentSignal → showScrollbar boolean**：语义诚实，作者只声明要不要滚动条。
5. **C5 forEach 无 keyFn 重载**：Function.identity() 做默认 key，对齐 SolidJS <For> 隐式 key。
6. **C6 scrollList 一体化方法**：一行建 container+viewport+scrollbar+forEach，对齐 Slint ListView 零配置。

**本次收尾 commit**：P3-2 SceneLayoutEngine:223 Javadoc 字段名同步（lastSeenLayoutEpoch → lastBridgedLayoutEpoch）+ 新建合并决策 DECISION-20260701-scroll-api-slim-C1C6 + 旧两份决策加取代提示 + 决策 README 索引 + 本交接记录覆盖。

### 验证

- 编译/测试：本次为文档收尾，未触碰源码逻辑，未重跑测试（前 6 commit 已验证全绿）
- Oracle 复审：通过，守 I6/I1-I12/R1-R12

### 遗留 / 待做

- **真机验证 thumb 视觉**：scrollbar thumb 几何在真机滚动下的视觉表现需用户跑实机确认
- **合回 4.0**：refactor/scroll-api-slim 分支验证通过后，用 `git merge --no-ff` 合回 4.0 主分支
- **P3-3 死 import 清理**：既有问题非本次账，另开任务
- **已知边界**：overlay 独立引擎不 bump runtime signal，当前 overlay 无 scrollbar 不触发

### 分支与工作区

- 当前分支：`refactor/scroll-api-slim`
- 最新 commit：`refactor/scroll-api-slim` 分支 HEAD（本次文档收尾，`git log -1` 取得）
- 工作区：commit 后干净
