# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前分支：`refactor/decouple-lwjgl3ify`；此前已提交 `e389d584`、`cf1bf055`、`b803f0b6`、`d502864a`，当前批次准备提交 `[Refactor]: 继续解耦 lwjglx 宿主边界`。
- 已完成输入后端解耦：`UiInputService` facade 不变，`InputEvents` 继续反射订阅；新增 `UiKeyCodes` 后，业务、控件、remote、config、devtools 和测试侧不再为键码常量 import `org.lwjglx.input.Keyboard`。
- 当前批次新增 `LwjglInputRuntime`：`LwjglxPollingInputBackend` 与 `UiHostInputCoordinator` 通过包内反射桥访问键鼠运行时，优先 `org.lwjglx`，缺失时降级 legacy `org.lwjgl`，不新增 legacy LWJGL 显式运行依赖。
- `Lwjgl3ifyInputBackend` 已移除 `org.lwjglx.Sys`；`UiHudDocumentHost` 改走 `UiHostInputCoordinator.isNativeMouseGrabbed()`；`SystemDocumentCursorHost` 已移除 `Display` 静态 import，SDL 系统光标仍只在 lwjgl3ify / LWJGLX 光标桥可用时启用，否则 no-op。
- 已验证：`git diff --check`、`Lwjgl3ifyInputBackendTest`、`UiInputRouterTest`、`HtmlLikeDocumentWidgetCursorTest`、`UiHudDocumentHostTest`、`compileJava` 通过；此前第一阶段还验证过 `publishToMavenLocal` 且产物元数据不声明 `lwjgl3ify`。
- 暂缓项：主渲染链仍是 `org.lwjgl.opengl.*` / `org.lwjgl.BufferUtils`，OpenGL、字体 atlas 和渲染依赖不要与本轮 `lwjglx` / `lwjgl3ify` 输入可选化混做。

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
