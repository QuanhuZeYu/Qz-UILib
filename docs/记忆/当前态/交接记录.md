# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；最近批次分支 `test/dirty-subtree-translation-reuse` 已用于 HTML-like 脏子树可平移复用并合并回 `4.0`
- 已完成：HTML-like 脏子树布局缓存可复用静态 `display:block` / `display:none` 未脏子树；节点版本、子树版本、文本测量 epoch、containing 宽高和 forced size 不变时，`containingLeft` / `flowTop` 可变化并通过整体平移复用上一轮盒树
- 已完成：`DocumentLayoutBox.translated(...)` 会同步平移布局输入元数据，避免连续多次平移复用后仍以旧 `layoutContainingLeft` / `layoutFlowTop` 计算差值；`HtmlLikeDocumentWidget` 仍只在静态重排传入上一轮 `cachedLayoutBox`，layout 动画运行态不复用
- 本批新增测试：`HtmlLikeDocumentWidgetTest` 覆盖普通 block-flow 平移、margin collapse、auto margin、relative/sticky 偏移，以及 absolute/fixed containing block 与 transform fixed containing block 保守边界
- 当前边界：仍不复用含 absolute/fixed 盒的子树、transform fixed containing block 子树、flex/table/inline 子树和 layout 动画运行态；后续 flex/table/inline 必须独立补测试后再扩展，详见 `docs/记忆/决策/DECISION-20260606-dirty-subtree-layout-cache.md`
- 验证：已通过 `HtmlLikeDocumentWidgetTest`、`DocumentPaintEngineTest`、`UiDocumentTest`、`DocumentEventPropagationTest`、`UiStyleResolverTest`、`compileJava` 与 `git diff --check`；本批未跑 `runClient21`
- 注意：同一工作区内 Gradle 命令必须串行，禁止并行跑 `compileJava` / `test`

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
