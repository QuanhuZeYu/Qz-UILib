# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次发布分支：`release/4.2.3` 已通过 `git merge --no-ff` 合并回 `4.0`，`4.0` 与 `origin/4.0` 已同步
- 当前最新版本：`4.2.3` 已发布，tag 目标提交为 `b2b76495`，GitHub Release workflow #68 成功，Release 标题为 `4.2.3`
- 发布范围：包含 `DocumentSelectControl` 大列表虚拟化、默认滚轮/滚动条拖拽后的元素 `scroll` 事件分发、相关回归测试、使用文档和决策记录
- Release 正文：来自 `.changelogs/4.2.3.md`，只包含本次更新与兼容性说明；按用户要求不再向远端 Release 正文发布本地验证板块
- 本地验证：已通过 `git diff --check`、select/scroll 定向测试、`compileJava` 与 `publishToMavenLocal`；验证结果只记录在本地协作语境，不写入 release 正文
- 当前仍需注意：select 仍是传统单选控件，不提供搜索、过滤、分组或可变高度 option；需要快速定位超长候选时仍优先接 `DocumentAutocompleteInputControl`

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
