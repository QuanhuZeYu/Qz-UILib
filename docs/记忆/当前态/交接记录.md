# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次工作分支：`add/input-dropdown-autocomplete`；目标是新增通用 HTML-like 自动完成输入控件，服务后续字体名补全并保持可复用于其他配置/业务输入场景
- 已完成范围：新增 `DocumentAutocompleteInputControl`、`DocumentAutocompleteSuggestionProvider`、输入变更事件/handler、候选选择事件/handler；控件复用 `DocumentTextInputControl` 输入能力，候选面板按 `listbox` top-layer 弹出，支持固定 options、自定义 provider、空 query 开关、候选数量上限、鼠标选择、Up/Down/Enter/Escape、disabled 阻断和长列表滚动
- 运行时边界：`HtmlLikeDocumentWidget` 的 top-layer listbox 锚定逻辑已从 select 扩展到带内部锚定标记的 input/listbox 弹层，用于 autocomplete popup 在布局/transform 场景下跟随输入框视觉边界，避免误锚定普通 listbox top-layer
- 文档状态：已更新 `docs/使用文档/02-控件/基础控件.md`、`docs/使用文档/v4.x-LTS-稳定API清单.md`、`docs/使用文档/01-入门/项目定位与能力边界.md`、`docs/记忆/长期事实/架构边界.md`、`docs/记忆/当前态/当前上下文.md`、`docs/记忆/当前态/当前计划.md`
- 验证状态：已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.control.DocumentAutocompleteInputControlTest"`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.control.DocumentSelectControlTest"`、`./gradlew.bat --no-configuration-cache compileJava`
- 当前仍需注意：修复完成后按用户最新要求可提交并通过 `git merge --no-ff` 并入 `4.0`；`stash@{0}` 未恢复也未改写

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
