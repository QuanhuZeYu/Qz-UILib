# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；`HtmlLikeDocumentWidgetTest` 第一批拖拽拆分已完成并合并回主分支
- 已落地：新增同包 `HtmlLikeDocumentWidgetTestSupport` 承载共享 helper/fixture；新增 `HtmlLikeDocumentWidgetDragTest` 承载拖拽主题测试
- 已迁移测试范围：`shouldDragFixedElementThroughDocumentDragSupport` 到 `shouldExposeNativeDocumentCoordinatesForHtmlLikeDragEvents`，共 9 个拖拽相关测试；测试内容和断言语义保持不变
- 本轮验证已通过：`git diff --check`、`HtmlLikeDocumentWidgetTest`、`HtmlLikeDocumentWidgetDragTest`、`compileJava`
- 后续建议：继续按主题小批量拆 `FocusAndKeyboard`、`Scroll`、`LayoutCache`、`AnimationRuntime`、`EventDispatch`，每批只做测试组织搬家，不改生产代码语义
- 背景状态：Controls 视觉样例已接入并修复 `VIS-CTRL-005` table 同步；`/qzuilib test` TextFont 下一批可在测试拆分阶段性收口后恢复

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
