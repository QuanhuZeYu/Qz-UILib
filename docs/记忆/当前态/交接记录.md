# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-25 第 109 次）—— 控件审核 P0+P1 批次修复合回 4.0

### 一句话现状

四组 explorer 并行审核 25 个 scene 控件，发现 1 P0 + 13 P1 + 10+ P2。
批1-2 + P1-B 修复（SceneTable 退役 + disabled 焦点 + Tab 校验 + caret 兜底 + chrome 收口 + TextArea 范式对齐）已合回 4.0，工作区干净。

### 本会话关键 commit（已合回 4.0）

| commit | 内容 |
|--------|------|
| `488af5a4` | [Fix]: 批1 P0+P1（SceneTable 退役 + disabled 焦点 + Tab 校验 + caret 兜底） |
| `65d07c09` | [Fix]: 批2 P1（Breadcrumb/Select chrome 收口 + SimpleList 按钮 focusable 对齐） |
| `b52f77a3` | [Refactor]: P1-B TextArea caret 上色对齐 TextInput + 文本三态色补齐 |
| merge | [Merge]: 控件审核 P0+P1 批次修复 |

### 审核关键结论

- **P0 SceneTable 退役**：三违 I1/I5/I7，已被 SceneDataTable 取代，业务零依赖，用户拍板删除
- **P1-A disabled 焦点**：FocusManager 不检查 enabled，primitive 无条件 focusable → 新增 signal 驱动 focusable(Node, enabledSignal) 重载，9 控件改传 props.enabled()，5 验收测试
- **P1-B TextArea 范式对齐**：oracle 裁决"primitive 暴露只读状态 + wrapper 供颜色 token，禁止可写颜色 signal 反灌"，TextArea 删 caretColor 反向注入，补齐文本三态色
- **P1-D Tab 越界**：Props 紧凑构造器 fail-fast 校验
- **P1-E chrome 收口**：Breadcrumb 6 硬编码→linkBackground/linkText 查表，Select item→listItemBackground 查表
- **亮点**：Button 系 primitive+wrapper 范式最干净，四态零重排 R-D 测试模式，DataTable 是复合控件范式标杆

### 验证状态

- jetbrainsBuildProject 全量编译通过
- scene control + input 测试全绿（含新增 SceneFocusableDisabledTest 5 + Breadcrumb focus 1 + Tab 校验 1 + TextInput caret 1 + TextArea 色彩 3）
- **待真机验收**：disabled 焦点视觉 + Breadcrumb focus 态 + TextArea 文本三态色

### 当前分支

- `4.0`：主分支，审核修复已合入，工作区干净
- `fix/p1-batch1-audit-cleanup`：已合并，可清理

### 下个会话任务表

1. **剩余 P1**：行内编辑器 enabled/readOnly 全栈写死（DataTable/SimpleList/KeyValueMap/ObjectField）/ "谁负责 signal.set"文档 / Owner 回归断言 / 3 primitive 独立测试
2. **P2 改进项**：Props Builder 一致性 / chrome 收口（复合控件各自色值常量）/ 行高常量收口 / DataTable O(n) 优化
3. **已知 bug**：forEach 与 show 共享容器 anchor 误删（TextArea placeholder 占位节点未插入树）
4. **嵌套分类树导航** / **字段引擎最小切片**（待用户拍板）
5. **真机验收**：B2 帧率 + disabled 焦点视觉 + Breadcrumb focus 态 + TextArea 文本三态色

### 已知边界

- TextArea 颜色 token 裸值，主题切换不响应（scene 栈颜色全 static final int 常量，TextInput 同样，非本次引入）
- forEach 与 show 共享容器 anchor 误删 bug 影响 placeholder 显示，已登记待修
- oracle 裁决的 primitive 上色基调（只读状态 + wrapper 单向供 token）建议写入决策文档

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
- 并行 fixer 只并行写代码，build 统一由主 Agent 串行跑。
- 侦察报告的「推测/未验证」项 = 必须补查项；同类修复后全量 grep 兜底确认无残留旧模式。
- fixer 只写代码，记忆文档（docs/记忆/）由主 Agent 统一管理。
- demo 页是控件真机验证的前提，控件完成后必须建 demo 页接入 hub。
- fixer → build → test → review → commit 是硬性链路，build/test 通过不等于 review 通过，改动简单不是跳过 review 的借口（ERROR-20260624-skip-review-before-commit.md）。
- wrapper 延迟建树（return () -> {}）是 B1 教训硬约束，bind 必须在 Supplier 体内执行才能登记到正确 Owner。
