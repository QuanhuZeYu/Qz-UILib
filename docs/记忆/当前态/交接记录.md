# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 当前主分支：`4.0`；本批分支 `test/dirty-subtree-inline-formatting-boundaries` 用于 HTML-like 脏子树 display:inline 边界补测，完成后按项目规则合并回 `4.0`
- 已完成：`HtmlLikeDocumentWidgetInlineLayoutCacheTest` 新增 display:inline 多行 fragment、嵌套 inline、text-indent、text-align、文本测量 epoch、继承样式变化和脱流定位边界回归；当前实现保持重算且不误报子树复用
- 决策：本阶段不新增 display:inline fragment/text run 级缓存协议；如未来重开，需先设计独立行盒/fragment/run 缓存协议并记录决策，不能直接把 `display:inline` 加进元素盒复用白名单
- 仍保守排除：含 absolute/fixed 后代的子树、transform fixed containing block 子树、display:inline fragment/text run 级 inline formatting 和 layout 动画运行态
- 验证：已通过 `HtmlLikeDocumentWidgetInlineLayoutCacheTest`、`HtmlLikeDocumentWidgetTest`、`DocumentPaintEngineTest`、`UiDocumentTest`、`DocumentEventPropagationTest`、`UiStyleResolverTest`、`compileJava` 与 `git diff --check`；本批未跑完整 `test` 或 `runClient21`
- 注意：同一工作区内 Gradle 命令必须串行，禁止并行跑 `compileJava` / `test`

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
