# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接（2026-06-15）

- **当前分支**：`4.0`。本轮纯分析定位，未改动代码（除记忆/文档），工作区只有 docs 与外部记忆更新。
- **当前任务**：ModernConfigTemplateScreen（`/qzuilib test` MODCFG demo）大视图 ~3 FPS。上轮的「绘制重放期 ComputedStyle 单趟备忘缓存」修复（合并 `f3cf1146` / 修复 `4e56a612`）**已合入 4.0 并经用户本机编译实测**。

- **本轮结论（关键反转）**：修复已编译生效，但 **render 仍 312–366ms、fps 仍 2.7，与修复前逐数值一致，零改善**。⇒ 原「compute 递归级联是 replay ~300ms 主因」的归因被实测证伪。
  - **铁证**：修复后那次运行（`fml-client-latest.log`，00:22）所用 `DocumentPaintRenderer.class`（mtime 16:20，在修复合入 16:12 之后）经 `strings` 确认字节码含 `resolveStyle`/`computeWithParentStyle`/`IdentityHashMap`；对照修复前含诊断的 `fml-client-2.log`(23:05)/`fml-client-3.log`(23:30)：cmds≈727、resolve≈33ms、replay≈293–375ms、render≈326–388ms、fps≈2.7。
  - **修复保留不回滚**：它在语义等价前提下消除了绘制期递归级联，是正确微优化，只是不是本瓶颈。决策文件已追加 2026-06-15 证伪章节。

- **已逐项排除的候选**（静态核查+实测，勿再走）：compute 级联（实测零改善）、box-shadow（demo 无）、圆角 sin/cos（≪1ms）、stencil 圆角裁切（demo 边框走 layered 不 pushClip）、文本测量/bake（缓存+异步）、GPU 同步点（仅 backdrop/诊断路径）、表格 O(行²) 遍历（demo 无 table）、paint-context FBO（demo 稳态无 opacity/transform）、present/blur/世界渲染（前轮已排除）。

- **剩余真正大头方向**：replay 的 ~320ms 落在「即时模式 GL 全量绘制 + 无视口裁切」。727 命令每帧全量重放、无合批（fillRect 每次 Tessellator+glEnable/Disable 状态切换；圆角 glBegin/glEnd 即时模式）、native 2560×1369 分辨率 overdraw（半透明层叠）、且**整个 paint 包无任何视口剔除**（grep cull/viewport/offscreen 全空，屏幕外命令照样构建+重放；demo 根 overflow-y:auto 内容远高于视口）。

- **本轮进展（2026-06-15 第 2 次）**：「绘制重放按命令类型分类计时」诊断埋点**已合入 4.0**（`perf/modern-config-paint-command-profiling` commit `5b8e5b1f` 经 `--no-ff` 合并），并已 `compileJava` 重新编译进 build 产物，**待游戏内跑一帧定位 replay 大头类别**。
  - 实现：`DocumentPaintRenderer.render` 按 `Config.useDebug` 分流——关闭走新抽出的 `renderInternal`（与原循环逐行等价、零开销），开启走 `renderProfiled`（每命令/文本批前后 `System.nanoTime` 采样，按 `DocumentPaintCommandType` 累计 ms+计数，文本批整体计入 TEXT）。`CommandProfile` 用 ordinal 索引的定长 `long[]/int[]` 累加，零装箱。`logCommandProfile` 1 秒节流、关键字 `绘制命令分类诊断`、仅输出计数>0 类型按耗时降序、附总 replay 耗时与命令总数。
  - 校验已过：括号配对 207/207、符号引用、log 占位符数(3)=参数数、`git diff --check` 无空白错误，`compileJava` **BUILD SUCCESSFUL**，build 产物字节码确认含 `renderProfiled/CommandProfile/logCommandProfile`。
  - **坑（已解决）**：`runClient` 启动时 gradle 按当前工作区源码重新编译。8:23 那次运行在 4.0（无埋点）上启动，故 build/class 被无埋点版覆盖（mtime 8:18），日志只有旧 `UI 运行统计`、无 `绘制命令分类诊断`。已把埋点合入 4.0 并重编，**现在直接在 4.0 启动即命中埋点**。
  - **用户操作**：保持 4.0 分支，游戏内开 `useDebug`、进 MODCFG 大视图，grep `run/client/logs/fml-client-latest.log` 关键字 `绘制命令分类诊断`，把那行（含各类型 ms/count 与总 replay）回贴。

- **下一步（先实测后改，务必）**：
  1. 把含修复的 4.0 合进 `perf/modern-config-paint-diagnostics`（`git checkout perf/modern-config-paint-diagnostics && git merge --no-ff 4.0`）让用户跑一帧，grep `文档绘制诊断` 看 `replayStyleComputes` 是否从 3787 降到数百（确认备忘命中、修复语义生效）。
  2. ✅ 已埋点（见本轮进展）：在 `DocumentPaintRenderer.render` 内按命令类型累计 ms+计数（gate `useDebug`、1/秒节流），定位 BACKGROUND/BORDER/TEXT/SCROLLBAR 哪类是大头。**等用户回贴日志后据此判读**。
  3. CPU vs GPU 判别：replay 末尾临时 `glFinish` 前后计时；或临时「只遍历不 drawSurface/drawText」看 replay 是否暴跌。
  4. 据结果定方案：①视口裁切（CPU/GPU 都线性削减，最稳，命令带 left/top/right/bottom 可按视口相交跳过）②CPU-bound→合批同状态 drawSurface ③GPU-bound→减 overdraw / 离屏缓存整篇为纹理、scroll 平移 blit。
  - 改代码走 AGENTS.md 流程：从 4.0 建 `perf/<简述>` 分支、`[Perf]: 中文标题`、`git merge --no-ff` 合回、QuanhuZeYu 身份；并提醒用户本机补跑 `git diff --check`、`./gradlew.bat --no-configuration-cache compileJava`、定向测试与游戏内 MODCFG 大视图实测。
  - 注意：诊断埋点属临时设施。本轮 profiling 因 `runClient` 按工作区源码重编的机制，已主动合入 4.0 以便直接命中；**replay 大头定位结论确定后，需把这部分诊断从 4.0 回退**。既有 `perf/modern-config-paint-diagnostics` 仍只在该分支、未并入。

- **环境说明**：本机有 JDK25，可直接 `./gradlew.bat --no-configuration-cache compileJava` 编译（约 42s，已验证可成功）。仅游戏内运行/帧率实测需交用户（沙箱无 GUI 客户端）。
