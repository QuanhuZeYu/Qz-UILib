# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次工作分支：`add/character-font-rules`；目标是让玩家配置特定字符/范围使用特定字体，并在配置页中复用通用自动完成输入控件选择字体名
- 当前基线：`add/character-font-rules` 基于本地 `4.0`，包含已合并的 `DocumentAutocompleteInputControl`、provider、输入变更事件/handler、候选选择事件/handler，以及相关 top-layer 锚定与纯 JVM回归
- 已完成范围：字符字体规则实现已完成审查修复，包含 `FontCharacterRule` / `FontCharacterRuleSet`、`fontSystem.characterFontRules`、`FontMatcher` 规则优先匹配、配置页专用编辑器、远程同步字段级校验、规则测试和文档；`FontCharacterRuleControl` 字体名输入复用 `DocumentAutocompleteInputControl`，候选来源为 `FontConfig.getFontSortSnapshot()` 且单次最多渲染 128 个候选
- 验证状态：已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.font.config.FontCharacterRuleTest" --tests "club.heiqi.uilib.font.config.FontConfigCategoryTest" --tests "club.heiqi.uilib.font.util.FontMatcherRuntimeVersionTest" --tests "club.heiqi.uilib.config.FontSortPropertyEditorFactoryTest" --tests "club.heiqi.uilib.config.FontCharacterRuleDraftsTest" --tests "club.heiqi.uilib.config.FontCharacterRuleControlTest" --tests "club.heiqi.uilib.config.ConfigTemplateRemoteSessionTest" --tests "club.heiqi.uilib.ui.control.DocumentAutocompleteInputControlTest"`
- 当前仍需注意：如需游戏内确认配置页交互，可再运行 `runClient21` 从模组配置入口打开页面手动检查；本次不处理历史 `stash@{0}`

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
