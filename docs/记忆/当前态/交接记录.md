# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-30 Flutter 坐标系两层对齐 第 2+3 轮完成 + I12 信条修订，待 C 复审 + 终审）

### 一句话现状

指针坐标系对齐 Flutter 两层（raw + local via ctx）第 2+3 轮已完成（commit `b3bc28c9`）：A1 localPointer 移 ctx 每级重算 + B2 hitTestable 改造（Slider/TextArea 交互单元改到 track/content）+ C1 改名 getRawPointerX/Y + D2 废弃 host 层 + I12 三层→两层 + overlay 修复 + 4 控件改造。I12 作为**信条修订**（经用户确认，非偏离登记），`<deviation-log>` revision 条目已删。全量测试绿。待 oracle C 复审 + 终审 + 真机验证。

### 主线：坐标系两层对齐（第 2+3 轮）

**用户拍板**：最彻底方案 A1+B2+C1+D2+I12 两层，一次性完成。与 LWJGL 解耦正交。

**第 2+3 轮范围**（commit `b3bc28c9`）：
- **A1**：`SceneEvent` 删 `localPointerX/Y` 字段，local 移 `SceneEventContext.getLocalPointerX/Y()` = `rawPointer - absoluteBox(currentNode, treeAbs)`，每级 bubble 重算
- **B2**：`SceneSliderPrimitive` root hitTestable=false / track=true，interactionState/focusable/on 挂 track；`SceneTextAreaPrimitive` content hitTestable=true，挂 content；wrapper（Slider/TextArea）cursor/interactionState 同步改到 track/content
- **C1**：`SceneEvent.pointerX/Y` → `rawPointerX/Y`，getter `getRawPointerX/Y`
- **D2**：删 `SceneEvent.hostPointerX/Y` 字段 + getter + 构造器参数
- **I12 两层（信条修订，非偏离登记）**：NORTH_STAR I12 + §4.5 同步更新为两层（raw + local via ctx）。I12 三层→两层属信条/不变量本身改动，经用户确认，按 NORTH_STAR §修订纪律「信条本身的改动属重大架构变更，必须经用户确认」直接修订信条正文，**不**在 `<deviation-log>` 登记 revision 条目（D 收尾已删除此前误登记的 `<revision id="2026-06-30-i12-two-layer">` 块）
- **overlay 修复**：主 dispatch/CLICK/CANCEL 三处 ctx 注入 treeAbs（overlay=anchor，主树=rootAbs），删 TODO
- **4 控件**：Slider/TextArea/Scrollbar/TextInput handler 改 `ctx.getLocalPointerX/Y`，删手动换算
- **测试**：SceneInputRouterTest I12 段改 ctx.getLocalPointer + evt.getRawPointer；Slider/TextArea 测试 requestFocus 改 track/content、hitTestable 断言反转
- 受影响文件：`SceneEvent` / `SceneEventContext` / `SceneInputRouter` / `SceneSliderPrimitive` / `SceneSlider` / `SceneTextAreaPrimitive` / `SceneTextArea` / `SceneScrollbar` / `SceneTextInputPrimitive` / `NORTH_STAR.md` + 6 测试

### D 收尾（本次，commit 待提交）

- 删除 NORTH_STAR.md `<revision id="2026-06-30-i12-two-layer">` 块（I12 是信条修订，非偏离登记）
- 决策文档 DECISION-20260630 把"按修订纪律登记 revision"改为"直接修订 I12 信条（经用户确认）"
- 交接记录覆盖式更新

### wrapper 排查结论

- **Slider wrapper**：cursor bind 原挂 root，hover 现写 track → cursor 改到 track（SceneCursorResolver 读 hoveredNode.cursor）
- **TextArea wrapper**：interactionState 原挂 root，focused 现写 content → interactionState 改到 content；cursor 同理改到 content；root.setHitTestable 保留（padding 区命中退化，Oracle 拍板"primitive 不处理 padding 区落地"）
- **TextArea padding 区点击退化**：content hitTestable=true，padding 区命中 root 无 handler → 不定位 caret。可接受（基础版范围）

### 验证

- 编译：main + test 通过（jetbrainsBuildProject）
- 测试：全量 `./gradlew.bat test` 绿（141+ 测试）
- 单次超时未超 300 秒

### 真机确认待项（交用户）

- 真机 GUI 居中（rootAbs≠0）时，scrollbar/slider/textInput/textArea 交互正确
- overlay（如 dropdown/tooltip）命中时 local 正确（treeAbs=overlay anchor）
- 帧率正常

### 分支与工作区

- 当前分支：`feat/modern-config-p0-core`
- 最新 commit：`b3bc28c9`（第 2+3 轮主体）+ 本次 D 收尾 commit（待提交）
- 工作区：本次提交后干净
- 测试：全量绿

### 编排循环进度（A→B→C→D）

- A：oracle 规划第 2+3 轮方案（A1+B2+C1+D2+I12 两层）✅
- B：fixer 实施第 2+3 轮（commit `b3bc28c9`，全量绿）✅
- D：fixer 收尾（I12 信条修订转正 + 删 revision 登记 + 决策文档 + 交接记录，本次）✅
- 待 C：oracle 复审（第 2+3 轮代码 + I12 信条修订）
- 待终审：oracle 收尾复核
- 待真机验证：交用户跑
