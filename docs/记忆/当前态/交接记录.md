# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-06-24 第 87 次）—— reviewer 9 项债务清理收尾

### 一句话现状
reviewer 9 项非阻断建议已全部处置：6 项还清（B2/B5/B6/B7 + B3/B4），
1 项经 oracle 裁决接受不修（B1），2 项 P3 无需处理（B8/B9）。
下一步进入字段引擎最小切片。

### 本轮清理概要

**commit `a924d07d`** — 快胜档 4 项（B2/B5/B6/B7）
- B7: `SceneGeometry.maxScrollY` 加 `!isScrollable()` + `instanceof LayoutBox` 守卫
- B2: 3 处 `cachedLayout` 强转加 `instanceof` 守卫 + 延伸清理 2 处同类
- B6: `SceneTable` `contentBox` → `contentNodeBox` 消除术语混淆
- B5: 测试反射改用包内方法 `__getOverlayLayoutEngine`
- 测试注释修正 + 补 `scrollable=true` 但 `box==null` 用例

**commit `750b3ca3`** — 架构小重构 2 项（B3/B4）
- B3: `AnchorRect` 从 `overlay.SceneAnchorResolver` 内部类迁移为 `layout` 包顶层类，
  消除 layout/input → overlay 反向依赖，API 完全等价
- B4: `SceneHitTester` 递归改为传 `hasClip + clipX/Y/Width/Height` int 参数，
  递归热路径零对象分配，clip bounds 交集计算内联

**B1 oracle 裁决** — 接受不修（`DECISION-20260624-overlay-anchor-hit-test-frame-delay.md`）
- 原始前提有误：replay 读同帧 flush 后 anchor，**无视觉错位**
- 真实窗口在 hit-test：route 在 layoutOverlays② 之前，读上一帧 anchor，滞后一帧
- 视觉零错位、触发面极窄（仅 page 级滚动同帧命中，listbox 内滚动不触发）
- retained-mode 固有延迟（React/DOM 同款），不破 I7/I8/I11，无需偏离登记

### 验证
- `compileJava` + `compileTestJava` 通过
- 6 个测试类全绿（SceneHitTesterTest/SceneAnchoredOverlayPipelineTest/
  SceneSelectTest/SceneAnchorResolverTest/SceneScrollViewportTest/SceneOverlayPipelineTest）
- 两轮 reviewer 审核 PASS，NORTH_STAR I7/I8/I10/I11 守住
- `git diff --check` 通过

### 剩余债务
- B8 测试命名略误导（P3，未定位具体方法，无需处理）
- B9 CRLF warning（P3，判定无需处理）
- reviewer 建议3：守卫失败路径单测缺口（P3，防御性）
- reviewer 建议4：测试方法遗留 `throws Exception`（P4）
- A1/A2 NORTH_STAR 偏离登记待转正（需 oracle + 用户确认）

### 下一步
1. **字段引擎最小切片**：scene-native 字段 spec、draft/current、校验、
   dirty、canSave/canCancel 派生与保存语义
2. **接真实配置数据源**：优先接 `MutableConfig`
3. **暂缓**：搜索、树、多行文本、列表编辑、表格行内编辑、高级 picker、
   raw editor、远程同步、完整 CSS/token/theme 和 Phase 5

### 已知边界
- SceneSelect 三批缺陷已修复并通过真机验收（用户确认）
- reviewer 9 项建议已全部处置（6 还清 + 1 接受 + 2 无需处理）
- 旧对外现代配置页文档仍描述 DOM 12 模板能力；scene 一期完成前不得宣称等价
- NORTH_STAR `:247`/`:248` 待 overlay/scrollable 稳定后经 oracle 评估转正
- 旧栈 ui.dom 仍完整存在（65+ 源文件），scene 包零引用，Phase 5 暂缓

---

## 重要纪律（长期沿用）
- 帧率/真机实测必交用户跑，沙箱无 GUI。
- scene 新栈改动对照 NORTH_STAR 不变量自检，偏离按《修订纪律》登记。
- 完成独立任务后检查是否需更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/`。
- 子代理任务中断必须用原 task_id 恢复 session 保留进度，不得新开 session 空跑重做。
- Markdown 正文不写超长行（≤200 字符），表格行结构例外但控制单格长度。
