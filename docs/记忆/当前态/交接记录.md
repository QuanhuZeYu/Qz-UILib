# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。
只保留最近一次交接的当前真相，历史交接不在此堆叠。

## 最近一次交接（2026-07-01 移除 ArchUnit + 测试规则文档化 + 注释语义统一收口完成）

### 一句话现状

ArchUnit 自动守卫移除（commit `7b20c36c`）+ L2 纯数学边界转评审纪律文档化（commit `539c0843`）+ 全仓「保留自建」旧注释统一为 §7.1 归类格式（本次 commit 待提交）。全量测试绿。L2 边界纯评审纪律守卫（无自动守卫），后续可选补零依赖 grep 脚本。

### 主线：移除 ArchUnit + 测试规则文档化 + 注释统一收口

**任务三段**：
1. **ArchUnit 移除**（commit `7b20c36c`）：删 `archunit-junit4:1.3.0` 依赖 + `ArchitectureTest.java`。规则1/2 待 B5 启用，规则3（layout 包禁依赖 runtime/input/reactive/paint）改评审纪律。
2. **文档化收尾**（commit `539c0843`）：`测试体系约定.md` §6 架构纪律 checklist + §7.1 输入侧两入口边界五类白盒场景 + `AGENTS.md` 测试规范索引 + `testkit/package-info.java` 简版边界。
3. **注释语义统一收口**（本次 commit 待提交）：全仓 13 处旧格式「保留自建」统一为 `白盒回退（归类）：理由。判据见 §7.1` 格式，含 SelectTest absCenter 打脸修复（:279 与同文件已改 :297 对齐）。

### 注释统一收口明细（18 处全归类）

| 文件:行 | 归类 |
|---|---|
| SceneSelectTest:46 类javadoc | overlay 树外路由 |
| SceneSelectTest:279 absCenter | overlay 树外路由 |
| SceneSelectPrimitiveTest:55 类javadoc | overlay 树外路由 |
| SceneSelectPrimitiveTest:134 absCenter | overlay 树外路由 |
| SceneTextInputTest:42 类javadoc | 多帧批次 + 精确 localX |
| SceneTextInputTest:139 routeText | 多帧批次 |
| SceneTextInputTest:149 routeTextFrame | 多帧批次 |
| SceneTextInputTest:180 clickLocalX | 精确 localX + host margin（判据2+4）|
| SceneTextInputTest:291 重建runtime | 多 runtime（判据5）|
| SceneTextInputTest:474 批次 | 多帧批次 |
| SceneTextInputTest:496 批次 | 多帧批次 |
| SceneTextAreaTest:44 类javadoc | 精确 localX（判据2）|
| SceneTextAreaTest:209 clickAt | 精确 localX（判据2）|
| SliderTest:56 类javadoc | 精确坐标序列（判据2）|
| SegmentedTest:34 | 已迁 harness，无需改 |

**疑似新白盒场景**（五类字面未覆盖，已按 Oracle 裁决暂归近邻，建议后续补入 §7.1）：
- **多帧批次**（routeText+routeKey 同批次最后统一 flush，harness.typeText 中途 flush 破坏批次）：归判据5近邻（harness 实例/批次生命周期结构性限制）。涉及 TextInputTest 4 处。
- **drag 坐标序列**（Slider pointer drag，harness 不支持 drag 序列）：归判据2（精确坐标序列）。涉及 SliderTest 1 处。

### 验证

- 编译：`compileTestJava` 通过
- 测试：SceneSelectTest / SceneSelectPrimitiveTest / SceneTextInputTest / SceneTextAreaTest / SceneSliderTest / SceneSegmentedTest 回归全绿
- grep `保留自建`：0 残留

### 遗留

- L2 纯数学边界纯评审纪律守卫（无自动守卫），依赖评审者知晓 §6 checklist（已由 AGENTS.md 索引缓解）
- 后续可选补零依赖 grep 脚本断言 `scene/layout/**` 无四包 import
- §7.1 五类是否补第六类「多帧批次」/ 第七类「drag 序列」，待后续 Oracle 裁决

### 分支与工作区

- 当前分支：`feat/modern-config-p0-core`
- 最新 commit：`539c0843`（文档收尾）+ 本次收尾 commit（待提交）
- 工作区：本次提交后干净
- 测试：全量绿
