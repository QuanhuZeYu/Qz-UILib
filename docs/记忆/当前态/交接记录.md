# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 本次工作分支：`fix/select-large-options-virtualization`，完成 `DocumentSelectControl` 大列表虚拟化并按要求通过 `git merge --no-ff` 合并回 `4.0`
- 当前基线：`4.0` 已包含 select popup 固定窗口 option 池、上下 spacer 滚动高度模拟，以及默认滚轮/滚动条拖拽后的元素 `scroll` 事件分发
- 已完成范围：一万条 select 选项不再一次性创建全部 `option` DOM；键盘 Home/End/方向键、程序化滚动、鼠标点击仍按真实选项索引选择；使用文档和决策记录已写回
- 验证状态：已通过 `git diff --check`、`./gradlew.bat --no-configuration-cache test --tests "club.heiqi.uilib.ui.control.DocumentSelectControlTest" --tests "club.heiqi.uilib.ui.document.HtmlLikeDocumentWidgetScrollTest"`、`./gradlew.bat --no-configuration-cache compileJava`
- 当前仍需注意：select 仍是传统单选控件，不提供搜索、过滤、分组或可变高度 option；需要快速定位超长候选时仍优先接 `DocumentAutocompleteInputControl`

## 交接要求

- 控制在一屏内，优先写“下一位现在最需要知道什么”
- 不复制长篇背景，详细信息放回对应文档
