# 交接（单一权威状态源）

> 本文件是项目唯一权威状态源。`现状.md`/`计划.md` 已并入本文件并删除。
> 任何状态冲突以本文件为准（消除多头发散，建立单一误差向量）。
> 控制律（完成标准/约束/审核纪律）不入本文件，归 `AGENTS.md` 与
> `docs/控制律层/编排模式/SUBAGENT-ORCHESTRATION.md`。

## 1. 当前状态 Current State

- 当前分支：`refactor/config-fullmigrate-modern`（基于 4.0 + actionBar 合并，阶段 A 6 commit + 阶段 B 1 commit + 阶段 C P0 核心 5 commit + 阶段 C C4 1 commit + 反向持久化改向 A2 1 commit）
- 当前分支：`refactor/config-fullmigrate-modern`（基于 4.0 + actionBar 合并，阶段 A 6 + B 1 + C P0 核心 5 + C4 1 + 反向改向 A2 1 + 日志增强 1 + 删旧栈整支 1 + 统收死代码 1 = 17 commit）
- HEAD：`3d0f758c` 阶段 E.2 统收 Config.java + FontConfig.java 旧栈死代码
- 工作区：干净（**阶段 A-E 全部完成**，旧栈整支已删，新栈单栈运行）
- 全量测试：34 用例全绿（ConfigValueBridgeTest 6 + ConfigSaveListenerTest 5 + ModernConfigBootstrapTest 3 + QzUiLibModernEndToEndTest 14 + FontMatcherRuntimeVersionTest 6）
- 真机综合验证：✅ 用户已确认无异常（启动加载首次回灌 + 改配置保存触发 reload + 重启验证回灌 + 异常容错 + 字体兜底不插队 5 项全过；日志 debug 已就位）
- 4.0 状态：被并发会话污染（含半成品阶段 A commit `82ffe6da` + 别人 agent-env .md），refactor 合回时补齐（用户已拍板接受既成事实）

## 2. 当前未收敛项 Open Items

> 仿 `NORTH_STAR.md`《偏离登记》范式。每项含 what/why/blocked-by/next/优先级。
> 收敛后即从本节移除，不保留历史。

<open-items>

<item id="U3-config">
  <what>U3 config 侧深改造：DraftSignalAdapter / Renderer / ConfigScreen
  基于 U2 组合式骨架口径进一步改造</what>
  <why>U2 已统一 FormPageShell 骨架口径，U3 为 config 侧更深改造，
  核心价值在草稿适配与渲染层收敛</why>
  <blocked-by>用户拍板改造范围（哪些层动、动到什么程度）</blocked-by>
  <next>等用户给出 U3 范围裁决后启动</next>
  <priority>待用户拍板</priority>
</item>

<item id="SceneTab-真机目视">
  <what>SceneTab 真机目视验收：4 条清单
 （fill 填满 / 切页稳定 / 常规页无回归 / tabBar 高度）</what>
  <why>本地测试无法替代真机像素与交互验收</why>
  <blocked-by>用户真机跑 `/qzuilib test` 目视确认</blocked-by>
  <next>用户真机验证 4 条清单，反馈结果</next>
  <priority>待用户真机</priority>
</item>

<item id="push-4.0">
  <what>本地 4.0 ahead origin/4.0，按需 push</what>
  <why>含 config 接入 5 单元 + Oracle C 3 项 + 控制论重构 5 单元 + 阶段 A-E 配置迁移 17 commit</why>
  <blocked-by>无阻塞，按需</blocked-by>
  <next>用户确认后 push</next>
  <priority>低（按需）</priority>
</item>

<item id="悬空溯源上溯评估">
  <what>Javadoc 中悬空溯源行号第 2 次累积（C4/C5/C6 + 阶段 E.2），按 §4.3 上溯规则评估补不变量</what>
  <why>删除 API 时未同步清理 Javadoc 中对该 API 的溯源行号引用，反复出现已达上溯触发点</why>
  <blocked-by>无</blocked-by>
  <next>评估是否在 NORTH_STAR 或 AGENTS.md 补一条"删除 API 时同步清理 Javadoc 中对该 API 的溯源行号引用"不变量；若确认无更高层补丁可打，移到错误预防通则层</next>
  <priority>中（已第 2 次累积）</priority>
</item>

</open-items>

## 3. 待拍板项 Awaiting User

- 深化 uilib 配置内容方向（fontSort + characterFontRules 深化的具体维度：拖拽支持 / 自动补全 / 字段拆分 / 验证规则 / 默认预设 / UI 编辑器等，待用户给方向）
- 悬空溯源上溯评估（§2 已登记，是否补不变量）

## 4. 最近完成 Recently Done

| 单元 | 结果 | 分支 |
|---|---|---|
| 阶段 E.2 统收 Config.java + FontConfig.java 旧栈死代码 | 1 commit `3d0f758c`；Config.java 124→27 行（保留 4 字段 useDebug/uiDebug/fontRuntimeDebug/netTransport + 私有构造）/ FontConfig.java 359→200 行（删反向持久化整支含 A2 detach 方法）/ CommonProxy.preInit 时序改 [1/3]-[3/3]（删 Config.init）/ ConfigValueBridge 删 detach 调用；4 文件 Javadoc P1 悬空溯源顺手修；34 用例绿；守 I1/I3/I7 | refactor/config-fullmigrate-modern |
| 阶段 D+E.1 合并 删旧栈整支 + 修引用方 | 1 commit `8a84d560`（原 commit 2+3 合并因分两 commit 编译失败）；删 22 主代码（旧栈 UI 11 + 网络同步 11） + 10 测试；修 6 引用方（CommonProxy/ClientProxy/VanillaMixinTransport/NetSelfCheckRunner/NetRuntimeSelfChecks + 清单外 NetEnvelopeDispatcher）；P1 ModConfigGui Javadoc 顺手修；31 用例绿；守 I1/I3/I7 | refactor/config-fullmigrate-modern |
| 新栈关键路径日志增强（INFO 关键 + DEBUG 细节） | 1 commit `e2ca2ba2`；ModernConfigBootstrap/ConfigSaveListener/ConfigValueBridge/CommonProxy 加 log；17 用例绿；真机排错依赖 | refactor/config-fullmigrate-modern |
| 数值字段默认走 input + fontSortConfigured 兜底改向（用户真机验证后调整） | 1 commit `f4ecd951`；删 7 处 .slider()（lerpMode/aaMode/smoothRangeMin/smoothRangeMax/aaStrength/awtCharSize/charSize，保留 .range()）+ fontSortConfigured 改按 fontSort 非空（让 DefaultFontOrderHints 系统字体优先级提示在用户空配置时自动生效，中文字体排前）；17 用例回归全绿；守 I1/I3/I7 | refactor/config-fullmigrate-modern |
| 阶段 C 反向持久化改向 A2（detachLegacyConfiguration 切断 .cfg 反向写） | 1 commit `7e3b1735`；编译绿 + 17 用例回归全绿（含 FontConfigCategoryTest 老用例 2 项不破坏）；reviewer 全过 + P2 行号瑕疵顺手修；守 I1/I3/I7 + 不 publish BATCH_SAVE 防回环；保留方法体留阶段 D/E 统收 | refactor/config-fullmigrate-modern |
| 阶段 C C4 ModConfigGui 中转到 ModernConfigEntry（guiFactory 入口正式切到新栈） | 1 commit `946be55b`；编译绿 + 14 用例回归全绿；独立复审全过 + P1 Javadoc 收尾；守 I1/I3/I7；C5/C6 死代码暂留窗口按拍板待阶段 D/E 统收 | refactor/config-fullmigrate-modern |
| 阶段 C P0 核心三块（C1 ConfigValueBridge 值回灌 / C2 ConfigSaveListener 保存回调 / C3 ModernConfigBootstrap 启动加载首次回灌） | 5 commit + 各 P2 收尾；编译绿 + 14 用例绿（C1 6 + C2 5 + C3 3）；三轮独立复审全过；守 I1/I3/I7；CommonProxy.preInit 时序铁律 reviewer 独立自核；resolve P0 缺口（新栈无值回灌链路 + 保存回调唯一重挂点） | refactor/config-fullmigrate-modern |
| 阶段 B ModernConfigEntry 抽同步 createScreen 入口 | 编译回归通过；主 agent 盘查通过（低风险纯加法）；bootstrap 失败返 parent，open 复用 createScreen DRY | refactor/config-fullmigrate-modern |
| 阶段 A FieldType.SIMPLE_LIST + SimpleListFieldRenderer + QzUiLibModernSchema 接入 + FormFieldShell 高度修复 | 45 测试绿（含 integration I5/reset 守卫）；reviewer 全通过；两项设计取舍固化（fontSort 拖拽降级登记技术债 / reset 多帧延迟落决策沉淀） | refactor/config-fullmigrate-modern |
| 早期已稳定单元归并：actionBar 上移 + 旧栈迁移决策5项拍板 + AbstractSceneHostWidget 提正式包 + 旧决策清理（49文件） + U1 FormFieldShell/FormTheme 下沉 + U2 FormPageShell 产品化 + ConfigScreen 复用 + Oracle C 3 项 + 控制论重构 U1-U5（grep门禁/状态源收敛/Subagent收口/AGENTS闭环骨架/docs控制分层） + opencode 原生子 agent 体系 | 见各提交历史；控制论重构总评（Oracle C 复审）：结构层面优秀（五环成链+反向反馈+双传感器+内容零丢失），grep 门禁 5 断言计数口径已统一，`scene技术债.md` 残留锚点已修正 | 各 refactor/* 已合回 4.0 |

> 控制论重构总评（Oracle C 复审）：结构层面优秀（五环成链+反向反馈通道+双传感器+内容零丢失），
> 实例层面已修复（交接状态源更新为当前真值）。grep 门禁 5 断言计数口径已统一。
> `scene技术债.md` 残留 `AGENTS §5` 已改语义锚点。

## 5. 长期未回填（指针）

- scene 技术债权威清单 → `docs/诊断层/scene技术债.md`
- 宪章偏离登记 → `NORTH_STAR.md`《偏离登记》段
- 架构边界与 strangler 路线图 → `docs/设定值层/边界.md`
- 关键取舍背景 → `docs/反馈层/决策/`

## 维护说明

- 本文件是唯一权威状态源，状态变化直接覆盖，不堆历史交接。
- 只保留：当前状态 + 未收敛项 + 待拍板 + 最近完成简表 + 长期指针。
- 瞬时状态（分支/HEAD/工作区/测试/阻塞/待办）写本文件；
  控制律（完成标准/约束/审核纪律/fixer 串行规则）归 `AGENTS.md` 与
  `SUBAGENT-ORCHESTRATION.md`，不入本文件（避控制律误并入状态）。
- 长期事实按控制分层归位（设定值/控制律/传感/诊断层），关键取舍归 `docs/反馈层/决策/`，错误预防通则归
  `docs/反馈层/错误预防.md`，本文件只留指针。
- 未收敛项收敛后即从 §2 移除，不保留历史。

## 状态层次边界（防混淆）

本文件是**项目级**状态源（跨多任务、长期）。**会话级**工作记忆（单任务内、易失）不进本文件，归
`docs/控制律层/编排模式/SESSION-HANDOFF.md` 模式下的 `.opencode/session-handoff.md`。
会话 handoff 中"有持续价值的事实"任务完成后才回流到本文件或决策/错误预防，其余丢弃。
