# 交接（单一权威状态源）

> 本文件是项目唯一权威状态源。`现状.md`/`计划.md` 已并入本文件并删除。
> 任何状态冲突以本文件为准（消除多头发散，建立单一误差向量）。
> 控制律（完成标准/约束/审核纪律）不入本文件，归 `AGENTS.md` 与
> `docs/控制律层/编排模式/SUBAGENT-ORCHESTRATION.md`。

## 1. 当前状态 Current State

- 当前分支：`4.0`（本会话功能支已合回；工作区应干净）
- 最新提交：精确以 `git log --oneline -1` 为准（本会话末：`1a515f4a` 合并命中区 SHRINK）
- 工作区：干净；精确状态以 `git status` 为准
- 主分支 4.0 状态：本会话合入 Checkbox/Toggle/Radio 命中区 SHRINK；叠加 fontsort 拖拽 + 恢复默认字段策略 + agent 模型切换 + 旧栈移除/复用性修复链；本地 ahead origin ~176 commit，按需 push
- 全量测试：本会话相关 `SceneCheckboxTest` / `SceneToggleTest` / `SceneRadioGroupTest` 通过；历史全量以既有 `./gradlew.bat test` 记录为准
- 复用性评分：9.2/10（初始 6 → 移除旧栈 8.5 → P1修复+文件拆分 9 → P2清理 9.2）；P0超千行文件 0 个，重复模式三项清零（__getChildren按下标取子 / nullSafe克隆 / 硬编码色值），评审判可合并
- 迁移工程真机综合验证：✅ 用户已确认无异常（启动加载 + 保存 reload + 重启回灌 + 异常容错 + 字体兜底 5 项全过）
- 深化 + 输入修复真机验证：✅ 用户已确认全部通过（P6 补全、下拉框输入修复全链、SceneSelect 展开/点击收起行为、characterFontRules fontName autocomplete 候选行为、fontSort 预填充等）
- 字体渲染真机验证：✅ 用户已确认通过（字符硬裁切 P1 修复见 §4，硬边消失、细笔画不再被吃薄）
- 命中区 SHRINK 真机：待用户在控件页点验 Checkbox/Toggle/Radio 行尾空白是否仍误触（单测已覆盖）

## 2. 当前未收敛项 Open Items

> 仿 `NORTH_STAR.md`《偏离登记》范式。每项含 what/why/blocked-by/next/优先级。
> 收敛后即从本节移除，不保留历史。

<open-items>

<item id="push-4.0">
  <what>本地 4.0 ahead origin/4.0 ~176 commit，按需 push</what>
  <why>本会话叠加：Checkbox/Toggle/Radio 命中区 SHRINK（merge `1a515f4a`）；此前：旧栈移除整链 + 复用性 P1/P2 + 超千行拆分 + fontsort 拖拽 + 恢复默认字段策略 + agent 模型切换 + config 全迁移链等</why>
  <blocked-by>无阻塞，按需</blocked-by>
  <next>用户确认后 push</next>
  <priority>低（按需）</priority>
</item>

</open-items>

## 3. 待拍板项 Awaiting User

- 无架构待拍板；唯一剩余项为 push 4.0 到远端（§2 `push-4.0`），用户按需确认

## 4. 最近完成 Recently Done

| 单元 | 结果 | 分支 |
|---|---|---|
| Checkbox/Toggle/Radio 命中区对齐内容宽 | 根因：交互根默认 FILL + 透明根 → hit ≫ 可见 chrome。ToggleablePrimitive root / Radio option wrapper 设 `WidthSizing.SHRINK`（Radio 不改 SingleSelectPrimitive，避 Segmented/Tab）。测 root/option 宽=内容宽 + 行尾不命中。复审无阻断。merge `1a515f4a`。通则见错误预防「scene 交互根宽度」。 | fix/toggle-checkbox-hit-shrink + fix/radio-option-hit-shrink → 4.0 |
| 会话扫尾（本会话） | 更新交接状态源；错误预防补 SHRINK 通则；session-handoff 清为模板；文档纪律门禁通过。 | 4.0 |
| 恢复默认 fontSort/characterFontRules 特殊策略 | 新增 `FieldRestorePolicy`，经 `ConfigUI.buildScreen` 注入；`fontSort` 恢复为 `FontConfig.getFontSortSnapshot()` 并 `onFieldEdit` 标 dirty；`characterFontRules` 跳过。测试全绿。 | fix/restore-default-fontsort → 4.0 |
| explorer/fixer/librarian 模型切换 | 对照全局 provider，三 agent 改为 `grok/grok-4.5` + `reasoningEffort: high`。需重启 opencode 生效。 | fix/restore-default-fontsort → 4.0 |
| fontsort 拖拽全套手感重构 | 完成 0-5 + P2；决策见 `docs/反馈层/决策/fontsort-drag-signal-2026-07.md`。 | refactor/fontsort-drag → 4.0 |
| 旧栈移除复用性提升全链 | 初始审核评 6/10（双栈并存+12超千行+多处克隆）。本会话完成：①旧栈 HTMLlike Document 体系完全移除（11阶段，−500文件/−98560行，merge d6d0b322）；②P1修复 SceneSimpleList R4 + nullSafe/safeRows收敛 + SceneKeyValueMap/ObjectField R12 + DataTable Props构造器收敛；③P0超千行文件全拆：SceneNode 1798→919（ScenePaintProps+SceneLayoutProps值对象）、SceneKeyValueMap 1028→759（嵌套类独立化）、UiMainLayerSnapshotService 1195→729（4协作者）、UiRenderContext 1045→996。终评 9.2/10，P0超千行0个，重复三项清零。详见 `.opencode/session-handoff.md`。 | 多分支合回 4.0 |
| P2 小尾巴清理（复用性收尾） | `SceneListOps.immutableCopy()` 收敛 13 处防御性不可变拷贝（10 个文件：SceneDataTable/Select/SelectPrimitive/SingleSelectPrimitive/SimpleList/KeyValueMap/AutocompletePrimitive/InputFrameBuilder×3/OverlayHost/PaintFragment）；删除 `SceneTextGeometry` 的 `@Deprecated nullSafe` 死代码（已迁移到 SceneTextUtils，零调用）。复用性评分 9→9.2。JetBrains 构建通过，gradle test 全绿。 | refactor/p2-tail-cleanup |
| UiRenderContext GL 助手提取收尾 | 提取 `UiContextGlHelpers` 承载 `fillRoundedRect`/`drawRoundedBorder`/`applyColor`，`UiRenderContext` 从 1034 行降至 996 行；`drawText`/`pushClip`/`pushTransform` 多态钩与构造器链保持不动。gradle test 全绿。 | refactor/ui-render-context-gl-helpers |
| render 层超千行文件拆分 | `UiMainLayerSnapshotService` 提取 `FrameSnapshot` / `TileAssemblyEntry` / `SnapshotTileAtlasAssembler` / `SnapshotFilterPassRenderer`，主类降至 729 行；`UiRenderContext` 提取 `DeferredPostMainPassQueue`，主类降至 1034 行（oracle 预估约 1003，实际按保留 API Javadoc 与委托口径收敛）。JetBrains 构建 Step0/Step1/Step2 通过；文件2阶段 JetBrains MCP 超时，回退 `./gradlew.bat --no-configuration-cache compileJava` 通过；最终全量 `test` 通过。 | refactor/render-layer-file-split |
| SceneKeyValueMap 嵌套类独立化 Step1 | `ValueType` / `ValidationErrorType` / `KeyValueRow` / `ValidationError` 提升为同包顶级类型，`KeyValueRow` 接管行 id 分配器；`SceneKeyValueMap` 保留 `Props`/`Builder` 与 create 工厂，行数降至 759。JetBrains 构建通过，`./gradlew.bat --no-configuration-cache test` 全绿。 | refactor/scene-key-value-map-types |
| P2 DataTable 硬编码颜色 token 化 + P1 R12 反模式修复 | `SceneDataTable` / `SceneDataTableEditorChrome` 的 DataTable 专用色值收归 `SceneChromeTokens`；`SceneKeyValueMap` 删除 3 处 `__getChildren().get(N)`，改用 mount 返回根节点与动作按钮局部引用；`SceneObjectField` toggle 改用 `ButtonParts` 具名引用绑定内部 label。JetBrains 构建通过，`git diff --check` 无输出，scene 边界门禁通过。 | fix/p2-token-r12-children |
| 旧栈核心清除阶段 8-10 | 删除旧 `ui/layout`、`ui/dom`、`ui/style` 剩余包；按外部引用清理规则同步删除旧 `ui/animation`、`ui/component` 残留及测试；补齐 `ui/base/layout/UiInsets` 并断开 base 对旧 style/layout 的依赖；`NetStoreUiBridgeTest`、命令用法测试与分层门禁更新到旧栈清零边界。JetBrains 构建通过，`./gradlew.bat --no-configuration-cache test` 全绿。 | refactor/migrate-ui-base-umbilical |
| config 全迁移链（refactor/config-fullmigrate-modern 整支合回 4.0） | merge `29e4286b` `--no-ff` 整支合回（34 提交，含字符硬裁切原 `15a222e7` 与 cherry-pick 版同改动 git patch ID 自动识别无重复）。聚合本链全部内容（详见各专项决策文档）：①**字符硬裁切 P1 根治**（uvBounds 硬墙切 mipmap 羽化，约束 AA 偏移<INK_PADDING=8，真机已验）；②**scene 输入系统 P0→P2-3 修复链**（下拉框点击失效根治：CLICK 合成升级 LCA 容差 + R13 浮层显隐独立契约 + ReactiveScheduler flush 双通道补全 + harness `pressReleaseAcrossFrames` 时序原语 + 测试体系约定 §7.5 帧时序语义）；③**autocomplete/characterFontRules 深化链**（P-1+P0-P5+P6，SceneAutocompletePrimitive 组合 + caret 末尾 + CONTAINS 抑制重弹，详见 `docs/反馈层/决策/font-character-deepen.md`）；④**U3 档 A FieldRenderer 样板收敛**（FieldRenderSupport+FieldShellBinder，守 U1/U2/I2/I8）；⑤**I2 措辞正名 + revisionSignal×I8 裁决**（NORTH_STAR I2 正名非放松，详见 `docs/反馈层/决策/reactive-computed-i2-i8-rulings.md`）；⑥**悬空溯源门禁上溯**（check-javadoc-stale-line-refs.ps1 + check-doc-stale-lineno.ps1 两道门禁）；⑦真机两轮全过（深化+输入+字体渲染）。 | refactor/config-fullmigrate-modern（已合 4.0） |
| autocomplete 空候选独立路径 + getFontSortSnapshot 补测（技术债还清） | commit `b7f5e6ca`。补 L3 `emptyCandidateSourceNeverExpandsOverlay` + `FontConfigTest` 3 边界。详见 `docs/诊断层/scene技术债.md`。 | test/autocomplete-empty-font-snapshot |
## 5. 长期未回填（指针）

- scene 技术债权威清单 → `docs/诊断层/scene技术债.md`
- 宪章偏离登记 → `NORTH_STAR.md`《偏离登记》段
- 架构边界与 strangler 路线图 → `docs/设定值层/边界.md`
- 关键取舍背景 → `docs/反馈层/决策/`

## 维护说明

- 本文件是唯一权威状态源，状态变化直接覆盖，不堆历史交接。
- 只保留：当前状态 + 未收敛项 + 待拍板 + 最近完成简表 + 长期指针。
- 瞬时状态（分支/HEAD/工作区/测试/阻塞/待办）写本文件；
  控制律（完成标准/约束/审核纪律/fixer 串行规则）归 `AGENTS.md` 与
  `SUBAGENT-ORCHESTRATION.md`，不入本文件（避控制律误并入状态）。
- 长期事实按控制分层归位（设定值/控制律/传感/诊断层），关键取舍归 `docs/反馈层/决策/`，错误预防通则归
  `docs/反馈层/错误预防.md`，本文件只留指针。
- 未收敛项收敛后即从 §2 移除，不保留历史。

## 状态层次边界（防混淆）

本文件是**项目级**状态源（跨多任务、长期）。**会话级**工作记忆（单任务内、易失）不进本文件，归
`docs/控制律层/编排模式/SESSION-HANDOFF.md` 模式下的 `.opencode/session-handoff.md`。
会话 handoff 中"有持续价值的事实"任务完成后才回流到本文件或决策/错误预防，其余丢弃。
