# 交接（单一权威状态源）

> 本文件是项目唯一权威状态源。`现状.md`/`计划.md` 已并入本文件并删除。
> 任何状态冲突以本文件为准（消除多头发散，建立单一误差向量）。
> 控制律（完成标准/约束/审核纪律）不入本文件，归 `AGENTS.md` 与
> `docs/控制律层/编排模式/SUBAGENT-ORCHESTRATION.md`。

## 1. 当前状态 Current State

- 当前分支：`fix/config-stale-draft-recovery`（基于 `add/config-draft-validator@2a959fb`；待审合入）
- 最新提交：精确以 `git log --oneline -1` 为准
- 工作区：提交后应干净；精确状态以 `git status` 为准
- 发布：本分支完成 **4.5.3** stale draft 易用性修复（changelog fragment 已写）；正式 tag / push / GitHub Release **尚未执行**
- 前一分支工作：`add/config-draft-validator` 含 4.5.2 DraftValidator（尚未 tag）
- 前一正式版：`4.5.1`（issue #63 裁切）
- 相关测试：ConflictType 四类精确断言、同 draft 连续 save、双 ConfigScreen stale→reload（Signal identity）、replaceDraft schema 拒绝、presentation seed 不落盘、requiresReload 不清冲突、runtime/schema/UI 定向 + 全量 `test` 绿

## 2. 当前未收敛项 Open Items

> 仿 `NORTH_STAR.md`《偏离登记》范式。每项含 what/why/blocked-by/next/优先级。
> 收敛后即从本节移除，不保留历史。

<open-items>

<item id="flushraw-prepare-conflict-test">
  <what>flushRaw 已有 prepare 后 Authority 冲突守卫，但尚缺精确卡在 prepare 窗口的确定性并发回归</what>
  <why>Persistence 为 final 且由 ConfigManager 私有构造，文件对象仅参与 write；现有结构无可靠 fault injection，拒绝用固定耗时或反射造脆弱测试</why>
  <blocked-by>未来若生产设计自然引入可替换 persistence 协作者，再补确定性测试</blocked-by>
  <next>维持现有冲突检查；涉及 flushRaw/Persistence 构造边界变更时补回归</next>
  <priority>低（P2 测试缺口）</priority>
</item>

<item id="remote-api-lts-cleanup">
  <what>LTS / 使用文档中通用远程 HTML（RemoteDocumentPages / RemoteHudOverlays）仍标可用，但 ui.remote 包可能已从源树移除</what>
  <why>阶段 1 审核发现的预存文档债，不在 config 对齐 scope</why>
  <blocked-by>无</blocked-by>
  <next>另开 docs 任务核对源码后清 LTS/远程页面文档</next>
  <priority>低</priority>
</item>

<item id="release-4-5-3">
  <what>4.5.3 stale draft 恢复 + 4.5.2 DraftValidator 待审合入后打 tag 并推送 Release</what>
  <why>用户要求未审前不 merge / tag / push / release；本分支不 merge 回主线</why>
  <blocked-by>代码审 + 用户确认合入路径（先合 draft-validator 再合 stale-recovery，或整支）</blocked-by>
  <next>review → merge --no-ff → 4.0 → tag 4.5.3 → push</next>
  <priority>中</priority>
</item>

<item id="qz-miner-consume-4-5-2">
  <what>Qz-Miner 在 UILib 4.5.2/4.5.3 发布后接入 DraftValidator 与冲突恢复语义并验收</what>
  <why>上游尚未 tag/release；本分支不跨仓改 Miner</why>
  <blocked-by>release-4-5-3</blocked-by>
  <next>依赖发布版 → bootstrap 三参 + 实机验收 INVALID/stale reload</next>
  <priority>中</priority>
</item>

</open-items>

## 3. 待拍板项 Awaiting User

- 无架构待拍板（stale draft 恢复方案用户已批准）

## 4. 最近完成 Recently Done

| 单元 | 结果 | 分支 |
|---|---|---|
| 4.5.3 stale draft 易用性 | ConflictType + 事务 base + replaceDraft + presentation seed + 显式 reload UI；全量 test 绿 | fix/config-stale-draft-recovery |
| DraftValidator 完整终审 | capture→锁外 validate/prepare→verify/write/引用交换；通知期跨线程拒绝；SIMPLE_LIST 类型闭环 | add/config-draft-validator |
| 发布 4.5.1 | issue #63 宿主裁切基线修复合入 | 4.0 + tag 4.5.1 |

## 5. 长期未回填（指针）

- scene 技术债权威清单 → `docs/诊断层/scene技术债.md`
- 宪章偏离登记 → `NORTH_STAR.md`《偏离登记》段
- 架构边界与 strangler 路线图 → `docs/设定值层/边界.md`
- 关键取舍背景 → `docs/反馈层/决策/`
