# 交接（单一权威状态源）

> 本文件是项目唯一权威状态源。`现状.md`/`计划.md` 已并入本文件并删除。
> 任何状态冲突以本文件为准（消除多头发散，建立单一误差向量）。
> 控制律（完成标准/约束/审核纪律）不入本文件，归 `AGENTS.md` 与
> `docs/控制律层/编排模式/SUBAGENT-ORCHESTRATION.md`。

## 1. 当前状态 Current State

- 当前分支：`4.0`（`refactor/ui-render-context-gl-helpers` 已按 `--no-ff` 合回）
- 最新提交：UiRenderContext GL 助手提取合并提交（精确哈希以 `git log -1` 为准，详见 §4）
- 工作区：干净；精确状态以 `git status` 为准
- 主分支 4.0 状态：refactor/config-fullmigrate-modern 已 `--no-ff` 整支合回（merge commit `29e4286b`，含字体硬裁切 P1 修复原 `15a222e7` 与 4.0 已 cherry-pick 版 `155325bf` 同改动 git patch ID 自动识别无冲突）；git 经过 fsck + reflog 核查状态正常，无半成品；4 个 dangling 是历史 stash 锚点与本会话无关
- 全量测试：全绿（2026-07-09 本轮 `./gradlew.bat --no-configuration-cache compileJava` 与 `./gradlew.bat --no-configuration-cache test` 通过；具体用例计数以本地 Gradle 输出为准）
- 迁移工程真机综合验证：✅ 用户已确认无异常（启动加载 + 保存 reload + 重启回灌 + 异常容错 + 字体兜底 5 项全过）
- 深化 + 输入修复真机验证：✅ 用户已确认全部通过（P6 补全、下拉框输入修复全链、SceneSelect 展开/点击收起行为、characterFontRules fontName autocomplete 候选行为、fontSort 预填充等）
- 字体渲染真机验证：✅ 用户已确认通过（字符硬裁切 P1 修复见 §4，硬边消失、细笔画不再被吃薄）

## 2. 当前未收敛项 Open Items

> 仿 `NORTH_STAR.md`《偏离登记》范式。每项含 what/why/blocked-by/next/优先级。
> 收敛后即从本节移除，不保留历史。

<open-items>

<item id="push-4.0">
  <what>本地 4.0 ahead origin/4.0 ~132 commit，按需 push</what>
  <why>本轮代码/文档整理（merge `73325a9b`+`b902e17f`+`03e637d7`，共 6 commit）叠加此前各轮；含 config 全迁移链 + scene 输入系统 P0→P2-3 + 字符硬裁切 P1 + U3 档 A + 悬空溯源门禁 + I2/revisionSignal 裁决沉淀 + autocomplete 空候选/getFontSortSnapshot 补测 + 若干「人工修改」信息违规提交（已知，原样进历史，下次起严守规范）</why>
  <blocked-by>无阻塞，按需</blocked-by>
  <next>用户确认后 push</next>
  <priority>低（按需）</priority>
</item>

</open-items>

## 3. 待拍板项 Awaiting User

- U3 范围与本会话三件裁决已于本次会话拍板落地（见 §2 各 item 演进段与 §4 末段），无待拍板项；唯一剩余项为 push 4.0 到远端（§2 `push-4.0`），用户按需确认

## 4. 最近完成 Recently Done

| 单元 | 结果 | 分支 |
|---|---|---|
| UiRenderContext GL 助手提取收尾 | 提取 `UiContextGlHelpers` 承载 `fillRoundedRect` / `drawRoundedBorder` / `applyColor`，`UiRenderContext` 从 1034 行降至 996 行；`drawText` / `pushClip` / `pushTransform` 多态钩与构造器链保持不动。JetBrains 构建 MCP 超时，回退 `./gradlew.bat --no-configuration-cache compileJava` 通过；最终全量 `test` 通过；`git diff --check` 无输出。 | refactor/ui-render-context-gl-helpers |
| render 层超千行文件拆分 | `UiMainLayerSnapshotService` 提取 `FrameSnapshot` / `TileAssemblyEntry` / `SnapshotTileAtlasAssembler` / `SnapshotFilterPassRenderer`，主类降至 729 行；`UiRenderContext` 提取 `DeferredPostMainPassQueue`，主类降至 1034 行（oracle 预估约 1003，实际按保留 API Javadoc 与委托口径收敛）。JetBrains 构建 Step0/Step1/Step2 通过；文件2阶段 JetBrains MCP 超时，回退 `./gradlew.bat --no-configuration-cache compileJava` 通过；最终全量 `test` 通过。 | refactor/render-layer-file-split |
| SceneKeyValueMap 嵌套类独立化 Step1 | `ValueType` / `ValidationErrorType` / `KeyValueRow` / `ValidationError` 提升为同包顶级类型，`KeyValueRow` 接管行 id 分配器；`SceneKeyValueMap` 保留 `Props`/`Builder` 与 create 工厂，行数降至 759。JetBrains 构建通过，`./gradlew.bat --no-configuration-cache test` 全绿。 | refactor/scene-key-value-map-types |
| P2 DataTable 硬编码颜色 token 化 + P1 R12 反模式修复 | `SceneDataTable` / `SceneDataTableEditorChrome` 的 DataTable 专用色值收归 `SceneChromeTokens`；`SceneKeyValueMap` 删除 3 处 `__getChildren().get(N)`，改用 mount 返回根节点与动作按钮局部引用；`SceneObjectField` toggle 改用 `ButtonParts` 具名引用绑定内部 label。JetBrains 构建通过，`git diff --check` 无输出，scene 边界门禁通过。 | fix/p2-token-r12-children |
| 旧栈核心清除阶段 8-10 | 删除旧 `ui/layout`、`ui/dom`、`ui/style` 剩余包；按外部引用清理规则同步删除旧 `ui/animation`、`ui/component` 残留及测试；补齐 `ui/base/layout/UiInsets` 并断开 base 对旧 style/layout 的依赖；`NetStoreUiBridgeTest`、命令用法测试与分层门禁更新到旧栈清零边界。JetBrains 构建通过，`./gradlew.bat --no-configuration-cache test` 全绿。 | refactor/migrate-ui-base-umbilical |
| 代码与文档整理（2026-07-08） | merge `73325a9b`+`b902e17f`+`03e637d7`（3轮闭环）。**文档悬空修复**：specs 下 `FieldShell`→`FormFieldShell` 全替换（20+ 处），旧包路径 `scene/config/`→`scene/form/`；`项目结构.md` 旧栈矛盾统一（旧栈活跃，退役待裁），`scene技术债.md` §四矛盾告示清除；`modern-config-ui-design-review.md` M3 节残留行号与过时结论修复（P1，reviewer 发现后回填）。**废弃清理**：`DocumentEffectChain.MAX_BACKDROP_BLUR_RADIUS`（零引用 @Deprecated 常量）删除。**门禁扩展**：新建 `scripts/check-doc-stale-lineno.ps1`（扫 .md 中 file.ext:NNN，oracle 选项B设计），`硬约束总目录.md` 10处行号引用改语义锚，接入 `check-doc-discipline.ps1` 链；全链双绿 + gradle 全测试通过。SystemDocumentCursorHost 清理判 C（保留，现状已100% 转发给 SystemUiCursorHost，接口迁移三步走留后续轮）。 | 4.0（直接） |
| autocomplete 空候选独立路径 + getFontSortSnapshot 空返回补测（技术债还清） | commit `b7f5e6ca`。侦察坐实空候选**无独立行为分支**（共用 collapse），既有 L3 已覆盖「打字后 filtered 变空」×2；补 L3 `emptyCandidateSourceNeverExpandsOverlay`（空候选源 focus+打字 expanded=false）锁死契约。`getFontSortSnapshot` 原零断言，新建 `FontConfigTest` 补 3 边界（null→空数组/空数组→空/非空→防御拷贝）。reviewer 全过（3+19 双绿、零生产代码改动、断言有区分度）。详见 `docs/诊断层/scene技术债.md`。 | test/autocomplete-empty-font-snapshot |
| refactor 分支整支合回 4.0 | merge commit `29e4286b` `--no-ff` 整支合并 34 提交（含字符硬裁切原 `15a222e7` 与 4.0 上已 cherry-pick 版 `155325bf` 同改动、git patch ID 自动识别无冲突无重复）；dry-run 预判通过；fsck+reflog 核查工作区与分支正常无半成品，4 个 dangling 是历史 stash 锚点与本次合并无关。5 个「人工修改」信息违规提交（.opencode/agents 配置微调）按用户拍板原样进历史不 rebase（避免 reorder 经 30+ 其他提交区间风险），下次起严守规范。4.0 现 ahead origin/4.0 118 commit，未 push。 | 4.0 |
| I2 措辞正名 + revisionSignal×I8 裁决沉淀 | commit `c8e01062`。NORTH_STAR I2 修订为「所有外部 signal 写入都经过中央事务，没有任何『绕过调度器直接生效』的外部写入。Computed 派生值向下游的传播属调度器 flush 内部 sweep（recompute Effect 内 `cell.applyAndNotify`），不构成独立外部写入路径，其可追溯性由源 signal 回放后自动重算兑现」——正名非放松（评审 I2 checklist 不再字面误伤 Computed）。同时 revisionSignal 全局 bump × I8 张力判非违反（记忆化挡下游传播、非热路径、精确化破零 uilib 依赖国策、收益虚），裁决沉淀到 `docs/反馈层/决策/reactive-computed-i2-i8-rulings.md`，scene技术债:69 挂账同步销。属设定值层重大变更，用户拍板 A 正名 + ④ 判非违反+决策文档。保留升级路径。 | refactor/config-fullmigrate-modern（已合 4.0） |
| 悬空溯源上溯传感层门禁（§4.3 上溯） | commit `f9b48cef` 立门禁 `scripts/check-javadoc-stale-line-refs.ps1`（仅注释/Javadoc 文本跑正则，模式 1 `Method():NNN` / 模式 2 `ClassName:NNN` 3 位起防端口误报，剔除 URL/mail）+ `docs/反馈层/错误预防.md` 追加 Javadoc 行号引用段通则旁注 + `docs/传感层/门禁脚本说明.md` 新增三、Javadoc 行号引用门禁段；commit `f7cfd4c9` 执行 Path A 全清理 4 处存量跨类维护锚（SceneTab:207 / ConstraintResolver:347,368,371 改 `Class#method` 不带行号）+ 接入 `check-doc-discipline.ps1` 全链，自检 0 命中、全链双绿、编译干净。oracle 否决选项 A（不动 NORTH_STAR）与选项 C（不选仅通则，§4.3 要求上两层都不可行才退回）。 | refactor/config-fullmigrate-modern（已合 4.0） |
| 真机会话收尾与文档回写 | 4 commit `7dce2dfa`+`9f9c600b`+`a1ddbcf5`+`b909f14f`：深化+输入修复全链真机确认通过；P7 字符网格 picker 用户取消不做（§2 同移除）；`docs/诊断层/Config模块.md:3` stale 措辞订正为「U1/U2 已落地、U3 规划中」；交接简表阶段 C/D/E 5 行归并为 1 行降阈值（16>15→12 行）恢复 check-doc-discipline 全链双绿。 | refactor/config-fullmigrate-modern（已合 4.0） |
| U3 档 A FieldRenderer 样板收敛 | commit `2569dd3d`。新建 `FieldRenderSupport`（labelOf/toStringSignal/toNumberStringSignal/toDoubleSignal/toDouble/formatReadout 六工具）+ `FieldShellBinder`（build 双重载对偶 FormFieldShell.build），收口 7 renderer 三类重复样板（labelOf 7 处同源 / NumberFieldRenderer double 转换+格式化 / error+dirty+labelOf+helper+FormFieldShell.build 装配序列）。承继约束全守：FormFieldShell 未改守 U1、不抽基类守 U2、DraftSignalAdapter/Computed 未改 I2/I8 已裁决。13 新单测 + 82 既有回归全绿；编译干净；scene 边界/文档纪律/Javadoc 行号引用门禁全链通；reviewer 10 项 OK 无 P0/P1/P2。oracle 否决档 B（YAGNI）档 C（破国策+推翻 U2 结论）。 | refactor/config-fullmigrate-modern（已合 4.0） |
| autocomplete / characterFontRules / 深化 P-1+P0-P5+P6 一链 | 多 commit (`7d3067e4`+`f37cd2d4`+`df73117f`+`daebbd55`+`6751a329`+`9fcfea5b`+`3561a9c4`+`f678d19f`+`5a2af136`+`9cc39ed6`+`dc3bb87c`)；合成一链：P0 helper / P1 Registry path / P2 SceneSimpleList draggable / P3 fontSort 接入 / P4 CharacterRuleFieldRenderer 三栏 / P5 selector 逗号多点 / P-1 errorMessage 派生改 parseLine / P6 SceneAutocompletePrimitive（487 行组合）+ caret 到末尾 + characterFontRules autocomplete 候选修复（初始高亮修复 + CONTAINS 精确命中抑制重弹）。L2/L3 回归全过 + reviewer 全过；守 I1/I3/I5/I11 + R1-R12/R11/R13。详见 `docs/反馈层/决策/font-character-deepen.md`。P7 字符网格 picker 用户取消不做。 | refactor/config-fullmigrate-modern |
| 组件行为对齐现代组件低风险打磨（M2 滚动条可发现性 + m1 error 空节点常驻 + M4 徽标对比度） | commit `a0c5d62c`。生产 4：SceneChromeTokens `SCROLLBAR_THUMB_IDLE` 0x66→0x99（idle 60%）；SceneScrollbar `DEFAULT_TRACK_COLOR` 0x33→0x44；ConfigScreen 类头"固定宽 8"→"DEFAULT_BAR_WIDTH(=8)" + `badge()` 边框 1px→2px；FormFieldShell error 节点无条件 append 改 `rt.show` 条件挂载（守 R11，补作用域注释）。测试 5：field renderer 测试 helper 从"倒数第二个子节点"改为按结构特征扫描，消除位置依赖。全量测试通过，reviewer P2 已收。 | refactor/config-fullmigrate-modern |
| scene 输入系统 P0→P2-3 修复链（下拉框 item 点击失效根治 + harness 抽取 + 文档沉淀） | 分 P0/P1/P2-1/P2-2/P2-3 五段，全程闭环 reviewer 通过。**真因 D1**：真机 DOWN 一帧/UP 另一帧时，DOWN 帧 flush 触发 focus 掐断 + overlay 卸载，UP 命不中已卸载 item → CLICK 不合成；**真因 D2**：9 参兼容构造致 chrome=NOOP 浮层无背景。**P0 止血**（`28da6cc7`+`6c1aaf09`+`ac58dc14`）：命中 active overlay 豁免 clearFocus + 注入真 chrome + 跨帧回归守卫。**P1 LCA 化 + R13**（`7182b166`..`bd132f7f` 5 commit）：CLICK 合成从严格相等升级为 LCA 祖先容差（复刻旧栈 findNearestCommonInclusiveAncestor）+ 新立控件契约 R13（浮层显隐状态独立可写禁派生自瞬态交互态）+ SceneInputRouterLcaClickTest 4 用例。**P2-1 R13 重构 + scheduler 补全**（`6e297e1c`+`7df29594`）：autocomplete expanded 从 Computed(focused) 重构为独立 Signal + effect 驱动；ReactiveScheduler flush 补全 drainPendingWrites+runDirtyEffectsOneSweep 双通道交替到不动点（撤应急 setImmediate 守 I2，还清 A1 技术债），ReactiveSchedulerEffectSetSameFlushTest 4 用例。**P2-2 harness 抽取**（`ed363b51`+`517d72e5`）：clickCrossFrame 抽为 harness `pressReleaseAcrossFrames(node, betweenFrames)` 时序原语（oracle 深评否决重方案「复刻 layoutOverlays」，采轻方案 harness 不承担 overlay layout、overlay 重排回调交还测试侧，守 YAGNI+I12），harness 自测 3 用例，reviewer 通过。**P2-3 文档**（`8fc90c8e`）：测试体系约定 §7.5 事件注入帧时序语义（同帧 vs 跨帧判据表 + 同帧假绿红线 + overlay 跨帧卸载陷阱）。守 I2/I9/I11/I12 + R11/R13。 | refactor/config-fullmigrate-modern（已合回 4.0） |
| 真机问题修复 2 项（下拉框点击 + fontSort 预填充） | 2 commit `1c3cbab7`+`fdbfddf9`；**下拉框**：oracle 裁决真因 E'（LwjglInputSource poll-based 差分对连续点击第二次操作系统性丢失 DOWN+UP 边沿，浮层 mount 负载骤增导致帧间双丢）；方案 A（McScreenBridge 重写 mouseClicked/mouseMovedOrUp → pushPointerButton 事件旁路，externalPointerMode 开关与 poll 去重，坐标换算 physicalX=mouseX*scaleFactor Y 轴无需翻转）；explorer 5 维度诊断 ses_0cae7edeeffe + oracle ses_0cad66ac7ffe + reviewer ses_0cac1156b 通过（无 P0/P1，Y 轴翻转数学推导确认正确）；7 新测试 + 全回归。**fontSort 预填充**：诊断确认空配置是有意设计但用户要"自动预填"；方案 A'+选项①（seedFieldBaseline 抹平 dirty：draft+current 双写使 dirty=false，用户不编辑不写盘运行时零改变）；已验证 FontConfig.getFontSortSnapshot() 空配置时已中文优先（FontOrderPlanner 用 DefaultFontOrderHints 排序）零退化；explorer ses_0cae7edf8ffe + oracle ses_0cad66abdffe + reviewer ses_0caafe1cd 通过（P1-1 浅拷贝 Javadoc 已采纳）；核心层 DraftBuffer/DraftSignalAdapter 纯加法零回归 + 4 新测试 | refactor/config-fullmigrate-modern |
| 字符硬裁切 P1 根治（uvBounds 硬墙切 mipmap 羽化） | refactor 原提交 `15a222e7` 已随分支整支合回 4.0（merge `29e4286b`，与早先 cherry-pick `155325bf` 同改动 git 自动识别无重复）；根因：`fontF.frag` safeSample 把 mipmap 缩小时物理存在于 padding 区的羽化低 alpha 当作越界归零，原实现顶点 texCoord 与 uvBounds clip 复用同一组 ink±bleed UV，硬墙只放 1px 不足吸收 mipmap 羽化尾巴。修复：`GlyphQuadMetrics` 拆分 texCoord 与 uvBounds clip（slot 边界含完整 8px padding），`GlyphRenderBatch.addQuad` 顶点 texCoord 不变、uvBounds 字段改传 clip 值；shader 无改动。font 模块 13 测试类全绿，reviewer 通过。约束：后续调 aaStrength 须保证 AA 采样偏移 < INK_PADDING=8。真机已确认通过。 | refactor/config-fullmigrate-modern（已合 4.0） |
## 5. 长期未回填（指针）

- scene 技术债权威清单 → `docs/诊断层/scene技术债.md`
- 宪章偏离登记 → `NORTH_STAR.md`《偏离登记》段
- 架构边界与 strangler 路线图 → `docs/设定值层/边界.md`
- 关键取舍背景 → `docs/反馈层/决策/`

## 维护说明

- 本文件是唯一权威状态源，状态变化直接覆盖，不堆历史交接。
- 只保留：当前状态 + 未收敛项 + 待拍板 + 最近完成简表 + 长期指针。
- 瞬时状态（分支/HEAD/工作区/测试/阻塞/待办）写本文件；
  控制律（完成标准/约束/审核纪律/fixer 串行规则）归 `AGENTS.md` 与
  `SUBAGENT-ORCHESTRATION.md`，不入本文件（避控制律误并入状态）。
- 长期事实按控制分层归位（设定值/控制律/传感/诊断层），关键取舍归 `docs/反馈层/决策/`，错误预防通则归
  `docs/反馈层/错误预防.md`，本文件只留指针。
- 未收敛项收敛后即从 §2 移除，不保留历史。

## 状态层次边界（防混淆）

本文件是**项目级**状态源（跨多任务、长期）。**会话级**工作记忆（单任务内、易失）不进本文件，归
`docs/控制律层/编排模式/SESSION-HANDOFF.md` 模式下的 `.opencode/session-handoff.md`。
会话 handoff 中"有持续价值的事实"任务完成后才回流到本文件或决策/错误预防，其余丢弃。
