# 交接（项目状态权威源）

> 业务状态、阻断项和待决策事项以本文件为准；实时分支、HEAD 与工作区以 Git 命令为准。

## 当前状态

- 已推送的 `4.6.3` 代码候选为 `fix/font-depth-state@79aa0bd640a6fc7546dfd3ff719b5652c138b67c`；绑定该提交的 GitHub Actions `Build and test` run [`30153547093`](https://github.com/QuanhuZeYu/Qz-UILib/actions/runs/30153547093) 已为 `completed/success`。该 run 只证明代码候选的常规 CI，不是后续最终 merge commit 或正式发布四资产的身份。
- 本次四文档提交在本状态快照中尚未创建、尚未推送；合入前远端 `4.0` 基线为 `15545344139f36fb36de1a78af3de1f1fd5cb926`。完整正式发布链已获授权，但 `--no-ff` 合入后的 merge commit `M`、annotated tag object `T` 与数字 Release ID 均尚不存在，也尚未 merge、tag 或创建 Release，正式发布闭环状态为 `INCOMPLETE`。
- 用户已完成基础客户端 smoke，未发现严重问题；该反馈不等同于完整 Angelica、shader/shadow、dedicated server、FML 双端握手或游戏内标签矩阵验证。
- FML 同 minor 远端版本范围已由源码提交 `2c025422d5b0ca21066b4128cef8bc5d78f74519` 固定为 `[4.6.2,4.7.0)`；主信封 v2 与 Realtime v1 未变，相关实现与契约测试已包含在成功的候选 run `30153547093` 中。已发布旧 `4.6.2` 端点仍按精确版本拒绝正式 `4.6.3`，不能由新端点单方面兼容；发布 JAR annotation、dedicated server 双向握手与真实流量仍无本次实证，状态为 `INCOMPLETE`。
- `4.6.2` annotated tag object 固定为 `6155c157b823c928accc25b037f7a95e7e83d669`，peeled commit 固定为 `e86a731cf10c5fa9e0f3dd87fe52126646bf8ed1`，不可移动。
- 固定 recovery 的 [verify run `29989023055`](https://github.com/QuanhuZeYu/Qz-UILib/actions/runs/29989023055) 与 [publish run `29989322298`](https://github.com/QuanhuZeYu/Qz-UILib/actions/runs/29989322298) 均为 attempt 1、`completed/success`。
- GitHub Release [`4.6.2`](https://github.com/QuanhuZeYu/Qz-UILib/releases/tag/4.6.2)（ID `357902877`）已于 `2026-07-23T07:53:45Z` 发布；ID/tag 两端点一致，notes 与 main/dev/sources/dev-preshadow 四资产的名称、大小和 digest 均已核对。
- 仓库自动化已收敛为常规 build CI、内联零 PowerShell policy 和已完成的固定 `4.6.2` recovery。verify 只读构建并生成、上传 bundle manifest artifact；publish 仅在 PATCH 前按固定 ID 完整核对，唯一写操作是 `draft=false`。通用 tag、JitPack/Maven gate、外部 checker 与 agent 本地 Gradle 能力均已退役；未来需要时须独立重建。
- Qz-UILib 4.6.0 与 4.6.1 的既有 GitHub Release 保持完成；4.6.1 的历史 JitPack 验证结果不变，但不代表仓库当前仍有通用 advisory 能力。
- GTNH `2.9.0-beta-2` 自动化适配已有历史测试/编译/build 成功结果；当前 `4.6.3` 新增的运行态证据仅为上述基础客户端 smoke，未覆盖完整运行矩阵。
- issue [#65](https://github.com/QuanhuZeYu/Qz-UILib/issues/65) 已[回复终态](https://github.com/QuanhuZeYu/Qz-UILib/issues/65#issuecomment-5055913391)并关闭，状态为 `CLOSED/COMPLETED`。
- Picker 当前复现的 HostImage 崩溃、全空白及 14 项末行裁切已得到抑制；完整交互矩阵与第三方 renderer 尚未覆盖。
- 字体共享准备已改为继承调用阶段的 depth test/mask/func，screen/HUD 仍由 UI host 显式建立无 depth test 状态；实现与纯源码契约测试已包含在成功的候选 run `30153547093` 中。基础客户端 smoke 未见严重问题，但不证明告示牌正反面遮挡与 screen/HUD 字体矩阵，专项真机验证仍缺失。
- 玩家标签顺序与字体 depth 所有权保持正交：`6a9a375b` 继续保留，world-last 初版 `de15830c` 及安全回退 `4d25977c` 的历史结论不变；当前由 `8d55cf45`、`efc0d755`、`f48a4e46` 三笔独立提交实现 pass-scoped FIFO、普通名称/计分板最终调用点 `@WrapOperation`、Angelica `2.1.50` 可选 guard 与 lightmap 双层恢复。最终组合独立静态复审未发现 P0/P1/P2，A1-A9 静态满足，且已包含在成功的候选 run `30153547093` 中；基础 smoke 不替代 dedicated server、Angelica shader/shadow 与游戏内标签矩阵，最终 merge CI 与发布制品也尚不存在，状态为 `INCOMPLETE`。
- scene 宿主关闭系统光标扫尾已补齐：`SceneRuntime` 对每个已绑定 backend 在关闭生命周期强制复位 `DEFAULT`，不改 `cursorSignal`，并以幂等 finally 覆盖 Owner 清理失败；runtime fake backend 回归测试已包含在成功的候选 run `30153547093` 中。基础 smoke 未专项覆盖非默认光标关闭与连续界面切换，游戏内矩阵仍无实证，状态为 `INCOMPLETE`。

## 当前未收敛项

<open-items>

<item id="release-4.6.3">
  <what>代码候选 `79aa0bd6` 已推送且候选 CI 成功；四文档提交在本状态快照中待创建、待推送，完整正式发布已授权，但 `M`、`T` 与 Release ID 尚不存在</what>
  <blocked-by>依次完成文档提交推送及其候选 CI、基于 `4.0@15545344` 的 `--no-ff` 合入、最终 merge CI 与四 JAR 核验、annotated tag 和 ID-bound draft 正式化</blocked-by>
  <next>严格按 `docs/控制律层/发布流程.md` 的 `4.6.3` 一次性合同执行；任一身份不符即停止，部分成功时保留现场并回写 `M/T/Release ID`</next>
  <priority>高</priority>
</item>

<item id="fml-remote-version-compatibility">
  <what>有界同 minor 范围与契约测试已随候选 run `30153547093` 成功，但双端 FML 握手和真实线协议兼容尚未闭环</what>
  <blocked-by>缺少最终发布制品 JAR annotation，以及 dedicated server 双向版本矩阵实证</blocked-by>
  <next>核对最终生产 JAR 的 `acceptableRemoteVersions`；使用两个都携带范围的端点覆盖同版本、后续 patch 双向与越界版本，再分别覆盖客户端缺 mod、服务端缺 mod，并在握手成功组合中发送真实主信封 v2 与 Realtime v1 流量</next>
  <priority>高</priority>
</item>

<item id="font-world-depth-runtime">
  <what>全局字体替换后的告示牌世界深度遮挡与 screen/HUD 二维文字运行态尚未实测</what>
  <blocked-by>用户运行客户端并启用原版字体替换</blocked-by>
  <next>从告示牌正反面核对文字可见性与模型遮挡，并回归 screen/HUD 二维文字</next>
  <priority>中</priority>
</item>

<item id="player-name-tag-render-order">
  <what>普通玩家名称/计分板标签的 pass-scoped 延后实现、静态复审与候选 CI 已完成；旧视觉问题已有受限方案，但运行态闭环尚未完成</what>
  <blocked-by>缺少最终发布制品与 shader/shadow、dedicated server、游戏内矩阵实证</blocked-by>
  <next>核对无 Angelica、精确 2.1.50、未知版本降级和 dedicated server；游戏内覆盖普通名称/计分板/潜行、箱子前后遮挡、昼夜与多亮度，并分别观察 Angelica shader 主 pass/shadow 无重复、串帧或状态泄漏</next>
  <priority>高</priority>
</item>

<item id="scene-vanilla-slot-host">
  <what>issue [#66](https://github.com/QuanhuZeYu/Qz-UILib/issues/66)“为 scene 提供正式 GuiContainer / vanilla Slot 宿主桥”保持 `OPEN + enhancement`，仅排队为高优先级独立架构/API任务</what>
  <blocked-by>公共 API、兼容边界与宿主桥契约尚未在独立任务中定稿</blocked-by>
  <next>另开任务完成契约分析、设计与实施；当前不代表已有实现、版本或发布日期，也未修改远端 issue、label、project 或 milestone</next>
  <priority>高</priority>
</item>

<item id="scene-cursor-close-cleanup">
  <what>scene screen close 的 `forceApply(DEFAULT)` 生命周期扫尾与 fake backend 测试已随候选 CI 成功；同一 screen 内结构卸载后的 hover/cursor 重算不在本次范围</what>
  <blocked-by>缺少游戏内非默认光标关闭、连续切换界面的运行态实证</blocked-by>
  <next>游戏内分别从 pointer/text/none 等非默认样式关闭 scene 界面并打开后续界面，确认系统光标立即恢复 DEFAULT，另将 portal/条件节点/动态列表卸载残留作为独立任务处理</next>
  <priority>中</priority>
</item>

<item id="gtnh-runtime-and-picker">
  <what>GTNH beta-2 基础运行态与 Picker 完整交互/第三方 renderer 矩阵未完成</what>
  <blocked-by>用户运行客户端并按需覆盖 smoke</blocked-by>
  <next>验证启动、配置页、scene test hub、连续添加、滚动/焦点、编辑删除与第三方 renderer</next>
  <priority>中</priority>
</item>

<item id="remote-api-lts-cleanup">
  <what>LTS/使用文档中的通用远程 HTML 能力可能与已删除源码不一致</what>
  <next>另开文档任务核对源码与对外文档</next>
  <priority>低</priority>
</item>

<item id="cross-process-os-cas">
  <what>配置写入仍不提供跨进程 OS 级 CAS</what>
  <next>若产品需要，再独立设计文件锁或单写者协议</next>
  <priority>低</priority>
</item>

</open-items>

## 待拍板项

- 未来通用 tag、JitPack、Maven 或 agent 本地 Gradle 能力均不在当前范围，重建前须重新拍板。

## 长期指针

- scene 技术债：`docs/诊断层/scene技术债.md`
- 宪章偏离：`NORTH_STAR.md`《偏离登记》
- 架构边界：`docs/设定值层/边界.md`
- 关键取舍：`docs/反馈层/决策/`
