# 交接（单一权威状态源）

> 本文件是项目唯一权威状态源。`现状.md`/`计划.md` 已并入本文件并删除。
> 任何状态冲突以本文件为准（消除多头发散，建立单一误差向量）。
> 控制律（完成标准/约束/审核纪律）不入本文件，归 `AGENTS.md` 与
> `docs/控制律层/编排模式/SUBAGENT-ORCHESTRATION.md`。

## 1. 当前状态 Current State

- 当前分支：`add/config-structured-list`；当前 HEAD 精确值以 `git log -1` 为准；本次交接状态提交包含本文件更新；工作区干净；未执行 merge/push/tag/release。
- 提交对象与提交信息精确以 `git log -1` 为准。
- 工作区：干净；未 merge/push/tag/release。
- 发布口径：**4.5.3-beta-5** 本地 Maven 发布验证；beta API 非 LTS；**不是**稳定 4.5.3；正式 tag / push / GitHub Release **尚未执行**
- 前一分支工作：`add/config-draft-validator` 的 4.5.2 DraftValidator 已被本分支 4.5.3-beta-1 能力取代，不再作为当前阻断口径
- 前一正式版：`4.5.1`（issue #63 裁切）
- 本轮完成：beta-1 配置事务；beta-2 递归 ValueSpec；beta-3 输入与文本桥；beta-4 `List<CHOICE>`；beta-5 SearchPicker 契约、scene 控件、ConfigUI 与 StructuredList member 接入，并完成 provider 注册快照与截断标志保留纠偏。未执行 merge/push/tag/release，未修改 Qz-Miner。

### 本轮诊断证据边界

- **日志证据**：`Qz-Miner/run/client/logs/fml-client-latest.log:15313-15321` 是 beta-2 修复前基线，仅含 ROW/COLUMN grow WARN，不能证明 beta-3 行为。
- **代码诊断**：生产 Config 未注册 text bridge 是中文 IME 根因；renderer 本地 SSOT 缺失是确定的一键失焦根因。两者是代码结论，不冒充日志结论。
- ROW/COLUMN grow WARN 本轮未顺手改布局，待修复后实机复验是否能定位到本 renderer 固定兄弟缺 preferred 尺寸。



## 2. 当前未收敛项 Open Items

> 仿 `NORTH_STAR.md`《偏离登记》范式。每项含 what/why/blocked-by/next/优先级。
> 收敛后即从本节移除，不保留历史。

<open-items>

<item id="remote-api-lts-cleanup">
  <what>LTS / 使用文档中通用远程 HTML（RemoteDocumentPages / RemoteHudOverlays）仍标可用，但 ui.remote 包可能已从源树移除</what>
  <why>阶段 1 审核发现的预存文档债，不在 config 对齐 scope</why>
  <blocked-by>无</blocked-by>
  <next>另开 docs 任务核对源码后清 LTS/远程页面文档</next>
  <priority>低</priority>
</item>

<item id="release-4-5-3-beta">
  <what>4.5.3-beta-5 搜索选择器与结构化列表能力实现、测试及本地制品验证已完成，待 dedicated smoke、用户决定发布，再决定 beta 连续迭代或稳定 4.6.0</what>
  <why>本轮为 4.5.3-beta-5（非稳定 4.5.3）；未执行 merge / tag / push / release</why>
  <blocked-by>dedicated smoke + 用户确认 beta 发布路径与 beta/稳定节奏</blocked-by>
  <next>dedicated smoke → 用户确认后再发布 beta；稳定公共能力归 4.6.0</next>
  <priority>中</priority>
</item>

<item id="qz-miner-consume-beta">
  <what>Qz-Miner 已接入 UILib 4.5.3-beta-1:dev、owner 语义、写前检测、RELOAD 事件与冲突恢复；待发布版可解析后跨仓验收</what>
  <why>正式远端尚无 beta；空 Maven Local 时解析 404。消费者必须处理 RELOAD，不得假设 reload 伪装 BATCH_SAVE</why>
  <blocked-by>release-4-5-3-beta + Qz-Miner 用户 runClient21/runServer25</blocked-by>
  <next>beta 发布后 → Miner 依赖正式来源解析 → 验收 INVALID/stale/reload、网络与字体</next>
  <priority>中</priority>
</item>

<item id="cross-process-os-cas">
  <what>跨进程配置写仍非 OS 级 CAS；beta 仅保证同 JVM classloader 参与式 writer 串行精确字节写前检测+replace</what>
  <why>用户拍板 beta 纳入写前检测但不虚假承诺 OS 级；跨进程窗口需更高层文件锁/原子 rename 协议</why>
  <blocked-by>产品是否要求跨进程强一致</blocked-by>
  <next>若 4.6.0 需要跨进程，再设计 OS 级文件锁或单写者约定</next>
  <priority>低（P2 口径已文档化）</priority>
</item>

</open-items>

## 3. 待拍板项 Awaiting User

- 无架构待拍板（stale draft 恢复 + owner fail-closed + I3 prefill + 写前检测 + reload 校验/RELOAD 用户已批准本轮清单）

## 4. 最近完成 Recently Done

| 单元 | 结果 | 分支 |
|---|---|---|
| 4.5.3-beta-5 通用 SceneSearchPicker | SearchResult 二次限额保留上游 truncated；Registry 冻结 id/codec/visual/search；受控控件、ConfigUI/StructuredList 接入及缺失/可变 provider、少量截断 UI 回归 | add/config-structured-list |
| 4.5.3-beta-4 结构化列表 choice 多选 | `List<CHOICE>` 受控 checkbox；schema 顺序去重；未知失效项只删；passthrough、INVALID 零写盘、输入与 reset/reload 回归 | add/config-structured-list |
| 4.5.3-beta-3 输入体验修复 | StructuredList 先 rows 后 adapter、有限 identity lineage、逐字符 ASCII/中文完整 String/focus/reset 回归；McScreenBridge 唯一 owner；文本桥无初始化探测、完整配对计划、半副作用回滚与清理重试 | add/config-structured-list |
| 4.5.3-beta-2 结构化列表纠偏 | identity keyed reset/reload；identity 仅允许四种稳定标量，LIST/OBJECT 构建期拒绝；模型覆盖缺失/null identity；动态 List 后代错误、renderer 删错项/前插/前项平移/清错、事务失败零提交断言 | add/config-structured-list |
| 4.5.3-beta-1 配置协调器异常活性 | hook 无条件释放 owner + ErrorSink Assertion 尾重排 + drain owner CAS + scalar section fail-closed + Config 测试矩阵 | fix/config-stale-draft-recovery |
| fontSort 回归修复与交互增强 | screen-open frozen discovered snapshot；纯 Java merge/filter/index/filtered-drag；presentation-only 预览与完整列表提交；稳定筛选栏/索引输入；modern/scene 回归与文档同步 | fix/config-stale-draft-recovery |
| fontSort 拖拽终审 P1 收口 | 已激活原位 UP 仍结束拖拽；阈值前 CANCEL 不回调；逻辑状态在 consumer 回调前清理，视觉 offset 同帧 flush 后归零；filter 解冻回读、finish/cancel 异常安全与 headless 事件链回归；最新视觉交互仍待用户手工实机 | fix/config-stale-draft-recovery |
| 4.5.3-beta-1 批次/线性化/raw 闭合 | batch swap + Registration 强线性化 + raw overlay + Category + 全量门禁 | fix/config-stale-draft-recovery |
| 4.5.3-beta-1 并发回滚纠偏 | reload 三阶段+ConfigReloadException+主线程回灌+类型严格+Persistence 测试缝内收 | fix/config-stale-draft-recovery |

| 4.5.3-beta-1 终审收口 | reload 完整校验 + RELOAD 事件 + 通知期封锁 + 参与式 writer 口径 + owns/ConfigScreen fail-fast + ABA/IO 测试缝 | fix/config-stale-draft-recovery |
| 4.5.3-beta-1 磁盘写前检测 | ConfigFileSnapshot + save/flushRaw + reloadDraftFromDisk + UI 主线程 + changelog | fix/config-stale-draft-recovery |
| 4.5.3-beta-1 纠偏 | Draft 所有权 + I3 prefill + 真实 reload 按钮 + 冲突窗口精确断言 | fix/config-stale-draft-recovery |
| 4.5.3 stale draft 易用性 | ConflictType + 事务 base + replaceDraft + presentation seed + 显式 reload UI | fix/config-stale-draft-recovery |
| DraftValidator 完整终审 | capture→锁外 validate/prepare→verify/write/引用交换 | add/config-draft-validator |


## 5. 长期未回填（指针）

- scene 技术债权威清单 → `docs/诊断层/scene技术债.md`
- 宪章偏离登记 → `NORTH_STAR.md`《偏离登记》段
- 架构边界与 strangler 路线图 → `docs/设定值层/边界.md`
- 关键取舍背景 → `docs/反馈层/决策/`
