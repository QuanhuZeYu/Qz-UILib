# 交接（项目状态权威源）

> 本文件是业务状态、阻断项和待决策事项的权威源；实时 Git 分支、HEAD 与工作区状态以 `git branch --show-current`、`git rev-parse HEAD`、`git status` 为权威。控制律归 `AGENTS.md` 与编排文档。

## 当前状态

- Qz-UILib 4.6.0 的最终 `4.0` commit `93e7ac07`、annotated tag 与 [GitHub Release 4.6.0](https://github.com/QuanhuZeYu/Qz-UILib/releases/tag/4.6.0) 均已完成，Release 附带 4 个正确 JAR 资产；GTNH Maven 当次未发布，但用户已明确它不是 Qz-Miner 或补丁发布的前置条件，不存在 Maven 凭据阻断。4.6.0 的 JitPack canonical metadata/classifier 缺陷已由不可移动的新补丁 tag 4.6.1 修复，4.6.0 tag 保持不动。
- Qz-UILib 4.6.1 已从 `4.0@c2d3ea91` 以 annotated tag 发布；[tag workflow 29554928156](https://github.com/QuanhuZeYu/Qz-UILib/actions/runs/29554928156) 的 Release gate、真实 JitPack Remote、clean `:dev` consumer、reusable Release 与 notes 均成功。JitPack canonical POM/main/dev/sources、sha1、Build API 与 build.log 验证通过，JitPack-only `.module=404` 符合方案合同；[GitHub Release 4.6.1](https://github.com/QuanhuZeYu/Qz-UILib/releases/tag/4.6.1) 非 draft/prerelease，正文匹配版本说明且 4 个 JAR 资产齐全，Modrinth/CurseForge 成功。GTNH Maven 步骤 skipped、尚未发布且不是前置条件；当前无 4.6.1 发布阻断。
- 开发依赖基线已适配 GTNH `2.9.0-beta-2`；受审计 Gradle 协议下 `test --no-configuration-cache`、`compileJava --no-configuration-cache` 与 `build` 均成功。`runClient21` 运行态验证未执行，当前结论保持 INCOMPLETE。
- HostImage ItemStack 已改为跨帧 identity/尺寸 LRU、小 FBO、每帧 2 次/2ms 公平补图与 5 秒失败冷却；`UiRuntimeAdapters` 对任意 renderer 统一施加唯一完整 GL 状态围栏。Core Profile 三类固定管线栈逐项能力降级，并对 client-active texture 子能力独立运行态探测。用户复测确认当前复现的 Core Profile client-active 崩溃、图标全空白及 ItemStack 显示阻断得到抑制；该结论不覆盖第三方 `IItemRenderer` 或完整 renderer 矩阵。

- Scene 输入按钮旁路已改为在 push 时读取与 MOVE 同源的物理坐标，不再由 Minecraft scaled 回调反推；TextInput 增加 enabled+未聚焦 hover chrome，并在建树期声明 focused signal，Portal 首次权威聚焦可同步显示 border/caret。ItemStack scene backend 使用 GUI 可见 zLevel 并恢复调用前值。StructuredList 测试已改用语义定位真实 `Manage`，摘要继续保持非交互。
- StructuredList member 已具备可选中文显示名/辅助说明元数据，保持 YAML key、校验和兼容语义不变；成员编辑区改用可复用纵向 labeled-control 安全默认，去除 90px 标签与 180px 输入固定宽。
- 4.6.0 通用被动 HUD API 已按 I13 收口：bridge 使用 framebuffer 尺寸，layout/paint/font/clip 使用 UILib logical px；host-owned scale 默认 1.0、五档可选且不读取 MC GUI scale。Compact/Normal 字号为 12/14px，外框按内容 intrinsic 宽收缩并在视口边界 clamp，支持通用 min/max width；`HudSpan`、`HudLine.rich/richProgress` 已提供 keyed 富文本，tone 仅表达语义且旧 `HudLine` API 兼容。registration 在断线或世界卸载后保留，不要求仅因这些生命周期事件重新注册。
- 4.6.0 发布说明与 CHANGELOG 已收口为最终 6×42px / 12×34px Picker 契约，并归并 HostImage 缓存/FBO/GL 围栏、Core Profile 降级、输入/焦点/zLevel 与最终 Picker 修复；测试结果不写入 Release 正文。
- beta-10 将 SearchPicker beta API 收敛为 ALL/SELECTED 两态，SELECTED 支持 1..N checkbox，并无损展示、保留和移除当前未枚举 key。稳定契约见 `docs/反馈层/决策/config-search-picker.md`。
- SearchPicker 的 `LIST_MEMBERS` 关闭态现按左侧 `Manage`、摘要顺序排列，raw 默认折叠为高级修正/删除入口；管理 portal 采用 480px 目标宽、360px 最小宽、8px safe inset 与视口高度 cap，顶部搜索。当前成员改为 6×42px 双行：第一行主名称与 54/118px 固定按钮列，第二行完整 registry/canonical 与 0/136px issue badge；结果区仍为 12×34px。中英文、normal/pending 与问题状态不移动按钮盒，长文本各自在自身盒内裁剪，且只有真实按钮命中。候选展示只读用户 query；当前成员按唯一 candidate key 独立精确解析，空 query 仍不枚举全量。可添加结果按合法 selection key 排除已选项；新增成功后保留 portal、query、合法窗口与搜索焦点并立即重新武装。有 variants 编辑进入变体页，unknown/无 variants 保留稳定 id 进入候选替换态，确认原位替换后关闭。malformed/duplicate 继续只提示、不泄露 raw、不合并；旧 `SINGLE_VALUE`/`Codec` 与未设置 secondary 的 Provider 兼容。提交 `1fec5a85` 在固定 6×42 契约内统一真实 lineHeight、padding 与 maxHeight 后，用户复测确认 14 项滚到底时末成员 secondary 底部裁切已得到抑制。该能力已纳入 GitHub Release 4.6.0。
- Picker 目标测试、全量 `test`、`compileJava`、scene 边界与文档纪律门禁已通过；Qz-Miner 消费仓当前复现的 HostImage 崩溃/全空白与 14 项末行裁切已得到抑制，但连续批量添加、完整滚动/焦点/编辑删除状态矩阵及第三方 renderer 尚未完整实机覆盖。4.6.0 最终 CI、tag 与 GitHub Release 已完成；这不等于 Picker 全面真机通过。
- beta-9 不改配置核心语义、YAML 或 network。Picker 返回完整精确结果并用固定 8 行窗口化连续滚动；键盘高亮与 hover 独立。STRUCTURED_LIST 默认使用无框字段外壳、单层紧凑对象卡片与稳定 key 折叠意图，editor 可通过通用 current-value presenter 提供摘要。

## 当前未收敛项

<open-items>

<item id="gtnh-2.9.0-beta-2-runtime">
  <what>GTNH 2.9.0-beta-2 自动化适配已通过，runClient21 运行态验证仍为 INCOMPLETE</what>
  <why>agent 只完成受审计协议允许的 test、compileJava 与 build；runClient* 仍由用户执行</why>
  <blocked-by>用户运行 runClient21 并完成基础 UI smoke</blocked-by>
  <next>由用户按需启动 runClient21，进入配置页与 /qzuilib test 核对启动、输入和渲染基础行为</next>
  <priority>中</priority>
</item>

<item id="config-search-picker-ux">
  <what>Picker 当前报障已得到抑制，完整交互矩阵与第三方 renderer 兼容仍待 Qz-Miner 实机覆盖</what>
  <why>用户已确认当前复现的 HostImage 崩溃/全空白与 14 项末行裁切得到抑制；自动化覆盖摘要、双行裁剪、360/480px 中英文固定按钮盒、portal 滚动、稳定 id 编辑/删除和焦点边界，但不足以证明全部运行态组合</why>
  <blocked-by>用户后续按需完成完整真机矩阵</blocked-by>
  <next>按需在 GUI Scale 2 补验连续批量添加、Portal 尺寸与首次焦点/border/caret、TextInput hover、normal/pending 中英文按钮盒、issue badge、非按钮区域零动作、有 variants 编辑、unknown/无 variants 原位替换、删除/折叠及第三方 `IItemRenderer`；不把已关闭报障改写为全部 renderer/交互矩阵通过</next>
  <priority>高</priority>
</item>

<item id="remote-api-lts-cleanup">
  <what>LTS/使用文档中的通用远程 HTML 能力可能与已删除源码不一致</what>
  <why>预存文档债，不属于 Picker 优化范围</why>
  <blocked-by>无</blocked-by>
  <next>另开文档任务核对源码与对外文档</next>
  <priority>低</priority>
</item>

<item id="cross-process-os-cas">
  <what>配置写入仍不提供跨进程 OS 级 CAS</what>
  <why>当前只保证同 JVM 参与式 writer 的写前检测与替换</why>
  <blocked-by>产品是否要求跨进程强一致</blocked-by>
  <next>若后续需要，再独立设计文件锁或单写者协议</next>
  <priority>低</priority>
</item>

</open-items>

## 待拍板项

- 当前无 publication 方案或 Maven 凭据待拍板项；GTNH Maven 不是前置，4.6.1 发布闭环已完成。

## 最近完成

- 4.6.1 发布闭环：不可移动 annotated tag、真实 JitPack canonical publication、clean `:dev` consumer、GitHub Release/notes 与 4 个 JAR 资产均验证完成；普通 publication 的正确 GMM 合同不变，GTNH Maven 未发布且非前置。
- `LIST_MEMBERS`：StructuredList 的 `List<String>` 成员管理收敛为摘要 + Manage、默认折叠 raw 高级入口、稳定 id 编辑与两步确认删除；当前成员为 6×42px 双行、结果为 12×34px，仍动态收缩并受 portal 可用高度裁剪；字段级视口默认 320 logical px、可显式 640，旧 API 兼容。
- 4.6.0 稳定 API 与通用被动 HUD 文档已覆盖富文本 span：稳定且行内唯一的 id、MUTED label + INFO value、语义 tone 边界及旧 `HudLine` API 兼容；未扩展交互能力。
- 通用 HUD token/layout 已完成 12/14px 字号、内容宽背景、长文本 clamp、多行最长宽、动态文本重排与左右锚点稳定回归；未开放任意文本样式。
- HUD viewport 生产提取已改为可注入 Minecraft 环境，GUI scale 仅作诊断且不进入 factory；五档独立 HUD scale 的完整 PaintPlan/replay 几何、clip、offset 与字体单次缩放测试已闭合。
- `4.5.3-beta-12`：StructuredList 独立 320px 视口、260px 可伸缩标题槽与非空 FieldSpec 默认恢复。
- `4.5.3-beta-11`：StructuredList header 标题使用可收缩的裁剪槽，长 identity 不再遮挡固定按钮。
- `4.5.3-beta-10`：Picker ALL/SELECTED 两态、空确认门禁、草稿往返与未枚举 key 无损编辑。
- `4.5.3-beta-9`：Picker 完整结果与 8 行窗口化、独立 hover/键盘导航；StructuredList 无框单层卡片、稳定折叠意图和 current-value presenter SPI。
- `4.5.3-beta-8`：完整生产树宽窄回归；raw/picker 改为纵向 column，修复横排越界裁剪。
- `4.5.3-beta-7`：无状态 codec、领域错误、raw+picker、identity 标题与 scrollbar 布局收口。
- `4.5.3-beta-1..6`：结构化列表、choice、多变体 SearchPicker、快照/受控状态、scene 图片与 reset/reload 接线。

## 长期指针

- scene 技术债：`docs/诊断层/scene技术债.md`
- 宪章偏离：`NORTH_STAR.md`《偏离登记》
- 架构边界：`docs/设定值层/边界.md`
- 关键取舍：`docs/反馈层/决策/`
