# 交接（项目状态权威源）

> 业务状态、阻断项和待决策事项以本文件为准；实时分支、HEAD 与工作区以 Git 命令为准。

## 当前状态

- `4.6.2` annotated tag object 固定为 `6155c157b823c928accc25b037f7a95e7e83d669`，peeled commit 固定为 `e86a731cf10c5fa9e0f3dd87fe52126646bf8ed1`，不可移动。
- draft Release ID `357902877` 仍为 draft；notes 与 main/dev/sources/dev-preshadow 四资产已由既有 run 核对。旧 recovery 在 PATCH 前因 ID 处理错误失败，正式化未执行。
- 仓库自动化已收敛为常规 build CI、内联零 PowerShell policy 和固定 `4.6.2` recovery。通用 tag、JitPack/Maven gate、外部 checker 与 agent 本地 Gradle 能力均已退役；未来需要时须独立重建。
- 本轮只执行无 Gradle、无网络静态验证；尚未 merge/push/运行新 CI 或 dispatch recovery，故 `4.6.2` 正式 Release 状态保持 `INCOMPLETE`。issue #65 不操作。
- Qz-UILib 4.6.0 与 4.6.1 的既有 GitHub Release 保持完成；4.6.1 的历史 JitPack 验证结果不变，但不代表仓库当前仍有通用 advisory 能力。
- GTNH `2.9.0-beta-2` 自动化适配已有历史测试/编译/build 成功结果；`runClient21` 运行态仍未执行。
- issue #65 HUD GL 状态泄漏已有自动化修复，真机物品栏玩家预览 A/B 尚待用户验证。
- Picker 当前复现的 HostImage 崩溃、全空白及 14 项末行裁切已得到抑制；完整交互矩阵与第三方 renderer 尚未覆盖。

## 当前未收敛项

<open-items>

<item id="release-4.6.2-recovery">
  <what>固定 4.6.2 recovery 已本地收敛，尚未独立 review、merge/push、CI、verify-only 与正式化</what>
  <why>本地无网络静态检查不能替代 GitHub-hosted 构建、API 与 PATCH 实证</why>
  <blocked-by>完成独立 review 后，由用户逐项授权 merge/push、CI、verify-only 与 publish-existing-draft</blocked-by>
  <next>先核对默认分支 CI，再 dispatch verify-only；通过后另行授权固定 ID 357902877 正式化并核对最终四资产/notes</next>
  <priority>高</priority>
</item>

<item id="issue-65-hud-gl-runtime">
  <what>HUD GL 状态泄漏自动化修复已完成，真机物品栏玩家预览 A/B 尚未验证</what>
  <blocked-by>用户运行客户端</blocked-by>
  <next>覆盖 HUD 开启/无内容两种场景并观察玩家预览深度遮挡</next>
  <priority>高</priority>
</item>

<item id="gtnh-runtime-and-picker">
  <what>GTNH beta-2 基础运行态与 Picker 完整交互/第三方 renderer 矩阵未完成</what>
  <blocked-by>用户运行客户端并按需覆盖 smoke</blocked-by>
  <next>验证启动、配置页、scene test hub、连续添加、滚动/焦点、编辑删除与第三方 renderer</next>
  <priority>中</priority>
</item>

<item id="remote-api-lts-cleanup">
  <what>LTS/使用文档中的通用远程 HTML 能力可能与已删除源码不一致</what>
  <next>另开文档任务核对源码与对外文档</next>
  <priority>低</priority>
</item>

<item id="cross-process-os-cas">
  <what>配置写入仍不提供跨进程 OS 级 CAS</what>
  <next>若产品需要，再独立设计文件锁或单写者协议</next>
  <priority>低</priority>
</item>

</open-items>

## 待拍板项

- 本地提交通过 review 后，仍需用户逐项授权 merge、push、recovery verify-only 与 publish-existing-draft。
- 未来通用 tag、JitPack、Maven 或 agent 本地 Gradle 能力均不在当前范围，重建前须重新拍板。

## 长期指针

- scene 技术债：`docs/诊断层/scene技术债.md`
- 宪章偏离：`NORTH_STAR.md`《偏离登记》
- 架构边界：`docs/设定值层/边界.md`
- 关键取舍：`docs/反馈层/决策/`
