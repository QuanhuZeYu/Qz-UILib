# 交接（项目状态权威源）

> 业务状态、阻断项和待决策事项以本文件为准；实时分支、HEAD 与工作区以 Git 命令为准。

## 当前状态

- agent 终端命令已统一走 Python 参数列表，OpenCode 使用内置默认 `build`，任务笔记按需创建；代码改动按仓库 `AGENTS.md` 在提交前执行本机完整 `build`。`runClient*`、`runServer*`、发布与生产操作仍不自动执行；CI 保持零 PowerShell 边界。配置改动仍需重启 OpenCode 才生效。
- `4.6.3` 已是不可移动的历史发布；当前 `feat/ui-projection-material-item` Breaking 功能分支目标发布身份为 `5.0.0`。版本继续由 Git tag / GTNH Gradle 推导，仓库已删除临时 `gradle.properties` 版本钉死。本地 annotated tag `5.0.0-beta` 指向 `f100d6e2`；`2026-08-01` 已在当前工作树执行 `call gradlew.bat --no-configuration-cache build`，编译、checkstyle、JUnit、classpath isolation 与 assemble 全部成功。当前仍无正式 `5.0.0` tag、Release、JitPack 制品、CI 或游戏运行态证据，状态为 `INCOMPLETE`。
- 当前功能分支在开发/beta 阶段省略 `acceptableRemoteVersions`，使用 FML 默认精确版本检查，避免 Git tag 派生的开发身份被尚未存在的正式 `5.0.0` 下界拒绝；同一 JAR 的客户端与集成服务端可互认，不同版本仍 fail closed。正式 `5.0.0` tag 前必须恢复 `[5.0.0,5.1.0)` 及 qualifier 契约测试；主信封 v2 与 Realtime v1 未变。dedicated server 双向握手与真实流量仍无实证，状态为 `INCOMPLETE`。
- `4.6.2` annotated tag object 固定为 `6155c157b823c928accc25b037f7a95e7e83d669`，peeled commit 固定为 `e86a731cf10c5fa9e0f3dd87fe52126646bf8ed1`，不可移动。
- 默认分支已到达 `a86838b7`。固定 recovery 的 [verify run `29989023055`](https://github.com/QuanhuZeYu/Qz-UILib/actions/runs/29989023055) 与 [publish run `29989322298`](https://github.com/QuanhuZeYu/Qz-UILib/actions/runs/29989322298) 均为 attempt 1、`completed/success`。
- GitHub Release [`4.6.2`](https://github.com/QuanhuZeYu/Qz-UILib/releases/tag/4.6.2)（ID `357902877`）已于 `2026-07-23T07:53:45Z` 发布；ID/tag 两端点一致，notes 与 main/dev/sources/dev-preshadow 四资产的名称、大小和 digest 均已核对。
- 仓库自动化已收敛为常规 build CI、内联零 PowerShell policy；已完成的固定 `4.6.2` recovery workflow 已删除。其 verify/publish 历史结果保留在本交接中，但不再是可运行能力。通用 tag、JitPack/Maven gate 与外部 checker 均已退役；本地完整 build 现按 `AGENTS.md` 作为代码提交门禁。
- Qz-UILib 4.6.0、4.6.1 与 4.6.3 的既有 GitHub Release 保持历史事实；4.6.1/4.6.3 的历史 JitPack 验证结果不变，但不代表仓库当前仍有通用 advisory 能力或 `5.0.0` 制品。
- GTNH `2.9.0-beta-2` 自动化适配已有历史测试/编译/build 成功结果；`runClient21` 运行态仍未执行。
- issue [#65](https://github.com/QuanhuZeYu/Qz-UILib/issues/65) 已[回复终态](https://github.com/QuanhuZeYu/Qz-UILib/issues/65#issuecomment-5055913391)并关闭，状态为 `CLOSED/COMPLETED`。
- Picker 当前复现的 HostImage 崩溃、全空白及 14 项末行裁切已得到抑制；完整交互矩阵与第三方 renderer 尚未覆盖。
- 字体共享准备已改为继承调用阶段的 depth test/mask/func，screen/HUD 仍由 UI host 显式建立无 depth test 状态；纯源码契约测试已补，尚待 CI 执行，告示牌正反面遮挡尚待用户真机验证。
- 玩家标签顺序与字体 depth 所有权保持正交：`6a9a375b` 继续保留，world-last 初版 `de15830c` 及安全回退 `4d25977c` 的历史结论不变；当前由 `8d55cf45`、`efc0d755`、`f48a4e46` 三笔独立提交实现 pass-scoped FIFO、普通名称/计分板最终调用点 `@WrapOperation`、Angelica `2.1.50` 可选 guard 与 lightmap 双层恢复。最终组合独立静态复审未发现 P0/P1/P2，A1-A9 静态满足；相关测试已随本地完整 build 通过，CI、发布制品、dedicated server、Angelica shader/shadow 与游戏内标签矩阵仍无实证，状态为 `INCOMPLETE`。
- scene 宿主关闭系统光标扫尾已补齐：`SceneRuntime` 对每个已绑定 backend 在关闭生命周期强制复位 `DEFAULT`，不改 `cursorSignal`，并以幂等 finally 覆盖 Owner 清理失败；runtime fake backend 回归测试已随本地完整 build 通过。CI 与游戏内界面切换仍无实证，状态为 `INCOMPLETE`。
- Config-scoped Material Motion 已从 Button/Toggle/Navigation/section 扩到共享 focus border/选中态、TextInput/TextArea/Autocomplete/Select chrome、Slider/Scrollbar 反馈与字段 dirty/error 语义色；TextInput 的 enabled 内凹背景不再随 hover/focus 反向变暗，透明 caret/选中层的中间帧保留可见端点 RGB、只插值 alpha。其它 runtime 仍立即应用，布局与树挂卸不做动画，唯一滚动例外是配置主视口的显式滚轮 intent。section presentation shell 仅在整棵输入门禁关闭期间做 GEOMETRY 级像素位移，归零后恢复 hit-test/Tab。
- 现代配置页在世界内保持完整 framebuffer 布局并使用 80% 不透明暗色根遮罩，游戏画面从整个背景连续透出；已删除缩短 surface 只露底部条带的错误方案。主菜单仍绘制默认背景，`McScreenBridge` 投影、`UiRenderContext` 与 surface 始终共用完整 native framebuffer。
- `SceneNavList` 选中反馈已增强为背景/文字色插值、非交互指示条伸缩与标签 4px 横移；配置主视口滚轮以 160ms keyed ease-out Motion 合并同帧/跨帧目标，旧段先推进再从当前显示值重定向，内容和 thumb 同步且持续输入不会反复零速起步。反向滚轮立即截断旧方向，scrollbar 拖动从当前可见 offset 取消 Motion 后直接接管；平滑滚动与 section 换树都会按粘滞指针请求 hover 重算。
- 配置 section 已改为立即严格单 live 切换；incoming 标题和字段卡片在 post-flush 完整布局发布后从 `-22px` 以 240ms 满 opacity 归位，相邻项延迟 36ms、最大启动延迟 252ms。字段 presentation shell 使用不改 LayoutBox 的像素对齐 geometry offset，让卡片内容与后代输入控件 clip 同步位移、父 viewport clip 保持固定，避免输入框上沿暂时被裁且不创建逐卡全屏 FBO。旧 Owner 立即取消迟到轨道；位移期间整棵 presentation 子树不参与 hit-test/Tab，逐项归位或卸载后恢复并重算 hover。scene host 仅在主树与 overlay 都完成最终布局后发布 epoch，并最多同帧 settle 三轮 observer 写入；这不是跨线程 layout/paint。
- fontSort 与 draggable SimpleList 的拖拽已从整行外边缘越界改为相邻行中线插槽换位；实际激活重采样顺序/keyed 几何，手势内即时顺序不回读待 flush preview，UP 发布最终 preview，换位 transform 与落点都计入目标槽位及完整待应用 scroll。SimpleList 通过 runtime-scoped 两阶段 terminal binding 等待单跳 reset bridge，结算后主动退订；scene Router 同步派发 `FOCUS_LOST/FOCUS_GAINED` 且不改变 focused signal 的帧末语义，fontSort 索引据此在 action/拖拽/section 卸载前处理即时文本，Owner cleanup 另在 handler 注销前兜底，连续索引与同帧 `TEXT_INPUT→Enter/Tab` 不再回读旧值。fontSort drag handle 从 presentation 即时 visible rows 开手势；`ConfigScreen` 将 `RESTORE/CANCEL/SAVE` CLICK 按事件顺序入队，待同帧字段与列表终态收敛后按 DraftBuffer 即时状态执行。`DOWN→受控更新→激活`、同帧 `MOVE→UP/CANCEL/SCROLL`、未 flush 外部 Draft、保存/reset 与等于 preview 的外部 authority 优先、同帧动作 FIFO 与失败批次清理、内层滚动边界阻断冒泡、筛选隐藏项保留和 layout 后抓取点跟手均有 headless 回归。Minecraft 内长字体列表、边缘自动滚动与实际手感仍待用户 smoke。
- 字体异步核心 Phase A 已提交为 `01c96839`：命令式 debouncer 改为 durable desired/applied signal，任意线程只发布，唯一 `RenderTick.END` owner 在 150ms quiet-only 稳定后 single-flight reconcile，失败不确认并退避，draw-stage 只上传，Splash 只延后且不丢 intent。Phase B 已冻结 `FontRuntimeSettings` 与 stable line metrics，以 CPU-only candidate 在破坏旧代前完成字体发现/catalog 规划；`ActiveFontGeneration` 单引用统一 version、measure epoch、settings、catalog、metrics、唯一 direct-table storage 与 lifecycle。完整 Unicode tables 只分配一次，在 generation RW barrier 内原地清理转移；layout 整次调用持 read scope，旧 worker 的 matcher cache publication 短持 read lock并锁内重验，upload/page/draw 不再回读 generation-sensitive `FontConfig`。candidate 失败保留旧 active/table；version/epoch 严格后继在停 worker 前验证，不可逆 core 不调用 dispatcher，发布 active 后先保留 durable worker recovery intent。executor 首次 stop 只做一次有界等待，超时保留 retiring owner，后续 tick 非阻塞探测且不创建并行 worker；诊断组件写入口受 owner token 保护，renderer 只读 view 不暴露 raw tables。`2026-08-02` 本机完整 build 已通过。真实 Splash、F3+T、资源包风暴、GL 与 dedicated server 仍为 `INCOMPLETE`。后续 Phase C-F 继续收口 glyph token、有界 demand scheduler、事务化 upload 与异步 candidate/frame lease。

## 当前未收敛项

<open-items>

<item id="fml-remote-version-compatibility">
  <what>开发/beta 阶段使用 FML 默认精确版本检查；正式 `5.0.0` 的有界同 minor 范围与双端握手尚未闭环</what>
  <blocked-by>正式 tag 前尚未恢复 `[5.0.0,5.1.0)` 契约；缺少发布制品 JAR annotation 与 dedicated server 双向版本矩阵实证</blocked-by>
  <next>正式发布前恢复范围与 qualifier 测试并执行完整 build；核对生产 JAR annotation，使用两个都携带范围的端点覆盖同版本、后续 patch 双向与越界版本，再覆盖缺 mod 方向及真实主信封 v2、Realtime v1 流量</next>
  <priority>高</priority>
</item>

<item id="font-world-depth-runtime">
  <what>全局字体替换后的告示牌世界深度遮挡与 screen/HUD 二维文字运行态尚未实测</what>
  <blocked-by>用户运行客户端并启用原版字体替换</blocked-by>
  <next>从告示牌正反面核对文字可见性与模型遮挡，并回归 screen/HUD 二维文字</next>
  <priority>中</priority>
</item>

<item id="font-async-core-signal-reload">
  <what>字体异步核心 Phase A signal reload 已提交；Phase B immutable settings、CPU-only candidate、单一 ActiveFontGeneration、唯一 direct-table storage 转移、executor ownership 与诊断 owner guard 已实现并通过完整 build</what>
  <blocked-by>字体资源 fingerprint、glyph token/异常终态、有界优先级、事务化 upload、异步 candidate 与 frame lease 尚未实施</blocked-by>
  <next>按 `.opencode/tasks/20260801-font-async-core-signal-reload.md` 开始 Phase C，以完整 GlyphRequestToken 收口 worker/result/upload/fail/cancel/ready settlement</next>
  <priority>高</priority>
</item>

<item id="player-name-tag-render-order">
  <what>普通玩家名称/计分板标签的 pass-scoped 延后实现与静态复审已完成；旧视觉问题已有受限方案，但运行态闭环尚未完成</what>
  <blocked-by>测试源码尚未运行；缺少 CI 编译/JUnit、发布制品与 shader/shadow、dedicated server、游戏内矩阵实证</blocked-by>
  <next>由 CI/用户完成编译与 JUnit；核对无 Angelica、精确 2.1.50、未知版本降级和 dedicated server；游戏内覆盖普通名称/计分板/潜行、箱子前后遮挡、昼夜与多亮度，并分别观察 Angelica shader 主 pass/shadow 无重复、串帧或状态泄漏</next>
  <priority>高</priority>
</item>

<item id="scene-projection-material-item">
  <what>issue [#66](https://github.com/QuanhuZeYu/Qz-UILib/issues/66) 已按用户授权以 `CLOSED / NOT_PLANNED` 关闭；UILib 不实现 GuiContainer / vanilla Slot 宿主桥，也不合入 #66 worktree。当前功能分支已写入 U0 screen/overlay projection、Breaking snapshot-only `itemIcon` seam，以及真实 M2 Motion：每个 `SceneRuntime` 独立采样 host 单帧时间，Button 使用 90ms state-layer 色彩过渡，Toggle 与 Navigation 使用 160ms transform/selection 过渡；Navigation 另有非交互指示条伸缩、标签 4px 横移与文字色插值。section 先 dismiss overlay 并立即严格单 live 切换，标题/字段卡片在完整布局发布后以满 opacity、240ms、`-22px` 与 36ms 间隔级联归位，最大启动延迟 252ms；旧 Owner 立即取消迟到轨道，presentation 位移期间关闭整棵子树输入，归位后恢复。配置主视口滚轮以 160ms keyed ease-out Motion 从当前显示 offset 收敛到连续累计目标，内容与 scrollbar thumb 同步，连续重定向不再反复零速起步，拖动仍直接定位，跨帧 hover 按新 geometry 重算。共享 focus border/选中态、输入与选择控件 chrome、Slider/Scrollbar 反馈及字段 dirty/error 语义色也已接入 fast/standard 色彩轨道；TextInput 背景在 hover/focus 间保持稳定，透明色轨道使用 alpha-safe 中间帧；逐帧 sample 不写 Draft/Persistence/transaction history。scene host 在 post-flush 主树与 overlay 完成布局后发布最终 epoch，并最多 settle 三轮 observer 写入；当前不启用跨线程 layout/paint。现代配置页在世界内使用覆盖完整 framebuffer 的 80% 不透明暗色遮罩，游戏从整个背景连续透出，不再裁切 surface。目标回归已覆盖手动时钟、透明色中间帧、真实 hover→click→focus、单次/连续/反向滚轮轨迹、Owner cleanup、满 opacity、命中/Tab 门禁、主树/overlay 首帧 publication 与 settle 上限；`2026-07-31` 本机完整 `build` 已通过全量 JUnit、checkstyle、classpath isolation 与 assemble。用户已确定 UILib 正式目标版本 `5.0.0`、正式 FML 范围 `[5.0.0,5.1.0)`、Miner 编译坐标 `5.0.0:dev` 与运行范围 `[5.0.0,6.0.0)`；开发/beta 阶段暂用 FML 默认精确版本检查</what>
  <blocked-by>正式 `5.0.0` tag/JitPack `dev` 制品尚不存在，Miner 新坐标当前不可从权威远端解析；Minecraft/LWJGL2 真实帧率下的半透明背景、增强导航、字段卡片级联、平滑滚轮、连续导航及 resize 表现仍需用户运行态复验</blocked-by>
  <next>用户先从主菜单与世界内分别打开 `/qzuilib modernconfig`，确认世界画面从整个背景连续透出且无底部条带；连续快速切换 Tab 时观察指示条伸缩、标签横移和标题/字段卡片级联，字段区不得明灭，输入框/选择框上沿不得在位移期间被裁，位移中的卡片不得误点或被 Tab 聚焦；输入框从 hover 点击到 focus 时背景不得反向变暗，caret/下拉选中层不得暗闪；连续滚轮应立即响应、平滑累计且 thumb 同步，再回归 scrollbar 拖动、overlay 关闭、滚动保持与 resize。在另行授权发布操作后创建并验收不可移动的正式 `5.0.0` tag/JitPack 制品，再完成 Miner 客户端 smoke</next>
  <priority>高</priority>
</item>

<item id="scene-cursor-close-cleanup">
  <what>scene screen close 的 `forceApply(DEFAULT)` 生命周期扫尾与 fake backend 测试源码已补齐；同一 screen 内结构卸载后的 hover/cursor 重算不在本次范围</what>
  <blocked-by>缺少 CI 编译/JUnit 与游戏内非默认光标关闭、连续切换界面的运行态实证</blocked-by>
  <next>由 CI/用户执行测试；游戏内分别从 pointer/text/none 等非默认样式关闭 scene 界面并打开后续界面，确认系统光标立即恢复 DEFAULT，另将 portal/条件节点/动态列表卸载残留作为独立任务处理</next>
  <priority>中</priority>
</item>

<item id="gtnh-runtime-and-picker">
  <what>GTNH beta-2 基础运行态与 Picker 完整交互/第三方 renderer 矩阵未完成</what>
  <blocked-by>用户运行客户端并按需覆盖 smoke</blocked-by>
  <next>验证启动、配置页、scene test hub、连续添加、滚动/焦点、编辑删除与第三方 renderer</next>
  <priority>中</priority>
</item>

<item id="remote-api-lts-cleanup">
  <what>LTS/使用文档中的通用远程 HTML 能力可能与已删除源码不一致</what>
  <next>另开文档任务核对源码与对外文档</next>
  <priority>低</priority>
</item>

<item id="cross-process-os-cas">
  <what>配置写入仍不提供跨进程 OS 级 CAS</what>
  <next>若产品需要，再独立设计文件锁或单写者协议</next>
  <priority>低</priority>
</item>

</open-items>

## 待拍板项

- 未来通用 tag、JitPack 或 Maven 能力均不在当前范围，重建前须重新拍板。

## 长期指针

- scene 技术债：`docs/诊断层/scene技术债.md`
- 宪章已知限制：`NORTH_STAR.md`《已知限制》
- 架构边界：`docs/设定值层/边界.md`
- 关键取舍：`docs/反馈层/决策/`
