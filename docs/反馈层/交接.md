# 交接（单一权威状态源）

> 本文件只保留项目当前状态、未收敛项和待拍板事项；控制律归 `AGENTS.md` 与编排文档。

## 当前状态

- 当前分支 `add/config-structured-list`；本次收尾前 HEAD `629f071a`，历史包含 `cabd49e3`。最终提交以 `git log -1` 为准。
- 当前版本 `4.5.3-beta-8`，仅 Maven Local dev 验证；SHA-256 为 `10B84984997FC691557BFDAF4E519B525A98E7AEA95DB0D719A06C8CA13122EE`。未 merge、push、tag 或 release。
- beta-8 已完成 `STRUCTURED_LIST`、`LIST<CHOICE>`、SearchPicker 强快照与无状态 codec、scene 图片、ALL/SINGLE/MULTIPLE、领域 presentation/错误、raw+picker、identity 标题、固定宽 scrollbar，以及 raw/picker 纵向布局。稳定契约见 `docs/反馈层/决策/config-search-picker.md`。
- 完整生产 scene 树宽窄布局与交互测试已通过，Qz-Miner 已升级消费 beta-8。beta-7 日志仅证明 Picker 节点构建且存在横排越界裁剪；用户尚未报告 beta-8 真机结果，当前不能标记真机通过。
- 不改配置核心语义、YAML 或 network；NEI 不在首版；未实现虚拟化，搜索结果上限 64。

## 当前未收敛项

<open-items>

<item id="config-search-picker-ux">
  <what>将 SearchPicker 从技术控件优化为面向用户的“添加方块”及已选规则编辑工作流</what>
  <why>现有通用能力与布局已闭合，但技术字段、内部 key 和 ALL/SINGLE/MULTIPLE 仍不是最终产品表达</why>
  <blocked-by>Qz-Miner beta-8 真机复测；Designer + Oracle 产品设计</blocked-by>
  <next>Miner runClient21 并核对日志；随后新建 Designer、Oracle、fixer、reviewer tasks，旧 tasks 不恢复</next>
  <priority>高</priority>
</item>

<item id="release-4-5-3-beta">
  <what>4.5.3-beta-8 尚未发布</what>
  <why>当前仅 Maven Local dev 制品，且 beta-8 真机结果待确认</why>
  <blocked-by>config-search-picker-ux 的真机复测与用户发布决定</blocked-by>
  <next>每次 beta 坐标变更均升版本、重发 Maven Local 并核对 SHA；未经用户确认不发布</next>
  <priority>中</priority>
</item>

<item id="remote-api-lts-cleanup">
  <what>LTS/使用文档中的通用远程 HTML 能力可能与已删除源码不一致</what>
  <why>预存文档债，不属于 Picker 优化范围</why>
  <blocked-by>无</blocked-by>
  <next>另开文档任务核对源码与对外文档</next>
  <priority>低</priority>
</item>

<item id="cross-process-os-cas">
  <what>配置写入仍不提供跨进程 OS 级 CAS</what>
  <why>当前只保证同 JVM 参与式 writer 的写前检测与替换</why>
  <blocked-by>产品是否要求跨进程强一致</blocked-by>
  <next>若后续需要，再独立设计文件锁或单写者协议</next>
  <priority>低</priority>
</item>

</open-items>

## 待拍板项

- Picker UX 的具体产品投影由下一会话 Designer + Oracle 提案后交用户确认；不预先改变 canonical、YAML 或网络语义。

## 最近完成

- `4.5.3-beta-8`：完整生产树宽窄回归；raw/picker 改为纵向 column，修复横排越界裁剪。
- `4.5.3-beta-7`：无状态 codec、领域错误、raw+picker、identity 标题与 scrollbar 布局收口。
- `4.5.3-beta-1..6`：结构化列表、choice、多变体 SearchPicker、快照/受控状态、scene 图片与 reset/reload 接线。

## 长期指针

- scene 技术债：`docs/诊断层/scene技术债.md`
- 宪章偏离：`NORTH_STAR.md`《偏离登记》
- 架构边界：`docs/设定值层/边界.md`
- 关键取舍：`docs/反馈层/决策/`
