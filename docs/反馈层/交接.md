# 交接（单一权威状态源）

> 本文件是项目唯一权威状态源。`现状.md`/`计划.md` 已并入本文件并删除。
> 任何状态冲突以本文件为准（消除多头发散，建立单一误差向量）。
> 控制律（完成标准/约束/审核纪律）不入本文件，归 `AGENTS.md` 与
> `docs/控制律层/编排模式/SUBAGENT-ORCHESTRATION.md`。

## 1. 当前状态 Current State

- 当前分支：`fix/config-stale-draft-recovery`（基于 `add/config-draft-validator`；待审合入）
- 最新提交：精确以 `git log --oneline -1` 为准
- 工作区：提交后应干净；精确状态以 `git status` 为准
- 发布口径：**4.5.3-beta-1** 预发布（连续 beta 迭代）；**不是**稳定 4.5.3；稳定公共能力目标 **4.6.0**；正式 tag / push / GitHub Release **尚未执行**
- 前一分支工作：`add/config-draft-validator` 含 4.5.2 DraftValidator（尚未 tag）
- 前一正式版：`4.5.1`（issue #63 裁切）
- 本轮纠偏：reload **三阶段**（capture→锁外完整校验→写域 commit）+ 结构化 `ConfigReloadException` + BOOLEAN/类型严格 + ConfigSaveListener 主线程 latest-wins 回灌 + Atomic 包级 fault 缝（AtomicFileWrites 无 public install/clear）+ writeAll 非 CAS 文档 + same-byte ABA / 慢 validator latch 测试；changelog 为 `.changelogs/4.5.3-beta-1.md`


## 2. 当前未收敛项 Open Items

> 仿 `NORTH_STAR.md`《偏离登记》范式。每项含 what/why/blocked-by/next/优先级。
> 收敛后即从本节移除，不保留历史。

<open-items>

<item id="remote-api-lts-cleanup">
  <what>LTS / 使用文档中通用远程 HTML（RemoteDocumentPages / RemoteHudOverlays）仍标可用，但 ui.remote 包可能已从源树移除</what>
  <why>阶段 1 审核发现的预存文档债，不在 config 对齐 scope</why>
  <blocked-by>无</blocked-by>
  <next>另开 docs 任务核对源码后清 LTS/远程页面文档</next>
  <priority>低</priority>
</item>

<item id="release-4-5-3-beta">
  <what>4.5.3-beta-1 预发布 + 4.5.2 DraftValidator 待审合入后，再决定 beta 连续迭代或稳定 4.6.0</what>
  <why>用户拍板本轮为 4.5.3-beta-1（非稳定 4.5.3）；未审前不 merge / tag / push / release</why>
  <blocked-by>代码审 + 用户确认合入路径与 beta/稳定节奏</blocked-by>
  <next>review → 按用户指令 merge/tag；稳定公共能力归 4.6.0</next>
  <priority>中</priority>
</item>

<item id="qz-miner-consume-beta">
  <what>Qz-Miner 在 UILib 4.5.3-beta / 4.6.0 发布后接入 DraftValidator、owner 语义、写前检测、RELOAD 事件与冲突恢复并验收</what>
  <why>上游尚未 tag/release；本分支不跨仓改 Miner；消费者必须处理 RELOAD（不得假设 reload 伪装 BATCH_SAVE）</why>
  <blocked-by>release-4-5-3-beta</blocked-by>
  <next>依赖发布版 → bootstrap 三参 + 实机验收 INVALID/stale/reload 校验/RELOAD 回灌</next>
  <priority>中</priority>
</item>

<item id="cross-process-os-cas">
  <what>跨进程配置写仍非 OS 级 CAS；beta 仅保证同 JVM classloader 参与式 writer 串行精确字节写前检测+replace</what>
  <why>用户拍板 beta 纳入写前检测但不虚假承诺 OS 级；跨进程窗口需更高层文件锁/原子 rename 协议</why>
  <blocked-by>产品是否要求跨进程强一致</blocked-by>
  <next>若 4.6.0 需要跨进程，再设计 OS 级文件锁或单写者约定</next>
  <priority>低（P2 口径已文档化）</priority>
</item>

</open-items>

## 3. 待拍板项 Awaiting User

- 无架构待拍板（stale draft 恢复 + owner fail-closed + I3 prefill + 写前检测 + reload 校验/RELOAD 用户已批准本轮清单）

## 4. 最近完成 Recently Done

| 单元 | 结果 | 分支 |
|---|---|---|
| 4.5.3-beta-1 并发回滚纠偏 | reload 三阶段+ConfigReloadException+主线程回灌+类型严格+Persistence 测试缝内收 | fix/config-stale-draft-recovery |
| 4.5.3-beta-1 终审收口 | reload 完整校验 + RELOAD 事件 + 通知期封锁 + 参与式 writer 口径 + owns/ConfigScreen fail-fast + ABA/IO 测试缝 | fix/config-stale-draft-recovery |
| 4.5.3-beta-1 磁盘写前检测 | ConfigFileSnapshot + save/flushRaw + reloadDraftFromDisk + UI 主线程 + changelog | fix/config-stale-draft-recovery |
| 4.5.3-beta-1 纠偏 | Draft 所有权 + I3 prefill + 真实 reload 按钮 + 冲突窗口精确断言 | fix/config-stale-draft-recovery |
| 4.5.3 stale draft 易用性 | ConflictType + 事务 base + replaceDraft + presentation seed + 显式 reload UI | fix/config-stale-draft-recovery |
| DraftValidator 完整终审 | capture→锁外 validate/prepare→verify/write/引用交换 | add/config-draft-validator |
| 发布 4.5.1 | issue #63 宿主裁切基线修复合入 | 4.0 + tag 4.5.1 |


## 5. 长期未回填（指针）

- scene 技术债权威清单 → `docs/诊断层/scene技术债.md`
- 宪章偏离登记 → `NORTH_STAR.md`《偏离登记》段
- 架构边界与 strangler 路线图 → `docs/设定值层/边界.md`
- 关键取舍背景 → `docs/反馈层/决策/`
