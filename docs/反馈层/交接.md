# 交接（单一权威状态源）

> 本文件是项目唯一权威状态源。`现状.md`/`计划.md` 已并入本文件并删除。
> 任何状态冲突以本文件为准（消除多头发散，建立单一误差向量）。
> 控制律（完成标准/约束/审核纪律）不入本文件，归 `AGENTS.md` 与
> `docs/控制律层/编排模式/SUBAGENT-ORCHESTRATION.md`。

## 1. 当前状态 Current State

- 当前分支：`refactor/config-fullmigrate-modern`（已 fast-forward 同步至 4.0，含 config 全迁移链 + scene 输入系统 P0→P2-3 修复链）
- 最新功能提交：`a264612a`（fontSort 卡片式视觉优化，卡片化展示 + 行/把手 hover 联动）
- 工作区：干净；精确状态以 `git status` 为准
- 主分支 4.0 状态：chore/agent-framework-sync（含 P0-P2-1 修复 + agent 框架同步）已 `--no-ff` 合回（merge commit `8249cedd`）；合并冲突 SimpleListFieldRenderer/FieldRendererRegistry 取 chore 正式版、弃 4.0 半成品 `82ffe6da`（用户已拍板接受既成事实）；字体硬裁切 P1 根治已 cherry-pick 至 4.0（commit `155325bf`，拆分顶点 texCoord 与 uvBounds clip 语义，uvBounds 放宽到 slot 边界放行 mipmap 羽化；详见 §4）
- 全量测试：全绿（2026-07-07 本轮 `./gradlew.bat --no-configuration-cache test` 通过；具体用例计数以本地 Gradle 输出为准）
- 迁移工程真机综合验证：✅ 用户已确认无异常（启动加载 + 保存 reload + 重启回灌 + 异常容错 + 字体兜底 5 项全过）
- 深化 + 输入修复真机验证：✅ 用户已确认全部通过（P6 补全、下拉框输入修复全链、SceneSelect 展开/点击收起行为、characterFontRules fontName autocomplete 候选行为、fontSort 预填充等）
- 字体渲染真机验证：✅ 用户已确认通过（字符硬裁切 P1 修复见 §4，硬边消失、细笔画不再被吃薄）

## 2. 当前未收敛项 Open Items

> 仿 `NORTH_STAR.md`《偏离登记》范式。每项含 what/why/blocked-by/next/优先级。
> 收敛后即从本节移除，不保留历史。

<open-items>

<item id="U3-config">
  <what>U3 config 侧深改造档 A：只收敛 8 个 FieldRenderer 重复样板（labelOf 下沉静态工具 / draftSignal→类型转换 helper / FieldShellBinder.build 薄收口），不动 DraftSignalAdapter/Computed 机制</what>
  <why>U2 已统一 FormPageShell 组合式骨架口径；U3 命中 renderer 样板去重真实痛点，零架构风险（不碰 reactive 核心/scene 契约/核心层边界），可独立闭合。oracle 否决档 B（Adapter 拆 factory，367 行未达千行阈值踩 YAGNI）与档 C（revisionSignal 精确化破零 uilib 依赖国策且收益虚，记忆化已挡下游）</why>
  <blocked-by>I2 / revisionSignal×I8 设定值层前置已裁决（见 docs/反馈层/决策/reactive-computed-i2-i8-rulings.md），不再阻塞 U3 档 A</blocked-by>
  <next>派 fixer 实施：新建 FieldRenderSupport + FieldShellBinder；改 8 renderer 删本地 labelOf / 传 controlFn；新增 tool 单测 + 回归 8 renderer 测试；jetbrainsBuild 验证；reviewer 审 R1-R12 + I1/I3</next>
  <priority>中（已拍板待实施）</priority>
</item>

<item id="push-4.0">
  <what>本地 4.0 ahead origin/4.0，按需 push</what>
  <why>含 config 接入 5 单元 + Oracle C 3 项 + 控制论重构 5 单元 + 阶段 A-E 配置迁移 17 commit + 字符硬裁切 P1 修复 1 commit（4.0 现 ahead origin/4.0 81 commit）</why>
  <blocked-by>无阻塞，按需</blocked-by>
  <next>用户确认后 push</next>
  <priority>低（按需）</priority>
</item>

<item id="悬空溯源门禁接入">
  <what>悬空溯源门禁脚本已立（scripts/check-javadoc-stale-line-refs.ps1 + 错误预防通则 + 门禁说明），待 4 处存量跨类维护锚清理（Path A：改 `{@link Class#method}` 不带行号）+ 接入 check-doc-discipline.ps1 调用链</what>
  <why>Javadoc 行号引用第 2 次累积（C4/C5/C6 + E.2），按 §4.3 上溯至传感层立机械门禁守卫；自检 4 处存量命中当前准确但属 dangling-prone 模式，用户选 Path A 全清理</why>
  <blocked-by>无</blocked-by>
  <next>派 fixer 改 SceneTab.java:207 / ConstraintResolver.java:347,368,371 四处为 `{@link}` + 接入 check-doc-discipline.ps1 + 跑门禁自检 0 命中 + 跑全链</next>
  <priority>中（已拍板 Path A 待实施）</priority>
</item>

<item id="autocomplete候选空快照">
  <what>autocomplete filtered 候选为空时的浮层路径（空快照）行为分支覆盖 + FontConfig.getFontSortSnapshot 真机是否返回空</what>
  <why>P2-1 oracle 标注的未覆盖路径，未坐实</why>
  <blocked-by>无</blocked-by>
  <next>补空快照路径测试；真机核 getFontSortSnapshot 空配置返回值。详见 `docs/诊断层/scene技术债.md`</next>
  <priority>低（P2-1 oracle 标注开放项）</priority>
</item>

</open-items>

## 3. 待拍板项 Awaiting User

- U3 范围与本会话三件裁决已于本次会话拍板落地（见 §2 各 item 演进段与 §4 末段），无待拍板项；唯一剩余项为 push 4.0 到远端（§2 `push-4.0`），用户按需确认

## 4. 最近完成 Recently Done

| 单元 | 结果 | 分支 |
|---|---|---|
| autocomplete 候选 commit 后 caret 自动到末尾 | commit `7d3067e4`。`SceneTextInputPrimitive.Result` 新增受限 `moveCaretToEndOf` Consumer，仅按指定文本码点数移动 caret，不暴露可写 caret signal；`SceneAutocompletePrimitive` 在 ENTER 与 item CLICK commit 后调用该操作再 collapse；`SceneAutocompletePrimitiveIntegrationTest` 新增 ENTER/CLICK 两条旧实现会红的 caret 断言。JetBrains 文件编译、指定测试 `SceneAutocompletePrimitiveIntegrationTest` + `SceneAutocompletePrimitiveTest`（含 `--rerun-tasks`）、全量 `./gradlew.bat --no-configuration-cache test`、`scripts/check-scene-boundaries.ps1`、`git diff --check` 通过。 | refactor/config-fullmigrate-modern |
| characterFontRules fontName autocomplete 候选修复 | commit `f37cd2d4`。确认 characterFontRules 走 `SceneAutocompletePrimitive` 而非 `SceneSelect`；修复初始 `highlightedIndex=0` 导致第一个候选默认高亮，改为初始无高亮、键盘导航才建立高亮；关闭路径统一清空高亮；CONTAINS 模式下精确命中 filtered 任一候选即抑制展开，修复点击 `Arial` 后因 `Arial Black` 仍匹配而重弹。`SceneAutocompletePrimitiveIntegrationTest` 迁入 `scene/integration`，补 CONTAINS 点击/ENTER 回写不重弹、ESC 重开无高亮；`CharacterRuleFieldRendererIntegrationTest` 补真实 fontName smoke（打字→候选→点击→写回→不重弹）。全量 Gradle test、scene 边界门禁、JetBrains build、`git diff --check` 通过。 | refactor/config-fullmigrate-modern |
| 组件行为对齐现代组件低风险打磨（M2 滚动条可发现性 + m1 error 空节点常驻 + M4 徽标对比度） | commit `a0c5d62c`。生产 4：SceneChromeTokens `SCROLLBAR_THUMB_IDLE` 0x66→0x99（idle 60%）；SceneScrollbar `DEFAULT_TRACK_COLOR` 0x33→0x44；ConfigScreen 类头"固定宽 8"→"DEFAULT_BAR_WIDTH(=8)" + `badge()` 边框 1px→2px；FormFieldShell error 节点无条件 append 改 `rt.show` 条件挂载（守 R11，补作用域注释）。测试 5：field renderer 测试 helper 从"倒数第二个子节点"改为按结构特征扫描，消除位置依赖。全量测试通过，reviewer P2 已收。 | refactor/config-fullmigrate-modern |
| scene 输入系统 P0→P2-3 修复链（下拉框 item 点击失效根治 + harness 抽取 + 文档沉淀） | 分 P0/P1/P2-1/P2-2/P2-3 五段，全程闭环 reviewer 通过。**真因 D1**：真机 DOWN 一帧/UP 另一帧时，DOWN 帧 flush 触发 focus 掐断 + overlay 卸载，UP 命不中已卸载 item → CLICK 不合成；**真因 D2**：9 参兼容构造致 chrome=NOOP 浮层无背景。**P0 止血**（`28da6cc7`+`6c1aaf09`+`ac58dc14`）：命中 active overlay 豁免 clearFocus + 注入真 chrome + 跨帧回归守卫。**P1 LCA 化 + R13**（`7182b166`..`bd132f7f` 5 commit）：CLICK 合成从严格相等升级为 LCA 祖先容差（复刻旧栈 findNearestCommonInclusiveAncestor）+ 新立控件契约 R13（浮层显隐状态独立可写禁派生自瞬态交互态）+ SceneInputRouterLcaClickTest 4 用例。**P2-1 R13 重构 + scheduler 补全**（`6e297e1c`+`7df29594`）：autocomplete expanded 从 Computed(focused) 重构为独立 Signal + effect 驱动；ReactiveScheduler flush 补全 drainPendingWrites+runDirtyEffectsOneSweep 双通道交替到不动点（撤应急 setImmediate 守 I2，还清 A1 技术债），ReactiveSchedulerEffectSetSameFlushTest 4 用例。**P2-2 harness 抽取**（`ed363b51`+`517d72e5`）：clickCrossFrame 抽为 harness `pressReleaseAcrossFrames(node, betweenFrames)` 时序原语（oracle 深评否决重方案「复刻 layoutOverlays」，采轻方案 harness 不承担 overlay layout、overlay 重排回调交还测试侧，守 YAGNI+I12），harness 自测 3 用例，reviewer 通过。**P2-3 文档**（`8fc90c8e`）：测试体系约定 §7.5 事件注入帧时序语义（同帧 vs 跨帧判据表 + 同帧假绿红线 + overlay 跨帧卸载陷阱）。守 I2/I9/I11/I12 + R11/R13。 | refactor/config-fullmigrate-modern（已合回 4.0） |
| 真机问题修复 2 项（下拉框点击 + fontSort 预填充） | 2 commit `1c3cbab7`+`fdbfddf9`；**下拉框**：oracle 裁决真因 E'（LwjglInputSource poll-based 差分对连续点击第二次操作系统性丢失 DOWN+UP 边沿，浮层 mount 负载骤增导致帧间双丢）；方案 A（McScreenBridge 重写 mouseClicked/mouseMovedOrUp → pushPointerButton 事件旁路，externalPointerMode 开关与 poll 去重，坐标换算 physicalX=mouseX*scaleFactor Y 轴无需翻转）；explorer 5 维度诊断 ses_0cae7edeeffe + oracle ses_0cad66ac7ffe + reviewer ses_0cac1156b 通过（无 P0/P1，Y 轴翻转数学推导确认正确）；7 新测试 + 全回归。**fontSort 预填充**：诊断确认空配置是有意设计但用户要"自动预填"；方案 A'+选项①（seedFieldBaseline 抹平 dirty：draft+current 双写使 dirty=false，用户不编辑不写盘运行时零改变）；已验证 FontConfig.getFontSortSnapshot() 空配置时已中文优先（FontOrderPlanner 用 DefaultFontOrderHints 排序）零退化；explorer ses_0cae7edf8ffe + oracle ses_0cad66abdffe + reviewer ses_0caafe1cd 通过（P1-1 浅拷贝 Javadoc 已采纳）；核心层 DraftBuffer/DraftSignalAdapter 纯加法零回归 + 4 新测试 | refactor/config-fullmigrate-modern |
| 深化 P6 字体名自动补全 SceneAutocompletePrimitive | 2 commit `df73117f`+`daebbd55`；新建 SceneAutocompletePrimitive（487 行，组合 SceneTextInputPrimitive + portal 浮层 + filtered keyed diff + 键盘正交）；接入 characterFontRules fontNameInput（MatchMode.CONTAINS，用户拍板）；oracle 方案 ses_0cb337f41（含 F1-F4 关键发现：键集正交/同节点多 handler/focus 时序/suppressed+Computed 派生）；explorer 5 维度补侦察 ses_0cb3a198；L2 33 用例 + L3 12 用例 + 回归 15 全过；reviewer ses_0cb1a201b 全过（R1-R12+R11+I1/I5/I11/I12 逐条 + 三大风险点源码测试双线坐实）；P1 反馈 Locale.ROOT→ENGLISH 采纳（消除与 FontMatcher 同源承诺缺口）；fontSort 接入延后（oracle §7 范围控制） | refactor/config-fullmigrate-modern |
| 深化 P-1 CharacterRuleItem 构造 errorMessage 派生改用 parseLine 统一 | 1 commit `6751e329`（前置 Docs `2145fdc9` 回写 P0-P5）；构造内 errorMessage 派生从 parse（返回 List，全段空时空 list → null）改为 parseLine（与 fromRaw/normalize 三路径同源）；新增 P-1 防回归用例（fromRaw + withSelector 两路径断言 errorMessage 非空）；顺手清理 3 处历史 Javadoc（P5 fromRaw 改 parseLine 时遗留的 `{@link #parse}`）；CharacterRuleFieldRendererTest 15 用例绿；reviewer 全过（I5/D2/R 系列未破坏） | refactor/config-fullmigrate-modern |
| fontSort/characterFontRules 深化 P0-P5（6/8 阶段） | 6 commit `9fcfea5b`+`3561a9c4`+`f678d19f`+`5a2af136`+`9cc39ed6`+`dc3bb87c`；P0 helper 文案 / P1 Registry path 分发（path 格式 "section.field" 确认）/ P2 SceneSimpleList draggable 拖拽基建（档A越界跳变，守硬约束§5）/ P3 fontSort 接入（ConfigUI customizer hook 方案A）/ P4 CharacterRuleFieldRenderer 三栏（选项B适配层自建，D2 normalize 防抖动，I5 keyFn）/ P5 selector 逗号多点（parse 展开形态A，matches 零改动）；6 阶段 reviewer 全过 + 全量测试绿；守 I1/I3/I5/I11 + R1-R12；剩 P6/P7 新建控件 | refactor/config-fullmigrate-modern |
| 阶段 E.2 统收 Config.java + FontConfig.java 旧栈死代码 | 1 commit `3d0f758c`；Config.java 124→27 行（保留 4 字段 useDebug/uiDebug/fontRuntimeDebug/netTransport + 私有构造）/ FontConfig.java 359→200 行（删反向持久化整支含 A2 detach 方法）/ CommonProxy.preInit 时序改 [1/3]-[3/3]（删 Config.init）/ ConfigValueBridge 删 detach 调用；4 文件 Javadoc P1 悬空溯源顺手修；34 用例绿；守 I1/I3/I7 | refactor/config-fullmigrate-modern |
| 阶段 D+E.1 合并 删旧栈整支 + 修引用方 | 1 commit `8a84d560`（原 commit 2+3 合并因分两 commit 编译失败）；删 22 主代码（旧栈 UI 11 + 网络同步 11） + 10 测试；修 6 引用方（CommonProxy/ClientProxy/VanillaMixinTransport/NetSelfCheckRunner/NetRuntimeSelfChecks + 清单外 NetEnvelopeDispatcher）；P1 ModConfigGui Javadoc 顺手修；31 用例绿；守 I1/I3/I7 | refactor/config-fullmigrate-modern |
| 数值字段默认走 input + fontSortConfigured 兜底改向（用户真机验证后调整） | 1 commit `f4ecd951`；删 7 处 .slider()（lerpMode/aaMode/smoothRangeMin/smoothRangeMax/aaStrength/awtCharSize/charSize，保留 .range()）+ fontSortConfigured 改按 fontSort 非空（让 DefaultFontOrderHints 系统字体优先级提示在用户空配置时自动生效，中文字体排前）；17 用例回归全绿；守 I1/I3/I7 | refactor/config-fullmigrate-modern |
| 阶段 C C4 ModConfigGui 中转到 ModernConfigEntry（guiFactory 入口正式切到新栈） | 1 commit `946be55b`；编译绿 + 14 用例回归全绿；独立复审全过 + P1 Javadoc 收尾；守 I1/I3/I7；C5/C6 死代码暂留窗口按拍板待阶段 D/E 统收 | refactor/config-fullmigrate-modern |
| 阶段 C P0 核心三块（C1 ConfigValueBridge 值回灌 / C2 ConfigSaveListener 保存回调 / C3 ModernConfigBootstrap 启动加载首次回灌） | 5 commit + 各 P2 收尾；编译绿 + 14 用例绿（C1 6 + C2 5 + C3 3）；三轮独立复审全过；守 I1/I3/I7；CommonProxy.preInit 时序铁律 reviewer 独立自核；resolve P0 缺口（新栈无值回灌链路 + 保存回调唯一重挂点） | refactor/config-fullmigrate-modern |
| 字符硬裁切 P1 根治（uvBounds 硬墙切 mipmap 羽化） | 4.0 cherry-pick `155325bf`（refactor 分支原提交 `15a222e7`）。根因：`fontF.frag` safeSample 把 mipmap 缩小时物理存在于 padding 区的羽化低 alpha 当作越界归零，原实现顶点 texCoord 与 uvBounds clip 边界复用同一组 ink±bleed UV，硬墙只放 1px 不足以吸收 mipmap 羽化尾巴。修复：`GlyphQuadMetrics` 拆分 texCoord（仍 ink±bleed）与 uvBounds clip（改用 slot 边界含完整 8px padding），`GlyphRenderBatch.addQuad` 顶点 texCoord 不变、uvBounds 字段改传 clip 值；safeSample 可在整个 padding 内自由羽化，任何缩放不再硬切；shader 无改动，INK_BLEED 不变，quad 几何不变。`FontBatchRendererGlyphQuadTest` 补 clip 精确断言 + 保留 texCoord 断言；font 模块 13 测试类全绿，reviewer 通过（P2 附注：极端无 padding slot 下 clip 紧于 texCoord bleed 由 shader 裁至 slot，更安全行为 test2 已覆盖）。约束：后续调 aaStrength 须保证 AA 采样偏移 < INK_PADDING=8。 | 4.0（cherry-pick 自 refactor/config-fullmigrate-modern） |
| 早期已稳定单元归并：阶段 A（SIMPLE_LIST+SimpleListFieldRenderer+schema 接入+FormFieldShell 高度）+ 阶段 B（ModernConfigEntry createScreen 入口）+ actionBar 上移 + 旧栈迁移决策5项拍板 + AbstractSceneHostWidget 提正式包 + 旧决策清理（49文件） + U1 FormFieldShell/FormTheme 下沉 + U2 FormPageShell 产品化 + ConfigScreen 复用 + Oracle C 3 项 + 控制论重构 U1-U5（grep门禁/状态源收敛/Subagent收口/AGENTS闭环骨架/docs控制分层） + opencode 原生子 agent 体系 | 见各提交历史；控制论重构总评（Oracle C 复审）：结构层面优秀（五环成链+反向反馈+双传感器+内容零丢失），grep 门禁 5 断言计数口径已统一，`scene技术债.md` 残留锚点已修正 | 各 refactor/* 已合回 4.0 |

> 控制论重构总评（Oracle C 复审）：结构层面优秀（五环成链+反向反馈通道+双传感器+内容零丢失），
> 实例层面已修复（交接状态源更新为当前真值）。grep 门禁 5 断言计数口径已统一。
> `scene技术债.md` 残留 `AGENTS §5` 已改语义锚点。

## 5. 长期未回填（指针）

- scene 技术债权威清单 → `docs/诊断层/scene技术债.md`
- 宪章偏离登记 → `NORTH_STAR.md`《偏离登记》段
- 架构边界与 strangler 路线图 → `docs/设定值层/边界.md`
- 关键取舍背景 → `docs/反馈层/决策/`

## 维护说明

- 本文件是唯一权威状态源，状态变化直接覆盖，不堆历史交接。
- 只保留：当前状态 + 未收敛项 + 待拍板 + 最近完成简表 + 长期指针。
- 瞬时状态（分支/HEAD/工作区/测试/阻塞/待办）写本文件；
  控制律（完成标准/约束/审核纪律/fixer 串行规则）归 `AGENTS.md` 与
  `SUBAGENT-ORCHESTRATION.md`，不入本文件（避控制律误并入状态）。
- 长期事实按控制分层归位（设定值/控制律/传感/诊断层），关键取舍归 `docs/反馈层/决策/`，错误预防通则归
  `docs/反馈层/错误预防.md`，本文件只留指针。
- 未收敛项收敛后即从 §2 移除，不保留历史。

## 状态层次边界（防混淆）

本文件是**项目级**状态源（跨多任务、长期）。**会话级**工作记忆（单任务内、易失）不进本文件，归
`docs/控制律层/编排模式/SESSION-HANDOFF.md` 模式下的 `.opencode/session-handoff.md`。
会话 handoff 中"有持续价值的事实"任务完成后才回流到本文件或决策/错误预防，其余丢弃。
