# 交接（单一权威状态源）

> 本文件是项目唯一权威状态源。`现状.md`/`计划.md` 已并入本文件并删除。
> 任何状态冲突以本文件为准（消除多头发散，建立单一误差向量）。
> 控制律（完成标准/约束/审核纪律）不入本文件，归 `AGENTS.md` 与
> `docs/控制律层/编排模式/SUBAGENT-ORCHESTRATION.md`。

## 1. 当前状态 Current State

- 当前分支：`add/config-draft-validator`（待审合入 `4.0` 后打 tag `4.5.2`）
- 最新提交：精确以 `git log --oneline -1` 为准
- 工作区：提交后应干净；精确状态以 `git status` 为准
- 发布：代码、changelog 与完整终审阻断修复已备 **4.5.2**（DraftValidator）；正式 tag / push / GitHub Release **尚未执行**（待合入后按 `docs/控制律层/发布流程.md`）
- 前一正式版：`4.5.1`（issue #63 裁切）
- 相关测试：三阶段乐观事务、validator 零锁、stale/并发冲突、NUMBER 规范化、SIMPLE_LIST 保存类型闭环、manager 通知期跨线程拒绝、prepared 写盘、UI 全字段回读与事件异常隔离均有回归；runtime/schema/UI 定向测试、`compileJava`、全量 `test`、`check`、文档纪律门禁与 `git diff --check` 全绿

## 2. 当前未收敛项 Open Items

> 仿 `NORTH_STAR.md`《偏离登记》范式。每项含 what/why/blocked-by/next/优先级。
> 收敛后即从本节移除，不保留历史。

<open-items>

<item id="flushraw-prepare-conflict-test">
  <what>flushRaw 已有 prepare 后 Authority 冲突守卫，但尚缺精确卡在 prepare 窗口的确定性并发回归</what>
  <why>Persistence 为 final 且由 ConfigManager 私有构造，文件对象仅参与 write；现有结构无可靠 fault injection，拒绝用固定耗时或反射造脆弱测试</why>
  <blocked-by>未来若生产设计自然引入可替换 persistence 协作者，再补确定性测试</blocked-by>
  <next>维持现有冲突检查；涉及 flushRaw/Persistence 构造边界变更时补回归</next>
  <priority>低（P2 测试缺口）</priority>
</item>

<item id="remote-api-lts-cleanup">
  <what>LTS / 使用文档中通用远程 HTML（RemoteDocumentPages / RemoteHudOverlays）仍标可用，但 ui.remote 包可能已从源树移除</what>
  <why>阶段 1 审核发现的预存文档债，不在 config 对齐 scope</why>
  <blocked-by>无</blocked-by>
  <next>另开 docs 任务核对源码后清 LTS/远程页面文档</next>
  <priority>低</priority>
</item>

<item id="release-4-5-2">
  <what>4.5.2 DraftValidator 与完整终审阻断修复已完成，待合入 4.0 后打 annotated tag 并推送触发 Release</what>
  <why>用户要求未审前不 merge / tag / push / release</why>
  <blocked-by>代码审 + 用户确认合入</blocked-by>
  <next>merge --no-ff → 4.0 → tag 4.5.2 → push 分支与 tag</next>
  <priority>中</priority>
</item>

<item id="qz-miner-consume-4-5-2">
  <what>Qz-Miner 在 UILib 4.5.2 发布后接入 DraftValidator（跨字段业务校验）并验收保存 fail-closed / UI 错误展示</what>
  <why>上游 4.5.2 尚未 tag/release；本分支不跨仓改 Miner</why>
  <blocked-by>release-4-5-2</blocked-by>
  <next>依赖 4.5.2 → bootstrap 三参挂 validator → 实机/单测验收 INVALID 与字段红字</next>
  <priority>中</priority>
</item>

</open-items>

## 3. 待拍板项 Awaiting User

- 无架构待拍板（DraftValidator 方案用户已批准）

## 4. 最近完成 Recently Done

| 单元 | 结果 | 分支 |
|---|---|---|
| DraftValidator 完整终审 | capture→锁外 validate/prepare→verify/write/引用交换；通知期跨线程拒绝；SIMPLE_LIST 类型闭环；stale/并发冲突保留。 | add/config-draft-validator |
| DraftValidator 4.5.2 | 只读 DraftView + 提交错误 UI + patch 例外登记；待审合入 | add/config-draft-validator |
| 发布 4.5.1 | issue #63 宿主裁切基线修复合入；`.changelogs/4.5.1.md` + CHANGELOG；annotated tag | 4.0 + tag 4.5.1 |
| issue #63 字符裁切失效 | 修复宿主 scissor 基线求交/幂等恢复；静态 clear 与实例 restore 分离；clip 边界 flush deferred text；新增 render 回归测试。 | fix/issue-63-clip-state → 4.0 |
| 发布 4.5.0 | 新增 `.changelogs/4.5.0.md`（scene 新栈重构说明）+ CHANGELOG 条目；annotated tag 推送 | 4.0 + tag 4.5.0 |
| P2 样板收敛 | DraftListBridge；FormActionBar 下沉 scene.form；SceneAutocomplete 成品壳；CharacterRule 迁 modern；demo 对齐 FormFieldShell。 | refactor/config-p2-sample-converge → 4.0 |
| 配置页文档对齐 D1 | 重写 ModernConfig 使用文档、清边界/诊断/LTS/宿主入口旧指针。 | docs/config-modern-align → 4.0 |
| config 全迁移链 | merge 整支合回。 | refactor/config-fullmigrate-modern → 4.0 |

## 5. 长期未回填（指针）

- scene 技术债权威清单 → `docs/诊断层/scene技术债.md`
- 宪章偏离登记 → `NORTH_STAR.md`《偏离登记》段
- 架构边界与 strangler 路线图 → `docs/设定值层/边界.md`
- 关键取舍背景 → `docs/反馈层/决策/`
- 配置页接入 → `docs/使用文档/02-控件/配置页（ModernConfig）.md`
- Config 模块现状 → `docs/诊断层/Config模块.md`
- DraftValidator 决策 → `docs/反馈层/决策/config-draft-validator.md`

## 维护说明

- 本文件是唯一权威状态源，状态变化直接覆盖，不堆历史交接。
- 只保留：当前状态 + 未收敛项 + 待拍板 + 最近完成简表 + 长期指针。
- 瞬时状态（分支/HEAD/工作区/测试/阻塞/待办）写本文件；
  控制律（完成标准/约束/审核纪律/fixer 串行规则）归 `AGENTS.md` 与
  `SUBAGENT-ORCHESTRATION.md`，不入本文件（避控制律误并入状态）。
- 长期事实按控制分层归位（设定值/控制律/传感/诊断层），关键取舍归 `docs/反馈层/决策/`，错误预防通则归
  `docs/反馈层/错误预防.md`，本文件只留指针。
- 未收敛项收敛后即从 §2 移除，不保留历史。

## 状态层次边界（防混淆）

本文件是**项目级**状态源（跨多任务、长期）。**会话级**工作记忆（单任务内、易失）不进本文件，归
`docs/控制律层/编排模式/SESSION-HANDOFF.md` 模式下的 `.opencode/session-handoff.md`。
