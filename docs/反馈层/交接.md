# 交接（项目状态权威源）

> 业务状态、阻断项和待决策事项以本文件为准；实时分支、HEAD 与工作区以 Git 命令为准。

## 当前状态

- agent 终端命令已统一走 Python 参数列表，OpenCode 使用内置默认 `build`，任务笔记按需创建；代码改动按仓库 `AGENTS.md` 在提交前执行本机完整 `build`。`runClient*`、`runServer*`、发布与生产操作仍不自动执行；CI 保持零 PowerShell 边界。配置改动仍需重启 OpenCode 才生效。
- `4.6.3` 已是不可移动的历史发布；当前 `feat/ui-projection-material-item` Breaking 功能分支目标发布身份为 `5.0.0`。版本继续由 Git tag / GTNH Gradle 推导，仓库已删除临时 `gradle.properties` 版本钉死。`2026-07-31` 已在当前工作树执行 `call gradlew.bat --no-configuration-cache build`，编译、checkstyle、JUnit、classpath isolation 与 assemble 全部成功；当前仍无 `5.0.0` tag、Release、JitPack 制品、CI 或游戏运行态证据，状态为 `INCOMPLETE`。
- 当前功能分支的 FML 同 minor 远端版本范围已同步为 `[5.0.0,5.1.0)`，主信封 v2 与 Realtime v1 未变。范围接受正式 `5.0.0`、后续 `5.0.x` 与 Maven 排序低于正式上界的 `5.1.0` qualifier，拒绝低于正式下界的 `5.0.0` qualifier、正式 `5.1.0` 及全部 `4.x`；契约测试已随本地完整 build 通过。CI、发布 JAR annotation、dedicated server 双向握手与真实流量均无本次实证，状态为 `INCOMPLETE`。
- `4.6.2` annotated tag object 固定为 `6155c157b823c928accc25b037f7a95e7e83d669`，peeled commit 固定为 `e86a731cf10c5fa9e0f3dd87fe52126646bf8ed1`，不可移动。
- 默认分支已到达 `a86838b7`。固定 recovery 的 [verify run `29989023055`](https://github.com/QuanhuZeYu/Qz-UILib/actions/runs/29989023055) 与 [publish run `29989322298`](https://github.com/QuanhuZeYu/Qz-UILib/actions/runs/29989322298) 均为 attempt 1、`completed/success`。
- GitHub Release [`4.6.2`](https://github.com/QuanhuZeYu/Qz-UILib/releases/tag/4.6.2)（ID `357902877`）已于 `2026-07-23T07:53:45Z` 发布；ID/tag 两端点一致，notes 与 main/dev/sources/dev-preshadow 四资产的名称、大小和 digest 均已核对。
- 仓库自动化已收敛为常规 build CI、内联零 PowerShell policy；已完成的固定 `4.6.2` recovery workflow 已删除。其 verify/publish 历史结果保留在本交接中，但不再是可运行能力。通用 tag、JitPack/Maven gate 与外部 checker 均已退役；本地完整 build 现按 `AGENTS.md` 作为代码提交门禁。
- Qz-UILib 4.6.0、4.6.1 与 4.6.3 的既有 GitHub Release 保持历史事实；4.6.1/4.6.3 的历史 JitPack 验证结果不变，但不代表仓库当前仍有通用 advisory 能力或 `5.0.0` 制品。
- GTNH `2.9.0-beta-2` 自动化适配已有历史测试/编译/build 成功结果；`runClient21` 运行态仍未执行。
- issue [#65](https://github.com/QuanhuZeYu/Qz-UILib/issues/65) 已[回复终态](https://github.com/QuanhuZeYu/Qz-UILib/issues/65#issuecomment-5055913391)并关闭，状态为 `CLOSED/COMPLETED`。
- Picker 当前复现的 HostImage 崩溃、全空白及 14 项末行裁切已得到抑制；完整交互矩阵与第三方 renderer 尚未覆盖。
- 字体共享准备已改为继承调用阶段的 depth test/mask/func，screen/HUD 仍由 UI host 显式建立无 depth test 状态；纯源码契约测试已补，尚待 CI 执行，告示牌正反面遮挡尚待用户真机验证。
- 玩家标签顺序与字体 depth 所有权保持正交：`6a9a375b` 继续保留，world-last 初版 `de15830c` 及安全回退 `4d25977c` 的历史结论不变；当前由 `8d55cf45`、`efc0d755`、`f48a4e46` 三笔独立提交实现 pass-scoped FIFO、普通名称/计分板最终调用点 `@WrapOperation`、Angelica `2.1.50` 可选 guard 与 lightmap 双层恢复。最终组合独立静态复审未发现 P0/P1/P2，A1-A9 静态满足；相关测试已随本地完整 build 通过，CI、发布制品、dedicated server、Angelica shader/shadow 与游戏内标签矩阵仍无实证，状态为 `INCOMPLETE`。
- scene 宿主关闭系统光标扫尾已补齐：`SceneRuntime` 对每个已绑定 backend 在关闭生命周期强制复位 `DEFAULT`，不改 `cursorSignal`，并以幂等 finally 覆盖 Owner 清理失败；runtime fake backend 回归测试已随本地完整 build 通过。CI 与游戏内界面切换仍无实证，状态为 `INCOMPLETE`。

## 当前未收敛项

<open-items>

<item id="fml-remote-version-compatibility">
  <what>有界同 minor 范围与契约测试源码已提交并静态复审，但双端 FML 握手和真实线协议兼容尚未闭环</what>
  <blocked-by>缺少 CI 编译/JUnit、发布制品 JAR annotation，以及 dedicated server 双向版本矩阵实证</blocked-by>
  <next>由 CI/用户核对生产 JAR 的 `acceptableRemoteVersions`；使用两个都携带范围的端点覆盖同版本、后续 patch 双向与越界版本，再分别覆盖客户端缺 mod、服务端缺 mod，并在握手成功组合中发送真实主信封 v2 与 Realtime v1 流量</next>
  <priority>高</priority>
</item>

<item id="font-world-depth-runtime">
  <what>全局字体替换后的告示牌世界深度遮挡与 screen/HUD 二维文字运行态尚未实测</what>
  <blocked-by>用户运行客户端并启用原版字体替换</blocked-by>
  <next>从告示牌正反面核对文字可见性与模型遮挡，并回归 screen/HUD 二维文字</next>
  <priority>中</priority>
</item>

<item id="player-name-tag-render-order">
  <what>普通玩家名称/计分板标签的 pass-scoped 延后实现与静态复审已完成；旧视觉问题已有受限方案，但运行态闭环尚未完成</what>
  <blocked-by>测试源码尚未运行；缺少 CI 编译/JUnit、发布制品与 shader/shadow、dedicated server、游戏内矩阵实证</blocked-by>
  <next>由 CI/用户完成编译与 JUnit；核对无 Angelica、精确 2.1.50、未知版本降级和 dedicated server；游戏内覆盖普通名称/计分板/潜行、箱子前后遮挡、昼夜与多亮度，并分别观察 Angelica shader 主 pass/shadow 无重复、串帧或状态泄漏</next>
  <priority>高</priority>
</item>

<item id="scene-projection-material-item">
  <what>issue [#66](https://github.com/QuanhuZeYu/Qz-UILib/issues/66) 已按用户授权以 `CLOSED / NOT_PLANNED` 关闭；UILib 不实现 GuiContainer / vanilla Slot 宿主桥，也不合入 #66 worktree。当前功能分支已写入 U0 screen/overlay projection、Breaking snapshot-only `itemIcon` seam，以及真实 M2 Motion：每个 `SceneRuntime` 独立采样 host 单帧时间，Button 使用 90ms state-layer 色彩过渡，Toggle 与 Navigation 使用 160ms transform/selection 过渡，section 使用 240ms 单槽 fade-through，顶层优先关闭 outgoing overlay，严格先 dispose outgoing Owner 再 mount incoming；逐帧 sample 不写 Draft/Persistence/transaction history。完整 build 与手动时钟、长帧、失效级别、多 occurrence、single-live、overlay 顺序及滚动隔离测试已通过。用户已确定 UILib 目标版本 `5.0.0`、FML 范围 `[5.0.0,5.1.0)`、Miner 编译坐标 `5.0.0:dev` 与运行范围 `[5.0.0,6.0.0)`，两仓源码合同已同步</what>
  <blocked-by>`5.0.0` tag/JitPack `dev` 制品尚不存在，Miner 新坐标当前不可从权威远端解析；Minecraft/LWJGL2 真实帧率下的视觉节奏、连续导航与 GL opacity/transform 表现仍需用户运行态验证</blocked-by>
  <next>用户先从 `/qzuilib modernconfig` 验证 Button/Toggle/Navigation、section 连续切换、overlay 关闭、滚动保持与 resize；在另行授权发布操作后创建并验收不可移动的 `5.0.0` tag/JitPack 制品，再完成 Miner 客户端 smoke</next>
  <priority>高</priority>
</item>

<item id="scene-cursor-close-cleanup">
  <what>scene screen close 的 `forceApply(DEFAULT)` 生命周期扫尾与 fake backend 测试源码已补齐；同一 screen 内结构卸载后的 hover/cursor 重算不在本次范围</what>
  <blocked-by>缺少 CI 编译/JUnit 与游戏内非默认光标关闭、连续切换界面的运行态实证</blocked-by>
  <next>由 CI/用户执行测试；游戏内分别从 pointer/text/none 等非默认样式关闭 scene 界面并打开后续界面，确认系统光标立即恢复 DEFAULT，另将 portal/条件节点/动态列表卸载残留作为独立任务处理</next>
  <priority>中</priority>
</item>

<item id="gtnh-runtime-and-picker">
  <what>GTNH beta-2 基础运行态与 Picker 完整交互/第三方 renderer 矩阵未完成</what>
  <blocked-by>用户运行客户端并按需覆盖 smoke</blocked-by>
  <next>验证启动、配置页、scene test hub、连续添加、滚动/焦点、编辑删除与第三方 renderer</next>
  <priority>中</priority>
</item>

<item id="remote-api-lts-cleanup">
  <what>LTS/使用文档中的通用远程 HTML 能力可能与已删除源码不一致</what>
  <next>另开文档任务核对源码与对外文档</next>
  <priority>低</priority>
</item>

<item id="cross-process-os-cas">
  <what>配置写入仍不提供跨进程 OS 级 CAS</what>
  <next>若产品需要，再独立设计文件锁或单写者协议</next>
  <priority>低</priority>
</item>

</open-items>

## 待拍板项

- 未来通用 tag、JitPack 或 Maven 能力均不在当前范围，重建前须重新拍板。

## 长期指针

- scene 技术债：`docs/诊断层/scene技术债.md`
- 宪章偏离：`NORTH_STAR.md`《偏离登记》
- 架构边界：`docs/设定值层/边界.md`
- 关键取舍：`docs/反馈层/决策/`
