# 交接（项目状态权威源）

> 本文件是业务状态、阻断项和待决策事项的权威源；实时 Git 分支、HEAD 与工作区状态以 `git branch --show-current`、`git rev-parse HEAD`、`git status` 为权威。控制律归 `AGENTS.md` 与编排文档。

## 当前状态

- StructuredList member 已具备可选中文显示名/辅助说明元数据，保持 YAML key、校验和兼容语义不变；成员编辑区改用可复用纵向 labeled-control 安全默认，去除 90px 标签与 180px 输入固定宽。主 agent 已通过 `StructuredValueSpecTest`、`StructuredListFieldRendererTest`、`FormLabeledControlTest` 三目标测试、全量 `test`、`compileJava`、scene 门禁与文档门禁；剩余为 `runClient21` 实机配置页验收及消费方集成。
- 4.6.0 通用被动 HUD API 已按 I13 收口：bridge 使用 framebuffer 尺寸，layout/paint/font/clip 使用 UILib logical px；host-owned scale 默认 1.0、五档可选且不读取 MC GUI scale。Compact/Normal 字号为 12/14px，外框按内容 intrinsic 宽收缩并在视口边界 clamp，支持通用 min/max width；`HudSpan`、`HudLine.rich/richProgress` 已提供 keyed 富文本，tone 仅表达语义且旧 `HudLine` API 兼容。仍待 `runClient21` 实机确认四角像素、五档缩放、动态宽度和断线重建。
- `4.0` 已在 `d78b9616` 通过 `--no-ff` 合入 4.6.0 正式发布准备；关键提交为 `25312e15`（release prep）与 `ae2084f7`（blank gate fix）。本地发布门禁、构建和 Maven Local 均通过，合并后工作区洁净。
- `git push origin 4.0` 被 GitHub 拒绝：OAuth token 缺少 `workflow` scope，无法创建或更新 workflow。远端 `4.0` 仍为 `4c0c6b8`；`4.6.0` tag 未创建，也没有本次发布产生的 GitHub Release、workflow 或远端 Maven 制品。
- 恢复发布前须由用户执行 `gh auth refresh -h github.com -s workflow`。
- beta-10 将 SearchPicker beta API 收敛为 ALL/SELECTED 两态，SELECTED 支持 1..N checkbox，并无损展示、保留和移除当前未枚举 key。稳定契约见 `docs/反馈层/决策/config-search-picker.md`。
- SearchPicker 的 `LIST_MEMBERS` 关闭态现按左侧 `Manage`、摘要顺序排列，raw 默认折叠为高级修正/删除入口；管理 portal 采用 480px 目标宽、360px 最小宽、8px safe inset 与视口高度 cap，顶部搜索，当前成员最多 8 行、结果最多 12 行。可添加结果按精确 candidate key 排除合法当前成员，完整结果仍负责成员解码；新增成功后保留 portal、query、合法窗口与搜索焦点并立即重新武装，编辑仍按稳定 id 替换后关闭。malformed/duplicate 继续只提示、不泄露 raw、不合并；旧 `SINGLE_VALUE`/`Codec` 兼容。该能力继续纳入尚未远端发布的 4.6.0。
- Picker 目标测试、全量 `test`、`compileJava`、scene 边界与文档纪律门禁已通过；Qz-Miner 消费仓实机仍待验证连续批量添加、portal 尺寸、滚动、焦点与领域文案，当前未执行 merge/push/tag/release，也不能标记真机通过。
- beta-9 不改配置核心语义、YAML 或 network。Picker 返回完整精确结果并用固定 8 行窗口化连续滚动；键盘高亮与 hover 独立。STRUCTURED_LIST 默认使用无框字段外壳、单层紧凑对象卡片与稳定 key 折叠意图，editor 可通过通用 current-value presenter 提供摘要。

## 当前未收敛项

<open-items>

<item id="config-search-picker-ux">
  <what>Picker 列表成员管理重设计与 StructuredList 通用 UX 已完成，待 Qz-Miner 实机复测</what>
  <why>自动化已覆盖摘要、portal、滚动、稳定 id 编辑/删除和焦点边界，仍需消费方真机确认最终产品表达</why>
  <blocked-by>用户真机复测</blocked-by>
  <next>运行 Qz-Miner runClient21 核对长列表滚动、折叠和整行交互</next>
  <priority>高</priority>
</item>

<item id="release-4-6-0">
  <what>4.6.0 本地发布准备与合并已完成，远端发布尚未开始</what>
  <why>push 因 token 缺少 workflow scope 被拒；远端分支、tag、Release 与 Maven 均未改变</why>
  <blocked-by>用户执行 gh auth refresh -h github.com -s workflow，并明确恢复发布</blocked-by>
  <next>先核对 remote，再 push 4.0；成功后创建并 push annotated tag 4.6.0，核验 GitHub Release 与 GTNH Maven 四类制品。不得重做合并、移动 tag 或覆盖版本</next>
  <priority>高</priority>
</item>

<item id="remote-api-lts-cleanup">
  <what>LTS/使用文档中的通用远程 HTML 能力可能与已删除源码不一致</what>
  <why>预存文档债，不属于 Picker 优化范围</why>
  <blocked-by>无</blocked-by>
  <next>另开文档任务核对源码与对外文档</next>
  <priority>低</priority>
</item>

<item id="cross-process-os-cas">
  <what>配置写入仍不提供跨进程 OS 级 CAS</what>
  <why>当前只保证同 JVM 参与式 writer 的写前检测与替换</why>
  <blocked-by>产品是否要求跨进程强一致</blocked-by>
  <next>若后续需要，再独立设计文件锁或单写者协议</next>
  <priority>低</priority>
</item>

</open-items>

## 待拍板项

- 用户刷新 GitHub workflow scope 后，明确何时恢复 4.6.0 远端发布。

## 最近完成

- `LIST_MEMBERS`：StructuredList 的 `List<String>` 成员管理收敛为摘要 + Manage、默认折叠 raw 高级入口、稳定 id 编辑与两步确认删除；当前成员/结果容量为 8/12，仍动态收缩并受 portal 可用高度裁剪；字段级视口默认 320 logical px、可显式 640，旧 API 兼容。
- 4.6.0 稳定 API 与通用被动 HUD 文档已覆盖富文本 span：稳定且行内唯一的 id、MUTED label + INFO value、语义 tone 边界及旧 `HudLine` API 兼容；未扩展交互能力。
- 通用 HUD token/layout 已完成 12/14px 字号、内容宽背景、长文本 clamp、多行最长宽、动态文本重排与左右锚点稳定回归；未开放任意文本样式。
- HUD viewport 生产提取已改为可注入 Minecraft 环境，GUI scale 仅作诊断且不进入 factory；五档独立 HUD scale 的完整 PaintPlan/replay 几何、clip、offset 与字体单次缩放测试已闭合。
- `4.5.3-beta-12`：StructuredList 独立 320px 视口、260px 可伸缩标题槽与非空 FieldSpec 默认恢复。
- `4.5.3-beta-11`：StructuredList header 标题使用可收缩的裁剪槽，长 identity 不再遮挡固定按钮。
- `4.5.3-beta-10`：Picker ALL/SELECTED 两态、空确认门禁、草稿往返与未枚举 key 无损编辑。
- `4.5.3-beta-9`：Picker 完整结果与 8 行窗口化、独立 hover/键盘导航；StructuredList 无框单层卡片、稳定折叠意图和 current-value presenter SPI。
- `4.5.3-beta-8`：完整生产树宽窄回归；raw/picker 改为纵向 column，修复横排越界裁剪。
- `4.5.3-beta-7`：无状态 codec、领域错误、raw+picker、identity 标题与 scrollbar 布局收口。
- `4.5.3-beta-1..6`：结构化列表、choice、多变体 SearchPicker、快照/受控状态、scene 图片与 reset/reload 接线。

## 长期指针

- scene 技术债：`docs/诊断层/scene技术债.md`
- 宪章偏离：`NORTH_STAR.md`《偏离登记》
- 架构边界：`docs/设定值层/边界.md`
- 关键取舍：`docs/反馈层/决策/`
