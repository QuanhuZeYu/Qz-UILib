# Qz-UILib HTML-like UI 实施规划

## 目标边界

- 本项目主线调整为：在 Minecraft 1.7.10 / GTNH / LWJGL3ify 环境中实现一套完整的 HTML-like UI 渲染框架。
- “完整”指框架内部具备接近浏览器渲染模型的稳定分层：文档树、样式计算、盒模型、布局、绘制、裁剪、滚动、命中测试与输入分发。
- 本项目不直接实现通用浏览器内核，不承诺完整 HTML5、CSS 全量规范、JavaScript DOM API、网络加载、表单提交、可访问性树或浏览器安全模型。
- 页面作者最终应主要面向声明式文档/元素/样式 API 编写 UI，而不是直接操作底层 `Widget` 坐标、OpenGL 状态或 Minecraft GUI 生命周期。

## 现状判断

- 当前仓库已经有可运行的 retained widget tree、文档页宿主、Div-like flex 布局、overflow 滚动、surface 绘制、宿主级背景模糊和基础控件体系。
- 当前实现已符合部分 HTML 渲染模型原则，尤其是 `border-radius` 外观与 `overflow` 结构裁剪分离。
- 当前实现仍不是完整 HTML-like 渲染框架，因为缺少标准化文档树、样式层、computed style、统一 box tree、明确 paint order、stacking/clip/scroll 模型和面向作者的统一声明式入口。

## 清退原则

- 与 HTML-like UI 主线不对应的旧规划文档直接重写或迁移，不再作为当前开发依据。
- 新功能不得继续扩大直接 `Widget` 作者入口；底层 `Widget` 应逐步退为渲染后端或兼容层。
- 不再把视觉外观值对象用于隐式控制后代裁剪；裁剪必须来自 `overflow`、viewport 或显式 clip 容器。
- 不再把 backdrop、blur、FBO、stencil 等宿主渲染细节暴露给文档作者层。
- 不再新增与当前主线无关的 native/JNI 字体里程碑；字体系统后续只作为文本渲染能力被 UI 渲染模型消费。

## 目标架构

- 文档层：提供 `DocumentNode`、`ElementNode`、`TextNode` 一类稳定结构，承载标签名、属性、子节点与文本。
- 样式层：提供 author style、theme default、inline style、继承属性、computed style 与单位解析。
- 盒模型层：显式表达 content、padding、border、margin、border-radius、overflow、scroll container 与 containing block。
- 布局层：先落地 block flow 与 flex flow，再扩展 inline text layout；所有布局输入来自 computed style 与测量服务。
- 绘制层：按背景、边框、内容、滚动条、延迟物品层等阶段输出 paint commands，最终由 Minecraft/OpenGL backend 执行。
- 事件层：以 layout box 和 clip/scroll 状态做命中测试，再映射到文档元素事件，而不是直接暴露底层控件遍历细节。
- 兼容层：现有 `Widget`、`DivWidget`、`ScrollViewportWidget`、文档控件在迁移期可作为 backend adapter，但作者入口逐步收敛到新文档模型。

## 实施阶段

### 阶段 0：规划与文档清理

- 重写旧项目规划，移除与当前 UI 主线不对应的 JNI/MSDF 里程碑。
- 更新 AI 记忆文档，明确完整 HTML-like 渲染模型目标、清退边界和下一步。
- 保留现有可运行链路，禁止在没有替代测试前破坏当前文档页。

### 阶段 1：文档树与作者入口

- 新增文档模型包，定义元素节点、文本节点、属性、子节点操作与只读遍历接口。
- 新增最小 authoring API，例如 `document.div()`、`document.text()`、`element.style()`、`element.append()`。
- 将现有 `DocumentUiScope` 的高频组件工厂逐步适配到文档元素层。
- 验收：可用新 API 构建与现有诊断页等价的静态页面树。

### 阶段 2：样式与 computed style

- 定义样式属性集合：display、width、height、min/max、margin、padding、border、background、border-radius、overflow、flex、gap、text、visibility、enabled 等。
- 实现默认样式、主题样式、元素局部样式、继承属性与 computed style 解析。
- 统一长度单位：auto、px、percent，后续可扩展 em/rem/vw/vh。
- 验收：样式变更能稳定触发布局/绘制失效，且不需要直接改底层 widget 字段。

### 阶段 3：盒模型与布局树

- 建立 layout box tree，把文档节点转换为布局盒。
- 先实现 block flow、flex row/column、wrap、gap、align、justify、grow/shrink。
- 将现有 `DivWidget` 中可复用的测量和 overflow 算法迁入布局层或封装为后端服务。
- 验收：主要文档页不再依赖手写 `Widget.applyLayoutBounds()` 组合页面结构。

### 阶段 4：绘制、裁剪与滚动模型

- 建立 paint command 列表，明确背景、边框、文本、控件内容、滚动条和延迟回放层顺序。
- 用 `overflow` 与 scroll container 驱动矩形结构裁剪；rounded structural clip 仍需真实 GL 验证后才能进入生产路径。
- 保持 `border-radius` 只表达外观，除非 computed style 中存在显式 `overflow: hidden` 且后端 clip 已验证稳定。
- 验收：滚动、卡片、圆角外观、背包物品延迟回放在测试与真实运行中都不出现内容丢失。

### 阶段 5：事件与控件适配

- 将鼠标、键盘、文本输入从 widget 命中迁移到 element/layout box 命中。
- 将按钮、开关、输入框、背包格子等控件包装成 element-backed component。
- 明确 focus、hover、active、disabled 状态来源，后续可映射到伪类样式。
- 验收：现有控件行为在新文档模型下保持可用，输入分发不依赖作者直接持有底层 widget。

### 阶段 6：清退旧入口与稳定化

- 清退或降级旧的 public screen 构造入口，统一从 `UiDocumentScreens` 进入。
- 清退直接面向页面作者的底层 widget 组合示例。
- 建立完整回归测试集：文档树、样式计算、布局、绘制快照、命中测试、滚动、真实运行验收清单。
- 验收：新 HTML-like 路径成为默认生产链路，旧路径只保留必要兼容适配。

## 近期执行顺序

1. 新增 `ui.dom` 或同等命名的文档模型最小骨架。
2. 新增 `ui.style` 的属性值对象与 computed style 初版。
3. 写测试固定文档树、样式继承、长度解析和布局失效契约。
4. 用一个现有诊断页做迁移试点，不一次性重写全部页面。
5. 每完成一个阶段都运行 `compileJava` 与对应定向测试，必要时补真实游戏内渲染验收。
