# Qz-UILib HTML-like 当前建议

## 当前目标

- 继续把 HTML-like UI 从“可运行页面迁移”推进到“稳定底层渲染框架”。
- 优先补齐会影响后续上限的基础设施，不再优先添加简单静态动画属性。
- 同步清退旧 retained widget 作者入口，避免新页面继续回到旧 `Widget` 组合模式。

## 待添加基础建设

### 1. 真正 group opacity / effect FBO 合成

- 基于已存在的 `PAINT_CONTEXT_START` / `PAINT_CONTEXT_END` 命令边界，把指定子树绘制到离屏层后再整体合成。
- 让 `CUSTOM`、`BACKDROP_FILTER` 与后续 filter/effect 能参与真实 group opacity，而不是只对标准颜色命令提前乘 alpha。
- 需要在 `UiRenderContext.pushPaintContext(...)` / `popPaintContext()` 接入离屏目标、clip 继承、资源复用与 FBO 不可用降级。

### 2. CSS-like stacking context 隔离

- 当前已有 stacking phase，但还不是完整 stacking context。
- 后续应让 `opacity < 1`、positioned + 显式 `z-index`、`backdrop-filter` 等元素形成独立上下文。
- 子孙元素不应逃出父 stacking context 与外部 sibling 做全局乱序比较。

### 3. 可复用 UI 主层快照服务

- 当前元素级 `backdrop-filter` 会独立复制当前 framebuffer 局部区域。
- 后续应抽出 UI 主层快照服务，同帧复用 snapshot，统一采样区域扩张、尺寸限制、blur/saturate 参数和失败降级。
- 多 glass 元素、大面积 glass 页和滚动场景应避免重复昂贵复制。

### 4. dirty subtree 与细粒度缓存

- 当前已拆分 `layoutVersion` / `paintVersion`，但缓存粒度仍偏文档级。
- 后续应记录变更子树，让 layout、computed style refresh 与 paint command 只重算受影响分支。
- 该能力对输入框、列表、动态数据页和动画密集 UI 很关键。

### 5. `position: fixed` 与 viewport containing block

- 基于现有 absolute containing block 语义，新增 fixed 定位。
- fixed 元素应相对 `HtmlLikeDocumentWidget` viewport，而不是文档滚动内容。
- 目标场景包括固定工具栏、悬浮按钮、toast、modal 和调试 overlay。

### 6. absolute stretch 约束

- 补齐 `left + right`、`top + bottom` 同时存在时的 stretch 语义。
- 支持绝对定位元素贴边填充父 containing block，例如浮层铺满、右侧面板、底部栏。
- 该能力应进入 layout 层，而不是由页面作者手写宽高计算。

### 7. inline formatting 初版

- 当前直接文本可换行，但尚未支持真正 inline element 混排。
- 后续应支持 text/span 混排、局部文本颜色、inline box、高度对齐、图文组合。
- 这是继续接近 HTML-like 作者体验的核心能力，但工作量较大，建议排在合成与定位基础之后。

### 8. clip / effect chain 显式建模

- 把 overflow clip、rounded clip、paint context、backdrop/effect 的作用顺序整理成显式模型。
- 避免后续 FBO 合成、圆角裁剪、backdrop 采样和滚动组合时出现隐性顺序 bug。
- 该模型应同时服务 paint command、renderer、hit-test 和滚动条几何。

## 待清理旧代码

### 第一批：旧页面宿主链路

- 删除或降级 `UiTestScreen`。
- 删除或降级 `InventoryOverviewScreen`。
- 删除 `ControllerBackedDocumentScreen`。
- 删除 `BaseDocumentScreen`。
- 原因：当前 `UiDocumentScreens.create...` 已走 `DefinitionBackedHtmlLikeDocumentScreen`，生产页面不再需要旧 `DocumentPageWidget` 页面壳宿主链路。

### 第二批：旧页面壳适配层

- 移除 `DocumentPageAuthoringSurface.adapt(DocumentPageWidget)`。
- 移除 `DocumentPageWidgetAuthoringAdapter`。
- 删除或隔离 `DocumentPageWidget`。
- 前提：第一批旧 screen host 已清完，测试中直接 new `DocumentPageWidget` 的场景已替换为 direct surface 或 HTML-like widget 测试夹具。

### 第三批：收缩 `DocumentUiScope` 旧 factory

- 移除 retained widget factory：`text(...)`、`card()`、`section()`、`toolbar()`、`flowRow()`、`formRow(...)`。
- 移除 retained control factory：`button(...)`、`toggle(...)`、`segmented(...)`、`textInput()`、`inventoryGrid(...)`、`scrollDiv()`。
- 保留或改名为更小的运行时上下文，只暴露 `theme()`、`getTextMeasureService()`、`getRuntimeAdapters()` 等 HTML-like 页面仍需要的能力。

### 第四批：旧 retained 页面控件

- 在替代覆盖完成后删除 `DocumentTextWidget`、`DocumentCardWidget`、`DocumentToolbarWidget`、`DocumentFlowRowWidget`、`DocumentSectionWidget`、`DocumentFormRowWidget`。
- 在 HTML-like 控件覆盖稳定后删除或 internal 化 `ButtonWidget`、`ToggleSwitchWidget`、`SegmentedSelectorWidget`、`TextInputWidget`、`InventorySlotGridWidget`。
- `InventorySlotGridLayout`、`InventorySlotGridItemRenderer`、`InventorySlotSnapshot` 这类可复用底层能力暂不删除，因为 HTML-like 背包格子控件仍可复用。

### 暂不清理

- `Widget`：仍是 retained backend 根抽象。
- `UiInputRouter`：仍负责宿主输入分发。
- `UiRenderContext`：仍是渲染后端与效果服务入口。
- `DivWidget`、`ScrollViewportWidget`、`RoundedScrollViewportWidget`：可先降级为 backend/internal/兼容测试工具，不建议立即删除。

## 建议执行顺序

1. 先删除旧页面宿主链路：`UiTestScreen`、`InventoryOverviewScreen`、`ControllerBackedDocumentScreen`、`BaseDocumentScreen`。
2. 移除 `DocumentPageAuthoringSurface` 的 `DocumentPageWidget` adapter，并替换对应测试夹具。
3. 收缩 `DocumentUiScope`，让新页面作者无法再通过该作用域创建旧 retained widget。
4. 在 paint context 边界上实现真正 group opacity / effect FBO 合成。
5. 基于 paint context 完善 stacking context 隔离。
6. 推进可复用 UI 主层快照服务，优化 backdrop-filter 多元素场景。
7. 再补 `position: fixed`、absolute stretch、inline formatting 等布局和作者体验能力。

## 验收要求

- 每批清理都必须保持 `test` 与 `compileJava` 通过。
- 删除旧入口前必须确认 `UiDocumentScreens` 仍能创建诊断菜单、布局诊断页、Smoke 页、Glass 页和背包页。
- 清理旧 retained widget 时不能删除 HTML-like 控件仍在复用的底层算法与渲染适配能力。
- 新增底层能力不得把 FBO、OpenGL、shader、stencil 等宿主细节暴露给文档作者层。
