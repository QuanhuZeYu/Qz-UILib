# Qz-UILib HTML-like 当前建议

## 当前目标

- 继续把 HTML-like UI 从“可运行页面迁移”推进到“具备 CSS-like 动态语义的稳定渲染框架”。
- 当前下一阶段主目标是推进 CSS transition / animation 语义，而不是继续无限补静态布局、inline 细节或底层 effect 优化。
- inline formatting 仍是重要静态排版能力，但从现在起只作为动画演示与真实页面迁移的阻塞项收口，不再默认排在动画之前。
- 继续清退旧 retained widget 作者入口，避免新页面继续回到旧 `Widget` 组合模式。

## 主目标防丢失

- 后续长期会话在压缩上下文后，应优先回到“CSS 动画语义 MVP”这条主线。
- 除非用户明确切换目标，下一批开发不应再以“继续补完整 inline formatting”作为默认第一任务。
- 如果 inline、effect chain 或 dirty cache 的问题会直接阻塞动画 MVP，可以做最小必要修复；否则应记录为后续项。
- 动画 MVP 的可见验收必须落在 `HTML-like Smoke` 或等价诊断页中，避免只停留在底层测试。

## 待添加基础建设

### 0. CSS transition / animation 语义 MVP

- 已有基础：样式层已支持 transition property、duration、delay 与 timing function 的 Java API 表达；动画 timeline 已支持 transition 覆盖层，当前覆盖 background/border/text color、opacity、border-radius 与 backdrop blur radius。
- 首批 effect-affecting 动画已开始：动画属性新增 paint/effect/layout 影响范围分类，`backdropBlurRadius` 作为长度类 effect 属性接入 transition，绘制阶段会使用运行态 blur 半径；即使目标 blur 变为 0，transition 期间也会继续输出 backdrop 命令，避免效果直接跳断。
- 下一步应把动画从“少量 transition demo”推进到更明确的 CSS-like 动态语义层：统一 transition/animation 声明解析/存储、动画属性注册、插值值类型、timeline tick、动画生命周期、完成清理和 style invalidation 边界。当前已补充运行中 transition 的按元素/属性查询，并用它保证 backdrop blur 退场期间即使运行半径已归零，也会保留 effect command 到动画清理阶段。
- 首期 transition 优先属性已覆盖 `opacity`、background/border/text color、border-radius、backdrop blur radius，以及受控 layout-affecting 的 `WIDTH/HEIGHT/MARGIN_LEFT/MARGIN_RIGHT/PADDING_LEFT/PADDING_RIGHT`。当前已固定 transition 结束后回到静态 paint command 缓存路径，paint/effect 动画不应触发文本测量和 layout；layout 动画允许通过运行态布局值重排，并在结束后恢复静态缓存。layout 动画期间绘制、hit-test、滚动交互、焦点遍历和端到端点击分发都应使用同一份运行态布局几何。Smoke 页已增加可见动画诊断行，标明 pill 和 glass 探针覆盖的 transition 属性、时长与缓存恢复预期；layout 动画区还会显示当前 layout 覆盖属性清单、active 总数、transition/keyframe/fill 来源计数与 paint/effect/layout 分 impact 状态。下一步不要继续堆静态属性数量，应谨慎评估是否继续扩展 layout-affecting 属性面。
- transition 与 keyframe animation 的模型边界已开始明确：transition 处理 computed style 基准值变化；作者侧 keyframe animation MVP 已接入 `UiDocument.registerKeyframes(...)`、`animation-name`、duration、delay、iteration count、fill-mode 和 timing function，当前 keyframes 支持 paint/effect 属性以及受控 layout 数值属性 `WIDTH/HEIGHT/MARGIN_LEFT/MARGIN_RIGHT/PADDING_LEFT/PADDING_RIGHT` 的多段 stop 轨道并保留 from/to 便捷入口；时间线固定运行值优先级为 `transition > keyframe animation > computed style`。当前已固定 keyframe 声明变更重启条件：`animation-name`、keyframes 定义对象、duration、delay、iteration count、fill-mode 或 timing function 变化会重启，布局盒尺寸变化只刷新数值轨道 used value 归一化边界并保留进度；同名 keyframes 定义替换会重启引用元素并使用新轨道，定义移除会取消动画并清理旧 fill；forwards fill 结束后若作者修改同属性目标值，会按属性让作者值接管，未改动属性的 fill 可继续保留；layout keyframe 的 forwards fill 会继续作为运行态布局值参与 widget 重排，作者修改同属性目标、同属性 transition 接管、清除 animation 声明或移除 keyframes 定义后，应清理 layout 运行值并恢复作者布局；keyframe 结束或声明清除后 widget 会回到静态 paint cache。不应把布局动画一次性全量开放。
- keyframe 数值属性必须在动画层使用与最终绘制一致的 used value 空间；当前声明式 keyframes 启动时会把 opacity、border-radius 与 backdrop blur 先按运行约束归一化，避免 `999px` 胶囊圆角这类作者值在绘制阶段 clamp 后产生末尾跳变。
- keyframe fill 与 transition 交互必须避免旧末帧越权：当作者侧同属性目标值变化并触发 transition 后，应清除该属性旧 keyframe 运行/fill 覆盖，避免其他组件重绘时把视觉重新盖回 keyframe 末帧。
- 动画属性处理已完成一轮收敛：`DocumentAnimationProperty` 暴露 impact 与 color/float 值类型，`DocumentAnimationTimeline` 通过内部属性运行语义表集中处理 base value、px-only transition 判定和 keyframe used value 归一化，并通过单元素状态 helper 集中处理 transition/keyframe/fill 的存在性判断、完成计数和完成清理，`DocumentKeyframes` 复用属性值类型校验 color/float 轨道。后续新增属性应先补语义表项和测试，不再在多处追加分散白名单。
- 动画诊断已完成二期只读快照：`DocumentAnimationTimeline.DiagnosticsSnapshot` 可统计 transition、keyframe、forwards fill 三类来源，并按 `PAINT/EFFECT/LAYOUT` impact 切分；`HtmlLikeDocumentWidget.getAnimationDiagnosticsSnapshot()` 将其暴露给 Smoke 和测试。该快照不推进、不清理动画状态，不应被用作作者业务 API。
- layout-affecting 动画已建立底层运行值注入路径和 Smoke 可见验收：`DocumentAnimationProperty.WIDTH/HEIGHT/MARGIN_LEFT/MARGIN_RIGHT/PADDING_LEFT/PADDING_RIGHT` 会按 layout impact 分类，`DocumentLayoutEngine` 可通过 `LayoutRuntimeValueResolver` 注入运行态 content width/height、左右 margin 与左右 padding，`HtmlLikeDocumentWidget` 在 layout 动画活跃或存在 layout forwards fill 运行值时同帧重建运行态布局盒并刷新滚动范围；首期 transition 只支持 px-to-px，auto/% 不创建 width/height/margin/padding transition。当前已固定 height transition 过程中 `overflow:auto` 滚动范围会随运行态内容高度变化，内容收缩时已有滚动偏移会按运行态最大滚动范围夹取，而不是提前被目标静态布局夹到 0。Smoke 页 `Layout animation probe` 中，点击蓝色 `Layout card` 会在 92x34 与 190x58 间切换并推动右侧绿色 sibling；点击琥珀色 `Margin card` 会在 tight/wide 左右 margin 间切换并推动右侧棕色 sibling；点击紫色 `Padding card` 会在 tight/wide 左右 padding 间切换并推动右侧紫色 sibling，同时卡片内容随运行态 padding 位移；点击青色 `Keyframe card` 会启动 `layoutFillProbe` width keyframe，运行时 layout `k` 增加，结束后 forwards fill 让 layout `f` 保留，再次点击清除 animation 后 `f` 归零并恢复作者宽度；该区域会显示 layout 覆盖属性清单、active 总数、transition/keyframe/fill 来源计数与 paint/effect/layout 分 impact 状态，便于确认 transition/keyframe/fill 是否真的进入或退出 layout 运行态；`PADDING_LEFT/PADDING_RIGHT` 已完成游戏内 Smoke 验收，确认动画平滑、内容和 sibling 同步位移、视觉效果正确；layout `t/k/f` 游戏内诊断已确认进入与退出符合预期，当前动画 MVP 已覆盖 transition、keyframe、forwards fill 的可见诊断路径。
- 当前动画 MVP 进入收口阶段：暂停继续扩展 layout-affecting 属性面，下一批优先把“运行边界可验收”补齐到 Smoke 或测试诊断中，重点观察 paint/effect 动画不触发布局、layout 动画才重排、动画结束后回到静态缓存。
- 缓存与性能诊断可见性一期已接入：`HtmlLikeDocumentWidget.getPerformanceDiagnosticsSnapshot()` 暴露只读 `paintGen/staticLayout/runtimeLayout/textEpoch`，Smoke 页 `Layout animation probe` 显示 `Cache runtime` 行；纯 JVM 测试覆盖 paint/effect transition 只增长 paint cache、不增长 layout 计数，layout transition 会增长 runtime layout 计数，只剩 layout forwards fill 的稳态会复用 runtime layout 缓存，动画结束后重复绘制不再增长 paint/layout/text 测量相关计数。
- 动画 MVP 游戏内验收已收口通过：transition、keyframe、forwards fill、layout `t/k/f` 诊断、`Cache runtime` 缓存诊断、paint/effect 不增长 `runtimeLayout`、layout 动画增长 `runtimeLayout`、layout forwards fill 稳态缓存和清除恢复路径均已确认符合预期。短期不再继续补动画属性和诊断显示，下一阶段应优先转向真实页面/控件迁移验证。
- 真实页面/控件迁移验证已起步：`inventory_overview` 背包页新增真实迁移验证卡，组合可点击 transition 卡片、toggle 控件、overflow auto 文本、absolute 角标、fixed 提示和 inline pill，用于在非 Smoke 页面验证动画、控件、定位、滚动、inline 与缓存路径组合。
- 动画运行值不得写回作者侧 inline style；应继续作为 computed style 之上的运行时覆盖层，并能在动画结束后回到静态缓存路径。
- 验收目标：Smoke 页提供明确动画探针，能观察颜色、透明度、圆角和至少一个长度类属性的平滑变化，并能确认动画期间不触发不必要的整页重排或缓存失效。

### 1. 真正 group opacity / effect FBO 合成

- 首期已完成：`opacity < 1` 的 paint context 会把子树绘制到离屏层后按 group opacity 合成回父 framebuffer。
- 首期已覆盖：`CUSTOM` 内容可随子树一起参与真实 group opacity，标准颜色命令不再对当前 context 的 opacity 提前乘 alpha。
- 已接入 `UiRenderContext.pushPaintContext(...)` / `popPaintContext()`、clip snapshot 继承、`UiRenderTarget` 资源复用与 FBO 不可用降级。
- 已修复 `UiRenderTarget` 首次创建/resize 离屏 FBO 后错误恢复到 framebuffer 0 的状态泄漏；现在会恢复进入前 framebuffer、texture 2D 与 renderbuffer 绑定，避免 opacity context 首次进入时把 layer 合成到错误目标导致整屏闪烁。
- renderer 运行时 effect pass 已开始接入显式 effect chain：effect command 会携带 `DocumentEffectType`，renderer 按效果类型管理 paint context / overflow clip 栈与 stateless backdrop pass。
- 后续仍需扩展到更完整 effect chain，让后续 filter/effect、分块 snapshot、downsample/filter pass 与更多 stacking context 规则共享该离屏合成模型。

### 2. CSS-like stacking context 隔离

- 首期已完成：`opacity < 1`、positioned + 显式 `z-index`、`backdrop-filter` 等元素会形成独立 stacking context。
- 非 stacking context 祖先不会错误隔离 positioned 后代，高 z-index 后代可参与最近祖先 stacking context 排序。
- 子孙元素不会逃出父 stacking context 与外部 sibling 做全局乱序比较；绘制、命中测试与滚动命中已共享该语义。
- 当前 overflow clip 边界仍作为局部排序边界处理，后续可随 clip / effect chain 显式建模继续细化。

### 3. 可复用 UI 主层快照服务

- 首期已完成：`UiMainLayerSnapshotService` 会在同一帧内按 read framebuffer 与 UI 主层内容版本复用快照纹理。
- 已修正过度复用旧快照的问题：两次 backdrop 之间只要发生新的 UI 绘制写入，后一个 backdrop 必须重新捕获最新主层，避免 Glass Lab 嵌套玻璃采样到上一个场景的旧内容。
- 已统一 backdrop 采样区域扩张、128px block 对齐局部主层快照复制、较大 block 区域作为同帧临时 atlas 覆盖后续较小区域、多 tile atlas 组装、tile 覆盖计划诊断、按 blur 半径降采样并执行横向/纵向 separable blur 的 filter pass、采样尺寸保护和 snapshot 不可用时的 tint fallback 失败降级。
- `Large Glass Lab` 的 `Backdrop path` 诊断现在会显示 `snapshot=captured/reused WxH @x,y ... rev=... region=exact/block128/atlas-block128/tile-atlas-block128 tiles=N covered=M missing=K reused=R copied=C filter=raw/downsampleN+sepBlurR ...`，用于确认多 glass 场景是否复用快照、采样局部区域、采样版本、block 对齐或 atlas 覆盖状态、tile 覆盖/缺失状态、实际 tile 复用/复制数量以及 filter pass 是否降采样和预模糊。
- `Large Glass Lab` 底部保留就地 `Tile probe Backdrop path`，用于观察真实 `backdrop-filter` 绘制路径；临时 snapshot-only 探针已移除，避免把只读 snapshot 采样能力暴露成公开渲染 API。
- 后续可继续细化 multi tile atlas 的批量复制、tile source 合并、更完整 blur/filter pass 与 effect chain 协同。

### 4. dirty subtree 与细粒度缓存

- 当前已拆分 `layoutVersion` / `paintVersion`，但缓存粒度仍偏文档级。
- 后续应记录变更子树，让 layout、computed style refresh 与 paint command 只重算受影响分支。
- 该能力对输入框、列表、动态数据页和动画密集 UI 很关键。
- 在动画 MVP 阶段不要求一次性完成完整 dirty subtree 系统，但必须至少区分 paint-only 动画、layout-affecting 动画与 effect-affecting 动画，避免动画每帧无条件走最重路径。

### 5. `position: fixed` 与 viewport containing block

- 基于现有 absolute containing block 语义，新增 fixed 定位。
- fixed 元素应相对 `HtmlLikeDocumentWidget` viewport，而不是文档滚动内容。
- 目标场景包括固定工具栏、悬浮按钮、toast、modal 和调试 overlay。

### 6. absolute stretch 约束

- 首期已完成：`position:absolute/fixed` 元素在 `width:auto` 且 `left + right` 同时存在时会按 containing block 横向 stretch，在 `height:auto` 且 `top + bottom` 同时存在时会纵向 stretch。
- stretch 求解已进入 layout 层，会扣除 inset、margin、border 与 padding 后得到 content size，页面作者无需手写宽高计算。
- 当前覆盖绝对定位浮层铺满、右侧面板、底部栏与 fixed viewport 铺满类场景；更复杂 auto margin / over-constrained CSS 细节可后续补齐。

### 7. inline formatting 初版

- 首期已完成：block 容器包含 inline 子元素时，会启用最小 inline flow，支持 `text + span + text` 片段同一行混排并按可用宽度换行。
- 已继续补齐 inline 文本 fragment 的最小背景/边框绘制与命中测试：span 文本片段会保留所属元素身份，绘制阶段按 span 自身继承链解析 `textColor`、背景和边框，命中测试可把点击落到 span 本身。
- 已新增 layout 层 `DocumentLayoutInlineFragment` 几何：同一 inline 元素在同一行的相邻片段会合并，跨行内容会按行分片，父 inline fragment 会覆盖嵌套 inline 子内容。
- 已新增 inline box 边：inline 元素的左右 margin 参与行内流但不绘制，左右 border/padding 参与行内流；上下 border/padding 会扩展行高并让同一行文本共享对齐后的内容 top，border/padding 表面会参与 fragment 背景/边框绘制和命中区域。
- 已新增 inline fragment 跨行圆角细节：跨行 inline 元素的首片只保留左侧圆角，末片只保留右侧圆角，单行片段仍保留四角圆角。
- 已新增 `vertical-align` 初版：inline 元素可声明 `baseline/top/middle/bottom`，布局阶段会按行盒高度移动文本 run 与 fragment 表面，Smoke 页提供 `Vertical-align probe` 观察 rail/top/mid/bot pill 的垂直位置。
- 后续仍可补更完整 inline box 尺寸、更接近字体真实基线的 baseline 细节和图文组合。
- 但当前 inline 不再作为默认第一优先级；只有当动画探针、真实页面迁移或控件展示被 inline 缺陷直接阻塞时，才优先做最小收口。

### 8. clip / effect chain 显式建模

- 首期已完成：新增内部 `DocumentEffectChain` / `DocumentEffectType`，集中表达 paint context、backdrop-filter 与 overflow clip 的效果顺序和 stacking boundary 判定。
- 已接入 `DocumentPaintEngine`、`DocumentHitTestEngine` 与 `DocumentScrollState`，让绘制命令、命中测试和滚动命中复用同一套 overflow clip / effect boundary 语义。
- renderer 运行时 effect pass 已开始复用该模型：`DocumentPaintCommand` 的 effect command 会携带 `DocumentEffectType`，`DocumentPaintRenderer` 按效果类型回放 paint context、overflow clip 和 backdrop pass。
- 把 overflow clip、rounded clip、paint context、backdrop/effect 的作用顺序整理成显式模型。
- 避免后续 FBO 合成、圆角裁剪、backdrop 采样和滚动组合时出现隐性顺序 bug。
- 局部 snapshot 已开始接入：backdrop 只复制扩张后的采样区域，不再为每个元素复制整屏主层。
- downsample filter pass 已开始接入：大 blur 会把局部 snapshot 进一步缩到较小纹理并执行横向/纵向 separable blur，再供 shader/fixed path 采样，降低大面积玻璃的纹理采样成本并改善大半径 blur 质量。
- 分块 snapshot 已开始接入：采样区域会先对齐到 128px block 边界，使相近 glass 元素在同内容版本和同 blur 级别下更容易复用同一 snapshot/filter 结果；首个 atlas 切片已允许同帧已捕获的较大 block 区域覆盖后续较小 block 区域；当前 multi tile atlas 会把已覆盖 tile 从既有 snapshot 源纹理组装到新 atlas，缺失 tile 才从 read framebuffer 复制，诊断会暴露 `tiles=N covered=M missing=K reused=R copied=C` 方便游戏内观察 tile 覆盖计划和实际复用/复制数量。
- 后续仍需继续优化 multi tile atlas 批量路径、更完整 blur/filter pass 与更多 filter/effect 接入该模型。

## 已完成清理

- 已删除旧页面宿主链路：`UiTestScreen`、`InventoryOverviewScreen`、`ControllerBackedDocumentScreen`、`BaseDocumentScreen`。
- 已删除旧页面壳适配层：`DocumentPageAuthoringSurface.adapt(DocumentPageWidget)`、`DocumentPageWidgetAuthoringAdapter`、`DocumentPageWidget`。
- 已收缩 `DocumentUiScope`，只保留 `getTextMeasureService()` 与 `getRuntimeAdapters()`，不再暴露旧 retained widget factory 或兼容主题。
- 已删除旧 retained 文档控件：`DocumentTextWidget`、`DocumentCardWidget`、`DocumentToolbarWidget`、`DocumentFlowRowWidget`、`DocumentSectionWidget`、`DocumentFormRowWidget`。
- 已删除旧 retained control widget：`ButtonWidget`、`ToggleSwitchWidget`、`SegmentedSelectorWidget`、`TextInputWidget`、`InventorySlotGridWidget`、`LabelWidget`。
- 已删除旧兼容容器和滚动壳：`DivWidget`、`ScrollViewportWidget`、`RoundedScrollViewportWidget`、`OverflowScrollState`、`OverflowViewportLayout`、`UiScrollHost`。
- 已删除旧 retained 控件样式主题：`UiControlTheme`、`UiDocumentTheme`、`UiDocumentThemes`，`DocumentUiScope` 仅保留文本测量与运行时适配器。
- 已完成定向命名迁移：背包格子底层能力迁到 `ui.inventory`，运行时适配器改为 `ui.runtime.UiRuntimeAdapters`，`ViewportWidget` 迁到 `ui.widget`。

## 已完成添加

- 已完成首期 group opacity FBO 合成：opacity paint context 使用离屏 `UiRenderTarget`、clip snapshot 继承、资源复用和命令级 alpha 降级；`CUSTOM` 可参与整体透明度。
- 已在 `HTML-like Smoke` 页底部增加 group opacity 可观测 probe，用重叠红/蓝块验证真实 group opacity 与命令级 alpha 降级的视觉差异。
- 已完成首期 CSS-like stacking context 隔离：绘制、hit-test 与滚动命中按最近 stacking context 排序，Smoke 页底部提供红/蓝覆盖 probe。
- 已完成首期可复用 UI 主层快照服务：backdrop-filter 同帧按内容版本复用局部主层 snapshot，并在 Glass Lab 诊断中暴露 captured/reused、采样区域和 rev 状态。
- 已完成首期 clip / effect chain 显式建模：`DocumentEffectChain` 统一 paint、hit-test 与滚动命中的 overflow clip、backdrop 和 stacking boundary 判定。
- 已完成 renderer 运行时 effect pass 的首期显式类型接入：paint context、overflow clip 与 backdrop pass 通过 `DocumentEffectType` 回放。
- 已完成首期 downsample + separable blur filter pass：大半径 backdrop 会在局部主层 snapshot 后生成降采样纹理，并通过横向/纵向 filter pass 预模糊；shader 半径和 mip lod 会按降采样倍率折算，FBO 不可用时回退到 raw snapshot。
- 已完成首期 block-aligned snapshot 复用：局部采样区域会扩展到 128px block 边界，诊断显示 `region=block128`，相近 glass 元素可在同内容版本下复用同一块 snapshot。
- 已完成首个 atlas 覆盖复用切片：同帧已捕获的较大 block 区域可覆盖后续较小 block 区域，诊断显示 `region=atlas-block128`。
- 已完成首期 tile 覆盖计划诊断与游戏内可观测 probe：`Backdrop path` 现在显示 `tiles=N covered=M missing=K reused=R copied=C`，Glass Lab 底部新增 `Tile atlas probe / block128 tile diagnostics` 和就地 `Tile probe Backdrop path`。
- 已完成首期 multi tile atlas 组装：当同帧同 read framebuffer、内容版本、blur 级别下已有 snapshot 覆盖部分 tile 时，新 snapshot 会把可复用 tile 从既有源纹理绘入 atlas，缺失 tile 才从当前 read framebuffer 复制，诊断显示 `region=tile-atlas-block128`。
- 已移除临时 snapshot-only tile atlas 探针：multi tile atlas 仍保留在底层 snapshot 服务中，但游戏内只通过真实 `backdrop-filter` 绘制路径观察，不再提供只读 snapshot 诊断 API。
- 已完成首期 `position: fixed` 与 viewport containing block：fixed 元素相对 `HtmlLikeDocumentWidget` 视口脱流定位，不随根滚动内容移动，并已覆盖绘制、命中测试、滚动命中与 Smoke 页 `FIXED viewport probe` 可视探针。
- 已完成首期 absolute stretch 约束：absolute/fixed 元素在 auto 尺寸加双侧 inset 时会按 containing block stretch，并覆盖 margin/border/padding 扣减。
- 已完成首期 inline formatting：包含 inline 子元素的 block 会将直接文本与 span 文本按同一 inline flow 排版，span 文本使用自身 `textColor`，Smoke 页新增 `Absolute stretch + inline span probe`。
- 已重排 `HTML-like Smoke` 页：固定探针移到右下避免遮挡标题，交互控件集中到 `Controls probe` 区，布局、backdrop、absolute stretch、inline span、group opacity 与 stacking context 探针分区展示，便于逐项测试。
- 已补齐 inline span fragment 背景/边框绘制与命中测试，Smoke 页 `amber span hit` 可点击验证 inline 命中和 fragment 表面绘制。
- 已为 inline fragment 补 layout 层几何模型：跨行 span 会输出逐行 fragment，同一行相邻片段会合并，嵌套 inline 场景中父 span 背景可覆盖子 span 文本范围。
- 已为 inline fragment 补 box 边占位、绘制与命中：Smoke 页 `amber span hit` 已加入水平 margin 与上下左右 padding，用于观察 inline pill 表面、行高对齐与命中范围。
- 已为 inline fragment 补跨行圆角切片：底层 surface 支持内部局部圆角掩码，跨行 inline fragment 首片只画左侧圆角、末片只画右侧圆角，避免每行 fragment 都画成独立四角 pill；Smoke 页 inline probe 已限制行宽，让 `amber span hit` 自身跨行，便于游戏内验收。
- 已为 inline formatting 补 `vertical-align` 初版：作者侧样式新增 `UiVerticalAlign`，inline 行盒支持 top/middle/bottom 对齐，Smoke 页 `Vertical-align probe` 可观察同一行内 rail/top/mid/bot pill 的垂直对齐结果。
- 已为 CSS 动画语义 MVP 增加首批 effect-affecting 长度类 transition：`DocumentAnimationProperty` 暴露 paint/effect/layout 分类，`BACKDROP_BLUR_RADIUS` 可动画，Smoke 页玻璃卡片可点击观察 blur 4/22px 之间的平滑过渡；widget 层已用 paint cache generation 诊断固定动画期间重建 paint commands、结束后恢复静态 paint cache，且 paint/effect 动画不重新文本测量布局；Smoke 控制区已增加 `Animation diagnostics` 可见说明，避免验收者只靠源码或底层测试判断动画覆盖面；时间线已建立 keyframe animation 内部覆盖层并固定 `transition > keyframe > computed style` 运行值优先级。
- 已完成作者侧 keyframe animation MVP：新增 `DocumentKeyframes` 与 `DocumentAnimationFillMode`，`UiDocument` 可注册命名 keyframes，样式层支持 `animation-name`、duration、delay、iteration count、fill-mode 与 timing function；`DocumentAnimationTimeline` 会从 computed style 自动启动/取消命名 keyframes 覆盖层，支持 none/backwards/forwards/both 填充模式与有限迭代，当前覆盖 paint/effect 属性以及受控 layout 数值属性 `WIDTH/HEIGHT`；`DocumentKeyframes` 已支持 0%/50%/100% 这类多段 stop 轨道，且声明变更会重启 keyframe、布局尺寸变化只刷新 used value 归一化不重启进度、keyframes 定义替换/移除会分别重启或取消引用元素；forwards fill 后作者同属性目标变化时，该属性会回到作者值，未改动属性 fill 可继续保留；widget 层已固定 keyframe 结束、声明清除或 keyframes 定义移除后恢复静态 paint cache。Smoke 页首个 click pill 使用 `animation-name=smokePulse` 展示绿色胶囊、中段亮绿色胶囊、紫色小圆角三段 background-color + border-radius 动画，并提供 `Keyframe diagnostics` 可见诊断；自动 keyframe 和点击 transition 都不再混入 opacity，Smoke 页另有独立 `Opacity FBO probe`，同时覆盖点击 opacity transition、页面进入后的初始 opacity keyframe，以及同一元素同时具备初始三段 opacity keyframe 和点击 opacity transition 的组合路径，用于验收 opacity paint context 首次进入和后续点击切换是否稳定。
- 已完成首批 layout-affecting transition 与 layout keyframe 底层准备：`WIDTH/HEIGHT/MARGIN_LEFT/MARGIN_RIGHT/PADDING_LEFT/PADDING_RIGHT` 已进入动画属性表，timeline 支持 px-to-px width/height/margin/padding transition 和 width/height/margin/padding keyframe 运行值，布局引擎支持运行态 content width/height、左右 margin 与左右 padding 注入，widget 在 layout 动画活跃或存在 forwards fill 布局运行值时会重建运行态布局并让 sibling 几何、absolute/fixed 子树命中、overflow 滚动范围随动画或 fill 变化，作者修改同属性目标、同属性 transition 接管、清除 animation 声明或移除 keyframes 定义后恢复作者布局值；Smoke 页 `Layout animation probe` 可点击观察蓝色卡片宽高平滑变化、琥珀色卡片左右 margin 平滑变化、紫色卡片左右 padding 平滑变化、青色卡片 layout keyframe/forwards fill 和右侧 sibling 被推开，并可通过动态诊断行观察 active 总数、transition/keyframe/fill 来源计数与 paint/effect/layout 分 impact 状态；纯 JVM 测试已覆盖 px-to-px 插值、auto/% 不建 transition、delay/timing/fill-mode 组合、layout 动画重排、height keyframe 重排、margin keyframe 重排、padding keyframe 重排、layout 动画 hit-test 运行态几何、absolute/fixed 组合命中、端到端 click 运行态目标与 down/up 目标一致性、layout keyframe forwards fill 重排、layout keyframe 声明清除/定义移除/定义替换/fill 接管/transition 接管边界、滚动范围更新、收缩动画滚动夹取、缓存恢复、layout runtime 诊断状态、动画诊断快照、layout keyframe/fill Smoke 可触发探针和 Smoke probe 集成；动画属性元数据和值解析已收敛到 impact/valueType 与 timeline 属性运行语义表。

## 待清理旧代码

### 第一批：继续清点旧兼容残留

- 继续通过全局引用搜索确认是否还有只服务旧 retained widget 作者模式的 public 类、测试夹具或主题值对象。
- 若发现旧兼容入口没有 HTML-like 后端复用价值，应优先删除而不是继续 internal 化。
- 清理时仍需保留 Minecraft 宿主会话、渲染、输入与背包底层适配能力。

### 第二批：底层能力命名收缩

- `ui.control` 包已清空，背包格子布局、物品渲染适配器和 runtime adapters 已迁移到更中性的 `ui.inventory` / `ui.runtime` 包名。
- `ViewportWidget` 仍作为 `UiScreenHostSession` 根视口容器保留，当前位于 `ui.widget` 后端包；若后续 HTML-like 后端能直接挂到 root widget，可再评估是否收缩。
- `UiSurfaceStyle` 仍是底层绘制外观值对象，暂不删除。

### 暂不清理

- `Widget`：仍是 retained backend 根抽象。
- `ViewportWidget`：仍是 screen host 根视口容器，已迁到 `ui.widget`。
- `UiInputRouter`：仍负责宿主输入分发。
- `UiRenderContext`：仍是渲染后端与效果服务入口。

### 必须保留的底层能力

- `InventorySlotGridLayout`、`InventorySlotGridItemRenderer`、`InventorySlotSnapshot` 这类可复用底层能力暂不删除，因为 HTML-like 背包格子控件仍可复用。
- `UiRuntimeAdapters` 暂时保留，因为 HTML-like 背包格子控件仍通过它取得宿主物品绘制适配器。
- `MinecraftInventorySlotGridItemRenderer` 暂时保留，因为它是背包格子物品绘制的 Minecraft 宿主适配器。

## 建议执行顺序

1. 先暂停继续扩展动画属性和动画诊断显示：当前 `WIDTH/HEIGHT/MARGIN_LEFT/MARGIN_RIGHT/PADDING_LEFT/PADDING_RIGHT` 的 transition/keyframe/forwards fill、缓存、滚动、hit-test、fixed/absolute 和端到端 click 边界已完成首轮收口，且 layout `t/k/f` 与 `Cache runtime` 已完成游戏内 Smoke 诊断验收；后续新增属性必须重新证明必要性，并继续限制在少量可控属性与明确 fallback。
2. 下一批继续围绕真实页面/控件迁移验证：优先在 `inventory_overview` 背包页验收真实迁移验证卡，也可以再选择一个非 Smoke 小页面承载当前 HTML-like 动画、按钮/输入/开关、overflow 滚动、fixed/absolute、inline 与缓存路径，验证真实组合而不是继续扩展探针。
3. 迁移验证发现的阻塞问题优先最小修复：明显错误的 inline box 尺寸、baseline 偏差、必要图文混排、控件状态样式、焦点遍历或滚动/命中组合问题优先处理；非阻塞细节后置。
4. 若真实页面验证暴露性能问题，再进入 dirty subtree / 细粒度缓存，减少文档级 layout、computed style refresh 与 paint command 重算。
5. 继续扩展完整 effect chain 的优先级后置于真实页面验证，把 multi tile snapshot atlas 的批量复制、tile source 合并、更完整 blur/filter pass 与后续 filter/effect 接入当前显式模型。
6. 持续清点旧兼容残留，发现无 HTML-like 复用价值的入口直接删除。

## 验收要求

- 每批清理都必须保持 `test` 与 `compileJava` 通过。
- 动画 MVP 必须至少包含纯 JVM 测试和游戏内 Smoke 可见探针；测试应覆盖 transition 创建、插值、delay/duration/timing、声明关闭后的运行中取消、按属性运行状态查询、effect 退场命令保留、结束清理、paint/effect 不重排、layout 动画运行态重排、缓存恢复，以及 keyframe animation 的最小生命周期、多段 stop 插值、delay/timing/iteration/fill-mode、声明清除取消和作者侧 Smoke 可见探针。
- 删除旧入口前必须确认 `UiDocumentScreens` 仍能创建诊断菜单、布局诊断页、Smoke 页、Glass 页和背包页。
- 清理旧 retained widget 时不能删除 HTML-like 控件仍在复用的底层算法与渲染适配能力。
- 新增底层能力不得把 FBO、OpenGL、shader、stencil 等宿主细节暴露给文档作者层。
