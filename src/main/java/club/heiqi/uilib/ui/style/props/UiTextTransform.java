package club.heiqi.uilib.ui.style.props;

/**
 * HTML-like 文本大小写变换。
 */
public enum UiTextTransform {
    /** 不做大小写变换。 */
    NONE,
    /** 将可见文本转为大写。 */
    UPPERCASE,
    /** 将可见文本转为小写。 */
    LOWERCASE,
    /** 将每个词的首个字母转为大写。 */
    CAPITALIZE
}
