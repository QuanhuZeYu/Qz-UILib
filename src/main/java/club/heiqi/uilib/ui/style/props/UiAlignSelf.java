package club.heiqi.uilib.ui.style.props;

/**
 * HTML-like flex item 交叉轴对齐方式（覆盖父容器 align-items）。
 */
public enum UiAlignSelf {
    /** 跟随父容器 align-items（默认）。 */
    AUTO,
    START,
    CENTER,
    END,
    STRETCH,
    BASELINE
}
