package club.heiqi.uilib.ui.style.props;

/**
 * HTML-like flex 换行行为。
 */
public enum UiFlexWrap {
    /** 不换行（默认）：所有 flex item 排在一行/列内。 */
    NOWRAP,
    /** 换行：主轴空间不足时将 item 换到下一行/列。 */
    WRAP
}
