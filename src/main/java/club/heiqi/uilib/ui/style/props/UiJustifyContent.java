package club.heiqi.uilib.ui.style.props;

/**
 * HTML-like flex 主轴分布方式。
 */
public enum UiJustifyContent {
    START,
    CENTER,
    END,
    SPACE_BETWEEN,
    /** 每个 item 两侧各留相等间距（首尾各留半个间距）。 */
    SPACE_AROUND,
    /** 每个 item 之间及首尾各留相等间距。 */
    SPACE_EVENLY
}
