# Qz-UILib 4.5.3-beta-8

## 更新

- 结构化成员的 `List<String>` 编辑区改为 member label 右侧唯一的 grow column，raw 列表与可选 picker 纵向排列，避免两个填充控件在窄宽下横排裁剪。
- 无 picker 时仍使用同一 column；`List<CHOICE>` 保持原有装配。

## 兼容性与升级动作

- 升级动作：无需手工动作；本标签为 beta 预发布，不改变 schema 或既有字段语义。

<details>
<summary>验证边界</summary>

- 未验证：本版未留存专项验证结论，构建与真机运行态均未记录。

</details>
