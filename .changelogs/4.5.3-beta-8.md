# 4.5.3-beta-8

结构化成员的 `List<String>` 编辑区改为 member label 右侧唯一的 grow column，raw 列表与可选
picker 纵向排列，避免两个填充控件在窄宽下横排裁剪。无 picker 时仍使用同一 column，
`List<CHOICE>` 保持原有装配。

本次仅发布 Maven Local，不执行 merge、push、tag 或 release。
