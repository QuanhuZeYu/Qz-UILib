# 4.5.3-beta-7

搜索选择器以无状态 codec 按当前受控值写回，补齐领域文案与分阶段错误反馈；结构化列表
同时保留 raw member 与 picker，行标题按稳定 identity 显示。codec 异常或 null 均不擦除 Draft。

SceneScrollbar 固定占用 barWidth；无 overflow 时 track/thumb 透明且输入早退，避免配置页多帧布局
因滚动条宽度变化重复进入 ROW grow 诊断条件。

SearchPicker、ValueEditorProvider 与 ConfigUI editor registry 仍为 beta API，不属于 LTS 稳定承诺。
本次仅发布 Maven Local，不执行 merge、push、tag 或 release。
