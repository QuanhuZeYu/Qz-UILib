# 踩坑记录

按时间倒序。记录跨里程碑保留价值的开发踩坑（运行时错误仍按惯例入 `docs/反馈层/errors/`）。

## 2026-09 长 URL 断行后半截不是链接：语义作用域、同形断行、代理判据

真机现象：GTNH 欢迎语里
`https://github.com/GTNewHorizons/GT-New-Horizons-odpack/issues` 被硬断成两行，前半截是链接
（实测 hover 色 #9CCBF8 + 一条 326px 下划线），后半截 `odpack/issues` 渲染成 §6 金色 #E49902、
无下划线、不可命中。

1. **语义作用域必须等于语义对象的作用域**：`linkify` 跑在**每个显示行**上
   （`parseCached(renderLine, …)`），而 URL 是**整条消息**的语义对象。断行是排版事件，
   不该改变语义归属——续行不含 scheme 前缀，`indexOfScheme` 必然不命中，于是后半截"不是链接"。
   症状不是"颜色不对"而是"语义随排版漂移"。

2. **两种断行在行文本上同形，判据只能来自权威层**：词边界回退会 `trimTrailing` 丢掉断点空白，
   于是「上一行以完整 URL 结尾 + 下一行以 URL 字符开头」与「URL 内部字符硬断」从字符串上
   **完全无法区分**（重测宽度也不行：两种断行下 `line+nextChar` 都超宽）。只能让
   `ChatLineLayouter` 在切分时上报每行的断行来源（`LineFragment.continuesWord`）。
   这是"1 权威 + N 处重实现"反模式的又一次现身：下游想反推上游已经知道的事。

3. **我推断"误接结构性不可能"，写测试时立刻被自己的反例打破**：理由是"词边界回退总是整词
   移走，URL 不可能结尾+续行新词"。反例 `"hello world"`（5 字符宽）：词间空格此刻还在
   `pendingSpaces` 里、**尚未并入 current**，所以 `lastWhitespace(current) = -1`，走的正是
   字符硬断分支。教训：**"某种错误不可能发生"必须靠构造反例证伪，推理不算证据。**

4. **第一个修正判据仍然是错的**：改成 `pendingSpaces.length() == 0` 后，不变量测试又抓到
   `§6␣␣␣https://…` 超窄行——前导缩进会长期滞留在 `pendingSpaces` 里而与断点无关，
   断点实际落在 `h|ttps` 之间。教训：**临时代理变量 ≠ 它想表达的性质**；判据要直接对账
   源文本（`hasWhitespaceIn(text, lastVisibleAt + 1, i)`），别拿"离事实最近的中间状态"凑。

5. **不变量测试完胜手算夹具**：最初写了 3 条靠手算行数撑起来的断言，其中 2 条**前提就算错**
   （`MAX_WIDTH=20` 下 `"aaa http://a.co world"` 是 5 行不是 3 行）。换成一条
   「`continuesWord` ⟺ 断点无空白」对账 11 个语料的测试后，它当场抓出第 4 条真缺陷。
   以后写布局类断言，先写对账不变量，再写具体样例。

6. **撤回一条误报**：我先前断言"点击前半截会打开残缺地址"。不成立——本仓点击链是
   `ChatInputSurface.handleLineClick` → 原版 `ChatStyle.getChatClickEvent()` OPEN_URL，
   `LinkSpan.url` 只喂 hover tooltip。真实缺陷是三个（续行不是链接 / URL 中途变色断裂 /
   tooltip 显示半截），不是四个。教训与"日志前缀不是归属"同源：**我以为的下游消费者
   不是消费者，调用链两端都要查。**

7. **改段流形状前先确认首段是不是正文**：列表行的段流是 `[「• 」前缀段, …内容段]`，
   续链按可见字符数从头吞并会把项目符号划进 URL。已加 `bulletSegment == null` 保守闸门。

8. **段流节点不产链接命中区，回填点因此唯一**：`ScenePaintEngine` 的 `LinkHitRegion` 投影在
   `text != null` 分支内，而段流分支提前 return；chat3 行节点走 `setSegments` → 唯一的 URL
   消费端就是 `LinkSpan`。所以"链闭合时回填 `LinkSpan.url`"是充分落点，不必同步改段流
   （段流的 link 值只被当 null/非 null 用）。这类"要不要多处同步"的问题，答案在消费端清单里。

9. **反向对照两次都按名字红了**：`continuesWord = true` → `continuesWordMustEqual…` +
   `shouldNotMarkLineAfterPendingSpaceHardBreak` 失败；`continuesWord = false`（闸门失效）→
   `urlBrokenAcrossDisplayLinesStaysOneLinkWithCompleteUrl` 失败。新增 15 条测试，
   全量 `tests=3779 failures=0`。


10. **tooltip 的存在意义是揭示被截断的内容，别再截一次**：`TextEllipsizer.wrapLines` 对
    「无折行机会的超宽词」的历史语义是 ellipsize —— 链接 tooltip 走的正是这条路，于是气泡
    截了一半、tooltip 再截一半，用户什么也没看到。新增 `breakLongWords`（默认 false 保旧
    行为，聊天链接 tooltip 传 true）按码点贪心切开。
    附带两个必须处理的边界：`longestFitEnd` 按**码点**计数二分（按 char 会切出孤立代理对），
    且单码点超宽时至少推进一个码点（否则切分循环不终止）；切开的最后一段要留在当前行，
    后续词才能续排，否则白占一行。

11. **hover 的响应单位应该是语义对象，不是排版单元**：长 URL 断成两行后只点亮命中那一行，
    用户口径「选中的一整条链接都应该变色，目前跨行会断」。修法是 `markLinesOfUrl`：
    命中后把本消息内所有 `url` 相等的行一起置 hover。
    **不需要新增任何链状态** —— 上一轮链闭合时已把各行 `LinkSpan.url` 回填成同一完整地址，
    「同一链接」的判据当场就是 url 相等。修好上游的数据模型，下游的联动常常是免费的。

12. **撤回一处臆断的"顺手修"**：我先把 `AnchoredPortalLayout` 的 preferredWidth 从硬编码
    260 改成跟随 `props.maxWidthPx()`，理由是"260 会裁掉 320 的换行宽"。用户截图里 tooltip
    实测 333px 宽 > 260 —— 该值根本不硬裁，改动已回退。
    **推断出来的缺陷必须有直接证据**；"读代码觉得会裁"不是证据，而这次证据就在手边的截图里。

13. **测试夹具的度量模型要先读懂再写断言**：`TextEllipsizerTest` 的 `px(n)` 是「n 个字符
    的宽度」(= n×8px)，我按「n 像素」写了 `px(4)` 想构造"连一个字符都放不下"，实际得到
    32px 反而宽到能放下整词 —— 断言直接空转通过。改为 `s -> s.length() * 8` + 字面量 4。

14. **封顶值压过了它要保护的比例（用户拍板）**：`chatWidthMaxPx = 360` 配 `ratio = 0.25`，
    而视口喂的是 `mc.displayWidth`（**物理像素、不除 guiScale**）→ 1440p 窗口 2559 宽下
    1/4 = 640 被掐成 360，面板只占 14%，静默覆盖了用户自定的「约 1/4 随窗口缩放」。
    教训：**一个"兜底上限"必须检查它在主流分辨率上是否已经变成主约束**；360 的初衷是防
    4K 无限拉长，却在 1440p 先接管了。现改 640，并锁 `chatWidthFor(2559) == 640`
    （比例生效）与 `chatWidthFor(3840) == 640`（仅 4K 兜底）两条断言。

## 2026-09 气泡文字偏下与液态看不出：命名说谎的锚点、缘带饱和、日志归属

1. **方法名里的 "Baseline" 不代表那个参数是基线**：`UiRenderContext.drawSegments` 把 paint 层
   交来的 em-box 顶又加了一次 `getAscent(fontSize)` 才传给字形批，而字形批的 y 参数**本来就是
   字格顶**——quad 生成时内部按 `lineBaselineY × baselineScale` 换算到基线。ascent 被算两遍，
   13px 字号整体下沉 8px，气泡文字贴到下缘。自证证据有两处：`drawPreparedTextIntoCollector`
   里"装饰线通道无此换算、必须手动补基线偏移"的注释，以及 `collectDecorations` 按下划线
   `drawY + charSize` 取底边（顶锚才成立）。**治理：源码锁** `UiSegmentsBaselineAnchorTest`
   （方法体不得出现 getAscent）+ 文档锁（@param y 必须写"字格顶"）——复发入口是命名，
   所以文档本身就是被锁对象。
2. **同一锚点的第二处重实现走样，而 TEXT 路径一直是对的**：`drawTextResolved` 原样透传，
   只有 SEGMENTS 加了 ascent。"1 权威 + N 处重实现"在横轴（坐标）之后又应验在**纵轴**。
3. **headless 测试对锚点换算天然不可见**：`RecordingRenderBackend.drawSegments` 只记录命令
   坐标、不做 em-box→基线换算，所以 3700+ 个测试全绿也拦不住这 8px。补了两道产出级锁：
   端到端复刻气泡几何断言"em-box 上下留白差 ≤1px"（`ChatBubbleTextVerticalCenterTest`），
   以及上面那道源码级锁。**教训：渲染链上任何"坐标语义转换"环节，若只有真机能看见，就必须
   要么加源码锁、要么加一个跑到真实转换器的那层的测试，不能指望命令级录制测试兜底。**
4. **透明材质会让既有布局误差显形，别把它当成新功能的锅**：这 8px 在玻璃之前就存在，只是
   实心气泡看不见边界所以无人报。反过来说：**上玻璃是一次免费的布局审计**——边界一旦可见，
   所有 padding/基线误差都会立刻暴露。收到"上完 X 之后 Y 变丑了"的反馈，先查 Y 是不是本来
   就错，而不是回滚 X。
5. **对齐语义要配行框模型**：段流节点钉的行框高 = 字号 + 行距(5) > em-box(=字号)，
   `TextVerticalAlign.TOP` 会把整段行距留在文字**下方**，单行气泡净偏 2.5px。CENTER 才等价
   CSS half-leading 的上下均分。TOP 是重设计首版就写下的、无注释无理由的选择——**默认值
   被显式改回默认值同义的东西，往往是当年绕别的坑留下的**，改之前要查 git log 确认。
6. **缘带下限用固定像素会在小面板上饱和**：`clamp(shortHalf*0.35, 8.0, 28.0)` 在 28px 高的
   气泡上半高才 14，8px 缘带占掉 57%，lensBevel 在内部几乎恒 1。而**一致的平移在视觉上不可
   见**——整块面板同方向挪采样坐标等于没挪，透镜的辨识度 100% 来自位移的**梯度**。这和散光
   那次是同一条数学事实的两个方向（环旋转不变 ⇒ 旋转救不了径向分层；平移一致 ⇒ 位移救不了
   透镜感）。治理：下限降到 3px 并另加 `min(..., shortHalf*0.5)` 保证任何尺寸都留平坦中心。
7. **日志前缀不是归属证据**：首次进世界断线，日志里那行 `Qz vanilla transport client
   disconnected` 是 `VanillaMixinTransport` 的**观察者**在 DEBUG 转述断开原因；真正的栈是
   Baubles Expanded `PlayerHandler.getPlayerBaubles` 里 `HashMap.computeIfAbsent` 抛
   ConcurrentModificationException（第三方 mod 自身的非线程安全表）。判归属只能看堆栈里
   有没有自己的帧，不能看是谁打的这行日志。
### 同日追加：缘带「黑黑的、没有光泽」（第三轮真机反馈）

上一轮「加强液态」的四个参数改对了结构（缘带梯度终于看得见），却把观感推成了黑边。三处模型错误：

8. **把「厚度」表达成「朝材质 tint 加深」，对深色档等于把缘带涂黑**：`edgeTint` 沿缘带把
   `mix(color, materialTint.rgb, …)` 的 alpha 从 0.149 抬到 0.459，而 materialTint 是 0x181818。
   吸收型变暗只在**亮背景**上成立（光程长、吃掉得多）；暗背景上再叠近黑只是糊出一条脏黑边。
   治理：给 edgeTint 单独加 `thicknessGate = smoothstep(0.15, 0.55, backdropLuma)`——与
   whiteGate 复用同一个 `backdropLuma`，零新增 uniform。**玻璃缘的第一线索永远是镜面高光，
   吸收是次要线索**，我把它当成了主线索。
9. **跨用途复用参数没重标量级**：`edgeHighlight`（DARK 系 0.04~0.055）是为 1.5px 经典发丝边
   标定的，直接拿来撑 28px 宽的立体倒角差一个数量级——实测侧缘镜面 +4/255、同位置变暗
   -23/255，**比值 6:1，所以屏幕上只剩黑**。治理：液态档按透镜强度放大
   （`×(1+3.6·strength)`，经典档恒 1.0 倍）。教训：换一个视觉通道去撑同一个几何特征时，
   必须重新标定幅度——「参数存在」不等于「量级适配」。
10. **加强一个通道时必须同时核算同一位置上反向通道的净值**：上一轮同时抬高了 edgeTint 和
    缘光系数，就默认两个都会显现。实际它们作用在同一条 2px 缘带上，净值才是用户看到的。
    正确做法是先把净值算出来（这次预测 +12.9、实测 -12），**预测与实测反号就说明有一条
    通道根本没生效**，而不是「力度还不够」。

另两条配套教训：

- **纵向衰减只属于平面材质**：`mix(0.30, 1.0, 1-topUv)` 是 iOS 导航栏「亮边集中在顶缘」的
  写法；液态档模拟的是**立体倒角**，四条边都接光，再叠纵向衰减会把左右/下缘压到不可见。
  方向差异应由光源项 `dot(normal, lightDir)` 单独表达，不要两个机制叠乘。
- **半透明实心遮罩会给下面所有通道装衰减器**：气泡底色 0x8C（55%）乘在玻璃之上，把 shader
  算出的高光按 0.45 衰减。材质档自己已有 0.20 黑 tint 在压背景，实心层再叠 55% 属于双重
  遮罩。降到 0x73 后玻璃才透得出来。**上「玻璃」时底色 alpha 要跟着材质档一起重定，
  不能沿用实心档的值。**

**方法记录**：这次没有靠「看着调」。做法是把截图当数据——沿容器左缘取一条水平亮度剖面，
量到 71→47 的 23 单位下降、且暗带宽度恰好等于 `lensBandPx` 的 28px，于是立刻判定「变暗通道
生效、高光通道为 0」，再顺这条结论去查材质档数值。**观感缺陷只要能量化，就一律先量化。**

### 同日再追加：边缘「生硬」——参考实现的价值是机制，不是常数

第四轮真机反馈：「现在边缘比之前变得生硬了」。量化（实验室面板，圆角 8、lens 30%）：
轮廓处 1px 内亮度 42 -> 145，蓝通道 clip 到 255。**峰值正好压在物理轮廓上、且只有 2px 宽**
——那不是玻璃鼓起来，那是沿轮廓画了一条白线。

11. **「软」不是把高光调暗，而是把同一份能量摊开**。CPU 复算新旧公式（把 shader 表达式
    逐像素重放到 Python 里对照，不需要真机就能判定形态对不对）：
    | | 峰值 | 峰值所在 d | 轮廓上值 | 单px最大跳变 | 环带总能量 |
    |---|---|---|---|---|---|
    | 旧 | 83.5/255 | **0px** | 83.5 | 41.8 | 125 |
    | 新 | 33.4/255 | 2px | 20.8 | **7.9（软 5.3×）** | **186（更亮）** |
    新方案峰值更低但**总能量更高**——变软的同时光泽更强。上一轮把 1.5px 放宽到 2px 是
    方向性错误：要放宽的是一个数量级，而且必须把峰值往里挪。
12. **找参考的收益不在数值，在"你漏掉的机制"**。这次拿到一手参考
    （[WebGlass docs/specular.md + tokens.md](https://github.com/eirasmx/webglass)，
    SDF bezel + specular rim 的完整实现）后，发现我缺的是**两个结构件**而不是系数：
    - `--wg-specular-edge`（默认 0.05）：**高光峰值内移**，轮廓线上不是最亮点。这一条直接
      决定"描边感 vs 立体感"，而我完全没有这个概念。
    - `--wg-specular-back`（默认 0.20，iOS 预设 0.15）：**对向次高光**，恒锁在
      `light-angle + 180°`。真实玻璃背光侧那条弱反光我也没有——上一轮「黑黑的」有一半是它。
    教训：带着"我的模型对不对"去读参考，而不是带着"他调成了多少"。只抄常数会漏掉机制。
13. **高斯环带而不是 smoothstep 斜坡**：`exp(-4t²)` 天然双侧肩部、峰值可在任意位置；
    `1 - smoothstep(0, w, d)` 的峰值恒在 d=0（轮廓上），改宽度也改不掉描边感。
    形态选错时调参数是无效的——这条和散光那次（旋转救不了径向分层）是同一类：**先问
    这个函数族的不动点是什么，再调幅度**。

14. **参考文档自相矛盾时按几项取舍**：把默认光向挪到参考值时发现，WebGlass 的
    `--wg-light-angle` 数值是 **-55**、ASCII 示意图把光源箭头画在**右上 ↗**、而正文那句
    说明写的是 "upper-left" —— 三项里两项一致，正文是笔误。按数值+示意图取**右上 55°**，
    并把矛盾写进 javadoc。**"从网上找参考"不等于抄常数**：常数必须先在**自己的坐标系**
    里复算一遍（本 shader 的 y 轴向下，与参考的 0=右/-90=上 同构，可直接换算不需翻转；
    若参考用的是 y 轴向上，-55 就会镜像到左上）。
15. **顺带纠正了自己一处注释说谎**：旧默认向量 (0.32, -0.95) 实际是 **-71°（右上、近乎
    正上）**，而 javadoc 写着"退回左上"。注释与代码反号，且没有任何测试能发现——因为
    向量是手写常量。治理：改成 `DEFAULT_LIGHT_ANGLE_DEG = -55.0F` 并用 cos/sin 推导，
    守卫锁的是**"由角度推导"这个形式**而不是那对浮点数（`staticLightAngleAnchorsToReference`
    同时断言旧的 `0.32F` 不得复现）。锁推导方式而非锁数值：数值可以改，坐标系约定不能
    重新变成隐性知识。反向对照两条均按名拦下。

### 自我撤回：那条"疑似缺陷"是我没查完就写下的

16. **上一轮记的"磨玻璃 quad 未按圆角裁 alpha、大圆角会露方形边"是错的，特此撤回。**
    错因：我只读了绘制原语（`drawBackdropTextureQuad` 画矩形 + `gl_FragColor.a = blurred.a`）
    就下结论，**没检查调用侧的 clip 边界**。实际 `drawCurrentUiBackdropFilter` 绘制前做的是
    `context.pushClip(left, top, right, bottom, cornerRadii)`，`ResolvedCornerRadii` 一路走到
    `ClipStack` 的 `RoundedClipRegion` + `rebuildRoundedClipMask`（GL_STENCIL_TEST）。
    **圆角裁剪一直在，只是不在我以为的地方。**
    两条教训：① **裁剪/可见性这类问题必须在调用栈两端对账**（push/pop 配对、谁在哪层施加），
    盯着单个绘制函数永远看不出来；② 记"疑似缺陷"要标证据等级——我那条实质是"我没查完"，
    却写成"待真机确认的缺陷"还给了验证步骤，**白占用户一次真机往返**。给用户的验证请求，
    自己必须先把静态证据穷尽。

### 圆角定稿：聊天框 12 → 20，输入框按同心规则跟随

17. **圆角是液态倒角的载体**：容器 12px 在 320px 宽的面板上读作"几乎直角"。上玻璃前无感
    （实心面板看不见轮廓），上玻璃后轮廓第一次可见。缘带宽度按短边比例、镜面峰值内移
    0.35·band，**直角处这两项都退化**，所以半径不够大时镜面环带挂不住弧度。提到 20。
18. **同心不变量锁关系、不锁两个等值**：输入框圆角 = 容器圆角 − 输入区内缩(8) = 12。
    两个值都是独立 volatile 常量，各锁各的等值挡不住它们分别漂移；改容器忘了改输入框，
    两圈弧就不同心（圆角越大越明显）。治理：`inputRadiusStaysConcentricWithContainer`
    断言**差值**，并在 `ChatContainer.INPUT_AREA_PADDING_PX` 处放反向指针指回该测试。
    反向对照：输入框改回 8 → 该断言按名失败。

### 一处需要如实记录的既有抖动

19. `GlyphGenerationDispatcherReloadBarrierTest > promotionWaitsForConcurrentQueueSelection`
    在满套件下失败一次、单独跑 17/17 全过、下一次满套件又全过（本轮实测三次均复现该模式）。
    是字体 demand 调度里 ~100ms 闩式等待在 CPU 争用下的竞态，**与本轮改动无关**（diff 只碰
    chat3 四个文件）。记在这里是为了下次不再被它带偏去查不相干的东西。

## 2026-09 聊天上液态玻璃：装饰器穿透、命令重建丢字段、虚断言

1. **能力探测门面必须穿透装饰器链，否则 GUI scale != 1 时静默不干活**：聊天 HUD 宿主
   在 render 里做 `backend = backend.scaled(scale)`，MC 常态下 scale 是 2~3，于是门面
   里 `backend instanceof UiRenderContext` 直接 false —— 玻璃**整块不渲染、无异常无日志**。
   这类"包了一层装饰器就把能力探测打断"的坑，和坐标轴那次同源：判定不能只看最外层类型。
   治理：ScaledRenderBackend 暴露 package-private delegate()/scale()，门面循环下钻 +
   累计 scale 换算坐标与圆角；批次开关也走同一套穿透（否则 HUD 下批次静默不开）。
2. **给 PaintCommand 加字段必须同时改 translatedBy**：命令经 fragment 偏移重建，
   translatedBy 里漏传新字段，BACKDROP 命令平移后 backdrop 变 null —— 玻璃静默消失。
   新增字段清单要过一遍：构造器链、工厂、getter、equals、hashCode、toString、**translatedBy**。
   后者最易漏，因为它长得像"只改坐标"。
3. **虚断言：断言"树里存在满足条件的节点"会被无关节点满足**：容器与输入条都要挂玻璃，
   初版写成"深度优先找到任一带 backdrop 的节点"—— 容器自身必然满足，输入条漏接照样绿。
   改为逐节点精确断言后做反向对照（临时摘掉输入条挂载 -> 测试精确报出该断言）。
   教训：**新写的断言必须跑一次反向对照才知道它是不是活的**，尤其"存在性"型断言。
4. **改默认值会打破既有测试的原意，别改测试去迎合**：玻璃默认开使 7 个"设计令牌落到节点"
   的测试失败（它们锁的是非玻璃态等值）。正确处理不是把断言值改成玻璃档，而是让那 4 个
   测试类显式 setGlassEnabled(false) 保住原意，另建专项测试覆盖玻璃态；@After 复位，
   因为这是进程级 volatile static，泄漏会污染同 JVM 其它测试。
5. **动画每帧重烘焙会覆盖构造期设置**：气泡底色有两条写路（创建时 setBackgroundColor +
   每帧 bake() 按 hover/淡入重设），只在创建处改半透明会被下一帧覆盖回实心。必须改在
   **共同源头**（bubbleColor 局部变量）。输入条同理：SceneTextInput 内部每帧重烘焙背景，
   只能改在"注册晚于内部绑定"的那条覆盖绑定里。
6. **反直觉但有依据的约束**：聊天正文是浅色，白 tint 会把浅色文字一起洗白，必须打底
   DARK 系材质压暗背景才保对比度 —— 这既是可读性约束，也正是真机"DARK 更有苹果味"的
   成因。上一轮把它当口味偏好，这一轮它变成有意识的设计约束写进注释与测试断言。

## 2026-09 磨玻璃质感升级（iOS 材质档）：静默错值、GLSL 不编译、锚点消失

1. **漏设的 uniform 是静默错值，不是异常**：片元声明 panelSizePx、宿主赋值 panelSize，两边 GLSL 都
   "合法"，运行时该 uniform 恒 0 → 边缘亮边铺满整块面板，无编译错误、无日志、无崩溃。这类 bug 只能
   肉眼发现，是本轮最隐蔽的一处（自查发现并修）。治理：UiBackdropShaderContractTest 扫两阶段 uniform
   声明集，逐个断言宿主源码里出现过该名字，并锁同名 uniform 跨阶段类型一致、varying 必须由顶点写出。
   **"声明了但没人赋值"必须由 CI 判，不能靠人读。**
2. **构建链路根本不编译 GLSL**：resources 下的 .frag/.vert 只被当资源打包，语法错误要到真机运行才
   暴露，且表现是"编译失败后静默退回固定管线"——质感变差但无任何异常。本轮若只信 build 绿，等于
   没验证。缓解：UiBackdropShaderSyntaxTest 覆盖可静态判定的三类（剥注释后括号配平、#version 必须首行、
   扩展专属采样函数黑名单）+ 兜底锁 gl_FragColor 与材质分支存在性。本机无 glslangValidator，
   **真编译与观感仍必须真机验收，不能拿 build 绿冒充 shader 正确。**
3. **文本守卫必须先剥注释**：语法守卫初版直接对全文 indexOf("texture2Dbias")，被自己写在注释里的
   "为什么不用它"误报。任何源码文本守卫都要先剥行注释与块注释再匹配。
4. **GLSL 1.20 不含 ARB_shader_texture_lod 的采样函数**：texture2Dbias / texture2DLod 在片段着色器属
   扩展，缺扩展的老驱动上整个 program 编译失败 → 静默降级，**升级反而比不改更差**。mip 偏置这条路
   直接放弃（大半径预降采样已由快照 downsample + separable filter pass 承担）。教训：给 1.7.10 写
   shader 只能假定 GLSL 1.20 内建集，任何带后缀的采样函数都要先证其内建身份。
5. **反直觉的材质数值，直觉方向是错的**：
   - 深色玻璃的 tint 基色不是纯黑，是 systemBlack（约 0x18）；纯黑会让面板失去厚度。
   - 深色档的亮度补偿必须是**正**的：黑蒙层已经吃掉亮度，再给负 lift 等于二次压暗、面板发闷。
     第一版按"dark 就该压暗"写成负值，被参数契约测试的语义断言逼出来才纠正。
   - "往下压"应由内侧下缘暗带（局部几何项）表达，不是全局负偏移。
   凡这类观感系数：先调研取一手数值，再写守卫锁形状（单调厚度、恒等基线、噪点窄带）。
6. **参数位复用要重定义语义，别留死旋钮**：材质档不读线性饱和度，若直接忽略入参 saturation，
   验收页的"饱和度"滑杆会变成静默无效控件。改为材质档下该参数位 = vibrancy 乘子（100% 为配方原值），
   并在参数台写明。附带教训：乘子**不要夹下限**——夹到 1.0 会让 0~100% 全区间等值，又是一段死区。
7. **改核要按 RMS 校准，不能按"最远抽头距离"估**：从规则核换 Poisson 盘时，直觉做法是按最远抽头
   比例补偿（1.25/0.96≈1.30），实际决定糊度的是**加权 RMS 半径**：旧核 0.911、新核未补偿 0.654，
   真系数 1.394（取 1.35 差 3.2%）。观感口径的等价性要用能量/RMS 一类的积分量校，别用极值。
8. **几何锁锚在伴随副作用上，会被功能演进静默消灭**（对上一条坐标根因的补充）：坐标锁查的是
   "玻璃 tint 面"，而材质档把 tint 挪进 shader 后宿主不再叠 tint——锚点消失，坐标锁直接 FAILED
   （运气好），冻结锁则退化为**恒真**（更危险：功能没跑也测不出）。治理：GlassLabHost 暴露
   backdropRectsThisFrame（宿主请求滤镜的矩形 ledger），锁改锚 ledger。**锁要锚在被锁属性本身，
   不要锚在"碰巧能观察它的那个副作用"上。**
9. **TS 模板吃反斜杠复发**：本轮写 contract/syntax 测试时又用了带反斜杠 n 的 split 与换行字符字面量，
   落盘成真换行 → 5 个「未结束的字符串文字」。既有解法有效：按行索引手术修补（不整文件重写）。
   根治法仍是**run_code 模板里不要出现反斜杠转义字面量，一律 fromCharCode 拼接**——本轮再次验证
   "写进记录的规则只有变成肌肉记忆才生效"，Java 源码里的换行/引号转义应视为高危写法。
10. **shader 里的"近似"要写成守卫不覆盖时也自带可核验边界**：边缘亮边最初用
    min(到四直边) 近似圆角矩形边界，圆角段算出的距离偏大 → 亮边在弧段被 stencil 裁掉，
    留下一圈无高光的圆弧（iOS 玻璃最耐看处恰是沿弧的亮边）。改 rounded-box SDF 时，
    因构建链路不编译 GLSL、守卫只锁核能量/语法，**SDF 的数值正确性没有任何测试兜底**，
    故在本地用 JS 独立复算三项：radius=0 时与原近似逐点相等（保证直角面板零行为变化）、
    四象限半径按位取用正确、弧 45° 中点处新法给满亮边而旧法算作 2.3px 超出 1.5 band 丢边。
    教训：运行时才暴露的纯数值改动，交付前必须有"离线可复算的等价性证据"，否则等于没验。
11. **同一 hash 输出复用到两处会露相关性花纹**：盘旋转角与最终抗 banding 抖噪都用
    hashNoise(gl_FragCoord.xy) 时，同像素的旋转量与噪声幅度相关，静止看会出现规则花纹
    ——正是引入盘旋转本要消除的那类结构化伪影。改为偏移域取 hash（+vec2(37.0,91.0)）解耦。
    凡"多个视觉项共用同一噪声源"先问是否相关。
12. **观感参数必须分层标注证据等级，社区复现值不等于一手锚点**：质感升级第一轮依据的
    "调研值"实为设计圈复现配方（150% 饱和、blur/sat 区间等），第二轮一手核查（apple.com
    线上 CSS、WWDC13 官方示例 UIImage+ImageEffects.m 逐值可取）暴露出方向性错误：官方
    Dark 档 saturate(1.2) 明显低于 Light(1.8)，而我按"dark=同饱和叠黑"把深色档 vibrancy
    定到与浅色几乎同高——深色玻璃会把彩色背景烧成脏色斑。纠偏后补守卫
    darkMaterialsAreLessVibrantThanLightPeers 锁方向。两个连带收获：
    - 网传"dual filter blur 混合因子公式"（1.5·e^(−1.6σ²/…)−0.5）经数值自检恒给负权重，
      **抄任何圈内公式前先代两个数验符号/值域**，一手论文不可得本身就是风险信号；
    - 同一系数不同口径不可对标：官方 1.8 是线性 saturate 矩阵乘子，本 shader 是亮度域
      保护曲线（中间调吃色低于峰值），照搬数值等于偷偷改曲线。口径关系必须写进 javadoc。

## 2026-09 磨玻璃实验室真机首验：宿主叠加层把局部坐标当屏幕坐标（根因三层）

1. **直接根因**：GlassLabHost 在 render 里读 stage 的 cachedLayout(LayoutBox) 的 getX/getY 当屏幕
   坐标用，但 LayoutBox.x/y 是**相对父容器**的局部坐标（类 javadoc 白纸黑字）。采样场在居中列内
   （绝对 x≈222），局部 x≈0 → 玻璃面板画到了屏幕左缘。正解：跨层取绝对盒一律走
   SceneGeometry.absoluteBox(node, rootAbsX, rootAbsY)（hit test/事件/拖拽同源，且注入祖先滚动偏移，
   滚动时叠加层自动跟手）。
2. **深层根因（为什么框架允许犯这个错）**：LayoutBox.getX() 与 AnchorRect.getX() **同名**，一个返回
   局部坐标一个返回绝对坐标，类型系统完全不区分——坐标语义纯靠命名约定和"人记得换算"。
   权威单点 absoluteBox 一直存在，但没有任何机制强制跨层消费必须走它。这是审查报告点名的
   "1 个权威 + N 处各自重实现"病根的坐标版：重实现甚至不是写代码，是**忘了有权威、手搓局部坐标**。
   治理：新增 LayoutBoxLocalCoordGuardTest——main 源码中读 LayoutBox 变量 x/y 的文件必须在白名单
   （SceneGeometry/ScenePaintEngine/SceneHitTester 三个按设计递归累加的权威本体），其余 CI 直接拦。
3. **测试盲区（为什么 headless 全绿却真机翻车）**：上一轮 GlassLabHostTest 只断言"tint 在主树回放
   **之后**"（时序正确），没断言面板矩形的**空间坐标**——时序对了、坐标全错照样绿。补
   glassPanelUsesAbsoluteBoxNotParentLocalCoordinates：断言面板 left/right/top 精确等于
   absoluteBox(stage)+偏移，且先验证"绝对 x > 0"保证测试本身有区分度（居中布局下局部=0、
   绝对=222，回归时 left 会变 10 而非 232，直接失败）。教训：几何叠加类渲染，时序、坐标、尺寸
   三个维度都要锁，只测时序=没测。
4. **⚠ 同一条锁在下一轮被自己的功能演进消灭（二级教训，见下条第 8 点）**：上述坐标锁的断言锚点是
   "玻璃 tint 面"这一**伴随副作用**。质感升级后材质档把 tint 挪进 shader、宿主不再叠面，锚点随之
   消失，坐标锁静默失效、冻结锁退化为恒真。教训：**锁必须锚在被锁属性本身**（宿主请求滤镜的矩形），
   不能锚在碰巧能观察它的副作用上。

## 2026-09 磨玻璃核修复：run_code 写 Java 源的两类转义新变体

1. **反斜杠计数在「run_code 参数 JSON → TS 模板 → Java 源码 → Java 字符串字面量 → 运行时正则」
   五层传递里必然失控**：本轮往 Java 写正则，落盘后层数错位，Pattern.compile 抛
   `Unclosed group near index 80` 且炸在 static 初始化（ExceptionInInitializerError，build 里表现为
   整类 Error，易误判成环境问题）。**守卫测试一律改用纯字符串解析**（indexOf/substring/
   lastIndexOf），零正则、零反斜杠——本轮两仓契约测试最终形态都是零正则实现，一次通过。
2. **Java 字符串里的换行转义（反斜杠 n）会被 TS 层先解成真换行**，Java 源码变成跨行未闭合
   字符串字面量，一次 build 报 14 个「未结束的字符串文字」连锁错误。修法：需要 Java 源码里
   出现反斜杠转义时，用 `String.fromCharCode(92)+n` 拼接落盘文本；出错后用
   `String.fromCharCode(10)` 定位、按行索引手术修补（本轮即此法修复，未整文件重写）。
3. **GTNH 插件 `Failed to load the manifest from Github` 是配置阶段瞬时网络故障**，与代码无关；
   `--offline` 可用缓存直接续跑（本轮第二次 build 31s 即绿）。重跑前先 `git diff` 确认非自己引入。

## 2026-08 chat3 真机 markdown 行级规则失效（行首颜色码）+ 滚轮双路径核查

1. **玩家消息去发送者前缀后行首恒残留 § 颜色码**：原版 1.7.10 玩家聊天是
   `chat.type.text` translation 组件，formattedText = `§f&lt;名字&gt; §f消息`（每个 sibling
   自带颜色码）。`FormatPrefixStripper` 按有效字符数剥前缀后，行首的第二个 §f 保留在
   displayLines 里。`ChatMarkdownLineRule.classify` 只剥行尾格式码、不剥行首 →
   单行 `- item`/``$$...$$`` 等行级规则对玩家消息全部失效（真机截图字面 -/*/1.）。
   修法：classify 开头对称增加 `stripLeadingFormatCodes`（连续 §x 对零宽剥离）再判行首标记。
   测试坑：`new ChatComponentText("<Bob> - item")` 单组件形态 formattedText 只有一个开头的
   §f，会被前缀剥离消费，**测不出此缺陷**；必须用自定义 IChatComponent stub 构造
   plain/formatted 分离的 translation 同款形态（见 ChatMessageListTest.SiblingStyledComponent）。
2. **1.7.10 GuiScreen.handleMouseInput 没有滚轮→聊天转发**（javap 反编译原版 client.jar
   `bdw.k()` 实证：只有按钮 down/up/拖拽，无 getEventDWheel/chat scroll 调用）；滚轮聊天滚动
   只存在于 `GuiChat.handleMouseInput`（`bct.k()`：clamp ±1 → 非 Shift ×7 → chat.scroll）。
   chat3 的 ChatInputScreen 继承 GuiScreen（非 GuiChat）→ 原版转发路径不存在，与 scene 滚轮
   路径（×7）**天然互斥**，不会同帧叠加成 8 行/格。「非 GuiChat 屏幕调 chat.scroll」是 MC 1.8+
   行为，评估 1.7.10 代码时勿照搬。Forge patch（forgepatches.zip GuiScreen.java.patch）、
   lwjgl3ify MixinGuiScreen（仅剪贴板）、Hodgepodge GuiScreen mixin 均不涉及滚轮。
3. **测试脚本转义坑**：run_code 的 TS 模板字符串内嵌 Java 反引号字符串要用
   `String.fromCharCode(96)` 拼接；`String.raw` 模板里写 `\`` 不会解义（保留反斜杠），
   插进 Java 源码会变非法转义。

## 2026-08 LaTeX 真机渲染乱飞（第二批，子代理撇除偏见审查发现）


## 2026-08 chat3 输入框批(客户端命令补全+对齐)测试基建三坑

1. **headless 测试不能触 Minecraft 类初始化**：`ChatInputBar.onOpened` 的 vanilla 历史同步
   会经 `Minecraft.getMinecraft()` 触发 `Minecraft` 静态初始化 → LWJGL 链在测试 JVM 炸
   (`NoSuchMethodError: DisplayMode.<init>` / `ExceptionInInitializerError`,且类初始化失败后
   同 JVM 内后续引用级联 `NoClassDefFoundError`)。修法:把不含 MC 依赖的行为拆成独立方法
   (如 `focusAndAlignCaret()`)供测试直测,`onOpened` 只做编排;测试文件内务必避开任何
   会初始化 Minecraft 的调用路径。
2. **SceneRuntime Signal.set 是 pending 写队列**：外部 `signal.set(...)` 后未 flush 前
   `get()` 返回旧值(primitive 注释已写明「Signal.set 只入 pending 写队列」)。测试断言
   `ChatInputBar.inputText()` 前必须 `runtime.flush()`,否则断言拿到初值;harness 的
   `typeText` 内部 flush 但只覆盖输入路径。
3. **ICommandSender 测试假实现是 SRG 名视图**：dev 环境编译视角下接口含
   `func_145748_c_()`(返回 `IChatComponent`)而非常见 MCP 名,且需覆盖
   `getCommandSenderName/addChatMessage/canCommandSenderUseCommand/getPlayerCoordinates/
   getEntityWorld` 全量方法;先例见 `QzUiLibClientCommandTest.RecordingSender`。
   另注意 `ClientCommandHandler.instance` 是全局单例,测试注册的假命令用唯一名并在
   `@AfterClass` 从 `getCommands()` 移除。


## 2026-08 LaTeX 真机渲染乱飞（第二批，子代理撇除偏见审查发现）

1. **hasMixedSize 判定必须下沉到字形级**：第一次修 uniform 快路径时只在"段外层"判
   （`<size>` 包裹公式才置位），公式**内部**的 sizeScale 缩放字形（script 0.7×、
   伸缩定界符放大）不置位 → 默认字号公式仍走 uniform 全尺寸路径，缩放字形按正文大小
   绘制且与 0.7× 布局偏移错配。修法：收集后扫描公式盒内是否存在 sizeScale != 1.0 字形。
2. **decoration 通道没有顶→基线换算**：glyph quad 在 `resolveGlyphQuadMetrics` 内部做
   `baselineY = y + lineBaselineY × baselineScale`（y 是字格顶），而
   `collectDecoration` 的 y 直接是矩形顶边——LaTeX 规则线（相对盒基线）必须手动补
   `lineBaselineY × baselineScale` 偏移，否则整体上移约一个 ascent 与字形分离。
   教训：同一帧内不同绘制通道的坐标参照（顶/基线）必须逐一核对，不能假设一致。
3. **maxFontSizePx 是整行基线基准，必须含公式放大型字形**：伸缩括号 glyph 字号可能
   超过段字号，漏记会让整行基线换算偏小（用 int[] holder 在 fill 路径回传）。

## 2026-08 LaTeX 真机渲染混乱（渲染快路径双 bug）

1. **uniform 快路径误判混合字号**：渲染层以 `maxFontSizePx <= baseFontSizePx` 判定"全段同字号"，
   只防得住 `<size>` 放大段，识别不出 LaTeX 的 **0.7× 缩小字形**（分子分母/上下标/矩阵格），
   导致公式字形按正文全尺寸绘制、相互重叠不可读。修法：收集期显式跟踪
   `hasMixedSize`（任意 glyph 字号 ≠ 基准字号即置位），快路径条件改为 `!hasMixedSize`。
2. **分段填充函数必须回写游标**：latex 段填充（fillLatexSegment）以局部 glyphIndex 起写，
   若调用方不回写返回的新下标，后续普通文本段会覆盖公式条目（公式后文本错乱）。
   教训：fill 型辅助方法的返回语义（新游标/写入计数）要在签名 javadoc 写明。
3. **遗留未修（低危）**：SceneTextMeasurer.measureWidth 无模式参数，RICH 文本的水平
   对齐（居中/右对齐）会把标签按字面宽度计算——LaTeX 页默认左对齐不受影响，
   待增强包或对齐需求出现时再改。

## 2026-08 LaTeX 布局/集成（M2-M4）

1. **盒模型坐标约定先行**：MathBox 元素 y 以盒基线为 0、向下为正；拼接（addBox）时包围盒
   高度/深度按「child.height - dy / child.depth + dy」更新，平移（shiftBox）后宽/高/深必须同步
   （left/right 括号在负 x 侧时，漏平移宽度会裁掉左侧内容）。
2. **字号缩放必须复合传递**：script（0.7×）内的字形 sizeScale 要沿 addBox 链相乘（addBox 带
   glyphScale 参数），否则渲染侧按 1.0 光栅化；布局侧"字号"参数只管度量、渲染侧靠 sizeScale
   还原。
3. **矩阵行基线对齐最大 height 而非最大 depth**：行基线 = 行顶 + max(cell.height)，否则高格顶
   越行界（包围盒被顶穿）。矩阵整体垂直居中于数学轴（height = total/2 + axis）。
4. **渲染与布局度量口径**：latex 段渲染侧 advance 用 resolveAdvance、布局侧用 resolveCodepointAdvance，
   两者在无 sup/sub/spacing 时等价；公式段推进差补偿到段末字形（tail = 盒宽 - Σadvance），
   保证整体推进 = 盒宽。
5. **布局缓存 key 必须含 fontType**（BOLD/NORMAL 宽度不同）与 runtimeVersion（字体 reload 失效）；
   缓存满时断言改用「新 key 命中」行为而非条目数（LRU 淘汰老条目）。
6. **注释里写 LaTeX 命令**：javadoc 中 `\underline` 等 `\u` 开头命令须写双反斜杠（见 M1 记录），
   本次在富文本文档表格中又出现一次反引号错位（`<latex>` 与 `</latex>` 拆分反引号）。

## 2026-08 LaTeX 解析器（M1）

1. **Java 注释里的 `\u` 序列同样参与 Unicode 转义**：javadoc 注释写 `\underline` 等以
   `\u` 开头的命令会报「非法的 Unicode 转义」（词法层先于注释处理）。注释中需写双反斜杠
   `\\underline`（javadoc 渲染仍为 `\underline`），或改用 `{@code}` + 双反斜杠。
2. **脚本生成 Java 源码的反斜杠层级**：JS 模板生成 Java 源码时，反斜杠每层加倍
   （JS 字符串 → Java 源码字面量 → Java 运行时）。用 `String.raw` + 单次 replace 辅助函数
   显式构造 Java 字符串字面量（含引号），避免手写转义；生成的字符转义（`'\t'` 等）必须
   保持**单**反斜杠形式，双反斜杠是「未结束的字符文字」。
3. **TeX 上下标绑定语义**：`x_i^2` 中下标参数只取单原子/组（不得吞后续 `^`，否则
   `i` 被解析成 `i^2`）；命令原子必须与普通原子一样经过上下标循环（否则
   `\sum_{i=1}^{n}` 丢失上下限）。实现落点：参数解析用「单原子」入口、
   列表主循环对命令分支走「因子」入口。
4. **矩阵 AST 需保留列边界**：`a&b\\c&d` 若只存「行→节点」扁平列表，布局层无法做列宽
   对齐；采用三维结构「行 → 列 → 格内节点」，泛型维度易写错（行是二维 `List<List<Node>>`，
   不是三维）。