# Qz-UILib 字体系统专项任务里程碑

## 1. 目标定义

当前阶段将“旧版字体系统重构优化到新代码区”设为唯一最高优先级目标。

本阶段不再以完整 UI 库迁移为主，而是聚焦以下结果：

- 在新代码区建立可独立演进的字体系统主线
- 保留旧版字体系统的核心能力
- 修正旧版实现中的高耦合、高风险、难维护问题
- 为后续 UI 组件、配置界面、Mixin 兼容提供稳定字体底座

## 2. 旧版字体系统现状分析

旧版字体系统并不是单个 `FontRenderer` 替换类，而是一整套协作流水线。

核心链路如下：

1. `ReplaceFontRender`
   - 对外提供字符串测量、绘制、换行、阴影、样式码处理
   - 是外部调用入口
2. `PageManager`
   - 管理字符页、字符缓存、待生成字符、生成完成回写
   - 是字符查询与纹理页分配中心
3. `CharImageGenerator`
   - 异步生成字符图像和度量信息
4. `FontManager`
   - 发现字体、排序字体、选择可显示字符的字体
5. `BatchRenderFont`
   - 按字符页分批收集顶点并提交绘制
6. `FontRenderTool`
   - 封装 VAO/VBO/EBO 上传和实际绘制
7. `ShaderManager`
   - 管理字体渲染着色器
8. `CharPage`
   - 管理字符纹理页与字符坐标映射
9. 配置系统 + 事件总线 + Mixin
   - 负责重载、参数调节、替换原版字体、兼容 NEI/AE/TC 等场景

结论：

- 字体系统本身就是一个独立子工程
- 不能以“迁移一个类”的方式推进
- 必须按流水线分层重建

## 3. 旧版主要问题

### 3.1 生命周期不清晰

旧版大量使用：

- 单例
- 静态字段
- `magicInit()`
- 在调用链中被动触发初始化

表现为：

- 初始化顺序难以推断
- 重载后状态一致性依赖隐式约定
- 不利于后续定位问题

### 3.2 线程模型粗糙

`CharImageGenerator` 每次请求字符都会直接 `new Thread()`：

- 线程数量不可控
- 高并发文本场景容易膨胀
- 缺少统一队列和线程池治理
- 与重载锁之间存在竞态风险

这是旧版最优先需要治理的问题之一。

### 3.3 模块职责边界混乱

典型问题：

- `ReplaceFontRender` 同时负责：样式解析、宽度测量、批渲染收集、重载入口、状态管理
- `PageManager` 同时负责：页管理、缓存、生成调度、主线程回写、事件注册
- `FontManager` 同时负责：字体发现、字体排序、重载联动

结果是：

- 类太大
- 功能修改容易互相影响
- 很难做针对性优化

### 3.4 渲染状态污染风险高

字体渲染内部频繁直接操作：

- `GL11.glPushAttrib(GL11.GL_ALL_ATTRIB_BITS)`
- 纹理状态
- 混合状态
- Alpha Test
- 矩阵与管线状态

这会带来：

- 与其他 GUI/物品渲染冲突
- 第三方模组兼容风险提升
- 排查异常渲染现象成本高

### 3.5 配置热重载链路脆弱

当前重载依赖：

- `ReplaceFontRender.inReload`
- `FontReloadEvent`
- `FontReloadDoneEvent`
- 多个模块按照优先级隐式协作

风险点：

- 如果顺序被破坏，可能出现脏缓存、纹理泄露、生成中断不一致
- 当前方案更像“约定可用”，不够“结构可靠”

### 3.6 兼容层耦合过深

旧版通过 Mixin 直接覆盖：

- `FontRenderer`
- NEI 文本绘制
- AE 物品数量绘制
- 其他第三方字体场景

这意味着字体系统尚未稳定前，不能直接全量接管，否则故障面会非常大。

## 4. 本次专项重构目标

本次字体系统专项不追求“一步到位复制旧版全部效果”，而是分成两个大阶段：

### 阶段 A：稳定化重建

目标：

- 让新代码区拥有结构清晰、生命周期明确、线程模型受控的字体系统
- 先恢复“能稳定渲染”的基础能力

### 阶段 B：效果增强与兼容恢复

目标：

- 在稳定底座上逐步恢复旧版的特效、替换能力和第三方兼容
- 再做视觉质量和性能优化

## 5. 建议的新字体系统结构

建议新代码区围绕 `club.heiqi.uilib.font` 重建：

- `club.heiqi.uilib.font`
- `club.heiqi.uilib.font.api`
- `club.heiqi.uilib.font.config`
- `club.heiqi.uilib.font.event`
- `club.heiqi.uilib.font.glyph`
- `club.heiqi.uilib.font.page`
- `club.heiqi.uilib.font.render`
- `club.heiqi.uilib.font.shader`
- `club.heiqi.uilib.font.compat`
- `club.heiqi.uilib.font.util`

建议职责拆分：

- `FontService`：统一对外入口
- `FontRegistry`：字体发现、字体排序、字体选择
- `GlyphGenerator`：字符图像生成
- `GlyphPageManager`：字符页分配、缓存、回写
- `FontRendererAdapter`：提供 drawString/getStringWidth 等兼容接口
- `FontBatchRenderer`：批渲染提交
- `FontShaderProgram`：着色器生命周期管理
- `FontReloadCoordinator`：统一重载流程
- `FontCompatHooks`：原版/第三方兼容挂接

## 6. 专项推进原则

### 6.1 先可控，后高性能

先确保：

- 初始化顺序清晰
- 数据结构可推断
- 重载流程可验证

再考虑：

- 缓冲区复用
- 批量渲染吞吐
- shader 参数优化

### 6.2 先自渲染闭环，后接管原版

先完成：

- 独立字体渲染器可用
- 独立测试界面可验证

再做：

- `FontRenderer` Mixin 接管
- NEI/AE 等第三方兼容接入

### 6.3 先同步正确，后异步扩展

虽然旧版已经异步生成字符，但当前异步实现质量不高。

建议：

- 先明确“字符请求 -> 生成 -> 回写 -> 渲染”的正确状态机
- 再用线程池替代随建线程模型

### 6.4 先保留行为，再优化接口

例如：

- 保留 `drawString`、`getStringWidth`、`listFormattedStringToWidth` 等兼容调用方式
- 但内部实现改为新结构

这样能降低后续接入 UI 和 Mixin 的改造成本。

## 7. 字体系统专项里程碑

## M1. 建立字体系统最小骨架

目标：在新代码区建立字体系统目录与基础入口，不引入旧版全部复杂逻辑。

任务：

- 创建新包结构：`font`、`font.page`、`font.render`、`font.shader`、`font.config`、`font.event`
- 定义字体系统核心接口与基础类命名
- 明确哪些类保留单例，哪些改为受控实例
- 明确渲染入口类与服务协调类

输出物：

- 新字体系统目录骨架
- 字体系统职责说明
- 基础服务装配入口

验收标准：

- 新代码区存在清晰字体系统结构
- 不再依赖旧版 `magicInit()` 风格初始化

## M2. 重建字体配置模型

目标：把旧版字体配置从“全塞进 Config”改为可维护的字体配置模型。

任务：

- 梳理旧版字体相关配置项：
  - `awtCharSize`
  - `charSize`
  - `fontScale`
  - `spaceWidth`
  - `characterSpacing`
  - `shadowOffsetX/Y`
  - `lineSpacing`
  - `renderOffset`
  - `colorGain`
  - `smoothRangeMin/Max`
  - `aaMode`
  - `aaStrength`
  - `fontSort`
  - `replaceOrigin`
  - `customInvCountFont`
- 提炼为字体专项配置对象
- 明确配置默认值、边界值、热更新影响范围

输出物：

- 字体配置类
- 配置项说明与默认值表
- 热更新策略说明

验收标准：

- 字体参数与普通模组配置职责分离
- 后续重载时可明确知道哪些参数需要清空缓存，哪些只影响渲染表现

## M3. 重建字体注册与字体选择器

目标：重建 `FontManager`，但拆分其职责。

任务：

- 抽离字体发现逻辑：
  - 本地 `fonts` 目录
  - 系统字体加载
- 抽离字体排序逻辑
- 抽离“根据 codepoint 选择合适字体”的逻辑
- 规范字体集合的数据结构和排序更新行为

重点优化：

- 避免在同一个类里同时处理注册、排序、重载回调
- 统一“普通字重 / 粗体字重”选择策略

输出物：

- 字体注册器
- 字体匹配器
- 字体排序策略

验收标准：

- 能稳定找到可显示指定字符的字体
- `fontSort` 配置可独立生效

## M4. 重建字符生成流水线

目标：替换旧版 `CharImageGenerator` 的粗糙线程模型，建立可控字符生成流程。

任务：

- 重新定义字符生成请求对象
- 明确生成结果对象：图像 + 度量信息
- 用受控线程池或统一任务队列替换 `new Thread()`
- 保留对普通字符、粗体字符、Emoji 场景的处理能力
- 梳理字符尺寸、视觉边界、步进宽度的计算规则

重点优化：

- 避免无限线程增长
- 明确“生成中 / 已完成 / 失败 / 重载中断”状态
- 让字符生成可以被安全取消或忽略过期结果

输出物：

- 新字符生成器
- 生成任务调度器
- 生成状态模型

验收标准：

- 连续请求大量新字符时线程数可控
- 不因频繁生成导致主线程卡死或重载冲突

## M5. 重建字符页与缓存管理

目标：重建 `PageManager + CharPage` 体系，让缓存、纹理页、回写流程更稳定。

任务：

- 拆分“页分配”和“字符状态跟踪”职责
- 明确普通/粗体页池的管理方式
- 明确缓存结构：
  - 字符到页映射
  - 已生成标记
  - 生成中标记
- 定义主线程回写流程
- 明确纹理页关闭与资源释放策略

重点优化：

- 杜绝依赖 `finalize()` 的资源清理思路
- 减少脏状态残留
- 提高重载后的缓存一致性

输出物：

- 新字符页管理器
- 新字符页对象
- 主线程提交队列

验收标准：

- 字符可以稳定落到纹理页中
- 重载后旧页能被正确释放
- 不因缓存错乱导致字符缺失或映射错误

## M6. 重建批渲染与底层绘制管线

目标：把 `BatchRenderFont + FontRenderTool + ShaderManager` 重构成清晰的渲染层。

任务：

- 拆分“批次收集”和“OpenGL 提交”职责
- 规范 VAO/VBO/EBO 生命周期
- 梳理 Buffer 扩容策略
- 明确 shader 参数来源
- 抽离统一状态设置与状态恢复逻辑

重点优化：

- 降低 OpenGL 状态污染
- 明确每一帧的收集、提交、清理边界
- 避免字体渲染层泄漏实现细节到业务侧

输出物：

- 新批渲染器
- 新底层绘制器
- 新 shader 程序封装

验收标准：

- 批量文本绘制可正常工作
- 状态切换不会明显污染其他渲染流程
- Buffer 扩容后不会丢数据或错位

## M7. 重建文本测量与格式码处理

目标：把 `ReplaceFontRender` 中最复杂的文本逻辑抽离并整理。

任务：

- 梳理 `§` 样式码状态机
- 重建：
  - `getStringWidth`
  - `getCharWidth`
  - `trimStringToWidth`
  - `wrapFormattedStringToWidth`
  - `listFormattedStringToWidth`
  - `splitStringWidth`
- 明确粗体、阴影、下划线、删除线、随机字符效果的计算方式
- 为双向文字/阿拉伯语保留扩展点

重点优化：

- 避免测量和渲染逻辑混写
- 避免重复解析同一套格式码状态

输出物：

- 文本布局器 / 文本测量器
- 样式码解析器
- 渲染前文本片段模型

验收标准：

- 宽度测量结果稳定
- 换行、裁剪与实际绘制结果一致性高

## M8. 重建字体渲染兼容适配层

目标：在不立即全量 Mixin 接管的前提下，先建立兼容原版 `FontRenderer` 的适配接口。

任务：

- 建立 `FontRendererAdapter` 风格适配层
- 提供兼容方法：
  - `drawString`
  - `drawStringWithShadow`
  - `drawSplitString`
  - `getStringWidth`
  - `trimStringToWidth`
- 让外部调用者不必直接依赖底层页管理和批渲染细节

输出物：

- 字体渲染适配器
- 基础调用示例

验收标准：

- 新字体系统可以先在独立测试环境中直接使用
- 后续替换 Mixin 时不需要重新改底层渲染逻辑

## M9. 重建字体热重载协调器

目标：把旧版依赖优先级事件顺序的重载流程重构成显式协调流程。

任务：

- 明确重载时序：
  1. 停止新字符请求
  2. 中止或忽略旧生成结果
  3. 清空页缓存与页池
  4. 更新字体配置和字体注册信息
  5. 重建渲染资源
  6. 恢复字符请求
- 明确重载期间的保护状态
- 明确主线程与后台线程的同步边界

重点优化：

- 不再依赖“谁先监听事件谁先执行”的隐式假设
- 重载要可推理、可验证、可记录日志

输出物：

- 字体重载协调器
- 重载状态机
- 重载日志点设计

验收标准：

- 连续重载不会导致纹理页泄露、死锁、字符丢失
- 重载期间请求字符不会把系统打进异常状态

## M10. 建立独立测试与调试界面

目标：先验证字体系统本身，而不是立刻接管全项目。

任务：

- 创建最小字体测试界面
- 验证以下场景：
  - 英文
  - 中文
  - 粗体
  - 阴影
  - 换行
  - 大量新字符动态生成
  - Emoji 或特殊字符
- 增加必要调试输出：
  - 当前页数
  - 已生成字符数
  - 生成队列长度
  - 当前字体顺序

输出物：

- 字体调试 GUI 或调试页
- 基础手工验证清单

验收标准：

- 不依赖 Mixin 即可验证字体系统主要能力
- 出问题时能快速定位是在生成、页管理、还是渲染层

## M11. 接入配置界面与字体排序界面

目标：恢复旧版字体系统可调试、可配置的优势，但在新结构上实现。

任务：

- 重建字体配置 GUI
- 重建字体搜索、排序、保存流程
- 明确哪些配置改动需要立即重载，哪些只需局部更新

输出物：

- 新字体配置 GUI
- 字体排序保存能力

验收标准：

- 字体配置界面可独立使用
- 配置修改后行为符合预期

## M12. 分阶段接入 Mixin 与第三方兼容

目标：在字体底层稳定后，再逐步接管原版和第三方文本渲染。

任务：

- 第一阶段：只接管原版 `FontRenderer`
- 第二阶段：接入原版物品数量显示
- 第三阶段：接入 NEI / AE / TC 等第三方文本场景
- 每接入一个兼容点就单独验证

重点优化：

- 不做一口气全接管
- 保留开关，允许逐步灰度验证

输出物：

- 原版接管 Mixin
- 第三方兼容 Mixin
- 场景验证记录

验收标准：

- 开启字体替换后主流程稳定
- 第三方兼容异常可局部关闭，不影响主系统

## 8. 推荐执行顺序

建议按以下顺序推进：

1. M1 建立字体系统最小骨架
2. M2 重建字体配置模型
3. M3 重建字体注册与字体选择器
4. M4 重建字符生成流水线
5. M5 重建字符页与缓存管理
6. M6 重建批渲染与底层绘制管线
7. M7 重建文本测量与格式码处理
8. M8 重建字体渲染兼容适配层
9. M9 重建字体热重载协调器
10. M10 建立独立测试与调试界面
11. M11 接入配置界面与字体排序界面
12. M12 分阶段接入 Mixin 与第三方兼容

## 9. 当前阶段最重要的优化重点

如果只看“最该先做什么”，优先级应为：

1. 初始化与生命周期治理
2. 字符生成线程模型治理
3. 字符页和缓存一致性治理
4. 渲染状态污染治理
5. 热重载流程治理

原因：

- 这些是稳定性的地基
- 地基不稳时，继续做字体视觉效果和第三方接管只会放大故障面

## 10. 第一批实际开发建议

下一步最合理的第一批落地项应为：

1. 在新代码区创建 `font` 相关目录骨架。
2. 先定义新的字体配置类和字体服务入口。
3. 拆出字体注册器与字体匹配器，先把 `FontManager` 解体。
4. 设计新的字符生成任务模型和主线程回写模型。
5. 做一个不依赖 Mixin 的最小字体测试页。

## 11. 结论

旧版字体系统的价值很高，但它当前更像“已经证明可行的原型系统”，而不是“可以直接长期维护的稳定架构”。

本次专项推进的核心不是简单迁移，而是：

- 保留能力
- 重建结构
- 先稳定化
- 再恢复接管能力
- 最后再做视觉和兼容增强

这样完成后，新主线中的字体系统才会真正成为整个 Qz-UILib 的高优先级基础能力，而不是一个难以维护的历史大模块。
