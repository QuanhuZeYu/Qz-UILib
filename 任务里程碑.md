# Qz-UILib MC UI 系统专项任务里程碑

## 1. 阶段目标

当前阶段的最高优先级目标，已经从“字体系统重构”切换为“面向 Minecraft 1.7.10 的 UI 系统主线重建”。

本阶段聚焦以下结果：

- 在新代码区建立可持续演进的 UI 系统骨架
- 吸收旧版 `BaseGUI`、`Widget`、布局器、基础控件的有效设计
- 避免直接照搬旧版中高耦合、状态污染、职责混杂的问题
- 让新 UI 系统天然接入当前已经完成的字体系统
- 为后续配置界面、调试界面、业务 GUI 提供统一底座

## 2. 当前基础与前置条件

UI 系统专项开始前，已有基础如下：

- 新字体系统主链已经完成重建并可用
- 原版 `FontRenderer` 已通过 early mixin 接管
- 文本样式码、宽度测量、批渲染、字符页管理已具备基础能力
- Forge 配置 GUI 已接通，说明当前项目已经具备 GUI 集成入口
- 项目依赖已接入 GTNH 的 `lwjgl3if`，可直接使用 `lwjgl3ify` 提供的 GLFW 化输入能力

这意味着新的 UI 系统不需要再依赖旧版字体实现，而应直接建立在 `club.heiqi.uilib.font` 之上。

输入层新增约束如下：

- 键盘按键与文本输入优先基于 `me.eigenraven.lwjgl3ify.api.InputEvents`
- 鼠标输入优先基于 `org.lwjglx.input.Mouse` 事件队列
- 新 UI 系统不再以 `GuiScreen.handleMouseInput`、`handleKeyboardInput` 作为主输入入口

## 3. 旧版 UI 资产梳理

旧版 UI 系统并不是单个界面类，而是一套围绕组件树组织的完整结构。

核心入口与结构：

1. `gui/BaseGUI.java`
   - 负责界面生命周期、鼠标键盘输入、FBO 中转绘制、根组件驱动
2. `widget/Widget.java`
   - 负责组件树、布局应用、绘制分发、事件分发、裁切、提示文本、基础动画入口
3. `widget/layout/*`
   - 包含多种布局器：
   - `DefaultLayout`
   - `RelativeCoordinateLayout`
   - `CenterLayout`
   - `HorizontalLayout`
   - `VerticalLayout`
   - `GridLayout`
   - `FixedParentLayout`
   - `FixedContentLayout`
   - `SimpleHorizonLayout`
   - `SimpleVerticalLayout`
4. `widget/drawUtil/*`
   - 提供基础形状和绘制辅助：
   - `RenderTool`
   - `RoundedRectangle`
   - `Circle`
   - `FixedFunctionMesh`
   - `Shape`
5. 基础控件集
   - `LabelWidget`
   - `ButtonWidget`
   - `ButtonWithTextWidget`
   - `TextButtonWidget`
   - `DraggableWidget`
   - `ListWidget`
   - `TextEditWidget`
   - `IntegerEditWidget`
   - `DoubleEditWidget`
   - `IntegerSliderWidget`

结论：

- 旧版已经验证了“组件树 + 布局器 + 基础控件”这条路线是可行的
- 但新实现不能简单按文件名平移，必须先做职责重构

## 4. 旧版 UI 系统主要问题

### 4.1 `BaseGUI` 职责过重

旧版 `BaseGUI` 同时承担：

- FBO 生命周期管理
- OpenGL 矩阵与视口切换
- 输入轮询与事件转换
- 根组件驱动
- 背景绘制

结果是：

- 界面基类难以复用
- 渲染策略与输入策略绑死在一起
- 后续做测试页、弹窗、嵌套界面时扩展空间有限

### 4.2 `Widget` 职责过于混杂

旧版 `Widget` 同时负责：

- 组件树组织
- 布局与尺寸管理
- 事件分发
- 裁切
- tooltip
- tween 动画入口
- 调试描边
- 字体渲染入口

结果是：

- 类体量过大
- 修改一个能力容易影响其他能力
- 很难形成清晰的“布局层 / 事件层 / 绘制层”边界

### 4.3 事件模型偏原始

旧版输入分发主要依赖：

- 鼠标当前位置是否在 bounds 内
- 手工递归转发 press / release / drag / hover / wheel / type

问题在于：

- 焦点管理不明确
- 捕获与冒泡语义不清晰
- 拖拽、悬停、输入焦点之间容易出现竞争

### 4.4 布局系统可用但语义不统一

旧版布局器数量已经不少，但存在问题：

- 布局目标有时是改坐标，有时是改尺寸
- `perfectWidth` / `perfectHeight` 与实际约束语义不够明确
- 缺少统一测量阶段与排列阶段边界

这会让复杂容器在叠加使用时变得不可预测。

### 4.5 渲染状态污染风险高

旧版大量直接操作固定管线状态：

- 矩阵栈
- 视口
- `glPushAttrib(GL_ALL_ATTRIB_BITS)`
- 纹理、混合、深度、裁切等状态

这在 MC 环境里会直接增加与物品渲染、HUD、其他模组 GUI 的冲突风险。

### 4.6 UI 与字体实现耦合过深

旧版 `Widget` 直接持有旧字体渲染器。

这会导致：

- UI 层无法独立演进
- 字体系统替换时 UI 跟着连带修改
- 测试和调试界面难以抽象

新实现应只依赖新的字体适配接口，而不是依赖旧具体实现。

## 5. 新 UI 系统重建目标

本次 UI 专项不是先追求“控件数量最多”，而是先建立一条结构稳定的主线。

### 阶段 A：UI 核心骨架重建

目标：

- 建立新的 GUI 基类、组件树模型、布局协议、事件分发协议
- 先让最小界面闭环能跑起来

### 阶段 B：基础控件与渲染辅助恢复

目标：

- 重建文本、按钮、列表、编辑框、滑条等高频控件
- 接入字体系统、裁切、调试信息、绘制辅助

### 阶段 C：专项界面与调试能力落地

目标：

- 建立 UI 测试页、布局调试页、控件演示页
- 为后续模组配置界面和业务界面提供模板

## 6. 建议的新 UI 结构

建议围绕 `club.heiqi.uilib.ui` 重建，初步拆分为：

- `ui.screen`
- `ui.widget`
- `ui.layout`
- `ui.event`
- `ui.input`
- `ui.render`
- `ui.draw`
- `ui.text`
- `ui.debug`
- `ui.control`

建议职责：

- `BaseScreen`：GUI 生命周期与场景入口
- `Widget`：组件节点基础能力
- `Layout`：测量与排列协议
- `InputRouter`：鼠标键盘状态转事件
- `RawInputService`：对 `lwjgl3ify` 键盘/文本事件与鼠标队列做统一收集
- `FocusManager`：焦点、悬停、拖拽目标管理
- `UiRenderContext`：渲染上下文与状态边界
- `UiDraw`：矩形、圆角、描边、阴影、裁切等基础绘制工具
- `UiTextRenderer`：基于新字体系统的文本绘制封装
- `ui.control.*`：按钮、标签、输入框、列表、滑条等控件

## 7. 专项推进原则

### 7.1 先骨架稳定，后补控件数量

先把以下地基做稳：

- 组件树模型
- 输入分发
- 布局协议
- 渲染上下文

其中输入层默认技术路线应为：

- 键盘与文本事件直接消费 `InputEvents.KeyboardListener`
- 鼠标位移、按钮、滚轮直接消费 `org.lwjglx.input.Mouse`
- 先建立 UI 自己的输入适配层，再路由到控件树

然后再恢复旧版控件集。

### 7.2 先最小闭环，后高级效果

先做到：

- 一个测试界面可打开
- 至少可绘制文本、矩形、按钮
- 可以稳定响应鼠标与键盘

再考虑：

- FBO 中转
- 动画
- 复杂阴影
- 高级控件皮肤

### 7.3 先解耦字体，后做文本控件

UI 层只依赖新的字体适配能力，例如：

- `drawString`
- `drawStringWithShadow`
- `getStringWidth`
- 自动换行与裁剪

不要让控件层直接碰字符页、shader、glyph 生成等底层细节。

### 7.4 先规范事件语义，后扩展交互

要先明确：

- hover 目标
- pressed 目标
- drag 捕获目标
- keyboard focus 目标
- wheel 分发目标

否则控件数量越多，行为冲突越难收敛。

### 7.5 先降低状态污染，后谈渲染技巧

UI 渲染必须尽量减少对 MC 原有 OpenGL 状态的污染。

新实现要避免把旧版那种“大范围 push/pop 状态”当作默认方案。

## 8. MC UI 系统专项里程碑

## M1. 建立 UI 包结构与最小入口

目标：在新代码区建立 UI 系统骨架，不急着迁移全部旧控件。

任务：

- 创建 `ui` 相关包结构
- 确定 GUI 基类、组件基类、布局接口、渲染上下文的命名
- 明确 UI 系统与字体系统、配置界面的依赖方向
- 预留测试界面入口

输出物：

- UI 包骨架
- UI 主线职责说明
- 最小入口类

验收标准：

- 新代码区存在可继续扩展的 UI 目录结构
- 后续工作不需要再依赖旧版类直接参与运行

## M2. 重建 `BaseGUI` 对应的新界面基类

目标：把旧版 `BaseGUI` 拆成更清晰的界面基础层。

任务：

- 明确 screen 生命周期
- 抽离根组件管理
- 抽离输入采集与转发，并改为基于 `lwjgl3ify` 输入流
- 抽离背景绘制和可选的 FBO 策略
- 处理分辨率变化与 resize

输出物：

- 新界面基类
- 根节点驱动逻辑
- 分辨率变化处理逻辑

验收标准：

- 可以打开一个空白测试界面
- 根组件能随界面生命周期正常初始化和刷新

## M3. 重建 `Widget` 核心组件树

目标：建立新的组件节点模型，接管旧版 `Widget` 的核心价值，但拆清职责。

任务：

- 定义父子节点关系
- 定义位置、尺寸、可见性、启用状态
- 定义添加/移除子组件协议
- 定义本地坐标与全局坐标换算
- 定义基本命中检测

输出物：

- 新 `Widget` 基类
- 组件树遍历与更新规则
- 节点基础属性模型

验收标准：

- 多层嵌套组件可稳定绘制和命中
- 不再依赖旧版 `Widget` 的静态状态

## M4. 重建事件分发与焦点模型

目标：替换旧版原始递归事件分发，建立清晰的输入协议。

任务：

- 定义鼠标按下、抬起、移动、拖拽、滚轮、键盘输入事件对象
- 定义 `InputEvents` / `Mouse` 到 UI 事件对象的适配层
- 建立 hover、focus、capture 三类目标管理
- 明确事件从 screen 到 widget 的分发顺序
- 明确控件是否消费事件的语义

输出物：

- UI 输入事件模型
- 输入路由器
- 焦点管理器

验收标准：

- 按钮、输入框、拖拽控件之间不会明显串事件
- 拖拽和键盘焦点行为可推理、可复现

## M5. 重建布局系统

目标：把旧版布局器能力整理成统一协议，而不是零散规则堆叠。

任务：

- 设计统一布局接口
- 明确测量阶段与排列阶段
- 首批恢复基础布局：
- `RelativeCoordinateLayout`
- `CenterLayout`
- `HorizontalLayout`
- `VerticalLayout`
- `GridLayout`
- 明确间距、内边距、期望尺寸的语义

输出物：

- 布局接口与基础实现
- 基础容器布局规则

验收标准：

- 常见面板与列表布局可预测
- 布局结果不会因递归顺序不同而明显漂移

## M6. 建立 UI 绘制上下文与基础绘制工具

目标：替换旧版 `drawUtil` 的直接散装调用，建立统一绘制入口。

任务：

- 设计渲染上下文对象
- 提供矩形、描边、圆角矩形、纯色填充、简单裁切能力
- 视需要迁移 `RoundedRectangle`、`Circle` 等旧能力
- 规范 GL 状态保护边界

输出物：

- UI 绘制上下文
- 基础绘制工具集
- 状态保护约定

验收标准：

- 基础图形绘制可稳定复用
- UI 绘制不会明显污染其他渲染流程

## M7. 接入字体系统并建立文本组件基础

目标：让新 UI 系统正式使用新字体系统，而不是旧版文本渲染器。

任务：

- 封装 UI 文本绘制入口
- 接入字符串测量、阴影绘制、自动换行、裁剪
- 建立最小文本组件或标签组件
- 统一 tooltip 与普通文本的字体调用方式

输出物：

- UI 文本渲染封装
- `Label` 或等价文本控件

验收标准：

- 文本控件可直接使用新字体系统
- UI 层不依赖旧版 `ReplaceFontRender`

## M8. 重建第一批基础控件

目标：优先恢复最常用控件集，形成最小可用 UI 工具箱。

任务：

- 重建按钮控件
- 重建文本按钮或带文字按钮
- 重建基础输入框
- 重建滑条或数值输入控件
- 重建列表容器或滚动容器

输出物：

- 第一批基础控件
- 控件交互规范

验收标准：

- 能组合出配置页和调试页常见结构
- 控件间事件与布局配合稳定

## M9. 重建裁切、滚动与层级能力

目标：补齐复杂界面必须具备的容器级能力。

任务：

- 建立裁切区域管理
- 建立滚动偏移与滚轮联动
- 明确子组件溢出显示策略
- 明确前景层、背景层、浮层或 tooltip 层的绘制顺序

输出物：

- 裁切工具
- 滚动容器基础能力
- 分层绘制规则

验收标准：

- 长列表、下拉区域、tooltip 不会互相错误覆盖
- 父容器裁切行为稳定

## M10. 重建调试与测试界面

目标：让 UI 系统具备独立验证能力，而不是边接业务边调结构。

任务：

- 建立 UI showcase / playground 界面
- 建立布局测试页
- 建立输入事件测试页
- 提供必要调试信息：
- hover 目标
- focus 目标
- 组件树层级
- bounds 与裁切区域

输出物：

- UI 调试界面
- 基础手工验证清单

验收标准：

- 不依赖业务场景也能验证 UI 基础能力
- 出问题时能快速定位是布局、事件还是绘制问题

## M11. 接入现有配置界面与实际业务场景

目标：用真实场景验证新 UI 系统，而不是停留在 demo。

任务：

- 评估现有 Forge 配置 GUI 的接入方式
- 选择一个实际业务界面作为试点
- 用新 UI 控件替换局部旧逻辑或新建独立页面

输出物：

- 至少一个真实接入页面
- 接入经验记录

验收标准：

- 新 UI 不只停留在测试页
- 至少一个真实界面能稳定运行

## M12. 视觉增强与可维护性收尾

目标：在主线稳定后，再做动画、主题和代码收口。

任务：

- 评估是否保留旧版 tween 动画思路
- 补充主题、状态色、悬停/按下视觉反馈
- 整理公共样式与公共容器
- 清理不合理 API 与临时兼容层

输出物：

- UI 视觉增强方案
- 公共样式与主题基础

验收标准：

- 常见控件视觉反馈完整
- UI API 已具备可持续扩展性

## 9. 推荐执行顺序

建议按以下顺序推进：

1. M1 建立 UI 包结构与最小入口
2. M2 重建 `BaseGUI` 对应的新界面基类
3. M3 重建 `Widget` 核心组件树
4. M4 重建事件分发与焦点模型
5. M5 重建布局系统
6. M6 建立 UI 绘制上下文与基础绘制工具
7. M7 接入字体系统并建立文本组件基础
8. M8 重建第一批基础控件
9. M9 重建裁切、滚动与层级能力
10. M10 重建调试与测试界面
11. M11 接入现有配置界面与实际业务场景
12. M12 视觉增强与可维护性收尾

## 10. 当前阶段最重要的优先级

如果只看“现在最该先做什么”，优先级应为：

1. 新界面基类与根组件入口
2. 新 `Widget` 组件树模型
3. 事件分发与焦点机制
4. 基础布局系统
5. 文本与基础控件接入

原因：

- 这五项决定 UI 主线是否真正成立
- 如果地基没定型，后续继续搬运控件只会扩大返工面

## 11. 第一批实际开发建议

下一步最合理的第一批落地项应为：

1. 在新代码区创建 `ui` 相关目录骨架。
2. 先实现新的 screen 基类和根组件驱动。
3. 先实现基于 `lwjgl3ify` 的原始输入收集层。
4. 定义新的 `Widget` 基类与父子树模型。
5. 建立最小输入事件模型和命中分发流程。
5. 做一个仅包含文本与按钮的 UI 测试页。

## 12. 结论

旧版 UI 系统已经证明了方向可行，但它更像一个“功能齐全的原型框架”，还不是适合长期维护的新主线结构。

本次专项的核心不是直接搬运旧版全部类，而是：

- 保留组件树路线
- 重建 screen / widget / layout / event / render 边界
- 直接接入新的字体系统
- 先做最小闭环
- 再恢复控件、滚动、裁切、动画和真实界面接入

这样完成后，Qz-UILib 才会真正拥有一个适配 MC 环境、可演进、可调试、可复用的 UI 系统主线。
