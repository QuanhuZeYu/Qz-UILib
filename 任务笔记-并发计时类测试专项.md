# 专项：并发计时类测试（Linux CI flaky）

状态：**已根除（`c17188e6`）** —— 三条固定时长反证全部换成正向栈证据，selection 相位改为按队列
内容判定，合并窗口由 matcher park 稳定张开。**未动任何产品并发语义。** 120 轮全类压测 0 复发
（旧基线：本机 1/60~1/80、CI 2/7）。

追加定案（issue #71 那一轮）：第三成员 `resetWaitsForConcurrentClaimAdmission` 于 2026-09-04 在 CI
又红一次（`23555ff1` attempt 1，Linux），带回确切终态；定性为**判据挂错层**并已归位（见同名小节）。
**同样未动任何产品并发语义。**

## 根除办法（三条，缺一不可）

1. **相位按内容判定，不再数时钟调用次数。** 旧夹具 `BlockingSelectionClock(4)` 押"第 4 次读表就是
   要选走 A 的那一次"。真因：dispatcher 在 `submit` 里也读同一个时钟登记 `enqueuedNanos`
   （`GlyphGenerationDispatcher:196`），**提交路径的读表混进了计数**，所以 3 还是 4 说不清
   （我按纯 selection 次序改成 3 → 确定性失败，正是这个原因）。新闸门三条件同时成立才停车：
   当前线程是字体 worker、队列只剩一个候选、该码点尚未被匹配过 ⇒ 那一刻 A 必在队中且 worker
   必持队列锁。
2. **让合并窗口稳定张开。** 场地 matcher 原本立即返回 -1 ⇒ 需求秒结算为 FAILED ⇒ 之后同码点重新
   claim 完全合法（产品行为正确），"与在飞需求合并"的前提因此时有时无 —— 那 1/80 就是它。现在
   按码点 park 住 A，让它长时间保持 RASTERIZING（active），promoter 必然合并成功。
3. **"在等"用栈证据。** `assertParkedAt(thread, 帧名, 主张)`：既要求目标帧在栈里，也要求线程不处于
   RUNNABLE。三处 `assertFalse(finished.await(100ms))` 全部替换（promotion / reset / pause）。

保留：`promoted >= 1` 的显式前提断言 —— 万一将来节奏变化让前提再次蒸发，它会以"前提未成立"指名失败。

## 统计诚实

旧复现率 ~1/80 时，120 轮零复发有约 22% 纯属运气。所以"根除"的依据是**构造性保证**（上面三条）
加命名断言兜底，而不是"这次没撞到"。

## 已定案：`promotionWaitsForConcurrentQueueSelection` = 夹具相位失效，不是产品竞态

带仪表的本地压测抓到一手样本（`temp/hits/round37.xml`，1/60 命中）：

```
promoted=0 admitted=0 rejected=0 inFlight=0   expected:[X,B,A] actual:[X,B,A,A]
```

`promoted=0` 即结论：promoter **没有**与在飞需求合并 —— WARMUP 的 A 在那之前已被选走并结算为
失败（本场地 matcher 恒返回 -1）。此后同码点重新 claim 是产品的正确行为（失败字形被更高优先级
再要一次），所以多出的那个 A 合法。测试的隐含前提（"阻塞的那次选择就是本来要选走 A 的那一次"）
偶发失效，表现却像调度竞态。

处置：把前提写成显式断言（`promoted>=1`，与同文件 :376 的既有写法同型），失效时以
"fixture 前提未成立"失败。实验记录：**把阻塞相位从 4 改到 3 反而在 Windows 上确定性失败** ——
executor 实际调用节奏里还有一格选择，4 才对；此结论已写进注释，免得后来者凭直觉再改。

**遗留**：这仍是"押相位"的夹具。彻底确定化需要在 `offer` 与 `bestIndex` 之间开一个注入点，
属产品可测性改动 —— 未做，等放行。
> 更新（A/B 两项 LaTeX 收口之后）：**这一族已成为 CI 唯一的红灯来源。** 6 次执行里有 **4 次**
> （67%）被并发计时类测试打中，且牵涉 **3 个不同测试** —— 同一个根型反复出现，不是偶发抖动。

## 观测（6 次 CI 执行样本）

| 测试 | 出现于 | 断言 |
|---|---|---|
| `FontGenerationCandidateSchedulerTest.shutdownRejectsReplacementUntilRetiringExecutorTerminates` | 2/6 | `:144 shutdownNow 必须中断 retiring worker` |
| `GlyphGenerationDispatcherReloadBarrierTest.resetWaitsForConcurrentClaimAdmission` | 1/6（同 SHA 一红一绿） | `:230` 裸 `assertFalse`，无消息 |
| `GlyphGenerationDispatcherReloadBarrierTest.promotionWaitsForConcurrentQueueSelection` | 1/6 | `:411` 同款 `assertFalse(...await(100ms))` |

只在 Linux runner 上出现，同一次 push 的重跑结果不同 → 非确定性。本地 Windows 全绿。
第 3 条与第 2 条同文件同型：都靠"100ms 内没完成"证明"在等"，见下面待查一节的共同修法。

## 确诊：`FontGenerationCandidateSchedulerTest:144`（测试判据自相矛盾）

被测 worker 的循环（`:117-127`）：

```java
entered.countDown();
while (release.getCount() > 0L) {
    try { release.await(5L, TimeUnit.SECONDS); }
    catch (InterruptedException expected) { interruptionObserved.set(true); }
}
throw new IllegalStateException("retiring finished");
```

测试随后（`:142-144`）：

```java
release.countDown();
awaitQuiescence(scheduler);   // 内部触发 shutdownNow → interrupt
Assert.assertTrue("shutdownNow 必须中断 retiring worker", interruptionObserved.get());
```

`release` 这一个闩**同时承担两件事**：既是"放 worker 走"，又是"worker 的退出条件"。于是
worker 有两条合法出路：

- interrupt 先到 → `await` 抛 `InterruptedException` → `interruptionObserved = true` → 断言过；
- `countDown` 先到 → `await` 正常返回 → 循环条件已假 → **体面退出，从未被中断** → 断言红。

两条路都由调度决定，测试却要求只有第一条合法 —— 这就是 1/4 的复现率来源。**不是产品缺陷**
（除非另有证据说 shutdownNow 必须在已 release 的 worker 上补一刀）。
## 已定案：`resetWaitsForConcurrentClaimAdmission` = 判据挂错层（不是产品竞态）

一手现场（CI `23555ff1` attempt 1，Linux）：

    java.lang.AssertionError: 'F' 终态不应仍为 active：stateF=UPLOAD_QUEUED inFlight=0 admitted=0

上一轮给这条断言补的消息起了决定作用：一眼给出 `stateF / inFlight / admitted` 三项，直接排除了
"reset 没等到线性化"这条产品嫌疑。真正的根因是**把别的对象的性质挂在本方法的终态上**：

- `GlyphGenerationDispatcher.reset()`（:264-292）只做 pause + `generationEpoch`++ + `cancelInFlightTasks()`
  + `shutdownNow()` + `awaitTermination(2s)`，**全程不碰字符页**。字形生命周期（state / demands / requestId）
  归 `GlyphPageManager.reset()`（:230-255）与 `setGeneration()`，由 `FontService.commitReloadLocked` 另外调。
- 于是旧断言在两个**都合法**的终态之间抛硬币：worker 赶在 epoch 自增与 `shutdownNow` 之前跑完
  `queueUpload` ⇒ `UPLOAD_QUEUED`（红）；worker 被闸住 ⇒ `CANCELLED_STALE`（绿）。
- `assertParkedAt(resetThread, "reset")` 是同一条错误的共犯：`submit` 持着 dispatcher 监视器、
  `claimRequest` 又持着 pageManager 监视器 park，reset 必然停在锁上 —— 那是**夹具自己造成的恒真**。
  上一轮我把它从"时长反证"换成正向栈证据，方向没错（诚实多了），但它证明的是"在等这把锁"，
  不是用例名字声称的"等到了 admission 线性化"。这笔账记在我自己头上。

### 硬币两支的实测分布

本机（Windows，全空闲）跑 `resetWaits...` 15 次，每次强制打印 `'F'` 终态：

    15/15 terminal=CANCELLED_STALE inFlight=0 admitted=0

CI（满载 ubuntu runner）同一条断言的现场是 `UPLOAD_QUEUED`。⇒ 本地永远只看到绿的那一支，
所以它 1/5 的复现率长期被误判成"Linux 慢"；实际是两个合法终态轮流出场，旧判据把其中一个当缺陷。

### 归位后本文件锁的三件事

1. `resetWaitsForConcurrentClaimAdmission` 只锁 dispatcher 自己交付的性质：**reset 返回后不得再有 glyph
   状态推进**（`observeStateDrift`，窗口 250ms）。今天这条由 `awaitTermination` 构造保证；留着是给
   "把上传移出池线程"一类演进兜网 —— 那类改动会把它变成真可判的性质。
2. 新增 `completedClaimSettlesBeforeResetWithoutAnyRace`：**无竞争的确定性基线**。一次跑完的 claim 合法
   停在 `UPLOAD_QUEUED` 且 reset 返回后无漂移。旧判据在这条路径上必红，而这条路径产品完全正确 ——
   这就是"判据测错了层"的可执行证明，不靠嘴说。
3. 新增 `stateDriftObserverDetectsAWriteAfterBaseline`：量具自检。没有它，两处 `assertNull(drift)` 只是
   "没人写"的证据，不是"写了我就看得见"的证据。

换代让迟到 token 失效这条**没有在本文件重复实现**：`GlyphRuntimeVersionIsolationTest` 的
`oldGenerationTokenCannotMutateTransferredStorage`（:65-78，`setRuntimeVersion(2)` ⇒ 旧 token 的
`queueUpload` 返回 false）与 `lazyLifecycleResetGatesStaleGeometryAndInvalidatesOldTokens`（:704-738）
已经锁得更细。原计划新增的"复刻 commitReloadLocked 的版本推进"因此取消 —— 写它就是重复权威。

产品侧一条事实，防止下一个人在这里"顺手修"：`GlyphRuntimeTables.resetGlyphLifecycle()`（:317-335）
**不清** `requestIdArray`（2 个 long[] 共 17.8 MiB；P0-B 特意把每次 reload 的 fill 从 123 MiB 压到
29 MiB）⇒ requestId 不是 reset 的失效机制，epoch / state / generation 才是。

### 控制实验

- M7：`observeStateDrift` 恒返回 null ⇒ 量具自检出红（证明它真挂在仪表上）。
- M8：`observeStateDrift` 恒报漂移 ⇒ 3 个用例红（量具自检 + 两处消费者），并顺手留下本地基线
  `基线=CANCELLED_STALE`。
- M6 第一版是**坏实验**：变异后编译不过，15 次全部 NO-XML，什么都没测到 —— 不得记成"旧判据本地
  0 次红"。改成直接测终态分布后才拿到上面那行数据。

## 下一步（建议顺序）

1. 同 SHA 重跑若干次，确认归位后的三条用例在 Linux 上不再抖（本文件唯一的 CI 观察样本已用完）。
2. 本节全部关闭。再出现红，先读新消息里的 `基线=` 与 `漂移=` 字段 —— 它们分别指认"终态合法但不同"
   与"reset 返回后仍在推进"两类不同问题，不要再靠猜。

## 边界

- 与 LaTeX 那两项**无关**（那两项已按确定性缺陷/判据提精分别处理，见 `踩坑记录.md`）。
- 不改产品并发语义，除非第 2 步的样本证明 `reset()` 确实没有等 admission。
