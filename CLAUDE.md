<!--
本文件内容平移自 AGENTS.md，二者应保持一致。
协作规范以 AGENTS.md 为权威来源；如需修改规范，请先修改 AGENTS.md（须经用户确认），再同步回本文件。
-->

# 协作规范

回答内容优先使用中文

## 0. 架构宪章（North Star）
- 根目录 `NORTH_STAR.md` 是本项目 UI 系统的中心思想宪章，是所有架构决策、模块设计、性能优化和 API 取舍的最高准绳
- 做任何架构性改动前，必须先对照 `NORTH_STAR.md` 的《核心信条》《关键不变量》和《决策检查清单》，确认方案没有踩《反模式》
- 代码与宪章冲突时，遵循「先改文件再改代码」：要么调整方案对齐宪章，要么按宪章《修订纪律》在《偏离登记》显式登记一次偏离并说明理由、影响范围和回填计划；隐性偏离（不登记就绕过）不可接受
- 代码评审时，用《关键不变量》（I1-I9）逐条核对，任意一条被破坏都应阻断合并
- 信条（第 3 节）和不变量（第 5 节）本身的改动属于重大架构变更，必须经用户确认

## 1. Git提交规范
- 每次修改必须提交git，保持工作区整洁
- 提交信息应清晰描述更改内容
- 提交标题格式统一为：`[English]: 中文标题`
- 提交标题常用动作建议：`Add`、`Fix`、`Refactor`、`Docs`、`Style`、`Test`、`Chore`
- 提交正文必须与标题换行分隔，并使用 Markdown 记录详细修改内容
- 提交正文必须使用中文，优先说明修改范围、核心变更点、必要的兼容性或影响说明

## 2. 分支管理规范
- 每次改动前先确认当前分支，改动不要在 `4.0` / `master` 等主分支上直接进行
- 应在合适的命名分支中开发，命名格式建议：`<动作名称>/<任务简述或功能名>`（动作名称例如 `fix`、`add`、`refactor` 等）
- 改动结束后，确认测试通过、工作区干净，再将分支合并回主分支
- 合并时必须使用 `git merge --no-ff`，保留显式合并提交，禁止 Fast-forward 直接移动主分支指针

## 3. AGENTS.md修改规范
- AGENTS.md的修改必须经过用户确认
- 不可随意更改本文件内容

## 4. AI记忆文档协作规范
- 开始开发前，必须先阅读 `docs/AI记忆文档.md`
- `docs/AI记忆文档.md` 只作为 AI 协作文档总导航，不再承担阶段流水账、细节清单和大段项目事实汇总
- 开发过程中如涉及目录结构、主入口、脚本命令、运行方式、能力边界变化，必须同步更新 `docs/记忆/长期事实/` 对应文档
- 当阶段目标、当前状态、阻塞项、下一步执行项发生变化时，必须同步更新 `docs/记忆/当前态/` 对应文档
- 关键方案取舍、重要实现约束、接口开放边界变化，必须写入 `docs/记忆/决策/` 或 `docs/开发者文档/开放化调整.md`
- AI 记忆体系只记录对后续协作仍有持续价值的事实，临时调试记录和一次性试验结论不单独沉淀
- 发现某条信息已能稳定归属到 `docs/使用文档/`、`docs/记忆/长期事实/`、`docs/记忆/决策/`、`docs/开发者文档/reviews/`、`docs/开发者文档/errors/`、`docs/开发者文档/开放化调整.md` 时，应写回原文档，不再复制到总导航
- 更新记忆文档时优先做删减、归并和覆盖，不按日期持续追加；除非属于长期稳定边界，否则不要往总导航堆积内容

## 4.1 记忆分层与写回规则
- 规则层：`AGENTS.md`，只记录协作规范、编码规范、Git 规范和文档写回规则
- 当前态层：`docs/记忆/当前态/`，记录当前阶段目标、当前上下文、下一步计划和交接信息，可频繁覆盖更新
- 长期事实层：`docs/记忆/长期事实/`，记录目录结构、架构边界、稳定命令、项目约定等中长期有效事实
- 决策层：`docs/记忆/决策/`，记录关键技术取舍、边界约束和重要设计决定
- 专项沉淀层：`docs/开发者文档/errors/`、`docs/开发者文档/reviews/`，分别记录错误教训和审查结论
- 完成独立任务后，至少检查一次是否需要更新 `当前态/`、`长期事实/`、`决策/`、`errors/`、`reviews/` 中的任一文件
- 会话结束或中途交接时，必须更新 `docs/记忆/当前态/交接记录.md`

## 5. 代码规范
### 命名规范
- 类名：使用英文大驼峰命名法
- 方法名：使用英文小驼峰命名法
- 变量名：使用英文小驼峰命名法
- 常量名：使用英文全大写，下划线分隔

### 注释规范
- 类与方法必须添加注释
- 使用中文注释，提高可读性
- 重要方法必须添加Javadoc注释，说明功能、参数和返回值
- 字段、成员变量、临时变量等位置按实际可读性需要选择性添加注释，不强制要求逐一注释
- 复杂逻辑添加行内注释，解释实现思路
- 注释应简洁明了，避免冗余

### 代码格式
- 保持现有缩进风格（4个空格）
- 大括号风格：左大括号不换行，右大括号独占一行
- 运算符两侧添加空格
- 逗号、分号后添加空格

### 其他规范
- 异常处理：捕获具体异常，避免捕获通用异常
- 日志记录：选择合适的日志库来记录
- 代码复用：提取重复代码为独立方法
- 注释维护：修改代码时同步更新相关注释
- 文件规模：单个代码文件接近或超过 1000 行时必须评估职责拆分；优先按真实职责、变更频率和复用边界拆分，不按行数机械拆
- 新增大型功能时避免继续向已有千行文件堆叠逻辑，应优先拆出 registry、builder、checker、state 等协作者

## 6. 错误记录规范
- Agent在开发协作过程中发现的错误、反复出现的问题、曾经做错的事情、重要教训必须及时记录，避免同一错误重复发生
- 错误记录统一通过索引文件 `docs/开发者文档/errors/README.md` 管理
- 该文件作为"指针文档"：使用二级标题（## [日期或编号]-[错误简述]）作为指针，每条记录指向或链接到更具体的错误分析文档（存放于 `docs/开发者文档/errors/` 目录下，例如 `docs/开发者文档/errors/ERROR-20260418-git-commit.md`）
- 每条错误记录应至少包含：错误现象、触发场景、根本原因、修复方案、预防措施
- 目的是帮助后续Agent快速查阅历史问题，快速排错，避免同样错误反复找原因
